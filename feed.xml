<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://hyn128.site/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hyn128.site/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-09-25T17:51:23+09:00</updated><id>https://hyn128.site/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">Ubuntu에서 계정별 명령 로그를 Slack WebHook으로 기록하기</title><link href="https://hyn128.site/project-1-orbit-command-logging/" rel="alternate" type="text/html" title="Ubuntu에서 계정별 명령 로그를 Slack WebHook으로 기록하기" /><published>2026-09-24T00:00:00+09:00</published><updated>2026-09-24T00:00:00+09:00</updated><id>https://hyn128.site/project-1-orbit-command-logging</id><content type="html" xml:base="https://hyn128.site/project-1-orbit-command-logging/"><![CDATA[<h2 id="요약">요약</h2>
<blockquote>
  <p>여러 사용자가 함께 사용하는 Production·Development Ubuntu 서버의 작업 이력을 추적하기 위해 <code class="language-plaintext highlighter-rouge">auditd</code>와 Slack Webhook을 연동한 명령 로깅 시스템을 구축했습니다. 일반 사용자의 프로그램 실행과 <code class="language-plaintext highlighter-rouge">sudo</code> 사용 내역을 수집해 서버별, 사용자와 권한별 Slack 채널로 전송하고, Webhook 보호와 <code class="language-plaintext highlighter-rouge">systemd</code>를 이용한 상시 실행 과정까지 정리합니다.</p>
</blockquote>

<hr />
<h2 id="1-아이디어">1. 아이디어</h2>

<blockquote>
  <p>교육장에서 받은 서버와 홈서버를 각각 Production, Development 서버로 두기로 했습니다.
이 과정에서 팀원은 모두 개인의 계정을 갖고, <code class="language-plaintext highlighter-rouge">sudo</code> 를 포함한 명령을 사용하게 되는데 추후 트러블 슈팅 또는 본인의 작업 내역을 확인하기 위한 기록이 필요하다고 느껴 로깅 시스템 구축의 필요성을 느꼈습니다.</p>
</blockquote>

<p>먼저 저희가 협업 과정에서 활용할 구조는 다음과 같습니다.</p>
<ol>
  <li>Slack - 팀원별 소통 및 웹훅 애플리케이션 관리</li>
  <li>Jira/Github - 프로젝트 및 이슈 관리</li>
  <li>DevServer - Ubuntu Server에서 가용 최대 리소스로 개발 및 테스트를 위한 서버로 활용
    <ul>
      <li>libvirt와 KVM으로 Orbit 호스트 VM 생성해 사용</li>
      <li>VM 내에서는 각 사용자별 디렉토리 내에서 작업 및 테스트</li>
    </ul>
  </li>
</ol>

<p>이 구조에서 각 팀원들의 명령이 <code class="language-plaintext highlighter-rouge">sudo</code>로 사용될 수 있어야하고, 다른 사용자 환경에도 영향을 미칠 수 있다고 생각했습니다.</p>

<p>또한 개발 및 테스트 과정에서 문제를 해결한 과정 또는 공유할 내용을 그때마다 기록하기에는 어려울 수 있으므로, 대신 명령 시퀀스를 타임라인으로 볼 수 있는 기록이 필요하다고 생각했습니다.</p>

<h2 id="2-slack에-log-수집-채널-및-log-애플리케이션-생성">2. Slack에 Log 수집 채널 및 Log 애플리케이션 생성</h2>

<p>먼저 Slack에 각 서버별, 명령 권한(사용자) 별 로그 수집 채널을 생성해주었습니다.</p>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/25.webp" alt="" /></p>

<p>이후 <a href="https://api.slack.com/apps">Slack API</a> 에서 Application을 생성하고 아래와 같이 진행했습니다.</p>
<ol>
  <li>Create New App → Blank App → Continue</li>
  <li>App Name: Orbit Log</li>
  <li>사용할 워크스페이스 선택</li>
  <li>Create APP</li>
</ol>

<h2 id="3-webhook-생성">3. Webhook 생성</h2>

<p>앱이 생성되고 앱 설정 화면으로 넘어가면 왼쪽 메뉴에서 <code class="language-plaintext highlighter-rouge">Features → Incomming Webhooks</code>로 진입합니다.</p>

<p>이후 같은 페이지에서 <code class="language-plaintext highlighter-rouge">Add New Webhook to Workspace</code>를 선택해 훅을 수신할 채널을 전부 생성해줍니다.</p>

<p>저희 환경에서는 최종적으로 아래와 같은 구조가 구성됐습니다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Orbit Log (Slack App)
│
├─ webhook → #prod-orbit
├─ webhook → #prod-sudo
├─ webhook → #prod-hyn128
├─ webhook → #prod-jyvnee
├─ webhook → #prod-sungahbak
├─ webhook → #prod-xoruddl
│
├─ webhook → #dev-orbit
├─ webhook → #dev-sudo
├─ webhook → #dev-hyn128
├─ webhook → #dev-jyvnee
├─ webhook → #dev-sungahbak
├─ webhook → #dev-xoruddl
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/07.webp" alt="" /></p>

<blockquote>
  <p>생성된 Webhook은 공개 될 경우 URL 만으로도 아주 쉽게 해당 채널로 데이터를 보낼 수 있으므로 공개에 주의합니다.</p>
</blockquote>

<p>생성된 Webhook은 <code class="language-plaintext highlighter-rouge">curl</code>을 이용해 아래처럼 테스트 해볼 수 있습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> POST <span class="se">\</span>
  <span class="nt">-H</span> <span class="s1">'Content-type: application/json'</span> <span class="se">\</span>
  <span class="nt">--data</span> <span class="s1">'{"text":"[DEV] Ubuntu sudo log test"}'</span> <span class="se">\</span>
  <span class="s1">'여기에_WEBHOOK_URL'</span>
</code></pre></div></div>

<h2 id="4-ubuntu에-webhook-설정-파일-구축-및-패키지-설치">4. Ubuntu에 Webhook 설정 파일 구축 및 패키지 설치</h2>

<p>Production 서버와 Development 서버 모두 과정은 비슷하므로, Dev 서버를 기준으로 작성합니다.</p>

<p>개발 서버 기준으로 다시 정리하면 목표는 아래와 같습니다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>orbit 명령      → #dev-1-orbit (관리용 계정)
sudo 전체       → #dev-2-sudo
hyn128 명령     → #dev-hyn128
jyvnee 명령     → #dev-jyvnee
sungahbak 명령  → #dev-sungahbak
xoruddl 명령    → #dev-xoruddl
</code></pre></div></div>

<h3 id="1-디렉토리-구성">1) 디렉토리 구성</h3>

<p>먼저 스크립트를 담을 디렉토리를 <code class="language-plaintext highlighter-rouge">-p</code> 옵션으로 일괄 생성하고, 해당 디렉토리로 이동합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> ~/slack-log/scripts
<span class="nb">cd</span> ~/slack-log
</code></pre></div></div>

<p>사용하게 될 구조는 다음과 같습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/home/orbit/slack-log/       <span class="c"># Slack 명령 모니터링 관련 파일 관리</span>
├── webhook.env              <span class="c"># 사용자별 Slack Webhook URL 및 환경변수 저장</span>
└── scripts/
    └── command-monitor.sh   <span class="c"># Audit 로그 감시, 사용자별 명령 분기 및 Slack 전송</span>
</code></pre></div></div>

<blockquote>
  <p><strong>root가 아닌 사용자 디렉토리에 구성하는 이유</strong></p>

  <p>/root에 스크립트를 두면 수정과 실행 시마다 root 권한이 필요하고, 일반 사용자의 접근도 제한됩니다. 따라서 현재 협업 환경에서는 root 권한이 반드시 필요한 파일이 아니었고, 사용자의 홈 디렉토리에서 관리하여 필요한 작업에만 <code class="language-plaintext highlighter-rouge">sudo</code>를 사용하는 방식이 권한 관리와 유지보수 측면에서 적절하다고 판단했습니다.</p>
</blockquote>

<h3 id="2-webhook-설정">2) Webhook 설정</h3>

<p>각 채널별 웹훅을 담을 환경변수 파일을 먼저 생성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano webhook.env
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/11.webp" alt="" /></p>

<p>이후 다음처럼 권한을 설정합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod </span>600 ~/slack-log/webhook.env
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">webhook.env</code>는 <code class="language-plaintext highlighter-rouge">Slack Webhook URL</code> 같은 비밀 값이 들어가므로 <code class="language-plaintext highlighter-rouge">600</code>으로 설정했습니다.</p>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/13.webp" alt="" /></p>

<p><code class="language-plaintext highlighter-rouge">600</code>의 상세 권한은 아래와 같습니다.</p>
<ul>
  <li>소유자(owner) : 6 = 읽기(r) + 쓰기(w)</li>
  <li>그룹(group)   : 0 = 권한 없음</li>
  <li>기타(others) : 0 = 권한 없음</li>
</ul>

<blockquote>
  <p>orbit       → 읽기/수정 가능
다른 사용자 → 읽기 불가능</p>
</blockquote>

<h3 id="3-auditd-설치">3) auditd 설치</h3>

<p>다음으로 사용자별 명령 실행 내역을 수집하기 Linux의 감사 패키지인 <code class="language-plaintext highlighter-rouge">auditd</code>를 설치합니다.</p>

<p>먼저 패키지 목록을 갱신한 후 필요한 패키지를 설치합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> auditd audispd-plugins curl
</code></pre></div></div>

<p>각 패키지의 역할은 다음과 같습니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">auditd</code> : Linux Audit 데몬으로, 명령 실행 등의 시스템 이벤트를 감사 로그로 기록</li>
  <li><code class="language-plaintext highlighter-rouge">audispd-plugins</code> : Audit 이벤트를 다른 프로그램이나 외부 시스템과 연동하기 위한 플러그인 제공</li>
  <li><code class="language-plaintext highlighter-rouge">curl</code> : 수집한 로그를 Slack Webhook으로 전송하기 위한 HTTP 요청 도구</li>
</ul>

<p>설치가 완료되면 <code class="language-plaintext highlighter-rouge">auditd</code>를 활성화하고 바로 실행합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> auditd
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">enable</code>은 서버가 재부팅된 이후에도 <code class="language-plaintext highlighter-rouge">auditd</code>가 자동으로 실행되도록 설정하며, <code class="language-plaintext highlighter-rouge">--now</code>는 서비스 활성화와 동시에 현재 세션에서도 바로 실행하도록 합니다.</p>

<p>마지막으로 정상적으로 실행되고 있는지 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status auditd
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/15.webp" alt="" />
사진과 같이 <code class="language-plaintext highlighter-rouge">active (running)</code> 로 정상적으로 동작하고 있는 것을 확인할 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Active: active (running)
</code></pre></div></div>

<blockquote>
  <p><strong>Shell History 대신 auditd를 사용하는 이유</strong></p>

  <p><code class="language-plaintext highlighter-rouge">history</code>는 각 사용자의 Shell 환경에 종속되며 사용자가 직접 기록을 수정하거나 삭제할 수 있습니다. 반면 <code class="language-plaintext highlighter-rouge">auditd</code>는 Linux Audit 시스템을 통해 시스템 이벤트를 수집하므로, 여러 사용자가 함께 사용하는 서버에서 명령 실행 내역을 감사하기 위한 용도로 더 적합하다고 판단했습니다.</p>
</blockquote>

<h2 id="5-sudo-명령-audit-규칙-설정">5. sudo 명령 Audit 규칙 설정</h2>

<p><code class="language-plaintext highlighter-rouge">auditd</code> 설치가 완료되었으므로 이제 <code class="language-plaintext highlighter-rouge">sudo</code> 명령 실행을 감지할 수 있도록 Audit 규칙을 설정합니다.</p>

<h3 id="1-audit-규칙-파일-생성"><strong>1) Audit 규칙 파일 생성</strong></h3>

<p><code class="language-plaintext highlighter-rouge">/etc/audit/rules.d/</code> 디렉토리에 사용자 명령 실행을 감시하기 위한 Audit 규칙 파일을 생성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/audit/rules.d/command-monitor.rules
</code></pre></div></div>

<p>파일에 다음 내용을 추가합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">-a</span> always,exit <span class="nt">-F</span> <span class="nb">arch</span><span class="o">=</span>b64 <span class="nt">-S</span> execve <span class="nt">-F</span> auid&gt;<span class="o">=</span>1000 <span class="nt">-F</span> auid!<span class="o">=</span><span class="nb">unset</span> <span class="nt">-k</span> user_command
<span class="nt">-a</span> always,exit <span class="nt">-F</span> <span class="nb">arch</span><span class="o">=</span>b32 <span class="nt">-S</span> execve <span class="nt">-F</span> auid&gt;<span class="o">=</span>1000 <span class="nt">-F</span> auid!<span class="o">=</span><span class="nb">unset</span> <span class="nt">-k</span> user_command
</code></pre></div></div>

<p>각 옵션의 의미는 다음과 같습니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">-a always,exit</code> : 시스템 콜이 종료될 때 항상 Audit 이벤트를 기록</li>
  <li><code class="language-plaintext highlighter-rouge">-F arch=b64</code>, <code class="language-plaintext highlighter-rouge">-F arch=b32</code> : 각각 64비트와 32비트 시스템 콜을 감시</li>
  <li><code class="language-plaintext highlighter-rouge">-S execve</code> : 프로그램 실행에 사용되는 <code class="language-plaintext highlighter-rouge">execve</code> 시스템 콜을 감시</li>
  <li><code class="language-plaintext highlighter-rouge">-F auid&gt;=1000</code> : 일반 사용자 계정에서 발생한 이벤트만 대상으로 지정</li>
  <li><code class="language-plaintext highlighter-rouge">-F auid!=unset</code> : 로그인 사용자를 식별할 수 없는 이벤트를 제외</li>
  <li><code class="language-plaintext highlighter-rouge">-k user_command</code> : 기록된 이벤트에 <code class="language-plaintext highlighter-rouge">user_command</code>라는 검색용 키를 부여</li>
</ul>

<p>즉, 일반 사용자가 실행한 프로그램의 <code class="language-plaintext highlighter-rouge">execve</code> 이벤트를 Audit 로그에 기록하고, 이후 <code class="language-plaintext highlighter-rouge">user_command</code> 키를 이용해 해당 로그를 조회할 수 있도록 설정합니다.
<img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/31.webp" alt="" /></p>

<p><code class="language-plaintext highlighter-rouge">/etc/audit/rules.d/</code>는 <code class="language-plaintext highlighter-rouge">auditd</code>의 영구 Audit 규칙을 관리하는 디렉토리입니다. 이곳에 규칙 파일을 작성하면 재부팅 이후에도 규칙을 다시 적용할 수 있습니다.</p>

<h3 id="2-audit-규칙-적용"><strong>2) Audit 규칙 적용</strong></h3>

<p>작성한 규칙을 현재 시스템에 적용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>augenrules <span class="nt">--load</span>
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/19.webp" alt="" />
<code class="language-plaintext highlighter-rouge">augenrules</code>는 <code class="language-plaintext highlighter-rouge">/etc/audit/rules.d/</code>에 작성된 규칙들을 읽어 Audit 시스템에 적용합니다.</p>

<p>적용된 규칙은 다음 명령으로 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>auditctl <span class="nt">-l</span> | <span class="nb">grep </span>user_command
</code></pre></div></div>

<p>앞에서 작성한 <code class="language-plaintext highlighter-rouge">user_command</code> 규칙이 출력된다면 정상적으로 적용된 것입니다.</p>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/33.webp" alt="" /></p>

<h3 id="3-sudo-실행-및-audit-로그-확인"><strong>3) sudo 실행 및 Audit 로그 확인</strong></h3>

<p>실제로 <code class="language-plaintext highlighter-rouge">sudo</code> 명령을 실행하여 Audit 이벤트가 기록되는지 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo whoami</span>
</code></pre></div></div>

<p>정상적으로 실행되면 다음과 같이 출력됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root
</code></pre></div></div>

<p>이후 <code class="language-plaintext highlighter-rouge">sudo_command</code> 키를 기준으로 최근 Audit 로그를 조회합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>ausearch <span class="nt">-k</span> user_command <span class="nt">-i</span> <span class="nt">--start</span> recent
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/23.webp" alt="" /></p>

<p>여기서 <code class="language-plaintext highlighter-rouge">-k user_command</code>는 앞에서 설정한 키를 가진 이벤트만 조회하고, <code class="language-plaintext highlighter-rouge">-i</code>는 UID 등의 값을 사람이 읽기 쉬운 형태로 변환하여 출력합니다.</p>

<p>현재 단계에서는 Audit 로그가 정상적으로 수집되는지 확인하는 것이 목적입니다. 이후 수집된 원본 로그에서 필요한 정보를 추출하여 Slack으로 전송하도록 구성합니다.</p>

<p>최종적으로 Slack에 전달할 정보는 다음 5개로 정했습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User    : 명령을 실행한 사용자
Time    : 명령 실행 시간
Command : 실행한 명령
PWD     : 명령을 실행한 디렉토리
Run as  : sudo를 통해 명령을 실행한 대상 사용자
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">TTY</code>, PID, UID/GID 등의 정보도 Audit 로그에 포함될 수 있지만, Slack에서는 명령 실행 내역과 트러블 슈팅에 필요한 정보만 추출하여 전달하도록 구성합니다.</p>

<h2 id="6-사용자별-명령-slack-전송-스크립트-작성">6. 사용자별 명령 Slack 전송 스크립트 작성</h2>

<p>앞에서 설정한 <code class="language-plaintext highlighter-rouge">user_command</code> Audit 로그를 실시간으로 감시하고, 실행한 사용자에 따라 각 Slack 채널로 로그를 전송하도록 스크립트를 작성합니다.</p>

<p>로그는 다음과 같이 분기하도록 구성했습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>일반 명령
└─ 해당 사용자 채널

sudo 명령
├─ 해당 사용자 채널
└─ sudo 전용 채널
</code></pre></div></div>

<p>또한 Slack에서 로그를 쉽게 구분할 수 있도록 일반 명령과 <code class="language-plaintext highlighter-rouge">sudo</code> 명령의 Attachment 색상을 다르게 설정하고, 주요 항목은 굵게 표시하도록 구성했습니다.</p>

<p>스크립트 파일을 생성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano ~/slack-log/scripts/command-monitor.sh
</code></pre></div></div>

<p>다음 내용을 작성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nb">set</span> <span class="nt">-u</span>

<span class="nb">source</span> /home/orbit/slack-log/webhook.env

<span class="nv">AUDIT_LOG</span><span class="o">=</span><span class="s2">"/var/log/audit/audit.log"</span>

send_slack<span class="o">()</span> <span class="o">{</span>
    <span class="nb">local </span><span class="nv">webhook</span><span class="o">=</span><span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
    <span class="nb">local type</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">user</span><span class="o">=</span><span class="s2">"</span><span class="nv">$3</span><span class="s2">"</span>
    <span class="nb">local time</span><span class="o">=</span><span class="s2">"</span><span class="nv">$4</span><span class="s2">"</span>
    <span class="nb">local command</span><span class="o">=</span><span class="s2">"</span><span class="nv">$5</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">pwd_value</span><span class="o">=</span><span class="s2">"</span><span class="nv">$6</span><span class="s2">"</span>
    <span class="nb">local </span><span class="nv">run_as</span><span class="o">=</span><span class="s2">"</span><span class="nv">$7</span><span class="s2">"</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$webhook</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">return

    </span><span class="nb">local </span>payload

    <span class="nv">payload</span><span class="o">=</span><span class="si">$(</span>python3 - <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$type</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$user</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$time</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$command</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$pwd_value</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$run_as</span><span class="s2">"</span> <span class="o">&lt;&lt;</span><span class="sh">'</span><span class="no">PY</span><span class="sh">'
import json
import sys

type_, user, time, command, pwd, run_as = sys.argv[1:]

if type_ == "SUDO":
    color = "#E01E5A"
    title = "SUDO Command"
else:
    color = "#2EB67D"
    title = "Command"

payload = {
    "attachments": [
        {
            "color": color,
            "mrkdwn_in": ["text"],
            "text": (
                f"*{title}*</span><span class="se">\n\n</span><span class="sh">"
                f"*User*</span><span class="se">\n</span><span class="sh">`{user}`</span><span class="se">\n\n</span><span class="sh">"
                f"*Run as*</span><span class="se">\n</span><span class="sh">`{run_as}`</span><span class="se">\n\n</span><span class="sh">"
                f"*Time*</span><span class="se">\n</span><span class="sh">{time}</span><span class="se">\n\n</span><span class="sh">"
                f"*PWD*</span><span class="se">\n</span><span class="sh">`{pwd}`</span><span class="se">\n\n</span><span class="sh">"
                f"*Command*</span><span class="se">\n</span><span class="sh">```{command}```"
            )
        }
    ]
}

print(json.dumps(payload))
</span><span class="no">PY
</span><span class="si">)</span>

    curl <span class="nt">-fsS</span> <span class="se">\</span>
        <span class="nt">-H</span> <span class="s1">'Content-Type: application/json'</span> <span class="se">\</span>
        <span class="nt">--data</span> <span class="s2">"</span><span class="nv">$payload</span><span class="s2">"</span> <span class="se">\</span>
        <span class="s2">"</span><span class="nv">$webhook</span><span class="s2">"</span> <span class="se">\</span>
        <span class="o">&gt;</span>/dev/null 2&gt;&amp;1
<span class="o">}</span>

get_user_webhook<span class="o">()</span> <span class="o">{</span>
    <span class="k">case</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="k">in
        </span>orbit<span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$DEV_ORBIT_WEBHOOK</span><span class="s2">"</span>
            <span class="p">;;</span>
        hyn128<span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$DEV_HYN128_WEBHOOK</span><span class="s2">"</span>
            <span class="p">;;</span>
        jyvnee<span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$DEV_JYVNEE_WEBHOOK</span><span class="s2">"</span>
            <span class="p">;;</span>
        sungahbak<span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$DEV_SUNGAHBAK_WEBHOOK</span><span class="s2">"</span>
            <span class="p">;;</span>
        xoruddl<span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">"</span><span class="nv">$DEV_XORUDDL_WEBHOOK</span><span class="s2">"</span>
            <span class="p">;;</span>
        <span class="k">*</span><span class="p">)</span>
            <span class="nb">echo</span> <span class="s2">""</span>
            <span class="p">;;</span>
    <span class="k">esac</span>
<span class="o">}</span>

<span class="nb">tail</span> <span class="nt">-Fn0</span> <span class="s2">"</span><span class="nv">$AUDIT_LOG</span><span class="s2">"</span> | <span class="k">while </span><span class="nv">IFS</span><span class="o">=</span> <span class="nb">read</span> <span class="nt">-r</span> line<span class="p">;</span> <span class="k">do</span>

    <span class="c"># user_command SYSCALL 이벤트만 처리</span>
    <span class="o">[[</span> <span class="s2">"</span><span class="nv">$line</span><span class="s2">"</span> <span class="o">!=</span> <span class="nb">type</span><span class="o">=</span>SYSCALL<span class="k">*</span> <span class="o">]]</span> <span class="o">&amp;&amp;</span> <span class="k">continue</span>
    <span class="o">[[</span> <span class="s2">"</span><span class="nv">$line</span><span class="s2">"</span> <span class="o">!=</span> <span class="k">*</span><span class="s1">'key="user_command"'</span><span class="k">*</span> <span class="o">]]</span> <span class="o">&amp;&amp;</span> <span class="k">continue</span>

    <span class="c"># Audit Event ID 추출</span>
    <span class="nv">EVENT_ID</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$line</span><span class="s2">"</span> |
        <span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'s/.*msg=audit([^:]*:\([0-9]\+\)).*/\1/p'</span><span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$EVENT_ID</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">continue</span>

    <span class="c"># 현재 읽은 SYSCALL 로그 사용</span>
    <span class="nv">SYSCALL_LINE</span><span class="o">=</span><span class="s2">"</span><span class="nv">$line</span><span class="s2">"</span>

    <span class="c"># 실제 로그인 사용자</span>
    <span class="nv">USER_NAME</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$SYSCALL_LINE</span><span class="s2">"</span> |
        <span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'s/.*AUID="\([^"]*\)".*/\1/p'</span><span class="si">)</span>

    <span class="c"># 실행 파일</span>
    <span class="nv">EXE</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$SYSCALL_LINE</span><span class="s2">"</span> |
        <span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'s/.*exe="\([^"]*\)".*/\1/p'</span><span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">continue</span>

    <span class="c"># 사용자별 Webhook 선택</span>
    <span class="nv">USER_WEBHOOK</span><span class="o">=</span><span class="si">$(</span>get_user_webhook <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span><span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$USER_WEBHOOK</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">continue</span>

    <span class="c"># EXECVE와 CWD 이벤트가 기록될 시간 확보</span>
    <span class="nb">sleep </span>0.2

    <span class="nv">EVENT</span><span class="o">=</span><span class="si">$(</span>ausearch <span class="nt">-a</span> <span class="s2">"</span><span class="nv">$EVENT_ID</span><span class="s2">"</span> <span class="nt">-i</span> <span class="nt">--just-one</span> 2&gt;/dev/null<span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$EVENT</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">continue

    </span><span class="nv">EXECVE_LINE</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$EVENT</span><span class="s2">"</span> |
        <span class="nb">grep</span> <span class="s1">'^type=EXECVE'</span> |
        <span class="nb">head</span> <span class="nt">-1</span><span class="si">)</span>

    <span class="nv">CWD_LINE</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$EVENT</span><span class="s2">"</span> |
        <span class="nb">grep</span> <span class="s1">'^type=CWD'</span> |
        <span class="nb">head</span> <span class="nt">-1</span><span class="si">)</span>

    <span class="c"># 명령 실행 위치</span>
    <span class="nv">PWD_VALUE</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$CWD_LINE</span><span class="s2">"</span> |
        <span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'s/.*cwd=\([^ ]*\).*/\1/p'</span><span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$PWD_VALUE</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="nv">PWD_VALUE</span><span class="o">=</span><span class="s2">"unknown"</span>

    <span class="c"># EXECVE 인자를 하나의 명령으로 조합</span>
    <span class="nv">COMMAND</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$EXECVE_LINE</span><span class="s2">"</span> |
        <span class="nb">grep</span> <span class="nt">-oE</span> <span class="s1">'a[0-9]+=[^ ]+'</span> |
        <span class="nb">sed</span> <span class="nt">-E</span> <span class="s1">'s/^a[0-9]+=//'</span> |
        <span class="nb">paste</span> <span class="nt">-sd</span> <span class="s1">' '</span> -<span class="si">)</span>

    <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$COMMAND</span><span class="s2">"</span> <span class="o">]</span> <span class="o">&amp;&amp;</span> <span class="k">continue

    </span><span class="nv">TIME</span><span class="o">=</span><span class="si">$(</span><span class="nb">date</span> <span class="s1">'+%Y-%m-%d %H:%M:%S'</span><span class="si">)</span>

    <span class="c"># sudo 명령 처리</span>
    <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$EXE</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"/usr/bin/sudo"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then

        </span><span class="nv">SUDO_COMMAND</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">COMMAND</span><span class="p">#sudo</span><span class="k">}</span><span class="s2">"</span>
        <span class="nv">SUDO_COMMAND</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">SUDO_COMMAND</span><span class="p"># </span><span class="k">}</span><span class="s2">"</span>

        <span class="nv">RUN_AS</span><span class="o">=</span><span class="s2">"root"</span>

        <span class="c"># sudo -u user command 처리</span>
        <span class="k">if</span> <span class="o">[[</span> <span class="s2">"</span><span class="nv">$SUDO_COMMAND</span><span class="s2">"</span> <span class="o">==</span> <span class="s2">"-u "</span><span class="k">*</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then

            </span><span class="nv">RUN_AS</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$SUDO_COMMAND</span><span class="s2">"</span> |
                <span class="nb">awk</span> <span class="s1">'{print $2}'</span><span class="si">)</span>

            <span class="nv">SUDO_COMMAND</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$SUDO_COMMAND</span><span class="s2">"</span> |
                <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">' '</span> <span class="nt">-f3-</span><span class="si">)</span>
        <span class="k">fi</span>

        <span class="c"># 사용자 개인 채널</span>
        send_slack <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_WEBHOOK</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"SUDO"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$TIME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$SUDO_COMMAND</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$PWD_VALUE</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$RUN_AS</span><span class="s2">"</span>

        <span class="c"># sudo 공통 채널</span>
        send_slack <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$DEV_ALL_SUDO_WEBHOOK</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"SUDO"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$TIME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$SUDO_COMMAND</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$PWD_VALUE</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$RUN_AS</span><span class="s2">"</span>

    <span class="k">else</span>

        <span class="c"># sudo 내부에서 실행된 root 명령의 중복 전송 방지</span>
        <span class="nv">EUID_NAME</span><span class="o">=</span><span class="si">$(</span><span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s2">"</span><span class="nv">$SYSCALL_LINE</span><span class="s2">"</span> |
            <span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'s/.*EUID="\([^"]*\)".*/\1/p'</span><span class="si">)</span>

        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$EUID_NAME</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"root"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
            continue
        fi

        </span>send_slack <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_WEBHOOK</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"COMMAND"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$TIME</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$COMMAND</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$PWD_VALUE</span><span class="s2">"</span> <span class="se">\</span>
            <span class="s2">"</span><span class="nv">$USER_NAME</span><span class="s2">"</span>

    <span class="k">fi

done</span>
</code></pre></div></div>

<p>스크립트에서는 Audit 로그의 <code class="language-plaintext highlighter-rouge">SYSCALL</code>, <code class="language-plaintext highlighter-rouge">EXECVE</code>, <code class="language-plaintext highlighter-rouge">CWD</code> 이벤트를 이용해 다음 정보를 추출합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User    : 명령을 실행한 로그인 사용자
Run as  : 실제 명령을 실행하는 사용자
Time    : 명령 실행 시간
PWD     : 명령을 실행한 디렉토리
Command : 실행한 명령과 인자
</code></pre></div></div>

<p>Slack 메시지는 일반 명령과 <code class="language-plaintext highlighter-rouge">sudo</code> 명령을 쉽게 구분할 수 있도록 다음과 같이 표시합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>일반 명령 → 초록색 Attachment
sudo 명령 → 빨간색 Attachment
</code></pre></div></div>

<p>일반 명령은 해당 사용자의 개인 채널에만 전송됩니다.</p>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">hyn128</code> 사용자가 다음 명령을 실행하면,</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> <span class="nt">-al</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">#dev-hyn128</code> 채널에 다음 정보가 표시됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command

User
hyn128

Run as
hyn128

Time
2026-09-24 22:10:00

PWD
/home/hyn128

Command
ls --color=auto -al
</code></pre></div></div>

<p>반면 다음과 같이 <code class="language-plaintext highlighter-rouge">sudo</code>를 사용하면,</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo whoami</span>
</code></pre></div></div>

<p>동일한 로그가 <code class="language-plaintext highlighter-rouge">#dev-hyn128</code>과 <code class="language-plaintext highlighter-rouge">#dev-all-sudo</code>에 전송됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SUDO Command

User
hyn128

Run as
root

Time
2026-09-24 22:11:00

PWD
/home/hyn128

Command
whoami
</code></pre></div></div>

<h3 id="1-실행-권한-설정"><strong>1) 실행 권한 설정</strong></h3>

<p>작성한 스크립트에 실행 권한을 추가합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x ~/slack-log/scripts/command-monitor.sh
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/35.webp" alt="" /></p>

<h2 id="7-모니터링-스크립트-systemd-서비스-등록">7. 모니터링 스크립트 systemd 서비스 등록</h2>

<p><code class="language-plaintext highlighter-rouge">command-monitor.sh</code>를 터미널에서 직접 실행하면 해당 터미널을 계속 점유하며, SSH 연결이 종료되거나 서버가 재부팅되었을 때 모니터링도 중단됩니다.</p>

<p>따라서 스크립트를 <code class="language-plaintext highlighter-rouge">systemd</code> 서비스로 등록하여 백그라운드에서 계속 실행되도록 구성합니다.</p>

<p>서비스 파일을 생성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/systemd/system/command-monitor.service
</code></pre></div></div>

<p>다음 내용을 작성합니다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[Unit]</span>
<span class="py">Description</span><span class="p">=</span><span class="s">Orbit Slack Command Monitor</span>
<span class="py">After</span><span class="p">=</span><span class="s">auditd.service network-online.target</span>

<span class="nn">[Service]</span>
<span class="py">Type</span><span class="p">=</span><span class="s">simple</span>
<span class="py">User</span><span class="p">=</span><span class="s">root</span>
<span class="py">ExecStart</span><span class="p">=</span><span class="s">/home/orbit/slack-log/scripts/command-monitor.sh</span>
<span class="py">Restart</span><span class="p">=</span><span class="s">always</span>
<span class="py">RestartSec</span><span class="p">=</span><span class="s">3</span>

<span class="nn">[Install]</span>
<span class="py">WantedBy</span><span class="p">=</span><span class="s">multi-user.target</span>
</code></pre></div></div>

<p>서비스 설정을 적용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl daemon-reload
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> command-monitor.service
</code></pre></div></div>

<p>서비스 상태를 확인합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status command-monitor.service
</code></pre></div></div>

<p>정상적으로 실행되고 있다면 다음과 같이 표시됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Active: active (running)
</code></pre></div></div>

<p>스크립트를 수정한 이후에는 서비스를 다시 시작하여 변경 사항을 적용합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart command-monitor.service
</code></pre></div></div>

<h2 id="8-동작-테스트">8. 동작 테스트</h2>

<p>다른 사용자 계정으로 접속하여 일반 명령을 실행합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> <span class="nt">-al</span>
</code></pre></div></div>

<p>해당 사용자의 개인 Slack 채널에 일반 명령 로그가 전송되는지 확인합니다.</p>

<p>이후 <code class="language-plaintext highlighter-rouge">sudo</code> 명령을 실행합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo whoami</span>
</code></pre></div></div>

<p>정상적으로 동작한다면 다음과 같이 분기됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ls -al
└─ #dev-hyn128

sudo whoami
├─ #dev-hyn128
└─ #dev-all-sudo
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-24-project-1-orbit-command-logging/37.webp" alt="" /></p>

<p>이를 통해 사용자별 일반 명령과 <code class="language-plaintext highlighter-rouge">sudo</code> 명령을 Slack에서 실시간으로 확인할 수 있으며, 서버 재부팅 이후에도 <code class="language-plaintext highlighter-rouge">command-monitor.service</code>가 자동으로 실행되어 모니터링을 계속 수행합니다.</p>]]></content><author><name></name></author><category term="Project" /><category term="Orbit" /><category term="Auditd" /><category term="Webhook" /><summary type="html"><![CDATA[Develop 서버와 Production서버에서 sudo와 사용자별 로그를 Slack으로 기록하고 열람 가능하도록 하도록 구축하는 방법]]></summary></entry><entry><title type="html">Client Isolation으로 인한 단말 간 통신 실패</title><link href="https://hyn128.site/notes-11-troubleshooting-client-isolation/" rel="alternate" type="text/html" title="Client Isolation으로 인한 단말 간 통신 실패" /><published>2026-09-23T00:00:00+09:00</published><updated>2026-09-23T00:00:00+09:00</updated><id>https://hyn128.site/notes-11-troubleshooting-client-isolation</id><content type="html" xml:base="https://hyn128.site/notes-11-troubleshooting-client-isolation/"><![CDATA[<hr />

<h2 id="요약">요약</h2>
<blockquote>
  <p>Ubuntu Server 구축 후 동일한 Wi-Fi에 연결된 MacBook에서 서버로 SSH 접속 및 Ping이 되지 않는 문제가 발생했다.</p>

  <p>서버에서 외부 네트워크 연결은 정상적으로 이루어졌지만, 동일한 Wi-Fi에 연결된 MacBook에서 Ubuntu서버로, 심지어 반대로도 SSH 접속 및 Ping이 불가능했다.</p>

  <p>처음에는 이전 기수에서 사용하던 고정 IP 설정을 그대로 참고하면서 네트워크 설정 문제라고 판단했다. 확인 결과 기존 환경과 현재 환경의 네트워크 구성 및 서브넷이 달랐으며, 이를 현재 환경에 맞게 수정했다.</p>

  <p>하지만 네트워크 설정을 정상화한 이후에도 단말 간 통신은 불가능했다. 추가 확인 결과 강의실 Wi-Fi에 적용된 <strong>Client Isolation</strong>으로 인해 동일 Wi-Fi에 연결된 클라이언트 간 직접 통신이 차단되고 있음을 확인했다.</p>
  <h4 id="더-짧은-요약">더 짧은 요약</h4>
  <p><strong>SSH 접속 실패</strong><br />
→ 네트워크 설정 문제를 의심<br />
→ 이전 기수에서 사용하던 네트워크 환경과 다른 것을 인지<br />
→ 서브넷 설정을 현재 환경에 맞게 수정 → 여전히 실패<br />
→ 조사 결과 WiFi Client Isolation이 원인임을 확인<br />
→ 우리의 권한으로는 해결 불가능 → 우회를 해보자</p>
</blockquote>

<h4 id="해결-방법">해결 방법</h4>

<p>찾아보니 우회의 방법이 존재함 → TailScale 등의 VPN 활용(이미 다른 팀의 서버에 이전 기수가 설치한 내용이 발견됨)</p>

<p>AP에 대한 관리 권한이 없어 Client Isolation 자체를 해제할 수는 없으므로, 현재 환경에서는 필요한 내부 포트를 모두 강사님을 통해 개방시키거나, 최소한의 포트를 유지한 채로 <strong>Tailscale과 같은 VPN/Overlay Network를 통한 우회가 필요하다.</strong></p>

<h2 id="1-문제-상황">1. 문제 상황</h2>

<blockquote>
  <p>기존 Ubuntu Desktop 서버를 Ubuntu Server로 재설치한 뒤 Wi-Fi 네트워크를 설정했다.<br />
서버에서 외부 네트워크 연결은 정상적으로 이루어졌지만, 동일한 Wi-Fi에 연결된 MacBook에서 서버로 SSH 접속 및 Ping이 불가능했다.</p>
</blockquote>

<p>AutoEver Cloud Scool의 지난 기수에서 사용하던 서버를 받게 되었는데, 포맷이 되지 않은 상태였다.</p>

<p>Ubuntu Desktop이 설치된 서버에 Ubuntu Server를 설치하기 전, 기존의 <code class="language-plaintext highlighter-rouge">netplan.yaml</code> 에 DHCP가 아닌 고정 IP로 할당이 되어있는 것으로 보아 추후 할당 가능한 IP를 확인하고 네트워크 설정하는 과정이 줄어들 것이라 판단해 백업했다.</p>

<p><img src="../../assets/post/2026-09-22-notes-11-troubleshooting-client-isolation/02.webp" alt="" /></p>

<p>이후 Ubuntu Server 를 설치했고, 해당 설정을 참고하여 네트워크를 구성했지만, 다른 노트북에서 서버로 SSH 접속이 되지 않았다.</p>

<p>또한 SSH뿐만 아니라 서버 IP를 대상으로 한 Ping에도 응답이 없었다.</p>

<p>반면 서버 자체에서는 외부 네트워크 연결이 가능했다.</p>

<p>즉 문제 상황은 다음과 같았다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Server → Internet     정상
MacBook → Internet    정상

MacBook → Server Ping 실패
MacBook → Server SSH  실패
</code></pre></div></div>

<p>단순한 인터넷 연결 문제가 아니라 <strong>동일 네트워크에 연결된 단말 간 통신에서 문제가 발생하고 있는 상황</strong>이었다.</p>

<hr />

<h2 id="2-기존-환경과-현재-환경-비교"><strong>2. 기존 환경과 현재 환경 비교</strong></h2>

<p>문제를 확인하는 과정에서 이전 기수의 네트워크 환경과 현재 환경에 차이가 있다는 것을 확인했다.</p>

<p>기존 설정에서는 서버가 <strong>유선 LAN</strong>을 사용하고 있었지만, 현재 서버는 <strong>Wi-Fi</strong>를 통해 네트워크에 연결하고 있었다.</p>

<p>현재 서버의 유선 LAN 카드 상태가 확실하지 않았고 강의실 바닥의 LAN 포트가 Wi-Fi AP와 동일한 네트워크를 사용하는지도 확인되지 않았기 때문에, 우선 Wi-Fi를 기준으로 문제를 분석하기로 했다.</p>

<p>현재 Netplan 파일을 확인했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /etc/netplan
<span class="nb">ls</span>
</code></pre></div></div>

<p>확인된 파일:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>00-installer-config.yaml
</code></pre></div></div>

<p>설정을 변경하기 전 기존 파일을 백업했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cp </span>00-installer-config.yaml ~/.backup-netplan.yaml
</code></pre></div></div>

<h2><img src="../../assets/post/2026-09-22-notes-11-troubleshooting-client-isolation/06.webp" alt="" /></h2>

<h2 id="3-네트워크-설정-확인"><strong>3. 네트워크 설정 확인</strong></h2>

<p>현재 서버의 IP를 확인했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">hostname</span> <span class="nt">-I</span>
</code></pre></div></div>

<p>Netplan 설정도 확인했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/netplan/00-installer-config.yaml
</code></pre></div></div>

<p>처음에는 이전 설정을 참고하여 서버에 다음과 같이 Static IP를 설정했다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Server
192.168.200.3/24
</code></pre></div></div>

<p>하지만 MacBook의 네트워크 설정을 확인한 결과 다음과 같은 주소를 사용하고 있었다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MacBook
192.168.201.x/22
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">/24</code>와 <code class="language-plaintext highlighter-rouge">/22</code>는 서로 다른 서브넷 범위를 의미한다.</p>

<p><code class="language-plaintext highlighter-rouge">192.168.200.3/24</code>의 네트워크 범위는:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.168.200.0 ~ 192.168.200.255
</code></pre></div></div>

<p>반면 <code class="language-plaintext highlighter-rouge">192.168.201.x/22</code>가 포함되는 네트워크는 아래와 같았다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.168.200.0/22

192.168.200.x
192.168.201.x
192.168.202.x
192.168.203.x
</code></pre></div></div>

<p>이 내용을 확인하고 나니, 지난 기수가 사용하던 강의실과 현재 사용중인 강의실이 다르다는 것이 떠올랐다. 교육장에는 강의실마다 AP를 나눠서 사용하고 있었고, 이로인해 이전 기수와 서브넷이 달랐던 것이다.
따라서 이전 기수에서 사용하던 <code class="language-plaintext highlighter-rouge">/24</code> 설정을 현재 네트워크에 그대로 적용하는 것은 적절하지 않았다.</p>

<hr />

<h2 id="4-dhcp를-통한-실제-네트워크-설정-확인"><strong>4. DHCP를 통한 실제 네트워크 설정 확인</strong></h2>

<p>Static IP를 임의로 지정하기보다 현재 강의실 네트워크의 DHCP 서버가 어떤 설정을 제공하는지 먼저 확인하기로 했다.</p>

<p>Netplan을 DHCP 기반으로 변경했다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">network</span><span class="pi">:</span>
  <span class="na">version</span><span class="pi">:</span> <span class="m">2</span>
  <span class="na">wifis</span><span class="pi">:</span>
    <span class="na">wlo1</span><span class="pi">:</span>
      <span class="na">access-points</span><span class="pi">:</span>
        <span class="s2">"</span><span class="s">RAPA_Classroom-5"</span><span class="err">:</span>
          <span class="na">password</span><span class="pi">:</span> <span class="s2">"</span><span class="s">********"</span>
      <span class="na">dhcp4</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>기존에 직접 지정했던 다음 설정은 제거했다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">addresses</span><span class="pi">:</span>
<span class="na">routes</span><span class="pi">:</span>
<span class="na">nameservers</span><span class="pi">:</span>
</code></pre></div></div>

<p>설정을 검증하고 적용했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>netplan generate
<span class="nb">sudo </span>netplan try
</code></pre></div></div>

<p>이후 실제 할당된 IP와 라우팅 정보를 확인했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip <span class="nt">-4</span> addr show wlo1
ip route
</code></pre></div></div>

<p>이를 통해 현재 강의실 네트워크에서 사용하는 IP 대역과 서브넷 정보를 확인할 수 있었다.</p>

<p>하지만 네트워크 설정을 현재 환경에 맞게 수정한 이후에도 MacBook에서 서버로 Ping과 SSH가 되지 않았다.</p>

<p>따라서 <strong>서브넷 설정만의 문제는 아니라고 판단했다.</strong></p>

<hr />

<h2 id="5-ssh-및-방화벽-확인"><strong>5. SSH 및 방화벽 확인</strong></h2>

<p>SSH 서비스 및 방화벽 문제도 확인했다.</p>

<p>UFW 상태를 확인했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>ufw status
</code></pre></div></div>

<p>SSH에 사용되는 TCP 22번 포트가 허용되어 있었다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>22/tcp    ALLOW
</code></pre></div></div>

<p>따라서 SSH 접속 실패의 직접적인 원인이 UFW에서 TCP 22번 포트를 차단했기 때문은 아니었다.</p>

<p>또한 SSH뿐만 아니라 Ping을 통한 단말 간 기본 통신 자체가 실패하고 있었기 때문에 문제 범위를 SSH 애플리케이션 계층보다 아래의 네트워크 계층으로 좁힐 수 있었다.</p>

<hr />

<h2 id="6-동일-wi-fi-단말-간-통신-확인"><strong>6. 동일 Wi-Fi 단말 간 통신 확인</strong></h2>

<p>서버와 MacBook은 모두 동일한 Wi-Fi에 연결되어 있었으며 각각 인터넷 연결도 정상적이었다.</p>

<p>구조상 다음과 같은 상태였다.</p>

<p><img src="../../assets/post/2026-09-22-notes-11-troubleshooting-client-isolation/04.webp" alt="" /></p>

<p>각 장비에서 AP 및 외부 네트워크로의 통신은 가능하지만 두 클라이언트 사이의 직접 통신은 불가능했다.</p>

<p>즉 다음과 같은 특징을 보였다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Server  → Gateway    정상
MacBook → Gateway    정상

Server  → Internet   정상
MacBook → Internet   정상

MacBook → Server     실패
Server  → MacBook    실패
</code></pre></div></div>

<p>이 시점에서 개별 서버의 IP, SSH 또는 방화벽 설정보다는 <strong>AP에서 클라이언트 간 통신을 제한하고 있을 가능성</strong>을 의심했다.</p>

<hr />

<h2 id="7-원인--client-isolation"><strong>7. 원인 — Client Isolation</strong></h2>

<p>조사 결과 현재 Wi-Fi 환경에서 <strong>Client Isolation(AP Isolation)</strong>으로 인해 무선 클라이언트 간 직접 통신이 제한되고 있는 것으로 판단했다.</p>

<p>Client Isolation은 동일한 AP에 연결된 클라이언트가 서로 직접 통신하지 못하도록 격리하는 기능이다.</p>

<p>따라서 동일한 SSID를 사용하고 동일한 IP 서브넷에 속하더라도 다음과 같은 통신이 제한될 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MacBook ──X── Ubuntu Server
</code></pre></div></div>

<p>반면 AP를 통해 외부로 나가는 트래픽은 정상적으로 처리될 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MacBook ──→ AP ──→ Internet
Server  ──→ AP ──→ Internet
</code></pre></div></div>

<p>이 때문에 서버와 MacBook 모두 인터넷 연결에는 문제가 없었지만, MacBook에서 서버로 직접 접근하는 Ping과 SSH는 실패했다.</p>

<hr />

<h2 id="8-해결-여부-및-우회-방법"><strong>8. 해결 여부 및 우회 방법</strong></h2>

<p>이번 문제는 서버 자체의 설정을 변경하여 해결할 수 있는 문제가 아니었다.</p>

<p>Client Isolation은 AP 측에서 적용되는 정책이므로 이를 직접 해제하려면 해당 네트워크 장비에 대한 관리 권한이 필요하다.</p>

<p>현재는 AP에 대한 관리 권한이 없기 때문에 <strong>Client Isolation 자체는 해결할 수 없는 상태</strong>다.</p>

<p>다만 네트워크를 우회하는 방법은 존재한다.</p>

<p>서버를 확인하는 과정에서 다른 팀의 서버에 이전 기수가 설치한 <strong>Tailscale</strong> 관련 구성이 발견되었다. 이는 이전 환경에서도 동일하거나 유사한 네트워크 제약을 우회하기 위해 사용했을 가능성이 있다.</p>

<p>Tailscale과 같은 Overlay Network를 사용하면 각 장비에 별도의 가상 네트워크 인터페이스와 IP를 구성하여 물리적인 로컬 네트워크의 직접 통신에 의존하지 않고 장비 간 연결을 구성할 수 있다.</p>

<p>구조는 다음과 같다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Ubuntu Server
     │
     │ Tailscale Network
     │
  MacBook
</code></pre></div></div>

<p>따라서 현재 환경에서는 다음과 같은 방법을 고려할 수 있다.</p>

<ul>
  <li>Tailscale 등의 Overlay Network 사용</li>
  <li>Client Isolation이 적용되지 않은 별도 네트워크 사용</li>
  <li>유선 LAN을 통한 별도 네트워크 구성</li>
  <li>관리 가능한 별도의 AP/공유기 구성</li>
</ul>

<hr />

<h2 id="9-결론"><strong>9. 결론</strong></h2>

<p>초기에는 SSH 접속 실패를 서버의 SSH, UFW 또는 Static IP 설정 문제로 판단했다.</p>

<p>네트워크 설정을 확인하는 과정에서 이전 기수의 <code class="language-plaintext highlighter-rouge">/24</code> 기반 설정과 현재 강의실의 <code class="language-plaintext highlighter-rouge">/22</code> 기반 네트워크 환경이 다르다는 것을 확인했고, DHCP를 사용하여 현재 환경에 맞는 네트워크 설정을 다시 구성했다.</p>

<p>그러나 설정 정상화 이후에도 동일 Wi-Fi에 연결된 단말 간 Ping 및 SSH가 실패했다.</p>

<p>각 단말에서 게이트웨이와 외부 네트워크로의 통신은 정상인 반면 클라이언트 간 직접 통신만 실패한다는 점을 통해 문제 범위를 AP 정책으로 좁혔으며, 최종적으로 <strong>Wi-Fi Client Isolation에 의한 단말 간 통신 제한이 원인인 것으로 판단했다.</strong></p>

<h2 id="10-해야-할-것">10. 해야 할 것</h2>

<p>TailScale이 조직으로도 사용이 가능한지, 다른 Overlay Network 또는 VPN으로 구축이 가능한지 찾아보기</p>]]></content><author><name></name></author><category term="TroubleShooting" /><category term="TechBites" /><summary type="html"><![CDATA[서버 네트워크 환경 구축 과정에서 맞이한 통신 실패의 원인 추척과 해결 방안]]></summary></entry><entry><title type="html">Ansible 변수와 Vault·Facts·제어문</title><link href="https://hyn128.site/cloud-native-60-ansible-variables-vault-facts-control-flow/" rel="alternate" type="text/html" title="Ansible 변수와 Vault·Facts·제어문" /><published>2026-09-23T00:00:00+09:00</published><updated>2026-09-23T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-60-ansible-variables-vault-facts-control-flow</id><content type="html" xml:base="https://hyn128.site/cloud-native-60-ansible-variables-vault-facts-control-flow/"><![CDATA[<p><a href="/cloud-native-59-ansible-adhoc-playbook-nginx/">Ansible Ad-hoc 명령과 Playbook으로 Nginx 자동화</a>에서는 반복 가능한 Nginx 배포 절차를 Playbook으로 작성했다. 이번에는 Host와 환경마다 달라지는 값을 변수로 분리하고, Managed Node에서 수집한 Facts와 이전 Task의 결과를 이용해 실행 대상을 판단한다. 비밀 값은 Ansible Vault로 암호화하고 상태 변경이 발생했을 때만 Handler를 호출한다.</p>

<h2 id="1--변수의-역할과-이름">1 ) 변수의 역할과 이름</h2>

<hr />

<blockquote>
  <p><strong>변수</strong>는 사용자, Package, Service, File 경로처럼 Host나 환경에 따라 달라지는 값을 이름으로 저장한 것이다.</p>
</blockquote>

<p>같은 값을 Task마다 직접 적으면 환경이 바뀔 때 여러 위치를 수정해야 한다. 변수를 사용하면 Playbook의 작업 구조는 유지하면서 입력 값만 바꿀 수 있다.</p>

<p>변수 이름은 문자, 숫자와 밑줄로 구성하며 숫자로 시작할 수 없다. Python이나 Ansible이 내부에서 사용하는 이름은 피한다.</p>

<table>
  <thead>
    <tr>
      <th>이름</th>
      <th>판단</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">web_package</code></td>
      <td>사용 가능</td>
      <td>의미가 분명하고 영문자와 밑줄만 사용한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">web-package</code></td>
      <td>사용하지 않음</td>
      <td>하이픈은 식별자에서 빼기 연산자로 해석될 수 있다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">1st_user</code></td>
      <td>사용 불가</td>
      <td>숫자로 시작한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>사용하지 않음</td>
      <td>Ansible에서 특별한 의미를 가지는 예약 이름이다.</td>
    </tr>
  </tbody>
</table>

<p>변수는 Jinja2 표현식으로 참조한다. 값 전체가 변수라면 YAML 문자열로 감싸는 것이 안전하다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">ansible.builtin.user</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">managed_user</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
</code></pre></div></div>

<h2 id="2--변수의-정의-위치와-범위">2 ) 변수의 정의 위치와 범위</h2>

<hr />

<p>변수는 Inventory, Playbook, 외부 File, Role과 실행 명령에서 정의할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>정의 위치</th>
      <th>적용 범위</th>
      <th>일반적인 용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Group 변수</td>
      <td>Inventory의 Group 또는 <code class="language-plaintext highlighter-rouge">group_vars/</code></td>
      <td>Group에 속한 Host</td>
      <td>공통 Package, Port와 계정</td>
    </tr>
    <tr>
      <td>Host 변수</td>
      <td>Inventory의 Host 또는 <code class="language-plaintext highlighter-rouge">host_vars/</code></td>
      <td>특정 Host</td>
      <td>Host별 IP, 장치명과 예외 값</td>
    </tr>
    <tr>
      <td>Play 변수</td>
      <td>Play의 <code class="language-plaintext highlighter-rouge">vars</code></td>
      <td>해당 Play</td>
      <td>Play 안에서만 사용하는 값</td>
    </tr>
    <tr>
      <td>외부 변수</td>
      <td><code class="language-plaintext highlighter-rouge">vars_files</code></td>
      <td>불러온 Play</td>
      <td>환경별 변수 File 분리</td>
    </tr>
    <tr>
      <td>추가 변수</td>
      <td><code class="language-plaintext highlighter-rouge">--extra-vars</code>, <code class="language-plaintext highlighter-rouge">-e</code></td>
      <td>실행 시 전달</td>
      <td>일회성 Override</td>
    </tr>
    <tr>
      <td>등록 변수</td>
      <td>Task의 <code class="language-plaintext highlighter-rouge">register</code></td>
      <td>해당 Host의 현재 실행</td>
      <td>이전 Task 결과를 후속 Task에서 사용</td>
    </tr>
  </tbody>
</table>

<p>같은 이름이 여러 위치에 있으면 우선순위가 높은 값이 낮은 값을 덮어쓴다. 전체 우선순위에는 많은 단계가 있지만 이번 범위에서는 다음 관계를 먼저 기억한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Role defaults
  &lt; Inventory Group 변수
  &lt; Inventory Host 변수
  &lt; Play vars·vars_files
  &lt; Role vars
  &lt; Task·등록 변수
  &lt; Extra vars
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-e</code>로 전달한 Extra vars는 변수 중 가장 높은 우선순위를 가진다. 우선순위에 의존해 같은 이름을 여러 위치에서 반복 정의하기보다 값의 성격에 맞는 한 위치를 정하는 것이 좋다.</p>

<h2 id="3--group-변수와-host-변수">3 ) Group 변수와 Host 변수</h2>

<hr />

<p>간단한 실습에서는 <code class="language-plaintext highlighter-rouge">inventory.ini</code> 안에 변수를 함께 작성할 수 있다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[lab]</span>
<span class="err">master</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.11 managed_user=ansible-master</span>
<span class="err">worker1</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.12</span>
<span class="err">worker2</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.13</span>

<span class="nn">[lab:vars]</span>
<span class="py">managed_user</span><span class="p">=</span><span class="s">ansible</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">master</code>는 Host 변수의 <code class="language-plaintext highlighter-rouge">managed_user=ansible-master</code>가 Group 변수보다 우선한다. <code class="language-plaintext highlighter-rouge">worker1</code>과 <code class="language-plaintext highlighter-rouge">worker2</code>는 Group 값인 <code class="language-plaintext highlighter-rouge">ansible</code>을 사용한다.</p>

<p>Project가 커지면 Inventory의 연결 정보와 일반 변수를 분리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-lab/
├── ansible.cfg
├── inventory.ini
├── group_vars/
│   └── lab.yml
└── host_vars/
    └── master.yml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">group_vars/lab.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">managed_user</span><span class="pi">:</span> <span class="s">ansible</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">host_vars/master.yml</code>에서 <code class="language-plaintext highlighter-rouge">master</code>만 Override한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">managed_user</span><span class="pi">:</span> <span class="s">ansible-master</span>
</code></pre></div></div>

<p>변수가 Host별로 어떻게 해석되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-inventory <span class="nt">--host</span> master
ansible-inventory <span class="nt">--host</span> worker1
</code></pre></div></div>

<h2 id="4--play-변수와-외부-변수-file">4 ) Play 변수와 외부 변수 File</h2>

<hr />

<p>Play 안에서만 사용할 값은 <code class="language-plaintext highlighter-rouge">vars</code>에 정의한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create a managed user</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">vars</span><span class="pi">:</span>
    <span class="na">managed_user</span><span class="pi">:</span> <span class="s">ansible2</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure the managed user exists</span>
      <span class="na">ansible.builtin.user</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">managed_user</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
      <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>여러 Playbook이 같은 값을 사용하면 <code class="language-plaintext highlighter-rouge">vars/users.yml</code>로 분리한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">managed_user</span><span class="pi">:</span> <span class="s">ansible4</span>
</code></pre></div></div>

<p>Play에서 <code class="language-plaintext highlighter-rouge">vars_files</code>로 불러온다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create a user from an external variable file</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">vars_files</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">vars/users.yml</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure the managed user exists</span>
      <span class="na">ansible.builtin.user</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">managed_user</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
      <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>실행할 때만 값을 바꾸려면 Extra vars를 전달한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="se">\</span>
  <span class="nt">-e</span> <span class="s2">"managed_user=ansible5"</span> <span class="se">\</span>
  create-user.yml
</code></pre></div></div>

<p>Extra vars는 기존 값을 강제로 덮어쓰므로 자동화 Script에서 무분별하게 사용하면 실제 값의 출처를 추적하기 어렵다.</p>

<h2 id="5--task-결과를-등록-변수로-사용">5 ) Task 결과를 등록 변수로 사용</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">register</code>는 Task의 Return Data를 Host별 변수에 저장한다. 등록 변수는 현재 Playbook 실행 중 후속 Task에서 사용할 수 있으며 다음 실행까지 영구 보존되지 않는다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Inspect a service result</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">gather_facts</span><span class="pi">:</span> <span class="no">false</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Check whether rsyslog is active</span>
      <span class="na">ansible.builtin.command</span><span class="pi">:</span> <span class="s">systemctl is-active rsyslog</span>
      <span class="na">register</span><span class="pi">:</span> <span class="s">rsyslog_status</span>
      <span class="na">changed_when</span><span class="pi">:</span> <span class="no">false</span>
      <span class="na">failed_when</span><span class="pi">:</span> <span class="no">false</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print the registered result</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">var</span><span class="pi">:</span> <span class="s">rsyslog_status</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print active status only</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">msg</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Rsyslog</span><span class="nv"> </span><span class="s">is</span><span class="nv"> </span><span class="s">active</span><span class="nv"> </span><span class="s">on</span><span class="nv"> </span><span class="s">{{</span><span class="nv"> </span><span class="s">inventory_hostname</span><span class="nv"> </span><span class="s">}}"</span>
      <span class="na">when</span><span class="pi">:</span> <span class="s">rsyslog_status.rc == </span><span class="m">0</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">command</code> Module은 조회 명령도 기본적으로 변경으로 판단할 수 있으므로 <code class="language-plaintext highlighter-rouge">changed_when: false</code>를 지정했다. <code class="language-plaintext highlighter-rouge">systemctl is-active</code>의 Non-zero Exit Code도 결과로 비교하기 위해 <code class="language-plaintext highlighter-rouge">failed_when: false</code>를 사용했다.</p>

<p>주요 Return 값은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>값</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rc</code></td>
      <td>명령의 Exit Code</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">stdout</code></td>
      <td>표준 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">stderr</code></td>
      <td>표준 오류</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">changed</code></td>
      <td>상태 변경 여부</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">failed</code></td>
      <td>Task 실패 여부</td>
    </tr>
  </tbody>
</table>

<h2 id="6--ansible-vault">6 ) Ansible Vault</h2>

<hr />

<blockquote>
  <p><strong>Ansible Vault</strong>는 Password, API Key와 같은 변수를 File 또는 문자열 단위로 암호화하여 평문 노출을 줄이는 기능이다.</p>
</blockquote>

<p>Vault는 저장된 Data만 보호한다. Playbook 실행 중 복호화된 값이 Module 출력이나 Log에 표시되는 문제까지 자동으로 막지는 않는다. Secret을 사용하는 Task에는 필요에 따라 <code class="language-plaintext highlighter-rouge">no_log: true</code>를 설정한다.</p>

<h3 id="61-암호화-변수-file-생성">6.1 암호화 변수 File 생성</h3>

<p>Control Node에서 <code class="language-plaintext highlighter-rouge">vars/secrets.yml</code>을 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> vars
ansible-vault create <span class="se">\</span>
  <span class="nt">--vault-id</span> lab@prompt <span class="se">\</span>
  vars/secrets.yml
</code></pre></div></div>

<p>Editor에 다음 구조로 입력한다. 실제 Password를 문서나 Repository에 작성하지 않는다. Linux <code class="language-plaintext highlighter-rouge">user</code> Module의 <code class="language-plaintext highlighter-rouge">password</code>에는 평문 Password가 아니라 대상 OS가 이해하는 Password Hash가 필요하다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">vault_user_name</span><span class="pi">:</span> <span class="s">itstudy</span>
<span class="na">vault_user_password_hash</span><span class="pi">:</span> <span class="s2">"</span><span class="s">&lt;PASSWORD-HASH&gt;"</span>
</code></pre></div></div>

<p>암호화된 File은 <code class="language-plaintext highlighter-rouge">$ANSIBLE_VAULT</code> Header와 Ciphertext로 보인다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">head</span> <span class="nt">-n</span> 1 vars/secrets.yml
ansible-vault view <span class="se">\</span>
  <span class="nt">--vault-id</span> lab@prompt <span class="se">\</span>
  vars/secrets.yml
</code></pre></div></div>

<p>내용을 수정할 때는 평문 File로 복호화해 방치하지 않고 <code class="language-plaintext highlighter-rouge">edit</code>을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-vault edit <span class="se">\</span>
  <span class="nt">--vault-id</span> lab@prompt <span class="se">\</span>
  vars/secrets.yml
</code></pre></div></div>

<p>기존 File은 <code class="language-plaintext highlighter-rouge">encrypt</code>, 암호가 노출되었거나 교체 주기가 되면 <code class="language-plaintext highlighter-rouge">rekey</code>로 다시 암호화할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-vault encrypt <span class="nt">--vault-id</span> lab@prompt vars/existing-secret.yml
ansible-vault rekey vars/secrets.yml
</code></pre></div></div>

<h3 id="62-vault-변수-사용">6.2 Vault 변수 사용</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create a user with vaulted data</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">vars_files</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">vars/secrets.yml</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure the vaulted user exists</span>
      <span class="na">ansible.builtin.user</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">vault_user_name</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">password</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">vault_user_password_hash</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
      <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
      <span class="na">no_log</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>실행할 때 같은 Vault ID와 Password를 제공한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="se">\</span>
  <span class="nt">--vault-id</span> lab@prompt <span class="se">\</span>
  create-vault-user.yml
</code></pre></div></div>

<p>Vault Password File을 사용할 수도 있지만 그 File 자체가 복호화 Key이다. Repository나 일반 공유 Directory에 두지 않고 최소 Permission과 별도 Secret Manager를 사용한다.</p>

<h2 id="7--facts">7 ) Facts</h2>

<hr />

<blockquote>
  <p><strong>Facts</strong>는 Ansible이 Managed Node에서 자동으로 수집한 Hostname, OS, Kernel, CPU, Memory, Network와 Storage 정보이다.</p>
</blockquote>

<p>Play의 <code class="language-plaintext highlighter-rouge">gather_facts</code> 기본값은 <code class="language-plaintext highlighter-rouge">true</code>이다. Facts가 필요 없는 단순 작업은 <code class="language-plaintext highlighter-rouge">false</code>로 설정해 시작 시간을 줄일 수 있다.</p>

<p>Ad-hoc 명령으로 전체 Facts를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible lab <span class="nt">-m</span> ansible.builtin.setup
</code></pre></div></div>

<p>Playbook에서 필요한 일부 값만 출력한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print selected facts</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print host and network facts</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">msg</span><span class="pi">:</span> <span class="pi">&gt;-</span>
          <span class="s">{{ inventory_hostname }} runs</span>
          <span class="s">{{ ansible_facts['distribution'] }}</span>
          <span class="s">{{ ansible_facts['distribution_version'] }} and its default IPv4</span>
          <span class="s">address is {{ ansible_facts['default_ipv4']['address'] }}.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">inventory_hostname</code>은 Inventory에서 정한 이름이고 <code class="language-plaintext highlighter-rouge">ansible_facts['hostname']</code>은 Managed Node OS가 보고한 Hostname이다. 두 값은 같지 않을 수 있다.</p>

<p>Facts 수집에 실패하면 SSH 연결뿐 아니라 Managed Node의 Python과 <code class="language-plaintext highlighter-rouge">setup</code> Module 실행 조건을 확인한다.</p>

<h3 id="사용자-지정-local-facts">사용자 지정 Local Facts</h3>

<p>Managed Node의 <code class="language-plaintext highlighter-rouge">/etc/ansible/facts.d</code>에 <code class="language-plaintext highlighter-rouge">.fact</code> File을 두면 <code class="language-plaintext highlighter-rouge">ansible_local</code> Namespace에서 읽을 수 있다. 다음 작업은 Managed Node에 INI 형식의 Local Fact를 배포한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install a local fact</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create the local facts directory</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/etc/ansible/facts.d</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">directory</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0755"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Write package local facts</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/ansible/facts.d/packages.fact</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
        <span class="na">content</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">[packages]</span>
          <span class="s">web_package=nginx</span>
          <span class="s">db_package=mariadb-server</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Gather facts again</span>
      <span class="na">ansible.builtin.setup</span><span class="pi">:</span>
</code></pre></div></div>

<p>다시 수집한 값은 <code class="language-plaintext highlighter-rouge">ansible_local['packages']['packages']['web_package']</code>와 같은 구조로 접근한다. Local Fact의 File 이름과 INI Section이 각각 중첩 Key가 된다.</p>

<h2 id="8--반복문">8 ) 반복문</h2>

<hr />

<p>같은 Module을 여러 값에 적용할 때 <code class="language-plaintext highlighter-rouge">loop</code>를 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure required services are running</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">vars</span><span class="pi">:</span>
    <span class="na">required_services</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">ssh</span>
      <span class="pi">-</span> <span class="s">rsyslog</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Start required services</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">item</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>
      <span class="na">loop</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">required_services</span><span class="nv"> </span><span class="s">}}"</span>
</code></pre></div></div>

<p>여러 속성을 함께 반복하려면 Dictionary 목록을 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create log files</span>
  <span class="na">ansible.builtin.file</span><span class="pi">:</span>
    <span class="na">path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">item.path</span><span class="nv"> </span><span class="s">}}"</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">touch</span>
    <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">item.mode</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">loop</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/var/log/test1.log</span>
      <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
    <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/var/log/test2.log</span>
      <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0600"</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>반복 Task에 <code class="language-plaintext highlighter-rouge">register</code>를 사용하면 각 반복 결과가 <code class="language-plaintext highlighter-rouge">results</code> 목록에 저장된다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Check service states</span>
  <span class="na">ansible.builtin.command</span><span class="pi">:</span> <span class="s2">"</span><span class="s">systemctl</span><span class="nv"> </span><span class="s">is-active</span><span class="nv"> </span><span class="s">{{</span><span class="nv"> </span><span class="s">item</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">loop</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">ssh</span>
    <span class="pi">-</span> <span class="s">rsyslog</span>
  <span class="na">register</span><span class="pi">:</span> <span class="s">service_checks</span>
  <span class="na">changed_when</span><span class="pi">:</span> <span class="no">false</span>
  <span class="na">failed_when</span><span class="pi">:</span> <span class="no">false</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print each service result</span>
  <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
    <span class="na">msg</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">item.item</span><span class="nv"> </span><span class="s">}}:</span><span class="nv"> </span><span class="s">{{</span><span class="nv"> </span><span class="s">item.stdout</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">loop</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">service_checks.results</span><span class="nv"> </span><span class="s">}}"</span>
</code></pre></div></div>

<h2 id="9--조건문">9 ) 조건문</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">when</code>은 Boolean, Facts, 등록 변수와 반복 항목을 평가해 Task 실행 여부를 결정한다. 조건식 자체에는 Jinja2 이중 중괄호를 사용하지 않는다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print the package manager family</span>
  <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
    <span class="na">msg</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Use</span><span class="nv"> </span><span class="s">apt</span><span class="nv"> </span><span class="s">on</span><span class="nv"> </span><span class="s">{{</span><span class="nv"> </span><span class="s">inventory_hostname</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">when</span><span class="pi">:</span> <span class="s">ansible_facts['os_family'] == 'Debian'</span>
</code></pre></div></div>

<p>목록으로 작성한 여러 조건은 모두 참이어야 한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print supported Ubuntu version</span>
  <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
    <span class="na">msg</span><span class="pi">:</span> <span class="s">This host uses a supported Ubuntu release.</span>
  <span class="na">when</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">ansible_facts['distribution'] == 'Ubuntu'</span>
    <span class="pi">-</span> <span class="s">ansible_facts['distribution_major_version'] | int &gt;= </span><span class="m">22</span>
</code></pre></div></div>

<p>반복문과 조건문을 함께 사용해 Root Mount만 선택할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print root filesystem free space</span>
  <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
    <span class="na">msg</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Root</span><span class="nv"> </span><span class="s">free</span><span class="nv"> </span><span class="s">space</span><span class="nv"> </span><span class="s">is</span><span class="nv"> </span><span class="s">{{</span><span class="nv"> </span><span class="s">item.size_available</span><span class="nv"> </span><span class="s">}}</span><span class="nv"> </span><span class="s">bytes"</span>
  <span class="na">loop</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">ansible_facts['mounts']</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">when</span><span class="pi">:</span> <span class="s">item.mount == '/'</span>
</code></pre></div></div>

<p>Facts를 조건에 사용할 때 <code class="language-plaintext highlighter-rouge">gather_facts: false</code>를 설정하면 값이 존재하지 않을 수 있다. Facts Cache를 별도로 구성하지 않았다면 해당 Play에서 Facts를 수집한다.</p>

<h2 id="10--handler와-변경-알림">10 ) Handler와 변경 알림</h2>

<hr />

<blockquote>
  <p><strong>Handler</strong>는 Task가 실제 변경을 만들고 <code class="language-plaintext highlighter-rouge">notify</code>한 경우에만 실행되는 특수 Task이다.</p>
</blockquote>

<p>여러 Task가 같은 Handler를 알리더라도 기본적으로 Play의 해당 실행 지점에서 한 번만 처리한다. 설정 File이 변경되지 않으면 Service를 불필요하게 재시작하지 않는다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Manage rsyslog configuration</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy rsyslog configuration</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">files/50-lab.conf</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/rsyslog.d/50-lab.conf</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Restart rsyslog</span>

  <span class="na">handlers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Restart rsyslog</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">rsyslog</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">restarted</span>
</code></pre></div></div>

<p>Handler 이름은 Play 범위에서 고유하게 작성한다. 중간에 즉시 실행해야 하는 특별한 이유가 없다면 기본 실행 시점을 유지한다.</p>

<h2 id="11--실패-처리">11 ) 실패 처리</h2>

<hr />

<p>Task가 실패하면 Ansible은 기본적으로 해당 Host의 뒤 Task를 중단하고 다른 Host는 계속 처리한다.</p>

<p><code class="language-plaintext highlighter-rouge">ignore_errors: true</code>는 실행된 Task가 <code class="language-plaintext highlighter-rouge">failed</code> 결과를 반환해도 다음 Task로 진행한다. Undefined Variable, 문법 오류와 <code class="language-plaintext highlighter-rouge">unreachable</code> 상태까지 무시하는 기능은 아니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run an optional validation command</span>
  <span class="na">ansible.builtin.command</span><span class="pi">:</span> <span class="s">/usr/local/bin/optional-check</span>
  <span class="na">register</span><span class="pi">:</span> <span class="s">optional_check</span>
  <span class="na">changed_when</span><span class="pi">:</span> <span class="no">false</span>
  <span class="na">failed_when</span><span class="pi">:</span> <span class="no">false</span>
</code></pre></div></div>

<p>예상 가능한 Non-zero Exit Code라면 모든 오류를 무시하기보다 <code class="language-plaintext highlighter-rouge">failed_when</code>으로 성공과 실패의 의미를 정의한다.</p>

<p>설정 File을 변경해 Handler가 알림을 받은 뒤 후속 Task가 실패하면 기본적으로 그 Host의 Handler가 실행되지 않을 수 있다. 실패한 Host에서도 이미 알림을 받은 Handler를 실행해야 한다면 Play에 <code class="language-plaintext highlighter-rouge">force_handlers: true</code>를 설정하거나 <code class="language-plaintext highlighter-rouge">--force-handlers</code>를 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Apply configuration with forced handlers</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>
  <span class="na">force_handlers</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Explain force handler policy</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">msg</span><span class="pi">:</span> <span class="s">Notified handlers run even if a later task fails.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">force_handlers</code>도 Host가 <code class="language-plaintext highlighter-rouge">unreachable</code> 상태가 되는 문제까지 해결하지는 않는다.</p>

<h2 id="12--검사와-실행">12 ) 검사와 실행</h2>

<hr />

<p>변수와 조건을 추가한 뒤에는 선택되는 Host와 문법을 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-inventory <span class="nt">--graph</span>
ansible-playbook <span class="nt">--syntax-check</span> playbook.yml
ansible-playbook <span class="nt">--check</span> <span class="nt">--diff</span> playbook.yml
ansible-playbook playbook.yml
</code></pre></div></div>

<p>Check Mode는 외부 명령과 일부 Module의 실제 결과를 완전히 재현하지 않는다. 실제 실행 후 Play Recap의 <code class="language-plaintext highlighter-rouge">ok</code>, <code class="language-plaintext highlighter-rouge">changed</code>, <code class="language-plaintext highlighter-rouge">failed</code>, <code class="language-plaintext highlighter-rouge">unreachable</code>, <code class="language-plaintext highlighter-rouge">skipped</code>, <code class="language-plaintext highlighter-rouge">rescued</code>, <code class="language-plaintext highlighter-rouge">ignored</code>를 Host별로 확인한다.</p>

<p>공식 문서는 <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_variables.html">변수</a>, <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_vars_facts.html">Facts</a>, <a href="https://docs.ansible.com/projects/ansible/latest/vault_guide/vault.html">Vault</a>, <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_handlers.html">Handler</a>와 <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_error_handling.html">실패 처리</a>에서 확인할 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Group·Host·Play·외부·추가 변수는 적용 범위와 우선순위가 다르며 Extra vars는 기존 변수를 강제로 덮어쓴다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">register</code>는 현재 실행의 Host별 Task 결과를 저장하고 Facts는 Managed Node에서 수집한 System 정보를 제공한다.</p>
    </li>
    <li>
      <p>Vault는 저장 Data를 암호화하지만 실행 중 출력까지 보호하지 않으므로 Secret Task의 Log도 통제해야 한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">loop</code>는 같은 작업을 여러 값에 적용하고 <code class="language-plaintext highlighter-rouge">when</code>은 Facts와 이전 결과에 따라 Task를 선택한다.</p>
    </li>
    <li>
      <p>Handler는 실제 변경 알림이 있을 때만 실행하며 실패를 무시하기보다 <code class="language-plaintext highlighter-rouge">failed_when</code>으로 기대하는 결과를 정의한다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-61-ansible-roles-collections-docker/">Ansible Role·Collection과 Docker 자동화</a>에서는 관련 Task, File, 변수와 Handler를 Role로 묶고 Collection Module로 Docker Container를 관리한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Ansible" /><summary type="html"><![CDATA[Ansible 변수의 범위와 우선순위를 구분하고 Vault, Facts, 반복문, 조건문, Handler와 실패 처리를 이용해 재사용 가능한 Playbook을 작성한다]]></summary></entry><entry><title type="html">Ansible Role·Collection과 Docker 자동화</title><link href="https://hyn128.site/cloud-native-61-ansible-roles-collections-docker/" rel="alternate" type="text/html" title="Ansible Role·Collection과 Docker 자동화" /><published>2026-09-23T00:00:00+09:00</published><updated>2026-09-23T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-61-ansible-roles-collections-docker</id><content type="html" xml:base="https://hyn128.site/cloud-native-61-ansible-roles-collections-docker/"><![CDATA[<p><a href="/cloud-native-60-ansible-variables-vault-facts-control-flow/">Ansible 변수와 Vault·Facts·제어문</a>에서는 변수, Facts, 반복문과 조건문으로 Host별 실행을 제어했다. 이번에는 길어진 Playbook의 Task, 변수, File과 Handler를 표준 Directory 구조의 Role로 분리한다. 외부 Role과 Collection을 관리하는 <code class="language-plaintext highlighter-rouge">ansible-galaxy</code>를 살펴본 뒤 <code class="language-plaintext highlighter-rouge">community.docker</code> Collection으로 Docker Container를 배포한다.</p>

<h2 id="1--role이-필요한-이유">1 ) Role이 필요한 이유</h2>

<hr />

<blockquote>
  <p><strong>Role</strong>은 Task, Handler, 변수, 정적 File과 Template을 정해진 Directory 구조로 묶어 재사용하는 단위이다.</p>
</blockquote>

<p>하나의 Playbook에 모든 내용을 넣으면 다른 Project에서 일부 기능만 재사용하기 어렵고 여러 사람이 같은 File을 동시에 수정하게 된다. Role은 Web Server, Database 또는 공통 OS 설정처럼 독립된 책임으로 자동화 내용을 나눈다.</p>

<p>Role을 사용하면 다음과 같은 이점이 있다.</p>

<ul>
  <li>
    <p>관련 Task, Handler와 File을 한 Directory에서 관리한다.</p>
  </li>
  <li>
    <p>기본값과 환경별 Override 값을 분리한다.</p>
  </li>
  <li>
    <p>여러 Playbook에서 같은 구성을 재사용한다.</p>
  </li>
  <li>
    <p>Role 단위로 Test, Version과 의존성을 관리한다.</p>
  </li>
  <li>
    <p>Ansible Galaxy 또는 내부 Repository를 통해 공유한다.</p>
  </li>
</ul>

<h2 id="2--role-생성과-directory-구조">2 ) Role 생성과 Directory 구조</h2>

<hr />

<p>Control Node의 Project Root에서 <code class="language-plaintext highlighter-rouge">webserver</code> Role 골격을 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> roles
ansible-galaxy role init roles/webserver
tree roles/webserver
</code></pre></div></div>

<p>주요 Directory의 역할은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>경로</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tasks/main.yml</code></td>
      <td>Role이 수행할 기본 Task</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">handlers/main.yml</code></td>
      <td>Role Task가 <code class="language-plaintext highlighter-rouge">notify</code>하는 Handler</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">defaults/main.yml</code></td>
      <td>다른 변수로 쉽게 Override할 수 있는 낮은 우선순위 기본값</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">vars/main.yml</code></td>
      <td>Role 내부에서 강하게 유지할 높은 우선순위 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">files/</code></td>
      <td><code class="language-plaintext highlighter-rouge">copy</code> 등에서 사용하는 정적 File</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">templates/</code></td>
      <td>Jinja2 Template File</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">meta/main.yml</code></td>
      <td>Role Metadata와 의존 Role</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tests/</code></td>
      <td>Role 동작 확인용 Inventory와 Playbook</td>
    </tr>
  </tbody>
</table>

<p>사용하지 않는 Directory는 반드시 유지할 필요가 없다. Role의 <code class="language-plaintext highlighter-rouge">files/</code>와 <code class="language-plaintext highlighter-rouge">templates/</code> Resource는 Role Root를 기준으로 찾으므로 <code class="language-plaintext highlighter-rouge">../files/index.html</code> 같은 상대 경로를 만들 필요가 없다.</p>

<h2 id="3--apache-web-server-role-작성">3 ) Apache Web Server Role 작성</h2>

<hr />

<p>이번 Role은 Ubuntu와 Debian 계열 Managed Node에 Apache를 설치하고 정적 Page를 배포한다.</p>

<h3 id="31-override-가능한-기본값">3.1 Override 가능한 기본값</h3>

<p><code class="language-plaintext highlighter-rouge">roles/webserver/defaults/main.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">webserver_title</span><span class="pi">:</span> <span class="s">Apache Web Server</span>
<span class="na">webserver_package</span><span class="pi">:</span> <span class="s">apache2</span>
<span class="na">webserver_service</span><span class="pi">:</span> <span class="s">apache2</span>
<span class="na">webserver_document_root</span><span class="pi">:</span> <span class="s">/var/www/html</span>
<span class="na">webserver_security_config</span><span class="pi">:</span> <span class="s">/etc/apache2/conf-available/security.conf</span>
<span class="na">webserver_index_mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
<span class="na">webserver_supported_os_families</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">Debian</span>
</code></pre></div></div>

<p>사용자가 환경별로 바꿀 가능성이 있는 값은 <code class="language-plaintext highlighter-rouge">defaults</code>에 둔다. <code class="language-plaintext highlighter-rouge">vars/main.yml</code>은 우선순위가 높아 Inventory 값으로 바꾸기 어려우므로 반드시 고정해야 하는 값에만 사용한다.</p>

<h3 id="32-정적-page">3.2 정적 Page</h3>

<p><code class="language-plaintext highlighter-rouge">roles/webserver/files/index.html</code>을 작성한다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!doctype html&gt;</span>
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"en"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"utf-8"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;title&gt;</span>Hello from Ansible<span class="nt">&lt;/title&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;h1&gt;</span>Hello! Ansible Role<span class="nt">&lt;/h1&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>Host별 값이 들어가야 한다면 정적 <code class="language-plaintext highlighter-rouge">files</code> 대신 <code class="language-plaintext highlighter-rouge">templates/index.html.j2</code>와 <code class="language-plaintext highlighter-rouge">ansible.builtin.template</code> Module을 사용한다.</p>

<h3 id="33-task">3.3 Task</h3>

<p><code class="language-plaintext highlighter-rouge">roles/webserver/tasks/main.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Verify the managed operating system family</span>
  <span class="na">ansible.builtin.assert</span><span class="pi">:</span>
    <span class="na">that</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">ansible_facts['os_family'] in webserver_supported_os_families</span>
    <span class="na">fail_msg</span><span class="pi">:</span> <span class="pi">&gt;-</span>
      <span class="s">{{ ansible_facts['os_family'] }} is not supported by this role.</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install the web server package</span>
  <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_package</span><span class="nv"> </span><span class="s">}}"</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
    <span class="na">update_cache</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy the web server index page</span>
  <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
    <span class="na">src</span><span class="pi">:</span> <span class="s">index.html</span>
    <span class="na">dest</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_document_root</span><span class="nv"> </span><span class="s">}}/index.html"</span>
    <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
    <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
    <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_index_mode</span><span class="nv"> </span><span class="s">}}"</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Limit the Apache server signature</span>
  <span class="na">ansible.builtin.lineinfile</span><span class="pi">:</span>
    <span class="na">path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_security_config</span><span class="nv"> </span><span class="s">}}"</span>
    <span class="na">regexp</span><span class="pi">:</span> <span class="s1">'</span><span class="s">^ServerTokens</span><span class="nv"> </span><span class="s">'</span>
    <span class="na">line</span><span class="pi">:</span> <span class="s">ServerTokens Prod</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">notify</span><span class="pi">:</span> <span class="s">Restart web server</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure the web server is enabled and running</span>
  <span class="na">ansible.builtin.service</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_service</span><span class="nv"> </span><span class="s">}}"</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">state: latest</code>는 Repository의 새 Version이 공개될 때마다 Package를 변경할 수 있다. 재현 가능한 기본 구성에는 <code class="language-plaintext highlighter-rouge">present</code>를 사용하고 Upgrade는 별도 절차로 관리한다.</p>

<h3 id="34-handler">3.4 Handler</h3>

<p><code class="language-plaintext highlighter-rouge">roles/webserver/handlers/main.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Restart web server</span>
  <span class="na">ansible.builtin.service</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">webserver_service</span><span class="nv"> </span><span class="s">}}"</span>
    <span class="na">state</span><span class="pi">:</span> <span class="s">restarted</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<h3 id="35-role-호출">3.5 Role 호출</h3>

<p><code class="language-plaintext highlighter-rouge">role-example.yml</code>에서 <code class="language-plaintext highlighter-rouge">import_role</code>로 정적으로 불러온다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure web servers with a role</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">lab</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print the start of the role play</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">msg</span><span class="pi">:</span> <span class="s">Start the webserver role.</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Import the webserver role</span>
      <span class="na">ansible.builtin.import_role</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">webserver</span>
</code></pre></div></div>

<p>문법과 예상 변경을 확인한 뒤 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> role-example.yml
ansible-playbook <span class="nt">--check</span> <span class="nt">--diff</span> role-example.yml
ansible-playbook role-example.yml
</code></pre></div></div>

<p>Role을 찾지 못하면 Project의 <code class="language-plaintext highlighter-rouge">ansible.cfg</code>에 Search Path를 지정한다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[defaults]</span>
<span class="py">inventory</span> <span class="p">=</span> <span class="s">./inventory.ini</span>
<span class="py">roles_path</span> <span class="p">=</span> <span class="s">./roles</span>
</code></pre></div></div>

<h2 id="4--ansible-galaxy의-role-관리">4 ) Ansible Galaxy의 Role 관리</h2>

<hr />

<blockquote>
  <p><strong>Ansible Galaxy</strong>는 Role과 Collection을 검색하고 배포하는 공개 Service이다.</p>
</blockquote>

<p>Role 관련 명령을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy role <span class="nt">--help</span>
</code></pre></div></div>

<p>PostgreSQL Role을 검색하고 선택한 Role의 Metadata를 확인하는 기본 흐름은 다음과 같다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy role search postgresql <span class="nt">--platforms</span> Ubuntu
ansible-galaxy role info &lt;namespace.role_name&gt;
ansible-galaxy role <span class="nb">install</span> <span class="nt">-p</span> roles &lt;namespace.role_name&gt;
ansible-galaxy role list <span class="nt">-p</span> roles
</code></pre></div></div>

<p>자료에서 사용한 <code class="language-plaintext highlighter-rouge">buluma.postgres</code>는 Galaxy 명령 흐름을 확인하기 위한 특정 시점의 예시이다. Role의 존재 여부, 최신 Release, 지원 OS와 유지보수 상태는 바뀔 수 있으므로 설치 전에 다시 검색한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy role info buluma.postgres
</code></pre></div></div>

<p>더 이상 사용하지 않는 Role을 Project Directory에서 제거한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy role remove &lt;namespace.role_name&gt;
</code></pre></div></div>

<p>Galaxy에 공개됐다는 사실만으로 Code의 보안과 품질이 검증됐다고 판단할 수 없다. 설치 전에 Source Repository, License, Release 이력, 지원 Platform, 의존 Role과 Task가 수행하는 Privileged 작업을 확인한다.</p>

<h2 id="5--content-collection">5 ) Content Collection</h2>

<hr />

<blockquote>
  <p><strong>Collection</strong>은 Module, Plugin, Role, Playbook과 문서를 Namespace 단위로 묶어 Ansible Core와 독립적으로 배포하는 형식이다.</p>
</blockquote>

<p>초기의 Ansible은 많은 Module을 Core Package와 함께 배포했다. Collection은 Core Release와 Module·Plugin Release를 분리하고 필요한 Version을 Project별로 선택할 수 있게 한다.</p>

<p><code class="language-plaintext highlighter-rouge">ansible.builtin.copy</code>는 Builtin Collection의 FQCN이고 <code class="language-plaintext highlighter-rouge">community.docker.docker_container</code>는 <code class="language-plaintext highlighter-rouge">community.docker</code> Collection의 Module이다.</p>

<p>설치된 Collection을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy collection list
ansible-doc community.docker.docker_container
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">community.docker</code>가 없다면 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy collection <span class="nb">install </span>community.docker
</code></pre></div></div>

<p>Project에서 의존성을 재현하려면 검증한 Version을 <code class="language-plaintext highlighter-rouge">requirements.yml</code>에 기록한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">collections</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">community.docker</span>
    <span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">&lt;VERIFIED-VERSION&gt;"</span>
</code></pre></div></div>

<p>Placeholder를 설치 시점에 검증한 실제 Version으로 바꾼 뒤 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-galaxy collection <span class="nb">install</span> <span class="nt">-r</span> requirements.yml
ansible-galaxy collection list community.docker
</code></pre></div></div>

<p>Collection을 Offline 환경으로 전달해야 한다면 의존성을 포함한 Artifact와 생성된 <code class="language-plaintext highlighter-rouge">requirements.yml</code>을 Download할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> collection-bundle
ansible-galaxy collection download <span class="se">\</span>
  <span class="nt">-p</span> collection-bundle <span class="se">\</span>
  community.docker
</code></pre></div></div>

<h2 id="6--docker-설치-자동화-준비">6 ) Docker 설치 자동화 준비</h2>

<hr />

<p>Docker 설치는 <code class="language-plaintext highlighter-rouge">docker_hosts</code> Inventory Group에만 적용한다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[docker_hosts]</span>
<span class="err">master</span>
<span class="err">worker1</span>
<span class="err">worker2</span>
</code></pre></div></div>

<p>Docker의 현재 Ubuntu 설치 문서는 One-line <code class="language-plaintext highlighter-rouge">docker.list</code>보다 DEB822 형식의 <code class="language-plaintext highlighter-rouge">/etc/apt/sources.list.d/docker.sources</code>를 안내한다. 배포판 Codename과 Architecture를 고정 문자열로 넣지 않고 Managed Node에서 확인한다.</p>

<p><code class="language-plaintext highlighter-rouge">install-docker.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install Docker Engine from the official repository</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">docker_hosts</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install repository prerequisites</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">ca-certificates</span>
          <span class="pi">-</span> <span class="s">curl</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
        <span class="na">update_cache</span><span class="pi">:</span> <span class="no">true</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create the APT keyring directory</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/etc/apt/keyrings</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">directory</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0755"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Download the Docker repository signing key</span>
      <span class="na">ansible.builtin.get_url</span><span class="pi">:</span>
        <span class="na">url</span><span class="pi">:</span> <span class="s">https://download.docker.com/linux/ubuntu/gpg</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/apt/keyrings/docker.asc</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Read the Debian package architecture</span>
      <span class="na">ansible.builtin.command</span><span class="pi">:</span> <span class="s">dpkg --print-architecture</span>
      <span class="na">register</span><span class="pi">:</span> <span class="s">docker_dpkg_architecture</span>
      <span class="na">changed_when</span><span class="pi">:</span> <span class="no">false</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure the Docker DEB822 repository</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/apt/sources.list.d/docker.sources</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
        <span class="na">content</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">Types: deb</span>
          <span class="s">URIs: https://download.docker.com/linux/ubuntu</span>
          <span class="s">Suites: {{ ansible_facts['distribution_release'] }}</span>
          <span class="s">Components: stable</span>
          <span class="s">Architectures: {{ docker_dpkg_architecture.stdout }}</span>
          <span class="s">Signed-By: /etc/apt/keyrings/docker.asc</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install Docker Engine packages</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">docker-ce</span>
          <span class="pi">-</span> <span class="s">docker-ce-cli</span>
          <span class="pi">-</span> <span class="s">containerd.io</span>
          <span class="pi">-</span> <span class="s">docker-buildx-plugin</span>
          <span class="pi">-</span> <span class="s">docker-compose-plugin</span>
          <span class="pi">-</span> <span class="s">python3-requests</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
        <span class="na">update_cache</span><span class="pi">:</span> <span class="no">true</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure Docker is enabled and running</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">docker</span>
        <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>
</code></pre></div></div>

<p>자료의 <code class="language-plaintext highlighter-rouge">docker-ce-li</code>는 <code class="language-plaintext highlighter-rouge">docker-ce-cli</code>의 오탈자이다. 또한 Ubuntu <code class="language-plaintext highlighter-rouge">noble</code>, <code class="language-plaintext highlighter-rouge">amd64</code>를 고정하면 다른 Ubuntu Release와 ARM64 Host에서 실패하므로 Facts와 <code class="language-plaintext highlighter-rouge">dpkg</code> 결과를 사용했다.</p>

<p>자료의 Container 예제는 <code class="language-plaintext highlighter-rouge">python3-docker</code>를 설치했지만 현재 <code class="language-plaintext highlighter-rouge">community.docker</code>의 해당 Module은 Docker SDK for Python을 직접 사용하지 않고 <code class="language-plaintext highlighter-rouge">requests</code>를 요구한다. 설치한 Collection Version의 공식 Requirements를 확인하고 이 예제에서는 Managed Node에 <code class="language-plaintext highlighter-rouge">python3-requests</code>를 설치한다.</p>

<p>문법을 검사하고 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> install-docker.yml
ansible-playbook <span class="nt">--check</span> <span class="nt">--diff</span> install-docker.yml
ansible-playbook install-docker.yml <span class="nt">-K</span>
</code></pre></div></div>

<p>Check Mode에서는 APT Repository가 아직 생성되지 않아 뒤의 Package 설치 결과를 완전히 예측하지 못할 수 있다.</p>

<p>설치 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible docker_hosts <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"docker --version"</span>

ansible docker_hosts <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"systemctl is-active docker"</span>
</code></pre></div></div>

<p>현재 Docker Ubuntu Repository 형식과 Package 목록은 <a href="https://docs.docker.com/engine/install/ubuntu/">Docker Engine Ubuntu 설치 문서</a>에서 확인한다.</p>

<h2 id="7--nginx-container-배포">7 ) Nginx Container 배포</h2>

<hr />

<p>자료의 <code class="language-plaintext highlighter-rouge">community.gerneral</code>은 존재하지 않는 오탈자이다. Docker Resource는 <code class="language-plaintext highlighter-rouge">community.docker</code> Collection의 FQCN을 사용한다. <code class="language-plaintext highlighter-rouge">nginx-latest</code>도 Docker Hub의 일반적인 Tag 표기인 <code class="language-plaintext highlighter-rouge">nginx:latest</code>로 수정한다.</p>

<p><code class="language-plaintext highlighter-rouge">deploy-docker.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy an Nginx container</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">docker_hosts</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">vars</span><span class="pi">:</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">my-web-server</span>
    <span class="na">image_name</span><span class="pi">:</span> <span class="s">nginx:latest</span>
    <span class="na">host_port</span><span class="pi">:</span> <span class="m">8080</span>
    <span class="na">container_port</span><span class="pi">:</span> <span class="m">80</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Pull the Nginx image</span>
      <span class="na">community.docker.docker_image_pull</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">image_name</span><span class="nv"> </span><span class="s">}}"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create and start the Nginx container</span>
      <span class="na">community.docker.docker_container</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">container_name</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">image_name</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>
        <span class="na">restart_policy</span><span class="pi">:</span> <span class="s">always</span>
        <span class="na">published_ports</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s2">"</span><span class="s">{{</span><span class="nv"> </span><span class="s">host_port</span><span class="nv"> </span><span class="s">}}:{{</span><span class="nv"> </span><span class="s">container_port</span><span class="nv"> </span><span class="s">}}"</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">MY_ENV_VAR</span><span class="pi">:</span> <span class="s">hello-ansible</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print the published endpoint</span>
      <span class="na">ansible.builtin.debug</span><span class="pi">:</span>
        <span class="na">msg</span><span class="pi">:</span> <span class="pi">&gt;-</span>
          <span class="s">{{ inventory_hostname }} exposes {{ container_name }}</span>
          <span class="s">on TCP {{ host_port }}.</span>
</code></pre></div></div>

<p>과거 예제에서 <code class="language-plaintext highlighter-rouge">community.docker.docker_image</code>로 Image를 Pull할 수 있었고 현재도 지원되지만, 현재 Collection 문서는 Pull 전용 작업에 <code class="language-plaintext highlighter-rouge">community.docker.docker_image_pull</code> 사용을 권장한다. 설치한 Collection Version의 요구 Package와 Parameter는 <code class="language-plaintext highlighter-rouge">ansible-doc</code>으로 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-doc community.docker.docker_image_pull
ansible-doc community.docker.docker_container
ansible-playbook <span class="nt">--syntax-check</span> deploy-docker.yml
ansible-playbook deploy-docker.yml <span class="nt">-K</span>
</code></pre></div></div>

<p>Container와 HTTP 응답을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible docker_hosts <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"docker ps --filter name=my-web-server"</span>

curl http://192.0.2.11:8080
curl http://192.0.2.12:8080
curl http://192.0.2.13:8080
</code></pre></div></div>

<p>다른 Computer에서 접속한다면 Managed Node의 Firewall과 Network에서 TCP 8080 접근이 허용되어 있어야 한다.</p>

<h2 id="8--문제-해결">8 ) 문제 해결</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Role을 찾지 못함</td>
      <td><code class="language-plaintext highlighter-rouge">roles/</code> 위치와 <code class="language-plaintext highlighter-rouge">roles_path</code></td>
    </tr>
    <tr>
      <td>Role 변수 Override가 적용되지 않음</td>
      <td>값이 <code class="language-plaintext highlighter-rouge">defaults</code>가 아니라 우선순위가 높은 <code class="language-plaintext highlighter-rouge">vars</code>에 있는지 확인</td>
    </tr>
    <tr>
      <td>Collection Module을 찾지 못함</td>
      <td><code class="language-plaintext highlighter-rouge">ansible-galaxy collection list</code>, <code class="language-plaintext highlighter-rouge">requirements.yml</code> 설치 여부</td>
    </tr>
    <tr>
      <td>Docker APT 서명 오류</td>
      <td><code class="language-plaintext highlighter-rouge">docker.asc</code> Permission과 <code class="language-plaintext highlighter-rouge">Signed-By</code> 경로</td>
    </tr>
    <tr>
      <td>Docker Package를 찾지 못함</td>
      <td>Ubuntu Codename, Architecture와 <code class="language-plaintext highlighter-rouge">apt update</code> 결과</td>
    </tr>
    <tr>
      <td>Docker Socket 권한 오류</td>
      <td><code class="language-plaintext highlighter-rouge">become</code> 사용 여부와 <code class="language-plaintext highlighter-rouge">/var/run/docker.sock</code> Permission</td>
    </tr>
    <tr>
      <td>Container Module Parameter 오류</td>
      <td>설치된 Collection Version의 <code class="language-plaintext highlighter-rouge">ansible-doc</code> 결과</td>
    </tr>
    <tr>
      <td>TCP 8080 접속 실패</td>
      <td>Container Port Mapping, Host Firewall과 Network 경로</td>
    </tr>
    <tr>
      <td>모든 Host에서 Container가 생성됨</td>
      <td><code class="language-plaintext highlighter-rouge">hosts: docker_hosts</code> Group과 <code class="language-plaintext highlighter-rouge">--list-hosts</code> 결과</td>
    </tr>
  </tbody>
</table>

<p>Role 구조는 <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_reuse_roles.html">Ansible Roles</a>, Collection Module은 <a href="https://docs.ansible.com/projects/ansible/latest/collections/community/docker/index.html">Community.Docker</a>과 <a href="https://docs.ansible.com/projects/ansible/latest/collections/community/docker/docker_container_module.html">docker_container Module</a>에서 확인할 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Role은 Task, Handler, 변수와 File을 표준 Directory 구조로 묶어 Playbook을 재사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">defaults</code>는 쉽게 Override할 값, <code class="language-plaintext highlighter-rouge">vars</code>는 높은 우선순위가 필요한 내부 값에 제한해 사용한다.</p>
    </li>
    <li>
      <p>Galaxy의 공개 Role은 설치 전에 Source, 지원 Platform, Version과 Privileged 작업을 검토한다.</p>
    </li>
    <li>
      <p>Collection은 Module과 Plugin을 Core와 독립적으로 배포하며 Playbook에서는 FQCN을 사용한다.</p>
    </li>
    <li>
      <p>Docker 설치는 현재 DEB822 Repository 형식과 실제 Architecture를 사용하고 Container는 <code class="language-plaintext highlighter-rouge">community.docker</code> Module로 원하는 상태를 관리한다.</p>
    </li>
    <li>
      <p>Container 제거, Docker Package 제거와 전체 Data 삭제는 영향 범위가 다르므로 별도 절차로 분리한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Ansible" /><summary type="html"><![CDATA[Ansible Role의 표준 구조와 Galaxy·Collection의 관계를 이해하고 현재 Docker APT Repository와 community.docker Module로 Container 배포를 자동화한다]]></summary></entry><entry><title type="html">Ansible Architecture와 Inventory 구성</title><link href="https://hyn128.site/cloud-native-58-ansible-architecture-inventory/" rel="alternate" type="text/html" title="Ansible Architecture와 Inventory 구성" /><published>2026-09-22T00:00:00+09:00</published><updated>2026-09-22T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-58-ansible-architecture-inventory</id><content type="html" xml:base="https://hyn128.site/cloud-native-58-ansible-architecture-inventory/"><![CDATA[<p><a href="/cloud-native-57-argocd-git-repository-manual-sync/">Argo CD Git Repository 연동과 수동 Sync</a>에서는 Git의 Desired State를 Kubernetes Cluster에 반영하는 GitOps 흐름을 구성했다. 이번에는 Ansible이 Control Node에서 여러 Managed Node의 Package, File과 Service 상태를 일관되게 관리하는 구조를 살펴보고 실제 연결 환경을 준비한다.</p>

<h2 id="1--iac와-ansible의-역할">1 ) IaC와 Ansible의 역할</h2>

<hr />

<blockquote>
  <p><strong>Infrastructure as Code(IaC)</strong>는 Infrastructure의 구성과 원하는 상태를 Code 또는 선언 File로 관리하고, 변경 이력을 Version Control System에 남기는 방식이다.</p>
</blockquote>

<p>Infrastructure 자동화 도구는 담당하는 범위에 따라 역할이 다르다. Terraform과 Ansible은 모두 자동화에 사용하지만 중심 영역이 다르다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Terraform</th>
      <th>Ansible</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>중심 역할</td>
      <td>Infrastructure Provisioning과 수명 주기 관리</td>
      <td>Server 구성 관리와 IT 자동화</td>
    </tr>
    <tr>
      <td>정의 언어</td>
      <td>HCL</td>
      <td>YAML</td>
    </tr>
    <tr>
      <td>확장 단위</td>
      <td>Provider</td>
      <td>Module, Plugin과 Collection</td>
    </tr>
    <tr>
      <td>일반적인 대상</td>
      <td>Cloud, Network, VM과 Managed Service</td>
      <td>Package, File, Service, Application과 장비 설정</td>
    </tr>
    <tr>
      <td>Managed Node Agent</td>
      <td>별도 Agent 불필요</td>
      <td>별도 Agent 불필요</td>
    </tr>
  </tbody>
</table>

<p>두 도구의 영역이 완전히 분리되는 것은 아니다. Terraform으로 Network와 VM을 생성한 뒤 Ansible로 각 VM의 Package와 설정 File을 배포하는 방식으로 함께 사용할 수 있다. Terraform과 다른 IaC 도구의 관계는 <a href="/cloud-02-technology/">Cloud 기술 개요</a>에서, CI/CD Pipeline에서 Ansible이 담당하는 위치는 <a href="/cloud-native-45-cicd-pipeline/">CI/CD Pipeline 구조와 도구</a>에서 확인할 수 있다.</p>

<p>Ansible은 다음과 같은 작업을 자동화할 수 있다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">apt</code>, <code class="language-plaintext highlighter-rouge">dnf</code>와 같은 Package Manager를 이용한 Software 설치와 제거</p>
  </li>
  <li>
    <p>설정 File, Script와 Application Artifact 배포</p>
  </li>
  <li>
    <p>Service 시작, 중지와 재시작</p>
  </li>
  <li>
    <p>HTTP 또는 FTP Resource Download</p>
  </li>
  <li>
    <p>Git Repository Checkout</p>
  </li>
  <li>
    <p>Cloud, Virtualization, Network와 Container Resource 관리</p>
  </li>
  <li>
    <p>Build와 Test 명령 실행</p>
  </li>
</ul>

<h2 id="2--ansible의-동작-특성">2 ) Ansible의 동작 특성</h2>

<hr />

<p>Ansible은 Python으로 작성된 Agentless 자동화 도구이다. 사용자가 Control Node에서 명령을 실행하면 Ansible이 Inventory를 이용해 대상을 선택하고 Module을 실행한다.</p>

<table>
  <thead>
    <tr>
      <th>특성</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Agentless</td>
      <td>Managed Node에 상시 실행되는 Ansible Agent를 설치하지 않는다.</td>
    </tr>
    <tr>
      <td>Push 방식</td>
      <td>Control Node가 SSH 등의 연결을 통해 작업을 Managed Node에 전달한다.</td>
    </tr>
    <tr>
      <td>선언적 상태</td>
      <td>다수의 Module이 실행 절차보다 원하는 최종 상태를 매개변수로 받는다.</td>
    </tr>
    <tr>
      <td>멱등성</td>
      <td>멱등성을 지원하는 Module은 이미 원하는 상태이면 추가 변경을 만들지 않는다.</td>
    </tr>
    <tr>
      <td>순서 보장</td>
      <td>기본 Linear Strategy에서는 현재 Task가 대상 Host에서 처리된 뒤 다음 Task로 진행한다.</td>
    </tr>
    <tr>
      <td>병렬 실행</td>
      <td>선택한 Host를 병렬로 처리하지만 한 번에 실행하는 수는 <code class="language-plaintext highlighter-rouge">forks</code> 설정의 제한을 받는다.</td>
    </tr>
  </tbody>
</table>

<p>멱등성은 모든 명령이 한 번만 실행된다는 뜻이 아니다. <code class="language-plaintext highlighter-rouge">ansible.builtin.apt</code>, <code class="language-plaintext highlighter-rouge">ansible.builtin.copy</code>처럼 현재 상태를 확인하는 Module은 불필요한 변경을 피할 수 있다. 반면 <code class="language-plaintext highlighter-rouge">ansible.builtin.command</code>와 <code class="language-plaintext highlighter-rouge">ansible.builtin.shell</code>은 별도 조건을 설정하지 않으면 실행할 때마다 명령을 수행한다.</p>

<p>실행 결과에는 Host별 상태가 표시된다.</p>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ok</code></td>
      <td>Task가 정상 처리됐고 상태 변경은 없었다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">changed</code></td>
      <td>Task가 정상 처리됐으며 Managed Node의 상태가 변경됐다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">failed</code></td>
      <td>Managed Node에 연결했지만 Task 실행이 실패했다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unreachable</code></td>
      <td>Network, SSH 또는 인증 문제로 Managed Node에 연결하지 못했다.</td>
    </tr>
  </tbody>
</table>

<h2 id="3--ansible-architecture">3 ) Ansible Architecture</h2>

<hr />

<p>Ansible 환경은 자동화를 시작하는 Control Node와 실제 작업 대상인 Managed Node로 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>실행 위치</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Control Node</td>
      <td>Ansible 실행 Host</td>
      <td>Ansible Core를 실행하고 Inventory와 Playbook을 해석한다.</td>
    </tr>
    <tr>
      <td>Managed Node</td>
      <td>원격 관리 대상</td>
      <td>Module이 전달한 작업을 실행하고 결과를 반환한다.</td>
    </tr>
    <tr>
      <td>Inventory</td>
      <td>Control Node</td>
      <td>관리할 Host, Group과 연결 변수를 정의한다.</td>
    </tr>
    <tr>
      <td>Playbook</td>
      <td>Control Node</td>
      <td>Managed Node에 적용할 Play와 Task를 YAML로 정의한다.</td>
    </tr>
    <tr>
      <td>Module</td>
      <td>주로 Managed Node</td>
      <td>Package, File과 Service 같은 실제 작업 단위를 수행한다.</td>
    </tr>
    <tr>
      <td>Plugin</td>
      <td>주로 Control Node</td>
      <td>연결, Inventory 처리, Data 변환과 출력 기능을 확장한다.</td>
    </tr>
  </tbody>
</table>

<figure class="post-visual ansible-flow" aria-labelledby="ansible-control-managed-node-caption">
  <figcaption id="ansible-control-managed-node-caption" class="post-visual__caption">
    Control Node가 Inventory와 Playbook을 읽고 SSH로 Module을 실행한 뒤 Managed Node의 결과를 집계하는 흐름
  </figcaption>

  <div class="post-visual__frame pipeline-flow__frame">
    <ol class="pipeline-flow__phases">
      <li class="pipeline-flow__phase ansible-flow__phase--control" data-edge="자동화 정의 읽기">
        <div class="pipeline-flow__heading">
          <span class="pipeline-flow__role">CONTROL NODE</span>
          <strong>Ansible 실행 환경</strong>
        </div>
        <p class="ansible-flow__description">Ansible Core가 설치된 관리 시작점이다. 운영자가 Ad-hoc 명령이나 Playbook 실행을 요청한다.</p>
        <div class="ansible-flow__command">ansible lab -m ansible.builtin.ping</div>
      </li>

      <li class="pipeline-flow__phase ansible-flow__phase--definition" data-edge="대상·작업 결정">
        <div class="pipeline-flow__heading">
          <span class="pipeline-flow__role">AUTOMATION DEFINITION</span>
          <strong>누구에게 어떤 상태를 적용할지 정의</strong>
        </div>
        <ul class="pipeline-flow__steps pipeline-flow__steps--two">
          <li>
            <strong>Inventory</strong>
            <span>Managed Node와 Group, 접속 변수를 정의한다.</span>
          </li>
          <li>
            <strong>Playbook</strong>
            <span>Play, Task와 Module을 YAML 순서로 정의한다.</span>
          </li>
          <li>
            <strong>ansible.cfg</strong>
            <span>Project의 기본 Inventory와 실행 Option을 지정한다.</span>
          </li>
          <li>
            <strong>Module·Plugin</strong>
            <span>Module은 대상 작업을, Plugin은 Control Node 기능을 확장한다.</span>
          </li>
        </ul>
      </li>

      <li class="pipeline-flow__phase ansible-flow__phase--connection" data-edge="Module 실행">
        <div class="pipeline-flow__heading">
          <span class="pipeline-flow__role">CONNECTION</span>
          <strong>SSH 인증과 Host 검증</strong>
        </div>
        <p class="ansible-flow__description">Control Node가 검증된 Host Key와 등록된 공개키를 이용해 대상에 접속하고 필요한 Module을 전송한다.</p>
      </li>

      <li class="pipeline-flow__phase ansible-flow__phase--managed" data-edge="결과 반환">
        <div class="pipeline-flow__heading">
          <span class="pipeline-flow__role">MANAGED NODES</span>
          <strong>원하는 상태를 실제 시스템에 반영</strong>
        </div>
        <p class="ansible-flow__description">Ansible Agent는 설치하지 않는다. 일반적인 POSIX Module 실행에는 Python과 SSH 접속 계정이 필요하다.</p>
        <ul class="pipeline-flow__steps ansible-flow__nodes">
          <li>
            <strong>master</strong>
            <span>Package·File·Service 상태 적용</span>
          </li>
          <li>
            <strong>worker1</strong>
            <span>같은 Task를 독립적으로 실행</span>
          </li>
          <li>
            <strong>worker2</strong>
            <span>실행 결과와 변경 여부 반환</span>
          </li>
        </ul>
      </li>

      <li class="pipeline-flow__phase ansible-flow__phase--result">
        <div class="pipeline-flow__heading">
          <span class="pipeline-flow__role">EXECUTION RESULT</span>
          <strong>Host별 상태를 Control Node에서 집계</strong>
        </div>
        <ul class="pipeline-flow__steps pipeline-flow__steps--two">
          <li>
            <strong>ok</strong>
            <span>이미 원하는 상태이거나 정상 확인됨</span>
          </li>
          <li>
            <strong>changed</strong>
            <span>Managed Node의 상태가 실제로 변경됨</span>
          </li>
          <li>
            <strong>failed</strong>
            <span>연결 후 Task 실행이 실패함</span>
          </li>
          <li>
            <strong>unreachable</strong>
            <span>SSH·인증·Network 문제로 대상에 도달하지 못함</span>
          </li>
        </ul>
      </li>
    </ol>
  </div>
</figure>

<p>Control Node는 Python이 설치된 Linux, macOS, BSD와 Windows Subsystem for Linux 환경에서 구성할 수 있다. WSL을 사용하지 않는 Native Windows는 Control Node로 지원되지 않는다.</p>

<p>일반적인 POSIX Managed Node에는 다음 조건이 필요하다.</p>

<ul>
  <li>
    <p>Control Node에서 접속할 수 있는 SSH Service와 사용자 계정</p>
  </li>
  <li>
    <p>Ansible Module이 생성한 Python Code를 실행할 수 있는 Python</p>
  </li>
  <li>
    <p>Package 설치나 System File 변경에 필요한 <code class="language-plaintext highlighter-rouge">sudo</code> 또는 다른 Privilege Escalation 수단</p>
  </li>
</ul>

<p>Network 장비 전용 Module처럼 Managed Node의 Python을 요구하지 않는 예외도 있으므로 사용하는 Module의 요구 조건을 확인한다.</p>

<p>Ansible의 기본 실행 흐름은 다음과 같다.</p>

<ol>
  <li>
    <p>Control Node가 Inventory를 읽고 Host Pattern에 해당하는 Managed Node를 선택한다.</p>
  </li>
  <li>
    <p>Ad-hoc 명령 또는 Playbook의 Task에서 실행할 Module과 Argument를 결정한다.</p>
  </li>
  <li>
    <p>SSH 연결로 대상 Host를 인증하고 필요한 Module Code를 실행한다.</p>
  </li>
  <li>
    <p>Managed Node가 현재 상태를 확인하고 필요한 변경을 수행한다.</p>
  </li>
  <li>
    <p>Control Node가 Host별 <code class="language-plaintext highlighter-rouge">ok</code>, <code class="language-plaintext highlighter-rouge">changed</code>, <code class="language-plaintext highlighter-rouge">failed</code>, <code class="language-plaintext highlighter-rouge">unreachable</code> 결과를 집계한다.</p>
  </li>
</ol>

<h2 id="4--실습-구성">4 ) 실습 구성</h2>

<hr />

<p>하나의 Control Node와 세 개의 Managed Node를 사용한다. 아래 IP는 문서용 예시 주소이므로 실제 환경의 주소로 바꾼다.</p>

<table>
  <thead>
    <tr>
      <th>Hostname</th>
      <th>역할</th>
      <th>예시 IP</th>
      <th>Ansible 설치</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">control</code></td>
      <td>Control Node</td>
      <td><code class="language-plaintext highlighter-rouge">192.0.2.10</code></td>
      <td>필요</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">master</code></td>
      <td>Managed Node</td>
      <td><code class="language-plaintext highlighter-rouge">192.0.2.11</code></td>
      <td>불필요</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">worker1</code></td>
      <td>Managed Node</td>
      <td><code class="language-plaintext highlighter-rouge">192.0.2.12</code></td>
      <td>불필요</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">worker2</code></td>
      <td>Managed Node</td>
      <td><code class="language-plaintext highlighter-rouge">192.0.2.13</code></td>
      <td>불필요</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">master</code>, <code class="language-plaintext highlighter-rouge">worker1</code>, <code class="language-plaintext highlighter-rouge">worker2</code>는 Kubernetes 역할을 설명하기 위한 이름이 아니라 Ansible이 관리할 세 Linux Host의 식별자이다. Ansible은 세 Host를 같은 Inventory Group으로 묶어 동일한 작업을 적용한다.</p>

<p>작업 위치를 구분하면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>작업</th>
      <th style="text-align: right">Control Node</th>
      <th style="text-align: right">Managed Node</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Ansible 설치</td>
      <td style="text-align: right">O</td>
      <td style="text-align: right">X</td>
    </tr>
    <tr>
      <td>자동화 전용 계정 생성</td>
      <td style="text-align: right">X</td>
      <td style="text-align: right">O</td>
    </tr>
    <tr>
      <td>SSH Key 생성</td>
      <td style="text-align: right">O</td>
      <td style="text-align: right">X</td>
    </tr>
    <tr>
      <td>Public Key 등록</td>
      <td style="text-align: right">전송</td>
      <td style="text-align: right">저장</td>
    </tr>
    <tr>
      <td>Inventory와 Playbook 작성</td>
      <td style="text-align: right">O</td>
      <td style="text-align: right">X</td>
    </tr>
    <tr>
      <td>Module 실행</td>
      <td style="text-align: right">요청·결과 수집</td>
      <td style="text-align: right">실제 처리</td>
    </tr>
  </tbody>
</table>

<h2 id="5--ubuntu-control-node에-ansible-설치">5 ) Ubuntu Control Node에 Ansible 설치</h2>

<hr />

<p>다음 명령은 <code class="language-plaintext highlighter-rouge">control</code> Host에서 실행한다. Ubuntu PPA를 등록할 때 사용하는 <code class="language-plaintext highlighter-rouge">add-apt-repository</code> 명령을 위해 <code class="language-plaintext highlighter-rouge">software-properties-common</code>을 먼저 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> software-properties-common
<span class="nb">sudo </span>add-apt-repository <span class="nt">--yes</span> <span class="nt">--update</span> ppa:ansible/ansible
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> ansible
</code></pre></div></div>

<p>설치된 Ansible Core, Python, Module과 Collection 경로를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible <span class="nt">--version</span>
ansible-community <span class="nt">--version</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ansible --version</code>은 실제 실행 Runtime인 <code class="language-plaintext highlighter-rouge">ansible-core</code> Version을 표시한다. 전체 Community Package Version은 <code class="language-plaintext highlighter-rouge">ansible-community --version</code>으로 구분해 확인한다.</p>

<p>원격 Host를 연결하기 전에 Local Connection으로 Ansible 실행 자체를 확인한다. <code class="language-plaintext highlighter-rouge">localhost,</code> 뒤의 쉼표는 문자열을 Inventory Host 목록으로 해석하게 하고, <code class="language-plaintext highlighter-rouge">-c local</code>은 SSH 대신 Local Connection을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible localhost <span class="se">\</span>
  <span class="nt">-i</span> localhost, <span class="se">\</span>
  <span class="nt">-c</span> <span class="nb">local</span> <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.ping
</code></pre></div></div>

<p>정상 결과에는 <code class="language-plaintext highlighter-rouge">SUCCESS</code>, <code class="language-plaintext highlighter-rouge">changed: false</code>와 <code class="language-plaintext highlighter-rouge">ping: pong</code>이 표시된다.</p>

<h2 id="6--managed-node-계정과-권한-준비">6 ) Managed Node 계정과 권한 준비</h2>

<hr />

<p>다음 작업은 <code class="language-plaintext highlighter-rouge">master</code>, <code class="language-plaintext highlighter-rouge">worker1</code>, <code class="language-plaintext highlighter-rouge">worker2</code> 각각에서 실행한다. 자동화 전용 사용자 <code class="language-plaintext highlighter-rouge">ansible</code>을 만들고 처음 Public Key를 복사할 때 사용할 Password를 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>adduser ansible
<span class="nb">sudo </span>passwd ansible
</code></pre></div></div>

<p>Package와 System Service를 관리하려면 Privilege Escalation 권한이 필요하다. 실습 환경에서 Password 없이 <code class="language-plaintext highlighter-rouge">sudo</code>를 사용하려면 <code class="language-plaintext highlighter-rouge">/etc/sudoers</code> 자체의 Permission을 변경하지 않고 전용 Drop-in File을 <code class="language-plaintext highlighter-rouge">visudo</code>로 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>visudo <span class="nt">-f</span> /etc/sudoers.d/ansible
</code></pre></div></div>

<p>다음 한 줄을 입력하고 저장한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible ALL=(ALL:ALL) NOPASSWD: ALL
</code></pre></div></div>

<p>File Permission과 문법을 검증한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chmod </span>0440 /etc/sudoers.d/ansible
<span class="nb">sudo </span>visudo <span class="nt">-cf</span> /etc/sudoers.d/ansible
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">NOPASSWD: ALL</code>은 실습 절차를 단순화하지만 해당 계정에 넓은 Root 권한을 부여한다. 운영 환경에서는 자동화 전용 계정, 접속 Source와 실행 가능한 명령을 제한하거나 <code class="language-plaintext highlighter-rouge">NOPASSWD</code>를 사용하지 않고 실행 시 <code class="language-plaintext highlighter-rouge">--ask-become-pass</code>를 사용한다.</p>

<p>SSH Service 상태와 Python을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl is-active ssh
python3 <span class="nt">--version</span>
</code></pre></div></div>

<p>계정이나 Public Key를 추가했다는 이유만으로 SSH Service를 재시작할 필요는 없다. <code class="language-plaintext highlighter-rouge">sshd_config</code>를 변경한 경우에만 문법을 확인하고 Service를 Reload 또는 Restart한다.</p>

<h2 id="7--ssh-공개키-인증과-host-key-검증">7 ) SSH 공개키 인증과 Host Key 검증</h2>

<hr />

<blockquote>
  <p><strong>사용자 공개키 인증</strong>은 Control Node가 Private Key로 자신의 신원을 증명하고, Managed Node가 <code class="language-plaintext highlighter-rouge">authorized_keys</code>에 등록된 Public Key로 이를 확인하는 방식이다.</p>
</blockquote>

<blockquote>
  <p><strong>SSH Host Key</strong>는 접속 대상 Server의 신원을 확인한다. 사용자 Key와 Host Key를 모두 검증해야 사용자와 Server 양쪽의 신원을 확인할 수 있다.</p>
</blockquote>

<p>다음 명령은 <code class="language-plaintext highlighter-rouge">control</code> Host에서 실행한다. 기존 개인용 Key와 구분되는 Ansible 전용 Ed25519 Key Pair를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="se">\</span>
  <span class="nt">-t</span> ed25519 <span class="se">\</span>
  <span class="nt">-C</span> <span class="s2">"ansible-control"</span> <span class="se">\</span>
  <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible"</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>File</th>
      <th>역할</th>
      <th>보관 위치</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">id_ed25519_ansible</code></td>
      <td>Control Node가 서명에 사용하는 Private Key</td>
      <td>Control Node에서만 보관</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">id_ed25519_ansible.pub</code></td>
      <td>Managed Node가 접속 허용 여부를 확인하는 Public Key</td>
      <td>각 Managed Node의 <code class="language-plaintext highlighter-rouge">authorized_keys</code></td>
    </tr>
  </tbody>
</table>

<p>Public Key만 각 Managed Node의 <code class="language-plaintext highlighter-rouge">ansible</code> 계정에 복사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-copy-id <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible.pub"</span> ansible@192.0.2.11
ssh-copy-id <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible.pub"</span> ansible@192.0.2.12
ssh-copy-id <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible.pub"</span> ansible@192.0.2.13
</code></pre></div></div>

<p>Private Key는 전송하거나 Repository에 Commit하지 않는다. Managed Node에서 등록 상태와 Permission을 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod </span>700 <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh"</span>
<span class="nb">chmod </span>600 <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/authorized_keys"</span>
</code></pre></div></div>

<p>최초 연결 전에 각 Managed Node Console에서 실제 SSH Host Key Fingerprint를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>ssh-keygen <span class="nt">-lf</span> /etc/ssh/ssh_host_ed25519_key.pub
</code></pre></div></div>

<p>Control Node에서 처음 <code class="language-plaintext highlighter-rouge">ssh</code>로 접속할 때 표시되는 Fingerprint가 위 결과와 같은지 확인하고 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible"</span> ansible@192.0.2.11
ssh <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible"</span> ansible@192.0.2.12
ssh <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.ssh/id_ed25519_ansible"</span> ansible@192.0.2.13
</code></pre></div></div>

<p>Host Key 검사를 끄면 다른 Server를 대상 Host로 오인하는 공격을 탐지할 수 없다. <code class="language-plaintext highlighter-rouge">ANSIBLE_HOST_KEY_CHECKING=False</code>로 우회하지 않고 검증한 Host Key를 <code class="language-plaintext highlighter-rouge">known_hosts</code>에 유지한다.</p>

<h2 id="8--inventory-작성">8 ) Inventory 작성</h2>

<hr />

<blockquote>
  <p><strong>Inventory</strong>는 Ansible이 관리할 Host와 Group, 연결에 필요한 변수를 정의하는 Source이다.</p>
</blockquote>

<p>Control Node에서 Project Directory를 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/ansible-lab"</span>
<span class="nb">cd</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/ansible-lab"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">inventory.ini</code>를 작성한다. Host Alias와 실제 접속 주소를 분리하면 Playbook에서는 <code class="language-plaintext highlighter-rouge">master</code>, <code class="language-plaintext highlighter-rouge">worker1</code>, <code class="language-plaintext highlighter-rouge">worker2</code>라는 이름을 유지하면서 IP만 바꿀 수 있다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[lab]</span>
<span class="err">master</span>  <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.11</span>
<span class="err">worker1</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.12</span>
<span class="err">worker2</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">192.0.2.13</span>

<span class="nn">[lab:vars]</span>
<span class="py">ansible_user</span><span class="p">=</span><span class="s">ansible</span>
<span class="py">ansible_ssh_private_key_file</span><span class="p">=</span><span class="s">~/.ssh/id_ed25519_ansible</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ansible_host</code>를 생략하고 Hostname을 직접 사용하려면 Control Node의 DNS 또는 <code class="language-plaintext highlighter-rouge">/etc/hosts</code>에서 이름이 해석되어야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.0.2.11 master
192.0.2.12 worker1
192.0.2.13 worker2
</code></pre></div></div>

<p>이 경우 Inventory는 다음처럼 단순화할 수 있다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[lab]</span>
<span class="err">master</span>
<span class="err">worker1</span>
<span class="err">worker2</span>

<span class="nn">[lab:vars]</span>
<span class="py">ansible_user</span><span class="p">=</span><span class="s">ansible</span>
<span class="py">ansible_ssh_private_key_file</span><span class="p">=</span><span class="s">~/.ssh/id_ed25519_ansible</span>
</code></pre></div></div>

<p>Inventory에 Password나 Private Key 원문을 기록하지 않는다. Private Key는 제한된 File Permission으로 보관하고 Inventory에는 경로만 지정한다.</p>

<h2 id="9--project-local-ansiblecfg">9 ) Project Local ansible.cfg</h2>

<hr />

<p>Project Directory의 <code class="language-plaintext highlighter-rouge">ansible.cfg</code>에 기본 Inventory를 지정하면 매번 <code class="language-plaintext highlighter-rouge">-i inventory.ini</code>를 입력하지 않아도 된다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[defaults]</span>
<span class="py">inventory</span> <span class="p">=</span> <span class="s">./inventory.ini</span>
<span class="py">host_key_checking</span> <span class="p">=</span> <span class="s">True</span>
</code></pre></div></div>

<p>Ansible이 현재 Project의 설정을 읽었는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-config view
ansible-config dump <span class="nt">--only-changed</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">inventory</code>가 Project의 <code class="language-plaintext highlighter-rouge">inventory.ini</code>를 가리키고 <code class="language-plaintext highlighter-rouge">HOST_KEY_CHECKING</code>이 활성화되어 있어야 한다.</p>

<h2 id="10--inventory와-통신-검증">10 ) Inventory와 통신 검증</h2>

<hr />

<p>Inventory 문법과 Host 구성을 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-inventory <span class="nt">--graph</span>
ansible-inventory <span class="nt">--list</span>
ansible all <span class="nt">--list-hosts</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--graph</code>는 Group과 Host 관계를 보여주고, <code class="language-plaintext highlighter-rouge">--list</code>는 Ansible이 해석한 전체 Inventory Data를 JSON으로 출력한다. <code class="language-plaintext highlighter-rouge">--list-hosts</code>는 현재 Host Pattern이 실제로 선택하는 대상만 확인한다.</p>

<p>세 Managed Node에 Ping Module을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="nt">-m</span> ansible.builtin.ping
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ansible.builtin.ping</code>은 ICMP Echo를 보내는 Network Ping이 아니다. Ansible이 대상에 연결하고 사용할 수 있는 Python으로 Module을 실행한 뒤 <code class="language-plaintext highlighter-rouge">pong</code>을 반환하는 연결 검증이다.</p>

<p>문제가 발생하면 상태에 따라 확인 범위를 나눈다.</p>

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">No inventory was parsed</code></td>
      <td><code class="language-plaintext highlighter-rouge">ansible.cfg</code> 위치, <code class="language-plaintext highlighter-rouge">inventory</code> 경로와 INI 문법</td>
    </tr>
    <tr>
      <td>Host가 목록에 없음</td>
      <td>Group 이름, Host Pattern과 <code class="language-plaintext highlighter-rouge">ansible-inventory --graph</code> 결과</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">UNREACHABLE</code></td>
      <td>IP·DNS, SSH Port, Firewall, 계정과 Private Key 경로</td>
    </tr>
    <tr>
      <td>Host Key 검증 실패</td>
      <td>대상 Server 변경 여부와 <code class="language-plaintext highlighter-rouge">known_hosts</code>의 기존 Key</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Permission denied (publickey)</code></td>
      <td>Public Key 등록 계정, <code class="language-plaintext highlighter-rouge">authorized_keys</code>와 Directory Permission</td>
    </tr>
    <tr>
      <td>Python Interpreter 오류</td>
      <td>Managed Node의 Python 설치와 <code class="language-plaintext highlighter-rouge">ansible_python_interpreter</code> 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sudo</code> 실패</td>
      <td><code class="language-plaintext highlighter-rouge">become</code> 사용 여부, <code class="language-plaintext highlighter-rouge">sudoers</code> 문법과 Password 정책</td>
    </tr>
  </tbody>
</table>

<p>공식 문서는 <a href="https://docs.ansible.com/projects/ansible/latest/installation_guide/intro_installation.html">Ansible 설치 요구 조건</a>, <a href="https://docs.ansible.com/projects/ansible/latest/installation_guide/installation_distros.html">Ubuntu 설치</a>와 <a href="https://docs.ansible.com/projects/ansible/latest/inventory_guide/intro_inventory.html">Inventory 작성 방법</a>에서 확인할 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Ansible은 Control Node에서 Inventory와 자동화 정의를 읽고 SSH를 통해 Managed Node의 상태를 관리한다.</p>
    </li>
    <li>
      <p>Agentless는 Managed Node에 상시 Ansible Agent를 설치하지 않는다는 뜻이며 일반적인 POSIX Module 실행에는 SSH와 Python이 필요하다.</p>
    </li>
    <li>
      <p>멱등성을 지원하는 Module은 이미 원하는 상태이면 변경하지 않지만 <code class="language-plaintext highlighter-rouge">command</code>와 <code class="language-plaintext highlighter-rouge">shell</code> 같은 작업은 별도 조건 없이 매번 실행된다.</p>
    </li>
    <li>
      <p>Inventory는 Host Alias, 실제 주소, Group과 연결 변수를 분리하여 여러 Managed Node를 일관되게 선택한다.</p>
    </li>
    <li>
      <p>SSH 사용자 Key와 Server Host Key를 모두 검증하고 <code class="language-plaintext highlighter-rouge">ping</code> Module로 실제 Ansible 연결을 확인한다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-59-ansible-adhoc-playbook-nginx/">Ansible Ad-hoc 명령과 Playbook으로 Nginx 자동화</a>에서는 연결된 Managed Node에 일회성 명령을 실행하고 반복 가능한 Playbook으로 Nginx를 배포한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Ansible" /><summary type="html"><![CDATA[Ansible Control Node와 Managed Node의 역할을 이해하고 Ubuntu 설치, SSH 공개키 인증과 Inventory를 구성하여 연결을 검증한다]]></summary></entry><entry><title type="html">Ansible Ad-hoc 명령과 Playbook으로 Nginx 자동화</title><link href="https://hyn128.site/cloud-native-59-ansible-adhoc-playbook-nginx/" rel="alternate" type="text/html" title="Ansible Ad-hoc 명령과 Playbook으로 Nginx 자동화" /><published>2026-09-22T00:00:00+09:00</published><updated>2026-09-22T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-59-ansible-adhoc-playbook-nginx</id><content type="html" xml:base="https://hyn128.site/cloud-native-59-ansible-adhoc-playbook-nginx/"><![CDATA[<p><a href="/cloud-native-58-ansible-architecture-inventory/">Ansible Architecture와 Inventory 구성</a>에서는 Control Node와 Managed Node의 역할을 구분하고 SSH 공개키 인증과 Inventory를 구성했다. 이번에는 연결된 Host에 Ad-hoc 명령을 실행한 뒤 동일한 작업을 반복 가능한 Playbook으로 전환한다.</p>

<h2 id="1--ad-hoc-명령">1 ) Ad-hoc 명령</h2>

<hr />

<blockquote>
  <p><strong>Ad-hoc 명령</strong>은 Playbook File을 만들지 않고 Ansible CLI에서 하나의 Module을 대상 Host에 직접 실행하는 방식이다.</p>
</blockquote>

<p>일회성 상태 확인, 긴급한 File 복사와 간단한 Package 작업에 적합하다. 같은 작업을 반복하거나 여러 Task의 순서를 관리해야 한다면 Playbook으로 작성한다.</p>

<p>기본 형식은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible &lt;host-pattern&gt; [option]
</code></pre></div></div>

<p>예를 들어 Inventory의 모든 Host에 Ping Module을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="nt">-m</span> ansible.builtin.ping
</code></pre></div></div>

<p>주요 Option을 구분하면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>Long Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td><code class="language-plaintext highlighter-rouge">--inventory</code></td>
      <td>사용할 Inventory Source를 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-m</code></td>
      <td><code class="language-plaintext highlighter-rouge">--module-name</code></td>
      <td>실행할 Module을 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-a</code></td>
      <td><code class="language-plaintext highlighter-rouge">--args</code></td>
      <td>Module에 전달할 Argument를 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-u</code></td>
      <td><code class="language-plaintext highlighter-rouge">--user</code></td>
      <td>SSH로 접속할 Remote User를 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-b</code></td>
      <td><code class="language-plaintext highlighter-rouge">--become</code></td>
      <td><code class="language-plaintext highlighter-rouge">sudo</code> 등의 방법으로 권한을 상승한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-k</code></td>
      <td><code class="language-plaintext highlighter-rouge">--ask-pass</code></td>
      <td>SSH 접속 Password를 입력받는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-K</code></td>
      <td><code class="language-plaintext highlighter-rouge">--ask-become-pass</code></td>
      <td>Privilege Escalation Password를 입력받는다.</td>
    </tr>
    <tr>
      <td> </td>
      <td><code class="language-plaintext highlighter-rouge">--list-hosts</code></td>
      <td>명령을 실행하지 않고 선택되는 Host만 표시한다.</td>
    </tr>
    <tr>
      <td> </td>
      <td><code class="language-plaintext highlighter-rouge">--check</code></td>
      <td>지원하는 Module에서 실제 변경 없이 예상 결과를 확인한다.</td>
    </tr>
  </tbody>
</table>

<p>SSH Key 인증을 구성했다면 일반적으로 <code class="language-plaintext highlighter-rouge">-k</code>가 필요하지 않다. Managed Node의 <code class="language-plaintext highlighter-rouge">sudo</code>가 Password를 요구하면 <code class="language-plaintext highlighter-rouge">-b -K</code>를 함께 사용한다. <code class="language-plaintext highlighter-rouge">NOPASSWD</code> 정책을 사용한다면 <code class="language-plaintext highlighter-rouge">-b</code>만으로 권한을 상승할 수 있다.</p>

<p>명령을 실행하기 전에 Host Pattern이 의도한 대상을 선택하는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="nt">--list-hosts</span>
ansible lab <span class="nt">--list-hosts</span>
</code></pre></div></div>

<h2 id="2--module과-멱등성">2 ) Module과 멱등성</h2>

<hr />

<blockquote>
  <p><strong>Module</strong>은 Package, File, Service처럼 Managed Node에서 수행할 작업을 캡슐화한 실행 단위이다.</p>
</blockquote>

<p>이번 실습에서 사용하는 Module은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Module</th>
      <th>역할</th>
      <th>멱등성 판단</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.command</code></td>
      <td>Shell을 거치지 않고 명령 실행</td>
      <td>기본적으로 실행할 때마다 동작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.shell</code></td>
      <td>Pipe, Redirect와 환경 변수 등 Shell 문법 사용</td>
      <td>기본적으로 실행할 때마다 동작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.copy</code></td>
      <td>Control Node의 File을 Managed Node에 복사</td>
      <td>내용과 속성이 같으면 변경하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.file</code></td>
      <td>File, Directory와 Symbolic Link 상태 관리</td>
      <td>원하는 상태와 같으면 변경하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.get_url</code></td>
      <td>URL의 Resource를 Managed Node에 Download</td>
      <td>Checksum과 대상 상태에 따라 판단</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.apt</code></td>
      <td>Debian·Ubuntu Package 관리</td>
      <td>설치·삭제 상태를 비교하여 판단</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ansible.builtin.service</code></td>
      <td>Service 상태 관리</td>
      <td>현재 Service 상태를 확인하여 판단</td>
    </tr>
  </tbody>
</table>

<p>단순한 실행은 <code class="language-plaintext highlighter-rouge">command</code>를 우선한다. 다음처럼 Shell 기능이 필요한 경우에만 <code class="language-plaintext highlighter-rouge">shell</code>을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.shell <span class="se">\</span>
  <span class="nt">-a</span> <span class="s1">'ps -ef | grep [n]ginx &gt; /tmp/nginx-process.txt'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">shell</code>의 Argument에 외부 입력을 그대로 연결하면 Shell Injection이 발생할 수 있다. 입력을 제한하고, Pipe나 Redirect가 필요하지 않다면 <code class="language-plaintext highlighter-rouge">command</code>를 사용한다.</p>

<p>사용 가능한 Module과 상세 Argument는 Control Node에서 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-doc <span class="nt">-l</span>
ansible-doc ansible.builtin.command
ansible-doc ansible.builtin.copy
ansible-doc ansible.builtin.apt
</code></pre></div></div>

<p>공식 <a href="https://docs.ansible.com/projects/ansible/latest/collections/ansible/builtin/">Ansible Builtin Collection</a>에서도 Module별 Parameter와 Example을 확인할 수 있다.</p>

<h2 id="3--상태-확인과-file-복사">3 ) 상태 확인과 File 복사</h2>

<hr />

<p>현재 SSH로 접속한 계정을 확인한다. <code class="language-plaintext highlighter-rouge">whoami</code>는 Shell 기능이 필요하지 않으므로 <code class="language-plaintext highlighter-rouge">command</code> Module을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"whoami"</span>
</code></pre></div></div>

<p>Managed Node의 Memory 상태도 같은 방식으로 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"free -h"</span>
</code></pre></div></div>

<p>각 Host가 실행 결과를 반환하지만 <code class="language-plaintext highlighter-rouge">command</code> Module은 해당 명령이 상태를 변경하는지 판단할 수 없다. 조회 명령도 기본 결과에 <code class="language-plaintext highlighter-rouge">changed</code>가 표시될 수 있으므로 Playbook에서는 <code class="language-plaintext highlighter-rouge">changed_when: false</code>를 지정할 수 있다.</p>

<p>Control Node에서 전송할 File을 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch </span>test.txt
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">copy</code> Module로 모든 Managed Node의 <code class="language-plaintext highlighter-rouge">/tmp/test.txt</code>에 복사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.copy <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"src=./test.txt dest=/tmp/test.txt mode=0644"</span>
</code></pre></div></div>

<p>복사된 File의 속성을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"ls -l /tmp/test.txt"</span>
</code></pre></div></div>

<p>같은 <code class="language-plaintext highlighter-rouge">copy</code> 명령을 다시 실행하면 Source 내용과 Permission이 바뀌지 않았으므로 <code class="language-plaintext highlighter-rouge">changed: false</code>가 표시된다.</p>

<h2 id="4--nginx-설치와-삭제">4 ) Nginx 설치와 삭제</h2>

<hr />

<p>Ubuntu Managed Node의 Package 목록을 갱신하고 Nginx를 설치한다. Package 변경에는 Root 권한이 필요하므로 <code class="language-plaintext highlighter-rouge">-b</code>를 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.apt <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"name=nginx state=present update_cache=true"</span> <span class="se">\</span>
  <span class="nt">-b</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">sudo</code> Password가 필요한 환경에서는 <code class="language-plaintext highlighter-rouge">-K</code>를 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.apt <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"name=nginx state=present update_cache=true"</span> <span class="se">\</span>
  <span class="nt">-b</span> <span class="nt">-K</span>
</code></pre></div></div>

<p>Nginx Package Version과 Service 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"nginx -v"</span>

ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"systemctl is-active nginx"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">systemctl status nginx</code>는 사람이 읽기 좋은 상세 출력을 제공하지만 상태 확인 자동화에는 종료 Code가 명확한 <code class="language-plaintext highlighter-rouge">systemctl is-active nginx</code>가 간결하다.</p>

<p>Ad-hoc 실습 후 Nginx를 제거하려면 다음 명령을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.apt <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"name=nginx state=absent purge=true autoremove=true"</span> <span class="se">\</span>
  <span class="nt">-b</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">state=absent</code>는 Package를 제거하고 <code class="language-plaintext highlighter-rouge">purge=true</code>는 Package 설정 File 제거를 요청한다. <code class="language-plaintext highlighter-rouge">autoremove=true</code>는 더 이상 필요하지 않은 자동 설치 의존성을 정리한다.</p>

<h2 id="5--playbook-구조">5 ) Playbook 구조</h2>

<hr />

<blockquote>
  <p><strong>Playbook</strong>은 어떤 Host에 어떤 Task를 어떤 순서로 적용할지 YAML로 작성한 자동화 정의이다.</p>
</blockquote>

<p>Playbook은 다음 단위로 구성한다.</p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Play</td>
      <td>대상 Host Group과 실행 정책을 정의한다.</td>
    </tr>
    <tr>
      <td>Task</td>
      <td>순서대로 실행할 작업 하나를 정의한다.</td>
    </tr>
    <tr>
      <td>Module</td>
      <td>Task가 호출하는 실제 작업 기능이다.</td>
    </tr>
    <tr>
      <td>Handler</td>
      <td>다른 Task의 변경 알림을 받았을 때만 실행되는 보조 Task이다.</td>
    </tr>
  </tbody>
</table>

<p>Ansible은 Play를 위에서 아래로 처리하고 각 Play의 Task도 작성된 순서대로 실행한다. 기본 Linear Strategy에서는 현재 Task가 대상 Host에서 처리된 뒤 다음 Task로 이동한다.</p>

<p><code class="language-plaintext highlighter-rouge">ping-test.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Verify Ansible connections</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">all</span>
  <span class="na">gather_facts</span><span class="pi">:</span> <span class="no">false</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Check Python and Ansible connection</span>
      <span class="na">ansible.builtin.ping</span><span class="pi">:</span>
</code></pre></div></div>

<p>YAML 문법과 Ansible 구조를 먼저 검사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> ping-test.yml
</code></pre></div></div>

<p>문법이 정상이라면 Playbook을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook ping-test.yml
</code></pre></div></div>

<h2 id="6--file-복사-playbook">6 ) File 복사 Playbook</h2>

<hr />

<p>반복할 File 배포 작업을 <code class="language-plaintext highlighter-rouge">filecopy.yml</code>에 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy a file from the control node</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">all</span>
  <span class="na">gather_facts</span><span class="pi">:</span> <span class="no">false</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy test file to managed nodes</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">./test.txt</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/tmp/test.txt</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">ansible</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">ansible</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mode</code>를 문자열로 작성하면 YAML Parser가 앞자리 <code class="language-plaintext highlighter-rouge">0</code>이 있는 값을 다른 숫자 형식으로 해석하는 문제를 피할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> filecopy.yml
ansible-playbook filecopy.yml
</code></pre></div></div>

<h2 id="7--directory-생성과-file-download">7 ) Directory 생성과 File Download</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">file</code> Module로 Directory를 만들고 <code class="language-plaintext highlighter-rouge">get_url</code> Module로 Apache Tomcat Archive를 Download한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Prepare a Tomcat archive directory</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">all</span>
  <span class="na">gather_facts</span><span class="pi">:</span> <span class="no">false</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Create Tomcat download directory</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/tmp/tomcat9</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">directory</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">ansible</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">ansible</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0755"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Download Tomcat archive</span>
      <span class="na">ansible.builtin.get_url</span><span class="pi">:</span>
        <span class="na">url</span><span class="pi">:</span> <span class="s">https://archive.apache.org/dist/tomcat/tomcat-9/v9.0.8/bin/apache-tomcat-9.0.8.tar.gz</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/tmp/tomcat9/apache-tomcat-9.0.8.tar.gz</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
</code></pre></div></div>

<p>이 URL의 Tomcat 9.0.8은 <code class="language-plaintext highlighter-rouge">get_url</code> 동작을 확인하기 위한 과거 Archive 예시이다. 실제 운영 설치에는 Apache Tomcat의 현재 지원 Version과 보안 Update 상태를 확인하고 Download File의 Checksum도 함께 검증한다.</p>

<h2 id="8--nginx-설정-file-준비">8 ) Nginx 설정 File 준비</h2>

<hr />

<p>Control Node의 Project Directory를 다음처럼 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-lab/
├── ansible.cfg
├── inventory.ini
├── files/
│   ├── index.html
│   └── nginx-default.conf
├── ping-test.yml
├── filecopy.yml
├── download-tomcat.yml
├── webservers.yml
└── remove-nginx.yml
</code></pre></div></div>

<p>Nginx가 제공할 <code class="language-plaintext highlighter-rouge">files/index.html</code>을 작성한다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!doctype html&gt;</span>
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"en"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"utf-8"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;title&gt;</span>Welcome to Nginx<span class="nt">&lt;/title&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;h1&gt;</span>Nginx configured by Ansible<span class="nt">&lt;/h1&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>Ubuntu Nginx의 기본 Site 설정으로 사용할 <code class="language-plaintext highlighter-rouge">files/nginx-default.conf</code>를 작성한다. Document Root는 <code class="language-plaintext highlighter-rouge">/var/www/html</code>로 맞춘다.</p>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span> <span class="s">default_server</span><span class="p">;</span>
    <span class="kn">listen</span> <span class="s">[::]:80</span> <span class="s">default_server</span><span class="p">;</span>

    <span class="kn">root</span> <span class="n">/var/www/html</span><span class="p">;</span>
    <span class="kn">index</span> <span class="s">index.html</span> <span class="s">index.htm</span><span class="p">;</span>

    <span class="kn">server_name</span> <span class="s">_</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">try_files</span> <span class="nv">$uri</span> <span class="nv">$uri</span><span class="n">/</span> <span class="p">=</span><span class="mi">404</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="9--nginx-배포-playbook">9 ) Nginx 배포 Playbook</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">webservers.yml</code>은 Package 설치, 설정 File과 Welcome Page 복사, 설정 검증과 Service 시작을 순서대로 처리한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure web servers with Nginx</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">all</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install Nginx</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
        <span class="na">update_cache</span><span class="pi">:</span> <span class="no">true</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy Nginx default site configuration</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">files/nginx-default.conf</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-available/default</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Restart Nginx</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Enable Nginx default site</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-available/default</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-enabled/default</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">link</span>
        <span class="na">force</span><span class="pi">:</span> <span class="no">true</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Restart Nginx</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy welcome page</span>
      <span class="na">ansible.builtin.copy</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">files/index.html</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/var/www/html/index.html</span>
        <span class="na">owner</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">group</span><span class="pi">:</span> <span class="s">root</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Validate Nginx configuration</span>
      <span class="na">ansible.builtin.command</span><span class="pi">:</span> <span class="s">nginx -t</span>
      <span class="na">changed_when</span><span class="pi">:</span> <span class="no">false</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure Nginx is enabled and running</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>

  <span class="na">handlers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Restart Nginx</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">restarted</span>
</code></pre></div></div>

<p>설정 File이나 Symbolic Link가 실제로 변경된 경우에만 <code class="language-plaintext highlighter-rouge">notify</code>가 Handler를 호출한다. Welcome Page만 바뀌면 Nginx는 정적 File을 다시 읽을 수 있으므로 Service를 재시작하지 않는다.</p>

<p><code class="language-plaintext highlighter-rouge">nginx -t</code>가 실패하면 Play가 중단되고 알림을 받은 Handler도 실행되지 않는다. 잘못된 설정으로 Service를 재시작하는 상황을 피할 수 있다.</p>

<h2 id="10--playbook-검사와-실행">10 ) Playbook 검사와 실행</h2>

<hr />

<p>먼저 문법을 검사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> webservers.yml
</code></pre></div></div>

<p>지원되는 Module에서 예상 변경과 File 차이를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--check</span> <span class="nt">--diff</span> webservers.yml
</code></pre></div></div>

<p>Check Mode는 모든 Module의 실제 결과를 완전히 재현하지 않는다. 예를 들어 Package가 아직 설치되지 않은 환경에서는 뒤의 Nginx 검증 Task가 실행될 수 없으므로 Preview 결과와 실제 실행을 구분한다.</p>

<p>실제 배포를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook webservers.yml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">sudo</code> Password가 필요한 환경에서는 <code class="language-plaintext highlighter-rouge">-K</code>를 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">-K</span> webservers.yml
</code></pre></div></div>

<h2 id="11--nginx-배포-검증">11 ) Nginx 배포 검증</h2>

<hr />

<p>Control Node에서 Package Version, 설정 문법과 Service 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"nginx -v"</span>

ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"nginx -t"</span>

ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"systemctl is-active nginx"</span>
</code></pre></div></div>

<p>각 Managed Node의 HTTP 응답을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://192.0.2.11
curl http://192.0.2.12
curl http://192.0.2.13
</code></pre></div></div>

<p>응답에 <code class="language-plaintext highlighter-rouge">Nginx configured by Ansible</code>이 포함되어야 한다. 다른 Computer에서 확인한다면 Managed Node의 Firewall과 Network에서 TCP 80 Port 접근도 허용되어 있어야 한다.</p>

<p>같은 Playbook을 다시 실행해 불필요한 변경이 없는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook webservers.yml
</code></pre></div></div>

<p>두 번째 실행에서는 변경할 상태가 없다면 <code class="language-plaintext highlighter-rouge">changed=0</code>이 표시된다. <code class="language-plaintext highlighter-rouge">nginx -t</code> Task는 명령 자체를 다시 실행하지만 <code class="language-plaintext highlighter-rouge">changed_when: false</code>로 조회 작업임을 명시했기 때문에 변경으로 집계하지 않는다.</p>

<h2 id="12--nginx-제거-playbook">12 ) Nginx 제거 Playbook</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">remove-nginx.yml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Remove Nginx from managed nodes</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">all</span>
  <span class="na">become</span><span class="pi">:</span> <span class="no">true</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Stop Nginx before removal</span>
      <span class="na">ansible.builtin.service</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">stopped</span>
      <span class="na">ignore_errors</span><span class="pi">:</span> <span class="no">true</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Remove Nginx packages and configuration</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">nginx</span>
          <span class="pi">-</span> <span class="s">nginx-common</span>
          <span class="pi">-</span> <span class="s">nginx-core</span>
          <span class="pi">-</span> <span class="s">nginx-full</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">absent</span>
        <span class="na">purge</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">autoremove</span><span class="pi">:</span> <span class="no">true</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Reload systemd manager configuration</span>
      <span class="na">ansible.builtin.systemd_service</span><span class="pi">:</span>
        <span class="na">daemon_reload</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ignore_errors</code>는 Nginx가 설치되지 않았거나 Service가 없는 Host에서도 Package 제거 Task까지 진행하기 위한 실습용 처리이다. 오류 원인을 숨길 수 있으므로 운영 Playbook에서는 Service Facts나 조건문으로 존재 여부를 확인하는 방식을 우선한다.</p>

<p>문법을 확인한 뒤 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible-playbook <span class="nt">--syntax-check</span> remove-nginx.yml
ansible-playbook remove-nginx.yml
</code></pre></div></div>

<p>제거 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ansible all <span class="se">\</span>
  <span class="nt">-m</span> ansible.builtin.command <span class="se">\</span>
  <span class="nt">-a</span> <span class="s2">"systemctl is-active nginx"</span>
</code></pre></div></div>

<p>Package가 제거됐다면 명령이 실패할 수 있다. 이 결과는 Ansible 연결 실패가 아니라 Nginx Service가 더 이상 존재하지 않거나 실행 중이 아니라는 의미이다.</p>

<h2 id="13--문제-해결">13 ) 문제 해결</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Missing sudo password</code></td>
      <td><code class="language-plaintext highlighter-rouge">-K</code> 사용 여부 또는 Managed Node의 <code class="language-plaintext highlighter-rouge">sudoers</code> 정책</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Could not find or access</code></td>
      <td><code class="language-plaintext highlighter-rouge">copy.src</code>가 Control Node의 Playbook 기준 경로에 존재하는지 확인</td>
    </tr>
    <tr>
      <td>YAML Syntax Error</td>
      <td>들여쓰기, <code class="language-plaintext highlighter-rouge">:</code> 뒤 공백과 <code class="language-plaintext highlighter-rouge">--syntax-check</code> 결과</td>
    </tr>
    <tr>
      <td>Nginx 설정 검사 실패</td>
      <td><code class="language-plaintext highlighter-rouge">nginx-default.conf</code> 문법과 <code class="language-plaintext highlighter-rouge">nginx -t</code>의 <code class="language-plaintext highlighter-rouge">stderr</code></td>
    </tr>
    <tr>
      <td>Service 시작 실패</td>
      <td><code class="language-plaintext highlighter-rouge">systemctl status nginx</code>와 <code class="language-plaintext highlighter-rouge">journalctl -u nginx</code> 결과</td>
    </tr>
    <tr>
      <td>HTTP 연결 실패</td>
      <td>Service 상태, TCP 80 Firewall, Network 경로와 대상 IP</td>
    </tr>
    <tr>
      <td>매번 <code class="language-plaintext highlighter-rouge">changed</code> 표시</td>
      <td><code class="language-plaintext highlighter-rouge">command</code>·<code class="language-plaintext highlighter-rouge">shell</code> 사용 여부와 <code class="language-plaintext highlighter-rouge">changed_when</code>, Module의 상태 Parameter</td>
    </tr>
    <tr>
      <td>일부 Host만 실패</td>
      <td>Play Recap의 Host별 <code class="language-plaintext highlighter-rouge">failed</code>, <code class="language-plaintext highlighter-rouge">unreachable</code>와 해당 Host의 상세 출력</td>
    </tr>
  </tbody>
</table>

<p>Playbook 구조와 실행 방식은 <a href="https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_intro.html">Ansible Playbook 공식 문서</a>에서, 일회성 명령은 <a href="https://docs.ansible.com/projects/ansible/latest/command_guide/intro_adhoc.html">Ad-hoc 명령 공식 문서</a>에서 확인할 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Ad-hoc 명령은 하나의 Module을 즉시 실행하는 방식이며 반복 작업과 여러 단계 자동화는 Playbook으로 관리한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">-k</code>는 SSH Password, <code class="language-plaintext highlighter-rouge">-K</code>는 Privilege Escalation Password를 요청하고 <code class="language-plaintext highlighter-rouge">-b</code>는 권한 상승을 활성화한다.</p>
    </li>
    <li>
      <p>Shell 기능이 필요하지 않다면 <code class="language-plaintext highlighter-rouge">command</code>를 사용하고 Package, File과 Service는 목적에 맞는 전용 Module로 상태를 관리한다.</p>
    </li>
    <li>
      <p>Playbook은 Play, Task와 Module을 순서대로 실행하며 Handler는 변경 알림이 발생했을 때만 실행한다.</p>
    </li>
    <li>
      <p>Nginx 배포는 Package 설치, 설정·Page 복사, 문법 검사, Service 상태와 HTTP 응답을 함께 확인해야 완료된다.</p>
    </li>
    <li>
      <p>같은 Playbook을 다시 실행했을 때 불필요한 변경이 없는지 확인하면 Module의 멱등성과 Task 설계를 검증할 수 있다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-60-ansible-variables-vault-facts-control-flow/">Ansible 변수와 Vault·Facts·제어문</a>에서는 같은 Playbook을 여러 Host와 환경에서 재사용하기 위한 변수, 암호화 Data, Facts, 반복문과 조건문을 구성한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Ansible" /><summary type="html"><![CDATA[Ansible Ad-hoc 명령의 Option과 Module을 익히고 반복 가능한 Playbook으로 Nginx 설치, 설정 배포와 검증을 자동화한다]]></summary></entry><entry><title type="html">Kubernetes Gateway API</title><link href="https://hyn128.site/notes-10-techbites-gateway-api/" rel="alternate" type="text/html" title="Kubernetes Gateway API" /><published>2026-09-21T00:00:00+09:00</published><updated>2026-09-21T00:00:00+09:00</updated><id>https://hyn128.site/notes-10-techbites-gateway-api</id><content type="html" xml:base="https://hyn128.site/notes-10-techbites-gateway-api/"><![CDATA[<h2 id="요약">요약</h2>

<hr />

<blockquote>
  <p>Kubernetes의 <code class="language-plaintext highlighter-rouge">Ingress</code>는 HTTP와 HTTPS 요청을 클러스터 내부의 <code class="language-plaintext highlighter-rouge">Service</code>로 전달합니다. 사용하기 단순하지만, 기능을 확장할수록 컨트롤러별 <code class="language-plaintext highlighter-rouge">Annotation</code>에 의존하고 인프라 설정과 애플리케이션 라우팅 설정이 한 리소스에 섞이기 쉽습니다. <code class="language-plaintext highlighter-rouge">Gateway API</code>는 <code class="language-plaintext highlighter-rouge">GatewayClass</code>, <code class="language-plaintext highlighter-rouge">Gateway</code>, <code class="language-plaintext highlighter-rouge">Route</code>로 책임을 나누고 프로토콜별 라우팅 규칙을 별도의 리소스로 표현합니다.</p>
</blockquote>

<h2 id="1-ingress가-담당하는-역할">1. Ingress가 담당하는 역할</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">Ingress</code>는 외부에서 들어온 HTTP와 HTTPS 요청을 <code class="language-plaintext highlighter-rouge">Host</code>와 <code class="language-plaintext highlighter-rouge">Path</code> 조건에 따라 클러스터 내부의 <code class="language-plaintext highlighter-rouge">Service</code>로 전달합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[설정 반영]
Ingress → Ingress Controller → Load Balancer 또는 Proxy 설정

[요청 전달]
Client → Load Balancer 또는 Proxy → Service → Pod
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Ingress</code> 리소스만 생성해서는 트래픽이 처리되지 않습니다. 리소스를 감시하고 실제 Load Balancer나 Proxy 설정으로 변환하는 <code class="language-plaintext highlighter-rouge">Ingress Controller</code>가 함께 필요합니다. 실제 요청은 Controller의 조정 과정에서 만들어진 Data Plane을 통과합니다.</p>

<p>다음 설정은 <code class="language-plaintext highlighter-rouge">shop.example.com</code>으로 들어온 요청을 경로에 따라 두 <code class="language-plaintext highlighter-rouge">Service</code>로 전달합니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">shop-ingress</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">tls</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">hosts</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">shop.example.com</span>
      <span class="na">secretName</span><span class="pi">:</span> <span class="s">tls-secret</span>
  <span class="na">rules</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">host</span><span class="pi">:</span> <span class="s">shop.example.com</span>
      <span class="na">http</span><span class="pi">:</span>
        <span class="na">paths</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/app</span>
            <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">service</span><span class="pi">:</span>
                <span class="na">name</span><span class="pi">:</span> <span class="s">app-service</span>
                <span class="na">port</span><span class="pi">:</span>
                  <span class="na">number</span><span class="pi">:</span> <span class="m">80</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/admin</span>
            <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">service</span><span class="pi">:</span>
                <span class="na">name</span><span class="pi">:</span> <span class="s">admin-service</span>
                <span class="na">port</span><span class="pi">:</span>
                  <span class="na">number</span><span class="pi">:</span> <span class="m">8080</span>
</code></pre></div></div>

<p>이 예제는 클러스터에 기본 <code class="language-plaintext highlighter-rouge">IngressClass</code>가 지정되어 있다고 가정합니다. 기본 Class가 없다면 <code class="language-plaintext highlighter-rouge">spec.ingressClassName</code>으로 이 리소스를 처리할 Ingress Class를 명시해야 합니다.</p>

<p>이 설정에는 외부 진입점의 TLS 정보와 애플리케이션의 경로별 라우팅 정보가 함께 들어 있습니다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
      <th>주로 관리하는 영역</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.tls</code></td>
      <td>HTTPS 연결에 사용할 Host와 TLS Secret을 지정합니다.</td>
      <td>클러스터 운영 또는 보안</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.rules.host</code></td>
      <td>요청의 Host 조건을 지정합니다.</td>
      <td>클러스터 운영과 애플리케이션의 공통 영역</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.rules.http.paths</code></td>
      <td>URL Path에 연결할 <code class="language-plaintext highlighter-rouge">Service</code>와 Port를 지정합니다.</td>
      <td>애플리케이션</td>
    </tr>
  </tbody>
</table>

<p>작은 환경에서는 한 리소스로 관리해도 문제가 적습니다. 여러 팀이 하나의 진입점을 공유하면 TLS, Listener, 라우팅 규칙의 변경 주체가 달라지므로 관리 경계가 불분명해질 수 있습니다.</p>

<h2 id="2-ingress의-한계">2. Ingress의 한계</h2>

<hr />

<h3 id="21-controller별-annotation에-의존합니다">2.1 Controller별 Annotation에 의존합니다</h3>

<p><code class="language-plaintext highlighter-rouge">Ingress</code>의 기본 스펙에 없는 기능은 <code class="language-plaintext highlighter-rouge">Ingress Controller</code>가 제공하는 <code class="language-plaintext highlighter-rouge">Annotation</code>으로 설정하는 경우가 많습니다. NGINX Ingress Controller의 Rewrite 기능은 다음과 같이 지정할 수 있습니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">metadata</span><span class="pi">:</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">nginx.ingress.kubernetes.io/rewrite-target</span><span class="pi">:</span> <span class="s">/</span>
</code></pre></div></div>

<p>이 Key는 Kubernetes 공통 스펙이 아니라 NGINX Ingress Controller의 설정입니다. AWS Load Balancer Controller와 같은 다른 구현체는 서로 다른 Key와 값을 사용합니다. Controller를 교체할 때 기존 설정을 그대로 옮길 수 없는 이유입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Ingress 표준 필드
        +
Controller별 Annotation
        ↓
구현체에 종속된 설정
</code></pre></div></div>

<h3 id="22-http와-https를-중심으로-설계되었습니다">2.2 HTTP와 HTTPS를 중심으로 설계되었습니다</h3>

<p>Kubernetes의 표준 <code class="language-plaintext highlighter-rouge">Ingress</code> 규칙은 HTTP와 HTTPS 트래픽만 처리합니다. TCP와 UDP처럼 다른 프로토콜을 노출하려면 <code class="language-plaintext highlighter-rouge">NodePort</code> 또는 <code class="language-plaintext highlighter-rouge">LoadBalancer</code> 유형의 <code class="language-plaintext highlighter-rouge">Service</code>를 사용하거나 Controller가 제공하는 별도 기능을 사용해야 합니다.</p>

<p>Gateway API는 프로토콜별 라우팅 규칙을 Route 리소스로 분리합니다.</p>

<table>
  <thead>
    <tr>
      <th>트래픽</th>
      <th>Gateway API 리소스</th>
      <th>현재 채널</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>HTTP, TLS가 종료된 HTTPS</td>
      <td><code class="language-plaintext highlighter-rouge">HTTPRoute</code></td>
      <td>Standard</td>
    </tr>
    <tr>
      <td>gRPC</td>
      <td><code class="language-plaintext highlighter-rouge">GRPCRoute</code></td>
      <td>Standard</td>
    </tr>
    <tr>
      <td>TLS Passthrough 또는 TLS Metadata 기반 라우팅</td>
      <td><code class="language-plaintext highlighter-rouge">TLSRoute</code></td>
      <td>Standard</td>
    </tr>
    <tr>
      <td>TCP</td>
      <td><code class="language-plaintext highlighter-rouge">TCPRoute</code></td>
      <td>Experimental</td>
    </tr>
    <tr>
      <td>UDP</td>
      <td><code class="language-plaintext highlighter-rouge">UDPRoute</code></td>
      <td>Experimental</td>
    </tr>
  </tbody>
</table>

<p>HTTPS 전용 <code class="language-plaintext highlighter-rouge">HTTPSRoute</code>가 따로 존재하는 것은 아닙니다. <code class="language-plaintext highlighter-rouge">Gateway</code>의 HTTPS Listener가 인증서와 TLS 종료를 담당하고, 복호화된 HTTP 요청은 연결된 <code class="language-plaintext highlighter-rouge">HTTPRoute</code>가 처리합니다.</p>

<p><code class="language-plaintext highlighter-rouge">TLSRoute</code>는 Gateway API v1.5.0부터 Standard Channel에 포함되었습니다. <code class="language-plaintext highlighter-rouge">TCPRoute</code>와 <code class="language-plaintext highlighter-rouge">UDPRoute</code>는 Experimental Channel에 포함되므로 향후 호환성이 보장되지 않습니다. 실제 사용 전에는 설치한 CRD와 Gateway Controller가 해당 Route를 지원하는지 확인해야 합니다.</p>

<h3 id="23-관리-역할을-분리하기-어렵습니다">2.3 관리 역할을 분리하기 어렵습니다</h3>

<p><code class="language-plaintext highlighter-rouge">Ingress</code>에는 Host, Path, TLS, Backend와 Controller별 설정이 함께 들어갑니다. 다음과 같이 운영자와 개발자가 같은 리소스를 수정하게 될 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Ingress
├── Host
├── Path
├── TLS
├── Backend Service
└── Controller별 Annotation
</code></pre></div></div>

<p>클러스터 운영자는 외부 Listener와 TLS를 관리하고, 애플리케이션 개발자는 <code class="language-plaintext highlighter-rouge">/app</code>과 <code class="language-plaintext highlighter-rouge">/admin</code> 같은 라우팅 규칙을 관리해야 합니다. 설정이 하나의 리소스에 모이면 변경 권한과 검토 범위를 분리하기 어렵습니다.</p>

<h3 id="24-고급-라우팅의-표현-범위가-좁습니다">2.4 고급 라우팅의 표현 범위가 좁습니다</h3>

<p><code class="language-plaintext highlighter-rouge">Ingress</code>의 기본 라우팅 조건은 Host와 Path입니다. 다음 기능은 Controller별 <code class="language-plaintext highlighter-rouge">Annotation</code>이나 확장 설정을 사용해야 하는 경우가 많습니다.</p>

<ul>
  <li>
    <p>특정 Header가 있는 요청만 전달합니다.</p>
  </li>
  <li>
    <p>여러 Backend에 가중치를 주어 트래픽을 분할합니다.</p>
  </li>
  <li>
    <p>다른 URL로 Redirect합니다.</p>
  </li>
  <li>
    <p>요청을 다른 Backend로 복제해 Mirror합니다.</p>
  </li>
</ul>

<p>Gateway API는 이러한 조건과 동작을 <code class="language-plaintext highlighter-rouge">HTTPRoute</code>의 Match, Filter, <code class="language-plaintext highlighter-rouge">backendRefs</code> 같은 필드로 표현합니다. 각 필드의 지원 수준은 Core, Extended 또는 구현체별 기능으로 나뉠 수 있으므로 Controller의 Conformance 문서를 함께 확인해야 합니다.</p>

<h2 id="3-gateway-api의-리소스-구조">3. Gateway API의 리소스 구조</h2>

<hr />

<p>Gateway API는 클러스터의 진입점과 애플리케이션 라우팅을 여러 리소스로 나눕니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GatewayClass
     ↓
Gateway
     ↓
HTTPRoute / GRPCRoute / TCPRoute / UDPRoute / TLSRoute
     ↓
Service
     ↓
Pod
</code></pre></div></div>

<p>각 리소스의 책임은 다음과 같습니다.</p>

<table>
  <thead>
    <tr>
      <th>역할</th>
      <th>관리 리소스</th>
      <th>책임</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>인프라 제공자</td>
      <td><code class="language-plaintext highlighter-rouge">GatewayClass</code></td>
      <td>어떤 Gateway Controller가 Gateway를 구현할지 정의합니다.</td>
    </tr>
    <tr>
      <td>클러스터 운영자</td>
      <td><code class="language-plaintext highlighter-rouge">Gateway</code></td>
      <td>Listener, Port, Protocol, TLS와 Route 연결 범위를 정의합니다.</td>
    </tr>
    <tr>
      <td>애플리케이션 개발자</td>
      <td><code class="language-plaintext highlighter-rouge">HTTPRoute</code>, <code class="language-plaintext highlighter-rouge">GRPCRoute</code> 등의 Route</td>
      <td>요청 조건과 Backend <code class="language-plaintext highlighter-rouge">Service</code>를 정의합니다.</td>
    </tr>
  </tbody>
</table>

<p>한 사람이 모든 역할을 담당할 수도 있습니다. 리소스가 분리되어 있으므로 조직 규모가 커졌을 때 RBAC와 Namespace 경계를 이용해 책임과 변경 권한을 나눌 수 있습니다.</p>

<h3 id="31-gatewayclass">3.1 GatewayClass</h3>

<p><code class="language-plaintext highlighter-rouge">GatewayClass</code>는 Gateway 구현체의 종류를 정의하는 Cluster Scope 리소스입니다. <code class="language-plaintext highlighter-rouge">StorageClass</code>가 Storage Provisioner를 가리키는 것처럼 <code class="language-plaintext highlighter-rouge">GatewayClass</code>는 Gateway Controller를 가리킵니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GatewayClass
└── controllerName: 이 Class를 처리할 Gateway Controller
</code></pre></div></div>

<h3 id="32-gateway">3.2 Gateway</h3>

<p><code class="language-plaintext highlighter-rouge">Gateway</code>는 실제 트래픽을 받을 지점을 정의합니다. 어떤 <code class="language-plaintext highlighter-rouge">GatewayClass</code>를 사용할지 선택하고, Listener의 Hostname, Port, Protocol, TLS와 연결 가능한 Route 범위를 설정합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Gateway
├── gatewayClassName
└── listeners
    ├── hostname
    ├── port
    ├── protocol
    ├── tls
    └── allowedRoutes
</code></pre></div></div>

<p>기본 설정에서는 같은 Namespace의 Route만 Gateway에 연결할 수 있습니다. 다른 Namespace의 Route를 연결하려면 <code class="language-plaintext highlighter-rouge">allowedRoutes</code>로 허용 범위를 지정해야 합니다.</p>

<h3 id="33-route">3.3 Route</h3>

<p>Route는 Gateway가 받은 요청을 어느 <code class="language-plaintext highlighter-rouge">Service</code>로 전달할지 정의합니다. HTTP 요청은 <code class="language-plaintext highlighter-rouge">HTTPRoute</code>, gRPC 요청은 <code class="language-plaintext highlighter-rouge">GRPCRoute</code>처럼 프로토콜에 맞는 리소스를 사용합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTPRoute
├── parentRefs: 연결할 Gateway
├── hostnames: 요청 Host 조건
└── rules
    ├── matches: Path, Header 등의 조건
    ├── filters: Redirect, Rewrite, Mirror 등의 동작
    └── backendRefs: 전달할 Service와 가중치
</code></pre></div></div>

<p>Gateway와 Route는 서로의 연결을 모두 허용해야 합니다. Gateway의 <code class="language-plaintext highlighter-rouge">allowedRoutes</code> 범위에 Route가 포함되어야 하고, Route의 <code class="language-plaintext highlighter-rouge">parentRefs</code>도 대상 Gateway를 가리켜야 합니다.</p>

<h2 id="4-요청이-전달되는-흐름">4. 요청이 전달되는 흐름</h2>

<hr />

<p>Ingress Controller는 <code class="language-plaintext highlighter-rouge">Ingress</code>를 감시해 Load Balancer 또는 Proxy 설정을 만듭니다. 실제 요청은 구성된 Data Plane을 통과합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Control Plane]
Ingress → Ingress Controller → Data Plane 설정

[Data Plane]
Client → Load Balancer 또는 Proxy → Service → Pod
</code></pre></div></div>

<p>Gateway Controller도 <code class="language-plaintext highlighter-rouge">GatewayClass</code>, <code class="language-plaintext highlighter-rouge">Gateway</code>, Route를 함께 해석해 Data Plane을 구성합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Control Plane]
GatewayClass + Gateway + Route
              ↓
      Gateway Controller
              ↓
       Data Plane 설정

[Data Plane]
Client
  ↓
Gateway Listener
  ↓
Route 조건과 일치하는 Backend Service
  ↓
Pod
</code></pre></div></div>

<p>HTTP 요청은 다음 순서로 처리됩니다.</p>

<ol>
  <li>
    <p>Client가 DNS로 Gateway의 주소를 확인한 뒤 요청을 전송합니다.</p>
  </li>
  <li>
    <p>Gateway Controller가 구성한 Data Plane이 Listener에서 요청을 받습니다.</p>
  </li>
  <li>
    <p>Listener에 연결된 <code class="language-plaintext highlighter-rouge">HTTPRoute</code> 중 Host, Path, Header 등의 조건이 일치하는 규칙을 찾습니다.</p>
  </li>
  <li>
    <p>필요한 Filter를 적용한 뒤 <code class="language-plaintext highlighter-rouge">backendRefs</code>에 지정된 <code class="language-plaintext highlighter-rouge">Service</code>로 요청을 전달합니다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">Service</code>가 선택한 Pod에서 요청을 처리합니다.</p>
  </li>
</ol>

<h2 id="5-ingress와-gateway-api-비교">5. Ingress와 Gateway API 비교</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Ingress</th>
      <th>Gateway API</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기본 목적</td>
      <td>외부 HTTP와 HTTPS 요청을 내부 <code class="language-plaintext highlighter-rouge">Service</code>로 전달합니다.</td>
      <td>프로토콜을 인식하는 진입점과 고급 라우팅을 구성합니다.</td>
    </tr>
    <tr>
      <td>API 제공 방식</td>
      <td>Kubernetes에 포함된 기본 API입니다.</td>
      <td>CRD와 이를 구현하는 Gateway Controller를 추가해야 합니다.</td>
    </tr>
    <tr>
      <td>리소스 구조</td>
      <td><code class="language-plaintext highlighter-rouge">Ingress</code>를 중심으로 구성합니다.</td>
      <td><code class="language-plaintext highlighter-rouge">GatewayClass</code> → <code class="language-plaintext highlighter-rouge">Gateway</code> → Route로 구성합니다.</td>
    </tr>
    <tr>
      <td>프로토콜</td>
      <td>HTTP와 HTTPS를 처리합니다.</td>
      <td>Standard Route로 HTTP, HTTPS, gRPC, TLS를 처리하며 Experimental Route로 TCP와 UDP를 표현할 수 있습니다.</td>
    </tr>
    <tr>
      <td>고급 기능</td>
      <td>Controller별 <code class="language-plaintext highlighter-rouge">Annotation</code>에 의존하는 경우가 많습니다.</td>
      <td>Match, Filter, <code class="language-plaintext highlighter-rouge">backendRefs</code> 등의 필드로 표현합니다.</td>
    </tr>
    <tr>
      <td>역할 분리</td>
      <td>진입점과 애플리케이션 라우팅 설정이 섞이기 쉽습니다.</td>
      <td>인프라 제공자, 클러스터 운영자, 애플리케이션 개발자의 리소스를 분리합니다.</td>
    </tr>
    <tr>
      <td>이식성</td>
      <td>Controller별 Annotation과 확장 설정의 차이가 큽니다.</td>
      <td>공통 API로 이식성을 높이지만 구현체별 지원 범위는 확인해야 합니다.</td>
    </tr>
  </tbody>
</table>

<p>Gateway API가 공통 스펙을 제공해도 모든 Controller가 모든 Route와 기능을 같은 수준으로 구현하지는 않습니다. Controller를 선택할 때는 지원하는 Gateway API 버전, Release Channel, Conformance Profile과 기능별 지원 수준을 확인해야 합니다.</p>

<h2 id="6-현재-상태와-적용-전-확인-사항">6. 현재 상태와 적용 전 확인 사항</h2>

<hr />

<p>Kubernetes는 <code class="language-plaintext highlighter-rouge">Ingress</code> API를 제거할 계획이 없으며 기존 리소스도 계속 사용할 수 있습니다. 다만 <code class="language-plaintext highlighter-rouge">Ingress</code> API의 신규 개발은 동결되었고, Kubernetes 프로젝트는 새로운 구성을 위해 Gateway API 사용을 권장합니다.</p>

<p>Gateway API를 적용하기 전에는 다음 항목을 확인합니다.</p>

<ul>
  <li>
    <p>Gateway API CRD와 Gateway Controller가 설치되어 있는지 확인합니다.</p>
  </li>
  <li>
    <p>Controller가 사용할 <code class="language-plaintext highlighter-rouge">GatewayClass</code>를 제공하는지 확인합니다.</p>
  </li>
  <li>
    <p>필요한 Route가 Standard Channel인지 Experimental Channel인지 확인합니다.</p>
  </li>
  <li>
    <p>Header Match, Redirect, Rewrite, Mirror, Traffic Split 등 필요한 기능의 지원 수준을 확인합니다.</p>
  </li>
  <li>
    <p>Gateway와 Route를 다른 Namespace에 둘 경우 <code class="language-plaintext highlighter-rouge">allowedRoutes</code>와 권한 경계를 확인합니다.</p>
  </li>
  <li>
    <p>기존 Ingress를 전환할 때 Controller별 Annotation을 어떤 Gateway API 필드나 구현체별 정책으로 옮길지 확인합니다.</p>
  </li>
</ul>

<h2 id="참고-자료">참고 자료</h2>

<hr />

<ul>
  <li>
    <p><a href="https://kubernetes.io/docs/concepts/services-networking/ingress/">Kubernetes Ingress</a></p>
  </li>
  <li>
    <p><a href="https://kubernetes.io/docs/concepts/services-networking/gateway/">Kubernetes Gateway API</a></p>
  </li>
  <li>
    <p><a href="https://gateway-api.sigs.k8s.io/docs/concepts/api-overview/">Gateway API 개요</a></p>
  </li>
  <li>
    <p><a href="https://gateway-api.sigs.k8s.io/docs/implementations/list/">Gateway API 구현체 목록</a></p>
  </li>
</ul>]]></content><author><name></name></author><category term="TechBites" /><category term="Kubernetes" /><category term="GatewayAPI" /><summary type="html"><![CDATA[Ingress의 한계와 Gateway API의 역할별 리소스 구조를 비교합니다]]></summary></entry><entry><title type="html">Argo CD Git Repository 연동과 수동 Sync</title><link href="https://hyn128.site/cloud-native-57-argocd-git-repository-manual-sync/" rel="alternate" type="text/html" title="Argo CD Git Repository 연동과 수동 Sync" /><published>2026-09-18T00:00:00+09:00</published><updated>2026-09-18T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-57-argocd-git-repository-manual-sync</id><content type="html" xml:base="https://hyn128.site/cloud-native-57-argocd-git-repository-manual-sync/"><![CDATA[<p><a href="/cloud-native-56-argocd-application-cli-autopilot/">Argo CD Application 배포와 CLI·Autopilot</a>에서는 Helm Chart를 Source로 사용하는 Application과 자동 Sync를 구성했다. 이번에는 Namespace, Deployment와 Service Manifest를 GitHub Repository에 저장하고 <code class="language-plaintext highlighter-rouge">syncPolicy.automated</code>를 설정하지 않은 Application으로 등록한다. Git의 Desired State가 <code class="language-plaintext highlighter-rouge">OutOfSync</code>로 감지된 뒤 사용자가 수동 Sync를 요청했을 때 Control Plane과 Worker에서 어떤 동작이 이어지는지 확인한다.</p>

<h2 id="1--실습-구조와-준비-사항">1 ) 실습 구조와 준비 사항</h2>

<hr />

<p>이번 실습에서는 Application Source와 Argo CD Application 선언을 다음처럼 구분한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bgd Repository
└── manifest/
    ├── ns.yaml
    ├── deployment.yaml
    └── svc.yaml

Local 관리 File
└── bgd-app.yaml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">manifest/</code> Directory는 Argo CD가 읽는 Desired State이다. <code class="language-plaintext highlighter-rouge">bgd-app.yaml</code>은 해당 Repository와 배포 대상을 Argo CD에 등록하는 <code class="language-plaintext highlighter-rouge">Application</code> Resource이다.</p>

<p>작업 전에 다음 조건을 확인한다.</p>

<ul>
  <li>
    <p>Argo CD가 Kubernetes Cluster에 설치되어 있어야 한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">kubectl</code>의 현재 Context가 배포 대상 Cluster를 가리켜야 한다.</p>
  </li>
  <li>
    <p>Argo CD CLI로 수동 Sync할 수 있도록 Login이 완료되어야 한다.</p>
  </li>
  <li>
    <p>GitHub에 Push할 <code class="language-plaintext highlighter-rouge">bgd</code> Repository가 준비되어 있어야 한다.</p>
  </li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config current-context
kubectl get nodes
kubectl get applications <span class="nt">-n</span> argocd
argocd account get-user-info
</code></pre></div></div>

<p>명령은 Argo CD가 설치된 Server에서만 실행해야 하는 것이 아니다. 대상 Cluster의 Kubernetes API와 Argo CD API에 접근할 수 있고 필요한 인증 정보가 설정된 환경이면 실행할 수 있다.</p>

<h2 id="2--namespace-manifest">2 ) Namespace Manifest</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">manifest/ns.yaml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Namespace</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bgd</span>
</code></pre></div></div>

<p>Deployment와 Service는 <code class="language-plaintext highlighter-rouge">bgd</code> Namespace에 생성되는 namespaced Resource이다. Namespace Manifest를 같은 Source에 두면 Argo CD가 전체 Resource를 동기화할 때 Namespace도 Desired State로 관리한다.</p>

<h2 id="3--deployment-manifest">3 ) Deployment Manifest</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">manifest/deployment.yaml</code>을 작성한다. <code class="language-plaintext highlighter-rouge">&lt;dockerhub-username&gt;</code>은 실제 Image를 Push한 Docker Hub 계정으로 바꾼다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">bgd</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">bgd</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">bgd</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">docker.io/&lt;dockerhub-username&gt;/bgd:1.0.0</span>
          <span class="na">env</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">COLOR</span>
              <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">blue"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">spec.selector.matchLabels</code>와 <code class="language-plaintext highlighter-rouge">spec.template.metadata.labels</code>는 모두 <code class="language-plaintext highlighter-rouge">app: bgd</code>로 일치해야 한다. Deployment Controller는 이 Label을 이용해 자신이 관리할 Pod를 식별한다. Selector는 Deployment를 생성한 뒤 변경할 수 없으므로 이름만 비슷하게 두지 말고 실제 Key와 Value가 일치하는지 확인한다.</p>

<p>Manifest 생성 도구의 출력에 나타날 수 있는 <code class="language-plaintext highlighter-rouge">creationTimestamp: null</code>, <code class="language-plaintext highlighter-rouge">strategy: {}</code>와 <code class="language-plaintext highlighter-rouge">resources: {}</code>는 이 실습에서 필요한 설정이 아니다. API Server가 관리하거나 기본값을 적용하는 Field이므로 학습용 선언에서는 제외한다.</p>

<h2 id="4--service-manifest">4 ) Service Manifest</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">manifest/svc.yaml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">bgd</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">NodePort</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">bgd</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">nodePort</span><span class="pi">:</span> <span class="m">31080</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">selector</code></td>
      <td><code class="language-plaintext highlighter-rouge">app: bgd</code> Label을 가진 Pod를 Service Endpoint로 선택</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">port</code></td>
      <td>Cluster 내부에서 Service가 제공하는 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">targetPort</code></td>
      <td>선택된 Pod의 Application으로 전달할 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">nodePort</code></td>
      <td>Node IP를 통해 외부에서 접근할 고정 Port</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">31080</code>은 Kubernetes의 기본 NodePort 범위인 <code class="language-plaintext highlighter-rouge">30000-32767</code> 안에 있지만 다른 Service가 이미 사용 중이면 생성이 거절된다. 고정 번호가 필요하지 않다면 <code class="language-plaintext highlighter-rouge">nodePort</code>를 생략하고 Control Plane이 할당한 값을 조회할 수 있다. NodePort의 현재 동작과 Port 범위는 <a href="https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport">Kubernetes Service 공식 문서</a>에서 확인할 수 있다.</p>

<h2 id="5--manifest-검증과-github-push">5 ) Manifest 검증과 GitHub Push</h2>

<hr />

<p>Repository Root에서 YAML 문법과 Kubernetes Resource 변환 결과를 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="se">\</span>
  <span class="nt">--dry-run</span><span class="o">=</span>client <span class="se">\</span>
  <span class="nt">--filename</span> manifest/ <span class="se">\</span>
  <span class="nt">--output</span> yaml
</code></pre></div></div>

<p>이 명령은 Local 검증이며 Image가 Registry에 실제로 존재하는지, NodePort가 Cluster에서 사용 중인지까지 확인하지는 않는다.</p>

<p>검증한 Manifest를 Commit하고 GitHub에 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git add manifest/
git diff <span class="nt">--staged</span>
git commit <span class="nt">-m</span> <span class="s2">"deploy: add bgd Kubernetes manifests"</span>
git push origin main
</code></pre></div></div>

<p>Argo CD는 Working Tree의 File을 읽지 않는다. <code class="language-plaintext highlighter-rouge">repoURL</code>과 <code class="language-plaintext highlighter-rouge">targetRevision</code>이 가리키는 Remote Repository의 Commit을 기준으로 Desired State를 만든다.</p>

<h2 id="6--argo-cd-application-작성">6 ) Argo CD Application 작성</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">bgd-app.yaml</code>을 작성한다. <code class="language-plaintext highlighter-rouge">&lt;github-owner&gt;</code>는 실제 Repository Owner로 바꾼다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">argoproj.io/v1alpha1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Application</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bgd-app</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">argocd</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">project</span><span class="pi">:</span> <span class="s">default</span>
  <span class="na">source</span><span class="pi">:</span>
    <span class="na">repoURL</span><span class="pi">:</span> <span class="s">https://github.com/&lt;github-owner&gt;/bgd.git</span>
    <span class="na">path</span><span class="pi">:</span> <span class="s">manifest</span>
    <span class="na">targetRevision</span><span class="pi">:</span> <span class="s">main</span>
  <span class="na">destination</span><span class="pi">:</span>
    <span class="na">server</span><span class="pi">:</span> <span class="s">https://kubernetes.default.svc</span>
    <span class="na">namespace</span><span class="pi">:</span> <span class="s">bgd</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>값의 의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metadata.namespace: argocd</code></td>
      <td><code class="language-plaintext highlighter-rouge">Application</code> CRD가 저장되는 Namespace</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">source.repoURL</code></td>
      <td>Desired State를 읽을 Git Repository</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">source.path</code></td>
      <td>Repository 안에서 Manifest가 있는 Directory</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">source.targetRevision</code></td>
      <td>추적할 Branch, Tag 또는 Commit</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">destination.server</code></td>
      <td>Argo CD와 같은 Kubernetes Cluster의 API 주소</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">destination.namespace</code></td>
      <td>Namespace가 없는 namespaced Manifest에 적용할 기본 대상</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">destination.namespace</code>를 적었다고 해서 존재하지 않는 Namespace가 자동으로 만들어지는 것은 아니다. 이 실습에서는 <code class="language-plaintext highlighter-rouge">manifest/ns.yaml</code>이 <code class="language-plaintext highlighter-rouge">bgd</code> Namespace를 선언한다. Argo CD Application Field의 전체 구조는 <a href="https://argo-cd.readthedocs.io/en/stable/user-guide/application-specification/">Application Specification 공식 문서</a>에서 확인할 수 있다.</p>

<h2 id="7--application-등록과-초기-상태">7 ) Application 등록과 초기 상태</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">Application</code> Resource를 Cluster에 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">--filename</span> bgd-app.yaml
</code></pre></div></div>

<p>Application과 Argo CD가 계산한 Resource 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get applications <span class="nt">-n</span> argocd
argocd app list
argocd app get bgd-app
kubectl get namespace bgd
</code></pre></div></div>

<p>이 Application에는 <code class="language-plaintext highlighter-rouge">syncPolicy.automated</code>가 없다. Argo CD가 Git의 Manifest를 읽어 Cluster와 차이를 감지해도 자동으로 적용하지 않는다.</p>

<table>
  <thead>
    <tr>
      <th>표시</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OutOfSync</code></td>
      <td>Git의 Desired State와 Cluster의 Live State가 다름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Missing</code></td>
      <td>Git에 선언된 Resource가 Cluster에 아직 없음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Manual</code></td>
      <td>사용자가 Sync를 요청해야 Resource 변경이 적용됨</td>
    </tr>
  </tbody>
</table>

<p>상태 계산에는 Repository 조회와 Reconciliation 시간이 필요하므로 Application을 만든 직후 한 번의 출력만 보고 실패로 판단하지 않는다. <code class="language-plaintext highlighter-rouge">argocd app get bgd-app --refresh</code>로 Source를 다시 조회한 뒤 상세 Condition을 확인한다.</p>

<h2 id="8--수동-sync-실행">8 ) 수동 Sync 실행</h2>

<hr />

<p>수동 Sync를 요청한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>argocd app <span class="nb">sync </span>bgd-app
</code></pre></div></div>

<p>Sync 요청 후 Workload가 정상 상태에 도달할 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>argocd app <span class="nb">wait </span>bgd-app <span class="se">\</span>
  <span class="nt">--health</span> <span class="se">\</span>
  <span class="nt">--timeout</span> 300
</code></pre></div></div>

<p>수동 정책에서 Sync가 필요한 것은 최초 한 번뿐이 아니다. 이후 Git의 Manifest를 변경해 Push하면 Application은 다시 <code class="language-plaintext highlighter-rouge">OutOfSync</code>가 되며 사용자가 다시 Sync를 요청해야 한다. Git 변경을 자동 반영하려면 <code class="language-plaintext highlighter-rouge">syncPolicy.automated</code>를 별도로 선언해야 한다.</p>

<h2 id="9--control-plane과-worker의-동작">9 ) Control Plane과 Worker의 동작</h2>

<hr />

<p>수동 Sync 명령 이후에는 다음 순서로 상태가 변한다.</p>

<ol>
  <li>
    <p>Argo CD Application Controller가 Git에서 Rendering한 Namespace, Deployment와 Service를 Kubernetes API Server에 전달한다.</p>
  </li>
  <li>
    <p>API Server가 Resource를 검증하고 Cluster State에 저장한다.</p>
  </li>
  <li>
    <p>Deployment Controller가 Deployment의 Desired Replica에 맞는 ReplicaSet과 Pod를 생성한다.</p>
  </li>
  <li>
    <p>Scheduler가 Pod를 실행할 Worker Node를 선택한다.</p>
  </li>
  <li>
    <p>선택된 Worker의 kubelet이 Container Runtime에 <code class="language-plaintext highlighter-rouge">bgd:1.0.0</code> Image Pull과 Container 생성을 요청한다.</p>
  </li>
  <li>
    <p>Control Plane의 EndpointSlice Controller가 <code class="language-plaintext highlighter-rouge">app: bgd</code> Pod를 Service Endpoint로 연결하고, 각 Node의 Service Data Plane이 ClusterIP와 NodePort 전달 경로를 구성한다.</p>
  </li>
  <li>
    <p>Argo CD가 Kubernetes API에서 Live State를 다시 읽어 Sync와 Health 상태를 갱신한다.</p>
  </li>
</ol>

<p>Argo CD는 Worker에서 Container를 직접 실행하지 않는다. Argo CD는 Git과 Kubernetes Resource의 차이를 조정하고, 실제 Pod 배치와 실행은 Kubernetes Control Plane과 Worker 구성 요소가 담당한다.</p>

<h2 id="10--배포-결과-확인과-문제-진단">10 ) 배포 결과 확인과 문제 진단</h2>

<hr />

<p>Argo CD 상태와 Kubernetes Runtime 상태를 함께 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>argocd app get bgd-app
kubectl get deployment,pods,service <span class="nt">-n</span> bgd <span class="nt">-o</span> wide
kubectl get endpointslices <span class="nt">-n</span> bgd <span class="se">\</span>
  <span class="nt">--label-selector</span> kubernetes.io/service-name<span class="o">=</span>bgd
</code></pre></div></div>

<p>Service Manifest가 <code class="language-plaintext highlighter-rouge">type: NodePort</code>이므로 조회 결과도 <code class="language-plaintext highlighter-rouge">NodePort</code>여야 한다. <code class="language-plaintext highlighter-rouge">ClusterIP</code>로 표시된다면 GitHub에 Push된 <code class="language-plaintext highlighter-rouge">svc.yaml</code>, Application의 Source Revision과 실제 Live Manifest를 비교한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get service bgd <span class="nt">-n</span> bgd <span class="nt">-o</span> yaml
argocd app manifests bgd-app
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Repository 조회 실패</td>
      <td><code class="language-plaintext highlighter-rouge">repoURL</code>, Repository 공개 여부와 등록된 Credential</td>
    </tr>
    <tr>
      <td>Manifest가 표시되지 않음</td>
      <td><code class="language-plaintext highlighter-rouge">path</code>, <code class="language-plaintext highlighter-rouge">targetRevision</code>, Git Push 여부</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OutOfSync</code> 유지</td>
      <td>Sync Operation 결과와 Application Condition</td>
    </tr>
    <tr>
      <td>Pod가 <code class="language-plaintext highlighter-rouge">ImagePullBackOff</code></td>
      <td>Image 이름, Tag, Registry 공개 여부와 Pull Secret</td>
    </tr>
    <tr>
      <td>Service Endpoint 없음</td>
      <td>Service Selector와 Pod Label, Pod Ready 상태</td>
    </tr>
    <tr>
      <td>NodePort 생성 실패</td>
      <td><code class="language-plaintext highlighter-rouge">31080</code> 사용 여부와 Cluster NodePort 범위</td>
    </tr>
  </tbody>
</table>

<p>Pod가 생성됐지만 정상 실행되지 않으면 Event와 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod <span class="nt">-n</span> bgd &lt;pod-name&gt;
kubectl logs <span class="nt">-n</span> bgd &lt;pod-name&gt;
kubectl get events <span class="nt">-n</span> bgd <span class="nt">--sort-by</span><span class="o">=</span>.lastTimestamp
</code></pre></div></div>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>일반 Kubernetes Manifest를 Git Repository에 저장하면 Argo CD가 해당 Directory를 Desired State로 사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">Application</code>은 <code class="language-plaintext highlighter-rouge">argocd</code> Namespace에 생성되고 Namespace, Deployment와 Service는 배포 대상인 <code class="language-plaintext highlighter-rouge">bgd</code> Namespace에 생성된다.</p>
    </li>
    <li>
      <p>자동 Sync를 선언하지 않은 Application은 Git 변경을 감지해도 사용자가 Sync를 요청할 때까지 Cluster를 변경하지 않는다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">OutOfSync</code>와 <code class="language-plaintext highlighter-rouge">Missing</code>은 동기화 전 예상 가능한 상태이며, Sync 이후에는 Argo CD 상태와 Kubernetes Pod·Service 상태를 함께 확인한다.</p>
    </li>
    <li>
      <p>Argo CD가 Kubernetes API에 Desired Resource를 전달한 뒤 Control Plane의 Controller·Scheduler와 Worker의 kubelet·Runtime이 실제 Pod 실행을 이어간다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-58-ansible-architecture-inventory/">Ansible Architecture와 Inventory 구성</a>에서는 Control Node가 Inventory와 Playbook을 읽고 SSH로 여러 Managed Node를 자동화하는 구조를 구성한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="ArgoCD" /><summary type="html"><![CDATA[Kubernetes Manifest를 GitHub Repository에 저장하고 Argo CD Application으로 등록하여 OutOfSync 상태를 수동 동기화한다]]></summary></entry><entry><title type="html">Docker로 GitLab CE 설치와 운영 준비</title><link href="https://hyn128.site/cloud-native-54-gitlab-docker-installation/" rel="alternate" type="text/html" title="Docker로 GitLab CE 설치와 운영 준비" /><published>2026-09-17T00:00:00+09:00</published><updated>2026-09-17T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-54-gitlab-docker-installation</id><content type="html" xml:base="https://hyn128.site/cloud-native-54-gitlab-docker-installation/"><![CDATA[<p><a href="/cloud-native-46-github-actions-ci/">GitHub Actions Workflow와 CI/CD 실습</a>에서는 GitHub Repository의 Event를 기준으로 Test, Image Build와 SSH 배포를 자동화했다. GitLab CE는 Source Repository, Merge Request와 CI/CD 기능을 직접 운영할 수 있는 Git Server이다. 이 글에서는 Linux Server에 GitLab CE를 Docker Container로 구성하고 지속적으로 유지할 Data와 운영 확인 항목을 정리한다.</p>

<h2 id="1--gitlab-server-resource-준비">1 ) GitLab Server Resource 준비</h2>

<hr />

<p>GitLab은 Web Application, Background Job, PostgreSQL, Redis와 Repository Storage 등 여러 Component를 함께 실행한다. 작은 실습 환경이라도 Memory를 넉넉하게 할당하고 Host Disk의 여유 공간을 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>free <span class="nt">-h</span>
<span class="nb">df</span> <span class="nt">-h</span>
<span class="nb">nproc</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>실습 환경에서 확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Memory</td>
      <td>최소 4GiB 이상을 확보하고 실제 사용량을 관찰</td>
    </tr>
    <tr>
      <td>Swap</td>
      <td>Memory가 일시적으로 부족할 때 Process 종료 가능성을 낮추는 보조 공간</td>
    </tr>
    <tr>
      <td>Disk</td>
      <td>Repository, Artifact, Container Registry와 Log 증가량 고려</td>
    </tr>
    <tr>
      <td>CPU</td>
      <td>초기 설정과 Upgrade 중 사용량 증가 고려</td>
    </tr>
  </tbody>
</table>

<p>Swap은 Memory를 대체하지 않는다. Disk I/O를 사용하므로 지속적으로 Swap을 사용한다면 Memory 증설이나 GitLab Component 조정이 필요하다.</p>

<h2 id="2--swap-file-생성">2 ) Swap File 생성</h2>

<hr />

<p>4GiB Swap File을 만든다. <code class="language-plaintext highlighter-rouge">fallocate</code>를 지원하지 않는 File System에서는 <code class="language-plaintext highlighter-rouge">dd</code>를 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>fallocate <span class="nt">-l</span> 4G /swapfile
<span class="nb">sudo chmod </span>600 /swapfile
<span class="nb">sudo </span>mkswap /swapfile
<span class="nb">sudo </span>swapon /swapfile
</code></pre></div></div>

<p>생성 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>swapon <span class="nt">--show</span>
free <span class="nt">-h</span>
</code></pre></div></div>

<p>재부팅 후에도 활성화하려면 <code class="language-plaintext highlighter-rouge">/etc/fstab</code>에 다음 항목을 한 번만 추가한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/swapfile none swap sw 0 0
</code></pre></div></div>

<p>추가 후 문법과 Mount 가능 여부를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>mount <span class="nt">-a</span>
swapon <span class="nt">--show</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">/etc/fstab</code>을 잘못 작성하면 Boot 과정에 영향을 줄 수 있으므로 기존 내용을 Backup하고 중복 항목이 없는지 확인한다.</p>

<h2 id="3--gitlab-data-directory">3 ) GitLab Data Directory</h2>

<hr />

<p>Container를 삭제하거나 교체해도 설정과 Repository가 유지되도록 Host Directory를 준비한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> <span class="se">\</span>
  /srv/gitlab/config <span class="se">\</span>
  /srv/gitlab/logs <span class="se">\</span>
  /srv/gitlab/data
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Host Directory</th>
      <th>Container Directory</th>
      <th>저장 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/srv/gitlab/config</code></td>
      <td><code class="language-plaintext highlighter-rouge">/etc/gitlab</code></td>
      <td><code class="language-plaintext highlighter-rouge">gitlab.rb</code>, 인증서와 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/srv/gitlab/logs</code></td>
      <td><code class="language-plaintext highlighter-rouge">/var/log/gitlab</code></td>
      <td>GitLab Component Log</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/srv/gitlab/data</code></td>
      <td><code class="language-plaintext highlighter-rouge">/var/opt/gitlab</code></td>
      <td>Repository, Database와 Application Data</td>
    </tr>
  </tbody>
</table>

<p>세 Directory는 Backup 대상이다. Image만 보관해서는 GitLab Repository와 설정을 복구할 수 없다.</p>

<h2 id="4--gitlab-ce-container-실행">4 ) GitLab CE Container 실행</h2>

<hr />

<p>운영 환경에서는 <code class="language-plaintext highlighter-rouge">latest</code> 대신 검증한 GitLab Version Tag를 고정한다. 사용할 Version은 <a href="https://docs.gitlab.com/install/docker/installation/">GitLab Docker 설치 문서</a>와 Container Registry의 CE Tag에서 확인한다.</p>

<p>다음 예제에서 Hostname과 Version을 실제 환경 값으로 바꾼다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GITLAB_HOSTNAME</span><span class="o">=</span><span class="s1">'gitlab.example.com'</span>
<span class="nb">export </span><span class="nv">GITLAB_VERSION</span><span class="o">=</span><span class="s1">'&lt;major.minor.patch&gt;-ce.0'</span>

<span class="nb">sudo </span>docker run <span class="nt">--detach</span> <span class="se">\</span>
  <span class="nt">--hostname</span> <span class="s2">"</span><span class="nv">$GITLAB_HOSTNAME</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">--env</span> <span class="nv">GITLAB_OMNIBUS_CONFIG</span><span class="o">=</span><span class="s2">"external_url 'https://</span><span class="nv">$GITLAB_HOSTNAME</span><span class="s2">'; gitlab_rails['gitlab_shell_ssh_port'] = 2222"</span> <span class="se">\</span>
  <span class="nt">--publish</span> 443:443 <span class="se">\</span>
  <span class="nt">--publish</span> 80:80 <span class="se">\</span>
  <span class="nt">--publish</span> 2222:22 <span class="se">\</span>
  <span class="nt">--name</span> gitlab <span class="se">\</span>
  <span class="nt">--restart</span> always <span class="se">\</span>
  <span class="nt">--volume</span> /srv/gitlab/config:/etc/gitlab <span class="se">\</span>
  <span class="nt">--volume</span> /srv/gitlab/logs:/var/log/gitlab <span class="se">\</span>
  <span class="nt">--volume</span> /srv/gitlab/data:/var/opt/gitlab <span class="se">\</span>
  <span class="nt">--shm-size</span> 256m <span class="se">\</span>
  <span class="s2">"gitlab/gitlab-ce:</span><span class="nv">$GITLAB_VERSION</span><span class="s2">"</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--hostname</code></td>
      <td>GitLab이 Link와 Clone URL 생성에 사용할 DNS 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">external_url</code></td>
      <td>사용자가 접속할 외부 URL</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gitlab_shell_ssh_port</code></td>
      <td>SSH Clone URL에 표시할 외부 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--publish 2222:22</code></td>
      <td>Host <code class="language-plaintext highlighter-rouge">2222</code>를 Container SSH <code class="language-plaintext highlighter-rouge">22</code>에 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--restart always</code></td>
      <td>Docker Daemon 시작 후 GitLab 자동 복구</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--shm-size 256m</code></td>
      <td>Prometheus Metric 등에 사용하는 Shared Memory 확대</td>
    </tr>
  </tbody>
</table>

<p>Host SSH Server가 이미 Port <code class="language-plaintext highlighter-rouge">22</code>를 사용하므로 GitLab SSH는 <code class="language-plaintext highlighter-rouge">2222</code> 같은 별도 Port에 연결한다. Firewall이나 Cloud Security Group에서도 실제로 사용할 HTTP, HTTPS와 SSH Port만 허용한다.</p>

<p>공인 IP를 <code class="language-plaintext highlighter-rouge">--hostname</code>으로 고정하면 IP 변경과 TLS 인증서 관리가 어렵다. DNS 이름을 사용하고, DNS Record와 인증서가 준비되지 않은 학습 환경에서는 HTTP 구성과 운영용 HTTPS 구성을 명확히 분리한다.</p>

<h2 id="5--초기화-상태와-log-확인">5 ) 초기화 상태와 Log 확인</h2>

<hr />

<p>GitLab은 첫 실행에서 내부 Service와 Database를 구성하므로 시작까지 시간이 걸릴 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker ps <span class="nt">--filter</span> <span class="nv">name</span><span class="o">=</span>gitlab
<span class="nb">sudo </span>docker logs <span class="nt">--follow</span> gitlab
</code></pre></div></div>

<p>별도 Terminal에서 Health 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker inspect gitlab <span class="se">\</span>
  <span class="nt">--format</span> <span class="s1">'{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}'</span>
</code></pre></div></div>

<p>Log 추적은 <code class="language-plaintext highlighter-rouge">Ctrl+C</code>로 종료해도 Container 실행에는 영향을 주지 않는다. 오류가 반복되면 Memory, Disk 여유 공간, Volume 권한과 <code class="language-plaintext highlighter-rouge">external_url</code>을 먼저 확인한다.</p>

<h2 id="6--초기-root-password-처리">6 ) 초기 Root Password 처리</h2>

<hr />

<p>초기 Password File이 남아 있는 기간에는 다음 명령으로 값을 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker <span class="nb">exec </span>gitlab <span class="se">\</span>
  <span class="nb">grep</span> <span class="s1">'Password:'</span> /etc/gitlab/initial_root_password
</code></pre></div></div>

<p>이 값은 Blog, Screenshot, Shell Script나 Git Repository에 기록하지 않는다. 첫 Login 직후 Root Password를 변경하고, 초기 Password File이 영구적인 Credential 저장소가 아니라는 점을 전제로 운영한다.</p>

<p>이미 공개된 초기 Password는 File에서 문자열을 지우는 것으로 안전해지지 않는다. Password를 변경하고 활성 Session과 Access Token도 확인해야 한다.</p>

<h2 id="7--container-재시작과-data-유지-확인">7 ) Container 재시작과 Data 유지 확인</h2>

<hr />

<p>Container를 재시작한 뒤 Project와 설정이 유지되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker restart gitlab
<span class="nb">sudo </span>docker ps <span class="nt">--filter</span> <span class="nv">name</span><span class="o">=</span>gitlab
<span class="nb">sudo </span>docker logs <span class="nt">--tail</span> 100 gitlab
</code></pre></div></div>

<p>Volume 연결 상태도 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker inspect gitlab <span class="se">\</span>
  <span class="nt">--format</span> <span class="s1">'{{range .Mounts}}{{println .Source "-&gt;" .Destination}}{{end}}'</span>
</code></pre></div></div>

<p>GitLab Upgrade는 Container Image만 바꾸는 단순 작업으로 취급하면 안 된다. 현재 Version에서 지원하는 Upgrade Path를 확인하고 <code class="language-plaintext highlighter-rouge">/srv/gitlab</code> Backup을 만든 뒤 단계적으로 진행한다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>GitLab은 여러 Component를 함께 실행하므로 Memory, Swap과 Disk 사용량을 먼저 확인한다.</p>
    </li>
    <li>
      <p>설정, Log와 Application Data를 Host Volume에 분리해 Container 수명과 Data 수명을 분리한다.</p>
    </li>
    <li>
      <p>운영 환경에서는 <code class="language-plaintext highlighter-rouge">latest</code>가 아니라 검증한 GitLab Version Tag를 고정한다.</p>
    </li>
    <li>
      <p>Hostname, <code class="language-plaintext highlighter-rouge">external_url</code>, 외부 SSH Port와 실제 Port Mapping을 일치시킨다.</p>
    </li>
    <li>
      <p>초기 Root Password는 첫 Login 후 변경하고 공개된 Credential은 즉시 폐기한다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-55-gitops-argocd-architecture-installation/">GitOps와 Argo CD Architecture·설치</a>에서는 Git Repository의 Desired State와 Kubernetes Cluster의 Live State를 지속적으로 일치시키는 구조를 정리한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="GitLab" /><summary type="html"><![CDATA[GitLab CE를 Docker Container로 설치하고 Memory·Swap, Port, Volume, 초기 Password와 운영 점검 항목을 정리한다]]></summary></entry><entry><title type="html">GitOps와 Argo CD Architecture·설치</title><link href="https://hyn128.site/cloud-native-55-gitops-argocd-architecture-installation/" rel="alternate" type="text/html" title="GitOps와 Argo CD Architecture·설치" /><published>2026-09-17T00:00:00+09:00</published><updated>2026-09-17T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-55-gitops-argocd-architecture-installation</id><content type="html" xml:base="https://hyn128.site/cloud-native-55-gitops-argocd-architecture-installation/"><![CDATA[<p><a href="/cloud-native-54-gitlab-docker-installation/">Docker로 GitLab CE 설치와 운영 준비</a>에서는 Source Repository를 직접 운영하기 위한 Git Server를 구성했다. GitOps는 Git Repository에 선언한 Desired State를 기준으로 Kubernetes Cluster를 지속적으로 조정한다. 이 글에서는 일반적인 CI와 GitOps CD의 경계를 구분하고, Argo CD의 구성 요소와 설치 흐름을 Control Plane과 Worker 관점에서 연결한다.</p>

<h2 id="1--gitops">1 ) GitOps</h2>

<hr />

<blockquote>
  <p><strong>GitOps</strong>는 Application과 Infrastructure의 원하는 상태를 선언적 File로 작성해 Git에서 Version 관리하고, 자동화된 Agent가 실제 환경을 그 상태에 맞추는 운영 방식이다.</p>
</blockquote>

<p>Git을 단순한 Manifest 보관 장소로 사용하는 것만으로는 GitOps가 되지 않는다. 변경 Review, Version 이력, 자동 반영과 실제 상태의 지속적인 비교가 함께 동작해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>원칙</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>선언적 구성</td>
      <td>현재 실행 절차가 아니라 최종적으로 원하는 상태를 작성</td>
    </tr>
    <tr>
      <td>Version 관리와 불변 이력</td>
      <td>변경 내용을 Git Commit으로 추적하고 이전 상태를 확인</td>
    </tr>
    <tr>
      <td>자동 반영</td>
      <td>승인된 Git 변경을 Agent가 대상 환경에 적용</td>
    </tr>
    <tr>
      <td>지속적 조정</td>
      <td>Git의 Desired State와 Cluster의 Live State 차이를 반복 확인</td>
    </tr>
  </tbody>
</table>

<p>예를 들어 “Pod를 세 개 생성하라”는 일회성 명령보다 <code class="language-plaintext highlighter-rouge">replicas: 3</code>이라는 상태를 선언한다. Pod 하나가 사라지면 Kubernetes Controller가 세 개라는 상태를 다시 맞추고, Manifest가 바뀌면 GitOps Controller가 변경된 선언을 Cluster에 반영한다.</p>

<h2 id="2--gitops를-사용하는-이유">2 ) GitOps를 사용하는 이유</h2>

<hr />

<ul>
  <li>
    <p><strong>표준 Workflow</strong>: Branch, Pull Request, Review와 Merge라는 Git Workflow로 운영 변경을 처리한다.</p>
  </li>
  <li>
    <p><strong>감사 가능성</strong>: 누가 어떤 값을 언제 변경했는지 Commit 이력으로 확인한다.</p>
  </li>
  <li>
    <p><strong>Rollback 기준</strong>: 이전 Commit이나 Release의 Manifest로 돌아갈 근거가 남는다.</p>
  </li>
  <li>
    <p><strong>구성 Drift 탐지</strong>: 사람이 Cluster를 직접 변경해 Git과 달라진 상태를 찾을 수 있다.</p>
  </li>
  <li>
    <p><strong>다중 환경 일관성</strong>: 개발, 검증과 운영 환경의 공통 Base와 차이를 Version 관리한다.</p>
  </li>
</ul>

<p>GitOps가 모든 수동 작업을 없애는 것은 아니다. 긴급 변경, Secret 관리, Cluster Bootstrap과 Controller 장애 시 복구 절차는 별도로 설계해야 한다.</p>

<h2 id="3--kubernetes-ci와-gitops-cd">3 ) Kubernetes CI와 GitOps CD</h2>

<hr />

<p>CI는 Application Source를 Test하고 Container Image를 만든다. GitOps CD는 배포 Repository의 Manifest 변경을 감시하고 Cluster에 반영한다.</p>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>기준 Repository</th>
      <th>주요 결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Source 변경</td>
      <td>Application Source Repository</td>
      <td>Build와 Test 대상 Code</td>
    </tr>
    <tr>
      <td>CI</td>
      <td>Application Source Repository</td>
      <td>Commit을 식별할 수 있는 Image Tag</td>
    </tr>
    <tr>
      <td>Image 저장</td>
      <td>Container Registry</td>
      <td>배포 가능한 Immutable Artifact</td>
    </tr>
    <tr>
      <td>배포 변경</td>
      <td>Deployment Repository</td>
      <td>새 Image Tag가 기록된 Manifest</td>
    </tr>
    <tr>
      <td>CD 조정</td>
      <td>Deployment Repository</td>
      <td>Cluster의 Live State 변경</td>
    </tr>
  </tbody>
</table>

<p>Application Source와 Deployment Manifest를 두 Repository로 분리하면 Image 생성 권한과 운영 배포 승인 권한을 구분할 수 있다. CI가 Registry에 Image를 Push한 뒤 Deployment Repository의 Image Tag를 Pull Request로 변경하고, Argo CD가 Merge된 Manifest를 Cluster에 반영한다.</p>

<figure class="post-visual gitops-flow" aria-labelledby="argocd-gitops-flow-caption">
  <figcaption id="argocd-gitops-flow-caption" class="post-visual__caption">
    Application Source 변경이 Image와 Deployment Manifest로 분리되고, Argo CD와 Kubernetes의 두 조정 계층을 거쳐 Worker에서 실행되는 흐름
  </figcaption>

  <div class="post-visual__frame gitops-flow__frame">
    <ol class="gitops-flow__layers">
      <li class="gitops-flow__layer gitops-flow__layer--source" data-edge="Push Event">
        <span class="gitops-flow__role">SOURCE</span>
        <strong>Application Source Repository</strong>
        <p>Application Code, Test와 Image Build 정의를 Version 관리한다.</p>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--ci" data-edge="두 결과를 각각 저장">
        <span class="gitops-flow__role">CONTINUOUS INTEGRATION</span>
        <strong>CI Pipeline</strong>
        <p>Test를 통과한 Commit으로 Image를 Build하고 Commit SHA Tag를 부여한다.</p>
        <div class="gitops-flow__actions" aria-label="CI가 생성하는 두 결과">
          <span><b>Image 결과</b> Registry에 Immutable Tag를 Push</span>
          <span><b>배포 변경</b> Deployment Repository의 Image Tag를 수정</span>
        </div>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--handoff" data-edge="Git 변경 감지·Rendering">
        <span class="gitops-flow__role">VERSIONED HANDOFF</span>
        <strong>Artifact와 Desired State 분리</strong>
        <div class="gitops-flow__stores">
          <div>
            <b>Container Registry</b>
            <span>Worker가 실행할 Image를 보관한다.</span>
          </div>
          <div>
            <b>Deployment Repository</b>
            <span>배포할 Image Tag와 Kubernetes Manifest를 보관한다.</span>
          </div>
        </div>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--repository" data-edge="Desired Manifest 전달">
        <span class="gitops-flow__role">ARGO CD</span>
        <strong>Repository Server</strong>
        <p>Git의 Manifest를 가져와 Helm이나 Kustomize를 Kubernetes Manifest로 Rendering한다.</p>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--controller" data-edge="Resource 변경 요청">
        <span class="gitops-flow__role">ARGO CD</span>
        <strong>Application Controller</strong>
        <p>Git의 Desired State와 Cluster의 Live State를 비교하고 차이를 Kubernetes API에 반영한다.</p>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--control" data-edge="Pod를 Node에 배치">
        <span class="gitops-flow__role">KUBERNETES CONTROL PLANE</span>
        <strong>API Server · Controller · Scheduler</strong>
        <p>요청을 저장하고 Resource 상태를 조정한 뒤 새 Pod가 실행될 Worker Node를 선택한다.</p>
      </li>

      <li class="gitops-flow__layer gitops-flow__layer--worker">
        <span class="gitops-flow__role">KUBERNETES WORKER</span>
        <strong>kubelet · Container Runtime · Pod</strong>
        <p>kubelet이 Manifest에 기록된 Tag로 Registry에서 Image를 Pull하고 Container를 실행한다.</p>
      </li>
    </ol>

    <div class="gitops-flow__feedback">
      <strong>상태 Feedback</strong>
      <span>Worker와 Resource 상태는 Kubernetes API를 통해 Application Controller로 돌아가 Sync와 Health 판단에 사용된다.</span>
    </div>
  </div>
</figure>

<h2 id="4--argo-project의-도구">4 ) Argo Project의 도구</h2>

<hr />

<p>Argo 생태계에는 서로 다른 역할의 Kubernetes Native 도구가 있다.</p>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Argo CD</td>
      <td>Git Repository와 Kubernetes 상태를 조정하는 GitOps CD</td>
    </tr>
    <tr>
      <td>Argo Rollouts</td>
      <td>Blue-Green, Canary 같은 점진적 배포 Controller</td>
    </tr>
    <tr>
      <td>Argo Workflows</td>
      <td>DAG와 Step 기반 Kubernetes Workflow Engine</td>
    </tr>
    <tr>
      <td>Argo Events</td>
      <td>Event Source와 Sensor를 이용한 Event 기반 자동화</td>
    </tr>
  </tbody>
</table>

<p>Argo CD는 CI Build Tool이 아니다. Source Compile과 Image Build는 GitHub Actions나 Jenkins 같은 CI가 담당하고, Argo CD는 배포 가능한 Manifest와 Cluster 상태를 다룬다.</p>

<h2 id="5--argo-cd의-상태-모델">5 ) Argo CD의 상태 모델</h2>

<hr />

<p>Argo CD는 Git Repository에서 Rendering한 Manifest와 Kubernetes API에서 읽은 Live Resource를 비교한다.</p>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>확인 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Synced</code></td>
      <td>Desired State와 Live State가 일치</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OutOfSync</code></td>
      <td>Git과 Cluster Resource에 차이가 있음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Healthy</code></td>
      <td>Application Resource가 정상 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Progressing</code></td>
      <td>배포나 상태 전환이 진행 중</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Degraded</code></td>
      <td>일부 Resource가 실패하거나 비정상 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Missing</code></td>
      <td>Git에는 있지만 Cluster에 Resource가 없음</td>
    </tr>
  </tbody>
</table>

<p>Sync Status와 Health Status는 다른 질문에 답한다. Git과 값이 같아도 Application이 Crash하면 <code class="language-plaintext highlighter-rouge">Synced</code>이면서 <code class="language-plaintext highlighter-rouge">Degraded</code>일 수 있다. 반대로 정상 실행 중인 Resource를 Git에서 변경하면 <code class="language-plaintext highlighter-rouge">Healthy</code>이면서 <code class="language-plaintext highlighter-rouge">OutOfSync</code>가 될 수 있다.</p>

<h2 id="6--argo-cd-핵심-구성-요소">6 ) Argo CD 핵심 구성 요소</h2>

<hr />

<h3 id="api-server">API Server</h3>

<p>Web UI, CLI와 외부 System이 Argo CD를 조작하는 진입점이다.</p>

<ul>
  <li>
    <p>Application 생성과 상태 조회</p>
  </li>
  <li>
    <p>Repository와 Cluster Credential 관리</p>
  </li>
  <li>
    <p>Login, SSO와 RBAC</p>
  </li>
  <li>
    <p>Sync와 Rollback 요청 처리</p>
  </li>
</ul>

<h3 id="repository-server">Repository Server</h3>

<p>Git Repository를 Local Cache로 유지하고 배포할 Manifest를 생성한다. Plain YAML뿐 아니라 Kustomize와 Helm을 Rendering한다. Argo CD에서 Helm은 Chart를 Manifest로 변환하는 역할이며 Release Lifecycle은 Argo CD가 관리한다.</p>

<h3 id="application-controller">Application Controller</h3>

<p>Application의 Desired State와 Live State를 지속적으로 비교한다. 차이가 있고 Sync가 요청되면 Kubernetes API를 통해 Resource를 생성·수정·삭제하고 Health를 관찰한다.</p>

<p>Controller가 매번 <code class="language-plaintext highlighter-rouge">kubectl</code> Process를 실행한다고 이해하면 안 된다. Argo CD가 Kubernetes API와 통신해 같은 목적의 Resource 변경을 수행한다.</p>

<h2 id="7--control-plane과-worker에서-보는-조정-과정">7 ) Control Plane과 Worker에서 보는 조정 과정</h2>

<hr />

<p>Argo CD Component도 Kubernetes Pod로 실행되지만, 배포 대상 Workload를 직접 실행하지는 않는다.</p>

<ol>
  <li>
    <p>Repository Server가 Git 또는 Helm Repository에서 Manifest를 Rendering한다.</p>
  </li>
  <li>
    <p>Application Controller가 Rendering 결과와 API Server에서 읽은 Live State를 비교한다.</p>
  </li>
  <li>
    <p>차이가 있으면 Application Controller가 Kubernetes API Server에 Resource 변경을 요청한다.</p>
  </li>
  <li>
    <p>Control Plane의 Controller가 Deployment와 ReplicaSet 상태를 맞춘다.</p>
  </li>
  <li>
    <p>Scheduler가 새 Pod를 실행할 Worker Node를 선택한다.</p>
  </li>
  <li>
    <p>Worker의 kubelet이 Container Runtime에 Image Pull과 Container 생성을 요청한다.</p>
  </li>
  <li>
    <p>Application Controller가 API Server를 통해 Resource 상태를 다시 읽고 Sync와 Health를 갱신한다.</p>
  </li>
</ol>

<p>Argo CD의 조정과 Kubernetes Controller의 조정은 계층이 다르다. Argo CD는 Git과 Kubernetes Resource를 맞추고, Kubernetes Controller는 Resource Spec과 실제 Pod·Node 상태를 맞춘다.</p>

<h2 id="8--설치-전-확인">8 ) 설치 전 확인</h2>

<hr />

<p>Argo CD를 설치할 Cluster와 현재 Context를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config current-context
kubectl get nodes <span class="nt">-o</span> wide
helm version
</code></pre></div></div>

<p>실습 환경은 단일 Argo CD Instance가 같은 Cluster를 관리하는 구성을 사용한다. 운영 환경에서는 Kubernetes와 Argo CD Version 호환성, RBAC 범위, HA 여부와 관리 대상 Cluster를 먼저 결정한다. 공식 설치 유형은 <a href="https://argo-cd.readthedocs.io/en/stable/operator-manual/installation/">Argo CD Installation</a>에서 확인할 수 있다.</p>

<h2 id="9--helm으로-argo-cd-설치">9 ) Helm으로 Argo CD 설치</h2>

<hr />

<p>Community에서 관리하는 Argo Helm Repository를 추가하고 Index를 갱신한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm repo add argo https://argoproj.github.io/argo-helm
helm repo update
helm search repo argo/argo-cd <span class="nt">--versions</span> | <span class="nb">head</span>
</code></pre></div></div>

<p>Namespace를 만들고 사용할 Chart Version을 명시해 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create namespace argocd

helm <span class="nb">install </span>argocd argo/argo-cd <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  <span class="nt">--version</span> &lt;verified-chart-version&gt;
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">&lt;verified-chart-version&gt;</code>에는 <code class="language-plaintext highlighter-rouge">helm search</code>로 확인하고 Kubernetes Version과 검증한 값을 사용한다. Version을 생략하면 실행 시점의 최신 Chart가 선택되어 같은 명령의 결과가 달라질 수 있다.</p>

<p>Manifest 방식이 필요한 경우 공식 Stable Manifest를 적용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  <span class="nt">--filename</span> https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
</code></pre></div></div>

<p>평가용 Non-HA Manifest와 운영용 HA 구성은 목적이 다르다. 설치 방식을 혼합하기보다 Helm 또는 Manifest 중 하나를 선택해 Release와 Upgrade 경로를 일관되게 관리한다.</p>

<h2 id="10--설치-상태-확인">10 ) 설치 상태 확인</h2>

<hr />

<p>Argo CD Pod와 Service를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> argocd <span class="nt">-o</span> wide
kubectl get services <span class="nt">-n</span> argocd
kubectl get deployments,statefulsets <span class="nt">-n</span> argocd
</code></pre></div></div>

<p>Pod가 준비될 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait</span> <span class="se">\</span>
  <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Available <span class="se">\</span>
  deployment/argocd-server <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>300s
</code></pre></div></div>

<p>문제가 있으면 상태와 Event를 함께 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod <span class="nt">-n</span> argocd &lt;pod-name&gt;
kubectl logs <span class="nt">-n</span> argocd &lt;pod-name&gt; <span class="nt">--all-containers</span>
kubectl get events <span class="nt">-n</span> argocd <span class="nt">--sort-by</span><span class="o">=</span>.lastTimestamp
</code></pre></div></div>

<h2 id="11--web-ui-접근-방식">11 ) Web UI 접근 방식</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">argocd-server</code> Service는 기본적으로 Cluster 외부에 공개되지 않는다. 학습 환경에서는 Port Forward로 Local Computer에서만 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward <span class="se">\</span>
  service/argocd-server <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  8080:443
</code></pre></div></div>

<p>다른 Terminal에서 <code class="language-plaintext highlighter-rouge">https://localhost:8080</code>에 접속한다. 기본 인증서는 자체 서명 인증서이므로 운영 환경에서는 신뢰할 수 있는 인증서를 적용한 Ingress나 LoadBalancer를 구성한다.</p>

<p>NodePort로 바꾸면 모든 Node IP와 할당된 Port를 통해 접근할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl patch service argocd-server <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  <span class="nt">--type</span> merge <span class="se">\</span>
  <span class="nt">--patch</span> <span class="s1">'{"spec":{"type":"NodePort"}}'</span>

kubectl get service argocd-server <span class="nt">-n</span> argocd
</code></pre></div></div>

<p>NodePort 번호는 Cluster가 할당하므로 예제의 고정 번호를 가정하지 않고 조회 결과를 사용한다. 방화벽과 접근 가능한 Source 대역도 함께 제한한다.</p>

<h2 id="12--초기-admin-password">12 ) 초기 Admin Password</h2>

<hr />

<p>초기 계정 이름은 <code class="language-plaintext highlighter-rouge">admin</code>이다. Password는 Secret에서 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get secret argocd-initial-admin-secret <span class="se">\</span>
  <span class="nt">--namespace</span> argocd <span class="se">\</span>
  <span class="nt">--output</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.password}'</span> <span class="se">\</span>
  | <span class="nb">base64</span> <span class="nt">--decode</span>
<span class="nb">echo</span>
</code></pre></div></div>

<p>Password 값을 Blog, Screenshot이나 Shell Script에 저장하지 않는다. 첫 Login 직후 Password를 변경하고 초기 Secret의 보관 필요성을 검토한다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>GitOps는 Git의 선언과 실제 환경을 자동으로 지속해서 비교·조정한다.</p>
    </li>
    <li>
      <p>CI는 Test와 Image 생성을, Argo CD는 Manifest 기반 CD를 담당한다.</p>
    </li>
    <li>
      <p>Repository Server가 Manifest를 만들고 Application Controller가 Kubernetes API를 통해 상태를 맞춘다.</p>
    </li>
    <li>
      <p>Argo CD 조정 뒤에는 Control Plane의 Controller·Scheduler와 Worker의 kubelet·Runtime 동작이 이어진다.</p>
    </li>
    <li>
      <p>학습 환경에서는 Port Forward, 운영 환경에서는 TLS와 접근 제어를 갖춘 Ingress나 LoadBalancer를 사용한다.</p>
    </li>
  </ul>
</blockquote>

<p>다음 글인 <a href="/cloud-native-56-argocd-application-cli-autopilot/">Argo CD Application 배포와 CLI·Autopilot</a>에서는 실제 <code class="language-plaintext highlighter-rouge">Application</code> Resource를 생성하고 Sync 결과를 확인한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="ArgoCD" /><summary type="html"><![CDATA[GitOps의 선언적 Desired State와 지속적 조정을 이해하고 Argo CD 구성 요소를 Kubernetes에 Helm으로 설치한다]]></summary></entry></feed>