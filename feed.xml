<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://kkr010128.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://kkr010128.github.io/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-08-17T12:34:25+09:00</updated><id>https://kkr010128.github.io/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">Docker와 Container 가상화</title><link href="https://kkr010128.github.io/cloud-native-08-docker-overview/" rel="alternate" type="text/html" title="Docker와 Container 가상화" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-08-docker-overview</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-08-docker-overview/"><![CDATA[<h2 id="1--가상화">1 ) 가상화</h2>

<hr />

<p>가상화(Virtualization)는 <strong>물리적 시스템의 CPU, Memory, Storage, Network 같은 자원을 추상화하여 여러 가상 환경에서 사용할 수 있게 하는 기술</strong>이다.</p>

<p>가상화 소프트웨어는 물리적 Hardware의 기능을 논리적으로 분리한다. 하나의 물리적인 Computer에서도 여러 가상 System을 동시에 실행할 수 있으므로 Hardware Resource의 활용도를 높이고 Infrastructure를 유연하게 관리할 수 있다.</p>

<pre><code class="language-mermaid">flowchart TB
    A["물리적 컴퓨터"] --&gt; B["가상화 소프트웨어"]
    B --&gt; C["가상 시스템 1"]
    B --&gt; D["가상 시스템 2"]
    B --&gt; E["가상 시스템 3"]
</code></pre>

<h3 id="가상화의-장점">가상화의 장점</h3>

<ul>
  <li><strong>효율적인 리소스 사용</strong>
    <ul>
      <li>하나의 물리적 System을 여러 환경이 공유하여 Resource를 효율적으로 사용한다.</li>
    </ul>
  </li>
  <li><strong>자동화된 IT 관리</strong>
    <ul>
      <li>가상 환경의 생성, 배포 및 관리를 자동화하기 쉽다.</li>
    </ul>
  </li>
  <li><strong>신속한 재해 복구</strong>
    <ul>
      <li>Snapshot과 복제된 환경을 이용하여 장애 발생 시 빠르게 복구할 수 있다.</li>
    </ul>
  </li>
</ul>

<h3 id="가상화-서비스">가상화 서비스</h3>

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Server 가상화</td>
      <td>하나의 물리적 Server를 여러 가상 Server로 분리</td>
    </tr>
    <tr>
      <td>Storage 가상화</td>
      <td>여러 Storage Resource를 하나의 논리적 Storage처럼 구성</td>
    </tr>
    <tr>
      <td>Network 가상화</td>
      <td>물리적 Network를 논리적으로 분리하고 Software로 제어</td>
    </tr>
    <tr>
      <td>Data 가상화</td>
      <td>여러 위치의 Data를 하나의 Data Source처럼 제공</td>
    </tr>
    <tr>
      <td>Application 가상화</td>
      <td>Application을 운영체제와 분리된 환경에서 실행</td>
    </tr>
    <tr>
      <td>Desktop 가상화</td>
      <td>Desktop 환경을 중앙 Server에서 제공</td>
    </tr>
  </tbody>
</table>

<p>Network 가상화의 대표적인 기술로 SDN(Software-Defined Networking)이 있다. SDN은 물리적 장비가 담당하던 Traffic 제어 기능을 Software로 분리하여 Routing 정책을 중앙에서 관리한다. 예를 들어 영상 통화 Traffic을 일반 Application Traffic보다 우선 처리하도록 구성할 수 있다.</p>

<h3 id="가상화-방식">가상화 방식</h3>

<h4 id="host-os-가상화">Host OS 가상화</h4>

<p>Host OS 위에 가상화 Software를 설치하고 그 위에서 Guest OS를 실행하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
Guest OS
Virtualization Software
Host OS
Hardware
</code></pre></div></div>

<p>Host와 Guest 운영체제의 선택이 비교적 자유롭지만 운영체제 위에서 또 다른 운영체제를 실행하므로 Overhead가 클 수 있다.</p>

<h4 id="hypervisor-가상화">Hypervisor 가상화</h4>

<p>Host OS 없이 Hardware에 Hypervisor를 직접 설치하고 여러 Guest OS를 실행하는 방식이다. Hardware에 직접 설치하는 Hypervisor를 <strong>Bare Metal Hypervisor</strong>라고 하며 Server 가상화에서 주로 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
Guest OS
Hypervisor
Hardware
</code></pre></div></div>

<p>Host OS를 거치지 않으므로 Host OS 방식보다 Overhead가 작다.</p>

<h4 id="container-가상화">Container 가상화</h4>

<p>Host OS 위에 Container Runtime을 설치하고 Application 실행 환경을 논리적으로 격리하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container A        Container B
App + Library      App + Library
--------------------------------
Container Runtime
Host OS
Hardware
</code></pre></div></div>

<p>Container는 Guest OS 전체를 포함하지 않고 Host OS의 Kernel을 공유한다. 따라서 Virtual Machine보다 가볍고 빠르지만 서로 다른 종류의 Kernel을 요구하는 운영체제를 동시에 실행하기 어렵고, 격리 수준도 완전한 Virtual Machine과 차이가 있다.</p>

<p>Container 가상화 Software에는 OpenVZ, LXC, Linux VServer, Docker, Oracle Solaris Zones 등이 있다.</p>

<h2 id="2--container">2 ) Container</h2>

<hr />

<p>Container는 <strong>Application Code와 실행에 필요한 Library 및 의존성을 하나의 격리된 단위로 묶은 실행 환경</strong>이다. Guest OS Layer가 없으므로 Image 크기가 작고 시작 시간이 빠르다.</p>

<h3 id="container의-장점">Container의 장점</h3>

<ul>
  <li>Hypervisor와 Guest OS가 필요하지 않아 가볍다.</li>
  <li>Image의 복제, 이관 및 배포가 쉽다.</li>
  <li>Guest OS를 Booting하지 않으므로 Application 시작이 빠르다.</li>
  <li>동일한 Hardware에서 Virtual Machine보다 많은 Application을 실행할 수 있다.</li>
  <li>Application마다 의존성을 분리하여 Library와 설정의 충돌을 줄인다.</li>
</ul>

<p>Application 배포 방식은 다음과 같이 변화해 왔다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>운영체제에 직접 배포 → Virtual Machine 기반 배포 → Container 기반 배포
</code></pre></div></div>

<h2 id="3--docker">3 ) Docker</h2>

<hr />

<p>Docker는 <strong>Container형 가상화 기술을 구현하는 상주 애플리케이션과 이를 조작하는 명령행 도구</strong>로 구성된다. 애플리케이션과 데이터를 독립된 환경에 격리하여 실행할 수 있게 한다.</p>

<h3 id="특징">특징</h3>

<ul>
  <li>Microservice, DevOps, Testing 등 다양한 분야에서 활용된다.</li>
  <li>Linux Container 구현체의 사실상 표준(de facto)이다.</li>
  <li>가상 머신보다 가볍고 실행 및 배포가 빠르다.</li>
</ul>

<h3 id="docker와-linux">Docker와 Linux</h3>

<p>Docker는 Linux를 전제로 만들어졌다. Windows와 macOS에서도 Docker Desktop을 사용할 수 있지만 내부적으로 Linux 환경을 사용하며, Container에서 실행할 프로그램도 Linux용이다.</p>

<h3 id="lxclinux-containers">LXC(Linux Containers)</h3>

<p>LXC (Linux Containers)는 <strong>OS 수준의 가상화(OS-level Virtualization)</strong>를 구현하는 Linux Container 기술이다.
하나의 Linux System에서 격리된 Container 환경을 실행하며, 이를 위해 여러 Linux 기능을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>기술</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chroot</code></td>
      <td>특정 Directory를 Root처럼 인식시켜 File 영역을 격리</td>
    </tr>
    <tr>
      <td>cgroups</td>
      <td>CPU와 Memory 등의 Resource 사용량을 제한하고 격리</td>
    </tr>
    <tr>
      <td>Namespace</td>
      <td>Process Tree, Network, User ID, Mount 영역을 분리</td>
    </tr>
  </tbody>
</table>

<h4 id="chroot">chroot</h4>

<p><code class="language-plaintext highlighter-rouge">chroot</code>는 특정 Directory를 최상위 Directory인 Root로 인식하도록 설정하는 Linux 기능이다.
이를 통해 다음과 같은 영역을 격리할 수 있다.</p>

<ul>
  <li>User</li>
  <li>File</li>
  <li>Directory</li>
</ul>

<h4 id="cgroups">cgroups</h4>

<p>cgroups (Control Groups)는 <strong>Resource를 제한하고 격리하는 Linux Kernel 기능</strong>이다.</p>

<p>각 Container가 사용할 수 있는 CPU, Memory 등의 Resource를 제한하는 데 사용할 수 있다.</p>

<h4 id="namespace">Namespace</h4>

<p>Namespace는 <strong>Process를 독립시키기 위한 Linux Kernel의 격리 기술</strong>이다.</p>

<p>각 Process가 서로 분리된 운영 환경을 바라볼 수 있도록 하며 다음과 같은 영역을 격리할 수 있다.</p>

<ul>
  <li>Process Tree</li>
  <li>Network</li>
  <li>User ID</li>
  <li>Mount된 File System</li>
</ul>

<h3 id="프로그램과-데이터를-격리하는-이유">프로그램과 데이터를 격리하는 이유</h3>

<p>여러 프로그램이 동일한 실행 환경을 사용하면 서로 영향을 줄 수 있다.</p>

<p>예를 들어 여러 프로그램이 특정 Directory를 공유하거나 같은 경로의 설정 정보를 사용하는 경우, 하나의 프로그램을 Update했을 때 다른 프로그램에도 영향을 줄 수 있다.</p>

<p>이러한 문제는 다음과 같은 부분에서 발생할 수 있다.</p>

<ul>
  <li>실행 환경</li>
  <li>Library</li>
  <li>Directory</li>
  <li>설정 파일</li>
</ul>

<p>여러 프로그램이 같은 Directory, Library, 설정 파일을 공유하면 하나의 변경이 다른 프로그램에 영향을 줄 수 있다. Docker는 실행 환경을 Container 단위로 격리하여 이러한 충돌을 줄인다.</p>

<h2 id="4--image와-container">4 ) Image와 Container</h2>

<hr />

<p>Image는 Container를 만들기 위한 <strong>설계도</strong>, Container는 Image를 실행한 <strong>Instance</strong>에 해당한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image        → Class
Container    → Instance
</code></pre></div></div>

<p>하나의 Image로 동일한 환경의 Container를 여러 개 생성할 수 있고, 실행 중인 Container를 새로운 Image로 만드는 것도 가능하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              ┌─ Container 1
Docker Image ─┼─ Container 2
              └─ Container 3
</code></pre></div></div>
<h3 id="docker-hub">Docker Hub</h3>

<p>Docker Hub는 Docker가 제공하는 공식 Image Registry이다. 일반적으로 Docker Hub에서 Base Image를 내려받고 필요한 Layer를 추가하여 새로운 Image를 만든 뒤 배포한다.</p>

<h3 id="다양한-형태로-구성할-수-있는-container">다양한 형태로 구성할 수 있는 Container</h3>

<p>Container는 하나의 애플리케이션으로 구성할 수도 있고 여러 개의 애플리케이션을 묶어서 구성할 수도 있다.</p>
<blockquote>
  <p>WordPress를 Apache Web Server와 MySQL로 함께 구성한 사례</p>
</blockquote>

<h3 id="container와-데이터">Container와 데이터</h3>

<p>Container는 삭제 후 복구할 수 없는 일회성 실행 환경이다. Container 내부에만 저장한 데이터도 함께 사라질 수 있다.</p>

<p>따라서 Container의 Lifecycle과 저장해야 하는 데이터를 구분하여 다루어야 한다.</p>

<p>보존할 데이터는 Host의 Disk나 Docker Volume을 Mount하여 저장한다.</p>

<h2 id="5--docker의-장점과-구성-요소">5 ) Docker의 장점과 구성 요소</h2>

<hr />

<h3 id="장점">장점</h3>

<ul>
  <li>개발 환경과 운영 환경을 거의 동일하게 재현할 수 있다.</li>
  <li>물리적 환경과 Server 구성 차이의 영향을 줄일 수 있다.</li>
  <li>가상 머신보다 가볍고 Image의 복제·이관·배포가 쉽다.</li>
  <li>Cloud Platform과 Container Orchestration 환경에서 활용할 수 있다.</li>
</ul>

<p>Linux 운영체제 자체의 동작이나 비 Linux 환경이 필요한 경우에는 완전한 Guest OS를 제공하는 Virtual Machine이 더 적합할 수 있다.</p>

<p>Container는 완전한 Guest OS를 제공하는 Virtual Machine과는 구조적으로 차이가 있다.</p>

<h3 id="주요-도구">주요 도구</h3>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>containerd</td>
      <td>Image 전송·저장, Container 실행과 감독 등 Lifecycle 관리</td>
    </tr>
    <tr>
      <td>BuildKit</td>
      <td>Dockerfile을 기반으로 Docker Image Build</td>
    </tr>
    <tr>
      <td>Docker CLI</td>
      <td><code class="language-plaintext highlighter-rouge">docker</code> 명령을 통해 Docker Engine 조작</td>
    </tr>
    <tr>
      <td>Docker Compose</td>
      <td>YAML로 여러 Container를 하나의 Application처럼 관리</td>
    </tr>
    <tr>
      <td>Docker Registry</td>
      <td>Image 저장 및 Push/Pull 제공</td>
    </tr>
    <tr>
      <td>Docker Swarm</td>
      <td>여러 Docker Host를 Cluster로 관리</td>
    </tr>
  </tbody>
</table>

<h4 id="containerd">containerd</h4>

<p>containerd는 Container를 구동하고 관리하는 Runtime이다.</p>

<p>Linux 및 Windows용 Daemon으로 동작하며 다음과 같은 Container Lifecycle을 관리한다.</p>

<ul>
  <li>Image 전송 및 저장</li>
  <li>Container 실행</li>
  <li>Container 감독</li>
  <li>Network 연결</li>
  <li>Container Lifecycle 관리</li>
</ul>

<p>강의에서는 Kubernetes를 학습할 때 Docker와 containerd의 관계를 주의해야 한다고 설명하였다.</p>

<blockquote>
  <p>Kubernetes에서는 Docker를 직접 Container Runtime으로 사용하는 방식이 제거되었으며 containerd와 같은 Container Runtime을 사용한다.</p>
</blockquote>

<h4 id="buildkit">BuildKit</h4>

<p>BuildKit은 Dockerfile의 설정 정보를 이용하여 <strong>Docker Image를 Build하는 Open Source 도구</strong>이다.</p>

<p>Image Build 과정에서 다양한 Architecture와 Build 기능을 제공한다.</p>

<p>강의에서는 현재 Docker가 <strong>Docker Image를 만드는 도구로 많이 사용된다는 점</strong>을 강조하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile → BuildKit → Docker Image
</code></pre></div></div>

<h4 id="docker-cli">Docker CLI</h4>

<p>Docker CLI (Command Line Interface)는 Docker 명령을 실행하기 위한 명령행 도구이다.</p>

<p>Docker CLI를 이용하여 Docker Image와 Container 등을 조작할 수 있다.</p>

<h3 id="container와-orchestration">Container와 Orchestration</h3>

<p>Container 기술은 PaaS 서비스를 가능하게 하는 Software 개발 환경을 제공하는 데 활용할 수 있다.</p>

<p>하지만 Container의 수가 증가하면 각각의 Container를 직접 관리하기 어려워진다.</p>

<p>Container 수가 많아지면 자동 복구, Traffic Routing, Load Balancing 등을 직접 처리하기 어렵다. 이때 Kubernetes와 같은 Orchestration 도구를 사용한다.</p>

<p>다음과 같은 작업을 자동화하고 관리하기 위해 <strong>Orchestration 도구</strong>가 필요하다.</p>

<ul>
  <li>Container 자동 관리</li>
  <li>Traffic Routing</li>
  <li>Load Balancing</li>
</ul>

<p>강의에서는 Container 기반 환경의 구성을 다음과 같이 연결하여 설명하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container로 구동되는 Application
              ↓
      LXC / Docker 기술
              ↓
 Orchestration Tool
        Kubernetes
              ↓
             IaaS
</code></pre></div></div>

<p>즉, Docker와 같은 Container 기술을 이용하여 애플리케이션 실행 환경을 구성하고, 여러 Container를 운영해야 하는 환경에서는 Kubernetes와 같은 Orchestration 도구를 이용하여 이를 관리한다.</p>

<h3 id="docker의-구성-요소">Docker의 구성 요소</h3>

<p>Docker는 Client, Host, Engine을 중심으로 동작하며 Container 구성과 Image 관리, Cluster 관리 등을 위한 여러 도구로 구성된다.</p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker Client</td>
      <td>Docker에 명령을 전달할 수 있는 CLI</td>
    </tr>
    <tr>
      <td>Docker Host</td>
      <td>Docker가 설치되어 실행되는 Server 또는 Virtual Machine</td>
    </tr>
    <tr>
      <td>Docker Engine</td>
      <td>Docker를 이용한 Application 실행 환경을 제공하는 핵심 요소</td>
    </tr>
    <tr>
      <td>Docker Compose</td>
      <td>여러 Container의 구성 정보를 YAML로 작성하여 하나의 Application처럼 관리하는 도구</td>
    </tr>
    <tr>
      <td>Docker Registry</td>
      <td>Docker Image를 Push/Pull할 수 있도록 Image를 저장하고 배포하는 Registry</td>
    </tr>
    <tr>
      <td>Docker Swarm</td>
      <td>여러 Docker Host를 Cluster로 구성하여 관리하는 Docker Orchestration 도구</td>
    </tr>
    <tr>
      <td>Docker Hub</td>
      <td>Docker Image를 공유할 수 있는 Cloud 기반 Registry Service</td>
    </tr>
  </tbody>
</table>

<h4 id="docker-client">Docker Client</h4>

<p>Docker Client는 사용자가 Docker에 명령을 전달하기 위한 CLI (Command Line Interface)이다.</p>

<p>사용자가 <code class="language-plaintext highlighter-rouge">docker</code> 명령을 실행하면 Docker Client를 통해 Docker Engine에 명령이 전달된다.</p>

<h4 id="docker-host와-docker-engine">Docker Host와 Docker Engine</h4>

<p>Docker Host는 Docker가 설치되어 있는 Server 또는 Virtual Machine이다.</p>

<p>Docker Host 내부에서는 <strong>Docker Engine</strong>이 동작하며, Docker를 이용한 Application 실행 환경을 제공한다.</p>

<p>Docker Engine은 Docker의 핵심 요소로 Container와 Image를 실행하고 관리한다.</p>

<h4 id="docker-compose">Docker Compose</h4>

<p>Docker Compose는 서로 의존성을 가지는 여러 Container의 구성 정보를 <strong>YAML 파일</strong>로 작성하여 일원화된 Application 관리가 가능하도록 하는 도구이다.</p>

<p>여러 Container로 구성된 Application의 실행 환경을 하나의 설정 파일에서 관리할 수 있다.</p>

<h4 id="docker-registry와-docker-hub">Docker Registry와 Docker Hub</h4>

<p>Docker Registry는 Docker Image를 저장하고 <code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">pull</code>할 수 있도록 하는 Image 저장소이다.</p>

<p>Docker Hub는 Docker Image를 공유할 수 있도록 Docker에서 제공하는 Cloud Service이다.</p>

<h4 id="docker-swarm">Docker Swarm</h4>

<p>Docker Swarm은 여러 Docker Host를 하나의 Cluster로 구성하여 관리할 수 있도록 하는 <strong>Docker Orchestration 도구</strong>이다.</p>

<p>여러 Docker Host에서 실행되는 Container를 하나의 Cluster 단위로 관리할 때 사용할 수 있다.</p>

<h2 id="6--docker-설치">6 ) Docker 설치</h2>

<hr />

<p>Docker는 64-bit 운영체제에서 사용한다. Windows와 macOS에서는 Docker Desktop을 설치한다.</p>

<h3 id="windows">Windows</h3>

<p>WSL 2를 설치한 뒤 Docker Desktop을 설치한다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">wsl</span><span class="w"> </span><span class="nt">--install</span><span class="w">
</span><span class="n">wsl</span><span class="w"> </span><span class="nt">--set-default-version</span><span class="w"> </span><span class="nx">2</span><span class="w">
</span></code></pre></div></div>

<h3 id="ubuntu">Ubuntu</h3>

<p>Ubuntu에서는 Docker 공식 저장소를 등록한 뒤 Docker Engine과 관련 Plugin을 설치한다. 세부 명령은 버전에 따라 달라질 수 있으므로 <a href="https://docs.docker.com/engine/install/ubuntu/">Docker 공식 설치 문서</a>를 기준으로 진행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
</code></pre></div></div>

<p>설치 후 Version과 Service 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker version
<span class="nb">sudo </span>systemctl status docker
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker
</code></pre></div></div>

<p>현재 사용자가 <code class="language-plaintext highlighter-rouge">sudo</code> 없이 Docker를 실행하려면 <code class="language-plaintext highlighter-rouge">docker</code> Group에 추가한 뒤 다시 로그인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker <span class="s2">"</span><span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<blockquote>
  <p>Docker Socket의 권한을 <code class="language-plaintext highlighter-rouge">666</code>으로 변경하면 모든 사용자가 Docker Daemon을 제어할 수 있으므로 사용하지 않는다.</p>
</blockquote>

<h2 id="7--기본-사용">7 ) 기본 사용</h2>

<hr />

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Image 다운로드와 확인</span>
docker pull hello-world
docker images

<span class="c"># Container 생성 및 실행</span>
docker run hello-world

<span class="c"># 실행 중인 Container 확인</span>
docker ps

<span class="c"># 모든 Container 확인</span>
docker ps <span class="nt">-a</span>

<span class="c"># Docker Version과 System 정보 확인</span>
docker version
docker info
docker system <span class="nb">df</span>
</code></pre></div></div>

<p>대화형 Terminal이 필요한 Container는 <code class="language-plaintext highlighter-rouge">-it</code> Option으로 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-it</span> IMAGE_NAME
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker run</code>은 Image가 없으면 내려받고, Container를 생성한 뒤 시작한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[가상화와 Container의 원리부터 Docker의 구성 요소, 설치 및 기본 사용법까지 정리]]></summary></entry><entry><title type="html">Docker 명령어와 Registry</title><link href="https://kkr010128.github.io/cloud-native-09-docker-commands/" rel="alternate" type="text/html" title="Docker 명령어와 Registry" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-09-docker-commands</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-09-docker-commands/"><![CDATA[<h2 id="1--docker-registry">1 ) Docker Registry</h2>

<hr />

<p>Docker Registry는 Image를 저장하고 배포하는 장소이다. Docker Hub는 Docker가 운영하는 공식 Public Registry이며 Apache, MySQL, Ubuntu 등의 공식 Image를 제공한다.</p>

<p>Registry는 여러 <strong>Repository</strong>로 구성된다. 직접 Private Registry를 구축하거나 Cloud Service Provider가 제공하는 Registry를 사용할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Registry
  ├─ Repository A
  │    ├─ Image:1.0
  │    └─ Image:2.0
  └─ Repository B
</code></pre></div></div>

<h2 id="2--container-application-개발-흐름">2 ) Container Application 개발 흐름</h2>

<hr />

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application Code 작성
        ↓
Dockerfile 작성
        ↓
Image Build
        ↓
Container 생성 및 실행
        ↓
Application Test
        ↓
Registry에 Image Push
</code></pre></div></div>

<p>Dockerfile은 Git으로 관리하며 코드 변경 후 같은 과정을 반복한다. 여러 Container가 필요한 Application은 Docker Compose로 함께 실행하고 각각의 Container와 전체 Application을 테스트한다.</p>

<h2 id="3--docker-명령-구조">3 ) Docker 명령 구조</h2>

<hr />

<p>모든 Docker 명령은 <code class="language-plaintext highlighter-rouge">docker</code>로 시작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker [상위 명령] [하위 명령] [Option] [대상] [인자]
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image pull penguin
docker container run penguin
docker container run <span class="nt">-d</span> penguin
</code></pre></div></div>

<p>일부 명령은 상위 명령을 생략한 이전 형식도 지원하지만, 동작 대상을 명확히 나타내려면 <code class="language-plaintext highlighter-rouge">docker image ...</code>, <code class="language-plaintext highlighter-rouge">docker container ...</code> 형식을 사용하는 것이 좋다.</p>

<h3 id="image-명령">Image 명령</h3>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image pull IMAGE</code></td>
      <td>Registry에서 Image 다운로드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image ls</code></td>
      <td>Local Image 목록 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image build -t NAME .</code></td>
      <td>Dockerfile로 Image Build 및 Tag 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image rm IMAGE</code></td>
      <td>Image 삭제</td>
    </tr>
  </tbody>
</table>

<h3 id="container-명령">Container 명령</h3>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container create IMAGE</code></td>
      <td>Container 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container start CONTAINER</code></td>
      <td>정지된 Container 시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container run IMAGE</code></td>
      <td>Image Pull, Container 생성과 시작 수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container stop CONTAINER</code></td>
      <td>실행 중인 Container 정지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container rm CONTAINER</code></td>
      <td>정지된 Container 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container ls</code></td>
      <td>실행 중인 Container 목록 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container exec -it CONTAINER CMD</code></td>
      <td>실행 중인 Container에서 명령 수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container cp SRC DEST</code></td>
      <td>Host와 Container 사이에서 File 복사</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container commit CONTAINER IMAGE</code></td>
      <td>Container 상태를 Image로 저장</td>
    </tr>
  </tbody>
</table>

<p>자주 사용하는 <code class="language-plaintext highlighter-rouge">run</code> Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name</code></td>
      <td>Container 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>Background 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>표준 입력 유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-t</code></td>
      <td>가상 Terminal 할당</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e</code></td>
      <td>환경 변수 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p</code></td>
      <td>Host와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code></td>
      <td>Volume 또는 Directory Mount</td>
    </tr>
  </tbody>
</table>

<h2 id="4--volume과-network-명령">4 ) Volume과 Network 명령</h2>

<hr />

<h3 id="volume">Volume</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create VOLUME_NAME
docker volume inspect VOLUME_NAME
docker volume <span class="nb">ls
</span>docker volume <span class="nb">rm </span>VOLUME_NAME
docker volume prune
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">prune</code>은 사용하지 않는 Resource를 일괄 삭제하므로 삭제 대상을 확인한 후 실행한다.</p>

<h3 id="network">Network</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network create NETWORK_NAME
docker network inspect NETWORK_NAME
docker network <span class="nb">ls
</span>docker network connect NETWORK_NAME CONTAINER_NAME
docker network <span class="nb">rm </span>NETWORK_NAME
docker network prune
</code></pre></div></div>

<h2 id="5--그-밖의-명령">5 ) 그 밖의 명령</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker login</code> / <code class="language-plaintext highlighter-rouge">logout</code></td>
      <td>Registry 인증과 해제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker search</code></td>
      <td>Registry의 Image 검색</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker version</code></td>
      <td>Client와 Server Version 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker info</code></td>
      <td>Docker Engine 정보 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker system</code></td>
      <td>Docker System 정보와 Resource 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker swarm</code></td>
      <td>Docker Swarm 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker node</code></td>
      <td>Swarm Node 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker service</code></td>
      <td>Swarm Service 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker stack</code></td>
      <td>여러 Swarm Service로 구성한 Stack 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker secret</code></td>
      <td>Swarm의 비밀 정보 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker plugin</code></td>
      <td>Docker Plugin 관리</td>
    </tr>
  </tbody>
</table>

<p>명령의 자세한 사용법은 <code class="language-plaintext highlighter-rouge">--help</code>로 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--help</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Registry와 Container 개발 흐름, Image·Container·Volume·Network 관리 명령어]]></summary></entry><entry><title type="html">Docker 이미지와 Union File System</title><link href="https://kkr010128.github.io/cloud-native-10-docker-images/" rel="alternate" type="text/html" title="Docker 이미지와 Union File System" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-10-docker-images</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-10-docker-images/"><![CDATA[<h2 id="1--docker-image">1 ) Docker Image</h2>

<hr />

<p>Docker Image는 Container 형태로 Software를 배포하고 실행하는 데 필요한 코드, Runtime, Library와 설정을 담은 Package이다.</p>

<ul>
  <li>실행에 필요한 요소를 포함하므로 환경별 의존성 차이를 줄인다.</li>
  <li>특정 시점의 Container File System을 담은 Snapshot으로 볼 수 있다.</li>
  <li>하나의 Image로 동일한 환경을 가진 Container를 여러 개 생성할 수 있다.</li>
  <li>여러 개의 읽기 전용 Layer로 구성된다.</li>
</ul>

<h3 id="image-이름">Image 이름</h3>

<p>Image의 전체 참조 형식은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>REGISTRY/NAMESPACE/REPOSITORY:TAG
</code></pre></div></div>

<p>Docker Hub 공식 Image는 <code class="language-plaintext highlighter-rouge">docker.io/library</code>를 생략할 수 있고, Tag를 생략하면 기본적으로 <code class="language-plaintext highlighter-rouge">latest</code>가 사용된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker.io/library/debian:latest
library/debian:latest
debian:latest
debian
</code></pre></div></div>

<p>네 표현은 모두 같은 Image를 가리킨다. 다만 <code class="language-plaintext highlighter-rouge">latest</code>는 가장 최신 Version을 보장하는 의미가 아니라 이름이 <code class="language-plaintext highlighter-rouge">latest</code>인 Tag이므로, 재현 가능한 배포에는 명시적인 Version Tag나 Digest를 사용하는 것이 좋다.</p>

<h2 id="2--image-검색과-다운로드">2 ) Image 검색과 다운로드</h2>

<hr />

<h3 id="검색">검색</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker search <span class="o">[</span>OPTIONS] KEYWORD
docker search <span class="nt">--limit</span> 5 httpd
</code></pre></div></div>

<p>검색 결과에는 Image 이름, 설명, Star 수, 공식 Image 여부 등이 표시된다.</p>

<h3 id="다운로드">다운로드</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image pull NAME[:TAG]
docker image pull NAME@DIGEST
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull jenkins/jenkins:lts
docker pull debian:latest
docker pull gcr.io/google-samples/hello-app:1.0
</code></pre></div></div>

<p>주요 Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--all-tags</code>, <code class="language-plaintext highlighter-rouge">-a</code></td>
      <td>Repository의 모든 Tag 다운로드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--platform</code></td>
      <td>대상 Platform 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--quiet</code>, <code class="language-plaintext highlighter-rouge">-q</code></td>
      <td>상세 출력 생략</td>
    </tr>
  </tbody>
</table>

<p>Apple Silicon Mac에서는 필요에 따라 <code class="language-plaintext highlighter-rouge">--platform linux/arm64</code> 또는 실행할 Image가 요구하는 Platform을 지정한다.</p>

<p>Digest는 Image Content를 식별하는 Hash 값이다. 같은 Digest를 사용하면 동일한 Image Content를 가리킨다.</p>

<h2 id="3--image-조회">3 ) Image 조회</h2>

<hr />

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Local Image 목록</span>
docker images
docker image <span class="nb">ls</span>

<span class="c"># 상세 정보</span>
docker image inspect httpd

<span class="c"># 생성 시각만 출력</span>
docker image inspect <span class="nt">--format</span><span class="o">=</span><span class="s1">''</span> httpd

<span class="c"># Layer 생성 이력</span>
docker image <span class="nb">history </span>httpd
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker image history</code>를 사용하면 Image를 구성한 명령과 Layer 정보를 확인할 수 있다.</p>

<h2 id="4--union-file-system">4 ) Union File System</h2>

<hr />

<p>Union File System은 서로 다른 File System이나 Directory의 내용을 하나의 File System처럼 합쳐 보여주는 기술이다.</p>

<p>Docker Image는 변경할 수 없는 여러 읽기 전용 Layer로 구성된다. Image로 Container를 실행하면 그 위에 쓰기 가능한 Container Layer가 추가되고, 실행 중 발생한 변경 사항은 이 Layer에 기록된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container Layer     ← Read / Write
-----------------
Application Layer   ← Read Only
Library Layer       ← Read Only
Base Image Layer    ← Read Only
</code></pre></div></div>

<p>동일한 Image에서 만든 Container들은 읽기 전용 Image Layer를 공유하므로 Disk를 효율적으로 사용할 수 있다. Container Layer는 Container 삭제 시 함께 사라질 수 있으므로 영구 데이터는 Volume에 저장한다.</p>

<p>Linux Docker Engine의 데이터와 Log는 일반적으로 <code class="language-plaintext highlighter-rouge">/var/lib/docker</code> 아래에 저장되며 Storage Driver에 따라 실제 구조가 달라질 수 있다. <code class="language-plaintext highlighter-rouge">overlay2</code>를 사용하는 환경에서는 관련 Layer 데이터가 해당 영역에 관리된다.</p>

<h2 id="5--docker-hub-로그인과-image-배포">5 ) Docker Hub 로그인과 Image 배포</h2>

<hr />

<h3 id="로그인">로그인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker login
docker login <span class="nt">-u</span> USERNAME
docker <span class="nb">logout</span>
</code></pre></div></div>

<p>비밀번호를 Command에 직접 작성하지 않는다. Docker Hub가 요구하는 경우 Password 대신 Access Token을 사용하고, Token은 문서나 Git Repository에 저장하지 않는다.</p>

<h3 id="tag-지정">Tag 지정</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image tag SOURCE_IMAGE TARGET_IMAGE
docker image tag httpd:latest USERNAME/myapache:1.0
</code></pre></div></div>

<p>Tag는 같은 Image에 Registry에 업로드할 참조 이름과 Version을 부여한다.</p>

<h3 id="push">Push</h3>

<p>Docker Hub에서 Repository를 만든 뒤 계정 Namespace가 포함된 이름으로 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push USERNAME/myapache:1.0
</code></pre></div></div>

<p>새 Version은 다른 Tag를 붙여 같은 Repository에 추가할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image tag httpd:latest USERNAME/myapache:2.0
docker push USERNAME/myapache:2.0
</code></pre></div></div>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Image 검색과 다운로드, Layer 구조, Tag 및 Docker Hub 배포 방법]]></summary></entry><entry><title type="html">클라우드 보안</title><link href="https://kkr010128.github.io/cloud-03-security/" rel="alternate" type="text/html" title="클라우드 보안" /><published>2026-08-13T00:00:00+09:00</published><updated>2026-08-13T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-03-security</id><content type="html" xml:base="https://kkr010128.github.io/cloud-03-security/"><![CDATA[<h2 id="1--클라우드-보안">1 ) 클라우드 보안</h2>

<hr />

<p>클라우드 환경에서는 설정 오류, 자격 증명 노출, 과도한 권한 부여 등이 보안 사고로 이어질 수 있다.</p>

<p>대표적인 사례는 다음과 같다.</p>

<ul>
  <li>S3 Bucket을 Public으로 설정하여 고객 정보가 유출되는 경우</li>
  <li>개발자가 AWS Access Key를 GitHub에 업로드하는 경우</li>
  <li>사용자 또는 서비스에 과도한 권한을 부여하는 경우</li>
</ul>

<p>특히 Access Key를 소스 코드에 직접 포함하면 GitHub와 같은 외부 저장소에 코드가 공개될 때 자격 증명도 함께 노출될 수 있다.</p>

<p>최근에는 GitHub에서 AWS Access Key가 포함된 코드를 Push하지 못하도록 제한하며, AWS에서도 GitHub에 노출된 Key를 이용한 접근을 감지하면 계정을 잠그는 방식으로 대응한다.</p>

<h3 id="클라우드-보안이-중요한-이유">클라우드 보안이 중요한 이유</h3>

<p>클라우드 보안은 크게 비용, 신뢰, 법적인 측면에서 중요하다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>비용</td>
      <td>보안 사고로 인한 직접적인 금전적 피해가 발생할 수 있다.</td>
    </tr>
    <tr>
      <td>신뢰</td>
      <td>정보 유출 등의 사고는 서비스와 기업에 대한 신뢰 하락으로 이어질 수 있다.</td>
    </tr>
    <tr>
      <td>법</td>
      <td>개인정보 및 데이터와 관련된 법적 책임이 발생할 수 있다.</td>
    </tr>
  </tbody>
</table>

<h2 id="2--iam">2 ) IAM</h2>

<hr />

<p>IAM (Identity and Access Management)은 AWS에서 사용자와 서비스의 접근 권한을 관리하기 위한 서비스이다.</p>

<h3 id="주요-구성-요소">주요 구성 요소</h3>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>User</td>
      <td>AWS를 사용하는 개별 사용자</td>
    </tr>
    <tr>
      <td>Group</td>
      <td>여러 User를 묶어 관리</td>
    </tr>
    <tr>
      <td>Role</td>
      <td>권한의 묶음</td>
    </tr>
    <tr>
      <td>Policy</td>
      <td>허용하거나 거부할 권한을 정의하는 정책</td>
    </tr>
  </tbody>
</table>

<h3 id="root-사용자와-iam-사용자">Root 사용자와 IAM 사용자</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Root 사용자</th>
      <th>IAM 사용자</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>권한</td>
      <td>모든 권한 소유</td>
      <td>부여된 권한만 사용</td>
    </tr>
    <tr>
      <td>생성</td>
      <td>자동 생성</td>
      <td>Root 사용자가 생성</td>
    </tr>
    <tr>
      <td>삭제</td>
      <td>삭제할 수 없음</td>
      <td>삭제 가능</td>
    </tr>
    <tr>
      <td>일상 사용</td>
      <td>사용하지 않음</td>
      <td>일상 업무에 사용</td>
    </tr>
  </tbody>
</table>

<p>Root 계정에서는 다음과 같은 작업을 수행한다.</p>

<ul>
  <li>AWS 계정 이름, 이메일, 비밀번호 변경</li>
  <li>결제 정보 변경</li>
  <li>AWS Support Plan 변경</li>
  <li>계정 해지</li>
  <li>IAM 사용자의 권한 복원</li>
</ul>

<p>Root 계정은 모든 권한을 가지고 있으므로 반드시 MFA (Multi-Factor Authentication)를 설정한다.</p>

<h3 id="iam-role">IAM Role</h3>

<p>Role은 권한을 묶어서 필요한 대상에게 부여하는 방식이다.</p>

<p>대표적인 사용 사례는 다음과 같다.</p>

<ul>
  <li>AWS 서비스 간 접근</li>
  <li>다른 AWS 계정에서 접근</li>
  <li>임시 접근 권한 부여</li>
</ul>

<p>예를 들어 EC2가 S3에 접근해야 하는 경우 EC2에 필요한 Role을 부여할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>EC2
 │
 │ IAM Role
 ▼
S3
</code></pre></div></div>

<h3 id="iam-policy"><strong>IAM Policy</strong></h3>

<p>Policy는 무엇을 허용하고 무엇을 거부할지를 정의한 규칙이다.</p>

<p>Policy는 JSON으로 직접 작성하거나 GUI를 이용하여 작성할 수 있다.</p>

<p>주요 구성 요소는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>항목</strong></th>
      <th><strong>역할</strong></th>
      <th><strong>예</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Effect</td>
      <td>허용 또는 거부</td>
      <td><code class="language-plaintext highlighter-rouge">Allow</code>, <code class="language-plaintext highlighter-rouge">Deny</code></td>
    </tr>
    <tr>
      <td>Action</td>
      <td>수행할 작업</td>
      <td><code class="language-plaintext highlighter-rouge">s3:GetObject</code></td>
    </tr>
    <tr>
      <td>Resource</td>
      <td>작업 대상 Resource</td>
      <td><code class="language-plaintext highlighter-rouge">arn:aws:s3:::my-bucket/*</code></td>
    </tr>
  </tbody>
</table>

<p>예시는 다음과 같다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"s3:GetObject"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"s3:ListBucket"</span><span class="w">
      </span><span class="p">],</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"arn:aws:s3:::my-bucket"</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Policy에서 <code class="language-plaintext highlighter-rouge">Allow</code>와 <code class="language-plaintext highlighter-rouge">Deny</code>가 동시에 적용되는 경우 <code class="language-plaintext highlighter-rouge">Deny</code>가 적용된다.</p>

<h3 id="최소-권한-원칙"><strong>최소 권한 원칙</strong></h3>

<p>사용자와 서비스에는 작업에 필요한 최소한의 권한만 부여한다.</p>

<p>불필요하게 많은 권한을 부여하면 계정이나 Access Key가 탈취되었을 때 피해 범위도 커질 수 있다.</p>

<h3 id="iam-모범-사례"><strong>IAM 모범 사례</strong></h3>

<ul>
  <li>Root 계정에 MFA를 설정한다.</li>
  <li>일상적인 작업에는 IAM 계정을 생성하여 사용한다.</li>
  <li>Group을 이용하여 권한을 관리한다.</li>
  <li>최소 권한 원칙을 적용한다.</li>
  <li>강력한 비밀번호 정책을 설정한다.</li>
  <li>Access Key를 코드에 직접 포함하지 않는다.</li>
  <li>사용하지 않는 사용자와 Key를 정리한다.</li>
  <li>Access Key 대신 IAM Role을 활용한다.</li>
</ul>

<p>IAM 계정을 사용하면 누가 어떤 작업을 수행했는지 구분할 수 있으며, Group을 이용하면 여러 사용자의 권한을 일관되고 효율적으로 관리할 수 있다.</p>

<h3 id="비밀번호-정책"><strong>비밀번호 정책</strong></h3>

<p>권장하는 비밀번호 정책은 다음과 같다.</p>

<ul>
  <li>최소 길이 12자 이상</li>
  <li>대문자, 소문자, 숫자, 특수 문자 포함</li>
  <li>비밀번호 만료 시간 설정</li>
  <li>최근 12개의 비밀번호를 기억하여 재사용 방지</li>
  <li>사용자가 자신의 비밀번호를 변경할 수 있도록 허용</li>
</ul>

<p>짧은 비밀번호는 무차별 대입 공격에 취약할 수 있으며, 여러 종류의 문자를 사용하여 비밀번호 복잡도를 높일 수 있다.</p>

<h3 id="aws-접근-방법"><strong>AWS 접근 방법</strong></h3>

<p>AWS에는 콘솔을 이용하는 방법과 Access Key를 이용하는 방법이 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>Console Login</strong></th>
      <th><strong>Access Key</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>접근 방법</td>
      <td>Web Browser로 AWS Console 접속</td>
      <td>프로그램이나 명령어로 AWS API 호출</td>
    </tr>
    <tr>
      <td>사용 방법</td>
      <td>ID + Password + MFA</td>
      <td>Access Key ID + Secret Access Key</td>
    </tr>
    <tr>
      <td>사용 대상</td>
      <td>사람</td>
      <td>프로그램, CLI Tool, 자동화 Script</td>
    </tr>
    <tr>
      <td>보안 위험</td>
      <td>비밀번호 유출, Phishing</td>
      <td>Key를 코드에 포함하면 유출 위험</td>
    </tr>
  </tbody>
</table>

<p>프로그램에서 AWS에 접근할 때는 Access Key를 코드에 직접 저장하는 것보다 IAM Role을 사용하는 것을 권장한다.</p>

<p><strong>정리</strong></p>

<ul>
  <li>IAM은 AWS Resource에 대한 접근 권한을 관리한다.</li>
  <li>User, Group, Role, Policy가 주요 구성 요소이다.</li>
  <li>Root 계정은 일상적인 작업에 사용하지 않고 MFA를 설정한다.</li>
  <li>사용자와 서비스에는 필요한 최소한의 권한만 부여한다.</li>
  <li>Access Key를 소스 코드에 직접 포함하지 않는다.</li>
</ul>

<h2 id="3--네트워크-보안-아키텍처"><strong>3 ) 네트워크 보안 아키텍처</strong></h2>

<hr />

<p>AWS에서는 하나의 보안 장치에만 의존하지 않고 여러 계층에 보안 기능을 배치하는 방어 심층 전략 (Defense in Depth)을 적용할 수 있다.</p>

<h3 id="방어-심층-전략"><strong>방어 심층 전략</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>계층</strong></th>
      <th><strong>서비스</strong></th>
      <th><strong>보호 대상</strong></th>
      <th><strong>주요 기능</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Edge</td>
      <td>CloudFront, Shield, WAF</td>
      <td>외부 Internet 경계</td>
      <td>DDoS 방어, L7 Filtering</td>
    </tr>
    <tr>
      <td>VPC 경계</td>
      <td>Network Firewall, IGW</td>
      <td>VPC 진입점</td>
      <td>IDS/IPS, Stateful 검사</td>
    </tr>
    <tr>
      <td>Subnet</td>
      <td>NACL, Route Table</td>
      <td>Subnet 경계</td>
      <td>Stateless Filtering</td>
    </tr>
    <tr>
      <td>Instance</td>
      <td>Security Group</td>
      <td>Instance 경계</td>
      <td>Stateful Filtering</td>
    </tr>
    <tr>
      <td>Application</td>
      <td>IAM, 암호화, 인증</td>
      <td>Application 내부</td>
      <td>인증/인가, 데이터 암호화</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-네트워크-보안-서비스"><strong>AWS 네트워크 보안 서비스</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>서비스</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AWS Shield</td>
      <td>DDoS 방어</td>
    </tr>
    <tr>
      <td>AWS WAF</td>
      <td>Web Application 공격 Filtering</td>
    </tr>
    <tr>
      <td>AWS Firewall Manager</td>
      <td>여러 계정의 보안 정책 중앙 관리</td>
    </tr>
    <tr>
      <td>AWS Network Firewall</td>
      <td>VPC 수준 Firewall 및 IDS/IPS</td>
    </tr>
    <tr>
      <td>VPC Flow Logs</td>
      <td>Network Interface Traffic 기록</td>
    </tr>
    <tr>
      <td>Traffic Mirroring</td>
      <td>Traffic을 복제하여 보안 분석 도구로 전달</td>
    </tr>
    <tr>
      <td>Route 53 Resolver DNS Firewall</td>
      <td>악성 Domain DNS Query 차단 및 DNS Tunneling 방지</td>
    </tr>
  </tbody>
</table>

<p><strong>AWS Shield</strong></p>

<p>DDoS 방어를 목적으로 사용한다.</p>

<ul>
  <li>Standard: 무료이며 L3/L4 자동 보호</li>
  <li>Advanced: L7 방어</li>
</ul>

<p><strong>AWS WAF</strong></p>

<p>Web Application Firewall로 SQL Injection, XSS, Bot 등 L7 공격을 Filtering한다.</p>

<p>CloudFront와 ALB 등에 적용할 수 있다.</p>

<p><strong>AWS Firewall Manager</strong></p>

<p>여러 AWS 계정에서 사용하는 WAF, Shield, Security Group, NACL 규칙 등을 중앙에서 관리한다.</p>

<p><strong>AWS Network Firewall</strong></p>

<p>VPC 수준에서 Stateful/Stateless Packet 검사를 수행하며 IDS/IPS 기능을 제공한다.</p>

<p><strong>VPC Flow Logs</strong></p>

<p>VPC 내부 Network Interface의 Traffic 정보를 기록한다.</p>

<p><strong>Traffic Mirroring</strong></p>

<p>Traffic을 복제하여 보안 분석 도구로 전달한다.</p>

<p><strong>Route 53 Resolver DNS Firewall</strong></p>

<p>악성 Domain에 대한 DNS Query를 차단하고 DNS Tunneling을 방지하는 데 사용한다.</p>

<h2 id="4--aws-waf"><strong>4 ) AWS WAF</strong></h2>

<hr />

<p>AWS WAF는 Web ACL에 Rule을 정의하고 CloudFront 또는 ALB에 연결하여 HTTP/HTTPS Traffic을 Filtering한다.</p>

<h3 id="waf-동작-구조"><strong>WAF 동작 구조</strong></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Web Request
HTTP / HTTPS
     │
     ▼
CloudFront / ALB
     │
     │ WAF Web ACL
     ▼
Rule 평가
     │
     │ 우선순위 순서로 Rule Matching
     ▼
Allow / Block / Count
</code></pre></div></div>

<p>Rule에 Matching되면 해당 Action을 수행하고, Matching되지 않으면 기본 Action을 수행한다.</p>

<h3 id="waf-규칙-유형"><strong>WAF 규칙 유형</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>유형</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IP 기반</td>
      <td>특정 IP/CIDR을 허용하거나 차단</td>
    </tr>
    <tr>
      <td>Rate 기반</td>
      <td>일정 시간 동안의 요청 수 제한</td>
    </tr>
    <tr>
      <td>관리형 규칙</td>
      <td>AWS 또는 Marketplace에서 제공하는 규칙</td>
    </tr>
    <tr>
      <td>사용자 정의</td>
      <td>Header, Query String 등의 조건을 직접 정의</td>
    </tr>
    <tr>
      <td>정규식 패턴</td>
      <td>URL, User-Agent 등을 정규표현식으로 Matching</td>
    </tr>
  </tbody>
</table>

<p>관리형 규칙은 OWASP Top 10이나 Bot 제어 등에 활용할 수 있다.</p>

<h3 id="aws-관리형-규칙-그룹"><strong>AWS 관리형 규칙 그룹</strong></h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesCommonRuleSet</code>: XSS, 파일 포함, SSRF 등 공통 취약점</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesSQLiRuleSet</code>: SQL Injection 공격 패턴</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesKnownBadInputsRuleSet</code>: Log4j, Java Deserialization 등 알려진 악성 입력</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesAmazonIpReputationRuleSet</code>: AWS 위협 Intelligence 기반 악성 IP</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesBotControlRuleSet</code>: Bot Traffic 탐지 및 제어</li>
</ul>

<h3 id="waf-logging"><strong>WAF Logging</strong></h3>

<p>WAF Log를 분석하면 공격 패턴을 파악하고 Rule을 최적화할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>로그 대상</strong></th>
      <th><strong>활용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3</td>
      <td>대용량 저장 및 Athena를 이용한 장기 분석</td>
    </tr>
    <tr>
      <td>CloudWatch Logs</td>
      <td>실시간 조회 및 Metric Filter를 이용한 차단 현황 Monitoring</td>
    </tr>
    <tr>
      <td>Kinesis Data Firehose</td>
      <td>SIEM 연동 및 실시간 Dashboard 활용</td>
    </tr>
  </tbody>
</table>

<h2 id="5--vpc-보안-전략"><strong>5 ) VPC 보안 전략</strong></h2>

<hr />

<h3 id="vpc-보안-설계-원칙"><strong>VPC 보안 설계 원칙</strong></h3>

<p><strong>최소 노출</strong></p>

<p>Internet에 노출되는 Resource를 최소화한다. Public Subnet에는 ALB, NAT 등의 최소한의 Resource만 배치한다.</p>

<p><strong>네트워크 분리</strong></p>

<p>워크로드별 Network를 격리한다. 예를 들어 <code class="language-plaintext highlighter-rouge">dev</code>, <code class="language-plaintext highlighter-rouge">stg</code>, <code class="language-plaintext highlighter-rouge">prod</code> 환경별로 VPC를 분리할 수 있다.</p>

<p><strong>트래픽 제어</strong></p>

<p>SG와 NACL을 이용하여 허용된 Traffic만 통과시키는 White List 방식을 사용한다.</p>

<p><strong>가시성 확보</strong></p>

<p>VPC Flow Logs를 활성화하여 Traffic 흐름을 기록한다.</p>

<p><strong>암호화 전송</strong></p>

<p>Network 구간의 데이터를 보호하기 위해 TLS를 강제하고 VPN, PrivateLink 등을 활용한다.</p>

<h3 id="subnet-분리-전략"><strong>Subnet 분리 전략</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>Tier</strong></th>
      <th><strong>Subnet</strong></th>
      <th><strong>배치 Resource</strong></th>
      <th><strong>Internet 접근</strong></th>
      <th><strong>보안</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public Tier</td>
      <td>Public Subnet</td>
      <td>ALB, NAT Gateway, Bastion</td>
      <td>Inbound/Outbound 허용</td>
      <td>최소 Resource</td>
    </tr>
    <tr>
      <td>App Tier</td>
      <td>Private Subnet</td>
      <td>EC2, ECS, Lambda</td>
      <td>NAT를 통한 Outbound만</td>
      <td>ALB에서만 접근</td>
    </tr>
    <tr>
      <td>Data Tier</td>
      <td>격리된 Private Subnet</td>
      <td>RDS, ElastiCache</td>
      <td>Internet 접근 불가</td>
      <td>App Tier에서만 접근</td>
    </tr>
  </tbody>
</table>

<h3 id="vpc-간-연결-보안"><strong>VPC 간 연결 보안</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>연결 방식</strong></th>
      <th><strong>용도</strong></th>
      <th><strong>보안 고려사항</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>VPC Peering</td>
      <td>두 VPC 간 Private 연결</td>
      <td>CIDR 중복 불가, 전이적 Routing 불가</td>
    </tr>
    <tr>
      <td>Transit Gateway</td>
      <td>다수 VPC/On-Premises Hub 연결</td>
      <td>Routing Table을 통한 Traffic 제어, Blackhole Routing</td>
    </tr>
    <tr>
      <td>PrivateLink</td>
      <td>서비스 간 Private 접근</td>
      <td>Internet 노출 없이 서비스 제공</td>
    </tr>
    <tr>
      <td>Site-to-Site VPN</td>
      <td>On-Premises와 VPC의 암호화 연결</td>
      <td>IPSec Tunnel, BGP Routing, 이중화</td>
    </tr>
    <tr>
      <td>Direct Connect</td>
      <td>전용선 연결</td>
      <td>물리적 분리, MACsec 암호화</td>
    </tr>
  </tbody>
</table>

<h3 id="vpc-endpoint"><strong>VPC Endpoint</strong></h3>

<p>VPC Endpoint를 이용하면 Internet Gateway 없이 AWS 서비스에 Private하게 접근할 수 있어 공격 표면을 줄일 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>유형</strong></th>
      <th><strong>지원 서비스</strong></th>
      <th><strong>보안 제어</strong></th>
      <th><strong>비용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Gateway Endpoint</td>
      <td>S3, DynamoDB</td>
      <td>Endpoint Policy로 접근 제한</td>
      <td>무료</td>
    </tr>
    <tr>
      <td>Interface Endpoint</td>
      <td>대부분의 AWS 서비스</td>
      <td>Security Group 적용</td>
      <td>시간당 + GB당 과금</td>
    </tr>
  </tbody>
</table>

<p>Interface Endpoint는 PrivateLink 기반 ENI를 사용한다.</p>

<p>보안에 필요한 주요 VPC Endpoint는 다음과 같다.</p>

<ul>
  <li><strong>S3</strong>: Private Subnet에서 Internet을 경유하지 않고 접근하며 NAT Gateway 비용을 줄일 수 있다.</li>
  <li><strong>STS</strong>: 임시 자격 증명 발급을 Private하게 수행한다.</li>
  <li><strong>Secrets Manager</strong>: 비밀값을 Internet을 통하지 않고 조회한다.</li>
</ul>

<h2 id="6--security-group과-nacl"><strong>6 ) Security Group과 NACL</strong></h2>

<hr />

<p>Security Group과 NACL (Network Access Control List)은 AWS Network Traffic을 제어하지만 적용 범위와 동작 방식이 다르다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>Security Group</strong></th>
      <th><strong>Network ACL</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>적용 대상</td>
      <td>Instance 수준</td>
      <td>Subnet 수준</td>
    </tr>
    <tr>
      <td>상태</td>
      <td>Stateful</td>
      <td>Stateless</td>
    </tr>
    <tr>
      <td>응답 Traffic</td>
      <td>자동 허용</td>
      <td>별도 Rule 필요</td>
    </tr>
    <tr>
      <td>기본 동작</td>
      <td>Inbound 거부 / Outbound 허용</td>
      <td>기본 NACL 모두 허용 / Custom NACL 모두 거부</td>
    </tr>
    <tr>
      <td>Rule 유형</td>
      <td>Allow만 가능</td>
      <td>Allow / Deny 가능</td>
    </tr>
    <tr>
      <td>Rule 평가</td>
      <td>모든 Rule 종합 평가</td>
      <td>번호 순서대로 평가 후 첫 Matching에서 중단</td>
    </tr>
    <tr>
      <td>SG 참조</td>
      <td>다른 SG를 Source로 참조 가능</td>
      <td>IP/CIDR 사용</td>
    </tr>
  </tbody>
</table>

<h3 id="트래픽-평가-순서"><strong>트래픽 평가 순서</strong></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Internet
   ↓
NACL Inbound
   ↓
Security Group Inbound
   ↓
Instance
   ↓
Security Group 응답 자동 허용
   ↓
NACL Outbound
</code></pre></div></div>

<p>Security Group은 Stateful이므로 허용된 요청에 대한 응답 Traffic은 자동으로 허용된다.</p>

<p>NACL은 Stateless이므로 Inbound와 Outbound Rule을 각각 설정해야 한다.</p>

<h3 id="서비스별-security-group-설정"><strong>서비스별 Security Group 설정</strong></h3>

<p><strong>RDS</strong></p>

<ul>
  <li>App SG에서만 DB Port 허용</li>
  <li>Public 접근 비활성화</li>
  <li>DB 전용 Subnet 사용</li>
</ul>

<p><strong>ElastiCache</strong></p>

<ul>
  <li>App SG에서만 Redis/Memcached Port 허용</li>
  <li>VPC 외부 접근 불가</li>
  <li>전송 중 TLS 활성화</li>
</ul>

<p><strong>Lambda</strong></p>

<ul>
  <li>Outbound 중심으로 관리</li>
  <li>Lambda SG의 Outbound를 필요한 서비스에만 허용</li>
</ul>

<p><strong>정리</strong></p>

<ul>
  <li>AWS Network 보안은 Defense in Depth 방식으로 여러 계층에서 구성한다.</li>
  <li>WAF는 Web Application Traffic을 Filtering한다.</li>
  <li>VPC는 Public, App, Data Tier 등으로 분리하여 Resource 노출을 최소화할 수 있다.</li>
  <li>VPC Endpoint를 이용하면 AWS 서비스에 Internet을 통하지 않고 접근할 수 있다.</li>
  <li>Security Group은 Stateful이며 NACL은 Stateless이다.</li>
</ul>

<h2 id="7--데이터-보안-전략"><strong>7 ) 데이터 보안 전략</strong></h2>

<hr />

<p>데이터 보안의 기본 원칙은 기밀성, 무결성, 가용성이다.</p>

<ul>
  <li><strong>기밀성 (Confidentiality)</strong>: 허가된 대상만 데이터에 접근할 수 있도록 한다.</li>
  <li><strong>무결성 (Integrity)</strong>: 데이터가 허가되지 않은 방식으로 변경되지 않도록 한다.</li>
  <li><strong>가용성 (Availability)</strong>: 필요한 시점에 데이터와 서비스를 사용할 수 있도록 한다.</li>
</ul>

<h3 id="암호화-유형"><strong>암호화 유형</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>저장 데이터 암호화</strong></th>
      <th><strong>전송 데이터 암호화</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>보호 대상</td>
      <td>Disk, S3, DB 등에 저장된 데이터</td>
      <td>Network를 통해 이동하는 데이터</td>
    </tr>
    <tr>
      <td>방법</td>
      <td>AES-256 대칭키 암호화</td>
      <td>TLS 1.2/1.3</td>
    </tr>
    <tr>
      <td>AWS</td>
      <td>KMS, CloudHSM</td>
      <td>ACM</td>
    </tr>
    <tr>
      <td>적용 대상</td>
      <td>EBS, S3, RDS, DynamoDB, EFS 등</td>
      <td>HTTPS, VPN, PrivateLink</td>
    </tr>
  </tbody>
</table>

<h3 id="서비스별-암호화"><strong>서비스별 암호화</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>서비스</strong></th>
      <th><strong>저장 암호화</strong></th>
      <th><strong>전송 암호화</strong></th>
      <th><strong>특징</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3</td>
      <td>SSE-S3, SSE-KMS, SSE-C</td>
      <td>HTTPS 강제</td>
      <td>기본 암호화 설정 가능</td>
    </tr>
    <tr>
      <td>EBS</td>
      <td>KMS 암호화</td>
      <td>Instance-EBS 간 자동 암호화</td>
      <td>계정 수준 암호화</td>
    </tr>
    <tr>
      <td>RDS</td>
      <td>KMS 암호화</td>
      <td>SSL/TLS 인증서</td>
      <td>생성 후 암호화 변경 불가</td>
    </tr>
    <tr>
      <td>DynamoDB</td>
      <td>기본 암호화</td>
      <td>HTTPS Endpoint</td>
      <td>고객 관리 Key 선택 가능</td>
    </tr>
    <tr>
      <td>EFS</td>
      <td>KMS 암호화</td>
      <td>TLS Mount Helper</td>
      <td>생성 시 암호화 설정</td>
    </tr>
  </tbody>
</table>

<h2 id="8--보안-감사"><strong>8 ) 보안 감사</strong></h2>

<hr />

<p>AWS에서는 Resource의 설정을 지속적으로 확인하고 보안 상태와 규정 준수 여부를 점검하기 위한 여러 서비스를 제공한다.</p>

<h3 id="aws-config"><strong>AWS Config</strong></h3>

<p>AWS Config는 Resource의 구성 변경을 지속적으로 기록하고 Rule에 따라 규정 준수 여부를 자동으로 평가하는 서비스이다.</p>

<h4 id="config-rule-유형"><strong>Config Rule 유형</strong></h4>

<ul>
  <li><strong>AWS 관리형 Rule</strong>: AWS가 사전에 정의한 Rule</li>
  <li><strong>사용자 정의 Rule</strong>: Lambda 함수로 작성한 Custom Rule</li>
</ul>

<p>보안에 활용할 수 있는 Rule로 <code class="language-plaintext highlighter-rouge">restricted-ssh</code>가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Security Group
      │
      │ SSH 0.0.0.0/0
      ▼
AWS Config
      │
      ▼
Rule 위반 탐지
</code></pre></div></div>

<h4 id="auto-remediation"><strong>Auto Remediation</strong></h4>

<p>Config Rule을 위반하면 SSM Automation 문서를 연결하여 자동으로 교정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>위반 사항</strong></th>
      <th><strong>대응</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public S3 Bucket</td>
      <td>Block Public Access 자동 활성화</td>
    </tr>
    <tr>
      <td>암호화되지 않은 EBS</td>
      <td>알림 전송 후 새로운 Volume 생성 필요</td>
    </tr>
    <tr>
      <td>SSH <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 허용 SG</td>
      <td>해당 Rule 자동 제거</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-security-hub"><strong>AWS Security Hub</strong></h3>

<p>AWS Security Hub는 여러 보안 서비스의 결과를 중앙에서 통합 관리하는 서비스이다.</p>

<p>주요 보안 표준은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>보안 표준</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AWS Foundational Security Best Practices</td>
      <td>AWS 자체 보안 모범 사례, 약 200개</td>
    </tr>
    <tr>
      <td>CIS AWS Foundations Benchmark</td>
      <td>CIS의 AWS 보안 Benchmark, 약 50개</td>
    </tr>
    <tr>
      <td>PCI DSS</td>
      <td>결제 카드 산업 보안 표준, 약 30개</td>
    </tr>
    <tr>
      <td>NIST SP 800-53</td>
      <td>미국 연방 보안 Framework, 약 160개</td>
    </tr>
  </tbody>
</table>

<h4 id="cis-benchmark"><strong>CIS Benchmark</strong></h4>

<p>CIS (Center for Internet Security)는 System과 Cloud 환경을 안전하게 구성하기 위한 보안 Benchmark를 제공한다.</p>

<p>Ubuntu Server에서는 다음과 같은 항목을 점검할 수 있다.</p>

<ul>
  <li>불필요한 서비스 비활성화</li>
  <li>SSH 보안 설정</li>
  <li>비밀번호 정책 강화</li>
  <li>파일 및 Directory 권한 설정</li>
  <li>Log 및 감사 설정</li>
  <li>Firewall 설정</li>
  <li>계정 및 권한 관리</li>
  <li>시간 동기화</li>
  <li>Kernel 및 Package 보안 설정</li>
</ul>

<p>SSH를 예로 들면 단순히 SSH 설치 여부만 확인하는 것이 아니라 다음과 같은 보안 설정을 점검할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Root 직접 로그인 차단
        ↓
비밀번호 인증 제한
        ↓
적절한 암호화 알고리즘 사용
        ↓
접근 가능한 사용자 제한
</code></pre></div></div>

<p>Kubernetes에도 별도의 CIS Kubernetes Benchmark가 존재한다.</p>

<p>Security Hub를 활성화하면 Security Score가 산출되며 이를 기반으로 개선 우선순위를 설정하고 Critical/High 항목부터 대응할 수 있다.</p>

<h3 id="aws-audit-manager"><strong>AWS Audit Manager</strong></h3>

<p>AWS Audit Manager는 AWS 사용에 대한 감사 증거를 자동으로 수집하고 규정 준수 보고서를 생성하는 서비스이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>기능</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>사전 정의 Framework</td>
      <td>SOC 2, PCI DSS, GDPR, HIPAA 등 제공</td>
    </tr>
    <tr>
      <td>자동 증거 수집</td>
      <td>Config, CloudTrail, Security Hub 등에서 증거 수집</td>
    </tr>
    <tr>
      <td>Custom Framework</td>
      <td>조직의 내부 정책에 맞는 감사 Framework 생성</td>
    </tr>
    <tr>
      <td>감사 보고서</td>
      <td>감사인에게 제출 가능한 보고서 생성</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-trusted-advisor"><strong>AWS Trusted Advisor</strong></h3>

<p>AWS Trusted Advisor는 AWS 환경의 보안, 비용 최적화, 성능, 서비스 한도 등을 점검하는 서비스이다.</p>

<p>보안과 관련된 주요 점검 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>점검 항목</strong></th>
      <th><strong>내용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3 Bucket Public 접근</td>
      <td>Public 읽기/쓰기가 허용된 S3 Bucket 탐지</td>
    </tr>
    <tr>
      <td>Root 계정 MFA</td>
      <td>Root 계정 MFA 활성화 여부 확인</td>
    </tr>
    <tr>
      <td>Security Group 무제한 접근</td>
      <td><code class="language-plaintext highlighter-rouge">0.0.0.0/0</code>으로 개방된 Port 탐지</td>
    </tr>
    <tr>
      <td>IAM Access Key Rotation</td>
      <td>90일 이상 Rotation되지 않은 Access Key 탐지</td>
    </tr>
    <tr>
      <td>EBS Public Snapshot</td>
      <td>Public으로 공유된 EBS Snapshot 탐지</td>
    </tr>
    <tr>
      <td>CloudTrail Logging</td>
      <td>CloudTrail 활성화 여부 확인</td>
    </tr>
  </tbody>
</table>

<p>CloudTrail은 AWS에서 누가 무엇을 수행했는지를 기록하는 감사 및 보안 Logging 서비스이다.</p>

<h3 id="amazon-inspector"><strong>Amazon Inspector</strong></h3>

<p>Amazon Inspector는 EC2 Instance, Container Image, Lambda 함수의 Software 취약점과 Network 노출을 자동으로 Scan하는 서비스이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>Scan 대상</strong></th>
      <th><strong>검출 내용</strong></th>
      <th><strong>특징</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EC2 Instance</td>
      <td>OS Package 취약점, Network 도달 가능성</td>
      <td>SSM Agent 기반 Agentless Scan</td>
    </tr>
    <tr>
      <td>ECR Container Image</td>
      <td>Container Image 내부 Package 취약점</td>
      <td>Image Push 시 자동 Scan</td>
    </tr>
    <tr>
      <td>Lambda 함수</td>
      <td>함수 Code 및 Dependency 취약점</td>
      <td>배포 시 자동 Scan</td>
    </tr>
  </tbody>
</table>

<p>ECR은 Docker Image 등을 저장하는 Container Image Repository로 사용할 수 있다.</p>

<h3 id="규정-준수의-코드화"><strong>규정 준수의 코드화</strong></h3>

<p>규정 준수의 코드화는 규정 준수 요구 사항을 코드로 정의하고 자동으로 검증하는 접근 방식이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구성 요소</strong></th>
      <th><strong>AWS 서비스</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정책 정의</td>
      <td>AWS Config Rules, SCP, CloudFormation Guard</td>
      <td>보안 정책을 코드로 정의</td>
    </tr>
    <tr>
      <td>자동 평가</td>
      <td>AWS Config, Security Hub</td>
      <td>Resource의 정책 준수 여부 지속 평가</td>
    </tr>
    <tr>
      <td>자동 교정</td>
      <td>SSM Automation, Lambda</td>
      <td>위반 사항 자동 수정</td>
    </tr>
    <tr>
      <td>증거 수집</td>
      <td>Audit Manager, CloudTrail</td>
      <td>규정 준수 증거 자동 수집</td>
    </tr>
    <tr>
      <td>보고</td>
      <td>Security Hub, Audit Manager</td>
      <td>규정 준수 현황 보고</td>
    </tr>
  </tbody>
</table>

<p>전체 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>정책 코드 작성
      ↓
CI/CD 배포
      ↓
지속적 평가
      ↓
자동 교정 / 보고
</code></pre></div></div>

<hr />

<blockquote>
  <p><strong>전체 정리</strong></p>

  <ul>
    <li>클라우드 보안에서는 Public Resource 설정, Access Key 노출, 과도한 권한과 같은 보안 위험을 줄이는 것이 중요하다.</li>
    <li>IAM을 통해 User, Group, Role, Policy를 관리하며, Root 계정의 사용을 최소화하고 MFA와 최소 권한 원칙을 적용한다.</li>
    <li>AWS Network는 WAF, Network Firewall, NACL, Security Group 등을 이용하여 여러 계층을 보호하는 Defense in Depth 전략을 적용할 수 있다.</li>
    <li>VPC에서는 Public, App, Data 영역을 분리하고 필요한 Traffic만 허용하여 Resource의 외부 노출을 최소화한다.</li>
    <li>저장 데이터와 전송 데이터에는 각각 적절한 암호화를 적용하여 데이터의 기밀성과 무결성을 보호한다.</li>
    <li>AWS Config, Security Hub, Audit Manager, Trusted Advisor, Amazon Inspector 등을 활용하여 보안 상태와 취약점, 규정 준수 여부를 지속적으로 점검할 수 있다.</li>
    <li>규정 준수 요구 사항을 코드로 정의하면 정책 정의 → 지속적 평가 → 자동 교정 → 증거 수집 → 보고 과정으로 관리할 수 있다.</li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="Cloud" /><category term="AutoEverSW" /><summary type="html"><![CDATA[IAM과 네트워크 보안, 데이터 암호화, 보안 감사 및 규정 준수 중심의 AWS 클라우드 보안 전략]]></summary></entry><entry><title type="html">Apache Kafka</title><link href="https://kkr010128.github.io/cloud-native-04-kafka/" rel="alternate" type="text/html" title="Apache Kafka" /><published>2026-08-12T00:00:00+09:00</published><updated>2026-08-12T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-04-kafka</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-04-kafka/"><![CDATA[<h2 id="1--apache-kafka">1 ) Apache Kafka</h2>

<hr />

<h3 id="kafka의-등장-배경">Kafka의 등장 배경</h3>

<p>기존에는 데이터를 생성하는 <strong>소스 애플리케이션(Source Application)</strong> 과 데이터를 사용하는 <strong>타깃 애플리케이션(Target Application)</strong> 을 직접 연결하여 데이터를 전달하는 방식이 사용되었다.</p>

<p>예를 들어 하나의 서비스에서 발생한 데이터를 여러 시스템에서 사용해야 한다면 각각의 애플리케이션 사이에 데이터 파이프라인을 구성해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source A ────────→ Target A
    │
    └────────────→ Target B

Source B ────────→ Target A
    │
    └────────────→ Target C
</code></pre></div></div>

<p>서비스가 적을 때는 큰 문제가 없지만 애플리케이션이 증가하면 연결해야 하는 파이프라인도 함께 증가한다.</p>

<p>소스 애플리케이션과 타깃 애플리케이션을 연결하는 파이프라인의 개수가 많아지면서 소스 코드 및 버전 관리에서 이슈가 발생할 수 있다.</p>

<p>또한 타깃 애플리케이션에 장애가 발생하면 그 영향이 소스 애플리케이션에 그대로 전달될 수 있다.</p>

<p>즉, 각각의 애플리케이션이 서로 강하게 연결되어 있기 때문에 데이터 흐름이 복잡해질수록 전체 시스템을 관리하기 어려워진다.</p>

<p>Apache Kafka는 이러한 데이터 흐름을 개선하기 위해 <strong>LinkedIn</strong>에서 개발되었다.</p>

<p>각 애플리케이션을 직접 연결하는 대신 Kafka를 중간에 두고 데이터를 한곳으로 모아 전달하는 구조를 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Application
        │
        ▼
      Kafka
        │
        ▼
Target Application
</code></pre></div></div>

<p>여러 애플리케이션이 존재하더라도 Kafka를 중심으로 데이터 흐름을 구성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source A ─┐
Source B ─┼──→ Kafka ──┬──→ Target A
Source C ─┘            ├──→ Target B
                       └──→ Target C
</code></pre></div></div>

<p>이를 통해 애플리케이션마다 개별적인 데이터 파이프라인을 구성하는 복잡성을 줄일 수 있다.</p>

<p>Kafka는 오픈소스로 제공되며, 대규모 데이터를 처리하는 <strong>Big Data</strong> 환경이나 <strong>Data Pipeline</strong>을 구축하는 환경에서 주요 구성 요소로 활용된다.</p>

<h3 id="kafka의-데이터-처리-방식"><strong>Kafka의 데이터 처리 방식</strong></h3>

<p>Kafka에서 데이터를 전송하는 쪽을 <strong>Producer</strong>, 데이터를 가져가 처리하는 쪽을 <strong>Consumer</strong>라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer
   │
   ▼
 Kafka
   │
   ▼
Consumer
</code></pre></div></div>

<p>Producer가 Kafka로 데이터를 전송하면 Kafka는 데이터를 저장하고, Consumer는 필요한 데이터를 Kafka에서 가져가 처리한다.</p>

<p>Kafka에서 실제로 전달되고 저장되는 데이터 단위를 <strong>Record</strong>라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer
   │
   │ Record
   ▼
 Kafka
   │
   │ Record
   ▼
Consumer
</code></pre></div></div>

<p>Kafka에 전달된 Record는 단순히 메모리에 존재하는 것이 아니라 Broker의 <strong>파일 시스템에 기록하여 보관할 수 있다.</strong></p>

<p>따라서 Consumer가 데이터를 읽는 순간 데이터가 바로 사라지는 일반적인 Queue와는 차이가 있다.</p>

<h3 id="kafka를-사용하는-이유"><strong>Kafka를 사용하는 이유</strong></h3>

<p>Kafka를 사용하는 주요 이유는 <strong>높은 처리량, 확장성, 영속성, 고가용성</strong>이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>특징</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>높은 처리량</td>
      <td>대량의 데이터를 지속적으로 처리할 수 있다.</td>
    </tr>
    <tr>
      <td>확장성</td>
      <td>Broker와 Partition 등을 확장하여 처리 능력을 증가시킬 수 있다.</td>
    </tr>
    <tr>
      <td>영속성</td>
      <td>전달받은 데이터를 파일 시스템에 저장할 수 있다.</td>
    </tr>
    <tr>
      <td>고가용성</td>
      <td>여러 Broker에 데이터를 분산하여 장애에 대응할 수 있다.</td>
    </tr>
  </tbody>
</table>

<h4 id="높은-처리량"><strong>높은 처리량</strong></h4>

<p>Kafka는 많은 양의 데이터를 지속적으로 전달하고 처리하는 환경을 고려하여 설계되었다.</p>

<p>따라서 Big Data 처리나 여러 시스템 사이에 대량의 데이터를 전달하는 Data Pipeline에서 사용할 수 있다.</p>

<h4 id="확장성"><strong>확장성</strong></h4>

<p>데이터 처리량이 증가하면 시스템의 규모도 함께 확장해야 한다.</p>

<p>Kafka는 여러 Broker로 Cluster를 구성할 수 있기 때문에 필요에 따라 서버를 추가하는 <strong>Scale-Out</strong> 방식으로 확장할 수 있다.</p>

<p>강의에서는 Kafka의 Scale-Out과 Scale-In 과정이 비교적 용이하다는 점을 특징으로 다루었다.</p>

<h4 id="영속성"><strong>영속성</strong></h4>

<p>Kafka는 Producer로부터 전달받은 데이터를 파일 시스템에 기록할 수 있다.</p>

<p>따라서 데이터를 단순히 중간에서 전달하는 역할만 수행하는 것이 아니라 일정 기간 데이터를 저장하고 Consumer가 이후에 읽을 수 있도록 구성할 수 있다.</p>

<h4 id="고가용성"><strong>고가용성</strong></h4>

<p>Kafka는 하나의 서버만 사용하는 것이 아니라 여러 Broker를 이용하여 분산 환경을 구성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kafka Cluster

├── Broker 1
├── Broker 2
└── Broker 3
</code></pre></div></div>

<p>강의에서는 상용 환경의 경우 Kafka를 여러 대의 Broker로 분산 운영하며, 일반적으로 최소 3대 이상의 서버로 구성하는 형태를 예로 들었다.</p>

<p>Producer가 전달한 데이터는 Broker의 파일 시스템에 기록되고, 여러 Broker를 이용한 분산 구성을 통해 특정 Broker에 장애가 발생하는 상황에 대응할 수 있다.</p>

<p><strong>중간 정리</strong></p>

<ul>
  <li>기존 방식은 소스와 타깃 애플리케이션을 직접 연결하기 때문에 시스템이 커질수록 데이터 파이프라인이 복잡해질 수 있다.</li>
  <li>Kafka를 중간에 두면 애플리케이션 간 데이터 흐름을 Kafka 중심으로 구성할 수 있다.</li>
  <li>Producer는 데이터를 Kafka에 전송하고 Consumer는 Kafka의 데이터를 읽어 처리한다.</li>
  <li>Kafka는 높은 처리량, 확장성, 영속성, 고가용성을 제공하기 위해 사용된다.</li>
</ul>

<h2 id="2--data-lake-architecture">2 ) <strong>Data Lake Architecture</strong></h2>

<hr />

<p>Kafka를 이해하기 위해서는 대규모 데이터를 처리하는 아키텍처가 어떻게 변화했는지도 함께 볼 필요가 있다.</p>

<p>강의에서는 다음과 같은 데이터 처리 아키텍처를 다루었다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>전통적인 방식
      ↓
Lambda Architecture
      ↓
Kappa Architecture
      ↓
Streaming Data Lake Architecture
</code></pre></div></div>

<h3 id="전통적인-데이터-처리-방식"><strong>전통적인 데이터 처리 방식</strong></h3>

<p>전통적인 방식에서는 각 서비스 애플리케이션을 <strong>End-to-End</strong> 형태로 연결하여 데이터를 수집하고 처리하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A ─────────→ Storage A

Service B ─────────→ Storage B

Service C ─────────→ Storage C
</code></pre></div></div>

<p>각 서비스가 필요한 데이터를 직접 수집하고 처리하기 때문에 시스템이 커질수록 애플리케이션 사이의 데이터 연결 관계도 복잡해질 수 있다.</p>

<h3 id="lambda-architecture"><strong>Lambda Architecture</strong></h3>

<p>대규모 데이터를 처리하기 위한 방식으로 <strong>Lambda Architecture</strong>가 사용된다.</p>

<p>강의에서는 데이터 처리 아키텍처의 흐름에서 Lambda Architecture를 다루었으며, 이후 실시간 데이터 처리와 관련된 구조로 Kappa Architecture가 등장하는 흐름을 설명하였다.</p>

<h3 id="kappa-architecture"><strong>Kappa Architecture</strong></h3>

<p><strong>Kappa Architecture</strong>는 데이터 처리 흐름을 스트리밍 중심으로 구성하는 아키텍처이다.</p>

<p>강의에서는 Lambda Architecture 이후 실시간 데이터 처리와 속도 측면을 고려하면서 등장한 구조로 설명하였다.</p>

<h3 id="streaming-data-lake-architecture"><strong>Streaming Data Lake Architecture</strong></h3>

<p>실시간으로 지속해서 발생하는 데이터를 처리하는 환경에서는 <strong>Streaming Data Lake Architecture</strong>를 사용할 수 있다.</p>

<p>Kafka는 이러한 스트리밍 데이터 파이프라인을 구성할 때 데이터를 전달하고 저장하는 중간 계층으로 활용할 수 있다.</p>

<h2 id="3--kafka의-주요-구성-요소">3 ) <strong>Kafka의 주요 구성 요소</strong></h2>

<hr />

<p>Kafka의 기본적인 데이터 흐름은 다음과 같이 볼 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer
    │
    ▼
  Topic
    │
    ├── Partition 0
    ├── Partition 1
    └── Partition 2
              │
              ▼
          Consumer
</code></pre></div></div>

<p>이를 구성하는 주요 개념으로 <strong>Broker, Producer, Consumer, Topic, Partition, Record</strong>가 있다.</p>

<h3 id="broker"><strong>Broker</strong></h3>

<p>Kafka가 실행되는 서버를 <strong>Broker</strong>라고 한다.</p>

<p>Broker는 Producer로부터 전달받은 Record를 저장하고 Consumer가 데이터를 읽을 수 있도록 제공한다.</p>

<p>하나의 Kafka 시스템은 여러 Broker로 구성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kafka Cluster

┌──────────┐
│ Broker 1 │
└──────────┘

┌──────────┐
│ Broker 2 │
└──────────┘

┌──────────┐
│ Broker 3 │
└──────────┘
</code></pre></div></div>

<p>여러 Broker를 이용하여 데이터를 분산 처리하고 장애에 대응할 수 있다.</p>

<h3 id="producer"><strong>Producer</strong></h3>

<p><strong>Producer</strong>는 Kafka에 데이터를 전송하는 역할을 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
     │
     ▼
  Producer
     │
     ▼
   Kafka
</code></pre></div></div>

<p>Producer는 데이터를 어떤 Topic으로 보낼 것인지 결정하여 Kafka Broker로 전송한다.</p>

<h3 id="consumer"><strong>Consumer</strong></h3>

<p><strong>Consumer</strong>는 Kafka에 저장된 데이터를 가져와 처리하는 역할을 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kafka
  │
  ▼
Consumer
  │
  ▼
Application
</code></pre></div></div>

<p>여러 Consumer를 하나의 그룹으로 구성하여 Partition의 Record를 병렬로 처리할 수도 있다.</p>

<h3 id="topic"><strong>Topic</strong></h3>

<p><strong>Topic</strong>은 Kafka에서 데이터를 구분하기 위해 사용하는 단위이다.</p>

<p>Producer는 특정 Topic에 데이터를 전송하고 Consumer는 필요한 Topic의 데이터를 가져간다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer
   │
   ▼
book-topic
   │
   ▼
Consumer
</code></pre></div></div>

<p>하나의 Topic은 <strong>하나 이상의 Partition을 가진다.</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Topic
│
├── Partition 0
├── Partition 1
└── Partition 2
</code></pre></div></div>

<p>Producer가 보낸 데이터는 Partition에 저장되며, 이렇게 저장되는 각각의 데이터를 <strong>Record</strong>라고 한다.</p>

<h3 id="record"><strong>Record</strong></h3>

<p><strong>Record</strong>는 Kafka에 저장되는 데이터의 단위이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Partition 0

Offset     0          1          2
           ↓          ↓          ↓
Record   "hello"    "Kafka"    "message"
</code></pre></div></div>

<p>강의에서는 Kafka가 Java로 구현되어 Java 객체를 사용할 수 있다고 설명하였다.</p>

<p>실제로 Kafka를 통해 객체 형태의 데이터를 전달할 때는 객체 자체를 그대로 저장하는 것이 아니라 <strong>Serialization(직렬화)</strong> 과정을 통해 전송 가능한 데이터 형태로 변환한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java Object
    │
    ▼
Serialization
    │
    ▼
 Producer
    │
    ▼
  Kafka
</code></pre></div></div>

<p>따라서 Java 객체를 포함한 다양한 형태의 데이터를 직렬화하여 Kafka를 통해 전달할 수 있다.</p>

<h3 id="partition"><strong>Partition</strong></h3>

<p><strong>Partition</strong>은 Topic 내부에서 Record가 실제로 저장되는 단위이며 Kafka의 <strong>병렬 처리에서 핵심적인 역할</strong>을 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Topic
│
├── Partition 0 → Record → Record → Record
│
├── Partition 1 → Record → Record → Record
│
└── Partition 2 → Record → Record → Record
</code></pre></div></div>

<p>강의에서는 Partition을 자료구조의 Queue와 비슷한 구조로 설명하였다.</p>

<p>Partition 내부에서는 먼저 들어온 Record부터 순서대로 저장되기 때문에 <strong>FIFO(First In First Out)와 유사한 형태로 이해할 수 있다.</strong></p>

<p>다만 Kafka의 Record는 Consumer가 읽었다고 즉시 삭제되는 Queue와는 다르며, 설정된 보존 정책에 따라 일정 기간 유지된다.</p>

<p>Partition은 Kafka에서 병렬 처리를 가능하게 하는 핵심 요소이기도 하다.</p>

<p>예를 들어 하나의 Topic에 여러 Partition이 있다면 Consumer Group에 속한 여러 Consumer가 각각의 Partition을 담당할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Topic

Partition 0 ───→ Consumer 1
Partition 1 ───→ Consumer 2
Partition 2 ───→ Consumer 3
</code></pre></div></div>

<p>따라서 처리해야 하는 데이터가 많아지면 Partition과 Consumer를 적절하게 구성하여 여러 데이터를 병렬로 처리할 수 있다.</p>

<p>강의에서는 <strong>Consumer 개수와 Partition 개수를 늘리는 방법을 통해 처리량을 증가시킬 수 있다</strong>는 점을 다루었다.</p>

<h2 id="zookeeper"><strong>ZooKeeper</strong></h2>

<hr />

<p><strong>ZooKeeper</strong>는 분산 시스템에서 여러 서버의 상태와 구성 정보를 관리하는 <strong>분산 코디네이터(Distributed Coordinator)</strong> 역할을 한다.</p>

<p>기존 Kafka에서는 Kafka Cluster를 관리하기 위해 ZooKeeper를 함께 사용하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              ZooKeeper
                  │
          Kafka Cluster 관리
                  │
       ┌──────────┼──────────┐
       ↓          ↓          ↓
    Broker 1   Broker 2   Broker 3
</code></pre></div></div>

<p>Kafka에서 ZooKeeper는 Broker의 상태를 관리하고, Cluster를 제어하는 Controller를 선출하거나 Topic과 Partition 등의 Metadata를 관리하는 과정에 사용되었다.</p>

<p>따라서 ZooKeeper가 Kafka의 Record를 직접 저장하거나 Producer와 Consumer 사이에서 Message를 전달하는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer → Kafka Broker → Consumer
                ↑
            ZooKeeper
        Cluster 상태 관리
</code></pre></div></div>

<p>기존 Kafka에서는 이러한 Cluster 관리 기능을 ZooKeeper에 의존했지만, 이후 <strong>KRaft(Kafka Raft)</strong> 방식이 도입되면서 Kafka가 자체적으로 Metadata를 관리할 수 있게 되었다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>기존 방식

Kafka Broker
     ↕
ZooKeeper
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>KRaft 방식

Kafka
└─ 자체적으로 Metadata 관리
</code></pre></div></div>

<p>따라서 강의에서 사용하는 ZooKeeper 기반 Kafka는 기존 Kafka의 Cluster 관리 구조이며, 최신 Kafka에서는 ZooKeeper 없이 Kafka Cluster를 구성할 수 있다.</p>

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>Apache Kafka는 LinkedIn 내부의 복잡한 데이터 흐름을 개선하기 위해 개발된 오픈소스 분산 이벤트 스트리밍 플랫폼이다. 기존처럼 소스와 타깃 애플리케이션을 직접 연결하는 대신 Kafka를 중간에 두어 데이터 흐름을 구성할 수 있으며, Big Data와 Data Pipeline 구축에 활용된다.</p>
    </li>
    <li>
      <p>Kafka를 사용하는 주요 이유는 <strong>높은 처리량, 확장성, 영속성, 고가용성</strong>이다. Kafka Server를 Broker라고 하며, 여러 Broker를 이용하여 분산 환경을 구성할 수 있다.</p>
    </li>
    <li>
      <p>Producer는 Kafka에 데이터를 <strong>Record</strong> 형태로 전송하고 Consumer는 저장된 Record를 읽어 처리한다. Record는 <strong>Topic</strong>을 기준으로 구분되며, 하나의 Topic은 하나 이상의 <strong>Partition</strong>으로 구성된다.</p>
    </li>
    <li>
      <p>Partition은 Kafka의 병렬 처리에서 핵심적인 역할을 한다. 하나의 Partition 내부에서는 Record의 순서가 유지되며, Consumer Group의 Consumer들이 서로 다른 Partition을 담당하여 병렬로 처리할 수 있다.</p>
    </li>
    <li>
      <p>기존 Kafka에서는 분산 코디네이터인 <strong>ZooKeeper</strong>가 Broker와 Metadata 관리에 사용되었다. 이후 <strong>KRaft(Kafka Raft)</strong> 가 도입되면서 ZooKeeper 없이 Kafka 자체적으로 Metadata를 관리할 수 있게 되었다.</p>
    </li>
    <li>
      <p>강의에서는 데이터 처리 아키텍처의 변화와 함께 <strong>Lambda Architecture, Kappa Architecture, Streaming Data Lake Architecture</strong>를 다루었으며, Kafka가 실시간 데이터 처리와 Streaming Pipeline을 구성하는 데 활용될 수 있음을 확인하였다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kafka" /><summary type="html"><![CDATA[Apache Kafka의 등장 배경과 특징, Topic과 Partition 중심의 Kafka의 기본 구조와 동작 방식]]></summary></entry><entry><title type="html">Kafka 설치와 기본 실습</title><link href="https://kkr010128.github.io/cloud-native-05-kafka-practice/" rel="alternate" type="text/html" title="Kafka 설치와 기본 실습" /><published>2026-08-12T00:00:00+09:00</published><updated>2026-08-12T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-05-kafka-practice</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-05-kafka-practice/"><![CDATA[<h2 id="1--docker를-이용한-kafka-실습">1 ) Docker를 이용한 Kafka 실습</h2>

<hr />

<h3 id="실습-목적">실습 목적</h3>

<p>Docker를 이용하여 Kafka와 ZooKeeper를 실행하고 다음 과정을 실습한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kafka / ZooKeeper 실행
	↓
Topic 생성
	↓
Producer 실행
	↓
Message 전송
	↓
Consumer 실행
	↓
Message 확인
</code></pre></div></div>

<h3 id="docker-compose-구성">Docker Compose 구성</h3>

<p><code class="language-plaintext highlighter-rouge">docker-compose.yml</code> 파일을 생성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">3.8'</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">zookeeper</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">wurstmeister/zookeeper:latest</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">zookeeper</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">2181:2181"</span>

  <span class="na">kafka</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">wurstmeister/kafka:latest</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">kafka</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">9092:9092"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">KAFKA_ADVERTISED_HOST_NAME</span><span class="pi">:</span> <span class="s">127.0.0.1</span>
      <span class="na">KAFKA_ZOOKEEPER_CONNECT</span><span class="pi">:</span> <span class="s">zookeeper:2181</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">/var/run/docker.sock:/var/run/docker.sock</span>
</code></pre></div></div>

<p>주요 설정은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">2181:2181</code></td>
      <td>ZooKeeper가 사용하는 Port를 Host에 연결한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">9092:9092</code></td>
      <td>Kafka Broker가 사용하는 Port를 Host에 연결한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">KAFKA_ADVERTISED_HOST_NAME</code></td>
      <td>Client에게 알려줄 Kafka Broker의 Host 주소를 설정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">KAFKA_ZOOKEEPER_CONNECT</code></td>
      <td>Kafka가 연결할 ZooKeeper의 주소를 설정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/var/run/docker.sock</code></td>
      <td>Container에서 Host의 Docker Daemon과 통신할 수 있도록 Docker Socket을 연결한다.</td>
    </tr>
  </tbody>
</table>

<h3 id="kafka-실행">Kafka 실행</h3>

<p>작성한 Compose 파일을 이용하여 Container를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker-compose up <span class="nt">-d</span>
</code></pre></div></div>

<p>Kafka와 ZooKeeper가 실행되면 Kafka Container 내부로 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec</span> <span class="nt">-it</span> kafka /bin/bash
</code></pre></div></div>

<p>Kafka 명령어가 위치한 디렉터리로 이동한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /opt/kafka/bin
</code></pre></div></div>

<h3 id="topic-생성">Topic 생성</h3>

<p>실습에서 사용할 <code class="language-plaintext highlighter-rouge">exam-topic</code>을 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-topics.sh <span class="se">\</span>
  <span class="nt">--create</span> <span class="se">\</span>
  <span class="nt">--zookeeper</span> zookeeper:2181 <span class="se">\</span>
  <span class="nt">--replication-factor</span> 1 <span class="se">\</span>
  <span class="nt">--partitions</span> 1 <span class="se">\</span>
  <span class="nt">--topic</span> exam-topic
</code></pre></div></div>

<p>실습에서는 하나의 Kafka Broker를 사용하므로 Replication Factor를 <code class="language-plaintext highlighter-rouge">1</code>로 지정하고, Partition도 하나만 생성한다.</p>

<p>생성된 Topic을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-topics.sh <span class="se">\</span>
  <span class="nt">--bootstrap-server</span> localhost:9092 <span class="se">\</span>
  <span class="nt">--list</span>
</code></pre></div></div>

<p>→ 생성된 <code class="language-plaintext highlighter-rouge">exam-topic</code>을 확인한다.</p>

<p>Topic을 삭제할 경우 다음 명령을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-topics.sh <span class="se">\</span>
  <span class="nt">--delete</span> <span class="se">\</span>
  <span class="nt">--zookeeper</span> zookeeper:2181 <span class="se">\</span>
  <span class="nt">--topic</span> exam-topic
</code></pre></div></div>

<h2 id="2--producer와-consumer-실습">2 ) Producer와 Consumer 실습</h2>

<hr />

<h3 id="producer-실행">Producer 실행</h3>

<p>Producer를 실행하여 <code class="language-plaintext highlighter-rouge">exam-topic</code>으로 Message를 전송한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-console-producer.sh <span class="se">\</span>
  <span class="nt">--topic</span> exam-topic <span class="se">\</span>
  <span class="nt">--broker-list</span> localhost:9092
</code></pre></div></div>

<p>Producer가 실행되면 Terminal에서 입력한 Message가 <code class="language-plaintext highlighter-rouge">exam-topic</code>으로 전송된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer
   │
   │ Message
   ▼
exam-topic
</code></pre></div></div>

<h3 id="consumer-실행">Consumer 실행</h3>

<p>별도의 Terminal을 열고 Kafka Container에 다시 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec</span> <span class="nt">-it</span> kafka /bin/bash

<span class="nb">cd</span> /opt/kafka/bin
</code></pre></div></div>

<p>Consumer를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-console-consumer.sh <span class="se">\</span>
  <span class="nt">--topic</span> exam-topic <span class="se">\</span>
  <span class="nt">--bootstrap-server</span> localhost:9092 <span class="se">\</span>
  <span class="nt">--from-beginning</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--from-beginning</code>을 사용하면 해당 Consumer가 읽을 위치가 없는 경우 Topic의 처음부터 Record를 읽는다.</p>

<p>전체 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Terminal 1

Producer
   │
   ▼
exam-topic
   │
   ▼
Consumer

Terminal 2
</code></pre></div></div>

<p>Producer Terminal에서 Message를 입력하고 Consumer Terminal에서 해당 Message가 출력되는지 확인한다.</p>

<p><img src="../../assets/post/2026-08-12-cloud-native-05-kafka-practice/04.webp" alt="" />
<img src="../../assets/post/2026-08-12-cloud-native-05-kafka-practice/02.webp" alt="" /></p>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>Docker Compose를 이용하여 Kafka와 ZooKeeper를 함께 실행하였다.</li>
    <li>Kafka Container 내부에서 CLI를 이용하여 Topic을 생성하고 확인할 수 있다.</li>
    <li>Producer는 Topic으로 Message를 전송한다.</li>
    <li>Consumer는 Topic에 저장된 Message를 가져와 처리한다.</li>
  </ul>
</blockquote>

<h2 id="3--ubuntu에-kafka-직접-설치">3 ) Ubuntu에 Kafka 직접 설치</h2>

<hr />

<p>Docker Container가 아니라 Ubuntu 환경에 Kafka를 직접 설치하여 실행한다.</p>

<p>Kafka는 Java로 구현되어 있으므로 먼저 JDK를 설치한다.</p>

<h3 id="jdk-설치">JDK 설치</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>openjdk-17-jdk

java <span class="nt">-version</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">java -version</code>을 이용하여 JDK가 정상적으로 설치되었는지 확인한다.</p>

<h3 id="kafka-설치">Kafka 설치</h3>

<p>강의에서는 Kafka <code class="language-plaintext highlighter-rouge">3.6.0</code> 버전을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget https://archive.apache.org/dist/kafka/3.6.0/kafka_2.13-3.6.0.tgz
</code></pre></div></div>

<p>다운로드한 파일의 압축을 해제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">tar </span>xvf kafka_2.13-3.6.0.tgz
</code></pre></div></div>

<p>압축을 해제한 Kafka 디렉터리를 <code class="language-plaintext highlighter-rouge">/opt/kafka</code>로 이동한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mv </span>kafka_2.13-3.6.0 /opt/kafka
</code></pre></div></div>

<h3 id="환경-변수-설정">환경 변수 설정</h3>

<p>Kafka 명령어를 경로와 관계없이 사용할 수 있도록 환경 변수를 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">KAFKA_HOME</span><span class="o">=</span>/opt/kafka
<span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="nv">$PATH</span>:<span class="nv">$KAFKA_HOME</span>/bin
</code></pre></div></div>

<p>변경 사항을 바로 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> ~/.bashrc
</code></pre></div></div>

<h2 id="4--kafka-heap-memory-설정">4 ) Kafka Heap Memory 설정</h2>

<hr />

<h3 id="메모리를-확인해야-하는-이유">메모리를 확인해야 하는 이유</h3>

<blockquote>
  <p>EC와 Cloud Infrastructure의 Instance에 Kafka를 설치할 때는 Memory를 확인해야 한다.</p>
</blockquote>

<p>강의에서는 Kafka와 ZooKeeper를 함께 실행하는 데 약 <code class="language-plaintext highlighter-rouge">1.5GB</code>의 Memory가 필요할 수 있다고 설명하였다.</p>

<p>실습용 Cloud Instance나 VM에 약 <code class="language-plaintext highlighter-rouge">1GB</code> 정도의 Memory만 할당되어 있다면 Kafka가 정상적으로 실행되지 않을 수 있다.</p>

<p>따라서 더 많은 Memory를 가진 Instance를 사용하거나, 실습 환경에서는 Kafka가 사용하는 Heap Memory를 줄여 실행할 수 있다.</p>

<h3 id="heap-memory-제한">Heap Memory 제한</h3>

<p><code class="language-plaintext highlighter-rouge">~/.bashrc</code>를 연다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano ~/.bashrc
</code></pre></div></div>

<p>다음 환경 변수를 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">KAFKA_HEAP_OPTS</span><span class="o">=</span><span class="s2">"-Xmx400m -Xms400m"</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-Xms400m</code></td>
      <td>JVM의 초기 Heap Memory를 400MB로 설정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-Xmx400m</code></td>
      <td>JVM의 최대 Heap Memory를 400MB로 설정한다.</td>
    </tr>
  </tbody>
</table>

<p>설정을 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> ~/.bashrc
</code></pre></div></div>

<p>설정된 값을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="nv">$KAFKA_HEAP_OPTS</span>
</code></pre></div></div>

<p>→ 다음과 같이 설정되어 있는지 확인한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>-Xmx400m -Xms400m
</code></pre></div></div>

<h2 id="5--zookeeper와-kafka-broker-실행">5 ) ZooKeeper와 Kafka Broker 실행</h2>

<hr />

<h3 id="zookeeper-실행">ZooKeeper 실행</h3>

<p>강의에서 사용하는 Kafka 구성은 ZooKeeper를 이용하므로 Kafka Broker보다 먼저 ZooKeeper를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>zookeeper-server-start.sh <span class="se">\</span>
  <span class="nt">-daemon</span> <span class="se">\</span>
  /opt/kafka/config/zookeeper.properties
</code></pre></div></div>

<p>실행된 Java Process를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jps <span class="nt">-vm</span>
</code></pre></div></div>

<p>ZooKeeper Process가 실행되어 있는지 확인한다.</p>

<h3 id="kafka-broker-설정">Kafka Broker 설정</h3>

<p>Kafka의 설정 파일을 수정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /opt/kafka
nano config/server.properties
</code></pre></div></div>

<p>강의에서 수정한 주요 설정은 다음과 같다.</p>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">listeners</span><span class="p">=</span><span class="s">PLAINTEXT://:9092</span>

<span class="py">advertised.listeners</span><span class="p">=</span><span class="s">PLAINTEXT://127.0.0.1:9092</span>

<span class="py">delete.topic.enable</span><span class="p">=</span><span class="s">true</span>

<span class="py">auto.create.topics.enable</span><span class="p">=</span><span class="s">true</span>
</code></pre></div></div>

<h4 id="listeners"><code class="language-plaintext highlighter-rouge">listeners</code></h4>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">listeners</span><span class="p">=</span><span class="s">PLAINTEXT://:9092</span>
</code></pre></div></div>

<p>Kafka Broker가 실제로 요청을 받을 Listener와 Port를 설정한다.</p>

<p>여기서는 <code class="language-plaintext highlighter-rouge">9092</code> Port에서 Kafka Client의 연결을 받도록 설정한다.</p>

<h4 id="advertisedlisteners"><code class="language-plaintext highlighter-rouge">advertised.listeners</code></h4>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">advertised.listeners</span><span class="p">=</span><span class="s">PLAINTEXT://127.0.0.1:9092</span>
</code></pre></div></div>

<p>Kafka Broker가 Producer나 Consumer와 같은 Client에게 알려주는 접속 주소이다.</p>

<p>로컬에서 Kafka를 사용할 경우 <code class="language-plaintext highlighter-rouge">127.0.0.1</code>을 사용할 수 있다.</p>

<blockquote>
  <p>외부에서 이 Kafka Broker에 접근해야 한다면 <code class="language-plaintext highlighter-rouge">127.0.0.1</code> 대신 실제로 접근 가능한 IP 주소를 설정해야 한다.</p>
</blockquote>

<p>즉, 두 설정의 역할을 구분하면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>listeners
→ Broker가 어디에서 연결을 받을 것인가?

advertised.listeners
→ Client에게 어떤 주소로 접속하라고 알려줄 것인가?
</code></pre></div></div>

<h4 id="deletetopicenable"><code class="language-plaintext highlighter-rouge">delete.topic.enable</code></h4>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">delete.topic.enable</span><span class="p">=</span><span class="s">true</span>
</code></pre></div></div>

<p>Topic 삭제 기능을 활성화한다.</p>

<h4 id="autocreatetopicsenable"><code class="language-plaintext highlighter-rouge">auto.create.topics.enable</code></h4>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">auto.create.topics.enable</span><span class="p">=</span><span class="s">true</span>
</code></pre></div></div>

<p>존재하지 않는 Topic에 대한 요청이 들어왔을 때 Topic을 자동으로 생성할 수 있도록 설정한다.</p>

<h3 id="kafka-broker-실행">Kafka Broker 실행</h3>

<p>설정을 완료한 후 Kafka Broker를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-server-start.sh <span class="se">\</span>
  <span class="nt">-daemon</span> <span class="se">\</span>
  /opt/kafka/config/server.properties
</code></pre></div></div>

<p>실행 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jps <span class="nt">-m</span>
</code></pre></div></div>

<p>ZooKeeper와 Kafka가 모두 실행되어 있는지 확인한다.</p>

<p><img src="../../assets/post/2026-08-12-cloud-native-05-kafka-practice/06.webp" alt="" /></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Ubuntu

├── ZooKeeper
│
└── Kafka Broker
       │
       └── :9092
</code></pre></div></div>

<h2 id="6--python에서-kafka-사용">6 ) Python에서 Kafka 사용</h2>

<hr />

<p>Kafka CLI를 이용한 Message 송수신에 이어 Python 애플리케이션에서 Kafka의 Producer와 Consumer를 구현한다.</p>

<p>실습에서는 <code class="language-plaintext highlighter-rouge">kafka-python</code> 패키지를 사용하며, 앞에서 생성한 <code class="language-plaintext highlighter-rouge">exam-topic</code>을 그대로 사용한다.</p>

<p>전체 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Python Producer
      │
      │ Message 전송
      ▼
  exam-topic
      │
      │ Message 수신
      ▼
Python Consumer
</code></pre></div></div>

<h3 id="kafka-python-설치"><strong>kafka-python 설치</strong></h3>

<p>Python에서 Kafka를 사용하기 위해 <code class="language-plaintext highlighter-rouge">kafka-python</code> 패키지를 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>kafka-python
</code></pre></div></div>

<h3 id="producer-구현"><strong>Producer 구현</strong></h3>

<p><code class="language-plaintext highlighter-rouge">pythonproducer.py</code> 파일을 생성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">kafka</span> <span class="kn">import</span> <span class="n">KafkaProducer</span>
<span class="kn">import</span> <span class="nn">json</span>


<span class="k">class</span> <span class="nc">MessageProducer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">broker</span><span class="p">,</span> <span class="n">topic</span><span class="p">):</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">broker</span> <span class="o">=</span> <span class="n">broker</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">topic</span> <span class="o">=</span> <span class="n">topic</span>

        <span class="bp">self</span><span class="p">.</span><span class="n">producer</span> <span class="o">=</span> <span class="n">KafkaProducer</span><span class="p">(</span>
            <span class="n">bootstrap_servers</span><span class="o">=</span><span class="bp">self</span><span class="p">.</span><span class="n">broker</span><span class="p">,</span>
            <span class="n">value_serializer</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">json</span><span class="p">.</span><span class="n">dumps</span><span class="p">(</span><span class="n">x</span><span class="p">).</span><span class="n">encode</span><span class="p">(</span><span class="s">"utf-8"</span><span class="p">),</span>
            <span class="n">acks</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span>
            <span class="n">api_version</span><span class="o">=</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">),</span>
            <span class="n">key_serializer</span><span class="o">=</span><span class="nb">str</span><span class="p">.</span><span class="n">encode</span><span class="p">,</span>
            <span class="n">retries</span><span class="o">=</span><span class="mi">3</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">send_message</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">msg</span><span class="p">,</span> <span class="n">auto_close</span><span class="o">=</span><span class="bp">True</span><span class="p">):</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">future</span> <span class="o">=</span> <span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">send</span><span class="p">(</span>
                <span class="bp">self</span><span class="p">.</span><span class="n">topic</span><span class="p">,</span>
                <span class="n">value</span><span class="o">=</span><span class="n">msg</span><span class="p">,</span>
                <span class="n">key</span><span class="o">=</span><span class="s">"key"</span>
            <span class="p">)</span>

            <span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">flush</span><span class="p">()</span>

            <span class="k">if</span> <span class="n">auto_close</span><span class="p">:</span>
                <span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">close</span><span class="p">()</span>

            <span class="n">future</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>

            <span class="k">return</span> <span class="p">{</span>
                <span class="s">"status_code"</span><span class="p">:</span> <span class="mi">200</span><span class="p">,</span>
                <span class="s">"error"</span><span class="p">:</span> <span class="bp">None</span>
            <span class="p">}</span>

        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">exc</span><span class="p">:</span>
            <span class="k">raise</span> <span class="n">exc</span>


<span class="n">broker</span> <span class="o">=</span> <span class="p">[</span><span class="s">"localhost:9092"</span><span class="p">]</span>
<span class="n">topic</span> <span class="o">=</span> <span class="s">"exam-topic"</span>

<span class="n">pd</span> <span class="o">=</span> <span class="n">MessageProducer</span><span class="p">(</span><span class="n">broker</span><span class="p">,</span> <span class="n">topic</span><span class="p">)</span>

<span class="n">msg</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"name"</span><span class="p">:</span> <span class="s">"adam"</span><span class="p">,</span>
    <span class="s">"age"</span><span class="p">:</span> <span class="mi">50</span>
<span class="p">}</span>

<span class="n">res</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">send_message</span><span class="p">(</span><span class="n">msg</span><span class="p">)</span>

<span class="k">print</span><span class="p">(</span><span class="n">res</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="producer-설정"><strong>Producer 설정</strong></h3>

<p><code class="language-plaintext highlighter-rouge">KafkaProducer</code>를 생성할 때 Kafka Broker와 Message 직렬화 방법 등을 설정한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">self</span><span class="p">.</span><span class="n">producer</span> <span class="o">=</span> <span class="n">KafkaProducer</span><span class="p">(</span>
    <span class="n">bootstrap_servers</span><span class="o">=</span><span class="bp">self</span><span class="p">.</span><span class="n">broker</span><span class="p">,</span>
    <span class="n">value_serializer</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">json</span><span class="p">.</span><span class="n">dumps</span><span class="p">(</span><span class="n">x</span><span class="p">).</span><span class="n">encode</span><span class="p">(</span><span class="s">"utf-8"</span><span class="p">),</span>
    <span class="n">acks</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span>
    <span class="n">api_version</span><span class="o">=</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">),</span>
    <span class="n">key_serializer</span><span class="o">=</span><span class="nb">str</span><span class="p">.</span><span class="n">encode</span><span class="p">,</span>
    <span class="n">retries</span><span class="o">=</span><span class="mi">3</span>
<span class="p">)</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th><strong>설정</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">bootstrap_servers</code></td>
      <td>Producer가 연결할 Kafka Broker를 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">value_serializer</code></td>
      <td>Python 객체를 JSON 문자열로 변환한 뒤 UTF-8 Byte 형태로 변환한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">acks=0</code></td>
      <td>Broker의 Message 수신 확인을 기다리지 않는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">api_version</code></td>
      <td>사용할 Kafka API Version을 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">key_serializer</code></td>
      <td>Message의 Key를 Byte 형태로 변환한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">retries=3</code></td>
      <td>Message 전송에 실패했을 때 재시도 횟수를 지정한다.</td>
    </tr>
  </tbody>
</table>

<p>실습에서는 다음 Kafka Broker와 Topic을 사용한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">broker</span> <span class="o">=</span> <span class="p">[</span><span class="s">"localhost:9092"</span><span class="p">]</span>
<span class="n">topic</span> <span class="o">=</span> <span class="s">"exam-topic"</span>
</code></pre></div></div>

<p>전송할 데이터는 Python Dictionary로 작성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">msg</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"name"</span><span class="p">:</span> <span class="s">"adam"</span><span class="p">,</span>
    <span class="s">"age"</span><span class="p">:</span> <span class="mi">50</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">value_serializer</code>에 의해 이 객체가 JSON 형태로 직렬화되어 Kafka로 전달된다.</p>

<h3 id="message-전송"><strong>Message 전송</strong></h3>

<p>Message는 <code class="language-plaintext highlighter-rouge">send()</code>를 이용하여 Topic으로 전송한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">future</span> <span class="o">=</span> <span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">send</span><span class="p">(</span>
    <span class="bp">self</span><span class="p">.</span><span class="n">topic</span><span class="p">,</span>
    <span class="n">value</span><span class="o">=</span><span class="n">msg</span><span class="p">,</span>
    <span class="n">key</span><span class="o">=</span><span class="s">"key"</span>
<span class="p">)</span>
</code></pre></div></div>

<p>강의에서는 <code class="language-plaintext highlighter-rouge">send()</code>가 전송할 Message를 Buffer에 넣고, 실제 전송을 위해 <code class="language-plaintext highlighter-rouge">flush()</code>를 호출하는 방식으로 구현하였다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">flush</span><span class="p">()</span>
</code></pre></div></div>

<p>전송 후 Producer를 종료한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">auto_close</span><span class="p">:</span>
    <span class="bp">self</span><span class="p">.</span><span class="n">producer</span><span class="p">.</span><span class="n">close</span><span class="p">()</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">future.get()</code>을 이용하여 전송 결과를 기다린다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">future</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div>

<p>Producer의 흐름을 정리하면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Python Dictionary
       ↓
JSON 직렬화
       ↓
UTF-8 Byte 변환
       ↓
KafkaProducer
       ↓
send()
       ↓
Buffer
       ↓
flush()
       ↓
exam-topic
</code></pre></div></div>

<h3 id="consumer-구현"><strong>Consumer 구현</strong></h3>

<p>Producer가 전송한 Message를 읽기 위해 <code class="language-plaintext highlighter-rouge">pythonconsumer.py</code> 파일을 생성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">kafka</span> <span class="kn">import</span> <span class="n">KafkaConsumer</span>
<span class="kn">import</span> <span class="nn">json</span>


<span class="k">class</span> <span class="nc">MessageConsumer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">broker</span><span class="p">,</span> <span class="n">topic</span><span class="p">):</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">broker</span> <span class="o">=</span> <span class="n">broker</span>

        <span class="bp">self</span><span class="p">.</span><span class="n">consumer</span> <span class="o">=</span> <span class="n">KafkaConsumer</span><span class="p">(</span>
            <span class="n">topic</span><span class="p">,</span>
            <span class="n">bootstrap_servers</span><span class="o">=</span><span class="bp">self</span><span class="p">.</span><span class="n">broker</span><span class="p">,</span>
            <span class="n">value_deserializer</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">.</span><span class="n">decode</span><span class="p">(</span><span class="s">"utf-8"</span><span class="p">),</span>
            <span class="n">group_id</span><span class="o">=</span><span class="s">"my-group"</span><span class="p">,</span>
            <span class="n">auto_offset_reset</span><span class="o">=</span><span class="s">"earliest"</span><span class="p">,</span>
            <span class="n">enable_auto_commit</span><span class="o">=</span><span class="bp">True</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">receive_message</span><span class="p">(</span><span class="bp">self</span><span class="p">):</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="k">for</span> <span class="n">message</span> <span class="ow">in</span> <span class="bp">self</span><span class="p">.</span><span class="n">consumer</span><span class="p">:</span>
                <span class="k">print</span><span class="p">(</span><span class="n">message</span><span class="p">.</span><span class="n">value</span><span class="p">)</span>

        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">exc</span><span class="p">:</span>
            <span class="k">raise</span> <span class="n">exc</span>


<span class="n">broker</span> <span class="o">=</span> <span class="p">[</span><span class="s">"localhost:9092"</span><span class="p">]</span>
<span class="n">topic</span> <span class="o">=</span> <span class="s">"exam-topic"</span>

<span class="n">cs</span> <span class="o">=</span> <span class="n">MessageConsumer</span><span class="p">(</span><span class="n">broker</span><span class="p">,</span> <span class="n">topic</span><span class="p">)</span>

<span class="n">cs</span><span class="p">.</span><span class="n">receive_message</span><span class="p">()</span>
</code></pre></div></div>

<h3 id="consumer-설정"><strong>Consumer 설정</strong></h3>

<p>Consumer는 <code class="language-plaintext highlighter-rouge">KafkaConsumer</code>를 이용하여 생성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">self</span><span class="p">.</span><span class="n">consumer</span> <span class="o">=</span> <span class="n">KafkaConsumer</span><span class="p">(</span>
    <span class="n">topic</span><span class="p">,</span>
    <span class="n">bootstrap_servers</span><span class="o">=</span><span class="bp">self</span><span class="p">.</span><span class="n">broker</span><span class="p">,</span>
    <span class="n">value_deserializer</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">.</span><span class="n">decode</span><span class="p">(</span><span class="s">"utf-8"</span><span class="p">),</span>
    <span class="n">group_id</span><span class="o">=</span><span class="s">"my-group"</span><span class="p">,</span>
    <span class="n">auto_offset_reset</span><span class="o">=</span><span class="s">"earliest"</span><span class="p">,</span>
    <span class="n">enable_auto_commit</span><span class="o">=</span><span class="bp">True</span>
<span class="p">)</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th><strong>설정</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">topic</code></td>
      <td>Consumer가 Message를 읽을 Topic을 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">bootstrap_servers</code></td>
      <td>연결할 Kafka Broker를 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">value_deserializer</code></td>
      <td>Kafka에서 읽은 Byte 데이터를 UTF-8 문자열로 변환한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">group_id</code></td>
      <td>Consumer가 속할 Consumer Group을 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">auto_offset_reset="earliest"</code></td>
      <td>저장된 Offset이 없는 경우 가장 처음 위치부터 읽도록 설정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">enable_auto_commit=True</code></td>
      <td>Consumer가 읽은 Offset을 자동으로 Commit하도록 설정한다.</td>
    </tr>
  </tbody>
</table>

<h3 id="message-수신"><strong>Message 수신</strong></h3>

<p>생성한 Consumer를 반복하면서 Kafka에서 전달되는 Message를 읽는다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">message</span> <span class="ow">in</span> <span class="bp">self</span><span class="p">.</span><span class="n">consumer</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="n">message</span><span class="p">.</span><span class="n">value</span><span class="p">)</span>
</code></pre></div></div>

<p>Producer가 다음 데이터를 전송하면,</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">msg</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"name"</span><span class="p">:</span> <span class="s">"adam"</span><span class="p">,</span>
    <span class="s">"age"</span><span class="p">:</span> <span class="mi">50</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Consumer는 <code class="language-plaintext highlighter-rouge">exam-topic</code>에서 해당 Message를 읽어 출력한다.</p>

<p>전체 동작은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pythonproducer.py

{"name": "adam", "age": 50}
            │
            ▼
       KafkaProducer
            │
            ▼
        exam-topic
            │
            ▼
       KafkaConsumer
            │
            ▼
pythonconsumer.py
</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kafka-python</code>을 이용하면 Python 애플리케이션에서 Kafka Producer와 Consumer를 구현할 수 있다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">KafkaProducer</code>는 Message를 직렬화하여 Kafka Topic으로 전송하고, <code class="language-plaintext highlighter-rouge">KafkaConsumer</code>는 Topic에 저장된 Message를 읽는다.</p>
    </li>
    <li>
      <p>실습에서는 Python Dictionary를 JSON으로 직렬화한 뒤 UTF-8 Byte 형태로 변환하여 <code class="language-plaintext highlighter-rouge">exam-topic</code>으로 전송한다.</p>
    </li>
    <li>
      <p>Producer의 <code class="language-plaintext highlighter-rouge">send()</code>로 Message를 전송하고 <code class="language-plaintext highlighter-rouge">flush()</code>를 호출하여 Buffer의 Message를 전송한다.</p>
    </li>
    <li>
      <p>Consumer는 <code class="language-plaintext highlighter-rouge">group_id</code>를 통해 Consumer Group을 지정하며, <code class="language-plaintext highlighter-rouge">auto_offset_reset="earliest"</code>를 이용하여 저장된 Offset이 없는 경우 처음부터 Message를 읽도록 설정한다.</p>
    </li>
    <li>
      <p>Python에서도 CLI 실습과 동일하게 <code class="language-plaintext highlighter-rouge">Producer → Topic → Consumer</code>의 흐름으로 Kafka Message를 송수신할 수 있다.</p>
      <h2 id="7--docker와-ubuntu-실습-비교">7 ) Docker와 Ubuntu 실습 비교</h2>
    </li>
  </ul>
</blockquote>

<hr />

<p>이번 실습에서는 동일한 Kafka를 두 가지 방법으로 구성하였다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Docker</th>
      <th>Ubuntu 직접 설치</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>실행 환경</td>
      <td>Container</td>
      <td>Ubuntu OS</td>
    </tr>
    <tr>
      <td>Kafka 설치</td>
      <td>Image 사용</td>
      <td>Kafka Binary 직접 설치</td>
    </tr>
    <tr>
      <td>JDK 설치</td>
      <td>Image 내부 환경 사용</td>
      <td>직접 설치</td>
    </tr>
    <tr>
      <td>ZooKeeper</td>
      <td>별도 Container</td>
      <td>직접 Process 실행</td>
    </tr>
    <tr>
      <td>Kafka 설정</td>
      <td>환경 변수 중심</td>
      <td><code class="language-plaintext highlighter-rouge">server.properties</code> 직접 수정</td>
    </tr>
    <tr>
      <td>실행</td>
      <td>Docker Compose</td>
      <td>Kafka 실행 Script</td>
    </tr>
    <tr>
      <td>목적</td>
      <td>빠른 Kafka 환경 구성</td>
      <td>Kafka 실행 환경과 설정 구조 확인</td>
    </tr>
  </tbody>
</table>

<p>Docker를 이용하면 Kafka 실행 환경을 비교적 빠르게 구성할 수 있다.</p>

<p>반면 Ubuntu에 직접 설치하면 JDK, 환경 변수, Heap Memory, ZooKeeper, Kafka Broker 설정 등을 직접 구성하면서 Kafka가 어떤 환경에서 실행되는지 확인할 수 있다.</p>

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>Kafka는 <strong>Docker Compose</strong>를 이용해 ZooKeeper와 함께 빠르게 구성할 수도 있고, Ubuntu에 직접 설치하여 실행 환경을 구성할 수도 있다.</p>
    </li>
    <li>
      <p>Kafka CLI를 이용하면 <strong>Topic을 생성·조회·삭제</strong>할 수 있으며, Console Producer와 Consumer를 이용하여 Topic을 통한 Message 송수신 과정을 직접 확인할 수 있다.</p>
    </li>
    <li>
      <p>Ubuntu에 Kafka를 직접 설치하는 경우 Kafka가 Java 기반으로 동작하므로 <strong>JDK를 먼저 설치</strong>해야 하며, 강의에서 사용한 ZooKeeper 기반 구성에서는 ZooKeeper를 실행한 뒤 Kafka Broker를 실행한다.</p>
    </li>
    <li>
      <p>Memory가 제한된 VM이나 Cloud Instance에서는 Kafka 실행에 필요한 Memory를 확인해야 한다. 실습 환경에서는 <code class="language-plaintext highlighter-rouge">KAFKA_HEAP_OPTS</code>를 이용하여 Kafka JVM의 초기 및 최대 Heap Memory를 조정할 수 있다.</p>
    </li>
    <li>
      <p>Kafka Broker 설정에서 <code class="language-plaintext highlighter-rouge">listeners</code>는 <strong>Broker가 실제 연결을 수신할 주소와 Port</strong>를 지정하고, <code class="language-plaintext highlighter-rouge">advertised.listeners</code>는 <strong>Producer와 Consumer 등의 Client에게 알려줄 Broker의 접속 주소</strong>를 지정한다.</p>
    </li>
    <li>
      <p>Docker 실습은 Kafka 환경을 빠르게 구성하여 기본 동작을 확인하는 데 적합하고, Ubuntu 직접 설치 실습은 <strong>JDK, 환경 변수, Heap Memory, ZooKeeper, Broker 설정과 실행 과정</strong>을 직접 구성하면서 Kafka의 실행 구조를 확인하는 데 의미가 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kafka" /><summary type="html"><![CDATA[Docker와 Ubuntu 환경에 Kafka 구성, Broker와 ZooKeepeer 실행 및 Topic 생성과 Producer, Consumer 메시지 송수신 실습]]></summary></entry><entry><title type="html">CQRS와 읽기 · 쓰기 · 책임의 분리</title><link href="https://kkr010128.github.io/cloud-native-06-cqrs/" rel="alternate" type="text/html" title="CQRS와 읽기 · 쓰기 · 책임의 분리" /><published>2026-08-12T00:00:00+09:00</published><updated>2026-08-12T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-06-cqrs</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-06-cqrs/"><![CDATA[<h2 id="1--cqrs"><strong>1 ) CQRS</strong></h2>

<hr />

<h3 id="cqrs란">CQRS란</h3>

<p>CQRS(Command Query Responsibility Segregation)는 <strong>데이터를 변경하는 작업과 조회하는 작업의 책임을 분리하는 패턴</strong>이다.</p>

<p>여기서 애플리케이션의 요청을 크게 두 가지로 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Command</td>
      <td>데이터의 상태를 변경한다.</td>
    </tr>
    <tr>
      <td>Query</td>
      <td>데이터를 조회한다.</td>
    </tr>
  </tbody>
</table>

<p>즉, 하나의 모델에서 데이터의 조회와 변경을 모두 담당하는 대신 <strong>쓰기(Command)와 읽기(Query)를 서로 다른 책임으로 분리</strong>한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                  ┌→ Command → 데이터 변경
Client ───────────┤
                  └→ Query   → 데이터 조회
</code></pre></div></div>

<p>CQRS를 적용하면 읽기와 쓰기를 각각의 요구사항에 맞게 구성할 수 있으므로 성능, 확장성, 보안 등의 측면에서 서로 다른 전략을 적용할 수 있다.</p>

<h3 id="전통적인-구조의-문제점"><strong>전통적인 구조의 문제점</strong></h3>

<p>일반적인 CRUD 애플리케이션에서는 하나의 데이터 모델을 이용하여 데이터의 생성, 조회, 수정, 삭제를 모두 처리할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                하나의 Model
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
      Create       Read      Update / Delete
                     │
                 Database
</code></pre></div></div>

<p>단순한 애플리케이션에서는 이러한 구조가 오히려 이해하고 구현하기 쉽다.</p>

<p>하지만 애플리케이션과 비즈니스 로직이 복잡해지면 <strong>읽기와 쓰기가 하나의 모델에 서로 다른 요구사항을 요구하는 문제</strong>가 발생할 수 있다.</p>

<h4 id="읽기와-쓰기가-요구하는-형태의-차이"><strong>읽기와 쓰기가 요구하는 형태의 차이</strong></h4>

<p>데이터를 조회할 때는 사용자에게 필요한 형태에 따라 서로 다른 DTO(Data Transfer Object)를 반환해야 할 수 있다.</p>

<p>반면 데이터를 생성하거나 수정할 때는 단순한 데이터 저장뿐 아니라 다음과 같은 처리가 필요할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>입력
 ↓
유효성 검사
 ↓
비즈니스 로직
 ↓
데이터 변경
</code></pre></div></div>

<p>하나의 모델이 이러한 읽기와 쓰기의 요구사항을 모두 처리하기 시작하면 모델의 역할과 구조가 복잡해질 수 있다.</p>

<h4 id="읽기와-쓰기의-부하-차이"><strong>읽기와 쓰기의 부하 차이</strong></h4>

<p>일반적으로 읽기와 쓰기의 요청량이나 필요한 성능이 항상 동일하지는 않다.</p>

<p>예를 들어 조회 요청이 쓰기 요청보다 훨씬 많은 서비스라면 읽기 영역에 더 많은 자원이 필요할 수 있다.</p>

<p>하지만 읽기와 쓰기가 하나의 구조에 강하게 묶여 있으면 각각을 독립적으로 최적화하거나 확장하기 어려울 수 있다.</p>

<h4 id="불필요한-데이터-처리"><strong>불필요한 데이터 처리</strong></h4>

<p>읽기와 쓰기에서 사용하는 데이터 표현이 서로 일치하지 않을 수도 있다.</p>

<p>하나의 모델을 공통으로 사용하면 특정 작업에는 필요하지 않은 Column이나 속성까지 함께 처리해야 하는 상황이 발생할 수 있다.</p>

<h4 id="데이터-경합"><strong>데이터 경합</strong></h4>

<p>여러 사용자가 동일한 데이터에 대해 병렬로 작업하면 데이터 경합이 발생할 수 있다.</p>

<p>특히 하나의 데이터 모델과 저장소에 조회와 변경 요청이 집중되면 복잡한 동시성 처리가 필요해질 수 있다.</p>

<h4 id="복잡한-조회"><strong>복잡한 조회</strong></h4>

<p>사용자가 요구하는 형태의 데이터를 만들기 위해 여러 Table을 대상으로 복잡한 Query를 수행해야 할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Table A ─┐
Table B ─┼→ JOIN / 복잡한 Query → 조회 결과
Table C ─┘
</code></pre></div></div>

<p>조회 요구사항이 복잡해질수록 Query의 복잡도가 증가하고 성능에도 영향을 줄 수 있다.</p>

<p><strong>정리</strong></p>

<p>전통적인 방식 자체가 문제가 되는 것은 아니다.</p>

<p>하지만 시스템이 복잡해지면서 읽기와 쓰기가 서로 다른 데이터 형태, 성능, 확장 방식과 비즈니스 로직을 요구하기 시작하면 하나의 모델이 너무 많은 책임을 담당하게 될 수 있다.</p>

<h2 id="2-command와-query의-분리">2 )<strong>Command와 Query의 분리</strong></h2>

<hr />

<p>CQRS에서는 이러한 문제를 해결하기 위해 <strong>읽기와 쓰기에 서로 다른 모델을 사용한다.</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                     Application
                          │
              ┌───────────┴───────────┐
              ↓                       ↓
        Command Model             Query Model
              ↓                       ↓
         데이터 변경                데이터 조회
</code></pre></div></div>

<h3 id="command"><strong>Command</strong></h3>

<p>Command는 시스템의 상태를 변경하기 위한 요청이다.</p>

<p>단순히 데이터를 중심으로 표현하기보다 <strong>어떤 작업을 수행할 것인지</strong>를 중심으로 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터 중심
→ Book 데이터 변경

작업 중심
→ 도서 등록
→ 도서 가격 변경
→ 도서 삭제
</code></pre></div></div>

<p>강의에서 다루는 Kafka 기반 구조처럼 Command를 Queue에 전달하여 비동기적으로 처리하는 방식도 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command
   ↓
Queue
   ↓
Command 처리
   ↓
데이터 변경
</code></pre></div></div>

<p>이를 통해 요청을 전달하는 부분과 실제 작업을 처리하는 부분을 분리할 수 있다.</p>

<h3 id="query"><strong>Query</strong></h3>

<p>Query는 데이터를 조회하기 위한 요청이다.</p>

<p>Query는 데이터베이스의 상태를 변경하지 않고 조회 결과를 반환한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Query
  ↓
Read Model
  ↓
DTO
  ↓
Client
</code></pre></div></div>

<p>조회에 필요한 형태에 맞는 DTO를 구성함으로써 쓰기 모델의 복잡한 비즈니스 로직과 조회 로직을 분리할 수 있다.</p>

<h3 id="저장소까지-분리하는-구조"><strong>저장소까지 분리하는 구조</strong></h3>

<p>CQRS의 핵심은 <strong>Command와 Query의 책임과 모델을 분리하는 것</strong>이다.</p>

<p>필요한 경우 여기서 더 나아가 읽기와 쓰기의 저장소 자체를 분리할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command
   ↓
Write Model
   ↓
Write Database


Query
   ↓
Read Model
   ↓
Read Database
</code></pre></div></div>

<p>이렇게 구성하면 각각의 작업에 적합한 데이터베이스와 데이터 구조를 선택할 수 있다.</p>

<p>다음 실습에서는 이 구조를 이용하여 쓰기 저장소와 읽기 저장소를 서로 다른 데이터베이스로 구성한다.</p>

<h2 id="3--cqrs의-장점">3 ) <strong>CQRS의 장점</strong></h2>

<hr />

<h3 id="읽기와-쓰기의-독립적인-확장"><strong>읽기와 쓰기의 독립적인 확장</strong></h3>

<p>읽기와 쓰기의 부하가 서로 다르다면 각 영역을 독립적으로 확장할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Read 요청 증가
     ↓
Read 영역 확장

Write 요청 증가
     ↓
Write 영역 확장
</code></pre></div></div>

<p>전체 시스템을 동일한 비율로 확장하지 않고 필요한 부분에 맞게 자원을 조정할 수 있다.</p>

<h3 id="작업에-최적화된-데이터-구조"><strong>작업에 최적화된 데이터 구조</strong></h3>

<p>읽기 저장소와 쓰기 저장소를 분리하면 각 작업에 적합한 Schema를 구성할 수 있다.</p>

<p><strong>Write Model</strong></p>

<p>데이터 변경과 비즈니스 로직 처리에 적합한 구조를 사용할 수 있다.</p>

<p><strong>Read Model</strong></p>

<p>조회에 적합한 구조를 구성하여 복잡한 Join을 줄일 수 있다.</p>

<p>예를 들어 읽기에 최적화된 Materialized View와 같은 형태를 이용하면 미리 구성된 데이터를 조회하여 복잡한 Query를 줄일 수 있다.</p>

<h3 id="관심사의-분리"><strong>관심사의 분리</strong></h3>

<p>복잡한 비즈니스 로직은 주로 쓰기 모델에서 처리하고 읽기 모델은 상대적으로 단순한 조회에 집중하도록 구성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command Model
→ Validation
→ Business Logic
→ 데이터 변경

Query Model
→ 데이터 조회
→ DTO 반환
</code></pre></div></div>

<p>각 모델의 책임이 명확해지므로 하나의 모델이 읽기와 쓰기의 모든 요구사항을 처리하는 문제를 줄일 수 있다.</p>

<h3 id="보안"><strong>보안</strong></h3>

<p>Command와 Query의 진입점과 책임이 분리되므로 데이터 변경 작업에 대해 별도의 권한이나 보안 정책을 적용하는 구조를 구성할 수 있다.</p>

<h2 id="4--cqrs-적용-시-고려할-문제">4 ) <strong>CQRS 적용 시 고려할 문제</strong></h2>

<hr />

<p>CQRS는 읽기와 쓰기를 분리함으로써 여러 장점을 얻을 수 있지만 시스템 구조 자체는 기존 CRUD 방식보다 복잡해질 수 있다.</p>

<h3 id="구조의-복잡성"><strong>구조의 복잡성</strong></h3>

<p>단순했던 하나의 모델을 Command와 Query로 분리해야 하므로 관리해야 하는 구성 요소가 증가한다.</p>

<p>특히 Event Sourcing과 같은 패턴까지 함께 적용하면 전체 구조의 복잡성이 더욱 증가할 수 있다.</p>

<p>따라서 CQRS가 항상 기존 CRUD 방식보다 좋은 것은 아니다.</p>

<h3 id="messaging-처리"><strong>Messaging 처리</strong></h3>

<p>Command 처리 이후 Event를 발행하여 다른 시스템의 데이터를 갱신하는 구조에서는 Messaging을 사용하게 될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command
   ↓
Write 처리
   ↓
Event 발행
   ↓
Message Broker
   ↓
Read Model 갱신
</code></pre></div></div>

<p>이 경우 애플리케이션에서는 정상적인 Message 처리뿐 아니라 <strong>Message 전송 실패나 중복 Message</strong>와 같은 상황도 고려해야 한다.</p>

<h3 id="데이터-일관성"><strong>데이터 일관성</strong></h3>

<p>읽기와 쓰기의 저장소를 분리하고 Message를 통해 데이터를 전달하면 두 저장소가 항상 같은 순간에 갱신되는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write Database 갱신
        ↓
    Event 전달
        ↓
Read Database 갱신
</code></pre></div></div>

<p>따라서 Write Database의 변경 사항이 Read Database에 반영되기 전까지 일시적으로 두 데이터의 상태가 다를 수 있다.</p>

<p>CQRS 구조를 설계할 때에는 이러한 데이터 일관성 문제를 함께 고려해야 한다.</p>

<h3 id="orm-사용의-제약"><strong>ORM 사용의 제약</strong></h3>

<p>강의에서는 CQRS 적용 시 읽기와 쓰기의 모델을 서로 다른 목적에 맞게 구성하므로, 단일 데이터베이스 Schema를 기준으로 ORM을 통해 전체 모델을 단순하게 자동 생성하는 방식과는 맞지 않을 수 있다는 점을 다루었다.</p>

<p>각 모델이 자신의 목적에 맞는 구조를 가지기 때문에 모델과 데이터 구조를 별도로 설계해야 할 수 있다.</p>

<h2 id="5--cqrs를-적용할-수-있는-경우">5 ) <strong>CQRS를 적용할 수 있는 경우</strong></h2>

<hr />

<p>CQRS는 시스템의 복잡성이 증가하는 대신 읽기와 쓰기를 독립적으로 구성할 수 있다는 장점을 얻는 패턴이다.</p>

<p>따라서 이러한 분리가 실제로 필요한 상황에서 사용하는 것이 적절하다.</p>

<p>다음과 같은 경우 적용을 고려할 수 있다.</p>

<ul>
  <li>많은 사용자가 동일한 데이터에 병렬로 접근하는 경우</li>
  <li>복잡한 프로세스나 Domain Model을 기반으로 작업을 처리하는 경우</li>
  <li>읽기 성능과 쓰기 성능을 서로 독립적으로 조정해야 하는 경우</li>
  <li>읽기와 쓰기를 명확하게 분리하여 구현하려는 경우</li>
  <li>하나의 시스템이 장기간 운영되면서 여러 Version을 가지며 지속적으로 변경되는 경우</li>
  <li>다른 시스템과의 통합이 필요한 경우</li>
</ul>

<p>반대로 다음과 같은 경우에는 CQRS로 인한 구조적 복잡성이 더 큰 부담이 될 수 있다.</p>

<ul>
  <li>Domain이나 Business Logic이 단순한 경우</li>
  <li>단순한 CRUD 중심의 애플리케이션</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>단순한 CRUD
→ 기존 구조가 적합할 수 있음

복잡한 Domain
+ 읽기/쓰기 요구사항 차이
+ 독립적인 확장 필요
→ CQRS 적용 고려
</code></pre></div></div>

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>CQRS (Command Query Responsibility Segregation)는 데이터 변경을 담당하는 Command와 데이터 조회를 담당하는 Query의 책임을 분리하는 패턴이다.</p>
    </li>
    <li>
      <p>전통적인 구조에서는 읽기와 쓰기가 하나의 데이터 모델을 공유하므로, 시스템이 복잡해질수록 하나의 모델이 조회와 변경에 필요한 서로 다른 요구사항을 모두 담당하게 될 수 있다.</p>
    </li>
    <li>
      <p>읽기와 쓰기는 요구되는 데이터 형태와 부하가 서로 다를 수 있으며, 동일한 데이터에 병렬로 접근할 경우 데이터 경합이 발생할 수도 있다.</p>
    </li>
    <li>
      <p>CQRS에서는 Command Model과 Query Model을 분리하여 각각의 책임에 집중하도록 구성한다.</p>
    </li>
    <li>
      <p>Command는 데이터 자체보다 수행할 작업을 중심으로 구성하며, Message Queue 등을 이용하여 비동기적으로 처리할 수 있다.</p>
    </li>
    <li>
      <p>Query는 데이터를 변경하지 않고 조회 결과를 DTO 등의 형태로 반환한다.</p>
    </li>
    <li>
      <p>Write Database와 Read Database를 분리하면 각각의 작업에 적합한 데이터 스키마와 저장 기술을 사용할 수 있다.</p>
    </li>
    <li>
      <p>읽기 저장소에서는 Materialized View 등을 활용하여 복잡한 조회를 단순화할 수 있으며, 읽기와 쓰기를 독립적으로 확장하고 최적화할 수 있다.</p>
    </li>
    <li>
      <p>CQRS는 성능과 확장성, 보안, 관심사 분리 측면에서 장점이 있지만 구조 자체의 복잡성이 증가한다.</p>
    </li>
    <li>
      <p>Messaging을 함께 사용하는 경우 Message 처리 실패, 중복 Message, 데이터 일관성 등의 문제를 고려해야 한다.</p>
    </li>
    <li>
      <p>CQRS는 복잡한 Domain이나 읽기와 쓰기의 요구사항이 크게 다른 시스템에서 고려할 수 있으며, 단순 CRUD 애플리케이션에서는 불필요한 복잡성을 만들 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><summary type="html"><![CDATA[CQRS의 개념과 기존 데이터 모델의 문제점, Command와 Query 분리 구조 및 적용 기준]]></summary></entry><entry><title type="html">Kafka를 활용한 CQRS 구현</title><link href="https://kkr010128.github.io/cloud-native-07-kafka-cqrs/" rel="alternate" type="text/html" title="Kafka를 활용한 CQRS 구현" /><published>2026-08-12T00:00:00+09:00</published><updated>2026-08-12T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-07-kafka-cqrs</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-07-kafka-cqrs/"><![CDATA[<h2 id="1--spring-boot-기반-cqrs-구현">1 ) Spring Boot 기반 CQRS 구현</h2>

<hr />

<p>CQRS(Command Query Responsibility Segregation)는 데이터를 변경하는 책임과 조회하는 책임을 분리하는 패턴이다.</p>

<p>이번 실습에서는 이 책임을 두 개의 Spring Boot 프로젝트로 나눠서 구현한다.</p>

<p>쓰기 프로젝트는 MySQL을 사용해서 데이터를 저장하고, 읽기 프로젝트는 MongoDB를 사용해서 데이터를 조회한다.</p>

<p>두 프로젝트는 서로 독립적으로 동작하지만, 나중에 Kafka를 연결해서 쓰기 프로젝트에서 발생한 변경 사항이 읽기 프로젝트로 전달되도록 확장한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command
   ↓
Write (MySQL)
   ↓
 Kafka
   ↓
Read (MongoDB)
   ↓
Query
</code></pre></div></div>

<h3 id="실습-준비">실습 준비</h3>

<p>실습을 시작하기 전에 다음을 준비한다.</p>

<table>
  <thead>
    <tr>
      <th>준비 항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>MySQL / MariaDB</td>
      <td>설치 후 접속 확인</td>
    </tr>
    <tr>
      <td>Kafka</td>
      <td>설치</td>
    </tr>
    <tr>
      <td>JDK</td>
      <td>설치</td>
    </tr>
    <tr>
      <td>IntelliJ IDEA</td>
      <td>설치</td>
    </tr>
  </tbody>
</table>

<h2 id="2--쓰기-프로젝트-write-project">2 ) <strong>쓰기 프로젝트 (Write Project)</strong></h2>

<hr />

<p>쓰기 프로젝트는 CQRS에서 Command 영역을 담당한다.</p>

<p>사용자로부터 도서 정보를 받아서 MySQL에 저장하는 역할을 한다.</p>

<h3 id="프로젝트-생성">프로젝트 생성</h3>

<p>다음 의존성을 포함해서 Spring Boot 프로젝트를 생성한다.</p>

<ul>
  <li>Spring Boot DevTools</li>
  <li>Lombok</li>
  <li>Spring Web</li>
  <li>Spring Data JPA</li>
  <li>MySQL</li>
  <li>Spring for Apache Kafka (Messaging)</li>
</ul>

<h3 id="applicationyml-설정">application.yml 설정</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>

<span class="na">spring</span><span class="pi">:</span>
  <span class="na">application</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">Write</span>

  <span class="na">datasource</span><span class="pi">:</span>
    <span class="na">url</span><span class="pi">:</span> <span class="s">jdbc:mysql://localhost:3306/cqrs</span> <span class="c1"># cqrs DB</span>
    <span class="na">driver-class-name</span><span class="pi">:</span> <span class="s">com.mysql.cj.jdbc.Driver</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">root</span>
    <span class="na">password</span><span class="pi">:</span> <span class="s">password</span>

  <span class="na">jpa</span><span class="pi">:</span>
    <span class="na">hibernate</span><span class="pi">:</span>
      <span class="na">ddl-auto</span><span class="pi">:</span> <span class="s">update</span> <span class="c1"># Entity 클래스를 기준으로 테이블이 자동 생성 및 수정됨</span>
    <span class="na">properties</span><span class="pi">:</span>
      <span class="na">hibernate</span><span class="pi">:</span>
        <span class="na">format_sql</span><span class="pi">:</span> <span class="no">true</span>
        <span class="na">show_sql</span><span class="pi">:</span> <span class="no">true</span>

<span class="na">logging</span><span class="pi">:</span>
  <span class="na">level</span><span class="pi">:</span>
    <span class="na">org.hibernate.type.description.sql</span><span class="pi">:</span> <span class="s">trace</span>
</code></pre></div></div>

<p>8080번 포트로 실행되고, MySQL의 <code class="language-plaintext highlighter-rouge">cqrs</code> 데이터베이스에 연결한다.</p>

<p><code class="language-plaintext highlighter-rouge">ddl-auto: update</code> 설정 때문에 Entity 클래스를 기준으로 테이블이 자동으로 생성되거나 수정된다.</p>

<h3 id="book-entity">Book Entity</h3>

<p><code class="language-plaintext highlighter-rouge">Book</code>은 MySQL의 <code class="language-plaintext highlighter-rouge">book</code> 테이블과 매핑되는 Entity 클래스다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">jakarta.persistence.*</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">lombok.*</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.util.Date</span><span class="o">;</span>

<span class="nd">@Entity</span>
<span class="nd">@Table</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"book"</span><span class="o">)</span>
<span class="nd">@ToString</span>
<span class="nd">@Getter</span>
<span class="nd">@Builder</span>
<span class="nd">@AllArgsConstructor</span>
<span class="nd">@NoArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Book</span> <span class="o">{</span>
    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">AUTO</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">bid</span><span class="o">;</span>

    <span class="nd">@Column</span><span class="o">(</span><span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">,</span> <span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">title</span><span class="o">;</span>
    <span class="nd">@Column</span><span class="o">(</span><span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">,</span> <span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">author</span><span class="o">;</span>
    <span class="nd">@Column</span><span class="o">(</span><span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">,</span> <span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">category</span><span class="o">;</span>
    <span class="nd">@Column</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">pages</span><span class="o">;</span>
    <span class="nd">@Column</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">price</span><span class="o">;</span>
    <span class="nd">@Column</span>
    <span class="kd">private</span> <span class="nc">Date</span> <span class="n">published_date</span><span class="o">;</span>
    <span class="nd">@Column</span><span class="o">(</span><span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">,</span> <span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">description</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">bid</code>는 <code class="language-plaintext highlighter-rouge">@GeneratedValue(strategy = GenerationType.AUTO)</code>로 설정되어 있어서 저장할 때 자동으로 생성된다.</p>

<h3 id="bookdto">BookDTO</h3>

<p>DTO는 계층 사이에서 데이터를 주고받기 위한 객체다.</p>

<p>클라이언트가 보낸 도서 정보를 <code class="language-plaintext highlighter-rouge">BookDTO</code>로 받는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.Data</span><span class="o">;</span>

<span class="nd">@Data</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookDTO</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">title</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">author</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">category</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">pages</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">price</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">published_date</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">description</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 시점에는 아직 <code class="language-plaintext highlighter-rouge">bid</code> 필드가 없다.</p>

<h3 id="bookrepository">BookRepository</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.data.jpa.repository.JpaRepository</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">BookRepository</span> <span class="kd">extends</span> <span class="nc">JpaRepository</span><span class="o">&lt;</span><span class="nc">Book</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">JpaRepository</code>를 상속받는 것만으로 기본적인 CRUD 기능을 사용할 수 있다.</p>

<h3 id="bookservice">BookService</h3>

<p><code class="language-plaintext highlighter-rouge">BookDTO</code>를 <code class="language-plaintext highlighter-rouge">Book</code> Entity로 변환해서 MySQL에 저장한다.</p>

<p><code class="language-plaintext highlighter-rouge">published_date</code>는 문자열로 들어오기 때문에 <code class="language-plaintext highlighter-rouge">SimpleDateFormat</code>으로 <code class="language-plaintext highlighter-rouge">Date</code> 타입으로 변환해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.stereotype.Service</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.text.SimpleDateFormat</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.Date</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.Locale</span><span class="o">;</span>

<span class="nd">@Service</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookService</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">BookRepository</span> <span class="n">bookRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">saveBook</span><span class="o">(</span><span class="nc">BookDTO</span> <span class="n">bookDTO</span><span class="o">){</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">SimpleDateFormat</span> <span class="n">formatter</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">SimpleDateFormat</span><span class="o">(</span>
                    <span class="s">"yyyy-MM-dd"</span><span class="o">,</span> <span class="nc">Locale</span><span class="o">.</span><span class="na">ENGLISH</span><span class="o">);</span>
            <span class="nc">Date</span> <span class="n">published_date</span> <span class="o">=</span> <span class="n">formatter</span><span class="o">.</span><span class="na">parse</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPublished_date</span><span class="o">());</span>
            <span class="nc">Book</span> <span class="n">book</span> <span class="o">=</span> <span class="nc">Book</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
                    <span class="o">.</span><span class="na">title</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getTitle</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">author</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getAuthor</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">category</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getCategory</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">pages</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPages</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">price</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPrice</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">published_date</span><span class="o">(</span><span class="n">published_date</span><span class="o">)</span>
                    <span class="o">.</span><span class="na">description</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getDescription</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">build</span><span class="o">();</span>
            <span class="n">bookRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">book</span><span class="o">);</span>
        <span class="o">}</span>
        <span class="k">catch</span><span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">){</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="bookcontroller">BookController</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.GetMapping</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.PostMapping</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RequestBody</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RestController</span><span class="o">;</span>

<span class="nd">@RestController</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookController</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">BookService</span> <span class="n">bookService</span><span class="o">;</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">index</span><span class="o">(){</span>
        <span class="k">return</span> <span class="s">"homepage"</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/health"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">healthCheck</span><span class="o">(){</span>
        <span class="k">return</span> <span class="s">"success"</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/cqrs/book"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">saveBook</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">BookDTO</span> <span class="n">bookDTO</span><span class="o">){</span>
        <span class="n">bookService</span><span class="o">.</span><span class="na">saveBook</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">);</span>
        <span class="k">return</span> <span class="s">"success"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Endpoint</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>GET</td>
      <td><code class="language-plaintext highlighter-rouge">/</code></td>
      <td>서버 동작 확인</td>
    </tr>
    <tr>
      <td>GET</td>
      <td><code class="language-plaintext highlighter-rouge">/health</code></td>
      <td>Health Check</td>
    </tr>
    <tr>
      <td>POST</td>
      <td><code class="language-plaintext highlighter-rouge">/cqrs/book</code></td>
      <td>도서 데이터 저장</td>
    </tr>
  </tbody>
</table>

<p>서버를 실행하고 <code class="language-plaintext highlighter-rouge">localhost:8080</code>으로 접속해서 동작을 확인한다.</p>

<p>데이터 저장이 잘 되는지는 POSTMAN으로 테스트한다. (4번에서 자세히 다룬다.)</p>

<h2 id="3--읽기-프로젝트-read-project">3 ) <strong>읽기 프로젝트 (Read Project)</strong></h2>

<hr />

<p>읽기 프로젝트는 CQRS에서 Query 영역을 담당한다.</p>

<p>쓰기 프로젝트가 MySQL을 사용한 것과 다르게, 읽기 프로젝트는 MongoDB를 사용한다.</p>

<h3 id="프로젝트-생성-1">프로젝트 생성</h3>

<p>다음 의존성을 포함해서 Spring Boot 프로젝트를 생성한다.</p>

<ul>
  <li>Spring Boot DevTools</li>
  <li>Lombok</li>
  <li>Spring Web</li>
  <li>Spring Data MongoDB</li>
  <li>Spring for Apache Kafka (Messaging)</li>
</ul>

<p>읽기 프로젝트는 MongoDB만 사용하므로 <strong>Spring Data JPA는 추가하지 않는다.</strong> JPA를 추가하면 사용하지 않는 DataSource의 자동 설정이 동작하여 MySQL 접속 정보가 없을 때 애플리케이션 시작 오류가 발생할 수 있다.</p>

<h3 id="applicationyml-설정-1">application.yml 설정</h3>

<p>Reader의 <code class="language-plaintext highlighter-rouge">BookController</code>는 MongoDB 주소를 코드에서 직접 지정하므로 초기 조회 단계에서는 MongoDB 접속 설정이 없어도 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">MongoClients</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"mongodb://localhost:27017"</span><span class="o">);</span>
</code></pre></div></div>

<p>그러나 Write와 Reader의 기본 포트는 모두 8080이므로 두 프로젝트를 동시에 실행하면 포트가 충돌한다. Reader에는 최소한 다음 설정을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">port</span><span class="pi">:</span> <span class="m">8081</span>

<span class="na">spring</span><span class="pi">:</span>
  <span class="na">application</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">Reader</span>
</code></pre></div></div>

<p>따라서 Write는 8080번 포트, Reader는 8081번 포트에서 실행한다.</p>

<h3 id="bookcontroller-1">BookController</h3>

<p><code class="language-plaintext highlighter-rouge">/cqrs/book</code>으로 요청이 들어오면 MongoDB의 <code class="language-plaintext highlighter-rouge">books</code> Collection을 조회해서 반환한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">com.mongodb.client.*</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.bson.Document</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.http.HttpStatus</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.http.ResponseEntity</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.GetMapping</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RestController</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.ArrayList</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.List</span><span class="o">;</span>

<span class="nd">@RestController</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookController</span> <span class="o">{</span>
    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/cqrs/book"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">List</span><span class="o">&gt;</span> <span class="nf">getBooks</span><span class="o">(){</span>
        <span class="nc">MongoClient</span> <span class="n">mongoClient</span> <span class="o">=</span> <span class="nc">MongoClients</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"mongodb://localhost:27017"</span><span class="o">);</span>
        <span class="nc">MongoDatabase</span> <span class="n">database</span> <span class="o">=</span> <span class="n">mongoClient</span><span class="o">.</span><span class="na">getDatabase</span><span class="o">(</span><span class="s">"cqrs"</span><span class="o">);</span>
        <span class="nc">MongoCollection</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">mongo_books</span> <span class="o">=</span>
                <span class="n">database</span><span class="o">.</span><span class="na">getCollection</span><span class="o">(</span><span class="s">"books"</span><span class="o">);</span>
        <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;();</span>

        <span class="k">try</span><span class="o">{</span>
            <span class="k">try</span><span class="o">(</span><span class="nc">MongoCursor</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">mongo_books</span><span class="o">.</span><span class="na">find</span><span class="o">().</span><span class="na">iterator</span><span class="o">()){</span>
                <span class="k">while</span><span class="o">(</span><span class="n">cur</span><span class="o">.</span><span class="na">hasNext</span><span class="o">()){</span>
                    <span class="nc">Document</span> <span class="n">doc</span> <span class="o">=</span> <span class="n">cur</span><span class="o">.</span><span class="na">next</span><span class="o">();</span>
                    <span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">doc</span><span class="o">);</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span><span class="k">catch</span><span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">){</span>
            <span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
        <span class="o">}</span>
        <span class="k">finally</span><span class="o">{</span>
            <span class="n">mongoClient</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
        <span class="o">}</span>
        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">status</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">OK</span><span class="o">).</span><span class="na">body</span><span class="o">(</span><span class="n">list</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">MongoClients.create()</code>로 MongoDB에 연결하고 <code class="language-plaintext highlighter-rouge">cqrs</code> 데이터베이스의 <code class="language-plaintext highlighter-rouge">books</code> Collection을 가져온다.</p>

<p><code class="language-plaintext highlighter-rouge">find()</code>로 전체 Document를 조회하고, <code class="language-plaintext highlighter-rouge">MongoCursor</code>를 순회하면서 <code class="language-plaintext highlighter-rouge">List</code>에 담는다.</p>

<p>조회가 끝나면 <code class="language-plaintext highlighter-rouge">mongoClient.close()</code>로 연결을 닫고 결과를 응답으로 반환한다.</p>

<p>MongoDB를 실행한 뒤 Reader 프로젝트를 실행하고 다음 주소에서 정상적으로 조회되는지 확인한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://localhost:8081/cqrs/book
</code></pre></div></div>

<p>이 단계에서는 Kafka Consumer를 아직 작성하지 않았으므로 <code class="language-plaintext highlighter-rouge">spring.kafka</code> 설정이 없어도 된다.</p>

<p><strong>중간 정리</strong></p>

<ul>
  <li>쓰기 프로젝트는 MySQL에 도서 데이터를 저장하는 Command 영역을 담당한다.</li>
  <li>읽기 프로젝트는 MongoDB에서 도서 데이터를 조회하는 Query 영역을 담당한다.</li>
  <li>현재까지는 두 프로젝트가 완전히 분리되어 있어서, 쓰기 프로젝트에 저장된 데이터가 읽기 프로젝트로 자동으로 전달되지 않는다.</li>
  <li>이 문제는 이후 Kafka를 연결해서 해결한다.</li>
</ul>

<h2 id="4--쓰기-프로젝트-데이터-삽입-테스트-postman">4 ) <strong>쓰기 프로젝트 데이터 삽입 테스트 (POSTMAN)</strong></h2>

<hr />

<p>쓰기 프로젝트가 MySQL에 정상적으로 데이터를 저장하는지 POSTMAN으로 확인한다.</p>

<h3 id="요청-설정">요청 설정</h3>

<ul>
  <li>Method: <code class="language-plaintext highlighter-rouge">POST</code></li>
  <li>URL: <code class="language-plaintext highlighter-rouge">http://localhost:8080/cqrs/book</code></li>
  <li>Headers: <code class="language-plaintext highlighter-rouge">Content-Type: application/json</code></li>
  <li>Body: <code class="language-plaintext highlighter-rouge">raw</code> / <code class="language-plaintext highlighter-rouge">JSON</code></li>
</ul>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"오디세이"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"author"</span><span class="p">:</span><span class="w"> </span><span class="s2">"호메로스"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"category"</span><span class="p">:</span><span class="w"> </span><span class="s2">"신화"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"크리스토퍼 놀란"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"pages"</span><span class="p">:</span><span class="w"> </span><span class="mi">356</span><span class="p">,</span><span class="w">
    </span><span class="nl">"price"</span><span class="p">:</span><span class="w"> </span><span class="mi">15000</span><span class="p">,</span><span class="w">
    </span><span class="nl">"published_date"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-08-13"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Send</code>를 눌러서 요청을 전송한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>POSTMAN
   ↓
BookController
   ↓
BookService
   ↓
BookRepository
   ↓
MySQL
</code></pre></div></div>

<p>요청이 성공하면 MySQL의 <code class="language-plaintext highlighter-rouge">book</code> 테이블에 데이터가 저장된다.</p>

<p>데이터베이스를 직접 확인해서 저장 여부를 검증한다.</p>

<h2 id="5--kafka를-이용한-cqrs-연결">5 ) <strong>Kafka를 이용한 CQRS 연결</strong></h2>

<hr />

<p>쓰기 프로젝트와 읽기 프로젝트는 각각 MySQL, MongoDB를 사용하지만 서로 분리되어 있다.</p>

<p>그래서 쓰기 프로젝트에서 데이터가 바뀌어도 읽기 프로젝트는 그 사실을 알 수 없다.</p>

<p>이 문제를 해결하기 위해 두 프로젝트 사이에 Kafka를 연결한다.</p>

<p>쓰기 프로젝트가 MySQL 저장을 완료하면 Kafka로 Message를 발행하고, 읽기 프로젝트가 그 Message를 받아서 MongoDB에 반영하는 구조다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>POSTMAN
   ↓
Write Project → MySQL
   ↓
Kafka Producer → cqrs-topic → Kafka Consumer
   ↓
Read Project → MongoDB
   ↓
POSTMAN
</code></pre></div></div>

<h3 id="쓰기-프로젝트에-kafka-연결">쓰기 프로젝트에 Kafka 연결</h3>

<h4 id="라이브러리-추가">라이브러리 추가</h4>

<p>라이브러리를 프로젝트 생성 당시 추가하지 않았다면 <code class="language-plaintext highlighter-rouge">build.gradle</code>의 <code class="language-plaintext highlighter-rouge">dependencies</code>에 다음을 추가한다.</p>

<div class="language-gradle highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">implementation</span> <span class="s1">'org.springframework.boot:spring-boot-starter-kafka'</span>
</code></pre></div></div>

<p>추가한 뒤에는 IntelliJ에서 코끼리 아이콘을 눌러서 Gradle을 다시 Build한다.</p>

<h4 id="kafka-설정">Kafka 설정</h4>

<p><code class="language-plaintext highlighter-rouge">application.yaml</code>에 Kafka 연결 정보를 추가한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spring</span><span class="pi">:</span>
  <span class="na">kafka</span><span class="pi">:</span>
    <span class="na">bootstrap-servers</span><span class="pi">:</span> <span class="s">127.0.0.1:9092</span>
    <span class="na">consumer</span><span class="pi">:</span>
      <span class="na">group-id</span><span class="pi">:</span> <span class="s">hyeon</span>
      <span class="na">auto-offset-reset</span><span class="pi">:</span> <span class="s">earliest</span>
      <span class="na">key-deserializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringDeserializer</span>
      <span class="na">value-deserializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringDeserializer</span>
    <span class="na">producer</span><span class="pi">:</span>
      <span class="na">key-serializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringSerializer</span>
      <span class="na">value-serializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringSerializer</span>
</code></pre></div></div>

<p>Message를 문자열로 처리하기 위해 <code class="language-plaintext highlighter-rouge">StringSerializer</code>, <code class="language-plaintext highlighter-rouge">StringDeserializer</code>를 사용한다.</p>

<blockquote>
  <p>Kafka의 동작 흐름만 확인할 목적으로 실습을 진행하므로, Docker 컨테이너 하나에 브로커를 하나만 올린 상태이다. (고가용성 없음)</p>

  <p>실제 서비스에서는 브로커가 여러 개일 경우 <code class="language-plaintext highlighter-rouge">bootstrap-servers</code> 부분에 IP를 <code class="language-plaintext highlighter-rouge">,</code>로 구분하여 나열한다.</p>

  <p>이럴 경우 Producer/Consumer가 처음 연결할 때 이 목록 중 하나에 붙어 전체 클러스터 정보를 받아오고, 그다음부터는 어느 브로커가 Leader인지 알게 되는 구조다.</p>
</blockquote>

<h4 id="kafkaconfiguration">KafkaConfiguration</h4>

<p><code class="language-plaintext highlighter-rouge">KafkaTemplate</code>을 생성하기 위한 설정 클래스를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.apache.kafka.clients.producer.ProducerConfig</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.beans.factory.annotation.Value</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.context.annotation.Bean</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.context.annotation.Configuration</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.DefaultKafkaProducerFactory</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.KafkaTemplate</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.ProducerFactory</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.apache.kafka.common.serialization.StringSerializer</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.util.HashMap</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.Map</span><span class="o">;</span>

<span class="nd">@Configuration</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">KafkaConfiguration</span> <span class="o">{</span>
    <span class="nd">@Value</span><span class="o">(</span><span class="s">"${spring.kafka.bootstrap-servers}"</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">bootStrapServers</span><span class="o">;</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">ProducerFactory</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="nf">producerFactory</span><span class="o">(){</span>
        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">configs</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">BOOTSTRAP_SERVERS_CONFIG</span><span class="o">,</span> <span class="n">bootStrapServers</span><span class="o">);</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">KEY_SERIALIZER_CLASS_CONFIG</span><span class="o">,</span> <span class="nc">StringSerializer</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">VALUE_SERIALIZER_CLASS_CONFIG</span><span class="o">,</span> <span class="nc">StringSerializer</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">DefaultKafkaProducerFactory</span><span class="o">&lt;&gt;(</span><span class="n">configs</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">KafkaTemplate</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="nf">kafkaTemplate</span><span class="o">(){</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">KafkaTemplate</span><span class="o">&lt;&gt;(</span><span class="n">producerFactory</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ProducerFactory</code>는 Kafka Producer를 만들기 위한 설정이고, <code class="language-plaintext highlighter-rouge">KafkaTemplate</code>은 실제로 Topic에 Message를 보낼 때 사용한다.</p>

<h4 id="bookdto-수정">BookDTO 수정</h4>

<p><code class="language-plaintext highlighter-rouge">bid</code> 필드를 추가한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookDTO</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">bid</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">title</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">author</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">category</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">pages</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">price</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">published_date</span><span class="o">;</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">description</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>클라이언트가 <code class="language-plaintext highlighter-rouge">bid</code>를 직접 보내는 게 아니라, MySQL에 저장한 뒤 생성된 <code class="language-plaintext highlighter-rouge">bid</code>를 여기에 다시 담아서 Kafka Message에 포함시킨다.</p>

<h4 id="kafkaproducer">KafkaProducer</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.slf4j.Logger</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.slf4j.LoggerFactory</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.beans.factory.annotation.Autowired</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.KafkaTemplate</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.stereotype.Service</span><span class="o">;</span>

<span class="nd">@Service</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">KafkaProducer</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">String</span> <span class="no">TOPIC</span> <span class="o">=</span> <span class="s">"cqrs-topic"</span><span class="o">;</span>

    <span class="nd">@Autowired</span>
    <span class="kd">private</span> <span class="nc">KafkaTemplate</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="n">kafkaTemplate</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Logger</span> <span class="n">log</span> <span class="o">=</span> <span class="nc">LoggerFactory</span><span class="o">.</span><span class="na">getLogger</span><span class="o">(</span><span class="n">getClass</span><span class="o">());</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">sendMessage</span><span class="o">(</span><span class="nc">BookDTO</span> <span class="n">bookDTO</span><span class="o">){</span>
        <span class="nc">String</span> <span class="n">message</span> <span class="o">=</span> <span class="s">"{\"bid\":"</span> <span class="o">+</span> <span class="s">"\""</span> <span class="o">+</span> <span class="n">bookDTO</span><span class="o">.</span><span class="na">getBid</span><span class="o">()</span> <span class="o">+</span> <span class="s">"\"}"</span><span class="o">;</span>
        <span class="c1">//메시지 전송</span>
        <span class="k">this</span><span class="o">.</span><span class="na">kafkaTemplate</span><span class="o">.</span><span class="na">send</span><span class="o">(</span><span class="no">TOPIC</span><span class="o">,</span> <span class="n">message</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">bid</code>를 <code class="language-plaintext highlighter-rouge">{"bid":"1"}</code> 형태의 JSON 문자열로 만들어서 <code class="language-plaintext highlighter-rouge">cqrs-topic</code>으로 전송한다.</p>

<p>여기서는 <code class="language-plaintext highlighter-rouge">bid</code>만 보낸다. 제목이나 저자 같은 나머지 정보는 Message에 포함되지 않는다.</p>

<h4 id="bookservice-수정">BookService 수정</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">BookService</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">BookRepository</span> <span class="n">bookRepository</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">KafkaProducer</span> <span class="n">kafkaProducer</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">saveBook</span><span class="o">(</span><span class="nc">BookDTO</span> <span class="n">bookDTO</span><span class="o">){</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">SimpleDateFormat</span> <span class="n">formatter</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">SimpleDateFormat</span><span class="o">(</span>
                    <span class="s">"yyyy-MM-dd"</span><span class="o">,</span> <span class="nc">Locale</span><span class="o">.</span><span class="na">ENGLISH</span><span class="o">);</span>
            <span class="nc">Date</span> <span class="n">published_date</span> <span class="o">=</span> <span class="n">formatter</span><span class="o">.</span><span class="na">parse</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPublished_date</span><span class="o">());</span>
            <span class="nc">Book</span> <span class="n">book</span> <span class="o">=</span> <span class="nc">Book</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
                    <span class="o">.</span><span class="na">title</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getTitle</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">author</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getAuthor</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">category</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getCategory</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">pages</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPages</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">price</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getPrice</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">published_date</span><span class="o">(</span><span class="n">published_date</span><span class="o">)</span>
                    <span class="o">.</span><span class="na">description</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">.</span><span class="na">getDescription</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">build</span><span class="o">();</span>
            <span class="n">bookRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">book</span><span class="o">);</span>
            <span class="n">bookDTO</span><span class="o">.</span><span class="na">setBid</span><span class="o">(</span><span class="n">book</span><span class="o">.</span><span class="na">getBid</span><span class="o">());</span>
            <span class="c1">//쓰기 작업을 완료할 때 카프카에게 메시지를 전송</span>
            <span class="n">kafkaProducer</span><span class="o">.</span><span class="na">sendMessage</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">);</span> 
        <span class="o">}</span>
        <span class="k">catch</span><span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">){</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>MySQL 저장 이후 코드 3줄이 Kafka 연결의 핵심이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">bookRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">book</span><span class="o">);</span>
<span class="n">bookDTO</span><span class="o">.</span><span class="na">setBid</span><span class="o">(</span><span class="n">book</span><span class="o">.</span><span class="na">getBid</span><span class="o">());</span>
<span class="c1">//쓰기 작업을 완료할 때 카프카에게 메시지를 전송</span>
<span class="n">kafkaProducer</span><span class="o">.</span><span class="na">sendMessage</span><span class="o">(</span><span class="n">bookDTO</span><span class="o">);</span>
</code></pre></div></div>

<p>저장하고, 생성된 <code class="language-plaintext highlighter-rouge">bid</code>를 받아오고, 그 <code class="language-plaintext highlighter-rouge">bid</code>를 Kafka로 보낸다.</p>

<h4 id="cqrs-topic-생성-및-확인">cqrs-topic 생성 및 확인</h4>

<p>Kafka 컨테이너에 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec</span> <span class="nt">-it</span> kafka /bin/bash
</code></pre></div></div>

<p>Topic을 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-topics.sh <span class="nt">--create</span> <span class="nt">--zookeeper</span> zookeeper:2181 <span class="nt">--replication-factor</span> 1 <span class="nt">--partitions</span> 1 <span class="nt">--topic</span> cqrs-topic
</code></pre></div></div>

<p>Console Consumer로 Message를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kafka-console-consumer.sh <span class="nt">--topic</span> cqrs-topic <span class="nt">--bootstrap-server</span> localhost:9092 <span class="nt">--from-beginning</span>
</code></pre></div></div>

<p>POSTMAN으로 책을 저장하면 Console Consumer에 다음과 같은 Message가 찍힌다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="nl">"bid"</span><span class="p">:</span><span class="s2">"1"</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>
<p><img src="../../assets/post/2026-08-12-cloud-native-07-kafka-cqrs/02.webp" alt="" />
이 Message가 보이면 쓰기 프로젝트에서 Kafka까지 연결이 정상적으로 동작하는 것이다.</p>

<h3 id="읽기-프로젝트에-kafka-연결">읽기 프로젝트에 Kafka 연결</h3>

<h4 id="라이브러리-추가-1">라이브러리 추가</h4>

<p>Kafka에서 받은 JSON 문자열을 처리하기 위해 <code class="language-plaintext highlighter-rouge">build.gradle</code>에 추가한다.</p>

<div class="language-gradle highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">implementation</span> <span class="s1">'org.json:json:20190722'</span>
</code></pre></div></div>

<h4 id="kafka-설정-1">Kafka 설정</h4>

<p>Reader의 기존 <code class="language-plaintext highlighter-rouge">application.yml</code>에 Kafka Consumer 설정을 추가한다. <code class="language-plaintext highlighter-rouge">server</code>와 <code class="language-plaintext highlighter-rouge">spring.application</code>을 유지한 상태에서 <code class="language-plaintext highlighter-rouge">spring.kafka</code>를 같은 <code class="language-plaintext highlighter-rouge">spring</code> 아래에 병합한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">port</span><span class="pi">:</span> <span class="m">8081</span>

<span class="na">spring</span><span class="pi">:</span>
  <span class="na">application</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">Reader</span>

  <span class="na">kafka</span><span class="pi">:</span>
    <span class="na">bootstrap-servers</span><span class="pi">:</span> <span class="s">127.0.0.1:9092</span>
    <span class="na">consumer</span><span class="pi">:</span>
      <span class="na">group-id</span><span class="pi">:</span> <span class="s">hyeon</span>
      <span class="na">auto-offset-reset</span><span class="pi">:</span> <span class="s">earliest</span>
      <span class="na">key-deserializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringDeserializer</span>
      <span class="na">value-deserializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringDeserializer</span>
    <span class="na">producer</span><span class="pi">:</span>
      <span class="na">key-serializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringSerializer</span>
      <span class="na">value-serializer</span><span class="pi">:</span> <span class="s">org.apache.kafka.common.serialization.StringSerializer</span>
</code></pre></div></div>

<p>Reader 컨트롤러만 확인하는 단계에는 위 Kafka 설정이 필요하지 않지만, <code class="language-plaintext highlighter-rouge">@KafkaListener</code>를 작성하여 Consumer를 실행할 때부터 필요하다.</p>

<h4 id="kafkaconfiguration-1">KafkaConfiguration</h4>

<p>읽기 프로젝트에도 쓰기 프로젝트와 동일한 <code class="language-plaintext highlighter-rouge">KafkaConfiguration</code> 클래스를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.apache.kafka.clients.producer.ProducerConfig</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.beans.factory.annotation.Value</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.context.annotation.Bean</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.context.annotation.Configuration</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.DefaultKafkaProducerFactory</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.KafkaTemplate</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.core.ProducerFactory</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.apache.kafka.common.serialization.StringSerializer</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.util.HashMap</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.Map</span><span class="o">;</span>

<span class="nd">@Configuration</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">KafkaConfiguration</span> <span class="o">{</span>
    <span class="nd">@Value</span><span class="o">(</span><span class="s">"${spring.kafka.bootstrap-servers}"</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">bootStrapServers</span><span class="o">;</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">ProducerFactory</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="nf">producerFactory</span><span class="o">(){</span>
        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">configs</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">BOOTSTRAP_SERVERS_CONFIG</span><span class="o">,</span> <span class="n">bootStrapServers</span><span class="o">);</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">KEY_SERIALIZER_CLASS_CONFIG</span><span class="o">,</span> <span class="nc">StringSerializer</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">configs</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="nc">ProducerConfig</span><span class="o">.</span><span class="na">VALUE_SERIALIZER_CLASS_CONFIG</span><span class="o">,</span> <span class="nc">StringSerializer</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">DefaultKafkaProducerFactory</span><span class="o">&lt;&gt;(</span><span class="n">configs</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">KafkaTemplate</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="nf">kafkaTemplate</span><span class="o">(){</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">KafkaTemplate</span><span class="o">&lt;&gt;(</span><span class="n">producerFactory</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="kafkaconsumer">KafkaConsumer</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">com.mongodb.client.MongoClient</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">com.mongodb.client.MongoClients</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">com.mongodb.client.MongoCollection</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">com.mongodb.client.MongoDatabase</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.bson.Document</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.json.JSONObject</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.kafka.annotation.KafkaListener</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.stereotype.Service</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.io.IOException</span><span class="o">;</span>

<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">KafkaConsumer</span> <span class="o">{</span>
    <span class="nd">@KafkaListener</span><span class="o">(</span><span class="n">topics</span><span class="o">=</span><span class="s">"cqrs-topic"</span><span class="o">,</span> <span class="n">groupId</span> <span class="o">=</span> <span class="s">"hyeon"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">consumer</span><span class="o">(</span><span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">IOException</span><span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"message:"</span> <span class="o">+</span> <span class="n">message</span><span class="o">);</span>
        <span class="c1">//JSON 파싱: JSON문자열로 온 것을 자바 객체로 변환</span>
        <span class="nc">JSONObject</span> <span class="n">messageObj</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">JSONObject</span><span class="o">(</span><span class="n">message</span><span class="o">);</span>
		
		<span class="c1">//JSON 파싱: JSON문자열로 온 것을 자바 객체로 변환</span>
        <span class="nc">MongoClient</span> <span class="n">mongoClient</span> <span class="o">=</span> <span class="nc">MongoClients</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"mongodb://localhost:27017"</span><span class="o">);</span>
        <span class="nc">MongoDatabase</span> <span class="n">database</span> <span class="o">=</span> <span class="n">mongoClient</span><span class="o">.</span><span class="na">getDatabase</span><span class="o">(</span><span class="s">"cqrs"</span><span class="o">);</span>
        <span class="nc">MongoCollection</span><span class="o">&lt;</span><span class="nc">Document</span><span class="o">&gt;</span> <span class="n">mongo_books</span> <span class="o">=</span>
                <span class="n">database</span><span class="o">.</span><span class="na">getCollection</span><span class="o">(</span><span class="s">"books"</span><span class="o">);</span>
		<span class="c1">//받은 데이터로 삽입할 데이터를 생성</span>
        <span class="nc">Document</span> <span class="n">book</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Document</span><span class="o">();</span>
        <span class="n">book</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"bid"</span><span class="o">,</span> <span class="n">messageObj</span><span class="o">.</span><span class="na">getLong</span><span class="o">(</span><span class="s">"bid"</span><span class="o">));</span>
        <span class="n">mongo_books</span><span class="o">.</span><span class="na">insertOne</span><span class="o">(</span><span class="n">book</span><span class="o">);</span>
        <span class="n">mongoClient</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@KafkaListener</code>로 <code class="language-plaintext highlighter-rouge">cqrs-topic</code>을 구독하고 있다가 Message가 오면 동작한다.</p>

<p>받은 문자열을 <code class="language-plaintext highlighter-rouge">JSONObject</code>로 파싱해서 <code class="language-plaintext highlighter-rouge">bid</code>를 꺼내고, MongoDB의 <code class="language-plaintext highlighter-rouge">cqrs</code> 데이터베이스, <code class="language-plaintext highlighter-rouge">books</code> Collection에 저장한다.</p>

<h3 id="전체-구조-및-실행">전체 구조 및 실행</h3>

<pre><code class="language-mermaid">flowchart LR
    A[Client] --&gt; B[Write Project]
    B --&gt; C[(MySQL)]
    B --&gt; D[Kafka]
    D --&gt; E[Read Project]
    E --&gt; F[(MongoDB)]
</code></pre>

<p>두 프로젝트를 모두 실행한 상태에서 POSTMAN으로 쓰기 프로젝트에 데이터를 보내면 다음 순서로 처리된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>POSTMAN
   ↓
BookController (Write)
   ↓
BookService
   ↓
MySQL 저장 → bid 생성
   ↓
KafkaProducer → cqrs-topic
   ↓
KafkaConsumer (Read)
   ↓
MongoDB 저장
</code></pre></div></div>

<p>읽기 프로젝트의 <code class="language-plaintext highlighter-rouge">GET /cqrs/book</code>으로 조회하면 방금 저장된 <code class="language-plaintext highlighter-rouge">bid</code>가 MongoDB에 들어있는 것을 확인할 수 있다.
<img src="../../assets/post/2026-08-12-cloud-native-07-kafka-cqrs/04.webp" alt="" /></p>

<h3 id="실습-메모">실습 메모</h3>

<p>지금 구조는 CQRS를 구현해보는 실습 구조로, Kafka로 전달되는 정보는 <code class="language-plaintext highlighter-rouge">bid</code> 하나뿐이다.</p>

<p>읽기 프로젝트가 나중에 도서의 상세 정보를 조회하려면, <code class="language-plaintext highlighter-rouge">KafkaProducer</code>에서 <code class="language-plaintext highlighter-rouge">bid</code>뿐만 아니라 도서 정보 전체를 JSON으로 보내도록 수정해야 한다.</p>

<hr />

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>이번 실습은 CQRS 패턴에 따라 쓰기와 읽기 책임을 두 개의 Spring Boot 프로젝트로 분리하는 것이다. 쓰기 프로젝트는 MySQL에, 읽기 프로젝트는 MongoDB에 도서 데이터를 저장하거나 조회한다.</p>
    </li>
    <li>
      <p>쓰기 프로젝트는 <code class="language-plaintext highlighter-rouge">BookController → BookService → BookRepository</code> 순서로 요청을 처리해서 MySQL에 <code class="language-plaintext highlighter-rouge">Book</code> Entity를 저장한다.</p>
    </li>
    <li>
      <p>읽기 프로젝트는 <code class="language-plaintext highlighter-rouge">BookController</code>에서 <code class="language-plaintext highlighter-rouge">MongoClient</code>로 MongoDB에 직접 연결해서 <code class="language-plaintext highlighter-rouge">books</code> Collection을 조회한다.</p>
    </li>
    <li>
      <p>두 프로젝트가 분리되어 있어서 쓰기 프로젝트의 변경 사항이 읽기 프로젝트에 자동으로 전달되지 않는데, 이 문제를 Kafka로 해결한다.</p>
    </li>
    <li>
      <p>쓰기 프로젝트는 MySQL 저장 후 생성된 <code class="language-plaintext highlighter-rouge">bid</code>를 <code class="language-plaintext highlighter-rouge">KafkaProducer</code>를 통해 <code class="language-plaintext highlighter-rouge">cqrs-topic</code>으로 전송한다.</p>
    </li>
    <li>
      <p>읽기 프로젝트는 <code class="language-plaintext highlighter-rouge">@KafkaListener</code>로 <code class="language-plaintext highlighter-rouge">cqrs-topic</code>을 구독하고, 받은 <code class="language-plaintext highlighter-rouge">bid</code>를 MongoDB의 <code class="language-plaintext highlighter-rouge">books</code> Collection에 저장한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kafka" /><summary type="html"><![CDATA[Spring Boot, MySQL, MongoDB와 Kafka를 이용하여 쓰기와 읽기 모델을 분리한 CQRS 구조 구현]]></summary></entry><entry><title type="html">클라우드 네이티브의 핵심 요소 CI/CD와 Container</title><link href="https://kkr010128.github.io/cloud-native-02-pillars/" rel="alternate" type="text/html" title="클라우드 네이티브의 핵심 요소 CI/CD와 Container" /><published>2026-08-11T00:00:00+09:00</published><updated>2026-08-11T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-02-pillars</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-02-pillars/"><![CDATA[<h2 id="pillars-of-cloud-native">Pillars of Cloud Native</h2>

<hr />

<h3 id="cicd">CI/CD</h3>

<h4 id="개요">개요</h4>

<p>CI/CD는 애플리케이션 개발 단계를 자동화하여 애플리케이션을 더 짧은 주기로 고객에게 제공하기 위한 방법이다.</p>

<p>새로운 코드를 기존 코드에 통합하는 과정에서 개발 및 운영에 발생할 수 있는 <strong>Integration Hell</strong> 문제를 줄이는 데 목적이 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CI (Continuous Integration)</td>
      <td>지속적 통합</td>
    </tr>
    <tr>
      <td>CD (Continuous Delivery)</td>
      <td>지속적 제공</td>
    </tr>
    <tr>
      <td>CD (Continuous Deployment)</td>
      <td>지속적 배포</td>
    </tr>
  </tbody>
</table>

<h4 id="흐름">흐름</h4>

<p>개발자가 코드를 작성한 뒤 저장소에 Push 또는 Upload하면 이후 Build, Test, Merge 등의 과정이 CI 파이프라인에서 수행된다.</p>

<table>
  <thead>
    <tr>
      <th>Development</th>
      <th>CI</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Coding → Push / Upload</td>
      <td>Build → Test → Merge</td>
    </tr>
    <tr>
      <td>Coding은 수동</td>
      <td>대부분 자동화되며 일부 과정은 Manual일 수 있음</td>
    </tr>
  </tbody>
</table>

<p>CI 이후에는 Acceptance Test와 Staging 배포를 거쳐 Production 환경에 배포한다.</p>

<p><strong>Continuous Delivery</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Coding
  ↓
Push / Upload
  ↓
Build
  ↓
Test
  ↓
Merge
  ↓
Acceptance Test
  ↓
Deploy to Staging
  ↓
Deploy to Production (Manual)
  ↓
Smoke Test
</code></pre></div></div>

<p>Continuous Delivery에서는 Production에 배포할 수 있는 상태까지 자동화하지만, 실제 Production 배포는 수동으로 수행한다.</p>

<p><strong>Continuous Deployment</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Coding
  ↓
Push / Upload
  ↓
Build
  ↓
Test
  ↓
Merge
  ↓
Acceptance Test
  ↓
Deploy to Staging
  ↓
Deploy to Production
  ↓
Smoke Test
</code></pre></div></div>

<p>Continuous Deployment에서는 검증을 통과한 변경 사항을 Production 환경까지 자동으로 배포한다.</p>

<h4 id="테스트-종류"><strong>테스트 종류</strong></h4>

<p><strong>CI 단계의 테스트</strong></p>

<p>CI에서 수행하는 테스트는 코드 검증과 정적 테스트를 포함한다.</p>

<p>예를 들어 다음 항목을 점검할 수 있다.</p>

<ul>
  <li>사용하지 않는 변수</li>
  <li>사용하지 않는 패키지</li>
  <li>코드 및 로직 문제</li>
</ul>

<p><strong>Acceptance Test</strong></p>

<p>인수 테스트(Acceptance Test)는 개발된 애플리케이션이 고객의 요구사항에 부합하는지 확인하는 테스트이다.</p>

<p><strong>Smoke Test</strong></p>

<p>Smoke Test는 새로운 빌드나 배포판이 생성되었을 때 기본적인 기능이 정상적으로 동작하는지 빠르게 확인하는 테스트이다.</p>

<h3 id="ci-continuous-integration"><strong>CI (Continuous Integration)</strong></h3>

<p>CI는 개발팀이 작은 변경 사항을 구현하고 코드를 버전 제어 저장소(Repository)에 자주 체크인하도록 하는 코딩 철학이자 일련의 관행이다.</p>

<p>최신 애플리케이션은 다양한 플랫폼과 도구를 이용하여 개발되기 때문에 여러 개발자가 만든 변경 사항을 지속적으로 통합하고 검증하기 위한 메커니즘이 필요하다.</p>

<p>CI의 기술적 목표는 애플리케이션을 <strong>빌드, 패키징, 테스트하는 일관되고 자동화된 방법을 구축하는 것</strong>이다.</p>

<p>통합 프로세스의 일관성을 유지하면 개발자가 코드 변경을 더 자주 커밋할 수 있고, 공동 작업과 소프트웨어 품질을 향상시킬 수 있다.</p>

<p>소프트웨어 공학에서 지속적 통합은 품질 관리를 마지막 단계에서 한 번 수행하는 방식이 아니라, 작은 단위로 자주 통합하면서 지속적으로 품질 관리를 수행하는 프로세스이다.</p>

<p>이를 통해 소프트웨어의 품질을 높이고 배포에 걸리는 시간을 줄이는 데 초점을 맞춘다.</p>

<h3 id="continuous-delivery"><strong>Continuous Delivery</strong></h3>

<p>Continuous Delivery는 개발자가 애플리케이션에 적용한 변경 사항을 테스트한 뒤 Repository에 자동으로 업로드하고, 언제든 Production 환경에 배포할 수 있는 상태로 유지하는 방식이다.</p>

<p>Repository에는 Git 저장소나 Image Registry 등이 사용될 수 있다.</p>

<p>운영팀은 이 Repository에 저장된 결과물을 가져와 실제 Production 환경에 배포한다.</p>

<p><strong>목적</strong></p>

<ul>
  <li>개발팀과 비즈니스팀 간의 가시성 부족 문제 완화</li>
  <li>개발팀과 운영팀 간의 커뮤니케이션 문제 감소</li>
  <li>최소한의 노력으로 새로운 코드를 배포할 수 있는 상태 유지</li>
</ul>

<p>Continuous Delivery에서는 Production 배포가 가능한 상태까지 자동화하지만 실제 배포 시점은 사람이 결정한다.</p>

<h3 id="continuous-deployment"><strong>Continuous Deployment</strong></h3>

<p>Continuous Deployment는 개발자의 변경 사항을 Repository에서 고객이 실제로 사용할 수 있는 Production 환경까지 자동으로 릴리즈하는 방식이다.</p>

<p>수동 배포 과정으로 인해 운영팀에 프로세스 과부하가 발생하거나, 애플리케이션 제공 속도가 늦어지는 문제를 줄이는 데 목적이 있다.</p>

<p>Continuous Deployment는 Continuous Delivery에서 제공하는 자동화 기반을 활용하고, 그 다음 단계인 Production 배포까지 자동화한다.</p>

<p><strong>정리</strong></p>

<ul>
  <li>CI는 코드 변경 사항을 자주 통합하고 Build와 Test를 자동화하는 방식이다.</li>
  <li>Continuous Delivery는 Production에 배포 가능한 상태까지 자동화한다.</li>
  <li>Continuous Deployment는 Production 배포까지 자동화한다.</li>
  <li>Acceptance Test는 고객 요구사항 충족 여부를 확인한다.</li>
  <li>Smoke Test는 새로운 빌드나 배포 이후 기본 기능의 정상 동작 여부를 빠르게 확인한다.</li>
</ul>

<h3 id="containers"><strong>Containers</strong></h3>

<h4 id="개요-1"><strong>개요</strong></h4>

<p>Linux Container는 애플리케이션 실행에 필요한 파일과 실행 환경을 하나의 단위로 패키징하고, 다른 애플리케이션과 분리하여 실행할 수 있도록 하는 기술이다.</p>

<p>컨테이너화된 애플리케이션은 기능을 유지하면서 개발, 테스트, Production 환경 사이를 쉽게 이동할 수 있다.</p>

<p>또한 컨테이너 파이프라인에 보안을 적용하고 인프라를 보호함으로써 컨테이너 환경의 안정성, 확장성, 신뢰성을 확보할 수 있다.</p>

<h4 id="실행-구조-비교"><strong>실행 구조 비교</strong></h4>

<p><strong>가상화를 사용하지 않는 경우</strong></p>

<p>하나의 운영체제 위에 여러 애플리케이션을 직접 배치하여 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
Application
Application
    ↓
Operating System
    ↓
Hardware
</code></pre></div></div>

<p>각 애플리케이션이 동일한 운영체제 환경을 공유한다.</p>

<p><strong>KVM 가상화를 이용하는 경우</strong></p>

<p>운영체제 위에 Hypervisor를 배치하고, 그 위에 Guest OS를 설치한 뒤 애플리케이션을 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application      Application
    ↓                ↓
Guest OS         Guest OS
    ↓                ↓
      Hypervisor
          ↓
    Operating System
          ↓
       Hardware
</code></pre></div></div>

<p>애플리케이션을 서로 분리하려면 여러 Guest OS를 구성해야 한다.</p>

<p><strong>Linux Container를 이용하는 경우</strong></p>

<p>운영체제 위에 여러 Container를 직접 배치하지만, 각 Container는 서로 격리된 환경에서 실행된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container
Container
Container
    ↓
Operating System
    ↓
Hardware
</code></pre></div></div>

<p>Guest OS를 각각 설치하지 않고도 애플리케이션을 분리할 수 있다는 점이 가상 머신 방식과의 주요 차이이다.</p>

<h3 id="devops와-cicd에서의-container-활용"><strong>DevOps와 CI/CD에서의 Container 활용</strong></h3>

<p>Container는 DevOps와 CI/CD를 구현하는 주요 기술 요소 중 하나로 활용된다.</p>

<p>개발자가 코드를 Push하면 다음과 같은 흐름으로 배포가 진행될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer
    ↓
Push
    ↓
Code Repository
    ↓
Build Tool
    ↓
Docker Build
    ↓
Image Registry
    ↓
Kubernetes Deploy
</code></pre></div></div>

<p>개발자가 Code Repository에 코드를 Push하면 Build Tool이 애플리케이션을 빌드하고, Docker Image를 생성한다.</p>

<p>생성된 이미지는 Image Registry에 저장되고, 이후 Kubernetes 환경에 배포된다.</p>

<blockquote>
  <p><strong>정리</strong></p>
  <ul>
    <li>
      <p>Cloud Native의 주요 요소에는 CI/CD와 Container가 포함된다.</p>
    </li>
    <li>
      <p>CI는 코드 변경 사항을 지속적으로 통합하고 검증하는 과정이다.</p>
    </li>
    <li>
      <p>Continuous Delivery는 Production에 배포 가능한 상태까지 자동화하고, 실제 Production 배포는 수동으로 수행한다.</p>
    </li>
    <li>
      <p>Continuous Deployment는 Production 환경까지 자동으로 배포한다.</p>
    </li>
    <li>
      <p>Acceptance Test는 고객 요구사항 충족 여부를 확인하고, Smoke Test는 배포 이후 기본 기능의 정상 동작 여부를 빠르게 확인한다.</p>
    </li>
    <li>
      <p>Container는 애플리케이션과 실행 환경을 패키징하고 서로 격리하여 실행할 수 있도록 한다.</p>
    </li>
    <li>
      <p>가상 머신은 Guest OS를 각각 구성하지만, Container는 동일한 운영체제 환경을 공유하면서 격리된다.</p>
    </li>
    <li>
      <p>CI/CD 파이프라인에서는 Code Repository, Build Tool, Docker Build, Image Registry, Kubernetes 등을 연결하여 배포 과정을 자동화할 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><summary type="html"><![CDATA[CI, Continuous Delivery, Continuous Deployment의 차이와 Container 기반 배포 흐름]]></summary></entry><entry><title type="html">클라우드 네이티브 - Micro Serivce</title><link href="https://kkr010128.github.io/cloud-native-03-micro-service/" rel="alternate" type="text/html" title="클라우드 네이티브 - Micro Serivce" /><published>2026-08-11T00:00:00+09:00</published><updated>2026-08-11T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-03-micro-service</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-03-micro-service/"><![CDATA[<h2 id="1--비즈니스-민첩성과-cloud-native">1 ) 비즈니스 민첩성과 Cloud Native</h2>

<hr />

<h3 id="비즈니스-민첩성-business-agility">비즈니스 민첩성 (Business Agility)</h3>

<p>인터넷 기업의 경쟁력 중 하나는 <strong>새로운 서비스를 얼마나 빠르게 시장에 적용하고 개선할 수 있는가</strong>이다.</p>

<p>이를 비즈니스 민첩성(Business Agility)이라고 한다.</p>

<p>기존 비즈니스에 새로운 기술과 아이디어를 결합하여 서비스를 빠르게 출시하고, 사용자 피드백을 지속적으로 반영하면서 서비스를 개선하는 것이 핵심이다.</p>

<p>Amazon과 Netflix 같은 기업은 Cloud를 적극적으로 활용하면서 이러한 방식을 실제 비즈니스 성과로 연결한 대표적인 사례이다.</p>

<h4 id="빠른-배포가-중요한-이유">빠른 배포가 중요한 이유</h4>

<p>서비스를 빠르게 개선하려면 코드 작성뿐 아니라 실제 사용자에게 변경 사항을 전달하는 <strong>배포 주기 자체가 짧아야 한다.</strong></p>

<p>강의에서는 Amazon의 배포 속도가 2011년 약 11.6초 수준이었으며, 이후에는 초당 1.5회 이상, 즉 약 0.66초마다 서비스를 업데이트할 수 있는 수준까지 향상된 사례를 설명하였다.</p>

<p>배포 주기가 짧아지면 다음 과정을 빠르게 반복할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>서비스 개발
↓
배포
↓
사용자 피드백
↓
서비스 개선
↓
재배포
</code></pre></div></div>

<p>결국 비즈니스 민첩성은 단순히 개발 속도가 빠르다는 의미보다 <strong>개발 → 배포 → 피드백 → 개선의 주기를 얼마나 짧게 반복할 수 있는가</strong>와 관련된다.</p>

<h3 id="cloud가-비즈니스-민첩성을-높이는-이유"><strong>Cloud가 비즈니스 민첩성을 높이는 이유</strong></h3>

<p>과거에는 새로운 서비스를 시작하거나 기존 서비스를 확장하려면 서버와 네트워크 등 필요한 인프라를 먼저 준비해야 했다.</p>

<p>장비 구매와 설치에 시간이 필요했기 때문에 애플리케이션을 개발하더라도 인프라 준비가 서비스 출시 속도를 제한할 수 있었다.</p>

<p>Cloud에서는 필요한 컴퓨팅 자원을 빠르게 확보하고 확장할 수 있기 때문에 이러한 인프라 준비 시간을 줄일 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[기존 환경]

서비스 기획
   ↓
장비 구매
   ↓
인프라 구축
   ↓
애플리케이션 배포
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Cloud 환경]

서비스 기획
   ↓
Cloud 자원 생성
   ↓
애플리케이션 배포
</code></pre></div></div>

<p>하지만 <strong>애플리케이션을 Cloud에 올리는 것만으로 Cloud의 장점을 모두 활용할 수 있는 것은 아니다.</strong></p>

<h3 id="cloud에-맞는-애플리케이션-구조"><strong>Cloud에 맞는 애플리케이션 구조</strong></h3>

<p>예를 들어 하나의 시스템에 여러 기능이 존재한다고 가정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>회원
주문
결제
검색
</code></pre></div></div>

<p>이 중 검색 기능에만 사용자가 몰린다면 이상적인 방식은 <strong>검색 서비스만 확장하는 것</strong>이다.</p>

<p>하지만 애플리케이션 전체가 하나의 큰 덩어리로 구성되어 있다면 검색 기능 하나를 확장하기 위해 전체 애플리케이션을 함께 확장해야 할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>전체 애플리케이션
├── 회원
├── 주문
├── 결제
└── 검색  ← 트래픽 증가
</code></pre></div></div>

<p>이러한 구조에서는 Cloud가 자원을 탄력적으로 제공하더라도 애플리케이션 구조 때문에 특정 기능만 독립적으로 확장하기 어렵다.</p>

<p>따라서 Cloud의 탄력성을 효과적으로 활용하려면 시스템을 <strong>작은 단위의 독립적인 서비스로 분리</strong>할 필요가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>회원 서비스

주문 서비스

결제 서비스

검색 서비스  ← 필요한 서비스만 확장
</code></pre></div></div>

<h3 id="cloud-friendly와-cloud-native"><strong>Cloud Friendly와 Cloud Native</strong></h3>

<p>Cloud 환경에서 실행되는 애플리케이션은 구조에 따라 Cloud Friendly와 Cloud Native로 구분하여 생각할 수 있다.</p>

<h4 id="cloud-friendly"><strong>Cloud Friendly</strong></h4>

<p>Cloud Friendly는 기존 방식으로 개발한 애플리케이션을 <strong>Cloud 환경에서도 실행할 수 있도록 구성한 형태</strong>이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>기존 Application
       ↓
   Cloud Infra
</code></pre></div></div>

<p>서비스를 운영하는 것 자체에는 문제가 없지만, 애플리케이션이 하나의 덩어리로 구성되어 있다면 특정 기능만 독립적으로 확장하거나 배포하기 어렵다.</p>

<p>즉, <strong>Cloud를 사용할 수는 있지만 Cloud의 특성을 충분히 활용하지 못하는 구조</strong>이다.</p>

<h4 id="cloud-native"><strong>Cloud Native</strong></h4>

<p>Cloud Native Application은 애플리케이션을 <strong>독립적으로 분리하고 배포할 수 있는 작은 서비스 단위로 구성한 형태</strong>이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A ─┐
Service B ─┼─ Cloud Infra
Service C ─┘
</code></pre></div></div>

<p>각 서비스가 독립적으로 구성되기 때문에 필요한 부분만 확장하거나 새로운 버전을 배포할 수 있다.</p>

<p>따라서 Cloud가 제공하는 탄력성과 빠른 자원 공급 능력을 애플리케이션 수준에서도 활용할 수 있다.</p>

<h3 id="cloud-friendly에서-cloud-native로"><strong>Cloud Friendly에서 Cloud Native로</strong></h3>

<p>두 구조의 가장 중요한 차이는 단순히 <strong>Cloud에서 실행되는가</strong>가 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cloud Friendly
→ Cloud에서 실행 가능

Cloud Native
→ Cloud의 특성을 활용하도록 설계
</code></pre></div></div>

<p>Cloud Friendly 애플리케이션도 Cloud에서 정상적으로 서비스를 제공할 수 있다.</p>

<p>하지만 비즈니스 민첩성을 높이기 위해서는 특정 기능을 독립적으로 확장하고 배포할 수 있어야 하므로 궁극적으로는 Cloud Native 구조로 전환하는 것을 지향한다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>Cloud Friendly</strong></th>
      <th><strong>Cloud Native</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Cloud 실행</td>
      <td>가능</td>
      <td>가능</td>
    </tr>
    <tr>
      <td>서비스 구조</td>
      <td>하나의 큰 단위로 구성될 수 있음</td>
      <td>독립적인 작은 서비스로 구성</td>
    </tr>
    <tr>
      <td>특정 기능 확장</td>
      <td>어려움</td>
      <td>독립적으로 가능</td>
    </tr>
    <tr>
      <td>특정 기능 배포</td>
      <td>어려움</td>
      <td>독립적으로 가능</td>
    </tr>
    <tr>
      <td>Cloud 활용</td>
      <td>Cloud에 올려 사용하는 수준</td>
      <td>Cloud의 탄력성을 구조적으로 활용</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>비즈니스 민첩성은 서비스를 빠르게 출시하고 사용자 피드백을 반영하여 지속적으로 개선하는 능력이다.</p>
    </li>
    <li>
      <p>Cloud는 인프라 준비 시간을 줄여 서비스 개발과 배포 주기를 단축하는 데 유리하다.</p>
    </li>
    <li>
      <p>애플리케이션이 하나의 큰 덩어리로 구성되어 있으면 특정 기능만 독립적으로 확장하거나 배포하기 어렵다.</p>
    </li>
    <li>
      <p>Cloud Friendly는 기존 애플리케이션을 Cloud 환경에서 실행할 수 있도록 구성한 형태이다.</p>
    </li>
    <li>
      <p>Cloud Native는 서비스를 독립적인 단위로 분리하여 필요한 서비스만 확장하고 배포할 수 있도록 설계한 형태이다.</p>
    </li>
    <li>
      <p>Cloud의 탄력성과 확장성을 효과적으로 활용하려면 Cloud Friendly에서 Cloud Native 구조로 전환하는 것이 중요하다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="2--micro-service">2 ) Micro Service</h2>

<hr />

<h3 id="monolithic과-micro-service">Monolithic과 Micro Service</h3>

<h4 id="monolithic">Monolithic</h4>

<p>Monolithic Architecture는 애플리케이션의 여러 기능을 <strong>하나의 애플리케이션 및 배포 단위로 구성하는 일체형 구조</strong>이다.</p>

<p>일반적으로 다음과 같은 3-Tier 구조로 설명할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client ↔ Server ↔ Storage
</code></pre></div></div>

<ul>
  <li><strong>Client</strong>: 사용자 인터페이스를 제공한다.</li>
  <li><strong>Server</strong>: 비즈니스 로직을 처리한다.</li>
  <li><strong>Storage</strong>: 애플리케이션의 데이터를 저장한다.</li>
</ul>

<p>Monolithic은 여러 기능이 하나의 애플리케이션으로 묶여 있기 때문에 특정 기능에 변경이 발생하더라도 전체 애플리케이션을 다시 빌드하고 배포해야 하는 경우가 많다.</p>

<p>애플리케이션의 여러 인스턴스를 운영하는 경우에도 새로운 버전을 적용하려면 각 인스턴스에 변경된 애플리케이션을 배포해야 한다.</p>

<p>확장 방법으로는 서버의 CPU와 Memory 등의 사양을 높이는 <strong>Scale-up</strong>을 사용할 수 있으며, 동일한 Monolithic 애플리케이션 인스턴스를 여러 개 실행하는 <strong>Scale-out</strong>도 가능하다.</p>

<p>다만 특정 기능에만 부하가 집중되더라도 해당 기능만 독립적으로 확장하기 어려워 전체 애플리케이션 단위로 확장해야 할 수 있다는 한계가 있다.</p>

<h4 id="micro-service">Micro Service</h4>

<p>Micro Service는 서버 측 애플리케이션을 여러 개의 독립적인 서비스로 분리하여 구성하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    ┌─ Service A ↔ Storage A
Client ↔ Gateway ───┼─ Service B ↔ Storage B
                    └─ Service C ↔ Storage C
</code></pre></div></div>

<p>각 서비스는 별도의 프로세스나 인스턴스로 실행되며, 여러 서비스가 모여 하나의 비즈니스 애플리케이션을 구성한다.</p>

<p>서비스를 업무 기능 단위로 분리하고 각 서비스가 자신의 데이터를 관리하도록 구성하면 모듈의 경계를 명확하게 나눌 수 있다.</p>

<p><strong>특징</strong></p>

<ul>
  <li>필요한 서비스만 독립적으로 확장할 수 있다.</li>
  <li>변경이 발생한 서비스만 빌드하고 배포할 수 있다.</li>
  <li>전체 시스템을 다시 배포할 필요가 줄어 배포 주기를 단축할 수 있다.</li>
  <li>서비스마다 서로 다른 언어와 데이터 저장 기술을 사용할 수 있다.</li>
  <li>서비스별 소유권을 분리하여 서로 다른 팀이 개발과 운영을 담당할 수 있다.</li>
</ul>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Monolithic</th>
      <th>Micro Service</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>구성</td>
      <td>하나의 애플리케이션</td>
      <td>여러 개의 독립적인 서비스</td>
    </tr>
    <tr>
      <td>배포 단위</td>
      <td>전체 애플리케이션</td>
      <td>개별 서비스</td>
    </tr>
    <tr>
      <td>확장 단위</td>
      <td>애플리케이션 전체</td>
      <td>필요한 서비스</td>
    </tr>
    <tr>
      <td>저장소</td>
      <td>통합 저장소를 사용하는 경우가 많음</td>
      <td>서비스별 데이터 소유를 지향</td>
    </tr>
    <tr>
      <td>기술 선택</td>
      <td>전체 애플리케이션의 기술 스택에 영향을 받음</td>
      <td>서비스별 기술 선택 가능</td>
    </tr>
    <tr>
      <td>팀 구성</td>
      <td>기능별 조직 간 협업이 필요할 수 있음</td>
      <td>비즈니스 기능 중심의 독립적인 팀 구성 가능</td>
    </tr>
  </tbody>
</table>

<h3 id="soa와-micro-service">SOA와 Micro Service</h3>

<h4 id="모듈화의-발전">모듈화의 발전</h4>

<p>소프트웨어는 복잡성을 줄이고 재사용성과 유지보수성을 높이기 위해 지속적으로 더 작은 단위로 모듈화되어 왔다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>구조적 방법론
↓
객체 지향 방법론
↓
CBD
↓
SOA
↓
Micro Service
</code></pre></div></div>

<p><strong>구조적 방법론</strong></p>

<p>기능을 하향식으로 분해하면서 시스템을 설계하는 방식이다.</p>

<p><strong>객체 지향 방법론</strong></p>

<p>데이터와 행위를 객체 단위로 캡슐화하여 시스템을 모듈화한다.</p>

<p><strong>CBD (Component Based Development)</strong></p>

<p>기능별로 재사용할 수 있는 컴포넌트(Component)를 조합하여 시스템을 개발하는 방식이다.</p>

<p><strong>SOA (Service Oriented Architecture)</strong></p>

<p>컴포넌트보다 더 큰 범위에서 비즈니스적으로 의미 있고 완결된 기능을 <strong>서비스(Service)</strong> 단위로 구성하고 연계하는 아키텍처이다.</p>

<p>CBD와 SOA 역시 독립적인 컴포넌트 또는 서비스를 조합하여 하나의 시스템을 만든다는 점에서 Micro Service와 공통점이 있다.</p>

<p>Micro Service는 이러한 서비스 지향 개념이 Cloud 인프라, 자동화된 배포, DevOps 등의 기술 및 개발 방식과 결합하면서 발전한 형태로 이해할 수 있다.</p>

<p>Micro Service를 기반으로 시스템을 설계하고 개발하는 아키텍처를 <strong>MSA (Microservice Architecture)</strong> 라고 한다.</p>

<h4 id="msa의-특징">MSA의 특징</h4>

<p>Microservice라는 용어는 Amazon, Netflix와 같은 인터넷 기업이 대규모 서비스를 운영하면서 사용한 아키텍처의 공통적인 특징을 정리하는 과정에서 널리 알려졌다.</p>

<p>MSA의 주요 특징은 다음과 같다.</p>

<ul>
  <li>하나의 애플리케이션을 여러 개의 작은 서비스 집합으로 구성한다.</li>
  <li>각 서비스는 독립적인 프로세스에서 실행될 수 있다.</li>
  <li>서비스 간에는 HTTP API나 Messaging 등의 비교적 가벼운 통신 방식을 사용한다.</li>
  <li>서비스를 비즈니스 기능 단위로 구성한다.</li>
  <li>자동화된 배포 체계를 이용하여 각 서비스를 독립적으로 배포한다.</li>
  <li>중앙 집중적인 관리를 최소화하고 각 서비스에 자율성을 부여한다.</li>
  <li>서비스별로 서로 다른 언어와 데이터 저장 기술을 선택할 수 있다.</li>
  <li>서비스가 자신의 데이터를 소유하도록 하여 다른 서비스와의 결합도를 낮춘다.</li>
  <li>서비스 간에는 명시적인 API나 메시지를 통해 통신한다.</li>
</ul>

<p>서비스가 서로 독립적이라면 다른 서비스와 약속한 API 계약을 유지하는 범위에서 내부 구현 언어나 저장 기술을 자유롭게 선택할 수 있다.</p>

<p>이를 다음과 같이 표현할 수 있다.</p>

<p><strong>Polyglot Programming</strong></p>

<p>서비스마다 목적에 적합한 서로 다른 프로그래밍 언어나 개발 기술을 사용하는 방식이다.</p>

<p><strong>Polyglot Persistence</strong></p>

<p>서비스마다 데이터 특성에 적합한 서로 다른 데이터 저장 기술을 사용하는 방식이다.</p>

<p>예를 들어 한 서비스는 RDBMS를 사용하고 다른 서비스는 NoSQL이나 Redis를 사용할 수 있다.</p>

<h4 id="soa와-msa의-차이">SOA와 MSA의 차이</h4>

<p>SOA와 MSA를 단순히 완전히 다른 개념으로 구분하기는 어렵다.</p>

<p>둘 모두 서비스 단위로 시스템을 구성한다는 공통점이 있다.</p>

<p>다만 전통적인 SOA 구현에서는 여러 서비스가 공통 데이터베이스나 ESB(Enterprise Service Bus)와 같은 중앙 집중적인 통합 요소를 공유하는 형태가 많이 사용되었으며, 이 경우 서비스 간 결합도가 높아질 수 있었다.</p>

<p>Micro Service에서는 서비스의 독립성을 강화하기 위해 다음 원칙을 중요하게 다룬다.</p>

<p><strong>서비스의 데이터 캡슐화</strong></p>

<p>서비스가 자신의 데이터를 소유하며, 다른 서비스가 해당 저장소를 직접 접근하지 않도록 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>잘못된 접근

Service A ──────────→ Storage B
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>권장 구조

Service A → API / Message → Service B → Storage B
</code></pre></div></div>

<p>다른 서비스의 데이터가 필요한 경우 해당 서비스를 통해 접근하도록 하여 저장소의 구현을 캡슐화한다.</p>

<p><strong>가벼운 통신 방식</strong></p>

<p>서비스 간 통신에는 REST API, gRPC, Messaging 등 비교적 가벼운 통신 방식을 사용할 수 있다.</p>

<p>이러한 구조를 통해 서비스 간 결합도를 낮추고 각 서비스를 독립적으로 변경하거나 배포할 수 있도록 한다.</p>

<h3 id="micro-service를-위한-변화">Micro Service를 위한 변화</h3>

<p>Micro Service는 단순히 애플리케이션을 작은 서비스로 나누는 것만으로 완성되지 않는다.</p>

<p>조직, 개발 방식, 배포 방식, 데이터 관리, 장애 대응 등 여러 영역이 함께 변화해야 한다.</p>

<h4 id="조직의-변화">조직의 변화</h4>

<p>기존처럼 개발, 데이터베이스, 운영 등 기술 분야별 부서를 중심으로 조직을 구성하기보다 <strong>비즈니스 기능을 중심으로 팀을 구성</strong>할 수 있다.</p>

<p>하나의 팀이 특정 서비스의 개발과 운영을 함께 담당한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>기존

개발팀
DB팀
운영팀
↓
여러 팀이 하나의 기능을 함께 처리
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Micro Service

주문팀 → 주문 서비스
결제팀 → 결제 서비스
배송팀 → 배송 서비스
</code></pre></div></div>

<p>서비스에 대한 의사결정을 하나의 팀에서 수행할 수 있으므로 다른 팀과 조율해야 하는 범위를 줄일 수 있다.</p>

<p>다만 Micro Service라고 해서 다른 팀과의 의사소통이 없어지는 것은 아니며, 서비스 간 API 계약이나 비즈니스 프로세스에 대한 협업은 여전히 필요하다.</p>

<h4 id="관리-체계의-변화">관리 체계의 변화</h4>

<p>각 서비스와 팀에 일정 수준의 자율성을 부여하는 <strong>분권형 관리 구조</strong>를 지향한다.</p>

<p>서비스 특성에 따라 적절한 언어, 프레임워크, 데이터 저장 기술을 선택할 수 있으며 이를 Polyglot한 환경이라고 표현한다.</p>

<h4 id="개발-생명-주기의-변화">개발 생명 주기의 변화</h4>

<p>기존에는 애플리케이션 개발을 하나의 <strong>Project</strong>로 바라보는 경우가 많았다.</p>

<p>필요한 인력이 한시적으로 모여 장기간 개발한 뒤 개발이 완료되면 운영 조직에 시스템을 넘기는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>요구사항
↓
장기간 개발
↓
완료
↓
운영 조직에 인계
</code></pre></div></div>

<p>개발 조직과 운영 조직이 분리되어 있으면 프로젝트 진행 중 발생한 새로운 아이디어나 요구사항 변경을 빠르게 반영하기 어려울 수 있다.</p>

<p>Micro Service와 Cloud Native 환경에서는 소프트웨어를 일회성 프로젝트가 아니라 지속적으로 발전시키는 <strong>Product</strong>로 바라보는 방식을 지향한다.</p>

<p>폭포수 모델이나 한 번에 대규모 기능을 개발하는 Big Bang 방식보다는 점진적이고 반복적인 Agile 개발 방식을 사용할 수 있다.</p>

<p>예를 들어 2~3주 정도의 Sprint를 반복하면서 다음 과정을 수행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발
 ↓
배포
 ↓
사용자 피드백
 ↓
개선
 ↓
다음 Sprint
</code></pre></div></div>

<p>소프트웨어를 고정된 요구사항을 모두 만족하면 끝나는 결과물이 아니라 <strong>요구사항 변화에 따라 지속적으로 개선되는 제품</strong>으로 바라본다.</p>

<h4 id="개발-환경의-변화">개발 환경의 변화</h4>

<p>Micro Service는 각 서비스를 독립적으로 배포한다.</p>

<p>Monolithic 애플리케이션처럼 배포 단위가 하나라면 수동 작업으로 관리할 수 있는 범위가 상대적으로 넓지만, 서비스가 수십 또는 수백 개로 늘어나면 모든 서비스를 수동으로 관리하고 배포하기 어렵다.</p>

<p>소프트웨어 개발 과정은 크게 다음과 같이 나눌 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발 환경 준비
      ↓
소프트웨어 개발
      ↓
Build / Test / Deploy
</code></pre></div></div>

<p>Cloud 인프라를 이용하면 개발 및 실행에 필요한 인프라를 빠르게 준비할 수 있다.</p>

<p>또한 Build, Test, Deploy 과정을 자동화함으로써 개발과 운영의 반복적인 작업을 줄일 수 있다.</p>

<p>이러한 자동화 환경은 개발과 운영을 함께 수행하는 DevOps를 지원하는 중요한 기반이 된다.</p>

<h4 id="배포-파이프라인-자동화">배포 파이프라인 자동화</h4>

<p>Micro Service 환경에서는 각 서비스가 독립적으로 변경되고 배포되므로 Build와 Deploy 과정의 자동화가 중요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Build
(Compile / Unit Test)
        ↓
개발 환경 배포
(기능 테스트)
        ↓
테스트 환경 배포
(통합 테스트)
        ↓
Staging 환경 배포
(인수 테스트 / 성능 테스트 / 비기능 테스트)
        ↓
Production 환경 배포
</code></pre></div></div>

<p><strong>Staging 환경</strong></p>

<p>실제 Production과 최대한 유사한 환경을 구성하고 운영 환경에 배포하기 전에 다음과 같은 요소를 검증한다.</p>

<ul>
  <li>기능</li>
  <li>Security</li>
  <li>성능</li>
  <li>장애 대응</li>
  <li>기타 비기능 요구사항</li>
</ul>

<p>서비스의 수가 증가하면 배포 환경과 인프라 역시 빠르게 증가한다.</p>

<p>따라서 인프라 설정을 사람이 반복해서 수행하기보다 소프트웨어 코드처럼 정의하여 관리하는 <strong>IaC (Infrastructure as Code)</strong> 를 활용할 수 있다.</p>

<p>IaC를 이용하면 인프라 설정을 버전 관리하고 반복적으로 동일한 환경을 구성할 수 있다.</p>

<h3 id="분권형-데이터-관리">분권형 데이터 관리</h3>

<h4 id="서비스별-저장소">서비스별 저장소</h4>

<p>Monolithic 애플리케이션에서는 여러 비즈니스 기능이 하나의 통합 데이터베이스를 사용하는 경우가 많다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service Logic A ─┐
Service Logic B ─┼─ Shared Database
Service Logic C ─┘
</code></pre></div></div>

<p>Micro Service에서는 서비스의 독립성을 높이기 위해 <strong>Database per Service</strong> 형태를 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A ↔ Database A

Service B ↔ Database B

Service C ↔ Database C
</code></pre></div></div>

<p>각 서비스는 자신의 저장소를 소유하고 다른 서비스는 해당 저장소를 직접 접근하지 않는다.</p>

<p>다른 서비스의 데이터가 필요하면 해당 서비스가 제공하는 API나 Event를 이용한다.</p>

<p>각 서비스는 데이터 특성에 따라 적합한 저장 기술을 선택할 수 있으므로 Polyglot Persistence를 적용할 수 있다.</p>

<h4 id="rdbms와-데이터-정규화">RDBMS와 데이터 정규화</h4>

<p>RDBMS에서는 정규화를 통해 불필요한 데이터 중복을 줄이고 데이터의 삽입·수정·삭제 과정에서 발생할 수 있는 이상 현상(Anomaly)을 줄일 수 있다.</p>

<blockquote>
  <p><strong>RDBMS와 정규화</strong></p>

  <ul>
    <li>불필요한 데이터 중복 최소화</li>
    <li>데이터 무결성 및 일관성 관리</li>
    <li>데이터 간 종속 관계를 체계적으로 표현</li>
    <li>삽입·수정·삭제 이상 현상 감소</li>
  </ul>
</blockquote>

<p>과거보다 Storage 비용이 낮아지고 Network 환경이 발전하면서 일부 분산 시스템에서는 성능과 독립성을 위해 데이터의 복제나 중복을 의도적으로 허용하기도 한다.</p>

<p>그러나 이것이 정규화가 더 이상 필요하지 않다는 의미는 아니다.</p>

<p>Micro Service에서는 서비스 독립성을 확보하기 위해 서비스별 저장소를 구성하면서 일부 데이터가 여러 서비스에 중복 저장될 수 있으며, 이로 인해 <strong>분산 데이터의 일관성 문제</strong>가 발생한다.</p>

<h3 id="분산-트랜잭션과-데이터-일관성">분산 트랜잭션과 데이터 일관성</h3>

<h4 id="2단계-커밋의-한계">2단계 커밋의 한계</h4>

<p>하나의 데이터베이스에서는 ACID Transaction을 이용하여 여러 작업을 하나의 Transaction으로 처리할 수 있다.</p>

<p>하지만 Micro Service에서는 저장소가 서비스별로 분리되어 있기 때문에 여러 저장소에 걸친 Transaction을 하나로 처리하기 어렵다.</p>

<p>분산 Transaction을 처리하는 전통적인 방법으로 <strong>2PC (Two-Phase Commit)</strong> 가 있다.</p>

<p>그러나 여러 서비스를 하나의 분산 Transaction으로 묶으면 각 서비스가 서로의 상태에 의존하게 되어 독립성과 가용성에 영향을 줄 수 있다.</p>

<p>또한 모든 데이터 저장 기술이 동일한 형태의 분산 Transaction을 지원하는 것도 아니다.</p>

<p>따라서 Micro Service에서는 2PC로 모든 서비스를 하나의 Transaction으로 묶기보다 <strong>각 서비스의 Local Transaction을 독립적으로 처리하고 서비스 간 일관성을 별도로 관리하는 방식</strong>을 사용할 수 있다.</p>

<h4 id="결과적-일관성-eventual-consistency">결과적 일관성 (Eventual Consistency)</h4>

<p>분산 시스템에서는 모든 서비스의 데이터가 항상 같은 순간에 즉시 일치하도록 만들기 어려울 수 있다.</p>

<p>따라서 일정 시간 동안 일부 데이터의 상태가 서로 다를 수 있지만, 새로운 변경이 없다면 <strong>최종적으로 시스템이 일관된 상태에 도달하도록 설계하는 방식</strong>을 결과적 일관성(Eventual Consistency)이라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>시간 T1

Order Service   → 주문 완료
Delivery Service → 아직 배송 정보 없음


시간 T2

Order Service   → 주문 완료
Delivery Service → 배송 준비


결과적으로 비즈니스 상태가 일관된 상태에 도달
</code></pre></div></div>

<h4 id="saga-pattern">Saga Pattern</h4>

<p>Saga는 여러 Micro Service에 걸친 하나의 비즈니스 작업을 여러 개의 <strong>Local Transaction</strong>으로 분리하여 수행하는 패턴이다.</p>

<p>각 서비스는 자신의 Local Transaction만 처리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Transaction A
     ↓
Transaction B
     ↓
Transaction C
</code></pre></div></div>

<p>중간 단계가 실패하면 이미 수행된 작업을 되돌리기 위한 <strong>Compensating Transaction(보상 트랜잭션)</strong> 을 실행하여 비즈니스 정합성을 맞춘다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>주문 생성
   ↓
결제 성공
   ↓
배송 실패
   ↓
결제 취소
   ↓
주문 취소
</code></pre></div></div>

<p>Saga는 크게 Choreography 방식과 Orchestration 방식으로 구현할 수 있다.</p>

<h4 id="queue를-이용한-주문배송-처리-사례">Queue를 이용한 주문·배송 처리 사례</h4>

<p>주문 서비스와 배송 서비스를 분리한 경우 Message Queue를 이용하여 비동기적으로 Transaction을 연결할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>주문 서비스
    ↓
주문 처리 Local Transaction
    ↓
주문 이벤트 발행
    ↓
Message Queue
    ↓
배송 서비스
    ↓
배송 처리 Local Transaction
</code></pre></div></div>

<p>배송 처리 중 오류가 발생하면 다음과 같이 처리할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>배송 서비스
    ↓
배송 처리 실패
    ↓
배송 실패 이벤트 발행
    ↓
Message Queue
    ↓
주문 서비스
    ↓
주문 취소 보상 Transaction
</code></pre></div></div>

<p>이 방식에서는 하나의 거대한 분산 Transaction으로 모든 서비스를 묶지 않고 각 서비스의 Local Transaction과 Event를 이용하여 전체 비즈니스 정합성을 관리한다.</p>

<h3 id="실패를-고려한-설계">실패를 고려한 설계</h3>

<p>분산 시스템에서는 Network, Server, Application, 외부 API 등 다양한 요소에서 장애가 발생할 수 있다.</p>

<p>따라서 <strong>장애가 절대로 발생하지 않는 시스템</strong>을 목표로 하기보다 장애가 발생하더라도 전체 시스템으로 확산되지 않고 복구할 수 있도록 설계할 필요가 있다.</p>

<p>이를 <strong>내결함성(Fault Tolerance)</strong> 관점에서 생각할 수 있다.</p>

<p>이를 위해서는 다음과 같은 환경이 필요하다.</p>

<ul>
  <li>다양한 장애 상황을 검증하는 테스트</li>
  <li>장애 발생 여부를 확인할 수 있는 Monitoring</li>
  <li>장애를 빠르게 탐지하는 체계</li>
  <li>장애가 다른 서비스로 전파되지 않도록 하는 격리 구조</li>
  <li>장애 발생 후 정상 상태로 회복할 수 있는 복구 전략</li>
</ul>

<h4 id="circuit-breaker-pattern">Circuit Breaker Pattern</h4>

<p>Circuit Breaker는 전기 회로의 차단기처럼 장애가 발생하는 외부 서비스에 대한 요청을 일정 시간 차단하여 <strong>연쇄 장애(Cascading Failure)</strong> 를 방지하는 패턴이다.</p>

<p>예를 들어 다음 구조가 있다고 가정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>주문 서비스
    ↓
결제 서비스
    ↓
카드사 API
</code></pre></div></div>

<p>결제 서비스에 장애가 발생했는데 주문 서비스가 계속 요청을 보내면 다음과 같은 자원이 누적되어 고갈될 수 있다.</p>

<ul>
  <li>Thread</li>
  <li>Connection</li>
  <li>CPU</li>
  <li>Memory</li>
</ul>

<p>Circuit Breaker는 일정한 실패 조건을 감지하면 더 이상 장애 서비스로 요청을 보내지 않고 빠르게 실패를 반환한다.</p>

<p>일반적인 상태는 다음과 같이 표현한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Closed
  ↓ 실패 누적
Open
  ↓ 일정 시간 후
Half-Open
  ↓
성공 → Closed
실패 → Open
</code></pre></div></div>

<p>핵심은 <strong>장애가 발생한 서비스를 계속 호출하지 않고 일시적으로 호출을 차단하여 장애가 전체 시스템으로 전파되는 것을 막는 것</strong>이다.</p>

<p><strong>구현 기술</strong></p>

<ul>
  <li>Resilience4j</li>
  <li>Spring Cloud CircuitBreaker</li>
  <li>Istio / Envoy 등의 Traffic Management 기능</li>
</ul>

<p><strong>사용할 수 있는 환경</strong></p>

<ul>
  <li>AWS Lambda → 외부 API</li>
  <li>EKS / Kubernetes → 다른 Micro Service</li>
  <li>API Gateway → Backend Service</li>
  <li>MSA 서비스 간 REST / gRPC 호출</li>
  <li>외부 결제·인증·배송 API</li>
  <li>Database / Redis 등 외부 의존 서비스</li>
</ul>

<h4 id="chaos-monkey">Chaos Monkey</h4>

<p>Netflix는 장애 상황에서도 시스템이 정상적으로 대응할 수 있는지 검증하기 위해 <strong>Chaos Monkey</strong>를 개발하였다.</p>

<p>Chaos Monkey는 Production 환경의 VM이나 Container Instance를 의도적으로 종료하여 실제 장애 상황을 발생시키고 시스템의 Resilience를 검증하는 Chaos Engineering 도구이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>정상 시스템
   ↓
의도적인 Instance 장애 발생
   ↓
자동 복구 / 우회 / 확장 확인
</code></pre></div></div>

<h3 id="reactive-system">Reactive System</h3>

<p>현대적인 분산 애플리케이션이 갖추어야 할 특성을 정리한 문서 중 하나가 <strong>Reactive Manifesto(리액티브 선언문)</strong> 이다.</p>

<p>Reactive Manifesto는 다음 네 가지 특성을 강조한다.</p>

<table>
  <thead>
    <tr>
      <th>요소</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Responsive</td>
      <td>응답성</td>
    </tr>
    <tr>
      <td>Resilient</td>
      <td>탄력성·회복성</td>
    </tr>
    <tr>
      <td>Elastic</td>
      <td>유연성</td>
    </tr>
    <tr>
      <td>Message Driven</td>
      <td>메시지 구동</td>
    </tr>
  </tbody>
</table>

<h4 id="responsive">Responsive</h4>

<p>사용자에게 빠르고 일관된 응답을 제공하는 특성이다.</p>

<p>단순히 평균 응답 시간이 빠른 것뿐 아니라 문제가 발생했을 때 이를 신속하게 탐지하고 적절하게 대응할 수 있어야 한다.</p>

<h4 id="resilient">Resilient</h4>

<p>일부 구성 요소에 장애가 발생하더라도 전체 시스템이 함께 중단되지 않고 정상적인 서비스를 지속하거나 빠르게 회복할 수 있는 특성이다.</p>

<p>장애 격리, 복제, 복구 등의 방법을 이용할 수 있다.</p>

<h4 id="elastic">Elastic</h4>

<p>서비스 사용량 변화에 따라 필요한 자원을 증가시키거나 감소시켜 일정한 수준의 응답성을 유지하는 특성이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>트래픽 증가 → 자원 증가

트래픽 감소 → 자원 감소
</code></pre></div></div>

<h4 id="message-driven">Message Driven</h4>

<p>구성 요소 사이에서 <strong>비동기 메시지 전달</strong>을 사용하여 느슨한 결합, 장애 격리, 위치 투명성을 확보하는 특성이다.</p>

<p>즉, 논블로킹 통신을 지양하는 것이 아니라 <strong>비동기적이고 느슨하게 결합된 통신 구조를 지향</strong>한다.</p>

<p>네 가지 요소는 서로 독립적인 특징이 아니라 상호 보완적으로 Reactive System을 구성한다.</p>

<h3 id="강결합에서-느슨한-결합으로">강결합에서 느슨한 결합으로</h3>

<p>과거에는 애플리케이션의 구성 요소를 특정 Vendor의 제품군에 강하게 의존하여 구축하는 경우가 많았다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
    ↓
Vendor Framework
    ↓
Vendor Middleware
    ↓
Vendor Infrastructure
</code></pre></div></div>

<p>검증된 Vendor의 제품군을 사용함으로써 품질과 지원을 확보할 수 있다는 장점이 있지만 특정 기술에 대한 의존도가 높아질 수 있다.</p>

<p>특정 기술이나 Vendor에 지나치게 종속되면 다른 기술로 변경하기 어려워지는 <strong>Vendor Lock-in</strong> 문제가 발생할 수 있다.</p>

<p>Cloud 환경에서는 다양한 Open Source 및 Open Source 기반 상용 제품을 조합하여 아키텍처를 구성할 수 있다.</p>

<p>따라서 최근 아키텍처 설계에서는 특정 하나의 Vendor 제품군만 사용하는 것보다 요구사항에 맞는 여러 솔루션을 선택하고 조합하는 경우도 많다.</p>

<p>이는 아키텍처가 다음과 같이 변화하는 과정으로 이해할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>강하게 결합된 Monolithic Architecture
               ↓
교체 가능한 구성 요소
               ↓
느슨하게 결합된 Micro Service Architecture
</code></pre></div></div>

<p>각 구성 요소는 명확한 인터페이스를 기준으로 연결하고 필요한 경우 다른 구현체로 대체할 수 있도록 구성하는 것을 지향한다.</p>

<h3 id="msa-구성-요소">MSA 구성 요소</h3>

<p>MSA는 Micro Service 코드만으로 구성되지 않는다.</p>

<p>서비스를 실행하고 배포하며 관찰하기 위한 다양한 인프라와 플랫폼 요소가 필요하다.</p>

<h4 id="인프라">인프라</h4>

<p>Micro Service를 어떤 환경에서 실행할 것인지 결정한다.</p>

<ul>
  <li>Public Cloud</li>
  <li>Private Cloud</li>
  <li>Bare Metal</li>
  <li>Virtual Machine</li>
  <li>Container</li>
  <li>Container Orchestration</li>
</ul>

<h4 id="backing-service">Backing Service</h4>

<p>Backing Service는 애플리케이션이 Network를 통해 사용하는 외부 의존 서비스를 의미한다.</p>

<p><strong>Persistence</strong></p>

<p>데이터를 영구적으로 저장한다.</p>

<ul>
  <li>RDBMS</li>
  <li>NoSQL</li>
</ul>

<p><strong>Cache</strong></p>

<p>자주 사용하는 데이터를 빠르게 제공하기 위한 Cache를 구성한다.</p>

<ul>
  <li>Redis</li>
</ul>

<p><strong>Message Broker</strong></p>

<p>서비스 사이에서 Message 또는 Event를 전달한다.</p>

<ul>
  <li>Kafka</li>
  <li>RabbitMQ</li>
  <li>Queue</li>
</ul>

<h3 id="telemetry와-observability">Telemetry와 Observability</h3>

<p>Micro Service는 서비스가 여러 개로 분산되어 있기 때문에 하나의 시스템에서 다음 문제가 발생한다.</p>

<ul>
  <li>여러 서비스에 분산된 Log를 어떻게 모을 것인가?</li>
  <li>하나의 요청이 어떤 서비스를 거쳤는지 어떻게 확인할 것인가?</li>
  <li>각 서비스와 Node의 상태를 어떻게 Monitoring할 것인가?</li>
</ul>

<p>따라서 <strong>Log, Trace, Metric</strong>을 통합적으로 관리할 필요가 있다.</p>

<h4 id="logging">Logging</h4>

<p>각 서비스에서 생성되는 Log를 중앙에서 수집하고 검색할 수 있도록 구성한다.</p>

<p>대표적인 구성은 다음과 같다.</p>

<p><strong>ELK</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Elasticsearch
+ Logstash
+ Kibana
</code></pre></div></div>

<p><strong>EFK</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Elasticsearch
+ Fluentd
+ Kibana
</code></pre></div></div>

<h4 id="distributed-tracing">Distributed Tracing</h4>

<p>Client의 하나의 요청이 여러 Micro Service를 순차적으로 호출하는 경우 전체 요청의 흐름을 추적해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓
Service A
  ↓
Service B
  ↓
Service C
</code></pre></div></div>

<p>각 서비스 호출에 대한 Trace와 Span을 연결하여 어떤 구간에서 지연이나 장애가 발생했는지 확인한다.</p>

<p>관련 기술은 다음과 같다.</p>

<ul>
  <li>Micrometer Tracing</li>
  <li>OpenTelemetry</li>
  <li>Zipkin</li>
  <li>Jaeger</li>
</ul>

<p>Spring Cloud Sleuth는 과거 Spring 기반 분산 추적에 사용되었으나 Spring Boot 3.x 이후에는 핵심 기능이 Micrometer Tracing으로 이전되었다.</p>

<h4 id="monitoring">Monitoring</h4>

<p>서비스와 인프라의 상태를 지속적으로 수집하고 확인한다.</p>

<p>예를 들어 다음 항목을 Monitoring할 수 있다.</p>

<ul>
  <li>CPU</li>
  <li>Memory</li>
  <li>Application 상태</li>
  <li>Node 상태</li>
  <li>Network I/O</li>
  <li>Disk 사용량</li>
  <li>HPA 동작 상태</li>
  <li>Request 처리량</li>
  <li>Error Rate</li>
</ul>

<p>대표적으로 Prometheus와 Grafana를 함께 사용할 수 있다.</p>

<p>Cloud Native 환경에서는 다음과 같은 구성을 조합할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prometheus + Grafana
OpenTelemetry
EFK
Jaeger
</code></pre></div></div>

<p>각 도구가 동일한 역할을 수행하는 것은 아니며 Metric, Log, Trace 등의 역할을 나누어 Observability 환경을 구성한다.</p>

<h3 id="cicd-구성-요소">CI/CD 구성 요소</h3>

<p>Micro Service는 독립적인 Build와 Deploy가 반복적으로 발생하기 때문에 CI/CD 자동화가 중요하다.</p>

<p>관련 기술의 예는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Git</td>
      <td>분산 버전 관리 시스템</td>
    </tr>
    <tr>
      <td>Jenkins</td>
      <td>CI/CD 자동화</td>
    </tr>
    <tr>
      <td>Gradle</td>
      <td>Build Tool</td>
    </tr>
    <tr>
      <td>Maven</td>
      <td>Build Tool</td>
    </tr>
    <tr>
      <td>JUnit</td>
      <td>Unit Test</td>
    </tr>
    <tr>
      <td>SonarQube</td>
      <td>정적 코드 및 품질 분석</td>
    </tr>
    <tr>
      <td>JaCoCo</td>
      <td>Java Code Coverage 측정</td>
    </tr>
    <tr>
      <td>Harbor</td>
      <td>Container Image Registry</td>
    </tr>
    <tr>
      <td>Nexus Repository</td>
      <td>Package 및 Build Artifact 저장소</td>
    </tr>
    <tr>
      <td>Helm</td>
      <td>Kubernetes Package Manager</td>
    </tr>
  </tbody>
</table>

<h3 id="cloud-인프라-서비스">Cloud 인프라 서비스</h3>

<h4 id="iaas">IaaS</h4>

<p>IaaS(Infrastructure as a Service)는 Compute, Network, Storage 등의 인프라 자원을 서비스 형태로 제공한다.</p>

<p><strong>예시</strong></p>

<ul>
  <li>AWS EC2</li>
  <li>Google Cloud Compute Engine</li>
  <li>Azure Virtual Machines</li>
</ul>

<h4 id="container-관련-managed-service">Container 관련 Managed Service</h4>

<p>Container 또는 Kubernetes 기반 애플리케이션을 운영할 수 있도록 관리형 환경을 제공한다.</p>

<p><strong>예시</strong></p>

<ul>
  <li>AWS ECS</li>
  <li>AWS EKS</li>
  <li>Google Kubernetes Engine(GKE)</li>
  <li>Azure Kubernetes Service(AKS)</li>
</ul>

<p>이러한 서비스를 넓은 의미에서 CaaS(Container as a Service) 범주로 설명하기도 하지만, 각 Cloud Provider가 모든 서비스를 공식적으로 CaaS라는 동일한 제품 분류로 정의하는 것은 아니다.</p>

<h4 id="paas">PaaS</h4>

<p>PaaS(Platform as a Service)는 인프라뿐 아니라 애플리케이션을 개발하고 실행하기 위한 Runtime과 관리 환경까지 제공하는 서비스 모델이다.</p>

<p><strong>예시</strong></p>

<ul>
  <li>Azure App Service</li>
  <li>Google App Engine</li>
  <li>AWS Elastic Beanstalk</li>
  <li>Cloud Foundry</li>
  <li>Heroku</li>
</ul>

<h3 id="msa-플랫폼-패턴">MSA 플랫폼 패턴</h3>

<p>인프라를 준비한 이후에는 여러 Micro Service를 운영하고 관리하기 위한 플랫폼 구조가 필요하다.</p>

<h4 id="devops-환경과-배포-pipeline">DevOps 환경과 배포 Pipeline</h4>

<p>서비스의 Build, Test, Packaging, Deployment 과정을 자동화하는 환경을 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source
  ↓
Build
  ↓
Test
  ↓
Package
  ↓
Registry
  ↓
Deploy
</code></pre></div></div>

<h4 id="service-registry와-service-discovery">Service Registry와 Service Discovery</h4>

<p>Cloud와 Container 환경에서는 서비스 Instance가 Scale-out되거나 재시작되면서 IP 주소가 동적으로 변경될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A

10.0.0.10
      ↓ 재시작
10.0.1.27
</code></pre></div></div>

<p>Client가 각 Instance의 IP를 직접 알고 호출하는 방식은 관리하기 어렵다.</p>

<p><strong>Service Registry</strong></p>

<p>Service 이름과 현재 접근 가능한 Instance 정보를 관리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>order-service
 ├─ 10.0.1.10
 ├─ 10.0.1.11
 └─ 10.0.1.12
</code></pre></div></div>

<p><strong>Service Discovery</strong></p>

<p>호출하려는 Service의 위치를 Registry 등에서 찾아 실제 Instance에 연결하는 과정이다.</p>

<p>Instance가 여러 개이면 Load Balancing과 함께 사용하여 요청을 적절하게 분산할 수 있다.</p>

<p>과거 Netflix OSS에서는 다음과 같은 기술이 사용되었다.</p>

<ul>
  <li>Zuul: Gateway 및 Routing</li>
  <li>Ribbon: Client-side Load Balancing</li>
</ul>

<p>Ribbon은 현재 Maintenance Mode이므로 현대적인 구현을 설명할 때에는 역사적인 Netflix OSS 사례로 이해하는 것이 적절하다.</p>

<p>Kubernetes에서는 <strong>Service와 DNS(CoreDNS)</strong> 를 통해 Service Discovery를 제공할 수 있다.</p>

<h4 id="api-gateway-pattern">API Gateway Pattern</h4>

<p>Client가 여러 Micro Service를 각각 직접 호출하면 복잡한 호출 관계가 형성될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client ─→ Service A
      ├─→ Service B
      ├─→ Service C
      └─→ Service D
</code></pre></div></div>

<p>API Gateway는 외부 요청을 위한 단일 진입점을 제공한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                  ┌→ Service A
Client → Gateway ─┼→ Service B
                  └→ Service C
</code></pre></div></div>

<p>Gateway에서는 다음과 같은 기능을 수행할 수 있다.</p>

<ul>
  <li>Routing</li>
  <li>인증 및 인가</li>
  <li>Rate Limiting</li>
  <li>Monitoring</li>
  <li>Trace 연계</li>
  <li>장애 대응</li>
  <li>Backend Service 선택</li>
</ul>

<p>관련 기술에는 다음이 있다.</p>

<ul>
  <li>Spring Cloud Gateway</li>
  <li>Cloud Provider의 API Gateway 서비스</li>
  <li>Kubernetes Gateway / Ingress 기반 구성</li>
  <li>Istio Gateway</li>
</ul>

<p>Kubernetes의 Service와 Ingress 자체를 일반적인 API Gateway와 완전히 동일한 개념으로 보기는 어렵고, 외부 Traffic Routing을 구성하는 기반 요소로 이해하는 것이 적절하다.</p>

<h4 id="bff-pattern">BFF Pattern</h4>

<p>최근에는 Web, Mobile, IoT 등 Client의 종류가 다양하다.</p>

<p>각 Client가 필요로 하는 데이터와 API 형태가 다를 수 있기 때문에 하나의 Backend API를 모든 Client에 동일하게 제공하면 불필요한 데이터나 복잡한 Client Logic이 발생할 수 있다.</p>

<p>BFF(Backend For Frontend)는 <strong>Client 종류마다 최적화된 Backend 또는 Gateway 계층을 구성하는 패턴</strong>이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Web Client    → Web BFF    ─┐
Mobile Client → Mobile BFF ─┼→ Micro Services
Other Client  → Other BFF  ─┘
</code></pre></div></div>

<h3 id="외부-구성-저장소-패턴">외부 구성 저장소 패턴</h3>

<p>Cloud 환경에서는 다음과 같은 설정값이 자주 변경될 수 있다.</p>

<ul>
  <li>Database 연결 정보</li>
  <li>외부 API 주소</li>
  <li>File Storage 정보</li>
  <li>Service Endpoint</li>
  <li>Application 설정</li>
</ul>

<p>이러한 정보를 애플리케이션 코드나 Image에 직접 포함하면 설정을 변경할 때 애플리케이션을 다시 Build 또는 Deploy해야 할 수 있다.</p>

<p>여러 Micro Service가 동일한 설정을 사용한다면 서비스별 설정이 서로 달라지는 문제도 발생할 수 있다.</p>

<p>따라서 <strong>환경별 설정을 애플리케이션 코드에서 분리하여 외부에서 관리</strong>할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Configuration Repository
          ↓
 ┌────────┼────────┐
 ↓        ↓        ↓
Service A Service B Service C
</code></pre></div></div>

<p>구현 방법의 예는 다음과 같다.</p>

<ul>
  <li>Git Repository + Spring Cloud Config</li>
  <li>Kubernetes ConfigMap</li>
  <li>외부 Configuration Service</li>
</ul>

<p>Kubernetes의 ConfigMap은 환경별 설정을 Container Image에서 분리하는 데 사용할 수 있다.</p>

<h3 id="인증과-인가-패턴">인증과 인가 패턴</h3>

<p>각 Micro Service가 동일한 인증 및 인가 기능을 반복해서 구현하면 중복이 발생한다.</p>

<p>이를 줄이기 위해 인증 정보를 중앙에서 관리하거나 Gateway 계층에서 공통 처리를 수행할 수 있다.</p>

<h4 id="중앙-집중식-session-관리">중앙 집중식 Session 관리</h4>

<p>각 서비스의 Local Memory에 Session을 저장하는 대신 여러 Instance가 공유할 수 있는 저장소에 Session을 보관한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A ─┐
Service B ─┼→ Redis / Shared Session Store
Service C ─┘
</code></pre></div></div>

<p>Session Store의 예는 다음과 같다.</p>

<ul>
  <li>Redis</li>
  <li>Memcached</li>
</ul>

<h4 id="token-기반-인증">Token 기반 인증</h4>

<p>Token 기반 방식에서는 Client가 인증 결과로 발급받은 Token을 보관하고 요청마다 서버에 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓ Token
API Gateway / Service
</code></pre></div></div>

<p>JWT(JSON Web Token)는 Token을 표현하는 방법 중 하나이다.</p>

<p>JWT 자체가 Session을 중앙 서버에 저장하는 방식과 반드시 함께 사용되는 것은 아니며, <strong>Session 기반 인증과 Token 기반 인증은 별개의 접근 방식</strong>으로 이해해야 한다.</p>

<h4 id="api-gateway와-인증">API Gateway와 인증</h4>

<p>API Gateway에서 공통적인 인증을 수행한 뒤 내부 Service로 인증 정보를 전달하는 구조를 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓
API Gateway
  ↓ 인증/인가
Micro Service
</code></pre></div></div>

<h3 id="service-mesh-pattern">Service Mesh Pattern</h3>

<p>Micro Service가 증가하면 각 서비스에 다음과 같은 공통 Network 기능을 반복해서 구현해야 하는 문제가 발생할 수 있다.</p>

<ul>
  <li>Service Discovery</li>
  <li>Routing</li>
  <li>Load Balancing</li>
  <li>Retry</li>
  <li>Circuit Breaking</li>
  <li>Security</li>
  <li>Telemetry</li>
  <li>Trace</li>
</ul>

<p>Service Mesh는 이러한 <strong>Service-to-Service 통신의 공통 기능을 애플리케이션 비즈니스 로직과 분리하여 Infrastructure 계층에서 처리하는 방식</strong>이다.</p>

<p>대표적인 구현체로 Istio가 있다.</p>

<h4 id="istio-sidecar-mode">Istio Sidecar Mode</h4>

<p>Istio의 Sidecar Mode에서는 애플리케이션 Container와 별도로 Envoy Proxy를 함께 배치한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pod
├─ Application Container
└─ Envoy Sidecar Proxy
</code></pre></div></div>

<p>Service 간 Traffic은 Proxy를 통해 전달되며 다음과 같은 기능을 적용할 수 있다.</p>

<ul>
  <li>Service-to-Service Traffic 관리</li>
  <li>Routing</li>
  <li>Load Balancing</li>
  <li>Circuit Breaking</li>
  <li>Security</li>
  <li>Telemetry</li>
</ul>

<p>Istio는 Sidecar Mode뿐 아니라 Sidecar Proxy를 각 Pod에 삽입하지 않는 Ambient Mode도 지원하므로 <strong>Istio = 반드시 Sidecar 구조</strong>라고 이해해서는 안 된다.</p>

<h3 id="msa-애플리케이션-패턴">MSA 애플리케이션 패턴</h3>

<h4 id="frontend-구성">Frontend 구성</h4>

<p>Frontend 역시 하나의 거대한 애플리케이션으로 구성할 수 있다.</p>

<p><strong>Monolithic Frontend</strong></p>

<p>Frontend 전체를 하나의 Application으로 구성한다.</p>

<p>Micro Service와 유사하게 Frontend를 기능별로 분리하는 방법도 있다.</p>

<p><strong>UI Composition / Micro Frontend</strong></p>

<p>Frontend를 업무 기능 또는 화면 단위로 분리하고 조합하여 하나의 사용자 화면을 구성하는 방식이다.</p>

<h3 id="통신-패턴">통신 패턴</h3>

<p>Micro Service 간 통신 방식은 크게 동기와 비동기로 구분할 수 있다.</p>

<h4 id="동기-통신">동기 통신</h4>

<p>요청을 보낸 서비스가 상대방의 응답을 기다리는 방식이다.</p>

<p>대표적으로 REST API나 gRPC 호출을 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A → Request → Service B
Service A ← Response ← Service B
</code></pre></div></div>

<h4 id="비동기-통신">비동기 통신</h4>

<p>Message Broker 등을 이용하여 메시지를 전달하고 즉시 응답을 기다리지 않는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A
    ↓
Message Queue
    ↓
Service B
</code></pre></div></div>

<p>Cloud 환경에서는 AWS SQS나 SNS 등의 서비스를 이용하여 비동기 통신을 구현할 수 있다.</p>

<h3 id="저장소-분리-패턴">저장소 분리 패턴</h3>

<p>각 Micro Service가 자신의 데이터를 직접 소유하도록 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service A ↔ DB A

Service B ↔ DB B
</code></pre></div></div>

<p>다른 Service가 DB를 직접 접근하지 않도록 하여 서비스의 내부 구현과 데이터 Schema를 캡슐화한다.</p>

<h3 id="분산-transaction-pattern">분산 Transaction Pattern</h3>

<p>여러 Service에 걸친 비즈니스 Transaction을 처리할 때 하나의 Global Transaction 대신 Local Transaction과 Messaging을 조합할 수 있다.</p>

<h4 id="saga-pattern-1">Saga Pattern</h4>

<p>Saga는 여러 개의 분산 Service를 하나의 ACID Transaction으로 묶지 않고 여러 Local Transaction으로 나누어 처리한다.</p>

<p>각 Local Transaction은 자신의 저장소를 갱신한 뒤 다음 작업을 Trigger하기 위한 Message 또는 Event를 발행할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Local Transaction A
        ↓ Event
Local Transaction B
        ↓ Event
Local Transaction C
</code></pre></div></div>

<p>도중에 실패하면 필요한 경우 보상 Transaction을 수행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A 성공
 ↓
B 성공
 ↓
C 실패
 ↓
B 보상
 ↓
A 보상
</code></pre></div></div>

<p>이를 통해 분산 환경에서 비즈니스 정합성을 관리한다.</p>

<h3 id="데이터-일관성에-대한-관점-변화">데이터 일관성에 대한 관점 변화</h3>

<p>서비스별 저장소 구조에서는 모든 데이터가 항상 동일한 순간에 일치하도록 강제하는 대신 일부 업무에서는 결과적 일관성을 허용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>즉각적 강한 일관성

→ 모든 상태가 즉시 일치
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>결과적 일관성

→ 일시적인 불일치 허용
→ 최종적으로 일관된 상태 도달
</code></pre></div></div>

<p>어떤 일관성 모델을 사용할지는 비즈니스 요구사항에 따라 결정해야 한다.</p>

<h3 id="cqrs-pattern">CQRS Pattern</h3>

<p>CQRS(Command Query Responsibility Segregation)는 <strong>명령(Command)과 조회(Query)의 책임을 분리하는 패턴</strong>이다.</p>

<ul>
  <li>Command: 상태를 변경하는 작업</li>
  <li>Query: 데이터를 조회하는 작업</li>
</ul>

<p>기존 방식에서는 하나의 Model과 저장소를 이용하여 Create, Read, Update, Delete를 모두 처리하는 경우가 많다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
     ↓
Single Model
     ↓
Database
</code></pre></div></div>

<p>CQRS에서는 읽기와 쓰기의 책임을 분리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>          ┌→ Command Model → Write
Client ───┤
          └→ Query Model   → Read
</code></pre></div></div>

<p>논리적으로 Model만 분리할 수도 있으며, 필요한 경우 다음과 같이 저장소까지 물리적으로 분리할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Command Service → Write Database

Query Service   → Read Database
</code></pre></div></div>

<p>CQRS의 핵심은 반드시 Database를 두 개 사용한다는 것이 아니라 <strong>읽기와 쓰기의 책임 및 Model을 분리하는 것</strong>이다.</p>

<h3 id="msa-전체-구조">MSA 전체 구조</h3>

<p>앞에서 설명한 요소를 하나의 흐름으로 정리하면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                         Client
                            ↓
                     API Gateway / BFF
                            ↓
              ┌─────────────┼─────────────┐
              ↓             ↓             ↓
          Service A     Service B     Service C
              ↓             ↓             ↓
          Storage A     Storage B     Storage C
              │             │             │
              └────── Message Broker ─────┘

                       Service Mesh
                            │
             Routing / Security / Telemetry
                            │

                Observability Platform
             Log / Metric / Distributed Trace

                            │
                        CI / CD
                            │
                     Cloud / Kubernetes
</code></pre></div></div>

<p>MSA의 핵심은 서비스를 단순히 작은 크기로 나누는 것이 아니다.</p>

<p>독립적인 서비스가 실제 운영 환경에서 유지될 수 있도록 <strong>조직 구조, 배포 자동화, 데이터 관리, 통신, 장애 대응, Observability와 인프라까지 함께 변화해야 한다.</strong></p>

<blockquote>
  <p><strong>정리</strong></p>

  <ul>
    <li>
      <p>Monolithic은 여러 기능을 하나의 애플리케이션과 배포 단위로 구성하며, Micro Service는 비즈니스 기능을 중심으로 독립적인 서비스로 분리한다.</p>
    </li>
    <li>
      <p>Monolithic도 Scale-out이 가능하지만 특정 기능만 독립적으로 확장하기 어렵다는 점에서 Micro Service와 차이가 있다.</p>
    </li>
    <li>
      <p>MSA는 서비스의 독립적인 배포, 느슨한 결합, 비즈니스 기능 중심의 구성과 분권적인 데이터 관리를 중요하게 다룬다.</p>
    </li>
    <li>
      <p>서비스마다 목적에 적합한 언어와 저장 기술을 선택하는 Polyglot Programming과 Polyglot Persistence를 적용할 수 있다.</p>
    </li>
    <li>
      <p>Micro Service를 효과적으로 운영하려면 비즈니스 기능 중심의 팀, Product 중심의 개발, DevOps, CI/CD, IaC 등의 변화가 함께 필요하다.</p>
    </li>
    <li>
      <p>서비스별 저장소를 사용하면 서비스 독립성은 높아지지만 분산 Transaction과 데이터 일관성이라는 새로운 문제가 발생한다.</p>
    </li>
    <li>
      <p>Saga는 여러 서비스를 하나의 Global Transaction으로 묶는 대신 Local Transaction과 보상 Transaction을 이용하여 분산 데이터의 정합성을 관리한다.</p>
    </li>
    <li>
      <p>결과적 일관성은 일시적인 데이터 불일치를 허용하되 최종적으로 일관된 상태에 도달하도록 하는 모델이다.</p>
    </li>
    <li>
      <p>Circuit Breaker는 장애가 발생한 서비스를 계속 호출하지 않고 요청을 일시적으로 차단하여 연쇄 장애를 방지한다.</p>
    </li>
    <li>
      <p>Reactive System은 Responsive, Resilient, Elastic, Message Driven이라는 네 가지 특성을 강조한다.</p>
    </li>
    <li>
      <p>Micro Service 환경에서는 Service Discovery, API Gateway, BFF, 외부 Configuration, 인증·인가, Service Mesh 등의 플랫폼 패턴을 활용할 수 있다.</p>
    </li>
    <li>
      <p>분산된 서비스를 운영하기 위해 Logging, Metric, Distributed Tracing을 포함한 Observability 체계가 중요하다.</p>
    </li>
    <li>
      <p>Service Mesh는 Routing, Load Balancing, Security, Circuit Breaking, Telemetry 등의 공통 Network 기능을 비즈니스 로직과 분리하는 데 사용한다.</p>
    </li>
    <li>
      <p>CQRS는 Command와 Query의 책임을 분리하는 패턴이며, 필요에 따라 읽기와 쓰기의 저장소까지 분리할 수 있다.</p>
    </li>
    <li>
      <p>MSA는 단순히 애플리케이션을 작은 서비스로 나누는 기술이 아니라 조직, 개발, 배포, 데이터, 통신, 장애 대응 및 운영 체계 전체를 함께 고려하는 아키텍처이다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><summary type="html"><![CDATA[Cloud Native 환경에서 비즈니스 민첩성을 높이기 위한 애플리케이션 구조와 Micro Service Architecture의 핵심 원리, 운영 방식, 주요 패턴]]></summary></entry></feed>