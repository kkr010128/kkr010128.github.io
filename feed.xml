<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://kkr010128.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://kkr010128.github.io/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-08-26T08:53:49+09:00</updated><id>https://kkr010128.github.io/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">Kubernetes 개요와 Architecture</title><link href="https://kkr010128.github.io/cloud-native-20-kubernetes-overview/" rel="alternate" type="text/html" title="Kubernetes 개요와 Architecture" /><published>2026-08-25T00:00:00+09:00</published><updated>2026-08-25T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-20-kubernetes-overview</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-20-kubernetes-overview/"><![CDATA[<p>Kubernetes는 여러 Node에서 Container Application을 배포하고, 확장하며, 선언한 상태로 유지하는 Container Orchestration Platform이다. Kubernetes를 이해하려면 먼저 Container Runtime이 Application을 실행하는 범위와 Orchestrator가 Cluster를 관리하는 범위를 구분해야 한다.</p>

<h2 id="1--container-runtime">1 ) Container Runtime</h2>

<hr />

<p>일반적인 Virtual Machine은 Hardware Resource를 가상화하고 각 Virtual Machine에 Guest OS를 실행한다. Container는 Host OS의 Kernel을 공유하면서 Process, File System과 Network를 격리한다. Guest OS 전체를 실행하지 않으므로 상대적으로 적은 Resource로 더 많은 Application을 실행할 수 있다.</p>

<p>Runtime은 Program이 실행되는 환경을 의미한다. JavaScript가 Browser에서 실행된다면 Browser가 Runtime 역할을 한다. 같은 관점에서 <strong>Container Runtime은 Container Image를 가져오고 Container를 생성·실행·감독하는 Software</strong>이다.</p>

<h3 id="containerd">containerd</h3>

<p>containerd는 Container 실행과 Image 관리를 담당하는 Open Source Container Runtime이다.</p>

<ul>
  <li>
    <p>Registry에서 Container Image를 내려받는다.</p>
  </li>
  <li>
    <p>Image Layer의 압축을 풀고 Local Storage에 관리한다.</p>
  </li>
  <li>
    <p>Container를 생성·시작·중지·삭제한다.</p>
  </li>
  <li>
    <p>실행 중인 Container의 상태를 감독한다.</p>
  </li>
</ul>

<p>Docker Engine은 내부적으로 containerd를 사용하며 Kubernetes도 CRI Plugin을 갖춘 containerd를 Runtime으로 사용할 수 있다.</p>

<h3 id="docker-engine">Docker Engine</h3>

<p>Docker Engine은 Image Build, Registry 전송, Network, Volume과 Container Lifecycle을 포함한 개발·운영 기능을 제공한다. containerd보다 넓은 범위를 담당하며 내부의 Container 실행 계층에서 containerd를 사용한다.</p>

<p>Kubernetes <code class="language-plaintext highlighter-rouge">v1.20</code>에서 Docker Engine 연동을 담당하던 Dockershim이 Deprecated되었고 <code class="language-plaintext highlighter-rouge">v1.24</code>에서 제거되었다. 이는 Docker로 만든 Image를 Kubernetes에서 사용할 수 없다는 의미는 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Build
    │
    ▼
OCI 호환 Container Image
    │
    ├── containerd
    ├── CRI-O
    └── Docker Engine + cri-dockerd
</code></pre></div></div>

<p>Docker로 Build한 OCI 호환 Image를 Registry에 Push하면 containerd와 CRI-O를 사용하는 Kubernetes Cluster에서도 실행할 수 있다. Docker Engine을 Kubernetes Runtime으로 연결해야 한다면 외부 CRI Adapter인 <code class="language-plaintext highlighter-rouge">cri-dockerd</code>가 필요하다.</p>

<h3 id="cri-o">CRI-O</h3>

<p>CRI-O는 Kubernetes의 CRI(Container Runtime Interface)를 구현하기 위해 만들어진 Open Source Runtime이다. Kubernetes에서 OCI 호환 Container를 실행하는 데 필요한 범위에 집중한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Docker Engine</th>
      <th>containerd</th>
      <th>CRI-O</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주요 범위</td>
      <td>Image Build·배포·Container 관리</td>
      <td>Image 관리·Container 실행</td>
      <td>Kubernetes용 Container 실행</td>
    </tr>
    <tr>
      <td>Kubernetes 연결</td>
      <td><code class="language-plaintext highlighter-rouge">cri-dockerd</code> 필요</td>
      <td>CRI Plugin 사용</td>
      <td>CRI 직접 구현</td>
    </tr>
    <tr>
      <td>Image Build</td>
      <td>지원</td>
      <td>기본 역할 아님</td>
      <td>기본 역할 아님</td>
    </tr>
    <tr>
      <td>주요 용도</td>
      <td>개발과 Container Platform 운영</td>
      <td>Docker와 Kubernetes의 Runtime</td>
      <td>Kubernetes Runtime</td>
    </tr>
  </tbody>
</table>

<h2 id="2--container-orchestration">2 ) Container Orchestration</h2>

<hr />

<p>Container 수가 많아지고 여러 Machine에 분산되면 개별 명령으로 상태를 관리하기 어렵다. Container Orchestration은 여러 Container를 배치하고 연결하며, 상태를 추적하고, 장애가 발생했을 때 원하는 상태로 복구하는 역할을 수행한다.</p>

<p>주요 기능은 다음과 같다.</p>

<ul>
  <li>
    <p>여러 Node에 Workload를 배치한다.</p>
  </li>
  <li>
    <p>Container 장애를 감지하고 대체 실행 단위를 생성한다.</p>
  </li>
  <li>
    <p>Replica 수를 조정하여 Application을 확장하거나 축소한다.</p>
  </li>
  <li>
    <p>Service Discovery와 Traffic Routing을 제공한다.</p>
  </li>
  <li>
    <p>설정, Secret과 Persistent Storage를 Application에 연결한다.</p>
  </li>
  <li>
    <p>선언한 상태와 실제 상태가 일치하도록 지속적으로 조정한다.</p>
  </li>
</ul>

<h3 id="orchestration-도구">Orchestration 도구</h3>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker Swarm</td>
      <td>Docker Engine에 내장되어 구성과 사용이 비교적 단순</td>
    </tr>
    <tr>
      <td>Apache Mesos</td>
      <td>분산 System Resource를 관리하며 Framework와 결합하여 사용</td>
    </tr>
    <tr>
      <td>Nomad</td>
      <td>HashiCorp가 개발한 Orchestrator로 Consul, Vault와 연동 가능</td>
    </tr>
    <tr>
      <td>Kubernetes</td>
      <td>Container Workload 배포·확장·상태 관리를 위한 CNCF Platform</td>
    </tr>
  </tbody>
</table>

<p>Docker Swarm은 작은 Cluster나 Docker 중심 환경에서 비교적 쉽게 시작할 수 있다. Mesos는 분산 Resource 관리 Framework와 다른 System을 함께 구성해야 하며, Nomad는 Consul의 Service Discovery 및 Vault의 Secret 관리와 결합할 수 있다. Kubernetes는 폭넓은 Cloud·Network·Storage 생태계를 갖추고 있다.</p>

<h2 id="3--kubernetes">3 ) Kubernetes</h2>

<hr />

<p>Kubernetes는 <code class="language-plaintext highlighter-rouge">K8s</code>라고도 표기한다. <code class="language-plaintext highlighter-rouge">K</code>와 <code class="language-plaintext highlighter-rouge">s</code> 사이의 여덟 글자 <code class="language-plaintext highlighter-rouge">ubernete</code>를 숫자 <code class="language-plaintext highlighter-rouge">8</code>로 줄인 표현이다.</p>

<p>Kubernetes에 배포할 Application은 Container 환경에서 독립적으로 실행되고, 외부 설정을 주입받으며, Replica가 교체되어도 Service를 유지할 수 있도록 설계해야 장점을 충분히 활용할 수 있다.</p>

<p>Kubernetes Cluster는 한 대의 Machine으로도 구성할 수 있지만 실제 운영 환경에서는 일반적으로 여러 Node를 사용한다. 각 Node에서 여러 Pod가 실행되며 Kubernetes는 YAML Manifest에 선언한 상태를 기준으로 Pod와 관련 Resource를 생성하고 관리한다.</p>

<h3 id="kubernetes를-사용하는-이유">Kubernetes를 사용하는 이유</h3>

<h4 id="자동화된-container-orchestration">자동화된 Container Orchestration</h4>

<p>Scheduler가 Resource와 조건을 확인하여 Pod를 Node에 배치하고 Controller가 선언한 Replica 수를 유지한다.</p>

<h4 id="service-지속성과-self-healing">Service 지속성과 Self-healing</h4>

<p>Pod나 Container에 장애가 발생하면 Controller와 kubelet이 필요한 복구 작업을 수행한다. Node가 사용할 수 없는 상태가 되면 Controller가 다른 가용 Node에 대체 Pod를 생성할 수 있다.</p>

<p>대체 Pod가 생성되는 것이 기존 Process나 Local Data가 그대로 이동한다는 의미는 아니다. Stateful Application은 Persistent Volume과 Application 수준의 복구 전략이 필요하다.</p>

<h4 id="효율적인-resource-사용">효율적인 Resource 사용</h4>

<p>Container별 CPU와 Memory Request·Limit을 지정할 수 있으며 Scheduler가 가용 Resource를 고려하여 Pod를 배치한다. Service는 변하는 Pod 집합에 안정적인 Endpoint를 제공하고 Ingress 또는 Gateway API는 HTTP·HTTPS Traffic Routing을 구성한다.</p>

<h4 id="유연한-확장">유연한 확장</h4>

<p>Replica 수를 직접 조정하거나 HorizontalPodAutoscaler를 사용하여 Metric에 따라 Workload를 확장·축소할 수 있다.</p>

<h4 id="desired-state-유지">Desired State 유지</h4>

<p>사용자는 YAML Manifest로 원하는 상태를 선언한다. Controller는 실제 Cluster 상태를 관찰하고 필요한 Resource를 생성·수정·삭제하여 두 상태를 일치시킨다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 선언한 상태
Deployment Replica: 3
          │
          ▼
Controller가 실제 상태 관찰
          │
          ├── Pod 2개 → Pod 1개 추가
          └── Pod 4개 → Pod 1개 제거
</code></pre></div></div>

<h4 id="infrastructure-선택의-유연성">Infrastructure 선택의 유연성</h4>

<p>Kubernetes는 On-premises, Virtual Machine과 여러 Public Cloud에서 사용할 수 있다. 다만 Kubernetes를 사용한다고 Application과 Infrastructure가 자동으로 모든 Cloud Provider에서 동일하게 동작하는 것은 아니다. Load Balancer, Storage와 IAM 같은 Provider별 연동 요소를 함께 고려해야 한다.</p>

<h2 id="4--kubernetes-resource">4 ) Kubernetes Resource</h2>

<hr />

<p>Kubernetes에서 상태를 관리하는 대상을 Resource 또는 Object라고 한다. Resource는 API Group과 역할을 기준으로 구분할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>주요 Resource</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Workload</td>
      <td>Pod, ReplicaSet, Deployment, DaemonSet, StatefulSet, Job, CronJob</td>
      <td>Container 실행과 Lifecycle 관리</td>
    </tr>
    <tr>
      <td>Service와 Network</td>
      <td>Service, EndpointSlice, Ingress, NetworkPolicy</td>
      <td>Service Discovery, Traffic 공개와 Network 정책</td>
    </tr>
    <tr>
      <td>Config와 Storage</td>
      <td>ConfigMap, Secret, PersistentVolumeClaim, PersistentVolume, StorageClass</td>
      <td>설정·기밀 정보와 영속 Storage 관리</td>
    </tr>
    <tr>
      <td>Cluster와 접근 제어</td>
      <td>Node, Namespace, ServiceAccount, Role, ClusterRole, RoleBinding, ClusterRoleBinding</td>
      <td>Cluster 구성과 RBAC 관리</td>
    </tr>
    <tr>
      <td>정책과 확장</td>
      <td>ResourceQuota, LimitRange, HorizontalPodAutoscaler, PodDisruptionBudget, CustomResourceDefinition</td>
      <td>Resource 제한·확장·가용성·API 확장</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">ReplicationController</code>도 지원되지만 현재는 일반적으로 ReplicaSet과 Deployment를 사용한다.</p>

<p><code class="language-plaintext highlighter-rouge">ClusterIP</code>, <code class="language-plaintext highlighter-rouge">NodePort</code>, <code class="language-plaintext highlighter-rouge">LoadBalancer</code>, <code class="language-plaintext highlighter-rouge">ExternalName</code>은 각각 별도 Resource가 아니라 Service의 <code class="language-plaintext highlighter-rouge">type</code> 값이다. External IP, Headless Service와 Selector가 없는 Service도 Service를 구성하는 방식이다. Ingress는 Service Type이 아니라 HTTP·HTTPS Routing 규칙을 표현하는 별도 Resource이다.</p>

<h2 id="5--kubernetes-architecture">5 ) Kubernetes Architecture</h2>

<hr />

<p>Kubernetes Cluster는 Control Plane과 Worker Node로 구성된다. 과거에 Master Node라고 부르던 역할은 현재 문서에서 <strong>Control Plane Node</strong>라고 표현한다. 이 글에서는 처음 역할을 대응할 때 <code class="language-plaintext highlighter-rouge">Master(Control Plane)</code>로 함께 표기하고 이후에는 Control Plane을 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes Cluster
├── Control Plane Node
│   ├── kube-apiserver
│   ├── etcd
│   ├── kube-scheduler
│   └── kube-controller-manager
│
├── Worker Node 1
│   ├── kubelet
│   ├── Container Runtime
│   ├── Network 구성 요소
│   └── Pod
│
└── Worker Node 2
    ├── kubelet
    ├── Container Runtime
    ├── Network 구성 요소
    └── Pod
</code></pre></div></div>

<h3 id="cluster와-node">Cluster와 Node</h3>

<p>Cluster는 Kubernetes Resource를 함께 관리하는 집합이다. Node는 Pod가 배치되는 Machine이며 물리 Server나 Virtual Machine으로 구성할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>관점</th>
      <th>Control Plane Node</th>
      <th>Worker Node</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주요 책임</td>
      <td>Cluster 상태와 Scheduling 관리</td>
      <td>할당된 Pod 실행</td>
    </tr>
    <tr>
      <td>주요 구성 요소</td>
      <td>API Server, etcd, Scheduler, Controller Manager</td>
      <td>kubelet, Container Runtime, Network 구성 요소</td>
    </tr>
    <tr>
      <td>Application Pod</td>
      <td>기본 kubeadm 구성에서는 일반 Workload Scheduling 제한</td>
      <td>일반적으로 Application Pod 실행</td>
    </tr>
    <tr>
      <td>장애 영향</td>
      <td>API와 Cluster 관리 기능에 영향</td>
      <td>해당 Node의 Workload에 영향</td>
    </tr>
  </tbody>
</table>

<p>운영 Cluster에서는 Control Plane을 여러 대로 구성하여 관리 기능의 고가용성을 확보할 수 있다. 학습 환경에서는 Control Plane 한 대와 Worker 두 대로 기본 구조를 확인할 수 있다.</p>

<h3 id="pod">Pod</h3>

<p>Pod는 Kubernetes에서 생성하고 Scheduling할 수 있는 가장 작은 배포 단위이다. 하나 이상의 Container를 포함하며 같은 Pod의 Container는 다음 Resource를 공유한다.</p>

<ul>
  <li>
    <p>Pod IP Address와 Network Namespace</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">localhost</code> Network</p>
  </li>
  <li>
    <p>연결한 Volume</p>
  </li>
  <li>
    <p>Pod Lifecycle</p>
  </li>
</ul>

<p>같은 Pod에는 Main Application과 그 Application의 동작을 직접 보조하는 Sidecar처럼 <strong>생명주기와 Resource를 밀접하게 공유해야 하는 Container</strong>를 배치한다. Web Application과 Database처럼 독립적으로 확장·배포·복구해야 하는 구성 요소는 일반적으로 서로 다른 Pod와 Workload로 분리한다.</p>

<h3 id="persistent-storage">Persistent Storage</h3>

<p>Pod는 교체될 수 있는 Resource이므로 Container Writable Layer에만 저장한 Data는 Pod와 함께 사라질 수 있다. 중요한 Data는 PersistentVolume과 PersistentVolumeClaim을 통해 Pod 외부의 Storage에 저장한다.</p>

<p>CSI(Container Storage Interface)는 Kubernetes가 다양한 Storage System과 연결되는 표준 Interface이다. 실제 Storage는 Local Disk, NFS, Distributed Storage 또는 Cloud Provider의 Block·File Storage가 될 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Container Runtime은 Image와 Container 실행을 담당하고 Kubernetes는 여러 Node의 Workload를 Orchestration한다.</p>
    </li>
    <li>
      <p>Dockershim은 제거되었지만 Docker로 Build한 OCI Image는 containerd와 CRI-O 환경에서도 사용할 수 있다.</p>
    </li>
    <li>
      <p>Kubernetes는 Desired State, Self-healing, Scheduling, Service Discovery와 Scaling을 제공한다.</p>
    </li>
    <li>
      <p>Control Plane은 Cluster 상태와 배치를 관리하고 Worker Node는 실제 Application Pod를 실행한다.</p>
    </li>
    <li>
      <p>Pod에는 독립된 Application을 무조건 함께 넣는 것이 아니라 생명주기와 Resource를 공유해야 하는 Container를 배치한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Container Runtime과 Orchestration의 개념부터 Kubernetes Resource, Pod, Control Plane과 Worker Node 구조까지 정리]]></summary></entry><entry><title type="html">kubeadm으로 Kubernetes Cluster 구축</title><link href="https://kkr010128.github.io/cloud-native-21-kubeadm-cluster/" rel="alternate" type="text/html" title="kubeadm으로 Kubernetes Cluster 구축" /><published>2026-08-25T00:00:00+09:00</published><updated>2026-08-25T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-21-kubeadm-cluster</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-21-kubeadm-cluster/"><![CDATA[<p>Kubernetes Cluster는 Local 도구, 직접 구성 도구 또는 Public Cloud의 Managed Service를 이용해 구축할 수 있다. 이 글에서는 Linux Virtual Machine 세 대와 <code class="language-plaintext highlighter-rouge">kubeadm</code>을 사용하여 Master(Control Plane) 한 대와 Worker 두 대를 구성한다.</p>

<p>공통 준비는 모든 Node에서 수행하고, Cluster 초기화와 관리는 Control Plane에서, <code class="language-plaintext highlighter-rouge">kubeadm join</code>은 Worker에서 수행한다.</p>

<h2 id="1--kubernetes-cluster-구축-방식">1 ) Kubernetes Cluster 구축 방식</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>예시</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Local Kubernetes</td>
      <td>Docker Desktop, Minikube, kind</td>
      <td>한 대의 개발 Machine에서 학습·개발·CI 용도로 사용</td>
    </tr>
    <tr>
      <td>경량 Distribution</td>
      <td>k3s, k0s</td>
      <td>설치와 운영 구성을 단순화한 Kubernetes Distribution</td>
    </tr>
    <tr>
      <td>직접 구성</td>
      <td>kubeadm, kOps, Kubespray</td>
      <td>On-premises 또는 Cloud VM에 Cluster 직접 구성</td>
    </tr>
    <tr>
      <td>Managed Kubernetes</td>
      <td>EKS, AKS, GKE, NKS</td>
      <td>Cloud Provider가 Control Plane 운영을 관리</td>
    </tr>
  </tbody>
</table>

<h3 id="local-kubernetes">Local Kubernetes</h3>

<ul>
  <li>
    <p>Docker Desktop은 Desktop 환경에서 Kubernetes Cluster를 활성화할 수 있다.</p>
  </li>
  <li>
    <p>Minikube는 Container, Virtual Machine 또는 Bare Metal Driver를 사용하여 Local Cluster를 만든다.</p>
  </li>
  <li>
    <p>kind(Kubernetes IN Docker)는 Kubernetes Node를 Container로 실행하며 Local 개발과 CI Test에 활용할 수 있다.</p>
  </li>
  <li>
    <p>k3s와 k0s는 비교적 간단하게 설치할 수 있는 Kubernetes Distribution이다.</p>
  </li>
</ul>

<h3 id="직접-구성과-managed-service">직접 구성과 Managed Service</h3>

<p><code class="language-plaintext highlighter-rouge">kubeadm</code>은 Kubernetes가 제공하는 Cluster Bootstrap 도구이다. Control Plane과 Worker를 직접 구성하면서 각 Node의 역할과 Component를 확인하기에 적합하다. kOps와 Kubespray 같은 도구는 Infrastructure 구성이나 반복 설치 자동화에 활용할 수 있다.</p>

<p>AWS EKS, Microsoft AKS, Google GKE와 NHN Cloud NKS 같은 Managed Kubernetes는 Control Plane 운영의 많은 부분을 Cloud Provider가 담당한다. SUSE Rancher와 Red Hat OpenShift는 Kubernetes 기반 Platform을 구축하고 관리하는 데 사용할 수 있다.</p>

<h2 id="2--실습-cluster-구성">2 ) 실습 Cluster 구성</h2>

<hr />

<p>서로 통신할 수 있는 Linux Virtual Machine 세 대를 준비한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes Cluster
├── master   192.168.0.100   Control Plane
├── worker1  192.168.0.101   Worker Node
└── worker2  192.168.0.102   Worker Node
</code></pre></div></div>

<p>IP Address는 내 현재 환경에 맞춘 예시이다. 실제 Network에 맞는 고정 Address를 사용하고 모든 Node에서 서로 통신할 수 있어야 한다.</p>

<h3 id="node별-역할">Node별 역할</h3>

<table>
  <thead>
    <tr>
      <th>작업</th>
      <th>Master(Control Plane)</th>
      <th>Worker</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Hostname·Network 설정</td>
      <td>수행</td>
      <td>수행</td>
    </tr>
    <tr>
      <td>Kernel·Swap 설정</td>
      <td>수행</td>
      <td>수행</td>
    </tr>
    <tr>
      <td>Container Runtime 설치</td>
      <td>수행</td>
      <td>수행</td>
    </tr>
    <tr>
      <td>kubelet·kubeadm 설치</td>
      <td>수행</td>
      <td>수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubeadm init</code></td>
      <td>수행</td>
      <td>수행하지 않음</td>
    </tr>
    <tr>
      <td>Kubeconfig 설정</td>
      <td>수행</td>
      <td>일반적으로 수행하지 않음</td>
    </tr>
    <tr>
      <td>CNI 설치</td>
      <td>수행</td>
      <td>수행하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubeadm join</code></td>
      <td>수행하지 않음</td>
      <td>수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubectl get nodes</code></td>
      <td>수행</td>
      <td>Kubeconfig가 있다면 가능</td>
    </tr>
  </tbody>
</table>

<h3 id="최소-resource와-network">최소 Resource와 Network</h3>

<ul>
  <li>
    <p>모든 Machine에 2GiB 이상의 Memory를 준비한다.</p>
  </li>
  <li>
    <p>Control Plane에는 2개 이상의 CPU Core를 할당한다.</p>
  </li>
  <li>
    <p>Application 실습을 고려하여 Worker에도 CPU, Memory와 Disk를 넉넉하게 할당한다.</p>
  </li>
  <li>
    <p>모든 Node에서 고유한 Hostname, MAC Address와 Product UUID를 사용한다.</p>
  </li>
  <li>
    <p>모든 Node의 시간 동기화와 양방향 Network 통신을 확인한다.</p>
  </li>
</ul>

<p>Control Plane 한 대가 중지되면 Cluster 관리 API를 사용할 수 없으므로 이 구성은 학습용이다. 운영 환경에서는 여러 Control Plane과 Load Balancer를 이용한 고가용성 구성을 검토한다.</p>

<h2 id="3--모든-node-공통-준비">3 ) 모든 Node 공통 준비</h2>

<hr />

<p>이 절의 명령은 <strong>Master와 모든 Worker에서 실행</strong>한다.</p>

<h3 id="hostname-설정">Hostname 설정</h3>

<p>각 Node에서 역할에 맞는 Hostname을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Master에서 실행</span>
<span class="nb">sudo </span>hostnamectl set-hostname master
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Worker 1에서 실행</span>
<span class="nb">sudo </span>hostnamectl set-hostname worker1
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Worker 2에서 실행</span>
<span class="nb">sudo </span>hostnamectl set-hostname worker2
</code></pre></div></div>

<p>모든 Node의 <code class="language-plaintext highlighter-rouge">/etc/hosts</code>에 Master와 Worker의 IP Address 및 Hostname을 등록한다. 기존 <code class="language-plaintext highlighter-rouge">localhost</code> 관련 항목은 유지하고 다음 세 줄을 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Master와 모든 Worker에서 실행</span>
<span class="nb">sudo </span>nano /etc/hosts

→ 127.0.0.1 <span class="o">{</span>hostName<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.168.0.100 master
192.168.0.101 worker1
192.168.0.102 worker2
</code></pre></div></div>

<p>세 Node에 동일한 내용을 등록하면 IP Address 대신 Hostname으로 통신할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ping <span class="nt">-c</span> 3 master
ping <span class="nt">-c</span> 3 worker1
ping <span class="nt">-c</span> 3 worker2
</code></pre></div></div>

<p>고정 IP는 Ubuntu의 경우 <code class="language-plaintext highlighter-rouge">/etc/netplan</code> 아래 YAML File 등 운영체제의 Network 설정 방식으로 구성한다. 설정 후 각 Node에서 다른 Node의 IP Address로 통신되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ping <span class="nt">-c</span> 3 192.168.0.100
ping <span class="nt">-c</span> 3 192.168.0.101
ping <span class="nt">-c</span> 3 192.168.0.102
</code></pre></div></div>

<h3 id="package-업데이트">Package 업데이트</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get upgrade <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> apt-transport-https ca-certificates curl gpg
</code></pre></div></div>

<h3 id="swap-비활성화">Swap 비활성화</h3>

<p>기본 kubelet 설정은 Swap이 활성화된 Node에서 시작을 거부한다. 현재 Session의 Swap을 비활성화하고 재부팅 후에도 다시 활성화되지 않도록 <code class="language-plaintext highlighter-rouge">/etc/fstab</code>을 수정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>swapoff <span class="nt">-a</span>
<span class="nb">sudo sed</span> <span class="nt">-i</span> <span class="s1">'/ swap / s/^\(.*\)$/#\1/g'</span> /etc/fstab
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>swapon <span class="nt">--show</span>
</code></pre></div></div>

<p>출력이 없으면 활성화된 Swap 영역이 없는 상태이다. 기존 <code class="language-plaintext highlighter-rouge">/etc/fstab</code> 항목을 수정하기 전에는 원본을 Backup하고 중복 실행 시 결과를 확인한다.</p>

<h3 id="kernel-module과-sysctl">Kernel Module과 <code class="language-plaintext highlighter-rouge">sysctl</code></h3>

<p>Container Network에서 Overlay File System과 Bridge Traffic 처리를 사용할 수 있도록 Module을 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
</span><span class="no">EOF
</span></code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>modprobe overlay
<span class="nb">sudo </span>modprobe br_netfilter
</code></pre></div></div>

<p>IPv4 Forwarding과 Bridge Traffic의 iptables 처리를 활성화한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
</span><span class="no">EOF
</span></code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sysctl <span class="nt">--system</span>
</code></pre></div></div>

<p>설정값을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sysctl net.bridge.bridge-nf-call-iptables
sysctl net.bridge.bridge-nf-call-ip6tables
sysctl net.ipv4.ip_forward
</code></pre></div></div>

<h2 id="4--모든-node에-containerd-설치">4 ) 모든 Node에 containerd 설치</h2>

<hr />

<p>Pod는 Worker에서 실행되지만 Control Plane도 Kubernetes Component를 Static Pod로 실행할 수 있으므로 <strong>모든 Node에 Container Runtime이 필요</strong>하다.</p>

<h3 id="containerd-설치">containerd 설치</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> containerd
<span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/containerd
containerd config default | <span class="nb">sudo tee</span> /etc/containerd/config.toml
</code></pre></div></div>

<p>Kubernetes의 kubelet과 Runtime이 같은 cgroup Driver를 사용하도록 <code class="language-plaintext highlighter-rouge">/etc/containerd/config.toml</code>에서 <code class="language-plaintext highlighter-rouge">SystemdCgroup</code>을 <code class="language-plaintext highlighter-rouge">true</code>로 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo sed</span> <span class="nt">-i</span> <span class="s1">'s/SystemdCgroup = false/SystemdCgroup = true/'</span> /etc/containerd/config.toml
</code></pre></div></div>

<p>설정이 실제 File에 반영되었는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">grep </span>SystemdCgroup /etc/containerd/config.toml
</code></pre></div></div>

<p>containerd를 재시작하고 Boot 시 자동으로 시작하도록 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart containerd
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>containerd
<span class="nb">sudo </span>systemctl status containerd
</code></pre></div></div>

<p>Distribution에서 제공하는 containerd Version에 따라 설정 File 구조가 다를 수 있다. <code class="language-plaintext highlighter-rouge">SystemdCgroup</code> 항목이 없거나 Service가 시작되지 않으면 설치된 containerd Version의 설정 구조를 먼저 확인한다.</p>

<h2 id="5--모든-node에-kubernetes-package-설치">5 ) 모든 Node에 Kubernetes Package 설치</h2>

<hr />

<p>Kubernetes <code class="language-plaintext highlighter-rouge">v1.30</code>은 지원이 종료된 Version이다. 이 글은 2026년 8월 25일 기준 지원 Branch인 Kubernetes <code class="language-plaintext highlighter-rouge">v1.36</code> Package Repository를 사용한다.</p>

<h3 id="repository-key-등록">Repository Key 등록</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> <span class="nt">-m</span> 755 /etc/apt/keyrings
curl <span class="nt">-fsSL</span> https://pkgs.k8s.io/core:/stable:/v1.36/deb/Release.key <span class="se">\</span>
  | <span class="nb">sudo </span>gpg <span class="nt">--dearmor</span> <span class="nt">-o</span> /etc/apt/keyrings/kubernetes-apt-keyring.gpg
</code></pre></div></div>

<h3 id="repository-등록">Repository 등록</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s1">'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.36/deb/ /'</span> <span class="se">\</span>
  | <span class="nb">sudo tee</span> /etc/apt/sources.list.d/kubernetes.list
</code></pre></div></div>

<p>Kubernetes Package Repository는 Minor Version별로 분리되어 있다. 다른 Version을 사용할 때는 Cluster 전체에서 Version 호환 정책을 확인하고 URL의 Minor Version을 일관되게 변경한다.</p>

<h3 id="kubelet-kubeadm-kubectl-설치">kubelet, kubeadm, kubectl 설치</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> kubelet kubeadm kubectl
<span class="nb">sudo </span>apt-mark hold kubelet kubeadm kubectl
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> kubelet
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Package</th>
      <th>역할</th>
      <th>필요한 Node</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubelet</code></td>
      <td>Node에서 Pod와 Container 상태 관리</td>
      <td>모든 Node</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubeadm</code></td>
      <td>Cluster 초기화와 Node Join</td>
      <td>모든 Node</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kubectl</code></td>
      <td>API Server에 관리 요청 전달</td>
      <td>Control Plane 또는 별도 관리 Client</td>
    </tr>
  </tbody>
</table>

<p>Worker에 <code class="language-plaintext highlighter-rouge">kubectl</code>이 반드시 필요한 것은 아니지만 Package 구성을 동일하게 유지하는 학습 환경에서는 함께 설치할 수 있다. Kubeconfig가 없으면 <code class="language-plaintext highlighter-rouge">kubectl</code>만 설치되어 있어도 Cluster를 관리할 수 없다.</p>

<p>초기화 전 kubelet이 반복 재시작될 수 있다. 아직 Cluster 설정을 받지 못한 상태이므로 <code class="language-plaintext highlighter-rouge">kubeadm init</code> 또는 <code class="language-plaintext highlighter-rouge">kubeadm join</code> 전에는 예상되는 동작이다.</p>

<h2 id="6--방화벽과-port-확인">6 ) 방화벽과 Port 확인</h2>

<hr />

<p>방화벽 전체를 중지하지 않고 Node 역할과 CNI Plugin에 필요한 Port를 허용한다.</p>

<h3 id="control-plane-port">Control Plane Port</h3>

<table>
  <thead>
    <tr>
      <th>Port</th>
      <th>Protocol</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">6443</code></td>
      <td>TCP</td>
      <td>Kubernetes API Server</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">2379-2380</code></td>
      <td>TCP</td>
      <td>etcd Client와 Peer 통신</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">10250</code></td>
      <td>TCP</td>
      <td>kubelet API</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">10257</code></td>
      <td>TCP</td>
      <td>kube-controller-manager</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">10259</code></td>
      <td>TCP</td>
      <td>kube-scheduler</td>
    </tr>
  </tbody>
</table>

<h3 id="worker-port">Worker Port</h3>

<table>
  <thead>
    <tr>
      <th>Port</th>
      <th>Protocol</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">10250</code></td>
      <td>TCP</td>
      <td>kubelet API</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">10256</code></td>
      <td>TCP</td>
      <td>kube-proxy Health Endpoint</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">30000-32767</code></td>
      <td>TCP/UDP</td>
      <td>기본 NodePort 범위</td>
    </tr>
  </tbody>
</table>

<p>Flannel VXLAN은 Node 사이의 UDP <code class="language-plaintext highlighter-rouge">8472</code> 등 별도 Port가 필요할 수 있다. 실제 허용 Port는 사용하는 CNI와 운영체제 방화벽 정책을 함께 확인한다.</p>

<h2 id="7--master에서-control-plane-초기화">7 ) Master에서 Control Plane 초기화</h2>

<hr />

<p>이 절은 <strong>Master(Control Plane)에서만 실행</strong>한다. Worker에서는 실행하지 않는다.</p>

<p>Flannel의 기본 Pod Network CIDR과 일치하도록 <code class="language-plaintext highlighter-rouge">10.244.0.0/16</code>을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm init <span class="nt">--pod-network-cidr</span><span class="o">=</span>10.244.0.0/16
</code></pre></div></div>

<p>여러 Network Interface가 있어 자동 선택한 Address가 적절하지 않다면 Control Plane의 고정 IP를 명시한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm init <span class="se">\</span>
  <span class="nt">--apiserver-advertise-address</span><span class="o">=</span>192.168.0.100 <span class="se">\</span>
  <span class="nt">--pod-network-cidr</span><span class="o">=</span>10.244.0.0/16
</code></pre></div></div>

<p>초기화에 성공하면 다음 정보가 출력된다.</p>

<ul>
  <li>
    <p>현재 사용자의 Kubeconfig 설정 명령</p>
  </li>
  <li>
    <p>CNI 설치 안내</p>
  </li>
  <li>
    <p>Worker가 사용할 <code class="language-plaintext highlighter-rouge">kubeadm join</code> 명령</p>
  </li>
</ul>

<p>Join Token은 Node를 Cluster에 참여시킬 수 있는 인증 정보이므로 공개 Repository나 문서에 실제 값을 저장하지 않는다.</p>

<h3 id="kubeconfig-설정">Kubeconfig 설정</h3>

<p>Master의 일반 사용자 계정에서 <code class="language-plaintext highlighter-rouge">kubectl</code>을 사용하도록 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.kube"</span>
<span class="nb">sudo cp</span> <span class="nt">-i</span> /etc/kubernetes/admin.conf <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.kube/config"</span>
<span class="nb">sudo chown</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">id</span> <span class="nt">-u</span><span class="si">)</span><span class="s2">:</span><span class="si">$(</span><span class="nb">id</span> <span class="nt">-g</span><span class="si">)</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.kube/config"</span>
</code></pre></div></div>

<p>API Server 연결을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl cluster-info
kubectl get nodes
</code></pre></div></div>

<p>CNI 설치 전에는 Node가 <code class="language-plaintext highlighter-rouge">NotReady</code>로 표시될 수 있다.</p>

<h2 id="8--master에서-cni-설치">8 ) Master에서 CNI 설치</h2>

<hr />

<p>Kubernetes는 CNI Specification을 제공하지만 Pod Network 구현을 직접 선택하여 설치해야 한다. 이 실습에서는 Flannel을 사용한다.</p>

<p><strong>실행 위치: Master(Control Plane)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> <span class="se">\</span>
  https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
</code></pre></div></div>

<p>Flannel Manifest의 기본 Network가 <code class="language-plaintext highlighter-rouge">10.244.0.0/16</code>이므로 <code class="language-plaintext highlighter-rouge">kubeadm init</code>의 <code class="language-plaintext highlighter-rouge">--pod-network-cidr</code>와 일치해야 한다. 다른 CNI를 사용한다면 해당 Plugin이 요구하는 CIDR과 설치 절차를 따른다. Cluster에는 일반적으로 하나의 Pod Network를 설치한다.</p>

<p>상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--all-namespaces</span>
kubectl get pods <span class="nt">-n</span> kube-flannel
kubectl get nodes
</code></pre></div></div>

<p>Pod Network가 정상적으로 구성되면 CoreDNS Pod와 Control Plane Node가 <code class="language-plaintext highlighter-rouge">Running</code>, <code class="language-plaintext highlighter-rouge">Ready</code> 상태로 전환된다.</p>

<h2 id="9--worker를-cluster에-연결">9 ) Worker를 Cluster에 연결</h2>

<hr />

<p>이 절은 <strong>각 Worker에서 실행</strong>한다. <code class="language-plaintext highlighter-rouge">kubeadm init</code> 결과에 출력된 Join 명령을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm <span class="nb">join</span> &lt;CONTROL_PLANE_IP&gt;:6443 <span class="se">\</span>
  <span class="nt">--token</span> &lt;TOKEN&gt; <span class="se">\</span>
  <span class="nt">--discovery-token-ca-cert-hash</span> sha256:&lt;HASH&gt;
</code></pre></div></div>

<p>예시의 <code class="language-plaintext highlighter-rouge">&lt;CONTROL_PLANE_IP&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;TOKEN&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;HASH&gt;</code>는 실제 출력값으로 바꾼다. 실제 Token과 Hash를 문서에 고정하여 저장하지 않는다.</p>

<p>Token이 만료되었거나 Join 명령을 잃어버렸다면 Master에서 다시 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubeadm token create <span class="nt">--print-join-command</span>
</code></pre></div></div>

<p>Worker에서 Join이 완료되면 kubelet은 API Server와 통신하고, Scheduler가 해당 Worker에 배치한 Pod를 Container Runtime으로 실행한다.</p>

<h2 id="10--master에서-cluster-확인">10 ) Master에서 Cluster 확인</h2>

<hr />

<p>Worker Join 이후에는 <strong>Master에서</strong> Cluster 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes <span class="nt">-o</span> wide
</code></pre></div></div>

<p>예상 구조는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME      STATUS   ROLES           VERSION
master    Ready    control-plane   v1.36.x
worker1   Ready    &lt;none&gt;          v1.36.x
worker2   Ready    &lt;none&gt;          v1.36.x
</code></pre></div></div>

<p>System Pod가 어느 Node에서 실행되는지도 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> kube-system <span class="nt">-o</span> wide
kubectl get pods <span class="nt">-n</span> kube-flannel <span class="nt">-o</span> wide
</code></pre></div></div>

<h3 id="master와-worker의-동작-흐름">Master와 Worker의 동작 흐름</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>관리 사용자
    │ kubectl 요청
    ▼
Master(Control Plane)
    ├── API Server가 요청 검증 및 저장
    ├── Controller가 원하는 상태 조정
    └── Scheduler가 Worker 선택
                    │
                    ▼
Worker
    ├── kubelet이 할당된 Pod 확인
    ├── Container Runtime이 Container 실행
    └── CNI가 Pod Network 구성
</code></pre></div></div>

<p>Master는 Cluster의 상태와 배치를 결정하고 Worker는 실제 Application Pod를 실행한다. Worker의 kubelet은 API Server에서 자신에게 할당된 PodSpec을 관찰하여 실행 상태를 맞춘다.</p>

<h2 id="11--node-제거와-cluster-reset">11 ) Node 제거와 Cluster Reset</h2>

<hr />

<p>Reset은 Cluster 상태와 Node 설정을 제거하므로 대상 Node와 필요한 Backup을 확인한 뒤 실행한다.</p>

<h3 id="worker-제거">Worker 제거</h3>

<p>먼저 Master에서 대상 Worker의 Workload를 비우고 Node Object를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl drain &lt;WORKER_NAME&gt; <span class="se">\</span>
  <span class="nt">--ignore-daemonsets</span> <span class="se">\</span>
  <span class="nt">--delete-emptydir-data</span>
kubectl delete node &lt;WORKER_NAME&gt;
</code></pre></div></div>

<p>대상 Worker에서 kubeadm 상태를 초기화한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm reset <span class="nt">-f</span>
</code></pre></div></div>

<h3 id="전체-학습-cluster-초기화">전체 학습 Cluster 초기화</h3>

<p>Worker를 먼저 Reset한 뒤 Master에서 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>kubeadm reset <span class="nt">-f</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubeadm reset</code>은 CNI 설정 Directory와 사용자 Kubeconfig를 자동으로 모두 제거하지 않는다. 같은 Machine을 새 Cluster에 재사용하면서 CNI를 변경한다면 <code class="language-plaintext highlighter-rouge">/etc/cni/net.d</code>의 내용을 Backup하고 대상을 확인한 후 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo rm</span> <span class="nt">-rf</span> /etc/cni/net.d
</code></pre></div></div>

<p>Cluster가 더 이상 존재하지 않고 Admin Kubeconfig가 필요하지 않은지 확인한 뒤 현재 사용자의 설정 File을 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.kube/config"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">/etc/kubernetes/*</code>, <code class="language-plaintext highlighter-rouge">/var/lib/kubelet/*</code>, <code class="language-plaintext highlighter-rouge">~/.kube</code>를 직접 모두 삭제하면 필요한 File까지 광범위하게 제거할 수 있다. <code class="language-plaintext highlighter-rouge">kubeadm reset</code> 결과와 Machine 재사용 목적을 확인한 뒤 필요한 항목만 정리한다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Host, Kernel, Swap, Container Runtime과 Kubernetes Package 준비는 Master와 Worker 모두에서 수행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubeadm init</code>, Kubeconfig 설정과 CNI 설치는 Master(Control Plane)에서 수행한다.</p>
    </li>
    <li>
      <p>Worker는 Master가 생성한 Token과 CA Hash를 사용하여 <code class="language-plaintext highlighter-rouge">kubeadm join</code>을 실행한다.</p>
    </li>
    <li>
      <p>Master는 API와 Cluster 상태를 관리하고 Worker는 kubelet과 Container Runtime으로 실제 Pod를 실행한다.</p>
    </li>
    <li>
      <p>방화벽 전체를 중지하지 않고 Kubernetes Component와 CNI에 필요한 Port를 허용한다.</p>
    </li>
    <li>
      <p>Kubernetes <code class="language-plaintext highlighter-rouge">v1.30</code>은 지원이 종료되었으므로 이 글의 설치 명령은 <code class="language-plaintext highlighter-rouge">v1.36</code> Repository를 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><category term="kubeadm" /><summary type="html"><![CDATA[Master와 Worker 공통 준비부터 Control Plane 초기화, CNI 설치, Worker Join과 Cluster 확인까지 단계별 구성]]></summary></entry><entry><title type="html">Kubernetes Component와 Service Network</title><link href="https://kkr010128.github.io/cloud-native-22-kubernetes-components-networking/" rel="alternate" type="text/html" title="Kubernetes Component와 Service Network" /><published>2026-08-25T00:00:00+09:00</published><updated>2026-08-25T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-22-kubernetes-components-networking</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-22-kubernetes-components-networking/"><![CDATA[<p>Kubernetes에서 사용자가 선언한 Pod가 실행되기까지 Control Plane과 Worker의 여러 Component가 역할을 나누어 동작한다. Pod가 실행된 후에는 Service가 변하는 Pod 집합에 안정적인 Endpoint를 제공하고 CNI 기반 Pod Network가 Node를 넘어 통신할 수 있게 한다.</p>

<h2 id="1--kubernetes-component-배치">1 ) Kubernetes Component 배치</h2>

<hr />

<p>Control Plane은 Cluster 전체의 상태와 Scheduling을 관리하고 Worker는 할당된 Pod를 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Control Plane
├── kube-apiserver
├── etcd
├── kube-scheduler
└── kube-controller-manager

Worker Node
├── kubelet
├── Container Runtime
├── kube-proxy 또는 Service Proxy 구현
└── CNI 기반 Pod Network
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Component</th>
      <th>주요 실행 위치</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>kubectl</td>
      <td>관리 Client</td>
      <td>API Server에 요청 전달</td>
    </tr>
    <tr>
      <td>kube-apiserver</td>
      <td>Control Plane</td>
      <td>Kubernetes API 제공, 요청 인증·인가·검증</td>
    </tr>
    <tr>
      <td>etcd</td>
      <td>Control Plane</td>
      <td>Cluster 상태 저장</td>
    </tr>
    <tr>
      <td>kube-scheduler</td>
      <td>Control Plane</td>
      <td>Scheduling되지 않은 Pod의 실행 Node 선택</td>
    </tr>
    <tr>
      <td>kube-controller-manager</td>
      <td>Control Plane</td>
      <td>여러 Controller의 Reconciliation Loop 실행</td>
    </tr>
    <tr>
      <td>kubelet</td>
      <td>모든 Node</td>
      <td>자신에게 할당된 Pod 실행 상태 유지</td>
    </tr>
    <tr>
      <td>Container Runtime</td>
      <td>모든 Node</td>
      <td>Image와 Container 실행 관리</td>
    </tr>
    <tr>
      <td>kube-proxy</td>
      <td>일반적으로 각 Node</td>
      <td>Service용 Network Rule 관리</td>
    </tr>
  </tbody>
</table>

<h2 id="2--kubectl과-api-server">2 ) kubectl과 API Server</h2>

<hr />

<h3 id="kubectl">kubectl</h3>

<p><code class="language-plaintext highlighter-rouge">kubectl</code>은 Kubernetes API를 호출하는 Command Line Client이다. 독립된 Binary이므로 반드시 Control Plane Node에서 실행할 필요는 없다.</p>

<p>다음 정보가 담긴 Kubeconfig가 있으면 별도 관리 PC나 다른 Node에서도 사용할 수 있다.</p>

<ul>
  <li>
    <p>API Server 주소</p>
  </li>
  <li>
    <p>Cluster 인증서 정보</p>
  </li>
  <li>
    <p>사용자 인증 정보</p>
  </li>
  <li>
    <p>Namespace와 Context</p>
  </li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config current-context
kubectl cluster-info
kubectl get nodes
</code></pre></div></div>

<h3 id="kube-apiserver">kube-apiserver</h3>

<p>API Server는 Kubernetes Control Plane의 Frontend이다. kubectl, Controller, Scheduler, kubelet과 외부 Client는 API Server를 통해 Cluster 상태를 조회하거나 변경한다.</p>

<p>주요 역할은 다음과 같다.</p>

<ul>
  <li>
    <p>요청한 사용자를 인증한다.</p>
  </li>
  <li>
    <p>RBAC 등으로 요청 권한을 확인한다.</p>
  </li>
  <li>
    <p>Resource 형식과 정책을 검증한다.</p>
  </li>
  <li>
    <p>Admission Control을 적용한다.</p>
  </li>
  <li>
    <p>유효한 Resource 상태를 etcd에 저장하고 Client에 응답한다.</p>
  </li>
</ul>

<p>API Server가 Worker의 kubelet에 Container 생성 명령을 직접 전달하는 구조는 아니다. 각 Component가 API Server를 중심으로 원하는 상태와 현재 상태를 관찰하고 자신의 역할을 수행한다.</p>

<h2 id="3--etcd-scheduler와-controller-manager">3 ) etcd, Scheduler와 Controller Manager</h2>

<hr />

<h3 id="etcd">etcd</h3>

<p>etcd는 Kubernetes Cluster의 상태 정보를 저장하는 분산 Key-value Database이다.</p>

<ul>
  <li>
    <p>Pod, Deployment, Service와 Node 같은 Resource의 선언 상태를 저장한다.</p>
  </li>
  <li>
    <p>Control Plane Component는 API Server를 통해 Cluster 상태를 읽고 갱신한다.</p>
  </li>
  <li>
    <p>운영 환경에서는 etcd Backup과 복구 절차가 Cluster 복구의 핵심이 된다.</p>
  </li>
</ul>

<p>API Server가 Pod Object를 etcd에 저장했다고 해서 Container가 이미 실행된 것은 아니다. 저장된 PodSpec을 Scheduler와 kubelet이 관찰하고 후속 작업을 수행해야 실제 Container가 실행된다.</p>

<h3 id="kube-scheduler">kube-scheduler</h3>

<p>Scheduler는 아직 Node가 정해지지 않은 Pod를 관찰하고 실행할 Node를 선택한다.</p>

<p>주요 판단 요소는 다음과 같다.</p>

<ul>
  <li>
    <p>Node의 가용 CPU와 Memory</p>
  </li>
  <li>
    <p>Pod의 Resource Request</p>
  </li>
  <li>
    <p>Node Selector와 Node Affinity</p>
  </li>
  <li>
    <p>Taint와 Toleration</p>
  </li>
  <li>
    <p>Pod Affinity와 Anti-affinity</p>
  </li>
  <li>
    <p>Volume과 Topology 조건</p>
  </li>
</ul>

<p>Scheduler는 Container를 직접 실행하지 않는다. 선택한 Node 정보를 Pod에 Binding하면 해당 Worker의 kubelet이 Pod 실행을 담당한다.</p>

<h3 id="kube-controller-manager">kube-controller-manager</h3>

<p>Controller Manager는 여러 Controller를 하나의 Process에서 실행한다. Controller는 선언된 상태와 실제 상태를 반복해서 비교하고 필요한 변경을 수행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>선언 상태: Replica 3개
실제 상태: Pod 2개
        │
        ▼
Controller가 차이 감지
        │
        ▼
새 Pod Object 생성
</code></pre></div></div>

<p>Controller는 Resource를 지속적으로 관찰하고 장애나 변경으로 상태 차이가 생기면 Reconciliation을 수행한다.</p>

<h2 id="4--worker의-kubelet과-container-runtime">4 ) Worker의 kubelet과 Container Runtime</h2>

<hr />

<h3 id="kubelet">kubelet</h3>

<p>kubelet은 각 Node에서 실행되는 Agent이다. API Server를 통해 자신에게 할당된 PodSpec을 확인하고 Container Runtime을 사용하여 Pod의 Container가 실행되도록 한다.</p>

<p>주요 역할은 다음과 같다.</p>

<ul>
  <li>
    <p>Pod에 필요한 Image와 Container 실행 요청</p>
  </li>
  <li>
    <p>Volume Mount</p>
  </li>
  <li>
    <p>Liveness, Readiness와 Startup Probe 실행</p>
  </li>
  <li>
    <p>Pod와 Node 상태를 API Server에 보고</p>
  </li>
  <li>
    <p>실패한 Container의 Restart Policy 적용</p>
  </li>
</ul>

<p>kubelet은 자신이 관리하도록 할당된 Pod를 대상으로 동작하며 Kubernetes 밖에서 별도로 실행한 Container를 일반적인 Pod처럼 관리하지 않는다.</p>

<h3 id="container-runtime">Container Runtime</h3>

<p>Container Runtime은 kubelet의 요청을 CRI를 통해 받아 Image를 준비하고 Container를 실행한다. containerd와 CRI-O가 대표적인 CRI Runtime이다.</p>

<h3 id="pod-생성-흐름">Pod 생성 흐름</h3>

<p>Deployment를 생성했을 때 Master와 Worker의 동작을 순서대로 보면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. kubectl이 Deployment Manifest를 API Server에 전달
2. API Server가 인증·인가·검증 후 상태 저장
3. Deployment Controller가 ReplicaSet 생성
4. ReplicaSet Controller가 필요한 Pod Object 생성
5. Scheduler가 Pod를 실행할 Worker 선택
6. 선택된 Worker의 kubelet이 PodSpec 확인
7. Container Runtime이 Image를 준비하고 Container 실행
8. CNI Plugin이 Pod Network 구성
9. kubelet이 Pod 상태를 API Server에 보고
</code></pre></div></div>

<p>Control Plane은 상태를 저장하고 배치를 결정하며 Worker는 실제 Container와 Network를 구성한다.</p>

<h2 id="5--kube-proxy와-service-구현">5 ) kube-proxy와 Service 구현</h2>

<hr />

<p>kube-proxy는 Kubernetes Service 개념의 일부를 구현하는 Network Proxy이다. 일반적으로 각 Node에서 실행되며 Service의 Virtual IP와 Port로 들어온 Traffic을 Backend Pod로 전달할 수 있도록 Network Rule을 관리한다.</p>

<p>구현 방식에는 iptables, IPVS와 nftables 기반 Mode가 사용될 수 있다. 일부 CNI는 자체 Service Proxy 기능으로 kube-proxy를 대체할 수 있으므로 kube-proxy는 모든 Cluster에 반드시 존재하는 Component는 아니다.</p>

<p>kubeadm 기반 Cluster에서는 일반적으로 kube-proxy가 DaemonSet으로 배포된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get daemonsets <span class="nt">-n</span> kube-system
kubectl get pods <span class="nt">-n</span> kube-system <span class="nt">-o</span> wide
</code></pre></div></div>

<h2 id="6--kubernetes-controller">6 ) Kubernetes Controller</h2>

<hr />

<p>Controller는 Pod를 직접 나열하여 수동 관리하는 대신 Workload의 원하는 상태를 선언하게 한다.</p>

<h3 id="replicaset">ReplicaSet</h3>

<p>ReplicaSet은 Label Selector와 일치하는 Pod가 지정한 수만큼 실행되도록 유지한다. ReplicaSet이 관리하는 동일 구성의 Pod를 Replica라고 한다.</p>

<p>일반적으로 ReplicaSet을 직접 만들기보다 Deployment를 통해 관리한다.</p>

<h3 id="deployment">Deployment</h3>

<p>Deployment는 Stateless Application을 배포할 때 가장 일반적으로 사용하는 Workload Resource이다.</p>

<ul>
  <li>
    <p>ReplicaSet과 Pod를 관리한다.</p>
  </li>
  <li>
    <p>Rolling Update와 Rollback을 제공한다.</p>
  </li>
  <li>
    <p>Replica 수를 변경하여 Scaling할 수 있다.</p>
  </li>
</ul>

<h3 id="daemonset">DaemonSet</h3>

<p>DaemonSet은 조건에 맞는 각 Node에 Pod가 하나씩 실행되도록 관리한다. Node가 추가되면 해당 Node에도 Pod를 배치한다.</p>

<p>주요 사용 사례는 다음과 같다.</p>

<ul>
  <li>
    <p>Log Collector</p>
  </li>
  <li>
    <p>Node Monitoring Agent</p>
  </li>
  <li>
    <p>CNI와 Storage의 Node Agent</p>
  </li>
</ul>

<h3 id="statefulset">StatefulSet</h3>

<p>StatefulSet은 순서와 고유한 식별자가 필요한 Stateful Application을 관리한다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">pod-name-0</code>, <code class="language-plaintext highlighter-rouge">pod-name-1</code>과 같은 안정적인 Pod 이름을 유지한다.</p>
  </li>
  <li>
    <p>순서가 있는 배포와 Scaling을 제공한다.</p>
  </li>
  <li>
    <p>VolumeClaimTemplate으로 Pod별 PersistentVolumeClaim을 생성할 수 있다.</p>
  </li>
</ul>

<p>Pod가 다시 Scheduling되어도 논리적 식별자는 유지되지만 Data 보존 여부는 연결한 Persistent Storage에 달려 있다.</p>

<h3 id="job과-cronjob">Job과 CronJob</h3>

<p>Job은 지정한 작업이 성공적으로 완료되도록 하나 이상의 Pod를 실행한다. CronJob은 Schedule에 따라 Job을 반복 생성한다.</p>

<table>
  <thead>
    <tr>
      <th>Controller</th>
      <th>목적</th>
      <th>대표 사례</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ReplicaSet</td>
      <td>지정한 Pod 수 유지</td>
      <td>Deployment의 하위 Controller</td>
    </tr>
    <tr>
      <td>Deployment</td>
      <td>Stateless Application 배포</td>
      <td>Web API, Frontend</td>
    </tr>
    <tr>
      <td>DaemonSet</td>
      <td>각 Node에 Pod 실행</td>
      <td>Log·Monitoring Agent</td>
    </tr>
    <tr>
      <td>StatefulSet</td>
      <td>식별자와 Storage가 필요한 Workload</td>
      <td>Database, Message Broker</td>
    </tr>
    <tr>
      <td>Job</td>
      <td>완료형 작업</td>
      <td>Batch, Migration</td>
    </tr>
    <tr>
      <td>CronJob</td>
      <td>예약된 Job 실행</td>
      <td>Backup, 정기 Report</td>
    </tr>
  </tbody>
</table>

<h2 id="7--kubernetes-service">7 ) Kubernetes Service</h2>

<hr />

<p>Pod는 장애 복구, Scaling과 Update 과정에서 생성되고 삭제되며 IP Address도 변경될 수 있다. Service는 Label Selector로 Pod 집합을 선택하고 Client가 사용할 안정적인 IP Address와 DNS 이름을 제공한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  │
  ▼
Service: web
  ├── Pod: web-a  10.244.1.10
  ├── Pod: web-b  10.244.2.11
  └── Pod: web-c  10.244.2.12
</code></pre></div></div>

<p>Pod가 여러 Worker에 나뉘어 있어도 하나의 Service가 같은 Label의 Pod를 Backend로 관리할 수 있다. EndpointSlice에는 Service가 전달할 Pod Endpoint가 기록된다.</p>

<h3 id="service-type">Service Type</h3>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>접근 범위</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ClusterIP</code></td>
      <td>Cluster 내부</td>
      <td>기본 Type, Cluster 내부 Virtual IP 제공</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">NodePort</code></td>
      <td>Cluster 외부</td>
      <td>각 Node의 지정 Port로 Service 공개</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">LoadBalancer</code></td>
      <td>외부 Load Balancer</td>
      <td>Cloud Provider 또는 Load Balancer 구현과 연동</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ExternalName</code></td>
      <td>외부 DNS 이름</td>
      <td>DNS CNAME으로 외부 FQDN 연결</td>
    </tr>
  </tbody>
</table>

<h4 id="clusterip">ClusterIP</h4>

<p>Cluster 내부 Client가 Service 이름이나 ClusterIP를 사용하여 Backend Pod에 접근한다.</p>

<h4 id="nodeport">NodePort</h4>

<p>각 Node의 IP Address와 할당된 Port를 통해 Service를 외부에 공개한다. 기본 NodePort 범위는 <code class="language-plaintext highlighter-rouge">30000</code>~<code class="language-plaintext highlighter-rouge">32767</code>이다.</p>

<h4 id="loadbalancer">LoadBalancer</h4>

<p>Cloud Provider나 Load Balancer Controller가 외부 Load Balancer를 구성하고 Service에 External IP 또는 Hostname을 제공한다. 지원 여부와 동작은 Cluster 환경에 따라 달라진다.</p>

<h4 id="externalname">ExternalName</h4>

<p>Service DNS 이름을 외부 FQDN의 CNAME으로 연결한다. Pod Proxy나 ClusterIP를 생성하는 방식과 다르다.</p>

<h3 id="그-밖의-service-구성">그 밖의 Service 구성</h3>

<ul>
  <li>
    <p>External IP는 Cluster 관리자가 지정한 외부 IP로 들어온 Traffic을 Service에 연결하는 설정이다.</p>
  </li>
  <li>
    <p>Headless Service는 <code class="language-plaintext highlighter-rouge">clusterIP: None</code>으로 설정하여 단일 Virtual IP 대신 개별 Endpoint의 DNS 정보를 제공한다.</p>
  </li>
  <li>
    <p>Selector가 없는 Service는 Kubernetes 밖의 Backend나 수동으로 관리하는 EndpointSlice와 연결할 수 있다.</p>
  </li>
</ul>

<h2 id="8--ingress">8 ) Ingress</h2>

<hr />

<p>Ingress는 HTTP와 HTTPS의 Hostname 및 Path 기반 Routing 규칙을 정의하는 Resource이다. Ingress Object만 생성해서는 Traffic이 처리되지 않으며 Cluster에 Ingress Controller가 필요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  │
  ▼
Ingress Controller
  ├── app.example.com/api  → api Service
  └── app.example.com/web  → web Service
</code></pre></div></div>

<p>Ingress Controller는 일반적으로 Cluster의 Pod와 Service로 배포되지만 환경에 따라 외부 Load Balancer와 연동할 수 있다. 별도의 전용 Hardware나 Cluster 밖 Node에서만 실행되는 것은 아니다.</p>

<p>Ingress는 주로 HTTP·HTTPS Routing을 담당한다. 더 다양한 Protocol과 Traffic 관리 기능에는 Service Type과 Gateway API 등을 함께 검토한다.</p>

<h2 id="9--pod-network">9 ) Pod Network</h2>

<hr />

<p>Kubernetes Network Model의 기본 원칙은 다음과 같다.</p>

<ul>
  <li>
    <p>각 Pod는 Cluster 전체에서 고유한 IP Address를 가진다.</p>
  </li>
  <li>
    <p>같은 Pod의 Container는 하나의 Network Namespace와 Pod IP를 공유한다.</p>
  </li>
  <li>
    <p>같은 Pod의 Container는 서로 다른 Port를 사용하며 <code class="language-plaintext highlighter-rouge">localhost:PORT</code>로 통신할 수 있다.</p>
  </li>
  <li>
    <p>Network Policy로 제한하지 않았다면 같은 Node와 다른 Node의 Pod가 서로 직접 통신할 수 있어야 한다.</p>
  </li>
  <li>
    <p>Node의 Agent는 해당 Node의 Pod와 통신할 수 있어야 한다.</p>
  </li>
</ul>

<h3 id="pod-내부-통신">Pod 내부 통신</h3>

<p>하나의 Pod에 Application Container와 Sidecar Container가 있으면 두 Container의 IP Address는 같다. 같은 Port를 동시에 사용할 수 없으므로 서로 다른 Port를 열고 <code class="language-plaintext highlighter-rouge">localhost</code>로 통신한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pod 10.244.1.10
├── Application :8080
└── Sidecar     :15000

Application → localhost:15000
</code></pre></div></div>

<h3 id="node와-pod-network">Node와 Pod Network</h3>

<p>Node는 Host Network의 IP Address를 사용하고 Pod는 CNI가 구성한 Pod Network의 IP Address를 사용한다. Pod CIDR은 Cluster와 CNI 설정에 따라 달라지며 항상 특정 <code class="language-plaintext highlighter-rouge">172.x.x.x</code> 대역을 사용하는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Physical or VM Network
├── Worker 1: 192.168.0.101
│   └── Pod Network: 10.244.1.0/24
└── Worker 2: 192.168.0.102
    └── Pod Network: 10.244.2.0/24
</code></pre></div></div>

<p>Kubernetes Network Model은 다른 Node에 있는 Pod 사이의 통신도 요구한다. 실제 Packet 전달 경로는 설치한 CNI 기반 Network 구현이 구성한다.</p>

<h2 id="10--cni와-overlay-network">10 ) CNI와 Overlay Network</h2>

<hr />

<p>CNI(Container Network Interface)는 Container Runtime이 Network Plugin을 호출하는 Interface 규격이다. Kubernetes Node의 Runtime은 CNI Plugin을 이용하여 다음 작업을 수행한다.</p>

<ul>
  <li>
    <p>Pod Network Interface 생성과 삭제</p>
  </li>
  <li>
    <p>Pod IP Address 할당과 회수</p>
  </li>
  <li>
    <p>Route와 Overlay Network 구성</p>
  </li>
  <li>
    <p>Network Namespace 연결</p>
  </li>
  <li>
    <p>Plugin에 따라 NetworkPolicy 적용</p>
  </li>
</ul>

<p>Overlay Network는 Node의 물리 Network 위에 논리적인 Pod Network를 구성한다. 각 Node의 Pod CIDR가 겹치지 않도록 관리하고 다른 Node의 Pod로 Packet을 전달한다.</p>

<h3 id="주요-cni-기반-network-구현">주요 CNI 기반 Network 구현</h3>

<table>
  <thead>
    <tr>
      <th>구현</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Flannel</td>
      <td>비교적 단순한 Pod Network 구성, VXLAN 등 Backend 제공</td>
    </tr>
    <tr>
      <td>Calico</td>
      <td>Routing과 NetworkPolicy 기능 제공</td>
    </tr>
    <tr>
      <td>Cilium</td>
      <td>eBPF 기반 Network, Security와 Observability 제공</td>
    </tr>
    <tr>
      <td>Multus</td>
      <td>하나의 Pod에 여러 Network Interface 연결</td>
    </tr>
  </tbody>
</table>

<p>Kubernetes 자체는 하나의 특정 CNI 구현을 강제하지 않는다. Cluster 요구 사항에 따라 NetworkPolicy, 암호화, 성능, 운영 복잡도와 Cloud 연동을 고려하여 선택한다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>API Server를 중심으로 Control Plane Component와 Worker Component가 Cluster 상태를 관찰하고 역할을 수행한다.</p>
    </li>
    <li>
      <p>Scheduler는 Pod의 Worker를 선택하고 해당 Worker의 kubelet과 Container Runtime이 실제 Container를 실행한다.</p>
    </li>
    <li>
      <p>Deployment, DaemonSet, StatefulSet, Job과 CronJob은 목적에 맞는 Pod 상태를 관리한다.</p>
    </li>
    <li>
      <p>Service는 변하는 Pod 집합에 안정적인 IP와 DNS Endpoint를 제공하며 Ingress는 HTTP·HTTPS Routing 규칙을 정의한다.</p>
    </li>
    <li>
      <p>같은 Pod의 Container는 Network Namespace를 공유하고 다른 Node의 Pod 통신은 CNI 기반 Pod Network가 구현한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><category term="Network" /><summary type="html"><![CDATA[Control Plane과 Worker Component의 동작 흐름, Controller 종류, Service와 Pod Network 및 CNI 구조]]></summary></entry><entry><title type="html">Docker Swarm Ingress가 요청을 Container까지 전달하는 방법</title><link href="https://kkr010128.github.io/notes-07-deepdive-swarm-published-port/" rel="alternate" type="text/html" title="Docker Swarm Ingress가 요청을 Container까지 전달하는 방법" /><published>2026-08-24T00:00:00+09:00</published><updated>2026-08-24T00:00:00+09:00</updated><id>https://kkr010128.github.io/notes-07-deepdive-swarm-published-port</id><content type="html" xml:base="https://kkr010128.github.io/notes-07-deepdive-swarm-published-port/"><![CDATA[<h2 id="1--실습에서-이상한-점을-발견했다">1 ) 실습에서 이상한 점을 발견했다</h2>

<hr />

<p>Docker Swarm에서 Nginx Container는 Worker에만 배치했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> web-alb <span class="se">\</span>
  <span class="nt">--replicas</span> 2 <span class="se">\</span>
  <span class="nt">--publish</span> <span class="nv">published</span><span class="o">=</span>8001,target<span class="o">=</span>80 <span class="se">\</span>
  <span class="nt">--constraint</span> <span class="s1">'node.role==worker'</span> <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>Cluster의 상태는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manager  192.168.0.100  Nginx Container 없음
Worker 1 192.168.0.101  Nginx Container 실행
Worker 2 192.168.0.102  Nginx Container 실행
</code></pre></div></div>

<p>그런데 Manager IP로 접속해도 Nginx Page가 출력됐다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://192.168.0.100:8001
</code></pre></div></div>

<p>Worker IP로 접속한 결과도 같았다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://192.168.0.101:8001
curl http://192.168.0.102:8001
</code></pre></div></div>

<p>Manager에는 Nginx Container가 없는데 누가 요청을 받은 것일까?</p>

<h2 id="2--결론부터-확인해-보자">2 ) 결론부터 확인해 보자</h2>

<hr />

<p>Manager가 Nginx 요청을 직접 처리한 것은 아니다. Manager는 요청을 받은 뒤 실제 Nginx Container가 있는 Worker로 전달했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  │ Manager_IP:8001로 요청
  ▼
Manager
  │ 실제 Nginx Task로 전달
  ▼
Worker 1 또는 Worker 2
  │
  ▼
Nginx Container가 응답
</code></pre></div></div>

<p>이 동작을 가능하게 만드는 기능이 Docker Swarm의 <strong>Routing Mesh</strong>이다.</p>

<p>Routing Mesh가 적용되면 모든 Swarm Node가 Service 요청의 입구가 된다. 요청을 받은 Node에 Container가 없어도 실제 Container가 실행 중인 다른 Node로 요청을 보낼 수 있다.</p>

<h2 id="3--일반-container의-port-연결과-무엇이-다를까">3 ) 일반 Container의 Port 연결과 무엇이 다를까</h2>

<hr />

<p>일반 Container의 Port를 다음과 같이 연결했다고 가정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">-p</span> 8001:80 nginx
</code></pre></div></div>

<p>이 명령은 현재 Host의 8001번 Port를 현재 Host에서 실행 중인 Nginx Container의 80번 Port와 연결한다. 다른 Host의 8001번 Port까지 열리지는 않는다.</p>

<p>Swarm Service에서는 다음 값을 사용했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>published=8001,target=80
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">published=8001</code></td>
      <td>Swarm 외부에서 Service에 접속할 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">target=80</code></td>
      <td>Nginx Container가 실제로 요청을 받는 Port</td>
    </tr>
  </tbody>
</table>

<p>8001번 Port는 특정 Container에 연결된 Port가 아니라 <code class="language-plaintext highlighter-rouge">web-alb</code> Service로 들어가는 공통 입구이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Swarm Service의 8001번 Port
          │
          ├── Worker 1의 Nginx:80
          └── Worker 2의 Nginx:80
</code></pre></div></div>

<p>Replica가 늘어나도 외부에서는 같은 8001번 Port를 사용한다.</p>

<h2 id="4--왜-모든-node가-8001번-port로-요청을-받을까">4 ) 왜 모든 Node가 8001번 Port로 요청을 받을까</h2>

<hr />

<p>Swarm Service의 기본 Port 공개 방식은 <code class="language-plaintext highlighter-rouge">ingress</code> Mode이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>published=8001,target=80,mode=ingress
</code></pre></div></div>

<p>Ingress Mode에서는 모든 Node가 8001번 Port로 들어오는 요청을 받을 수 있도록 Routing Mesh가 구성된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manager:8001  ─┐
Worker1:8001  ─┼─→ web-alb Service
Worker2:8001  ─┘
</code></pre></div></div>

<p>여기서 가장 중요한 차이는 다음과 같다.</p>

<blockquote>
  <p>모든 Node에 Nginx가 실행된다. → X</p>

  <p>모든 Node가 Service 요청의 입구가 된다. → O</p>
</blockquote>

<p>Manager는 Nginx를 실행하지 않는다. Service의 입구 역할만 하며 실제 HTTP 응답은 Worker의 Nginx가 만든다.</p>

<h2 id="5--manager로-들어온-요청은-어떻게-worker를-찾아갈까">5 ) Manager로 들어온 요청은 어떻게 Worker를 찾아갈까</h2>

<hr />

<p>Manager의 8001번 Port로 요청이 들어온 과정을 단계별로 살펴본다.</p>

<h3 id="1-manager가-service-요청을-받는다">1. Manager가 Service 요청을 받는다</h3>

<p>Client가 <code class="language-plaintext highlighter-rouge">192.168.0.100:8001</code>로 요청한다. Manager에는 8001번 Port가 <code class="language-plaintext highlighter-rouge">web-alb</code> Service에 공개됐다는 Network 규칙이 설정되어 있다.</p>

<h3 id="2-ipvs가-nginx-task-하나를-선택한다">2. IPVS가 Nginx Task 하나를 선택한다</h3>

<p>Swarm Node가 Published Port의 요청을 받으면 Linux의 IPVS(커널에서 동작하는 L4 LB)가 해당 Service에서 실행 중인 Task 하나를 선택한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>web-alb Service
   │
   ├── Worker 1의 Task
   └── Worker 2의 Task
</code></pre></div></div>

<p>IPVS는 요청을 어느 Task로 보낼지 결정하는 Load Balancer 역할을 한다.</p>

<h3 id="3-ingress-network로-요청을-전달한다">3. Ingress Network로 요청을 전달한다</h3>

<p>선택된 Task가 다른 Node에 있다면 <code class="language-plaintext highlighter-rouge">ingress</code> Overlay Network를 통해 요청을 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manager
  │ ingress Overlay Network
  ▼
Worker 2
  │
  ▼
Nginx Container:80
</code></pre></div></div>

<p>Overlay Network는 서로 다른 Docker Host의 Container가 같은 가상 Network에 연결된 것처럼 통신하게 한다.</p>

<h3 id="4-nginx가-응답한다">4. Nginx가 응답한다</h3>

<p>Worker의 Nginx Container가 HTTP 요청을 처리하고 응답을 반환한다. Client는 요청이 어느 Worker에서 처리됐는지 알 필요가 없다.</p>

<p>전체 경로를 합치면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  │ Manager_IP:8001
  ▼
Manager의 Routing Mesh
  │
  ▼
IPVS가 Nginx Task 선택
  │
  ▼
Ingress Overlay Network
  │
  ▼
Worker의 Nginx Container:80
</code></pre></div></div>

<h2 id="6--netstat에서-port가-보이지-않을-수도-있다">6 ) <code class="language-plaintext highlighter-rouge">netstat</code>에서 Port가 보이지 않을 수도 있다</h2>

<hr />

<p>일반적으로 Application이 Port를 열면 <code class="language-plaintext highlighter-rouge">netstat</code>이나 <code class="language-plaintext highlighter-rouge">ss</code>에서 해당 Process가 <code class="language-plaintext highlighter-rouge">LISTEN</code> 중인 것으로 나타난다.</p>

<p>Swarm의 Published Port는 일반 Application Process가 직접 Socket을 열고 기다리는 구조와 다르다. Docker의 Network 규칙과 IPVS가 Traffic을 처리하므로 8001번 Port가 일반적인 Listening Socket처럼 보이지 않을 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>netstat <span class="nt">-nlp</span> | <span class="nb">grep </span>8001
</code></pre></div></div>

<p>출력이 없다는 이유만으로 Service Port가 공개되지 않았다고 판단할 수 없다. Published Port는 Manager에서 Service 설정을 직접 확인하는 편이 정확하다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service inspect <span class="se">\</span>
  <span class="nt">--format</span> <span class="s1">''</span> <span class="se">\</span>
  web-alb
</code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
  </span><span class="p">{</span><span class="w">
    </span><span class="nl">"Protocol"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tcp"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"TargetPort"</span><span class="p">:</span><span class="w"> </span><span class="mi">80</span><span class="p">,</span><span class="w">
    </span><span class="nl">"PublishedPort"</span><span class="p">:</span><span class="w"> </span><span class="mi">8001</span><span class="p">,</span><span class="w">
    </span><span class="nl">"PublishMode"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ingress"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<h2 id="7--service-vip와는-어떤-차이가-있을까">7 ) Service VIP와는 어떤 차이가 있을까</h2>

<hr />

<p>Routing Mesh와 Service VIP는 모두 요청을 Replica로 분산하지만 요청이 시작되는 위치가 다르다.</p>

<h3 id="routing-mesh">Routing Mesh</h3>

<p>Swarm 외부의 Client가 Published Port를 통해 들어올 때 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부 Client
→ Node_IP:8001
→ Routing Mesh
→ Nginx Task
</code></pre></div></div>

<h3 id="service-vip">Service VIP</h3>

<p>같은 Overlay Network 안의 다른 Service가 Service 이름으로 접속할 때 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Backend Container
→ web-alb
→ Docker DNS
→ Service VIP
→ Nginx Task
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Routing Mesh</th>
      <th>Service VIP</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>요청 출발</td>
      <td>Swarm 외부</td>
      <td>Swarm 내부 Service</td>
    </tr>
    <tr>
      <td>사용하는 주소</td>
      <td><code class="language-plaintext highlighter-rouge">Node IP:Published Port</code></td>
      <td>Service 이름</td>
    </tr>
    <tr>
      <td>주요 목적</td>
      <td>외부 요청 전달</td>
      <td>Service 간 통신</td>
    </tr>
  </tbody>
</table>

<p>Manager IP로 Nginx에 접속할 수 있었던 이유는 Service VIP가 아니라 Routing Mesh 때문이다.</p>

<h2 id="8--실제-사용자는-어느-node-ip로-접속할까">8 ) 실제 사용자는 어느 Node IP로 접속할까</h2>

<hr />

<p>실습에서는 어느 Node IP를 사용해도 접속할 수 있다. 그러나 실제 사용자에게 여러 Node IP를 직접 알려주지는 않는다. Domain을 Node 하나에만 연결하면 해당 Node가 중단됐을 때 다른 Worker가 정상이어도 사용자가 접속하지 못하기 때문이다.</p>

<p>일반적으로 Swarm 앞에 외부 Load Balancer나 고가용성 VIP를 둔다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자
  │ service.example.com
  ▼
외부 Load Balancer
  │
  ├── Manager:8001
  ├── Worker 1:8001
  └── Worker 2:8001
          │
          ▼
    Swarm Routing Mesh
          │
          ▼
      Nginx Task
</code></pre></div></div>

<p>두 기능은 서로 다른 문제를 해결한다.</p>

<ul>
  <li>
    <p>외부 Load Balancer는 사용자가 접속할 하나의 주소와 Node 장애 대응을 제공한다.</p>
  </li>
  <li>
    <p>Routing Mesh는 Task가 어느 Node에 있는지 외부에서 추적하지 않아도 되게 한다.</p>
  </li>
</ul>

<h2 id="9--reverse-proxy와-routing-mesh는-같은-기능일까">9 ) Reverse Proxy와 Routing Mesh는 같은 기능일까</h2>

<hr />

<p>둘 다 요청을 뒤쪽 Server로 전달하지만 요청을 나누는 기준이 다르다.</p>

<p>Routing Mesh는 Published Port로 들어온 Network Connection을 Service Task 중 하나로 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8001번 Port의 요청
→ web-alb의 Nginx Task 중 하나
</code></pre></div></div>

<p>Nginx 같은 Reverse Proxy는 HTTP 요청을 읽고 Domain이나 URL Path에 따라 Backend를 선택한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/api     → Backend API
/images  → Image Server
</code></pre></div></div>

<p>Reverse Proxy의 목적은 단순히 Backend IP를 숨기는 것이 아니다. TLS 처리, HTTP Routing, Header 변경과 Cache 같은 Application 계층 기능도 담당한다.</p>

<p>Routing Mesh와 Reverse Proxy는 함께 사용할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부 Load Balancer
→ Swarm Routing Mesh
→ Nginx Reverse Proxy Service
→ Backend Service
</code></pre></div></div>

<h2 id="10--모든-node에서-port를-열고-싶지-않다면">10 ) 모든 Node에서 Port를 열고 싶지 않다면</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">mode=host</code>로 Port를 공개하면 Routing Mesh를 우회할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> web-host-mode <span class="se">\</span>
  <span class="nt">--mode</span> global <span class="se">\</span>
  <span class="nt">--publish</span> <span class="nv">published</span><span class="o">=</span>8001,target<span class="o">=</span>80,mode<span class="o">=</span>host <span class="se">\</span>
  nginx
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>접속 상황</th>
      <th>Ingress Mode</th>
      <th>Host Mode</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Task가 있는 Node</td>
      <td>접속 가능</td>
      <td>접속 가능</td>
    </tr>
    <tr>
      <td>Task가 없는 Node</td>
      <td>다른 Node의 Task로 전달</td>
      <td>접속 불가</td>
    </tr>
    <tr>
      <td>Load Balancing</td>
      <td>Routing Mesh가 처리</td>
      <td>별도로 구성</td>
    </tr>
  </tbody>
</table>

<p>Host Mode에서는 외부 Load Balancer가 Task가 실행 중인 Node를 알아야 한다. 같은 Published Port를 사용하는 Task를 한 Node에 여러 개 배치하기도 어렵다.</p>

<p>어느 Node로든 간편하게 요청을 받고 싶다면 Ingress Mode가 적합하다. 요청 경로와 Load Balancing을 직접 제어해야 한다면 Host Mode를 검토한다.</p>

<h2 id="11--다시-처음-질문으로-돌아가면">11 ) 다시 처음 질문으로 돌아가면</h2>

<hr />

<p>Manager에 Nginx Container가 없는데도 <code class="language-plaintext highlighter-rouge">Manager_IP:8001</code>로 접속할 수 있었던 이유는 다음과 같다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">published=8001</code>은 특정 Container가 아니라 Swarm Service에 적용된다.</p>
  </li>
  <li>
    <p>Ingress Mode에서는 모든 Swarm Node가 8001번 Port 요청의 입구가 된다.</p>
  </li>
  <li>
    <p>Manager로 들어온 요청은 IPVS와 Ingress Network를 거쳐 실제 Nginx Task로 전달된다.</p>
  </li>
  <li>
    <p>HTTP 응답은 Manager가 아니라 Worker의 Nginx Container가 생성한다.</p>
  </li>
</ol>

<blockquote>
  <p><strong>Swarm에서는 Container가 없는 Node도 Service의 입구가 될 수 있다.</strong></p>
</blockquote>

<p>이 구조 덕분에 Client는 Nginx Task가 어느 Worker에서 실행 중인지 추적하지 않고 Swarm Node의 Published Port로 요청할 수 있다.</p>]]></content><author><name></name></author><category term="DeepDive" /><category term="Docker" /><category term="DockerSwarm" /><category term="Network" /><summary type="html"><![CDATA[Published Port로 들어온 요청이 Routing Mesh와 IPVS, Ingress Overlay Network를 거쳐 Service Task에 도달하는 과정]]></summary></entry><entry><title type="html">Docker Swarm Cluster 구성과 Service 운영</title><link href="https://kkr010128.github.io/cloud-native-18-docker-swarm-cluster/" rel="alternate" type="text/html" title="Docker Swarm Cluster 구성과 Service 운영" /><published>2026-08-24T00:00:00+09:00</published><updated>2026-08-24T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-18-docker-swarm-cluster</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-18-docker-swarm-cluster/"><![CDATA[<h2 id="1--클러스터-실습-환경-구성">1 ) 클러스터 실습 환경 구성</h2>

<hr />

<p>Ubuntu 24.04 VM 세 대를 같은 Network에 연결하고 한 대는 Manager, 두 대는 Worker로 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Windows Host
LAPTOP
192.168.0.99
       │
       ├── LAB_MASTER
       │   hostname: master
       │   192.168.0.100
       │
       ├── LAB_WORKER_1
       │   hostname: worker1
       │   192.168.0.101
       │
       └── LAB_WORKER_2
           hostname: worker2
           192.168.0.102
</code></pre></div></div>

<p>각 VM은 Bridge Adapter를 사용하여 같은 Network에 연결하고 Host에서 SSH로 접속한다.
세 VM 모두 Docker Engine과 Compose Plugin을 설치해 환경을 준비했다.</p>

<p><img src="../../assets/post/2026-08-24-cloud-native-18-docker-swarm-cluster/02.webp" alt="Manager와 Worker VM에 Docker를 설치하는 화면" /></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="se">\</span>
  docker-ce <span class="se">\</span>
  docker-ce-cli <span class="se">\</span>
  containerd.io <span class="se">\</span>
  docker-buildx-plugin <span class="se">\</span>
  docker-compose-plugin
</code></pre></div></div>

<p>설치 후 Version과 Service 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker version
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker
<span class="nb">sudo </span>systemctl status docker
</code></pre></div></div>

<p>현재 사용자가 <code class="language-plaintext highlighter-rouge">sudo</code> 없이 Docker를 실행하려면 <code class="language-plaintext highlighter-rouge">docker</code> Group에 추가한 뒤 다시 로그인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker <span class="s2">"</span><span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<blockquote>
  <p>Docker Socket의 권한을 <code class="language-plaintext highlighter-rouge">666</code>으로 변경하면 모든 사용자가 Docker Daemon을 제어할 수 있으므로 사용하지 않는다.</p>
</blockquote>

<p>Docker Engine 설치는 모든 Node에서 수행하지만 Cluster를 생성하고 관리하는 명령은 Manager에서 수행한다.</p>

<blockquote>
  <p>Kubernetes도 Container Runtime과 Node 구성 요소는 모든 Machine에 필요하지만 Cluster 제어 명령은 Control Plane을 중심으로 수행한다.</p>
</blockquote>

<h2 id="2--swarm-cluster-구성">2 ) Swarm Cluster 구성</h2>

<hr />

<h3 id="manager-초기화">Manager 초기화</h3>

<p>Manager로 사용할 <code class="language-plaintext highlighter-rouge">master</code>에서 Swarm Mode를 초기화한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker swarm init

<span class="c"># docker swarm init --advertise-addr 192.168.0.100</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker swarm init</code>으로만 실행하면 Docker가 해당 Host의 IP Address를 자동으로 선택해 Swarm Manager를 초기화한다.</p>

<p><code class="language-plaintext highlighter-rouge">--advertise-addr</code>는 다른 Node가 Manager에 접속할 주소를 지정한다.</p>

<p>다른 Swarm 노드들에게 “이 Manager에는 계속 <code class="language-plaintext highlighter-rouge">192.168.0.100</code>으로 접속해”라고 명시하는 것이다.</p>

<p>초기화가 끝나면 현재 Node가 Manager가 되고 Worker가 Cluster에 참여할 때 사용할 <code class="language-plaintext highlighter-rouge">docker swarm join</code> 명령이 출력된다.</p>

<blockquote>
  <p>Kubernetes에서는 <code class="language-plaintext highlighter-rouge">kubeadm init</code>이 Control Plane을 초기화하고 Worker가 사용할 <code class="language-plaintext highlighter-rouge">kubeadm join</code> 명령을 출력한다. Cluster의 제어 주체를 먼저 만들고 다른 Node가 Token을 이용해 참여한다는 흐름은 비슷하다. 다만 Swarm은 Docker Engine에 Orchestrator가 내장되어 있고 Kubernetes는 API Server, Scheduler, Controller Manager 등 별도의 Control Plane 구성 요소를 사용한다.</p>
</blockquote>

<h3 id="worker-참여">Worker 참여</h3>

<p>Manager에서 출력된 명령을 <code class="language-plaintext highlighter-rouge">worker1</code>, <code class="language-plaintext highlighter-rouge">worker2</code>에서 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker swarm <span class="nb">join</span> <span class="se">\</span>
  <span class="nt">--token</span> SWMTKN-1-WORKER_TOKEN <span class="se">\</span>
  192.168.0.100:2377
</code></pre></div></div>

<p><img src="../../assets/post/2026-08-24-cloud-native-18-docker-swarm-cluster/04.webp" alt="Manager 초기화 후 두 Worker가 Swarm에 참여한 화면" /></p>

<p>Manager에서는 Worker Token을 다시 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker swarm join-token worker
</code></pre></div></div>

<p><img src="../../assets/post/2026-08-24-cloud-native-18-docker-swarm-cluster/08.webp" alt="Manager에서 Worker 참여 Token을 확인한 화면" /></p>

<p><code class="language-plaintext highlighter-rouge">docker swarm join-token</code>은 Manager가 가진 Cluster 상태를 조회해야 하므로 Worker에서는 실행할 수 없다.</p>

<blockquote>
  <p>Kubernetes의 Join Token도 Control Plane에서 <code class="language-plaintext highlighter-rouge">kubeadm token create --print-join-command</code>로 다시 만들거나 조회한다.</p>
</blockquote>

<p>Join Token은 Node를 Cluster에 참여시킬 수 있는 인증 정보이므로 공개 저장소나 게시물에 실제 값을 남기지 않는다. 노출되었다면 Token을 교체해야 한다.</p>

<p>물론 참여하기 위해서는 Manager와 네트워크 통신이 가능해야 한다. 하지만 토큰이 공개되어 있으면 공격자가 내부에 접근했을 때 필요한 인증 정보를 이미 확보한 상태가 되기에 인증 정보를 담고 있는 토큰은 비밀 정보로 취급해야 한다.</p>

<h3 id="swarm-port-확인">Swarm Port 확인</h3>

<p>Manager에서 Docker Daemon이 사용하는 Port를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>netstat <span class="nt">-nlp</span> | <span class="nb">grep </span>dockerd
</code></pre></div></div>

<p><img src="../../assets/post/2026-08-24-cloud-native-18-docker-swarm-cluster/06.webp" alt="Manager에서 Docker Swarm 통신 Port를 확인한 화면" /></p>

<p>Swarm Node 사이에는 다음 Port가 필요하다.</p>

<table>
  <thead>
    <tr>
      <th>Port</th>
      <th>Protocol</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">2377</code></td>
      <td>TCP</td>
      <td>Manager의 Cluster 관리 통신</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">7946</code></td>
      <td>TCP/UDP</td>
      <td>Node Discovery와 통신</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">4789</code></td>
      <td>UDP</td>
      <td>Overlay Network Traffic</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Kubernetes도 Control Plane API, kubelet, Service Network 등에 별도의 Port가 필요하지만 Swarm과 Port 번호 및 구성 요소는 다르다.</p>
</blockquote>

<h3 id="node-상태-확인">Node 상태 확인</h3>

<p>Manager에서 Node 목록을 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker node <span class="nb">ls</span>
</code></pre></div></div>

<p><img src="../../assets/post/2026-08-24-cloud-native-18-docker-swarm-cluster/10.webp" alt="Manager와 Worker에서 docker node ls를 실행한 결과" /></p>

<p>같은 명령을 Worker에서 실행하면 <code class="language-plaintext highlighter-rouge">This node is not a swarm manager</code> 오류가 발생한다. <code class="language-plaintext highlighter-rouge">docker node ls</code>는 Cluster 전체 상태를 조회하는 관리 명령이므로 Manager에서만 사용할 수 있다.</p>

<blockquote>
  <p>Kubernetes의 <code class="language-plaintext highlighter-rouge">kubectl get nodes</code>도 API Server를 통해 Cluster 상태를 조회한다. 명령 자체가 Control Plane Machine에서만 실행되어야 하는 것은 아니지만, 외부 Machine이나 Worker에서 실행하려면 API Server 주소와 인증 정보가 담긴 Kubeconfig가 필요하다. Swarm Worker가 관리 API를 제공하지 않는 것과 달리 Kubernetes Client는 인증 설정이 있으면 어느 Machine에서도 API Server에 요청할 수 있다.</p>
</blockquote>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker info
docker network <span class="nb">ls</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker info</code>의 <code class="language-plaintext highlighter-rouge">Swarm: active</code>와 Network 목록의 <code class="language-plaintext highlighter-rouge">ingress</code>를 확인한다.</p>

<h3 id="node-제거">Node 제거</h3>

<p>Worker를 정상적으로 제거할 때는 제거할 Worker에서 Swarm을 떠난 뒤 Manager에서 Node 정보를 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 제거할 Worker에서 실행</span>
docker swarm leave
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Manager에서 실행</span>
docker node <span class="nb">rm </span>worker1
</code></pre></div></div>

<p>Manager가 Swarm을 떠나면 Manager Quorum에 영향을 줄 수 있다. 특히 마지막 Manager에서 <code class="language-plaintext highlighter-rouge">--force</code>로 떠나면 Cluster를 더 이상 관리할 수 없으므로 실습 종료 외에는 신중하게 사용한다.</p>

<h2 id="3--service-생성과-조회">3 ) Service 생성과 조회</h2>

<hr />

<p>Manager에서 반복적으로 Message를 출력하는 Service를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> swarm-start <span class="se">\</span>
  alpine:3 <span class="se">\</span>
  /bin/sh <span class="nt">-c</span> <span class="s2">"while true; do echo 'Docker Swarm Start'; sleep 3; done"</span>
</code></pre></div></div>

<p>Service 상태와 Task 배치를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service <span class="nb">ls
</span>docker service ps swarm-start
docker service logs <span class="nt">-f</span> swarm-start
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker service ls</code>, <code class="language-plaintext highlighter-rouge">docker service ps</code>, <code class="language-plaintext highlighter-rouge">docker service logs</code>는 Cluster Service를 관리하는 명령이므로 Manager에서 실행한다. Worker에서는 자신에게 배치된 Container를 <code class="language-plaintext highlighter-rouge">docker container ls</code>로 확인할 수 있지만 Service의 전체 Replica 상태는 조회할 수 없다.</p>

<blockquote>
  <p>Kubernetes에서는 Deployment를 생성한 뒤 <code class="language-plaintext highlighter-rouge">kubectl get deployments</code>, <code class="language-plaintext highlighter-rouge">kubectl get pods</code>, <code class="language-plaintext highlighter-rouge">kubectl logs</code>로 비슷한 정보를 확인한다. Swarm의 Service는 원하는 Replica 상태를 선언하는 단위이며 Kubernetes에서는 주로 Deployment가 이 역할을 담당한다.</p>
</blockquote>

<p>Service를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service <span class="nb">rm </span>swarm-start
</code></pre></div></div>

<h2 id="4--nginx-service와-routing-mesh">4 ) Nginx Service와 Routing Mesh</h2>

<hr />

<p>Worker에만 Nginx Task가 배치되도록 Constraint를 지정하고 Service Port를 공개한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> web-alb <span class="se">\</span>
  <span class="nt">--replicas</span> 2 <span class="se">\</span>
  <span class="nt">--publish</span> <span class="nv">published</span><span class="o">=</span>8001,target<span class="o">=</span>80 <span class="se">\</span>
  <span class="nt">--constraint</span> <span class="s1">'node.role==worker'</span> <span class="se">\</span>
  nginx
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service ps web-alb
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">node.role==worker</code> Constraint 때문에 Nginx Task는 Manager에 배치되지 않는다. 그러나 Service의 8001번 Port를 공개하면 Manager와 두 Worker를 포함한 모든 Node가 동일한 8001번 Port로 들어오는 Traffic을 받는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://192.168.0.100:8001
curl http://192.168.0.101:8001
curl http://192.168.0.102:8001
</code></pre></div></div>

<p>세 주소 모두 같은 Service에 연결된다. 이것은 모든 Node가 공개 Port를 감시하는 Swarm Routing Mesh의 동작이다. 요청을 받은 Node에 Nginx Task가 없더라도 Ingress Network가 실제 Task가 실행 중인 Node로 Traffic을 전달한다.</p>

<blockquote>
  <p>Kubernetes에서는 Deployment만 생성해도 모든 Node에 같은 외부 Port가 자동으로 열리지 않는다. 외부 접근에는 NodePort, LoadBalancer 또는 Ingress 같은 별도의 Service 노출 방식이 필요하다. NodePort를 사용하면 여러 Node의 동일 Port로 접근할 수 있다는 점은 Swarm Routing Mesh와 비슷하지만, Kubernetes Service와 kube-proxy 또는 CNI가 Traffic 전달을 담당한다는 차이가 있다.</p>
</blockquote>

<h3 id="임시-page-변경">임시 Page 변경</h3>

<p>Task가 배치된 Worker에서 테스트용 <code class="language-plaintext highlighter-rouge">index.html</code>을 만든 뒤 특정 Container에 복사할 수 있다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;h1&gt;</span>Swarm Worker 1<span class="nt">&lt;/h1&gt;</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">ls
</span>docker <span class="nb">cp </span>index.html CONTAINER_ID:/usr/share/nginx/html/index.html
</code></pre></div></div>

<p>이 방법은 특정 Task의 쓰기 Layer만 변경한다. 다른 Replica에는 적용되지 않고 Task가 재생성되면 변경 내용도 사라지므로 Load Balancing을 눈으로 확인하는 임시 실습에만 사용한다. 실제 배포에서는 Page를 포함한 새로운 Image를 Build하여 Service를 Update한다.</p>

<h2 id="5--overlay-network와-service-discovery">5 ) Overlay Network와 Service Discovery</h2>

<hr />

<p>Swarm의 Overlay Network는 여러 Docker Host에 걸쳐 있는 Service를 하나의 가상 Network로 연결한다. 같은 Overlay Network의 Service는 IP를 직접 관리하지 않고 Service 이름으로 통신한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network create <span class="se">\</span>
  <span class="nt">--driver</span> overlay <span class="se">\</span>
  dailylog-net
</code></pre></div></div>

<p>Service에는 Virtual IP가 할당되며 내장 DNS가 Service 이름을 이 주소로 해석한다. Client가 Virtual IP로 요청하면 Swarm이 실행 중인 Replica 중 하나로 전달한다. Replica가 늘거나 다른 Node로 이동해도 Application은 같은 Service 이름을 사용한다.</p>

<blockquote>
  <p>Kubernetes도 Service 이름을 Cluster DNS로 조회하고 고정된 ClusterIP를 통해 여러 Pod로 요청을 전달한다. Service Discovery 관점은 비슷하지만 Swarm은 Service와 Overlay Network를 Docker Engine이 직접 관리하고 Kubernetes는 Service, EndpointSlice, kube-proxy 또는 Network Plugin이 역할을 나누어 담당한다.</p>
</blockquote>

<h2 id="6--service-확장과-배포-mode">6 ) Service 확장과 배포 Mode</h2>

<hr />

<h3 id="replicated-mode">Replicated Mode</h3>

<p>실행 중인 Service의 Replica 수를 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service scale web-alb<span class="o">=</span>5
</code></pre></div></div>

<p>Node 수보다 Replica가 많으면 한 Node에 여러 Task가 배치될 수 있다. Swarm은 Node 수와 Replica 수를 일치시키는 것이 아니라 가용 Resource와 Constraint를 기준으로 Task를 배치한다.</p>

<blockquote>
  <p>Kubernetes의 <code class="language-plaintext highlighter-rouge">kubectl scale deployment ... --replicas=5</code>도 원하는 Pod 수를 변경한다. 두 도구 모두 선언한 수와 실제 실행 수가 같아지도록 조정한다.</p>
</blockquote>

<h3 id="global-mode">Global Mode</h3>

<p>조건에 맞는 모든 Node에 Task를 하나씩 배치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> global-nginx <span class="se">\</span>
  <span class="nt">--mode</span> global <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>Global Mode는 각 Node에서 동작해야 하는 Monitoring Agent나 Log Collector에 적합하다. Kubernetes의 DaemonSet이 같은 목적을 담당한다.</p>

<h2 id="7--장애-복구와-desired-state">7 ) 장애 복구와 Desired State</h2>

<hr />

<p>Replica가 세 개인 Service를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> recovery-test <span class="se">\</span>
  <span class="nt">--replicas</span> 3 <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>Task가 배치된 Worker에서 Container 하나를 강제로 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">rm</span> <span class="nt">-f</span> CONTAINER_ID
</code></pre></div></div>

<p>Manager에서 일정 시간 뒤 Task 상태를 다시 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service ps recovery-test
</code></pre></div></div>

<p>실행 중인 Replica가 세 개보다 적어졌으므로 Swarm은 새로운 Task를 생성한다. 사용자가 선언한 Desired State와 실제 상태를 지속적으로 비교하고 차이를 복구하는 동작이다.</p>

<blockquote>
  <p>Kubernetes의 Deployment Controller도 Pod가 삭제되면 ReplicaSet을 통해 새로운 Pod를 생성한다. 자동 복구의 기본 원리는 같지만 Swarm은 Service Task를, Kubernetes는 Pod와 Controller Resource를 중심으로 상태를 관리한다.</p>
</blockquote>

<h2 id="8--rolling-update와-rollback">8 ) Rolling Update와 Rollback</h2>

<hr />

<p>Redis 6.0 Service를 생성한 뒤 Image Version을 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> my-database <span class="se">\</span>
  <span class="nt">--replicas</span> 3 <span class="se">\</span>
  redis:6.0-alpine
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service update <span class="se">\</span>
  <span class="nt">--image</span> redis:6.2.5-alpine <span class="se">\</span>
  my-database
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service ps my-database
</code></pre></div></div>

<p>Rolling Update는 기존 Task를 한꺼번에 교체하지 않고 정해진 단위와 간격으로 교체한다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--update-parallelism</code></td>
      <td>동시에 Update할 Task 수, 기본값 <code class="language-plaintext highlighter-rouge">1</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--update-delay</code></td>
      <td>한 Update Group이 끝난 뒤 다음 Group까지 대기 시간</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--update-order</code></td>
      <td><code class="language-plaintext highlighter-rouge">stop-first</code> 또는 <code class="language-plaintext highlighter-rouge">start-first</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--update-failure-action</code></td>
      <td>실패 시 <code class="language-plaintext highlighter-rouge">pause</code>, <code class="language-plaintext highlighter-rouge">rollback</code>, <code class="language-plaintext highlighter-rouge">continue</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--update-max-failure-ratio</code></td>
      <td>허용할 Update 실패 비율</td>
    </tr>
  </tbody>
</table>

<p>기본 Update 순서는 기존 Task를 먼저 중지하는 <code class="language-plaintext highlighter-rouge">stop-first</code>이다. 새 Task를 먼저 실행하여 중단 시간을 줄이려면 <code class="language-plaintext highlighter-rouge">start-first</code>를 명시한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service update <span class="se">\</span>
  <span class="nt">--image</span> redis:6.2.5-alpine <span class="se">\</span>
  <span class="nt">--update-parallelism</span> 1 <span class="se">\</span>
  <span class="nt">--update-delay</span> 10s <span class="se">\</span>
  <span class="nt">--update-order</span> start-first <span class="se">\</span>
  <span class="nt">--update-failure-action</span> rollback <span class="se">\</span>
  my-database
</code></pre></div></div>

<p>실패한 Update를 이전 Service Spec으로 되돌린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service rollback my-database
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker info
docker swarm update <span class="nt">--task-history-limit</span> 10
</code></pre></div></div>

<p>Task History Limit은 이전 Task 기록의 보존 수를 조정한다. Rollback 자체가 여러 Version의 Image 이력을 원하는 만큼 보관하는 기능이라는 의미는 아니다.</p>

<blockquote>
  <p>Kubernetes Deployment도 RollingUpdate Strategy, <code class="language-plaintext highlighter-rouge">maxUnavailable</code>, <code class="language-plaintext highlighter-rouge">maxSurge</code>와 <code class="language-plaintext highlighter-rouge">kubectl rollout undo</code>를 제공한다. 두 도구 모두 단계적 교체와 Rollback을 지원하지만 Option 이름과 상태를 기록하는 Resource 구조는 다르다.</p>
</blockquote>

<h3 id="resource와-service-설정-변경">Resource와 Service 설정 변경</h3>

<p>실행 중인 Service의 설정을 변경할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service update <span class="se">\</span>
  <span class="nt">--limit-cpu</span> 0.5 <span class="se">\</span>
  <span class="nt">--limit-memory</span> 512M <span class="se">\</span>
  <span class="nt">--env-add</span> <span class="nv">MY_VAR</span><span class="o">=</span>value <span class="se">\</span>
  <span class="nt">--env-rm</span> OLD_VAR <span class="se">\</span>
  <span class="nt">--publish-add</span> <span class="nv">published</span><span class="o">=</span>8002,target<span class="o">=</span>80 <span class="se">\</span>
  <span class="nt">--replicas</span> 5 <span class="se">\</span>
  web-alb
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service inspect <span class="nt">--pretty</span> web-alb
</code></pre></div></div>

<h2 id="9--node-drain과-유지-보수">9 ) Node Drain과 유지 보수</h2>

<hr />

<p>Node를 유지 보수할 때 <code class="language-plaintext highlighter-rouge">Drain</code>으로 변경하면 기존 Task를 다른 Active Node로 옮기고 새로운 Task도 배치하지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker node update <span class="se">\</span>
  <span class="nt">--availability</span> drain <span class="se">\</span>
  worker1
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service ps web-alb
</code></pre></div></div>

<p>유지 보수가 끝나면 다시 <code class="language-plaintext highlighter-rouge">Active</code>로 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker node update <span class="se">\</span>
  <span class="nt">--availability</span> active <span class="se">\</span>
  worker1
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker node update</code>는 Manager에서 실행한다. Worker는 자신의 Availability를 직접 변경할 수 없다.</p>

<blockquote>
  <p>Kubernetes에서는 <code class="language-plaintext highlighter-rouge">kubectl cordon NODE</code>로 새로운 Pod Scheduling을 막고 <code class="language-plaintext highlighter-rouge">kubectl drain NODE</code>로 기존 Workload를 다른 Node로 이동시킨다. Swarm의 <code class="language-plaintext highlighter-rouge">Drain</code>은 이 두 작업을 하나의 Availability 상태 변경으로 처리한다.</p>
</blockquote>

<h2 id="10--docker-stack-배포">10 ) Docker Stack 배포</h2>

<hr />

<p>Swarm에서는 여러 Service, Network와 Volume을 하나의 Stack으로 배포할 수 있다. <code class="language-plaintext highlighter-rouge">docker stack</code> 명령은 Manager에서 실행한다.</p>

<h3 id="overlay-network-생성">Overlay Network 생성</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network create <span class="se">\</span>
  <span class="nt">--driver</span> overlay <span class="se">\</span>
  dailylog-net
</code></pre></div></div>

<h3 id="stack-file-작성">Stack File 작성</h3>

<p><code class="language-plaintext highlighter-rouge">docker stack deploy</code>는 현재도 Legacy Compose v3 형식을 사용하므로 Stack File에는 <code class="language-plaintext highlighter-rouge">version</code>을 유지한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.9"</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">mongodb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">dbgurum/dailylog:db_1.0</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">27017:27017"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">dailylog-net</span>
    <span class="na">deploy</span><span class="pi">:</span>
      <span class="na">placement</span><span class="pi">:</span>
        <span class="na">constraints</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">node.role != manager</span>
      <span class="na">restart_policy</span><span class="pi">:</span>
        <span class="na">condition</span><span class="pi">:</span> <span class="s">on-failure</span>
        <span class="na">delay</span><span class="pi">:</span> <span class="s">10s</span>
        <span class="na">max_attempts</span><span class="pi">:</span> <span class="m">3</span>
        <span class="na">window</span><span class="pi">:</span> <span class="s">120s</span>

  <span class="na">backend</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">dbgurum/dailylog:back_1.0</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8000:8000"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">dailylog-net</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MONGO_URL</span><span class="pi">:</span> <span class="s">mongodb://mongodb:27017</span>
    <span class="na">deploy</span><span class="pi">:</span>
      <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span>
      <span class="na">placement</span><span class="pi">:</span>
        <span class="na">constraints</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">node.role != manager</span>
      <span class="na">restart_policy</span><span class="pi">:</span>
        <span class="na">condition</span><span class="pi">:</span> <span class="s">on-failure</span>
        <span class="na">delay</span><span class="pi">:</span> <span class="s">10s</span>
        <span class="na">max_attempts</span><span class="pi">:</span> <span class="m">3</span>
        <span class="na">window</span><span class="pi">:</span> <span class="s">120s</span>

  <span class="na">frontend</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">dbgurum/dailylog:front_1.0</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3000:8000"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">dailylog-net</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">PORT</span><span class="pi">:</span> <span class="s2">"</span><span class="s">8000"</span>
      <span class="na">DAILYLOG_API_ADDR</span><span class="pi">:</span> <span class="s">backend:8000</span>
    <span class="na">deploy</span><span class="pi">:</span>
      <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span>
      <span class="na">placement</span><span class="pi">:</span>
        <span class="na">constraints</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">node.role != manager</span>
      <span class="na">restart_policy</span><span class="pi">:</span>
        <span class="na">condition</span><span class="pi">:</span> <span class="s">on-failure</span>
        <span class="na">delay</span><span class="pi">:</span> <span class="s">10s</span>
        <span class="na">max_attempts</span><span class="pi">:</span> <span class="m">3</span>
        <span class="na">window</span><span class="pi">:</span> <span class="s">120s</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">dailylog-net</span><span class="pi">:</span>
    <span class="na">external</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>Stack 배포에서는 각 Node가 Image를 내려받을 수 있어야 하므로 Image를 Registry에 Push한 상태여야 한다. <code class="language-plaintext highlighter-rouge">depends_on</code>은 Stack Service의 준비 순서를 제어하는 기능으로 의존하지 않고, Backend가 Database 연결 실패를 재시도하도록 Application과 Restart Policy를 구성한다.</p>

<h3 id="배포와-확인">배포와 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker stack deploy <span class="se">\</span>
  <span class="nt">--compose-file</span> docker-compose.yaml <span class="se">\</span>
  dailylog
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker stack <span class="nb">ls
</span>docker stack services dailylog
docker stack ps dailylog
</code></pre></div></div>

<p>Stack을 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker stack <span class="nb">rm </span>dailylog
</code></pre></div></div>

<blockquote>
  <p>Kubernetes에서는 여러 Resource가 작성된 YAML을 <code class="language-plaintext highlighter-rouge">kubectl apply</code>로 배포한다. Swarm Stack과 마찬가지로 선언형 File로 여러 구성 요소를 배포하지만 Kubernetes는 Deployment, Service, ConfigMap, Secret 등 Resource 종류를 각각 정의한다.</p>
</blockquote>

<h2 id="11--docker-swarm의-volume">11 ) Docker Swarm의 Volume</h2>

<hr />

<p>Swarm Service의 Container도 데이터를 영속적으로 보관하기 위해 Volume을 사용한다. 다만 기본 <code class="language-plaintext highlighter-rouge">local</code> Driver로 만든 Named Volume은 <strong>각 Node의 Local Disk에 독립적으로 생성</strong>된다는 점을 고려해야 한다.</p>

<h3 id="named-volume과-재배치">Named Volume과 재배치</h3>

<p>다음 Service가 <code class="language-plaintext highlighter-rouge">worker1</code>에서 실행되면 <code class="language-plaintext highlighter-rouge">db-data</code> Volume도 <code class="language-plaintext highlighter-rouge">worker1</code>에 생성된다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">database</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb:11</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">db-data:/var/lib/mysql</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">db-data</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p>Service Task가 장애나 Node Drain으로 <code class="language-plaintext highlighter-rouge">worker2</code>에 다시 배치되면 Docker는 <code class="language-plaintext highlighter-rouge">worker2</code>에 같은 이름의 새 Local Volume을 생성한다. Volume 이름은 같지만 <code class="language-plaintext highlighter-rouge">worker1</code>의 기존 데이터가 자동으로 복사되거나 공유되지는 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>worker1                          worker2
┌────────────────────┐          ┌────────────────────┐
│ db-data            │          │ db-data            │
│ 기존 Database Data │          │ 새로 생성된 Volume │
└────────────────────┘          └────────────────────┘

같은 이름이지만 서로 다른 Local Data
</code></pre></div></div>

<h3 id="node-placement로-고정">Node Placement로 고정</h3>

<p>단일 Database처럼 특정 Node의 Local Data를 계속 사용해야 한다면 Node에 Label을 지정하고 Placement Constraint로 Task를 고정할 수 있다.</p>

<p>Manager에서 Storage를 보유한 Node에 Label을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker node update <span class="nt">--label-add</span> <span class="nv">storage</span><span class="o">=</span>db worker1
</code></pre></div></div>

<p>Stack File에서 해당 Label을 가진 Node에만 Database를 배치한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">database</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb:11</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">db-data:/var/lib/mysql</span>
    <span class="na">deploy</span><span class="pi">:</span>
      <span class="na">placement</span><span class="pi">:</span>
        <span class="na">constraints</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">node.labels.storage == db</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">db-data</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p>이 방식은 Local Data를 유지하지만 <code class="language-plaintext highlighter-rouge">worker1</code>에 장애가 발생하면 다른 Node에 자동으로 재배치할 수 없다. Node 고정은 공유 Storage가 아니며 고가용성을 제공하지 않는다.</p>

<h3 id="nfs와-network-storage">NFS와 Network Storage</h3>

<p>여러 Node에서 같은 Data에 접근해야 한다면 NFS 같은 Network File System이나 Cluster 전체에서 사용할 수 있는 Storage Driver를 구성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">database</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb:11</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">db-data:/var/lib/mysql</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">db-data</span><span class="pi">:</span>
    <span class="na">driver</span><span class="pi">:</span> <span class="s">local</span>
    <span class="na">driver_opts</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">nfs</span>
      <span class="na">o</span><span class="pi">:</span> <span class="s">addr=192.168.0.110,rw</span>
      <span class="na">device</span><span class="pi">:</span> <span class="s2">"</span><span class="s">:/srv/swarm-data"</span>
</code></pre></div></div>

<p>모든 Swarm Node가 NFS Server에 연결할 수 있어야 하며 NFS Directory, 접근 권한과 방화벽을 미리 구성해야 한다. Public Cloud에서는 Network File System이나 Swarm과 호환되는 외부 Volume Driver를 사용할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>Data 위치</th>
      <th>다른 Node 재배치</th>
      <th>적합한 경우</th>
      <th>주의 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Local Named Volume</td>
      <td>각 Node의 Local Disk</td>
      <td>기존 Data 자동 연결 불가</td>
      <td>Node와 Data의 결합이 중요하지 않은 경우</td>
      <td>같은 이름의 별도 Volume 생성 가능</td>
    </tr>
    <tr>
      <td>Placement Constraint</td>
      <td>지정 Node의 Local Disk</td>
      <td>지정 Node에서만 실행</td>
      <td>단일 Stateful Application</td>
      <td>Node 장애 시 Service 중단 가능</td>
    </tr>
    <tr>
      <td>NFS·Network Storage</td>
      <td>Network Storage</td>
      <td>같은 Data에 접근 가능</td>
      <td>여러 Node에서 Data 공유</td>
      <td>Storage 가용성·성능·권한 관리 필요</td>
    </tr>
  </tbody>
</table>

<p>Stateful Service를 운영할 때는 Container Replica 수만 늘리는 것으로 Data 고가용성이 완성되지 않는다. Database 자체의 Replication과 Backup, Storage 장애 복구 방식까지 함께 설계해야 한다.</p>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Manager에서 Swarm을 초기화하고 Worker는 Join Token으로 Cluster에 참여한다.</p>
    </li>
    <li>
      <p>Cluster 전체의 Node와 Service 관리 명령은 Manager에서 실행하며 Worker는 할당된 Task를 실행한다.</p>
    </li>
    <li>
      <p>Service Port를 공개하면 Task 배치 여부와 관계없이 모든 Node가 같은 Port로 Traffic을 받는다.</p>
    </li>
    <li>
      <p>Overlay Network와 내장 DNS를 통해 Service 이름으로 통신하며 Routing Mesh가 실행 중인 Task로 요청을 전달한다.</p>
    </li>
    <li>
      <p>Replicated Mode는 지정한 수의 Task를 유지하고 Global Mode는 조건에 맞는 모든 Node에 Task를 배치한다.</p>
    </li>
    <li>
      <p>Swarm은 Desired State, Rolling Update, Rollback과 Node Drain으로 Service 운영을 지원한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker stack deploy</code>는 여러 Service와 Network를 하나의 Application Stack으로 배포한다.</p>
    </li>
    <li>
      <p>Swarm의 기본 Named Volume은 Node Local Storage이므로 Task 재배치 시 Data가 자동으로 따라가지 않는다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><category term="DockerSwarm" /><summary type="html"><![CDATA[Manager와 Worker Cluster 구성, Service 배포, Routing Mesh, 확장, Rolling Update 및 Stack 배포]]></summary></entry><entry><title type="html">Docker Health Check와 Config 및 Secret</title><link href="https://kkr010128.github.io/cloud-native-19-healthcheck-config-secret/" rel="alternate" type="text/html" title="Docker Health Check와 Config 및 Secret" /><published>2026-08-24T00:00:00+09:00</published><updated>2026-08-24T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-19-healthcheck-config-secret</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-19-healthcheck-config-secret/"><![CDATA[<h2 id="1--process-상태와-application-상태">1 ) Process 상태와 Application 상태</h2>

<hr />

<p>Container는 지정된 주 Process가 실행되는 동안 Running 상태를 유지한다. Process가 종료되면 Container도 종료되므로 기본적인 실행 여부는 Container 상태만으로 확인할 수 있다.</p>

<p>하지만 Process가 실행 중이라고 Application이 정상적으로 요청을 처리한다는 의미는 아니다. Web Application이 처리 용량을 초과하여 <code class="language-plaintext highlighter-rouge">503 Service Unavailable</code>을 반환하거나 Database 연결이 끊긴 상태에서도 주 Process는 계속 실행될 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>확인 대상</th>
      <th>확인할 수 있는 상태</th>
      <th>확인하기 어려운 상태</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Container Process</td>
      <td>Process 실행 및 종료 여부</td>
      <td>Application이 실제 요청을 처리하는지 여부</td>
    </tr>
    <tr>
      <td>Health Check</td>
      <td>HTTP 응답, Database 접속 등 Application 상태</td>
      <td>비즈니스 기능 전체의 정상 여부</td>
    </tr>
  </tbody>
</table>

<p>Dockerfile의 <code class="language-plaintext highlighter-rouge">HEALTHCHECK</code>는 Container 안에서 검사 명령을 주기적으로 실행하여 Process보다 구체적인 Application 상태를 확인한다.</p>

<h2 id="2--dockerfile-health-check">2 ) Dockerfile Health Check</h2>

<hr />

<h3 id="flask-application">Flask Application</h3>

<p><code class="language-plaintext highlighter-rouge">app.py</code>에 일반 요청과 상태 확인을 위한 Endpoint를 작성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">flask</span> <span class="kn">import</span> <span class="n">Flask</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="k">return</span> <span class="s">"Hello Docker"</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/health"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">health_check</span><span class="p">():</span>
    <span class="k">return</span> <span class="s">"OK"</span><span class="p">,</span> <span class="mi">200</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s">"0.0.0.0"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">8080</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python:3.9-slim</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> curl <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> app.py ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> flask

<span class="k">HEALTHCHECK</span><span class="s"> \</span>
  --interval=10s \
  --timeout=3s \
  --start-period=5s \
  --retries=3 \
  CMD curl -f http://localhost:8080/health || exit 1

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">CMD</span><span class="s"> ["python", "app.py"]</span>
</code></pre></div></div>

<p>Health Check Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--interval</code></td>
      <td>검사 실행 간격</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--timeout</code></td>
      <td>한 번의 검사 제한 시간</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--start-period</code></td>
      <td>Application 시작 후 실패를 유예할 시간</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--retries</code></td>
      <td><code class="language-plaintext highlighter-rouge">unhealthy</code>로 판단하기 전 연속 실패 횟수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CMD</code></td>
      <td>실제 상태를 검사할 명령</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">curl -f</code>는 HTTP 응답 Code가 오류이면 실패 상태로 종료하게 되므로, <code class="language-plaintext highlighter-rouge">|| exit 1</code>로 정리한다.</p>

<h3 id="image-build와-상태-확인">Image Build와 상태 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> my-healthcheck-app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> healthcheck <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  my-healthcheck-app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container ps
docker container inspect healthcheck
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">STATUS</code>에는 Health Check 결과가 다음과 같이 표시된다.</p>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">starting</code></td>
      <td>Container가 시작되어 첫 검사 결과를 기다리는 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">healthy</code></td>
      <td>최근 Health Check가 성공한 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unhealthy</code></td>
      <td>설정한 횟수만큼 연속으로 검사에 실패한 상태</td>
    </tr>
  </tbody>
</table>

<p>Health Check 결과는 Load Balancing과 Rolling Update에서 새 Task가 요청을 처리할 준비가 되었는지 판단하는 자료가 된다. Kubernetes에서는 비슷한 목적을 Startup Probe, Readiness Probe와 Liveness Probe로 나누어 설정한다.</p>

<h2 id="3--compose-health-check">3 ) Compose Health Check</h2>

<hr />

<p>Compose File에서는 Service의 <code class="language-plaintext highlighter-rouge">healthcheck</code>에 검사 명령과 주기를 정의한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">my-healthcheck-app</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>
    <span class="na">healthcheck</span><span class="pi">:</span>
      <span class="na">test</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">CMD</span>
        <span class="pi">-</span> <span class="s">curl</span>
        <span class="pi">-</span> <span class="s">-f</span>
        <span class="pi">-</span> <span class="s">http://localhost:8080/health</span>
      <span class="na">interval</span><span class="pi">:</span> <span class="s">10s</span>
      <span class="na">timeout</span><span class="pi">:</span> <span class="s">5s</span>
      <span class="na">retries</span><span class="pi">:</span> <span class="m">3</span>
      <span class="na">start_period</span><span class="pi">:</span> <span class="s">5s</span>
</code></pre></div></div>

<p>Dockerfile과 Compose File에 Health Check가 모두 있으면 Compose File의 설정이 해당 Service에서 우선한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
</code></pre></div></div>

<h2 id="4--database-준비-후-web-service-시작">4 ) Database 준비 후 Web Service 시작</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">depends_on</code>만 지정하면 Compose는 Database Container를 먼저 시작하지만 Database가 Connection을 받을 준비까지 기다리지는 않는다. Database에 Health Check를 추가하고 Web Service에 <code class="language-plaintext highlighter-rouge">condition: service_healthy</code>를 지정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">db</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">postgres:15</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">POSTGRES_USER</span><span class="pi">:</span> <span class="s">user</span>
      <span class="na">POSTGRES_PASSWORD</span><span class="pi">:</span> <span class="s">password</span>
      <span class="na">POSTGRES_DB</span><span class="pi">:</span> <span class="s">mydb</span>
    <span class="na">healthcheck</span><span class="pi">:</span>
      <span class="na">test</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">CMD-SHELL</span>
        <span class="pi">-</span> <span class="s">pg_isready -U user -d mydb</span>
      <span class="na">interval</span><span class="pi">:</span> <span class="s">5s</span>
      <span class="na">timeout</span><span class="pi">:</span> <span class="s">5s</span>
      <span class="na">retries</span><span class="pi">:</span> <span class="m">5</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">my-network</span>

  <span class="na">web</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">.</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">DB_HOST</span><span class="pi">:</span> <span class="s">db</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="na">db</span><span class="pi">:</span>
        <span class="na">condition</span><span class="pi">:</span> <span class="s">service_healthy</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">my-network</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">my-network</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p>이 설정에서 실행 순서는 다음과 같다.</p>

<ol>
  <li>
    <p>Compose가 <code class="language-plaintext highlighter-rouge">db</code> Container를 시작한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">pg_isready</code>가 PostgreSQL의 준비 상태를 검사한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">db</code>가 <code class="language-plaintext highlighter-rouge">healthy</code>가 되면 <code class="language-plaintext highlighter-rouge">web</code> Container를 생성한다.</p>
  </li>
  <li>
    <p>Web Application은 <code class="language-plaintext highlighter-rouge">db</code>라는 Service 이름으로 Database에 접속한다.</p>
  </li>
</ol>

<p>Health Check가 성공했더라도 실행 중 Network 장애가 발생할 수 있으므로 Application 자체에도 Connection 재시도와 오류 처리가 필요하다.</p>

<h2 id="5--swarm-config">5 ) Swarm Config</h2>

<hr />

<p>Docker Config는 Application 설정과 Image를 분리하기 위한 Swarm Resource이다. Service에 Config를 연결하면 지정한 Target 경로에서 File로 읽을 수 있다.</p>

<p>Config에는 다음과 같은 비민감 설정을 저장한다.</p>

<ul>
  <li>
    <p>Web Server 설정</p>
  </li>
  <li>
    <p>Application Feature 설정</p>
  </li>
  <li>
    <p>일반적인 Database Client 설정</p>
  </li>
  <li>
    <p>환경별 설정 File</p>
  </li>
</ul>

<p>MySQL의 일반 설정 File을 Config로 생성한다.</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[mysqld]</span>
<span class="py">max_connections</span><span class="p">=</span><span class="s">200</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker config create <span class="se">\</span>
  mysql_server_config <span class="se">\</span>
  ./mysql.cnf
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker config <span class="nb">ls</span>
</code></pre></div></div>

<p>Config를 MySQL Service에 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> mysql-server <span class="se">\</span>
  <span class="nt">--config</span> <span class="nb">source</span><span class="o">=</span>mysql_server_config,target<span class="o">=</span>/etc/mysql/conf.d/custom.cnf <span class="se">\</span>
  mysql:8.0
</code></pre></div></div>

<p>Config를 변경해야 할 때는 기존 Config 자체를 덮어쓰기보다 새로운 이름으로 생성하고 Service가 참조하는 Config를 교체한다.</p>

<h2 id="6--config에-비밀번호를-넣어도-되는가">6 ) Config에 비밀번호를 넣어도 되는가</h2>

<hr />

<p>강의에서는 MySQL Root 비밀번호를 Config 객체로 만들어 File 형태로 주입했다. 이어서 비밀번호를 환경 변수로 직접 전달하면 Container 설정이나 <code class="language-plaintext highlighter-rouge">docker inspect</code>를 통해 노출될 수 있으므로 Secret을 사용할 수 있다고 설명했다.</p>

<p>두 실습을 비교하면서 일반 설정은 Config로, 비밀번호와 같은 민감 정보는 Secret으로 관리하는 것이 목적에 더 적합하다는 것을 알게 되었다.</p>

<p>Config는 민감하지 않은 설정을 배포하기 위한 Resource이다. 비밀번호, API Token, TLS Private Key처럼 노출되어서는 안 되는 값은 Secret으로 관리해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Config</th>
      <th>Secret</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>저장 대상</td>
      <td>일반 설정</td>
      <td>비밀번호, Token, 인증서 Key</td>
    </tr>
    <tr>
      <td>Service 전달</td>
      <td>Container의 File로 Mount</td>
      <td>권한을 받은 Task에만 전달</td>
    </tr>
    <tr>
      <td>Linux Container의 기본 경로</td>
      <td>지정한 Target</td>
      <td><code class="language-plaintext highlighter-rouge">/run/secrets/SECRET_NAME</code></td>
    </tr>
    <tr>
      <td>주요 보호 특성</td>
      <td>설정과 Image 분리</td>
      <td>Swarm에서 전송 중·저장 시 암호화</td>
    </tr>
  </tbody>
</table>

<p>환경 변수에 비밀번호를 직접 기록하는 방법도 Container 설정과 <code class="language-plaintext highlighter-rouge">docker inspect</code> 결과 등에 값이 나타날 수 있다. Secret을 지원하는 Image라면 <code class="language-plaintext highlighter-rouge">_FILE</code> 접미사가 붙은 환경 변수로 Secret File 경로를 전달한다.</p>

<h2 id="7--swarm-secret">7 ) Swarm Secret</h2>

<hr />

<p>Secret의 기본 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret 생성
→ Swarm Secret Store에 저장
→ Service에 Secret 사용 권한 부여
→ Task의 /run/secrets에 File로 Mount
→ Task 종료 시 Mount 제거
</code></pre></div></div>

<h3 id="secret-생성">Secret 생성</h3>

<p>비밀번호 File을 생성하고 Swarm Secret으로 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">printf</span> <span class="s1">'%s'</span> <span class="s1">'MyStrongPassword123!'</span> <span class="o">&gt;</span> mysql_root_pw.txt
docker secret create <span class="se">\</span>
  mysql_root_password_v1 <span class="se">\</span>
  ./mysql_root_pw.txt
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker secret <span class="nb">ls</span>
</code></pre></div></div>

<p>또는 표준 입력으로 값을 전달할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">printf</span> <span class="s1">'%s'</span> <span class="s1">'initial_db_password_111'</span> | <span class="se">\</span>
  docker secret create db_password_v1 -
</code></pre></div></div>

<p>Shell History와 Terminal 출력에 Secret을 남길 수 있으므로 실제 환경에서는 Secret 값을 명령행에 직접 작성하지 않는다.</p>

<h3 id="mysql-service에-secret-연결">MySQL Service에 Secret 연결</h3>

<p>MySQL 공식 Image는 <code class="language-plaintext highlighter-rouge">MYSQL_ROOT_PASSWORD_FILE</code>에 지정한 File의 내용을 Root 비밀번호로 읽을 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> mysql-server <span class="se">\</span>
  <span class="nt">--publish</span> <span class="nv">published</span><span class="o">=</span>3306,target<span class="o">=</span>3306 <span class="se">\</span>
  <span class="nt">--secret</span> <span class="nb">source</span><span class="o">=</span>mysql_root_password_v1,target<span class="o">=</span>mysql_root_password <span class="se">\</span>
  <span class="nt">--env</span> <span class="nv">MYSQL_ROOT_PASSWORD_FILE</span><span class="o">=</span>/run/secrets/mysql_root_password <span class="se">\</span>
  mysql:8.0
</code></pre></div></div>

<p>Service Task에서는 다음 경로로 Secret을 읽는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/run/secrets/mysql_root_password
</code></pre></div></div>

<p>Secret 값은 <code class="language-plaintext highlighter-rouge">docker secret inspect</code>로 조회되지 않는다. Manager와 사용 권한이 부여된 Service Task만 Secret에 접근하며 Linux Container에서는 Memory File System에 Mount된다.</p>

<h3 id="stack-file에서-config와-secret-사용">Stack File에서 Config와 Secret 사용</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.9"</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">db</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8.0</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3306:3306"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD_FILE</span><span class="pi">:</span> <span class="s">/run/secrets/db_root_password</span>
      <span class="na">MYSQL_DATABASE</span><span class="pi">:</span> <span class="s">myapp_db</span>
    <span class="na">configs</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">source</span><span class="pi">:</span> <span class="s">db_config</span>
        <span class="na">target</span><span class="pi">:</span> <span class="s">/etc/mysql/conf.d/custom.cnf</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="m">0444</span>
    <span class="na">secrets</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">source</span><span class="pi">:</span> <span class="s">db_root_password</span>
        <span class="na">target</span><span class="pi">:</span> <span class="s">db_root_password</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="m">0400</span>

<span class="na">configs</span><span class="pi">:</span>
  <span class="na">db_config</span><span class="pi">:</span>
    <span class="na">file</span><span class="pi">:</span> <span class="s">./mysql.cnf</span>

<span class="na">secrets</span><span class="pi">:</span>
  <span class="na">db_root_password</span><span class="pi">:</span>
    <span class="na">file</span><span class="pi">:</span> <span class="s">./mysql_root_pw.txt</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker stack deploy <span class="se">\</span>
  <span class="nt">--compose-file</span> docker-compose.yaml <span class="se">\</span>
  mysql-app
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mysql.cnf</code>와 비밀번호 File은 용도가 다르므로 Config와 Secret으로 분리한다.</p>

<h2 id="8--secret-version-교체">8 ) Secret Version 교체</h2>

<hr />

<p>Swarm Secret의 값은 직접 수정할 수 없다. 새 Version의 Secret을 생성한 뒤 Service가 참조하는 Secret을 교체한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker secret create <span class="se">\</span>
  mysql_root_password_v2 <span class="se">\</span>
  ./mysql_root_pw_v2.txt
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service update <span class="se">\</span>
  <span class="nt">--secret-rm</span> mysql_root_password_v1 <span class="se">\</span>
  <span class="nt">--secret-add</span> <span class="nb">source</span><span class="o">=</span>mysql_root_password_v2,target<span class="o">=</span>mysql_root_password <span class="se">\</span>
  mysql-server
</code></pre></div></div>

<p>기존 Secret을 사용하는 Service가 없다면 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker secret <span class="nb">rm </span>mysql_root_password_v1
</code></pre></div></div>

<p>Secret 이름에 Version을 포함하면 새 값으로 교체하고 이전 값을 제거하는 과정을 구분하기 쉽다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Container Process가 실행 중이어도 Application은 요청을 처리하지 못할 수 있으므로 별도의 Health Check가 필요하다.</p>
    </li>
    <li>
      <p>Dockerfile과 Compose의 Health Check는 HTTP 요청이나 Database 접속처럼 실제 Application 상태를 검사한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">depends_on</code>과 <code class="language-plaintext highlighter-rouge">condition: service_healthy</code>를 함께 사용하면 의존 Service가 준비된 뒤 다음 Service를 시작할 수 있다.</p>
    </li>
    <li>
      <p>Config는 일반 설정을 Image와 분리하고 Secret은 비밀번호와 Token 같은 민감 정보를 보호한다.</p>
    </li>
    <li>
      <p>환경 변수에 비밀번호를 직접 넣으면 Container 설정에서 노출될 수 있으므로 <code class="language-plaintext highlighter-rouge">_FILE</code> 환경 변수와 Secret File을 사용한다.</p>
    </li>
    <li>
      <p>Swarm Secret은 권한이 부여된 Service Task의 <code class="language-plaintext highlighter-rouge">/run/secrets</code>에 Mount되며 새 Version을 생성하는 방식으로 교체한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><category term="DockerSwarm" /><summary type="html"><![CDATA[Container 상태 검사, Compose 의존성 제어와 Swarm Config·Secret 관리 방법]]></summary></entry><entry><title type="html">Docker Compose 응용과 Docker Swarm</title><link href="https://kkr010128.github.io/cloud-native-17-docker-compose-swarm/" rel="alternate" type="text/html" title="Docker Compose 응용과 Docker Swarm" /><published>2026-08-21T00:00:00+09:00</published><updated>2026-08-21T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-17-docker-compose-swarm</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-17-docker-compose-swarm/"><![CDATA[<p>이전 글에서는 Docker Compose의 역할과 기본 File 구조를 살펴보았다. 이번 글에서는 여러 Service를 실제로 연결하고 확장하는 방법부터 Nginx Load Balancing, 환경 변수 관리와 Docker Swarm까지 다룬다.</p>

<p>Docker Compose는 주로 단일 Host에서 여러 Container로 구성된 Application을 관리한다. 여러 Host를 하나의 Cluster로 묶어 Service를 배치하고 원하는 상태를 유지해야 한다면 Docker Swarm이나 Kubernetes 같은 Container Orchestration 도구가 필요하다.</p>

<h2 id="1--compose-file-작성">1 ) Compose File 작성</h2>

<hr />

<h3 id="file-이름과-최상위-항목">File 이름과 최상위 항목</h3>

<p>Compose File은 다음 이름을 사용할 수 있다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">compose.yaml</code></p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">compose.yml</code></p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker-compose.yaml</code></p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker-compose.yml</code></p>
  </li>
</ul>

<p>현재는 <code class="language-plaintext highlighter-rouge">compose.yaml</code>이 선호되며 <code class="language-plaintext highlighter-rouge">docker-compose.yaml</code>도 호환을 위해 사용할 수 있다.</p>

<p>Python 기반 Compose V1에서 사용하던 <strong>Legacy 명령</strong>은 현재 호환 목적으로만 남은 Obsolete 항목이므로, 현재는 Docker CLI Plugin 형태의 <code class="language-plaintext highlighter-rouge">docker compose</code>를 사용하며</p>

<p>Compose File의 주요 최상위 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">services</code></td>
      <td>실행할 Container Service 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">networks</code></td>
      <td>Service가 사용할 Network 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Service가 사용할 Named Volume 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">configs</code></td>
      <td>일반 설정 정보 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">secrets</code></td>
      <td>민감한 정보 정의</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">services</code> 아래에는 함께 실행할 Container를 Service 단위로 작성한다. Service별 주요 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">image</code></td>
      <td>Registry에서 가져오거나 이미 Build한 Image 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">build</code></td>
      <td>Dockerfile과 Build Context 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">container_name</code></td>
      <td>생성할 Container 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ports</code></td>
      <td>Host Port와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">expose</code></td>
      <td>다른 Service에 Container Port 정보 제공</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">networks</code></td>
      <td>연결할 Network 선택</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Named Volume 또는 Bind Mount 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>Container 환경 변수 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">command</code></td>
      <td>Image의 기본 실행 명령을 덮어쓸 명령 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">restart</code></td>
      <td>Container 종료 후 재시작 정책 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">depends_on</code></td>
      <td>Service 사이의 생성 및 종료 순서 지정</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">restart</code>에는 <code class="language-plaintext highlighter-rouge">no</code>, <code class="language-plaintext highlighter-rouge">always</code>, <code class="language-plaintext highlighter-rouge">on-failure</code>, <code class="language-plaintext highlighter-rouge">unless-stopped</code> 등을 사용할 수 있다. YAML에서 <code class="language-plaintext highlighter-rouge">no</code>가 Boolean으로 해석될 수 있으므로 문자열인 <code class="language-plaintext highlighter-rouge">"no"</code>로 작성하는 편이 안전하다.</p>

<p>Docker Hub 등의 Registry에 있는 Image는 <code class="language-plaintext highlighter-rouge">image</code>로 지정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">myweb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">mydb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb</span>
</code></pre></div></div>

<p>직접 개발한 Application은 Dockerfile이 있는 현재 Directory를 Build Context로 지정할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">myweb</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">.</span>
</code></pre></div></div>

<p>Build Context와 Dockerfile 위치를 구분하려면 <code class="language-plaintext highlighter-rouge">build</code> 아래에 <code class="language-plaintext highlighter-rouge">context</code>와 <code class="language-plaintext highlighter-rouge">dockerfile</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">myweb</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span>
      <span class="na">context</span><span class="pi">:</span> <span class="s">.</span>
      <span class="na">dockerfile</span><span class="pi">:</span> <span class="s">docker/Dockerfile</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">depends_on</code>은 의존 Service를 먼저 생성하지만 해당 Application이 요청을 처리할 준비까지 마쳤음을 보장하지는 않는다. 준비 상태까지 기다려야 한다면 의존 Service에 <code class="language-plaintext highlighter-rouge">healthcheck</code>를 정의하고 <code class="language-plaintext highlighter-rouge">condition: service_healthy</code>를 사용한다.</p>

<h3 id="network-정의">Network 정의</h3>

<p><code class="language-plaintext highlighter-rouge">networks</code>를 생략하면 Compose가 Project 전용 기본 Network를 자동 생성한다. 같은 Network의 Service는 IP Address를 직접 확인하지 않고 Service 이름으로 통신할 수 있다.</p>

<p>여러 Network를 분리하려면 최상위에서 Network를 정의하고 Service별로 연결한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">myweb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">frontend-net</span>
  <span class="na">mydb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">backend-net</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">frontend-net</span><span class="pi">:</span> <span class="pi">{}</span>
  <span class="na">backend-net</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p>이미 만들어 둔 Network를 사용하려면 <code class="language-plaintext highlighter-rouge">external: true</code>와 실제 이름을 지정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">networks</span><span class="pi">:</span>
  <span class="na">default</span><span class="pi">:</span>
    <span class="na">external</span><span class="pi">:</span> <span class="no">true</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">myapp-net</span>
</code></pre></div></div>

<h3 id="volume-정의">Volume 정의</h3>

<p>데이터를 Container 수명과 분리하려면 최상위에서 Named Volume을 정의하고 Service의 내부 Directory에 Mount한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mydb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8.0</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">mydb-data:/var/lib/mysql</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">mydb-data</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p>Named Volume의 실제 데이터는 Docker가 관리한다. Linux의 기본 Rootless가 아닌 구성에서는 일반적으로 <code class="language-plaintext highlighter-rouge">/var/lib/docker/volumes</code> 아래에 배치되지만, Storage Driver나 실행 방식에 따라 위치가 달라질 수 있으므로 직접 경로를 조작하지 않는다.</p>

<h2 id="2--mysql과-wordpress-실행">2 ) MySQL과 WordPress 실행</h2>

<hr />

<p>MySQL 8.0과 WordPress 5.7로 구성한 Web Application을 각각의 <code class="language-plaintext highlighter-rouge">docker run</code> 명령과 Compose File로 실행해 본다. 여기서 사용하는 Image Version과 비밀번호는 강의 실습을 재현하기 위한 값이므로 실제 운영 환경에서는 지원 중인 Image와 안전한 Secret 관리 방식을 선택해야 한다.</p>

<h3 id="docker-run으로-실행"><code class="language-plaintext highlighter-rouge">docker run</code>으로 실행</h3>

<p>먼저 Database 데이터를 저장할 Volume과 두 Container가 함께 사용할 Network를 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create mydb_data
docker network create myapp_net
</code></pre></div></div>

<p>MySQL Container를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-dit</span> <span class="se">\</span>
  <span class="nt">--name</span> mysql_app <span class="se">\</span>
  <span class="nt">-v</span> mydb_data:/var/lib/mysql <span class="se">\</span>
  <span class="nt">--restart</span><span class="o">=</span>always <span class="se">\</span>
  <span class="nt">-p</span> 3306:3306 <span class="se">\</span>
  <span class="nt">--network</span> myapp_net <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_DATABASE</span><span class="o">=</span>adam <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_USER</span><span class="o">=</span>adam <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_PASSWORD</span><span class="o">=</span>wnddkd <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>wnddkd <span class="se">\</span>
  mysql:8.0
</code></pre></div></div>

<p>WordPress Container를 같은 Network에 연결하여 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-dit</span> <span class="se">\</span>
  <span class="nt">--name</span> wordpress_app <span class="se">\</span>
  <span class="nt">-v</span> myweb_data:/var/www/html <span class="se">\</span>
  <span class="nt">-v</span> <span class="s2">"</span><span class="k">${</span><span class="nv">PWD</span><span class="k">}</span><span class="s2">/myweb-log:/var/log"</span> <span class="se">\</span>
  <span class="nt">--restart</span><span class="o">=</span>always <span class="se">\</span>
  <span class="nt">-p</span> 8888:80 <span class="se">\</span>
  <span class="nt">--network</span> myapp_net <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">WORDPRESS_DB_HOST</span><span class="o">=</span>mysql_app:3306 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">WORDPRESS_DB_NAME</span><span class="o">=</span>adam <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">WORDPRESS_DB_USER</span><span class="o">=</span>adam <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">WORDPRESS_DB_PASSWORD</span><span class="o">=</span>wnddkd <span class="se">\</span>
  wordpress:5.7
</code></pre></div></div>

<p>같은 사용자 정의 Network에서는 <code class="language-plaintext highlighter-rouge">mysql_app</code>이라는 Container 이름으로 MySQL에 접근할 수 있다. 과거에는 <code class="language-plaintext highlighter-rouge">--link mysql_app:mysql</code> Option을 사용하기도 했지만 <code class="language-plaintext highlighter-rouge">--link</code>는 <strong>Legacy 기능</strong>이므로 사용자 정의 Network와 DNS 기반 이름 조회를 사용한다.</p>

<h3 id="docker-compose로-실행">Docker Compose로 실행</h3>

<p>같은 구성을 <code class="language-plaintext highlighter-rouge">compose.yaml</code> 하나로 표현하면 다음과 같다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mydb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8.0</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">mysql_app</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">mydb_data:/var/lib/mysql</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3306:3306"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">backend-net</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">wnddkd</span>
      <span class="na">MYSQL_DATABASE</span><span class="pi">:</span> <span class="s">adam</span>
      <span class="na">MYSQL_USER</span><span class="pi">:</span> <span class="s">adam</span>
      <span class="na">MYSQL_PASSWORD</span><span class="pi">:</span> <span class="s">wnddkd</span>

  <span class="na">myweb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">wordpress:5.7</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">wordpress_app</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">mydb</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8888:80"</span>
    <span class="na">networks</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">backend-net</span>
      <span class="pi">-</span> <span class="s">frontend-net</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">myweb_data:/var/www/html</span>
      <span class="pi">-</span> <span class="s">${PWD}/myweb-log:/var/log</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">WORDPRESS_DB_HOST</span><span class="pi">:</span> <span class="s">mydb:3306</span>
      <span class="na">WORDPRESS_DB_NAME</span><span class="pi">:</span> <span class="s">adam</span>
      <span class="na">WORDPRESS_DB_USER</span><span class="pi">:</span> <span class="s">adam</span>
      <span class="na">WORDPRESS_DB_PASSWORD</span><span class="pi">:</span> <span class="s">wnddkd</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">frontend-net</span><span class="pi">:</span> <span class="pi">{}</span>
  <span class="na">backend-net</span><span class="pi">:</span> <span class="pi">{}</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">mydb_data</span><span class="pi">:</span> <span class="pi">{}</span>
  <span class="na">myweb_data</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mydb</code>는 Backend Network에만 연결하고 <code class="language-plaintext highlighter-rouge">myweb</code>은 Frontend와 Backend Network에 모두 연결한다. WordPress는 MySQL Container의 IP가 아닌 Service 이름 <code class="language-plaintext highlighter-rouge">mydb</code>로 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>여러 <code class="language-plaintext highlighter-rouge">docker run</code> 명령의 Image, Port, Volume, Network와 환경 변수를 하나의 Compose File로 옮길 수 있다.</p>
    </li>
    <li>
      <p>동일한 Compose Network에서는 Service 이름을 DNS 이름으로 사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">depends_on</code>은 실행 순서를 정의하지만 준비 완료를 보장하지 않는다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="3--flask와-redis의-container-통신">3 ) Flask와 Redis의 Container 통신</h2>

<hr />

<p>Flask Application의 Web 접속 횟수를 Redis에 저장하는 예제로 <code class="language-plaintext highlighter-rouge">localhost</code>와 Container Network의 차이를 확인한다.</p>

<h3 id="redis만-container로-실행">Redis만 Container로 실행</h3>

<p>Redis Container를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">--name</span> myredis <span class="nt">-d</span> <span class="nt">-p</span> 6379:6379 redis
</code></pre></div></div>

<p>Host에서 Redis Server가 이미 실행 중이어서 Port가 충돌한다면 실습 전에 기존 Service를 중지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl stop redis-server
</code></pre></div></div>

<p>Python 가상 환경을 만들고 필요한 Package를 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 <span class="nt">-m</span> venv ./myvenv
<span class="nb">source </span>myvenv/bin/activate
pip <span class="nb">install </span>flask redis
pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">app.py</code>를 작성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">redis</span>
<span class="kn">from</span> <span class="nn">flask</span> <span class="kn">import</span> <span class="n">Flask</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">web_hit_cnt</span><span class="p">():</span>
    <span class="k">with</span> <span class="n">redis</span><span class="p">.</span><span class="n">Redis</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s">"localhost"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">6379</span><span class="p">)</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">conn</span><span class="p">.</span><span class="n">incr</span><span class="p">(</span><span class="s">"hits"</span><span class="p">)</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="n">count</span> <span class="o">=</span> <span class="n">web_hit_cnt</span><span class="p">()</span>
    <span class="k">return</span> <span class="sa">f</span><span class="s">"&lt;h1&gt;Web Access Count: </span><span class="si">{</span><span class="n">count</span><span class="si">}</span><span class="s">&lt;/h1&gt;"</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s">"0.0.0.0"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">9000</span><span class="p">,</span> <span class="n">debug</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div>

<p>Host에서 Application을 실행하면 Host의 <code class="language-plaintext highlighter-rouge">6379</code> Port를 통해 Redis Container에 접근한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python app.py
</code></pre></div></div>

<h3 id="flask도-container로-실행">Flask도 Container로 실행</h3>

<p>Flask Application을 Image로 Build하기 위해 <code class="language-plaintext highlighter-rouge">Dockerfile</code>을 작성한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python:3</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> requirements.txt .</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> app.py .</span>

<span class="k">CMD</span><span class="s"> ["python", "app.py"]</span>
</code></pre></div></div>

<p>Image를 Build하고 Container를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> flaskapp <span class="nb">.</span>
docker run <span class="nt">-dit</span> <span class="nt">-p</span> 9000:9000 <span class="nt">--name</span> flaskapp flaskapp
</code></pre></div></div>

<p>이 상태에서 Web Page를 호출하면 Redis 연결에 실패한다. Container 안의 <code class="language-plaintext highlighter-rouge">localhost</code>와 <code class="language-plaintext highlighter-rouge">127.0.0.1</code>은 Host나 다른 Container가 아니라 <strong>현재 Flask Container 자신</strong>을 가리키기 때문이다.</p>

<p>다음 명령으로 기본 Bridge Network와 Redis Container의 IP Address를 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network inspect bridge
docker inspect myredis
</code></pre></div></div>

<p>확인한 IP Address를 <code class="language-plaintext highlighter-rouge">app.py</code>에 직접 작성하면 임시로 연결할 수 있지만 Container를 다시 만들 때 IP가 바뀔 수 있다. 지속적으로 사용할 구성에서는 두 Container를 같은 사용자 정의 Network에 연결하고 이름으로 통신하거나 Compose를 사용한다.</p>

<h3 id="docker-compose에서-service-이름으로-통신">Docker Compose에서 Service 이름으로 통신</h3>

<p><code class="language-plaintext highlighter-rouge">app.py</code>에서 Redis 접속 Host를 Compose Service 이름인 <code class="language-plaintext highlighter-rouge">redis</code>로 변경한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">redis</span>
<span class="kn">from</span> <span class="nn">flask</span> <span class="kn">import</span> <span class="n">Flask</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">web_hit_cnt</span><span class="p">():</span>
    <span class="k">with</span> <span class="n">redis</span><span class="p">.</span><span class="n">Redis</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s">"redis"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">6379</span><span class="p">)</span> <span class="k">as</span> <span class="n">conn</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">conn</span><span class="p">.</span><span class="n">incr</span><span class="p">(</span><span class="s">"hits"</span><span class="p">)</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="n">count</span> <span class="o">=</span> <span class="n">web_hit_cnt</span><span class="p">()</span>
    <span class="k">return</span> <span class="sa">f</span><span class="s">"&lt;h1&gt;Web Access Count: </span><span class="si">{</span><span class="n">count</span><span class="si">}</span><span class="s">&lt;/h1&gt;"</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s">"0.0.0.0"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">9000</span><span class="p">,</span> <span class="n">debug</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">compose.yaml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">redis</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">redis:latest</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">6379:6379"</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">healthcheck</span><span class="pi">:</span>
      <span class="na">test</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">CMD"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">redis-cli"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">ping"</span><span class="pi">]</span>
      <span class="na">interval</span><span class="pi">:</span> <span class="s">5s</span>
      <span class="na">timeout</span><span class="pi">:</span> <span class="s">3s</span>
      <span class="na">retries</span><span class="pi">:</span> <span class="m">5</span>

  <span class="na">flask</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">.</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">9000:9000"</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="na">redis</span><span class="pi">:</span>
        <span class="na">condition</span><span class="pi">:</span> <span class="s">service_healthy</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
</code></pre></div></div>

<p>Application을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
</code></pre></div></div>

<p>Dockerfile만 사용하면 Image Build와 Container 실행 명령을 각각 수행해야 한다. Compose의 <code class="language-plaintext highlighter-rouge">build</code>를 사용하면 필요한 Image Build와 여러 Service 실행을 <code class="language-plaintext highlighter-rouge">docker compose up</code>으로 함께 처리할 수 있다.</p>

<h2 id="4--compose-명령과-scale-조정">4 ) Compose 명령과 Scale 조정</h2>

<hr />

<p>주요 Compose 명령은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">build</code></td>
      <td>Dockerfile을 이용해 Service Image Build</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">config</code></td>
      <td>Compose File 해석 및 검증 결과 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">create</code></td>
      <td>Service Container 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">up</code></td>
      <td>Service 생성 및 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">down</code></td>
      <td>Project의 Container와 Network 중지 후 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">events</code></td>
      <td>Container Event 실시간 수신</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">exec</code></td>
      <td>실행 중인 Service Container에서 명령 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">images</code></td>
      <td>Service가 사용하는 Image 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kill</code></td>
      <td>Service Container 강제 종료</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">logs</code></td>
      <td>Service Log 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pause</code>, <code class="language-plaintext highlighter-rouge">unpause</code></td>
      <td>Service Container 일시 정지 및 재개</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">port</code></td>
      <td>공개 Port 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ps</code></td>
      <td>Service Container 상태 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pull</code>, <code class="language-plaintext highlighter-rouge">push</code></td>
      <td>Service Image 가져오기 및 배포</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">restart</code>, <code class="language-plaintext highlighter-rouge">start</code>, <code class="language-plaintext highlighter-rouge">stop</code></td>
      <td>Service Container 재시작·시작·중지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm</code></td>
      <td>중지된 Service Container 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">run</code></td>
      <td>특정 Service 설정으로 일회성 명령 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">top</code></td>
      <td>실행 중인 Process 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">version</code></td>
      <td>Compose Version 출력</td>
    </tr>
  </tbody>
</table>

<p>과거 Compose V1에는 <code class="language-plaintext highlighter-rouge">scale</code> 하위 명령이 있었지만 현재는 <code class="language-plaintext highlighter-rouge">docker compose up --scale</code>을 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">server_web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd:2</span>
  <span class="na">server_db</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">redis</span>
</code></pre></div></div>

<p>기본 구성을 실행하고 확인한 뒤 종료한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
docker compose down
</code></pre></div></div>

<p>각 Service의 Replica 수를 3개로 조정하여 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="se">\</span>
  <span class="nt">--scale</span> <span class="nv">server_db</span><span class="o">=</span>3 <span class="se">\</span>
  <span class="nt">--scale</span> <span class="nv">server_web</span><span class="o">=</span>3 <span class="se">\</span>
  <span class="nt">-d</span>
</code></pre></div></div>

<p>Scale 대상 Service에 <code class="language-plaintext highlighter-rouge">container_name</code>을 지정하면 Container 이름이 중복되므로 Replica를 여러 개 만들 수 없다. Host의 고정 Port도 각 Replica가 동시에 사용할 수 없으므로 Scale 구성에서는 Port 충돌을 함께 고려해야 한다.</p>

<p>Project를 삭제할 때 사용할 수 있는 주요 Option은 다음과 같다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose <span class="o">[</span><span class="nt">-f</span> FILE] down <span class="o">[</span>OPTIONS]
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--rmi all</code></td>
      <td>Service가 사용하는 모든 Image 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--rmi local</code></td>
      <td>Custom Tag가 없는 Image만 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code>, <code class="language-plaintext highlighter-rouge">--volumes</code></td>
      <td>Compose File에 선언한 Named Volume과 Anonymous Volume 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--remove-orphans</code></td>
      <td>현재 Compose File에 정의되지 않은 Project Container도 삭제</td>
    </tr>
  </tbody>
</table>

<p>Volume 삭제는 영속 데이터를 제거할 수 있으므로 대상 데이터를 확인한 뒤 실행한다.</p>

<h2 id="5--load-balancer">5 ) Load Balancer</h2>

<hr />

<p>Load Balancer는 Client와 Server Pool 사이에서 요청을 여러 Server로 분산하는 장치 또는 기술이다. 한 Server에 부하가 집중되는 것을 방지하여 Resource 활용도와 처리량을 높이고 지연 시간을 줄이며, Health Check와 결합하면 장애가 발생한 Server를 대상에서 제외할 수 있다.</p>

<p>Docker 환경에서도 HAProxy, Nginx, Apache HTTP Server 같은 외부 Service를 Container와 결합하여 Load Balancing을 구성할 수 있다.</p>

<h3 id="load-balancing-algorithm">Load Balancing Algorithm</h3>

<table>
  <thead>
    <tr>
      <th>Algorithm</th>
      <th>동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Round Robin</td>
      <td>요청을 Server에 순서대로 분배하며 Weight 적용 가능</td>
    </tr>
    <tr>
      <td>Least Connections</td>
      <td>현재 연결 수가 가장 적은 Server에 요청 전달</td>
    </tr>
    <tr>
      <td>IP Hash</td>
      <td>Client IP의 Hash로 Server를 선택하여 같은 IP의 경로 유지</td>
    </tr>
    <tr>
      <td>Generic Hash</td>
      <td>URI 등 사용자가 정한 Key로 Server 선택</td>
    </tr>
    <tr>
      <td>Least Time</td>
      <td>응답 시간과 연결 수를 고려하여 Server 선택</td>
    </tr>
    <tr>
      <td>Random</td>
      <td>Server를 무작위로 선택</td>
    </tr>
  </tbody>
</table>

<p>IP Hash는 같은 Client를 같은 Server로 보내는 데 유용하지만 Server 장애나 구성 변경 시 경로가 달라질 수 있고 균등 분배를 항상 보장하지 않는다.</p>

<p><code class="language-plaintext highlighter-rouge">least_time</code>은 측정 기준에 따라 첫 Byte를 받는 시간인 <code class="language-plaintext highlighter-rouge">header</code>, 전체 응답을 받는 시간인 <code class="language-plaintext highlighter-rouge">last_byte</code>, 처리 중 요청까지 고려하는 <code class="language-plaintext highlighter-rouge">last_byte inflight</code> 방식을 사용할 수 있다. 이 지시어는 과거에는 Commercial Subscription에서만 제공되었지만 Nginx <code class="language-plaintext highlighter-rouge">1.31.0</code>부터 Open Source에서도 사용할 수 있다. Algorithm과 지시어의 지원 범위는 Nginx Version과 Edition에 따라 확인해야 한다.</p>

<h3 id="nginx-upstream-parameter">Nginx Upstream Parameter</h3>

<table>
  <thead>
    <tr>
      <th>Parameter</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">weight</code></td>
      <td>Server 선택 가중치이며 기본값은 1</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">max_conns</code></td>
      <td>Server의 최대 동시 연결 수 제한</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">queue</code></td>
      <td>즉시 전달할 Server가 없을 때 요청 대기열 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">max_fails</code></td>
      <td>지정 횟수 이상 실패한 Server를 일정 시간 대상에서 제외</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">backup</code></td>
      <td>Primary Server를 사용할 수 없을 때 요청을 받을 Backup 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">down</code></td>
      <td>해당 Server를 일시적으로 분배 대상에서 제외</td>
    </tr>
  </tbody>
</table>

<p>원문의 <code class="language-plaintext highlighter-rouge">max_connes</code>는 Nginx Parameter 이름에 맞게 <code class="language-plaintext highlighter-rouge">max_conns</code>로 바로잡았다. <code class="language-plaintext highlighter-rouge">queue</code> 등 일부 기능은 Nginx Plus에서만 제공될 수 있으므로 사용하는 Edition의 공식 문서를 확인해야 한다.</p>

<h2 id="6--nginx-container-load-balancing-실습">6 ) Nginx Container Load Balancing 실습</h2>

<hr />

<p>Flask Application 세 개와 Nginx Load Balancer를 Compose로 실행한다.</p>

<h3 id="directory-구성">Directory 구성</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>alb
<span class="nb">cd </span>alb
<span class="nb">mkdir </span>nginx_alb pyfla_app1 pyfla_app2 pyfla_app3
</code></pre></div></div>

<p>완성되는 Directory 구조는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>alb/
├── compose.yaml
├── nginx_alb/
│   ├── Dockerfile
│   └── nginx.conf
├── pyfla_app1/
│   ├── Dockerfile
│   └── pyfla_app.py
├── pyfla_app2/
│   ├── Dockerfile
│   └── pyfla_app.py
└── pyfla_app3/
    ├── Dockerfile
    └── pyfla_app.py
</code></pre></div></div>

<h3 id="nginx-구성">Nginx 구성</h3>

<p><code class="language-plaintext highlighter-rouge">nginx_alb/Dockerfile</code>을 작성한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> nginx</span>

<span class="k">RUN </span><span class="nb">rm</span> /etc/nginx/conf.d/default.conf
<span class="k">COPY</span><span class="s"> nginx.conf /etc/nginx/conf.d/default.conf</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">nginx_alb/nginx.conf</code>에는 세 Flask Service를 Upstream으로 지정한다.</p>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">upstream</span> <span class="s">web-alb</span> <span class="p">{</span>
    <span class="kn">server</span> <span class="s">pyfla_app1:5000</span><span class="p">;</span>
    <span class="kn">server</span> <span class="s">pyfla_app2:5000</span><span class="p">;</span>
    <span class="kn">server</span> <span class="s">pyfla_app3:5000</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://web-alb</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>원문은 Host Bridge Address인 <code class="language-plaintext highlighter-rouge">172.17.0.1</code>과 Host에 공개한 <code class="language-plaintext highlighter-rouge">5001</code>~<code class="language-plaintext highlighter-rouge">5003</code> Port를 Upstream으로 사용했다. Compose의 같은 Network에서는 Service 이름과 Container Port로 직접 통신할 수 있으므로 변경될 수 있는 Bridge Address에 의존하지 않도록 구성했다.</p>

<h3 id="flask-application-구성">Flask Application 구성</h3>

<p><code class="language-plaintext highlighter-rouge">pyfla_app1/pyfla_app.py</code>를 작성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">flask</span> <span class="kn">import</span> <span class="n">Flask</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">index</span><span class="p">():</span>
    <span class="k">return</span> <span class="s">"Web Application 1</span><span class="se">\n</span><span class="s">"</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">debug</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">host</span><span class="o">=</span><span class="s">"0.0.0.0"</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">pyfla_app1/Dockerfile</code>을 작성한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python:3</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>
<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> flask

<span class="k">ENTRYPOINT</span><span class="s"> ["python3"]</span>
<span class="k">CMD</span><span class="s"> ["pyfla_app.py"]</span>
</code></pre></div></div>

<p>두 File을 나머지 Application Directory에 복사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp </span>pyfla_app1/pyfla_app.py pyfla_app2/
<span class="nb">cp </span>pyfla_app1/pyfla_app.py pyfla_app3/
<span class="nb">cp </span>pyfla_app1/Dockerfile pyfla_app2/
<span class="nb">cp </span>pyfla_app1/Dockerfile pyfla_app3/
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">pyfla_app2/pyfla_app.py</code>와 <code class="language-plaintext highlighter-rouge">pyfla_app3/pyfla_app.py</code>의 응답 숫자를 각각 <code class="language-plaintext highlighter-rouge">2</code>, <code class="language-plaintext highlighter-rouge">3</code>으로 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano pyfla_app2/pyfla_app.py
nano pyfla_app3/pyfla_app.py
</code></pre></div></div>

<h3 id="compose-file과-실행">Compose File과 실행</h3>

<p><code class="language-plaintext highlighter-rouge">alb/compose.yaml</code>을 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">pyfla_app1</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">./pyfla_app1</span>

  <span class="na">pyfla_app2</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">./pyfla_app2</span>

  <span class="na">pyfla_app3</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">./pyfla_app3</span>

  <span class="na">nginx</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">./nginx_alb</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8000:80"</span>
    <span class="na">depends_on</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">pyfla_app1</span>
      <span class="pi">-</span> <span class="s">pyfla_app2</span>
      <span class="pi">-</span> <span class="s">pyfla_app3</span>
</code></pre></div></div>

<p>세 Flask Service는 Nginx를 통해 접근하므로 Host Port를 별도로 공개하지 않았다. Stack을 Build하고 Background에서 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">--build</span> <span class="nt">-d</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">http://localhost:8000</code>을 반복해서 호출하면 기본 Round Robin 방식으로 세 Application의 응답을 확인할 수 있다.</p>

<h3 id="weight-변경">Weight 변경</h3>

<p>첫 번째 Application에 더 많은 요청을 전달하려면 <code class="language-plaintext highlighter-rouge">nginx.conf</code>의 Weight를 변경한다.</p>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">upstream</span> <span class="s">web-alb</span> <span class="p">{</span>
    <span class="kn">server</span> <span class="s">pyfla_app1:5000</span> <span class="s">weight=3</span><span class="p">;</span>
    <span class="kn">server</span> <span class="s">pyfla_app2:5000</span> <span class="s">weight=1</span><span class="p">;</span>
    <span class="kn">server</span> <span class="s">pyfla_app3:5000</span> <span class="s">weight=1</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://web-alb</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>설정을 Image에 반영하려면 Nginx Service를 다시 Build한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">--build</span> <span class="nt">-d</span> nginx
</code></pre></div></div>

<h2 id="7--환경-변수-삽입-및-관리">7 ) 환경 변수 삽입 및 관리</h2>

<hr />

<p>Container에 환경 변수를 전달하는 방법은 다음과 같다.</p>

<ul>
  <li>
    <p>Compose File에 직접 작성한다.</p>
  </li>
  <li>
    <p>Shell 환경 변수를 사용한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.env</code> 또는 별도의 환경 변수 File을 사용한다.</p>
  </li>
  <li>
    <p>Dockerfile의 <code class="language-plaintext highlighter-rouge">ENV</code>로 Image에 포함한다.</p>
  </li>
</ul>

<p>Compose File에 비밀번호를 직접 작성할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mysql</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">unless-stopped</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3306:3306"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">password</span>
</code></pre></div></div>

<p>이 File을 Git Repository에 Commit하면 비밀번호가 노출된다. Shell 환경 변수로 분리하려면 먼저 값을 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>password
</code></pre></div></div>

<p>Compose File에서는 변수 치환 문법을 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mysql</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">unless-stopped</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3306:3306"</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">${MYSQL_ROOT_PASSWORD}</span>
</code></pre></div></div>

<p>값을 Compose File에서 분리해도 실행 중인 Container의 환경 변수나 Host 설정을 읽을 권한이 있으면 노출될 수 있다. <code class="language-plaintext highlighter-rouge">.env</code> 같은 File은 Version Control에서 제외하고 접근 권한을 관리해야 한다. 보안을 더 강화해야 하는 환경에서는 Docker Secret이나 HashiCorp Vault 같은 Secret 관리 방식을 사용한다.</p>

<h2 id="8--docker-swarm">8 ) Docker Swarm</h2>

<hr />

<p>Docker Swarm Mode는 여러 Docker Host를 하나의 Cluster로 묶어 Service를 배포하고 관리하는 Docker Engine 내장 Orchestration 기능이다. Orchestration 도구가 없다면 여러 Host 중 어디에 Container를 배치할지, 서로 다른 Host의 Container를 어떻게 연결할지, 장애가 발생했을 때 어떻게 복구할지를 직접 조정해야 한다.</p>

<h3 id="주요-용어">주요 용어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker Compose</td>
      <td>주로 단일 Host에서 여러 Container Application 관리</td>
    </tr>
    <tr>
      <td>Swarm</td>
      <td>여러 Docker Engine이 참여하는 Cluster</td>
    </tr>
    <tr>
      <td>Node</td>
      <td>Swarm에 참여한 Docker Host</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>Swarm에서 유지할 Container 실행 상태 정의</td>
    </tr>
    <tr>
      <td>Task</td>
      <td>Node에 배치되는 Service의 실행 단위</td>
    </tr>
    <tr>
      <td>Stack</td>
      <td>여러 Swarm Service로 구성된 전체 Application</td>
    </tr>
  </tbody>
</table>

<h3 id="node-역할과-분산-설계">Node 역할과 분산 설계</h3>

<p>Swarm Node는 Manager와 Worker 역할로 구분한다.</p>

<ul>
  <li>
    <p>Manager Node는 Cluster 상태, Service Scheduling과 원하는 상태 유지를 담당한다.</p>
  </li>
  <li>
    <p>여러 Manager 중 하나가 Raft 합의의 Leader가 되어 관리 명령을 조정한다.</p>
  </li>
  <li>
    <p>Worker Node는 Manager가 할당한 Task를 실행한다.</p>
  </li>
</ul>

<p>Manager Node도 기본적으로 Task를 실행할 수 있다. 관리 부하와 Application 부하를 분리하려면 Service에 다음 Placement Constraint를 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> my-service <span class="se">\</span>
  <span class="nt">--constraint</span> <span class="s1">'node.role!=manager'</span> <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>Kubernetes의 Control Plane Node도 일반적으로 Taint를 통해 일반 Workload 배치를 막지만, 설정에 따라 Workload를 실행할 수 있다. 따라서 “Kubernetes Master는 Container Service를 절대로 실행하지 않는다”라고 구분하기보다 기본 Scheduling 정책과 역할 분리 관점에서 이해해야 한다.</p>

<h3 id="service-확장과-원하는-상태">Service 확장과 원하는 상태</h3>

<p>Replica 수를 지정하면 Swarm Manager가 그 수만큼 Task를 유지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="nt">--name</span> web <span class="nt">--replicas</span> 3 nginx
</code></pre></div></div>

<p>실행 중인 Task에 장애가 발생하여 실제 상태가 사용자가 요청한 상태와 달라지면 Manager는 대체 Task를 생성한다. 이처럼 선언한 상태와 실제 상태가 일치하도록 지속적으로 조정하는 것을 <strong>Desired State Management</strong>라고 한다.</p>

<h3 id="service-scheduling">Service Scheduling</h3>

<p>Manager는 Node의 가용성, Resource와 Placement 조건을 확인하여 Task를 배치한다. 원문의 <code class="language-plaintext highlighter-rouge">swarm manage --strategy</code>와 <code class="language-plaintext highlighter-rouge">spread</code>, <code class="language-plaintext highlighter-rouge">binpack</code>, <code class="language-plaintext highlighter-rouge">random</code> Option은 별도 Classic Swarm 시기의 방식으로 현재 Docker Engine의 Swarm Mode 명령이 아니다.</p>

<p>현재 Swarm Mode에서는 다음 기능으로 배치를 제어한다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">--constraint</code>로 Task가 배치될 수 있는 Node를 제한한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">--placement-pref 'spread=...'</code>로 Label 값을 기준으로 Replica를 분산한다.</p>
  </li>
  <li>
    <p>CPU와 Memory 제한 또는 예약으로 Resource 조건을 설정한다.</p>
  </li>
</ul>

<p>Placement Preference는 강제 조건이 아니며 현재는 <code class="language-plaintext highlighter-rouge">spread</code>만 지원한다. 엄격한 배치 조건이 필요하면 Constraint를 함께 사용한다.</p>

<h3 id="ingress-network와-routing-mesh">Ingress Network와 Routing Mesh</h3>

<p>Swarm을 초기화하거나 참여하면 Service Load Balancing을 위한 특별한 Overlay Network인 <code class="language-plaintext highlighter-rouge">ingress</code>가 생성된다. Service의 Port를 공개하면 모든 Swarm Node가 Routing Mesh에 참여한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service create <span class="se">\</span>
  <span class="nt">--name</span> web <span class="se">\</span>
  <span class="nt">--replicas</span> 1 <span class="se">\</span>
  <span class="nt">--publish</span> <span class="nv">published</span><span class="o">=</span>8080,target<span class="o">=</span>80 <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>위 Service의 Task가 Worker Node 1에만 배치되어 있어도 Worker Node 2의 <code class="language-plaintext highlighter-rouge">8080</code> Port로 들어온 요청을 실행 중인 Task로 전달할 수 있다. Client는 실제 Task가 어느 Node에 있는지 알 필요가 없다.</p>

<p>Published Port를 생략하면 Swarm이 사용 가능한 높은 번호의 Port를 할당한다. 과거 문서에서는 자동 할당 범위를 <code class="language-plaintext highlighter-rouge">30000</code>~<code class="language-plaintext highlighter-rouge">32767</code>로 설명하며, 현재 Docker 문서의 핵심 개념에도 이 범위가 안내되어 있다. 다만 실제 배포에서는 자동 할당 결과를 <code class="language-plaintext highlighter-rouge">docker service inspect</code>로 확인해야 한다.</p>

<p>Routing Mesh를 사용하려면 Node 사이에 다음 Port가 열려 있어야 한다.</p>

<table>
  <thead>
    <tr>
      <th>Port</th>
      <th>Protocol</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">7946</code></td>
      <td>TCP/UDP</td>
      <td>Container Network Discovery</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">4789</code></td>
      <td>UDP</td>
      <td>Ingress Network Traffic</td>
    </tr>
  </tbody>
</table>

<p>Cluster 관리에는 별도로 Manager 통신용 TCP <code class="language-plaintext highlighter-rouge">2377</code>도 필요하다. 외부 Load Balancer나 Client가 Service에 접근한다면 공개한 Service Port도 허용해야 한다.</p>

<h3 id="service-discovery">Service Discovery</h3>

<p>Swarm은 내장 DNS를 통해 Service Discovery를 제공한다. 같은 Overlay Network의 Service는 각 Task의 IP를 직접 관리하지 않고 Service 이름으로 접근할 수 있으며, Swarm이 요청을 실행 중인 Task로 분산한다.</p>

<h3 id="rolling-update와-rollback">Rolling Update와 Rollback</h3>

<p>Swarm Service는 Replica를 단계적으로 교체하는 Rolling Update를 지원한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker service update <span class="se">\</span>
  <span class="nt">--image</span> nginx:stable <span class="se">\</span>
  <span class="nt">--update-parallelism</span> 1 <span class="se">\</span>
  <span class="nt">--update-order</span> start-first <span class="se">\</span>
  <span class="nt">--update-failure-action</span> rollback <span class="se">\</span>
  web
</code></pre></div></div>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">--update-parallelism</code>은 한 번에 Update할 Task 수를 정한다.</p>
  </li>
  <li>
    <p>기본 Update 순서는 기존 Task를 먼저 중지하는 <code class="language-plaintext highlighter-rouge">stop-first</code>이다. 새 Task를 먼저 실행하려면 <code class="language-plaintext highlighter-rouge">start-first</code>를 지정한다.</p>
  </li>
  <li>
    <p>Update 실패 시 기본 동작은 일시 중지이며, 자동 복구가 필요하면 <code class="language-plaintext highlighter-rouge">--update-failure-action rollback</code>을 명시한다.</p>
  </li>
</ul>

<p>따라서 “항상 새 Container를 먼저 만들며 실패하면 자동 Rollback한다”는 동작은 기본값이 아니라 Service Update 정책으로 구성해야 하는 동작이다.</p>

<h2 id="9--docker-swarm과-kubernetes">9 ) Docker Swarm과 Kubernetes</h2>

<hr />

<p>두 도구 모두 여러 Node에 Container Workload를 배치하고 원하는 상태를 유지하지만 적용 환경과 기능 범위가 다르다.</p>

<table>
  <thead>
    <tr>
      <th>기준</th>
      <th>Docker Swarm</th>
      <th>Kubernetes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>구성</td>
      <td>Docker Engine에 내장되어 비교적 단순</td>
      <td>별도의 Cluster 구성 요소와 Resource Model 사용</td>
    </tr>
    <tr>
      <td>학습 곡선</td>
      <td>Docker를 익혔다면 시작하기 쉬움</td>
      <td>개념과 구성 요소가 더 많음</td>
    </tr>
    <tr>
      <td>생태계와 확장성</td>
      <td>기본 Orchestration 기능 중심</td>
      <td>Network, Storage, Policy와 확장 생태계가 큼</td>
    </tr>
    <tr>
      <td>Cloud 연동</td>
      <td>Cluster를 직접 구성하는 형태가 일반적</td>
      <td>주요 Cloud에서 Managed Kubernetes 제공</td>
    </tr>
    <tr>
      <td>적합한 경우</td>
      <td>단순한 Docker 기반 Cluster와 학습</td>
      <td>복잡한 운영 요구와 Cloud Native Platform</td>
    </tr>
  </tbody>
</table>

<p>Docker Compose에서 익힌 Service와 Network 개념은 Swarm으로 이어지며, Swarm의 Desired State, Scheduling, Service Discovery, Rolling Update와 Ingress는 Kubernetes를 학습할 때도 연결되는 핵심 개념이다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Compose File의 <code class="language-plaintext highlighter-rouge">services</code>, <code class="language-plaintext highlighter-rouge">networks</code>, <code class="language-plaintext highlighter-rouge">volumes</code>로 여러 Container Application을 선언한다.</p>
    </li>
    <li>
      <p>같은 Network의 Container는 변경될 수 있는 IP 대신 Service 이름으로 통신한다.</p>
    </li>
    <li>
      <p>Nginx Upstream과 Compose Service를 결합하여 여러 Flask Application에 요청을 분산할 수 있다.</p>
    </li>
    <li>
      <p>환경 변수를 File 밖으로 분리해도 자동으로 안전해지는 것은 아니므로 접근 권한과 Secret 관리 방식을 함께 고려한다.</p>
    </li>
    <li>
      <p>Docker Swarm은 여러 Docker Host에서 Desired State, Scheduling, Service Discovery, Rolling Update와 Ingress Routing Mesh를 제공한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><category term="DockerSwarm" /><summary type="html"><![CDATA[다중 Container 실습, Nginx Load Balancing, 환경 변수 관리와 Docker Swarm의 핵심 구조]]></summary></entry><entry><title type="html">애플리케이션별 Docker Multi-stage Build</title><link href="https://kkr010128.github.io/cloud-native-15-multi-stage-build/" rel="alternate" type="text/html" title="애플리케이션별 Docker Multi-stage Build" /><published>2026-08-20T00:00:00+09:00</published><updated>2026-08-20T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-15-multi-stage-build</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-15-multi-stage-build/"><![CDATA[<h2 id="1--애플리케이션과-multi-stage-build">1 ) 애플리케이션과 Multi-stage Build</h2>

<hr />

<p>애플리케이션의 언어와 Framework가 달라도 Multi-stage Build의 기본 흐름은 같다.</p>

<ol>
  <li>
    <p>Builder Stage에서 의존성을 설치한다.</p>
  </li>
  <li>
    <p>Source Code를 Build하거나 실행에 필요한 환경을 준비한다.</p>
  </li>
  <li>
    <p>Runtime Stage를 별도의 Base Image로 시작한다.</p>
  </li>
  <li>
    <p>Builder Stage에서 만든 산출물만 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p>최종 Image에서 Application을 실행한다.</p>
  </li>
</ol>

<p>언어별 차이는 최종 산출물과 실행 환경에 있다.</p>

<table>
  <thead>
    <tr>
      <th>애플리케이션</th>
      <th>Builder Stage의 작업</th>
      <th>Runtime Stage로 전달하는 대상</th>
      <th>Runtime</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Node.js</td>
      <td>Package 설치</td>
      <td>Source Code와 설치된 Package</td>
      <td>Node.js</td>
    </tr>
    <tr>
      <td>Spring Boot</td>
      <td>Gradle Build</td>
      <td>JAR</td>
      <td>JRE</td>
    </tr>
    <tr>
      <td>Go</td>
      <td>Source Code Compile</td>
      <td>실행 Binary</td>
      <td>Alpine Linux</td>
    </tr>
    <tr>
      <td>Python</td>
      <td>가상 환경에 Package 설치</td>
      <td>Python 가상 환경</td>
      <td>Python</td>
    </tr>
    <tr>
      <td>React</td>
      <td>정적 File Build</td>
      <td><code class="language-plaintext highlighter-rouge">dist</code> Directory</td>
      <td>Nginx</td>
    </tr>
  </tbody>
</table>

<p>Spring Boot, Go, React는 Build 산출물이 명확하므로 Builder의 Build 도구를 최종 Image에서 제외하기 쉽다. Node.js와 Python은 Source Code를 Runtime에서 실행하므로 의존성 설치 결과를 별도 Stage로 전달한다.</p>

<h2 id="2--nodejs-multi-stage-build">2 ) Node.js Multi-stage Build</h2>

<hr />

<p>Node.js 예제는 Builder Stage에서 Package를 설치하고 Runtime Stage에서는 더 작은 Alpine 기반 Node.js Image로 Application을 실행한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">node:20-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">FROM</span><span class="s"> node:20-alpine</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /app ./</span>

<span class="k">EXPOSE</span><span class="s"> 3000</span>

<span class="k">USER</span><span class="s"> node</span>

<span class="k">CMD</span><span class="s"> ["node", "app.js"]</span>
</code></pre></div></div>

<p>Build 과정은 다음과 같다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node:20-slim</code>에서 <code class="language-plaintext highlighter-rouge">package.json</code>을 먼저 복사하고 Package를 설치한다.</p>
  </li>
  <li>
    <p>Source Code를 <code class="language-plaintext highlighter-rouge">/app</code>에 복사한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node:20-alpine</code>으로 Runtime Stage를 시작한다.</p>
  </li>
  <li>
    <p>Builder의 <code class="language-plaintext highlighter-rouge">/app</code>을 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node</code> 사용자로 <code class="language-plaintext highlighter-rouge">app.js</code>를 실행한다.</p>
  </li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> nodeapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p>이 예제는 Builder의 Application Directory 전체를 복사하므로 Source Code와 <code class="language-plaintext highlighter-rouge">node_modules</code>가 모두 Runtime Stage로 전달된다. 두 Stage를 분리하는 흐름과 더 작은 Runtime Base Image를 사용하는 방법을 보여주는 예제이다.</p>

<h2 id="3--spring-boot-gradle-multi-stage-build">3 ) Spring Boot Gradle Multi-stage Build</h2>

<hr />

<p>Spring Boot는 JDK와 Gradle이 필요한 Build 환경과 JAR 실행에 필요한 JRE 환경을 분리한다.</p>

<h3 id="프로젝트-준비">프로젝트 준비</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/itggangpae/spring_docker.git
<span class="nb">cd </span>spring_docker
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">eclipse-temurin:21-jdk</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> gradlew ./</span>
<span class="k">COPY</span><span class="s"> gradle ./gradle</span>
<span class="k">RUN </span><span class="nb">chmod</span> +x gradlew

<span class="k">COPY</span><span class="s"> build.gradle ./</span>
<span class="k">COPY</span><span class="s"> settings.gradle ./</span>
<span class="k">RUN </span>./gradlew dependencies <span class="nt">--no-daemon</span>

<span class="k">COPY</span><span class="s"> src ./src</span>
<span class="k">RUN </span>./gradlew clean build <span class="nt">-x</span> <span class="nb">test</span> <span class="nt">--no-daemon</span>

<span class="k">FROM</span><span class="s"> eclipse-temurin:21-jre</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/build/libs/*.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>이전 실습에서는 Host에서 Gradle Build를 수행하고 생성된 JAR를 Docker Image에 복사했다. Multi-stage Build에서는 다음 과정이 하나의 Dockerfile 안에서 이루어진다.</p>

<ol>
  <li>
    <p>JDK Image에서 Gradle Wrapper로 JAR를 Build한다.</p>
  </li>
  <li>
    <p>새로운 JRE Image를 Runtime Stage로 사용한다.</p>
  </li>
  <li>
    <p>Builder에서 생성한 JAR만 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p>최종 Image에는 Gradle, Source Code와 JDK가 남지 않는다.</p>
  </li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> spring-docker:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">build.gradle</code>과 <code class="language-plaintext highlighter-rouge">settings.gradle</code>을 Source Code보다 먼저 복사하여 의존성 Layer를 분리했다. 의존성 설정이 바뀌지 않았다면 Source Code 변경 시 기존 의존성 Cache를 활용할 수 있다.</p>

<h2 id="4--go-multi-stage-build">4 ) Go Multi-stage Build</h2>

<hr />

<p>Go는 Builder Stage에서 Source Code를 실행 Binary로 Compile하고 최종 Image에는 Binary만 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">golang:1.23-alpine</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> go.mod go.sum* ./</span>
<span class="k">RUN </span>go mod download

<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">RUN </span><span class="nv">CGO_ENABLED</span><span class="o">=</span>0 <span class="nv">GOOS</span><span class="o">=</span>linux go build <span class="nt">-o</span> main .

<span class="k">FROM</span><span class="s"> alpine:latest</span>

<span class="k">RUN </span>apk <span class="nt">--no-cache</span> add ca-certificates

<span class="k">WORKDIR</span><span class="s"> /root</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/main ./main</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["./main"]</span>
</code></pre></div></div>

<p>각 Stage의 역할은 다음과 같다.</p>

<ul>
  <li>
    <p>Builder Stage는 Go Compiler와 Module 의존성을 사용한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">CGO_ENABLED=0</code>으로 C Library에 대한 동적 의존성을 사용하지 않는 Binary를 Build한다.</p>
  </li>
  <li>
    <p>Runtime Stage는 <code class="language-plaintext highlighter-rouge">alpine:latest</code>를 사용하고 HTTPS 인증에 필요한 CA 인증서를 설치한다.</p>
  </li>
  <li>
    <p>최종 Image에는 Go Compiler와 Source Code 대신 <code class="language-plaintext highlighter-rouge">main</code> Binary만 포함한다.</p>
  </li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> goapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<h2 id="5--python-multi-stage-build">5 ) Python Multi-stage Build</h2>

<hr />

<p>Python은 Compile된 단일 실행 File보다 Source Code와 Package가 설치된 환경이 필요하다. Builder Stage에서 가상 환경을 만들고 Runtime Stage로 해당 가상 환경을 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">python:3.12-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">ENV</span><span class="s"> PYTHONDONTWRITEBYTECODE=1 \</span>
    PYTHONUNBUFFERED=1

<span class="k">RUN </span>python <span class="nt">-m</span> venv /opt/venv
<span class="k">ENV</span><span class="s"> PATH="/opt/venv/bin:$PATH"</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">FROM</span><span class="s"> python:3.12-slim</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /opt/venv /opt/venv</span>

<span class="k">ENV</span><span class="s"> PATH="/opt/venv/bin:$PATH" \</span>
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">RUN </span>adduser <span class="nt">--disabled-password</span> <span class="nt">--gecos</span> <span class="s2">""</span> appuser
<span class="k">USER</span><span class="s"> appuser</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]</span>
</code></pre></div></div>

<p>주요 설정은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PYTHONDONTWRITEBYTECODE=1</code></td>
      <td><code class="language-plaintext highlighter-rouge">.pyc</code> File 생성 방지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PYTHONUNBUFFERED=1</code></td>
      <td>표준 출력과 오류의 Buffering 비활성화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/opt/venv</code></td>
      <td>Builder와 Runtime에서 공통으로 사용하는 가상 환경 경로</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">appuser</code></td>
      <td>Application을 실행할 일반 사용자</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> fastapiapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p>강의에서는 처음에 일반 <code class="language-plaintext highlighter-rouge">python</code> Image를 사용한 뒤 <code class="language-plaintext highlighter-rouge">python:3.12-slim</code>으로 변경했다. Python은 Compiler를 완전히 제거하는 방식보다 작은 Base Image를 선택하고 가상 환경만 복사하는 방식으로 최종 Image를 줄인다.</p>

<h2 id="6--react-정적-file-multi-stage-build">6 ) React 정적 File Multi-stage Build</h2>

<hr />

<p>React Source Code는 Browser가 직접 실행할 수 있는 정적 HTML, JavaScript와 CSS File로 Build한다. Docker 환경에서는 Nginx나 Apache로 정적 File을 제공할 수 있고, Cloud 환경에서는 Web Endpoint를 제공하는 Object Storage에 배포할 수도 있다.</p>

<h3 id="프로젝트-생성과-확인">프로젝트 생성과 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm create vite@latest my-react-app <span class="nt">--</span> <span class="nt">--template</span> react
<span class="nb">cd </span>my-react-app
npm <span class="nb">install
</span>npm run dev <span class="nt">--</span> <span class="nt">--host</span> 0.0.0.0
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:5173
</code></pre></div></div>

<h3 id="dockerfile-1">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">node:20-alpine</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">RUN </span>npm run build

<span class="k">FROM</span><span class="s"> nginx:stable-alpine</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/dist /usr/share/nginx/html</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["nginx", "-g", "daemon off;"]</span>
</code></pre></div></div>

<p>Builder Stage에서는 Vite가 React Source Code를 <code class="language-plaintext highlighter-rouge">/app/dist</code>의 정적 File로 변환한다. Runtime Stage에는 Node.js, npm, Source Code를 복사하지 않고 <code class="language-plaintext highlighter-rouge">dist</code>만 Nginx의 기본 Document Root에 복사한다.</p>

<h3 id="dockerignore"><code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node_modules/
dist/
.git/
.DS_Store
</code></pre></div></div>

<h3 id="image-build와-container-실행">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> my-react-app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> my-react-app <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  my-react-app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="7--언어별-차이-비교">7 ) 언어별 차이 비교</h2>

<hr />

<p>반복된 실습의 핵심은 Dockerfile의 형태가 아니라 Builder에서 Runtime으로 무엇을 전달하는지에 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Node.js</th>
      <th>Spring Boot</th>
      <th>Go</th>
      <th>Python</th>
      <th>React</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>실행 형태</td>
      <td>Source Code</td>
      <td>JAR</td>
      <td>Native Binary</td>
      <td>Source Code</td>
      <td>정적 File</td>
    </tr>
    <tr>
      <td>Build 도구</td>
      <td>npm</td>
      <td>Gradle·JDK</td>
      <td>Go Compiler</td>
      <td>pip·가상 환경</td>
      <td>npm·Vite</td>
    </tr>
    <tr>
      <td>Runtime</td>
      <td>Node.js</td>
      <td>JRE</td>
      <td>Alpine</td>
      <td>Python</td>
      <td>Nginx</td>
    </tr>
    <tr>
      <td>전달 대상</td>
      <td>Application Directory</td>
      <td><code class="language-plaintext highlighter-rouge">app.jar</code></td>
      <td><code class="language-plaintext highlighter-rouge">main</code></td>
      <td><code class="language-plaintext highlighter-rouge">/opt/venv</code></td>
      <td><code class="language-plaintext highlighter-rouge">/app/dist</code></td>
    </tr>
    <tr>
      <td>주요 절감 대상</td>
      <td>Builder Base Image</td>
      <td>JDK·Gradle·Source</td>
      <td>Compiler·Source</td>
      <td>Build Cache</td>
      <td>Node.js·Source</td>
    </tr>
  </tbody>
</table>

<p>Multi-stage Build가 모든 언어에서 동일한 크기 절감 효과를 만드는 것은 아니다. Compile 결과만 전달할 수 있는 Spring Boot, Go, React에서는 Build 환경과 Runtime 환경의 구분이 명확하다. Node.js와 Python은 Runtime에도 언어 실행 환경과 Package가 필요하므로 필요한 의존성을 선별하고 작은 Base Image를 사용하는 것이 중요하다.</p>

<h2 id="8--종합-실습">8 ) 종합 실습</h2>

<hr />

<p>강의의 종합 실습은 Database와 Web Application을 Container로 구성하는 과정이다.</p>

<ol>
  <li>
    <p>MySQL, MariaDB, PostgreSQL, MongoDB, Redis 중 하나를 선택한다.</p>
  </li>
  <li>
    <p>외부에서 접속할 수 있도록 Database Container를 생성한다.</p>
  </li>
  <li>
    <p>프로그래밍 언어로 Database에 접속하는 Web Application을 작성한다.</p>
  </li>
  <li>
    <p>Table에 데이터 한 건을 삽입하고 전체 데이터를 조회하는 기능을 구현한다.</p>
  </li>
  <li>
    <p>Web Application을 Multi-stage Build로 Docker Image로 만든다.</p>
  </li>
  <li>
    <p>Image로 Container를 실행하고 외부에서 접속되는지 확인한다.</p>
  </li>
</ol>

<p>Kafka를 이용한 CQRS 구현은 별도의 Kafka·CQRS 실습 범위에 해당하므로 이 문서에서는 다루지 않는다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Multi-stage Build는 Build 환경과 Runtime 환경을 서로 다른 Stage로 분리한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">COPY --from</code>으로 Builder에서 생성한 실행 산출물이나 의존성 환경만 Runtime Stage에 전달한다.</p>
    </li>
    <li>
      <p>Spring Boot는 JAR, Go는 실행 Binary, React는 정적 File을 최종 Image에 복사한다.</p>
    </li>
    <li>
      <p>Node.js와 Python은 Runtime에도 언어 실행 환경과 Package가 필요하다.</p>
    </li>
    <li>
      <p>의존성 File을 Source Code보다 먼저 복사하면 Build Cache를 효율적으로 사용할 수 있다.</p>
    </li>
    <li>
      <p>최종 Stage에서 작은 Base Image와 일반 사용자를 사용하면 Image 크기와 실행 권한을 함께 관리할 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Node.js, Spring Boot, Go, Python, React 애플리케이션의 Multi-stage Image Build 방법]]></summary></entry><entry><title type="html">Docker Compose</title><link href="https://kkr010128.github.io/cloud-native-16-docker-compose/" rel="alternate" type="text/html" title="Docker Compose" /><published>2026-08-20T00:00:00+09:00</published><updated>2026-08-20T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-16-docker-compose</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-16-docker-compose/"><![CDATA[<h2 id="1--docker-compose-개요">1 ) Docker Compose 개요</h2>

<hr />

<p>분산 Application은 Web Frontend, Backend API, Database처럼 여러 구성 요소로 이루어진다. Dockerfile은 각 구성 요소를 하나의 Image로 Packaging하지만 전체 Application을 실행하려면 여러 Container의 Port, Volume, 환경 변수와 Network를 함께 설정해야 한다.</p>

<p>각 Container를 <code class="language-plaintext highlighter-rouge">docker container run</code>으로 하나씩 실행할 수도 있지만 명령이 많아질수록 Option을 누락하거나 실행 순서를 잘못 지정하기 쉽다. Docker Compose는 공통 목적을 가진 여러 Container의 구성을 하나의 YAML File에 선언하고 함께 관리하는 도구이다.</p>

<ul>
  <li>
    <p>각 Application 구성 요소를 <code class="language-plaintext highlighter-rouge">services</code>에 정의한다.</p>
  </li>
  <li>
    <p>Container의 Image, Port, Volume, 환경 변수와 재시작 정책을 기록한다.</p>
  </li>
  <li>
    <p>필요한 Network와 Volume 같은 Docker Object를 생성한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker compose up</code>으로 Application Stack을 생성하고 실행한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker compose down</code>으로 Stack의 Container와 Network를 중지하고 삭제한다.</p>
  </li>
</ul>

<p>Compose File은 여러 Service가 실행 중일 때 갖추어야 하는 원하는 상태를 기술한다. Compose가 File을 읽으면 Docker API를 통해 Container, Network와 Volume을 구성한다.</p>

<h2 id="2--dockerfile과-docker-compose">2 ) Dockerfile과 Docker Compose</h2>

<hr />

<p>Dockerfile과 Docker Compose는 서로 다른 범위를 담당한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Dockerfile</th>
      <th>Docker Compose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목적</td>
      <td>하나의 Image 생성</td>
      <td>여러 Container로 구성된 Application 실행</td>
    </tr>
    <tr>
      <td>주요 정의</td>
      <td>Base Image, Package, Source Code, 실행 명령</td>
      <td>Service, Port, Volume, 환경 변수, Network</td>
    </tr>
    <tr>
      <td>주요 명령</td>
      <td><code class="language-plaintext highlighter-rouge">docker build</code></td>
      <td><code class="language-plaintext highlighter-rouge">docker compose up</code></td>
    </tr>
    <tr>
      <td>결과</td>
      <td>Docker Image</td>
      <td>실행 중인 Application Stack</td>
    </tr>
  </tbody>
</table>

<p>Web Frontend, Backend API와 Database가 있다면 구성 요소마다 Dockerfile을 작성할 수 있다. Compose File은 이렇게 만든 Image와 외부 Image를 조합하여 전체 Application의 실행 관계를 정의한다.</p>

<p>Compose로 실행한 Container도 각자 독립된 기능을 담당한다. 같은 Compose Project의 Service는 기본 Network에 연결되므로 IP 주소 대신 Service 이름으로 서로 통신할 수 있다.</p>

<p>Docker Compose는 개발, 테스트, CI와 운영 환경에서 사용할 수 있다. 다만 여러 Host에 걸친 자동 확장, 장애 복구와 Cluster Scheduling 같은 기능을 제공하는 Orchestrator와 역할이 같지는 않다. 그러한 운영 기능이 필요하면 Docker Swarm이나 Kubernetes 같은 별도의 Orchestration Platform을 검토한다.</p>

<h2 id="3--docker-compose-설치">3 ) Docker Compose 설치</h2>

<hr />

<p>Docker Desktop에는 Docker Compose가 포함되므로 Windows, macOS와 Docker Desktop을 사용하는 Linux에서는 별도로 설치할 필요가 없다.</p>

<p>Docker Engine과 Docker CLI만 설치된 Linux에서는 Docker의 Package Repository를 설정한 뒤 Compose Plugin을 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> docker-compose-plugin
docker compose version
</code></pre></div></div>

<p>현재 권장 방식은 Docker CLI Plugin과 공백을 사용한 <code class="language-plaintext highlighter-rouge">docker compose</code> 명령이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
docker compose down
</code></pre></div></div>

<p>강의에서 사용한 <code class="language-plaintext highlighter-rouge">docker-compose</code> 명령과 <code class="language-plaintext highlighter-rouge">/usr/local/bin/docker-compose</code>에 실행 File을 직접 설치하는 방식은 Standalone Compose 방식이다. 현재는 하위 호환을 위해 제공되는 Legacy 설치 방식이므로 이 문서의 실행 예제에서는 Compose Plugin을 사용한다.</p>

<h2 id="4--compose-file-구조">4 ) Compose File 구조</h2>

<hr />

<p>Compose File의 기본 이름으로 <code class="language-plaintext highlighter-rouge">compose.yaml</code> 또는 <code class="language-plaintext highlighter-rouge">docker-compose.yaml</code>을 사용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:80"</span>
</code></pre></div></div>

<p>주요 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">services</code></td>
      <td>Application을 구성하는 Service 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">image</code></td>
      <td>Container 생성에 사용할 Image</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">build</code></td>
      <td>Dockerfile을 이용해 Image를 Build할 경로와 Option</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ports</code></td>
      <td><code class="language-plaintext highlighter-rouge">HOST_PORT:CONTAINER_PORT</code> 형식의 Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Service의 영속 데이터와 Host File 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>Container에 전달할 환경 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">restart</code></td>
      <td>Container 종료 후 재시작 정책</td>
    </tr>
  </tbody>
</table>

<p>과거 Compose File에서는 <code class="language-plaintext highlighter-rouge">version: "3"</code> 또는 <code class="language-plaintext highlighter-rouge">version: "3.8"</code>처럼 File 형식 Version을 작성했다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.8"</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
</code></pre></div></div>

<p>최상위 <code class="language-plaintext highlighter-rouge">version</code> 속성은 현재 Compose Specification에서 호환을 위해 남아 있는 Obsolete 항목이다. Compose는 이 값과 관계없이 현재 Specification으로 File을 검증하므로 새로운 Compose File에서는 생략한다.</p>

<h2 id="5--apache-service">5 ) Apache Service</h2>

<hr />

<h3 id="docker-container-run으로-실행"><code class="language-plaintext highlighter-rouge">docker container run</code>으로 실행</h3>

<p>Apache HTTP Server Container 하나를 직접 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> apache_1 <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8080:80 <span class="se">\</span>
  httpd
</code></pre></div></div>

<h3 id="compose-file로-실행">Compose File로 실행</h3>

<p>동일한 설정을 <code class="language-plaintext highlighter-rouge">compose.yaml</code>에 선언한다. 앞의 Container와 동시에 비교할 수 있도록 Host의 8081번 Port를 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">apache_1</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8081:80"</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
</code></pre></div></div>

<p>Compose File이 있는 Directory에서 Service를 실행하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
curl http://localhost:8081
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker container run</code>의 <code class="language-plaintext highlighter-rouge">--name</code>, <code class="language-plaintext highlighter-rouge">-p</code>, <code class="language-plaintext highlighter-rouge">--restart</code> Option이 Compose File의 Service 이름, <code class="language-plaintext highlighter-rouge">ports</code>, <code class="language-plaintext highlighter-rouge">restart</code> 항목에 대응한다.</p>

<h2 id="6--mariadb-service">6 ) MariaDB Service</h2>

<hr />

<h3 id="docker-container-run으로-실행-1"><code class="language-plaintext highlighter-rouge">docker container run</code>으로 실행</h3>

<p>강의에서는 다음 명령으로 MariaDB 10.4.6 Container를 실행했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> mariadb-server <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 3306:3306 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>your_password <span class="se">\</span>
  <span class="nt">-v</span> ~/mariadb/data:/var/lib/mysql <span class="se">\</span>
  <span class="nt">--restart</span> always <span class="se">\</span>
  mariadb:10.4.6
</code></pre></div></div>

<p>MariaDB 10.4 계열은 유지 관리가 종료된 Legacy Version이다. 명령의 Option 대응 관계를 이해하기 위한 강의 예제로 보존하며, 새 환경의 실행 예제에는 지원 중인 10.11 계열을 사용한다.</p>

<h3 id="compose-file로-실행-1">Compose File로 실행</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mariadb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb:10.11</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">mariadb-server</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3307:3306"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./data:/var/lib/mysql</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">${MARIADB_ROOT_PASSWORD}</span>
</code></pre></div></div>

<p>Compose File과 같은 Directory에 <code class="language-plaintext highlighter-rouge">.env</code>를 만들고 실습용 비밀번호를 지정한다.</p>

<pre><code class="language-dotenv">MARIADB_ROOT_PASSWORD=your_password
</code></pre>

<p><code class="language-plaintext highlighter-rouge">.env</code>에는 비밀번호가 포함되므로 Version Control에 Commit하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.env
</code></pre></div></div>

<p>Service를 실행하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
</code></pre></div></div>

<p>두 실행 방식의 Option은 다음과 같이 대응한다.</p>

<table>
  <thead>
    <tr>
      <th><code class="language-plaintext highlighter-rouge">docker container run</code></th>
      <th>Compose File</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name mariadb-server</code></td>
      <td><code class="language-plaintext highlighter-rouge">container_name</code></td>
      <td>Container 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p 3306:3306</code></td>
      <td><code class="language-plaintext highlighter-rouge">ports</code></td>
      <td>Host와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v PATH:/var/lib/mysql</code></td>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Database 데이터 영속화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e MYSQL_ROOT_PASSWORD=...</code></td>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>초기 Root 비밀번호 전달</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--restart always</code></td>
      <td><code class="language-plaintext highlighter-rouge">restart: always</code></td>
      <td>Container 재시작 정책</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">~/mariadb/data</code>는 사용자의 Home Directory를 기준으로 하고 <code class="language-plaintext highlighter-rouge">./data</code>는 Compose File이 있는 Project Directory를 기준으로 한다. 상대 경로를 사용하면 Project File과 Database 데이터를 같은 Directory 구조 안에서 관리하기 쉽다.</p>

<h2 id="7--compose-network">7 ) Compose Network</h2>

<hr />

<p>Compose는 별도의 Network를 지정하지 않아도 Project 전용 기본 Network를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network <span class="nb">ls</span>
</code></pre></div></div>

<p>같은 Compose Network에 연결된 Service는 Container IP가 아니라 Service 이름으로 통신할 수 있다. 예를 들어 Backend Service에서 MariaDB에 접속할 때 Host를 <code class="language-plaintext highlighter-rouge">localhost</code>로 지정하지 않고 <code class="language-plaintext highlighter-rouge">mariadb</code>로 지정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host: mariadb
Port: 3306
</code></pre></div></div>

<p>Container 내부에서 <code class="language-plaintext highlighter-rouge">localhost</code>는 해당 Container 자신을 가리킨다. Host에 공개한 3307번 Port는 Host에서 MariaDB에 접속할 때 사용하고, Compose Network 안의 다른 Container는 <code class="language-plaintext highlighter-rouge">mariadb:3306</code>으로 접속한다.</p>

<h2 id="8--compose-생명주기-관리">8 ) Compose 생명주기 관리</h2>

<hr />

<p>주요 명령은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose up -d</code></td>
      <td>Service 생성 및 Background 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose ps</code></td>
      <td>Project의 Container 상태 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose logs</code></td>
      <td>Service Log 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose stop</code></td>
      <td>Container를 삭제하지 않고 중지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose start</code></td>
      <td>중지한 Container 시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose down</code></td>
      <td>Container와 기본 Network 중지 및 삭제</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose logs apache_1
docker compose stop
docker compose start
docker compose down
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker compose down</code>은 기본적으로 Container와 Compose가 만든 Network를 삭제한다. 위 MariaDB 예제의 <code class="language-plaintext highlighter-rouge">./data</code>는 Host Bind Mount이므로 Container를 삭제해도 Host Directory의 데이터는 유지된다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Dockerfile은 하나의 Image를 만들고 Docker Compose는 여러 Container로 구성된 Application을 실행한다.</p>
    </li>
    <li>
      <p>Compose File에는 Service별 Image, Port, Volume, 환경 변수와 재시작 정책을 YAML로 선언한다.</p>
    </li>
    <li>
      <p>현재는 Standalone <code class="language-plaintext highlighter-rouge">docker-compose</code>보다 Docker CLI Plugin의 <code class="language-plaintext highlighter-rouge">docker compose</code> 명령을 사용한다.</p>
    </li>
    <li>
      <p>최상위 <code class="language-plaintext highlighter-rouge">version</code> 속성은 현재 Obsolete이므로 새로운 Compose File에서는 생략한다.</p>
    </li>
    <li>
      <p>같은 Compose Project의 Service는 기본 Network에서 Service 이름으로 통신한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker compose up</code>으로 Stack을 생성하고 <code class="language-plaintext highlighter-rouge">docker compose down</code>으로 Container와 기본 Network를 정리한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><category term="DockerCompose" /><summary type="html"><![CDATA[Docker Compose의 역할과 Compose File 작성 방법 및 Apache와 MariaDB 실행 실습]]></summary></entry><entry><title type="html">애플리케이션별 Docker 이미지 빌드</title><link href="https://kkr010128.github.io/cloud-native-14-application-image/" rel="alternate" type="text/html" title="애플리케이션별 Docker 이미지 빌드" /><published>2026-08-19T00:00:00+09:00</published><updated>2026-08-19T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-14-application-image</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-14-application-image/"><![CDATA[<h2 id="1--애플리케이션-image-build-흐름">1 ) 애플리케이션 Image Build 흐름</h2>

<hr />

<p>애플리케이션의 언어와 Framework가 달라도 Docker Image를 만드는 전체 과정은 비슷하다.</p>

<ol>
  <li>
    <p>Host에서 Application을 작성하고 실행을 확인한다.</p>
  </li>
  <li>
    <p>Package 의존성을 File로 정의한다.</p>
  </li>
  <li>
    <p>Base Image, 작업 Directory, 복사 대상과 실행 명령을 Dockerfile에 작성한다.</p>
  </li>
  <li>
    <p>가상 환경, Log, Build Cache처럼 Image에 넣지 않을 대상을 <code class="language-plaintext highlighter-rouge">.dockerignore</code>에 등록한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker build</code>로 Image를 생성한다.</p>
  </li>
  <li>
    <p>Image로 Container를 실행하고 Host Port를 연결한다.</p>
  </li>
  <li>
    <p>Browser나 <code class="language-plaintext highlighter-rouge">curl</code>로 Application의 응답을 확인한다.</p>
  </li>
</ol>

<p>각 애플리케이션은 의존성을 표현하고 실행하는 방식에서 차이가 난다.</p>

<table>
  <thead>
    <tr>
      <th>애플리케이션</th>
      <th>Base Image</th>
      <th>의존성 또는 Build 입력</th>
      <th>Image의 실행 대상</th>
      <th>Container 실행 명령</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Django</td>
      <td><code class="language-plaintext highlighter-rouge">python</code></td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td>Python Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">manage.py runserver</code></td>
    </tr>
    <tr>
      <td>FastAPI</td>
      <td><code class="language-plaintext highlighter-rouge">python</code></td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td>Python Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">uvicorn</code></td>
    </tr>
    <tr>
      <td>Spring Boot</td>
      <td><code class="language-plaintext highlighter-rouge">eclipse-temurin:21-jdk</code></td>
      <td>Gradle로 만든 JAR</td>
      <td>Build된 JAR</td>
      <td><code class="language-plaintext highlighter-rouge">java -jar</code></td>
    </tr>
    <tr>
      <td>Node.js</td>
      <td><code class="language-plaintext highlighter-rouge">node:20-slim</code></td>
      <td><code class="language-plaintext highlighter-rouge">package.json</code></td>
      <td>JavaScript Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">node app.js</code></td>
    </tr>
    <tr>
      <td>Go</td>
      <td><code class="language-plaintext highlighter-rouge">golang:1.23-alpine</code></td>
      <td><code class="language-plaintext highlighter-rouge">go.mod</code>, Go Source Code</td>
      <td>Compile한 Binary</td>
      <td><code class="language-plaintext highlighter-rouge">./main</code></td>
    </tr>
    <tr>
      <td>Apache Web</td>
      <td><code class="language-plaintext highlighter-rouge">ubuntu:20.04</code></td>
      <td><code class="language-plaintext highlighter-rouge">webapp.tar.gz</code></td>
      <td>압축 해제한 Web Source</td>
      <td><code class="language-plaintext highlighter-rouge">apachectl</code></td>
    </tr>
  </tbody>
</table>

<p>Python과 Node.js 예제는 Source Code와 Runtime을 함께 Image에 넣는다. Spring Boot는 Host에서 Build한 JAR를 복사하고, Go 예제는 Image를 Build하는 과정에서 Source Code를 Compile한다.</p>

<h2 id="2--django-application-image">2 ) Django Application Image</h2>

<hr />

<p>Django Application을 먼저 Host에서 실행한 뒤 Package 의존성과 Source Code를 Image에 복사한다. Host의 Python Package와 프로젝트 Package가 섞이지 않도록 가상 환경을 사용한다.</p>

<h3 id="프로젝트-생성과-실행">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>Python Package 설치와 가상 환경 생성에 필요한 Package를 준비한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> python3-pip python3-venv
</code></pre></div>    </div>
  </li>
  <li>
    <p>작업 Directory를 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>django_workspace
<span class="nb">cd </span>django_workspace
</code></pre></div>    </div>
  </li>
  <li>
    <p>Python 가상 환경을 생성하고 활성화한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 <span class="nt">-m</span> venv ./myvenv
<span class="nb">source</span> ./myvenv/bin/activate
</code></pre></div>    </div>

    <p>가상 환경 생성 기능이 설치되어 있지 않다면 Ubuntu에서는 <code class="language-plaintext highlighter-rouge">python3-venv</code> Package가 필요할 수 있다.</p>
  </li>
  <li>
    <p>Django를 설치하고 프로젝트를 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>django
django-admin startproject dockeredjango
<span class="nb">cd </span>dockeredjango
</code></pre></div>    </div>
  </li>
  <li>
    <p>개발 Server를 실행한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python manage.py runserver
</code></pre></div>    </div>
  </li>
  <li>
    <p>다른 Terminal에서 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="의존성-file-생성">의존성 File 생성</h3>

<p>현재 가상 환경의 Package와 Version을 <code class="language-plaintext highlighter-rouge">requirements.txt</code>로 저장한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python</span>

<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["python", "manage.py", "runserver", "0.0.0.0:8000"]</span>
</code></pre></div></div>

<p>Dockerfile의 주요 동작은 다음과 같다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">requirements.txt</code>를 먼저 복사하고 Package를 설치한다.</p>
  </li>
  <li>
    <p>프로젝트 Source Code는 Package 설치 뒤에 복사한다.</p>
  </li>
  <li>
    <p>Django 개발 Server가 Container 밖의 요청을 받을 수 있도록 <code class="language-plaintext highlighter-rouge">0.0.0.0:8000</code>에서 실행한다.</p>
  </li>
</ul>

<h3 id="image-build와-container-실행">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> dockerdjango:latest <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> dockerdjango <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  dockerdjango:latest
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">manage.py runserver</code>는 개발 과정에서 사용하는 Server이다. 이 실습에서는 Container Image 제작 흐름을 확인하기 위해 사용한다.</p>

<h2 id="3--fastapi-application-image">3 ) FastAPI Application Image</h2>

<hr />

<p>FastAPI도 Python Source Code와 <code class="language-plaintext highlighter-rouge">requirements.txt</code>를 Image에 복사하지만 Application은 Uvicorn으로 실행한다.</p>

<h3 id="프로젝트-생성과-실행-1">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>작업 Directory를 만들고 가상 환경을 활성화한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>fastapi_workspace
<span class="nb">cd </span>fastapi_workspace
python3 <span class="nt">-m</span> venv ./myvenv
<span class="nb">source</span> ./myvenv/bin/activate
</code></pre></div>    </div>
  </li>
  <li>
    <p>FastAPI와 Uvicorn을 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>fastapi uvicorn
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">main.py</code>를 작성한다.</p>

    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">fastapi</span> <span class="kn">import</span> <span class="n">FastAPI</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">FastAPI</span><span class="p">()</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">read_root</span><span class="p">():</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"Hello"</span><span class="p">:</span> <span class="s">"World"</span><span class="p">}</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"/items/{item_id}"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">read_item</span><span class="p">(</span><span class="n">item_id</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">q</span><span class="p">:</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span> <span class="o">=</span> <span class="bp">None</span><span class="p">):</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"item_id"</span><span class="p">:</span> <span class="n">item_id</span><span class="p">,</span> <span class="s">"q"</span><span class="p">:</span> <span class="n">q</span><span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>개발 Server를 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn main:app <span class="nt">--reload</span>
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl <span class="s1">'http://localhost:8000/items/1?q=docker'</span>
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="의존성-file과-dockerignore">의존성 File과 <code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<p>Host의 가상 환경과 Python Cache는 Image에 복사하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myvenv/
__pycache__/
.pytest_cache/
.env
</code></pre></div></div>

<h3 id="dockerfile-1">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">requirements.txt</code>를 Source Code보다 먼저 복사하면 다음과 같이 Build Cache를 활용할 수 있다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">requirements.txt</code>가 바뀌지 않으면 <code class="language-plaintext highlighter-rouge">pip install</code> Layer를 재사용한다.</p>
  </li>
  <li>
    <p>Source Code만 수정하면 <code class="language-plaintext highlighter-rouge">COPY . .</code> 이후의 Layer만 다시 Build한다.</p>
  </li>
  <li>
    <p>시간이 오래 걸리는 Package 설치를 매번 반복하지 않는다.</p>
  </li>
</ol>

<h3 id="image-build와-container-실행-1">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> my-fastapi-app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> my-fastapi-app <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  my-fastapi-app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>Django와 FastAPI는 Python Runtime, Package 의존성과 Source Code를 Image에 포함한다.</p>
    </li>
    <li>
      <p>두 예제 모두 <code class="language-plaintext highlighter-rouge">requirements.txt</code>를 먼저 복사하여 Package 설치 Layer의 Cache를 활용한다.</p>
    </li>
    <li>
      <p>Host의 가상 환경은 Image에 복사하지 않으며 Container 안에서 필요한 Package를 다시 설치한다.</p>
    </li>
    <li>
      <p>Container 밖에서 접속하려면 Application이 <code class="language-plaintext highlighter-rouge">0.0.0.0</code>에서 요청을 받아야 한다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="4--spring-boot-application-image">4 ) Spring Boot Application Image</h2>

<hr />

<p>Python 예제는 Source Code를 Interpreter로 실행했지만 Java Application은 Gradle로 JAR를 Build한 뒤 결과물을 Image에 복사한다.</p>

<h3 id="프로젝트-준비와-build">프로젝트 준비와 Build</h3>

<ol>
  <li>
    <p>Git을 설치하고 실습 Repository를 가져온다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> git
git clone https://github.com/itggangpae/spring_docker.git
<span class="nb">cd </span>spring_docker
</code></pre></div>    </div>
  </li>
  <li>
    <p>Linux에서 Gradle Wrapper를 실행할 수 있도록 권한을 부여한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x ./gradlew
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하여 동작을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew bootRun
</code></pre></div>    </div>
  </li>
  <li>
    <p>실행을 중지한 뒤 배포할 JAR를 Build한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew clean build
<span class="nb">ls</span> ./build/libs
</code></pre></div>    </div>
  </li>
</ol>

<p>Build 결과는 <code class="language-plaintext highlighter-rouge">./build/libs</code>에 생성된다.</p>

<h3 id="dockerfile-2">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:21-jdk</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> ./build/libs/*.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>이 Dockerfile은 Gradle Build를 Image 안에서 다시 수행하지 않는다. Host에서 생성한 JAR만 복사하여 <code class="language-plaintext highlighter-rouge">java -jar</code>로 실행한다.</p>

<h3 id="image-build와-container-실행-2">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> spring_docker:1.0.0 <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> spring_docker <span class="se">\</span>
  <span class="nt">-p</span> 8000:8080 <span class="se">\</span>
  spring_docker:1.0.0
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<p>강의 Dockerfile에는 Gradle 프로젝트에서 Maven Wrapper인 <code class="language-plaintext highlighter-rouge">./mvnw</code>를 실행하는 <code class="language-plaintext highlighter-rouge">CMD</code>와 JAR를 실행하는 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>가 함께 작성되어 있었다. 이 실습은 Host에서 Gradle Build를 마친 JAR를 실행하는 방식이므로 Maven 명령을 제거하고 JAR 실행 명령만 남겼다.</p>

<h2 id="5--nodejs-application-image">5 ) Node.js Application Image</h2>

<hr />

<p>Node.js Application은 Python Application과 마찬가지로 의존성 File과 Source Code를 Image에 복사한 뒤 Runtime으로 실행한다.</p>

<h3 id="프로젝트-생성과-실행-2">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>Node.js와 npm을 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> nodejs npm
</code></pre></div>    </div>
  </li>
  <li>
    <p>작업 Directory를 Node.js 프로젝트로 초기화하고 Package를 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>node_workspace
<span class="nb">cd </span>node_workspace
npm init
npm <span class="nb">install </span>express nodemon
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">package.json</code>의 <code class="language-plaintext highlighter-rouge">scripts</code>에 실행 명령을 작성한다.</p>

    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"scripts"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"start"</span><span class="p">:</span><span class="w"> </span><span class="s2">"node app.js"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">app.js</code>를 작성한다.</p>

    <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">express</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">express</span><span class="dl">'</span><span class="p">);</span>

<span class="kd">const</span> <span class="nx">app</span> <span class="o">=</span> <span class="nx">express</span><span class="p">();</span>
<span class="nx">app</span><span class="p">.</span><span class="kd">set</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">,</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">PORT</span> <span class="o">||</span> <span class="mi">3000</span><span class="p">);</span>

<span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">/</span><span class="dl">'</span><span class="p">,</span> <span class="p">(</span><span class="nx">req</span><span class="p">,</span> <span class="nx">res</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">res</span><span class="p">.</span><span class="nx">send</span><span class="p">(</span><span class="dl">'</span><span class="s1">Hello Express</span><span class="dl">'</span><span class="p">);</span>
<span class="p">});</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">listen</span><span class="p">(</span><span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">),</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">)}</span><span class="s2">번에서 대기 중`</span><span class="p">);</span>
<span class="p">});</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node app.js
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:3000
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="dockerignore"><code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<p>Host에서 설치한 <code class="language-plaintext highlighter-rouge">node_modules</code>는 Image에 복사하지 않는다. Container의 Platform에 맞는 Package는 Image Build 과정에서 다시 설치한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node_modules/
npm-debug.log
.git/
.env
</code></pre></div></div>

<h3 id="dockerfile-3">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> node:20-slim</span>

<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 3000</span>

<span class="k">CMD</span><span class="s"> ["node", "app.js"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">package.json</code>과 Package Lock File을 먼저 복사하면 Source Code만 변경되었을 때 <code class="language-plaintext highlighter-rouge">npm install</code> Layer를 재사용할 수 있다.</p>

<h3 id="image-build와-container-실행-3">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> node_app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> nodeapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:3000 <span class="se">\</span>
  node_app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="6--go-application-image">6 ) Go Application Image</h2>

<hr />

<p>Go Application은 Compile한 실행 File을 Container의 주 Process로 실행한다. 이 실습에서는 Go Base Image 안에서 의존성을 내려받고 Source Code를 Build한다.</p>

<h3 id="go-설치">Go 설치</h3>

<p>강의에서는 Go 1.22.0 Archive를 내려받아 <code class="language-plaintext highlighter-rouge">/usr/local</code>에 설치했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-OL</span> https://golang.org/dl/go1.22.0.linux-amd64.tar.gz
<span class="nb">sudo tar</span> <span class="nt">-C</span> /usr/local <span class="nt">-xzf</span> go1.22.0.linux-amd64.tar.gz
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">~/.profile</code>에 Go 실행 경로를 추가하고 현재 Shell에 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s1">'export PATH=$PATH:/usr/local/go/bin'</span> <span class="o">&gt;&gt;</span> ~/.profile
<span class="nb">echo</span> <span class="s1">'export PATH=$PATH:$(go env GOPATH)/bin'</span> <span class="o">&gt;&gt;</span> ~/.profile
<span class="nb">source</span> ~/.profile
</code></pre></div></div>

<h3 id="프로젝트-생성과-실행-3">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>빈 Directory에서 Go Module을 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>go_workspace
<span class="nb">cd </span>go_workspace
go mod init my-go-app
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">main.go</code>를 작성한다.</p>

    <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="n">main</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="s">"encoding/json"</span>
    <span class="s">"fmt"</span>
    <span class="s">"net/http"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">http</span><span class="o">.</span><span class="n">HandleFunc</span><span class="p">(</span><span class="s">"/"</span><span class="p">,</span> <span class="k">func</span><span class="p">(</span><span class="n">w</span> <span class="n">http</span><span class="o">.</span><span class="n">ResponseWriter</span><span class="p">,</span> <span class="n">r</span> <span class="o">*</span><span class="n">http</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">w</span><span class="o">.</span><span class="n">Header</span><span class="p">()</span><span class="o">.</span><span class="n">Set</span><span class="p">(</span><span class="s">"Content-Type"</span><span class="p">,</span> <span class="s">"application/json"</span><span class="p">)</span>
        <span class="n">json</span><span class="o">.</span><span class="n">NewEncoder</span><span class="p">(</span><span class="n">w</span><span class="p">)</span><span class="o">.</span><span class="n">Encode</span><span class="p">(</span><span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="kt">string</span><span class="p">{</span>
            <span class="s">"message"</span><span class="o">:</span> <span class="s">"Hello From Go"</span><span class="p">,</span>
        <span class="p">})</span>
    <span class="p">})</span>

    <span class="n">http</span><span class="o">.</span><span class="n">HandleFunc</span><span class="p">(</span><span class="s">"/health"</span><span class="p">,</span> <span class="k">func</span><span class="p">(</span><span class="n">w</span> <span class="n">http</span><span class="o">.</span><span class="n">ResponseWriter</span><span class="p">,</span> <span class="n">r</span> <span class="o">*</span><span class="n">http</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">fmt</span><span class="o">.</span><span class="n">Fprint</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="s">"OK"</span><span class="p">)</span>
    <span class="p">})</span>

    <span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"서버가 8000번 Port에서 시작되어 대기 중이다."</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">http</span><span class="o">.</span><span class="n">ListenAndServe</span><span class="p">(</span><span class="s">":8000"</span><span class="p">,</span> <span class="no">nil</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go run main.go
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl http://localhost:8000/health
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="dockerfile-4">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> golang:1.23-alpine</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">RUN </span>go mod download
<span class="k">RUN </span>go build <span class="nt">-o</span> main .

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["./main"]</span>
</code></pre></div></div>

<p>Build 단계에서는 다음 작업을 수행한다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">go mod download</code>로 <code class="language-plaintext highlighter-rouge">go.mod</code>에 정의된 의존성을 내려받는다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">go build -o main .</code>으로 Source Code를 <code class="language-plaintext highlighter-rouge">main</code> 실행 File로 Compile한다.</p>
  </li>
  <li>
    <p>Container가 시작되면 Compile된 <code class="language-plaintext highlighter-rouge">main</code>을 실행한다.</p>
  </li>
</ul>

<h3 id="image-build와-container-실행-4">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> goapp <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> goapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  goapp
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl http://localhost:8000/health
</code></pre></div></div>

<p>이 예제는 Build 도구와 실행 File을 하나의 Image에 함께 두는 단일 Stage Build이다.</p>

<h2 id="7--압축된-web-source로-apache-image-생성">7 ) 압축된 Web Source로 Apache Image 생성</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">ADD</code>는 Local Tar Archive를 Image에 추가할 때 압축을 자동으로 해제할 수 있다. 이 기능을 이용해 <code class="language-plaintext highlighter-rouge">webapp.tar.gz</code>의 Web Source를 Apache Document Root에 배치한다.</p>

<h3 id="source-code-준비">Source Code 준비</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/brayanlee/webapp.git
<span class="nb">cd </span>webapp
<span class="nb">ls</span>
</code></pre></div></div>

<p>Build Context에 <code class="language-plaintext highlighter-rouge">webapp.tar.gz</code>가 있는지 확인한다.</p>

<h3 id="dockerfile-5">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> apache2 <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="k">ADD</span><span class="s"> webapp.tar.gz /var/www/html/</span>

<span class="k">WORKDIR</span><span class="s"> /var/www/html</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["/usr/sbin/apachectl", "-D", "FOREGROUND"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ADD webapp.tar.gz /var/www/html/</code>은 Archive의 내용을 해당 Directory에 자동으로 압축 해제한다. 자동 압축 해제가 필요하지 않은 일반 File과 Directory는 동작이 더 명확한 <code class="language-plaintext highlighter-rouge">COPY</code>를 사용한다.</p>

<h3 id="image-build와-container-실행-5">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> webapp <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> webapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  webapp
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="8--애플리케이션별-build-방식-비교">8 ) 애플리케이션별 Build 방식 비교</h2>

<hr />

<p>애플리케이션 Image를 구성할 때는 다음 세 가지를 구분해야 한다.</p>

<ol>
  <li>
    <p>의존성을 어떤 File로 재현할 것인지 결정한다.</p>
  </li>
  <li>
    <p>Source Code와 Build 산출물 중 무엇을 Image에 복사할지 결정한다.</p>
  </li>
  <li>
    <p>Container가 시작될 때 실행할 하나의 주 Process를 지정한다.</p>
  </li>
</ol>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Django·FastAPI</th>
      <th>Node.js</th>
      <th>Spring Boot</th>
      <th>Go</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>의존성 정의</td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td><code class="language-plaintext highlighter-rouge">package.json</code></td>
      <td>Gradle 설정</td>
      <td><code class="language-plaintext highlighter-rouge">go.mod</code></td>
    </tr>
    <tr>
      <td>Build 시 Package 처리</td>
      <td><code class="language-plaintext highlighter-rouge">pip install</code></td>
      <td><code class="language-plaintext highlighter-rouge">npm install</code></td>
      <td>Host에서 Gradle Build</td>
      <td><code class="language-plaintext highlighter-rouge">go mod download</code></td>
    </tr>
    <tr>
      <td>Image에 포함하는 주요 대상</td>
      <td>Python Source Code</td>
      <td>JavaScript Source Code</td>
      <td>JAR</td>
      <td>Source Code와 실행 Binary</td>
    </tr>
    <tr>
      <td>실행 방식</td>
      <td>Python Runtime</td>
      <td>Node.js Runtime</td>
      <td>JVM에서 JAR 실행</td>
      <td>Native Binary 실행</td>
    </tr>
    <tr>
      <td>Cache 활용 지점</td>
      <td>의존성 File 우선 복사</td>
      <td>Package File 우선 복사</td>
      <td>JAR 변경 시 복사</td>
      <td>의존성 다운로드와 Compile</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>애플리케이션을 먼저 Host에서 실행하여 Source Code 자체의 동작을 확인한 뒤 Image를 Build한다.</p>
    </li>
    <li>
      <p>의존성 File을 Source Code보다 먼저 복사하면 Package 설치 Layer의 Build Cache를 활용할 수 있다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">.dockerignore</code>로 Host의 가상 환경, Package Directory, Cache와 환경 변수 File을 제외한다.</p>
    </li>
    <li>
      <p>Python과 Node.js는 Source Code를 Runtime으로 실행하고, Spring Boot와 Go는 Build 또는 Compile 결과를 실행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">CMD</code>와 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>에는 Container가 시작될 때 실행할 주 Process를 지정한다.</p>
    </li>
    <li>
      <p>Local Tar Archive의 자동 압축 해제가 필요한 경우 <code class="language-plaintext highlighter-rouge">ADD</code>를 사용하고 일반 복사에는 <code class="language-plaintext highlighter-rouge">COPY</code>를 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Django, FastAPI, Spring Boot, Node.js, Go 애플리케이션을 Docker 이미지로 빌드하고 실행하는 방법]]></summary></entry></feed>