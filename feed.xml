<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://kkr010128.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://kkr010128.github.io/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-08-30T12:30:21+09:00</updated><id>https://kkr010128.github.io/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">Kubernetes Job과 CronJob</title><link href="https://kkr010128.github.io/cloud-native-30-kubernetes-job-cronjob/" rel="alternate" type="text/html" title="Kubernetes Job과 CronJob" /><published>2026-08-28T00:00:00+09:00</published><updated>2026-08-28T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-30-kubernetes-job-cronjob</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-30-kubernetes-job-cronjob/"><![CDATA[<p>Deployment와 ReplicaSet은 Application이 계속 실행되는 상태를 유지하지만 Job은 정해진 작업이 성공적으로 끝나는 상태를 목표로 한다. CronJob은 Schedule에 따라 Job을 반복 생성하므로 완료 조건과 재시도 정책을 먼저 이해해야 한다.</p>

<h2 id="1--run-to-completion-workload">1 ) Run-to-Completion Workload</h2>

<hr />

<blockquote>
  <p><strong>Run-to-Completion</strong></p>

  <p>계속 실행되는 Service가 아니라 정해진 처리를 수행하고 정상 종료하는 것을 목표로 하는 Workload이다.</p>
</blockquote>

<p>Job은 Container를 이용하여 한 번 또는 지정한 횟수만큼 작업을 완료한다. 여러 Pod를 병렬로 실행할 수도 있으며 성공한 Pod 수가 원하는 완료 횟수에 도달하도록 관리한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>ReplicaSet·Deployment</th>
      <th>Job</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목표</td>
      <td>지정한 수의 Pod가 계속 실행</td>
      <td>지정한 작업이 성공적으로 완료</td>
    </tr>
    <tr>
      <td>정상 종료된 Pod</td>
      <td>부족한 Replica로 보고 다시 생성</td>
      <td>성공 횟수로 계산</td>
    </tr>
    <tr>
      <td>대표 사례</td>
      <td>Web Server, API Server</td>
      <td>Batch, Migration, Report 생성</td>
    </tr>
    <tr>
      <td>재시도 기준</td>
      <td>실행 중인 Pod 수</td>
      <td>작업 성공·실패 상태와 <code class="language-plaintext highlighter-rouge">backoffLimit</code></td>
    </tr>
  </tbody>
</table>

<p>ReplicaSet은 정상 종료 횟수를 작업 완료로 계산하지 않으므로 Batch 처리에는 Job을 사용한다.</p>

<h2 id="2--job의-control-plane과-worker-동작">2 ) Job의 Control Plane과 Worker 동작</h2>

<hr />

<p>Job Manifest를 적용하면 다음 순서로 처리된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client의 kubectl apply
                │
                ▼
            API Server
                │ Job의 원하는 상태 저장
                ▼
          Job Controller
                │ 필요한 Pod 생성
                ▼
             Scheduler
                │ Worker 선택
                ▼
Worker의 kubelet ──▶ Container Runtime ──▶ Command 실행
                │
                └── 성공·실패 상태를 API Server에 보고
</code></pre></div></div>

<p>Job Controller는 성공한 Pod 수와 실패 상태를 확인한다. 작업이 실패하면 <code class="language-plaintext highlighter-rouge">restartPolicy</code>와 <code class="language-plaintext highlighter-rouge">backoffLimit</code>에 따라 같은 Pod의 Container를 재시작하거나 새로운 Pod를 생성한다.</p>

<h2 id="3--job-생성과-완료-확인">3 ) Job 생성과 완료 확인</h2>

<hr />

<p>다음 예제는 시작 메시지를 출력하고 10초 동안 대기한 뒤 완료 메시지를 출력한다. 내용을 <code class="language-plaintext highlighter-rouge">sample-job.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Job</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello-job</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
          <span class="na">command</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">sh</span>
            <span class="pi">-</span> <span class="s">-c</span>
            <span class="pi">-</span> <span class="pi">|</span>
              <span class="s">echo "Job 시작"</span>
              <span class="s">sleep 10</span>
              <span class="s">echo "Job 완료"</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
  <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">4</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">apiVersion: batch/v1</code></td>
      <td>Job이 속한 Batch API Version</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.template</code></td>
      <td>작업을 실행할 Pod Template</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">restartPolicy</code></td>
      <td>실패한 Container 또는 Pod를 다시 실행하는 방식</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">backoffLimit</code></td>
      <td>Job 실패를 판단하기 전 허용할 재시도 한도</td>
    </tr>
  </tbody>
</table>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Job을 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-job.yaml
</code></pre></div></div>

<p>Job과 Job이 만든 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get <span class="nb">jobs
</span>kubectl get pods <span class="nt">-l</span> job-name<span class="o">=</span>hello-job
</code></pre></div></div>

<p>작업이 완료될 때까지 기다린 뒤 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span><span class="nb">complete</span> <span class="se">\</span>
  job/hello-job <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>120s

kubectl logs job/hello-job
</code></pre></div></div>

<p>정상적으로 완료되면 Job의 <code class="language-plaintext highlighter-rouge">COMPLETIONS</code>가 <code class="language-plaintext highlighter-rouge">1/1</code>이 되고 Pod의 <code class="language-plaintext highlighter-rouge">STATUS</code>는 <code class="language-plaintext highlighter-rouge">Completed</code>로 표시된다. 완료된 Pod는 실행 중이지 않지만 Log와 종료 상태 확인을 위해 남아 있을 수 있다.</p>

<h2 id="4--restartpolicy와-실패-재시도">4 ) restartPolicy와 실패 재시도</h2>

<hr />

<p>Job의 <code class="language-plaintext highlighter-rouge">spec.template.spec.restartPolicy</code>에는 <code class="language-plaintext highlighter-rouge">Never</code> 또는 <code class="language-plaintext highlighter-rouge">OnFailure</code>만 지정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>값</th>
      <th>실패 시 동작</th>
      <th>확인 특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Never</code></td>
      <td>실패한 Pod를 다시 사용하지 않고 Job Controller가 새 Pod를 생성할 수 있음</td>
      <td>실패한 Pod가 여러 개 남을 수 있음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OnFailure</code></td>
      <td>같은 Pod 안에서 실패한 Container를 다시 시작</td>
      <td>Pod의 <code class="language-plaintext highlighter-rouge">RESTARTS</code> 증가</td>
    </tr>
  </tbody>
</table>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-error-job.yaml</code>로 저장한다. 존재하지 않는 경로를 <code class="language-plaintext highlighter-rouge">ls</code>로 조회하므로 Container가 실패한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Job</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">error-job</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">error-job</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
          <span class="na">command</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">ls</span>
            <span class="pi">-</span> <span class="s">unvalid path</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
  <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">4</span>
</code></pre></div></div>

<p>Job을 적용하고 실패 상태와 재시도 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-error-job.yaml
kubectl get job error-job
kubectl get pods <span class="nt">-l</span> job-name<span class="o">=</span>error-job
kubectl describe job error-job
</code></pre></div></div>

<p>실패한 Pod의 이름을 확인하여 Log를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs &lt;ERROR_JOB_POD_NAME&gt;
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">backoffLimit</code>는 단순히 Container를 정확히 해당 횟수만큼 실행한다는 뜻이 아니라 Job Controller가 실패를 허용하고 재시도할 한도를 설정한다. 재시도 사이에는 점차 증가하는 대기 시간이 적용될 수 있다.</p>

<h2 id="5--completions와-parallelism">5 ) completions와 parallelism</h2>

<hr />

<p>Job의 완료 횟수와 동시에 실행할 수 있는 Pod 수를 조정한다.</p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
      <th>기본 동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">completions</code></td>
      <td>Job이 완료되기 위해 필요한 성공 횟수</td>
      <td>일반적인 비병렬 Job에서는 1회 완료</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">parallelism</code></td>
      <td>동시에 실행할 수 있는 최대 Pod 수</td>
      <td>기본값 1</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">backoffLimit</code></td>
      <td>실패 재시도 한도</td>
      <td>명시하지 않으면 기본값 사용</td>
    </tr>
  </tbody>
</table>

<p>4번 성공해야 하며 동시에 최대 2개 Pod를 실행하는 예시는 다음과 같다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Job</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">parallel-job</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">completions</span><span class="pi">:</span> <span class="m">4</span>
  <span class="na">parallelism</span><span class="pi">:</span> <span class="m">2</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">worker</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
          <span class="na">command</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">sh</span>
            <span class="pi">-</span> <span class="s">-c</span>
            <span class="pi">-</span> <span class="pi">|</span>
              <span class="s">echo "$(hostname) 작업 시작"</span>
              <span class="s">sleep 10</span>
              <span class="s">echo "$(hostname) 작업 완료"</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
  <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">4</span>
</code></pre></div></div>

<p>내용을 <code class="language-plaintext highlighter-rouge">sample-parallel-job.yaml</code>로 저장한 뒤 적용하고 Pod 수를 관찰한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-parallel-job.yaml
kubectl get pods <span class="nt">-l</span> job-name<span class="o">=</span>parallel-job <span class="nt">--watch</span>
</code></pre></div></div>

<p>한 번만 수행할 작업에서는 <code class="language-plaintext highlighter-rouge">completions: 1</code>, <code class="language-plaintext highlighter-rouge">parallelism: 1</code>과 낮은 <code class="language-plaintext highlighter-rouge">backoffLimit</code>를 명시할 수 있다. 여러 작업을 병렬 처리할 때는 Worker Resource와 외부 System의 처리 용량을 고려하여 <code class="language-plaintext highlighter-rouge">parallelism</code>을 조정한다.</p>

<p>한 번 실패하면 재시도하지 않아야 하는 Task는 <code class="language-plaintext highlighter-rouge">backoffLimit: 0</code>으로 설정할 수 있다. 반대로 일시적인 실패가 발생할 수 있는 병렬 작업은 허용할 재시도 횟수를 정해 두어야 한다.</p>

<p><code class="language-plaintext highlighter-rouge">parallelism: 1</code>은 하나의 Job 안에서 동시에 실행되는 Pod 수를 제한한다. 서로 다른 여러 Job의 실행 순서를 보장하지는 않는다. Job이 동일 작업을 다시 시도할 수 있으므로 외부 Data를 변경하는 작업은 중복 실행되어도 안전하도록 설계한다.</p>

<h2 id="6--완료된-job-자동-정리">6 ) 완료된 Job 자동 정리</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">spec.ttlSecondsAfterFinished</code>를 설정하면 Job이 <code class="language-plaintext highlighter-rouge">Complete</code> 또는 <code class="language-plaintext highlighter-rouge">Failed</code> 상태가 된 뒤 지정한 시간이 지나면 TTL Controller가 Job과 종속 Resource를 정리할 수 있다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-ttl-job.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Job</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-ttl-job</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">ttlSecondsAfterFinished</span><span class="pi">:</span> <span class="m">30</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">sample-job</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
          <span class="na">command</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="s">sh</span>
            <span class="pi">-</span> <span class="s">-c</span>
            <span class="pi">-</span> <span class="pi">|</span>
              <span class="s">echo "Job 시작"</span>
              <span class="s">sleep 10</span>
              <span class="s">echo "Job 완료"</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">OnFailure</span>
  <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">4</span>
</code></pre></div></div>

<p>Job을 적용하고 완료 후 삭제되는 과정을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-ttl-job.yaml
kubectl get job sample-ttl-job <span class="nt">--watch</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ttlSecondsAfterFinished: 30</code>은 Job 실행 시작 후 30초가 아니라 Job이 완료된 시점부터 30초를 계산한다.</p>

<h2 id="7--명령형-job-생성">7 ) 명령형 Job 생성</h2>

<hr />

<p>간단한 일회성 작업은 Manifest 없이 생성할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create job my-job <span class="se">\</span>
  <span class="nt">--image</span><span class="o">=</span>busybox:1.36 <span class="se">\</span>
  <span class="nt">--</span> <span class="nb">date</span>
</code></pre></div></div>

<p>Job 상태와 출력 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get job my-job
kubectl logs job/my-job
</code></pre></div></div>

<p>명령형 생성은 짧은 확인에는 편리하지만 재시도, 병렬성, TTL과 같은 설정을 반복 관리하려면 Manifest를 사용하는 편이 적합하다.</p>

<h2 id="8--cronjob">8 ) CronJob</h2>

<hr />

<blockquote>
  <p><strong>CronJob</strong></p>

  <p>Cron 형식의 Schedule에 따라 Job Object를 반복 생성하는 Workload Controller이다.</p>
</blockquote>

<p>CronJob이 Container를 직접 실행하는 것은 아니다. CronJob Controller가 Schedule에 맞춰 Job을 생성하고, Job Controller가 Pod를 만들며 Worker의 kubelet과 Container Runtime이 실제 Command를 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CronJob Controller
      │ Schedule에 따라 Job 생성
      ▼
  Job Controller
      │ Pod 생성과 완료 횟수 관리
      ▼
   Scheduler
      │ Worker 선택
      ▼
Worker의 kubelet ──▶ Container Runtime ──▶ Command 실행
</code></pre></div></div>

<h2 id="9--cronjob-생성과-schedule">9 ) CronJob 생성과 Schedule</h2>

<hr />

<p>다음 CronJob은 1분마다 현재 시간과 메시지를 출력한다. 내용을 <code class="language-plaintext highlighter-rouge">sample-cronjob.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">CronJob</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">hello</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span> <span class="s2">"</span><span class="s">*/1</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*"</span>
  <span class="na">jobTemplate</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">template</span><span class="pi">:</span>
        <span class="na">spec</span><span class="pi">:</span>
          <span class="na">containers</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello</span>
              <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
              <span class="na">imagePullPolicy</span><span class="pi">:</span> <span class="s">IfNotPresent</span>
              <span class="na">command</span><span class="pi">:</span>
                <span class="pi">-</span> <span class="s">/bin/sh</span>
                <span class="pi">-</span> <span class="s">-c</span>
                <span class="pi">-</span> <span class="s">date; echo Hello from the Kubernetes cluster</span>
          <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">OnFailure</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>위치</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.schedule</code></td>
      <td>Job을 생성할 Cron Schedule</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.jobTemplate</code></td>
      <td>Schedule마다 생성할 Job Template</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jobTemplate.spec.template</code></td>
      <td>Job이 생성할 Pod Template</td>
    </tr>
  </tbody>
</table>

<p>CronJob을 생성하고 Schedule과 최근 실행 시간을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-cronjob.yaml
kubectl get cronjobs
kubectl get <span class="nb">jobs</span> <span class="nt">--watch</span>
</code></pre></div></div>

<p>CronJob이 만든 Job과 Pod의 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get <span class="nb">jobs</span> <span class="nt">--sort-by</span><span class="o">=</span>.metadata.creationTimestamp
kubectl logs job/&lt;JOB_NAME&gt;
</code></pre></div></div>

<p>CronJob이 생성한 Job 이름은 일반적으로 <code class="language-plaintext highlighter-rouge">hello-&lt;schedule 식별값&gt;</code> 형태이므로 목록에서 최근 Job 이름을 확인한다.</p>

<h2 id="10--cronjob-일시-정지">10 ) CronJob 일시 정지</h2>

<hr />

<p>신규 Job 생성을 일시 정지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl patch cronjob hello <span class="se">\</span>
  <span class="nt">-p</span> <span class="s1">'{"spec":{"suspend":true}}'</span>
</code></pre></div></div>

<p>Suspend 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get cronjob hello
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">suspend: true</code>는 이후 Schedule에 대한 Job 생성을 멈추지만 이미 시작된 Job을 중단하지 않는다.</p>

<p>다시 Schedule 실행을 허용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl patch cronjob hello <span class="se">\</span>
  <span class="nt">-p</span> <span class="s1">'{"spec":{"suspend":false}}'</span>
</code></pre></div></div>

<h2 id="11--동시-실행-제어">11 ) 동시 실행 제어</h2>

<hr />

<p>이전 Schedule의 Job이 끝나기 전에 다음 실행 시간이 도착했을 때 <code class="language-plaintext highlighter-rouge">spec.concurrencyPolicy</code>로 동작을 결정한다.</p>

<table>
  <thead>
    <tr>
      <th>값</th>
      <th>동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Allow</code></td>
      <td>동시 실행을 허용하는 기본값</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Forbid</code></td>
      <td>이전 Job이 실행 중이면 새 실행을 건너뜀</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Replace</code></td>
      <td>이전 Job을 중단하고 새 Job으로 교체</td>
    </tr>
  </tbody>
</table>

<p>하나의 CronJob 안에서 실행이 겹치지 않게 하려면 다음 Field를 추가한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spec</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span> <span class="s2">"</span><span class="s">*/1</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*"</span>
  <span class="na">concurrencyPolicy</span><span class="pi">:</span> <span class="s">Forbid</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">concurrencyPolicy</code>는 서로 다른 CronJob 사이의 동시 실행을 제어하지 않는다.</p>

<h2 id="12--지연-실행-허용-시간">12 ) 지연 실행 허용 시간</h2>

<hr />

<p>Controller 중단이나 Scheduling 지연으로 Schedule을 놓친 경우 <code class="language-plaintext highlighter-rouge">spec.startingDeadlineSeconds</code> 이내라면 Job 생성을 허용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spec</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*"</span>
  <span class="na">startingDeadlineSeconds</span><span class="pi">:</span> <span class="m">300</span>
</code></pre></div></div>

<p>매시 정각 실행할 CronJob에서 <code class="language-plaintext highlighter-rouge">300</code>초를 지정하면 정각 Schedule을 놓친 뒤 5분 이내에는 Job을 시작할 수 있다. 허용 시간을 넘긴 실행은 놓친 Schedule로 처리한다.</p>

<h2 id="13--job-history-보관">13 ) Job History 보관</h2>

<hr />

<p>CronJob이 만든 완료·실패 Job을 몇 개까지 남길지 설정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spec</span><span class="pi">:</span>
  <span class="na">successfulJobsHistoryLimit</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">failedJobsHistoryLimit</span><span class="pi">:</span> <span class="m">1</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
      <th>기본값</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">successfulJobsHistoryLimit</code></td>
      <td>남겨 둘 성공 Job 수</td>
      <td><code class="language-plaintext highlighter-rouge">3</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">failedJobsHistoryLimit</code></td>
      <td>남겨 둘 실패 Job 수</td>
      <td><code class="language-plaintext highlighter-rouge">1</code></td>
    </tr>
  </tbody>
</table>

<p>보관된 Job과 Pod를 통해 완료 상태와 Log를 확인할 수 있다. 값을 <code class="language-plaintext highlighter-rouge">0</code>으로 설정하면 해당 상태의 완료 Job을 보관하지 않는다.</p>

<h2 id="14--실습-resource-정리">14 ) 실습 Resource 정리</h2>

<hr />

<p>Master 또는 관리 Client에서 생성한 Resource를 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-cronjob.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-parallel-job.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-error-job.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-ttl-job.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-job.yaml <span class="nt">--ignore-not-found</span>
kubectl delete job my-job <span class="nt">--ignore-not-found</span>
</code></pre></div></div>

<p>남아 있는 Job과 CronJob을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get <span class="nb">jobs</span>,cronjobs
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Job은 계속 실행되는 Pod 수가 아니라 정해진 작업의 성공적인 완료를 관리한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">restartPolicy</code>, <code class="language-plaintext highlighter-rouge">backoffLimit</code>, <code class="language-plaintext highlighter-rouge">completions</code>와 <code class="language-plaintext highlighter-rouge">parallelism</code>으로 실패 재시도와 병렬 실행을 조정한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">ttlSecondsAfterFinished</code>는 완료된 Job과 종속 Resource를 일정 시간 뒤 정리한다.</p>
    </li>
    <li>
      <p>CronJob은 Schedule에 따라 Job을 만들고 실제 Pod 실행과 완료 관리는 Job Controller가 담당한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">suspend</code>, <code class="language-plaintext highlighter-rouge">concurrencyPolicy</code>, <code class="language-plaintext highlighter-rouge">startingDeadlineSeconds</code>와 History Limit으로 예약 작업의 실행과 보관 범위를 조정한다.</p>
    </li>
    <li>
      <p>Control Plane의 Controller가 Job과 Pod의 원하는 상태를 관리하고 Worker의 kubelet과 Container Runtime이 Command를 실행한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[완료형 Workload인 Job의 재시도·병렬 실행·자동 정리와 CronJob의 Schedule·동시 실행·History 관리 정리]]></summary></entry><entry><title type="html">Kubernetes Service Discovery와 외부 노출</title><link href="https://kkr010128.github.io/cloud-native-31-kubernetes-service-network/" rel="alternate" type="text/html" title="Kubernetes Service Discovery와 외부 노출" /><published>2026-08-28T00:00:00+09:00</published><updated>2026-08-28T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-31-kubernetes-service-network</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-31-kubernetes-service-network/"><![CDATA[<p>Pod는 생성과 교체 과정에서 IP Address가 달라질 수 있으므로 Client가 개별 Pod IP를 직접 관리하면 Backend 변화에 대응하기 어렵다. Service는 Label Selector로 Pod 집합을 찾고 안정적인 IP Address와 DNS 이름을 제공하며, Service Type에 따라 Cluster 내부 또는 외부에서 접근할 수 있게 한다.</p>

<h2 id="1--kubernetes의-통신-범위">1 ) Kubernetes의 통신 범위</h2>

<hr />

<p>Kubernetes에서 Container와 Pod가 통신하는 범위는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>통신 대상</th>
      <th>주소</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>같은 Pod의 Container</td>
      <td><code class="language-plaintext highlighter-rouge">localhost:&lt;port&gt;</code></td>
      <td>Pod Network Namespace와 IP Address 공유</td>
    </tr>
    <tr>
      <td>다른 Pod</td>
      <td><code class="language-plaintext highlighter-rouge">&lt;pod-ip&gt;:&lt;port&gt;</code></td>
      <td>같은 Node와 다른 Node 모두 Pod Network를 통해 통신</td>
    </tr>
    <tr>
      <td>Service Backend</td>
      <td><code class="language-plaintext highlighter-rouge">&lt;service-name&gt;:&lt;port&gt;</code></td>
      <td>변하는 Pod 집합을 Service가 안정적인 이름으로 연결</td>
    </tr>
    <tr>
      <td>Cluster 외부 Client</td>
      <td>NodePort, LoadBalancer, Ingress·Gateway 등</td>
      <td>외부 노출 방식과 Infrastructure 필요</td>
    </tr>
  </tbody>
</table>

<p>같은 Pod의 Container는 같은 IP Address를 사용하므로 서로 다른 Port에서 Listen해야 한다. 다른 Pod와는 Cluster 전체에서 고유한 Pod IP로 통신할 수 있지만 Pod 교체 시 IP가 바뀔 수 있다.</p>

<p>Pod Network는 CNI(Container Network Interface) 구현이 구성한다. <code class="language-plaintext highlighter-rouge">kubeadm</code>으로 Cluster를 초기화한 것만으로 Pod 간 Network가 완성되는 것은 아니며 Calico, Flannel 등 Cluster에 맞는 CNI Plugin을 설치해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Worker 1                          Worker 2
Pod A: 10.244.1.10                Pod B: 10.244.2.20
        │                                 │
        └────── CNI가 구성한 Pod Network ─┘
</code></pre></div></div>

<p>CNI 구현에 따라 VXLAN 같은 Overlay, Native Routing 또는 다른 Data Plane 방식을 사용할 수 있다. 구현 방식은 달라도 NetworkPolicy 등 의도적인 제한이 없다면 Pod가 Node 경계를 넘어 통신할 수 있는 Kubernetes Network Model을 제공해야 한다.</p>

<h2 id="2--service와-외부-노출-resource">2 ) Service와 외부 노출 Resource</h2>

<hr />

<blockquote>
  <p><strong>Service</strong></p>

  <p>하나 이상의 Backend Pod에 안정적인 Network Endpoint와 Service Discovery 기능을 제공하는 Kubernetes Resource이다.</p>
</blockquote>

<p>Service와 관련 구성은 다음과 같이 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>설정</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ClusterIP</td>
      <td><code class="language-plaintext highlighter-rouge">type: ClusterIP</code></td>
      <td>Cluster 내부 Virtual IP와 Load Balancing</td>
    </tr>
    <tr>
      <td>NodePort</td>
      <td><code class="language-plaintext highlighter-rouge">type: NodePort</code></td>
      <td>각 Node의 IP와 지정 Port로 외부 노출</td>
    </tr>
    <tr>
      <td>LoadBalancer</td>
      <td><code class="language-plaintext highlighter-rouge">type: LoadBalancer</code></td>
      <td>외부 Load Balancer 구현과 연동</td>
    </tr>
    <tr>
      <td>ExternalName</td>
      <td><code class="language-plaintext highlighter-rouge">type: ExternalName</code></td>
      <td>Service DNS를 외부 DNS 이름의 CNAME으로 연결</td>
    </tr>
    <tr>
      <td>Headless</td>
      <td><code class="language-plaintext highlighter-rouge">clusterIP: None</code></td>
      <td>Virtual IP 없이 Backend별 DNS Record 제공</td>
    </tr>
    <tr>
      <td>Selector가 없는 Service</td>
      <td>Selector 생략, EndpointSlice 별도 관리</td>
      <td>Cluster 밖 Backend나 수동 Endpoint 연결</td>
    </tr>
    <tr>
      <td>External IP</td>
      <td><code class="language-plaintext highlighter-rouge">.spec.externalIPs</code></td>
      <td>외부에서 Node로 Routing되는 특정 IP를 Service에 연결</td>
    </tr>
    <tr>
      <td>Ingress</td>
      <td>별도 Resource</td>
      <td>HTTP·HTTPS의 Host와 Path 기반 Routing</td>
    </tr>
  </tbody>
</table>

<p>External IP, Headless와 Selector가 없는 Service는 별도의 <code class="language-plaintext highlighter-rouge">type</code> 값이 아니라 Service를 구성하는 방식이다. Ingress도 Service Type이 아니며 실제 Traffic 처리를 위해 Ingress Controller가 필요하다.</p>

<h2 id="3--service-backend-deployment-생성">3 ) Service Backend Deployment 생성</h2>

<hr />

<p>Service 실습에 사용할 Echo Server Deployment를 생성한다. 다음 내용을 <code class="language-plaintext highlighter-rouge">sample-service-deployment.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-service-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">amsy810/echo-nginx:v2.0</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div></div>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Deployment를 생성하고 Pod가 Ready 상태가 될 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-service-deployment.yaml
kubectl rollout status deployment/sample-service-deployment
</code></pre></div></div>

<p>Pod IP와 배치된 Worker를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="se">\</span>
  <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-service-app <span class="se">\</span>
  <span class="nt">-o</span> wide
</code></pre></div></div>

<p>Pod마다 서로 다른 IP가 할당되며 Scaling, Update와 장애 복구 과정에서 Pod가 다시 생성되면 IP도 바뀔 수 있다.</p>

<h2 id="4--clusterip-service-생성">4 ) ClusterIP Service 생성</h2>

<hr />

<blockquote>
  <p><strong>ClusterIP</strong></p>

  <p>Cluster 내부에서만 접근할 수 있는 Virtual IP를 제공하는 기본 Service Type이다.</p>
</blockquote>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-clusterip.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-clusterip</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">type: ClusterIP</code></td>
      <td>Cluster 내부 Virtual IP 사용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ports[].port</code></td>
      <td>Client가 Service에 요청할 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ports[].targetPort</code></td>
      <td>Service가 Backend Pod에 전달할 Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">selector</code></td>
      <td>Backend로 사용할 Pod Label 조건</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">containerPort</code>는 Container가 사용할 Port를 문서화하는 Field이며 실제 Process가 해당 Port에서 Listen하도록 만들지는 않는다. Service의 <code class="language-plaintext highlighter-rouge">targetPort</code>는 실제 Application이 Listen하는 Port와 일치해야 한다.</p>

<p>Service를 생성하고 할당된 ClusterIP를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-clusterip.yaml
kubectl get service sample-clusterip
kubectl describe service sample-clusterip
</code></pre></div></div>

<p>Selector와 일치하는 Backend가 EndpointSlice에 등록되었는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get endpointslices <span class="se">\</span>
  <span class="nt">-l</span> kubernetes.io/service-name<span class="o">=</span>sample-clusterip
</code></pre></div></div>

<p>Endpoint가 비어 있다면 다음 항목을 확인한다.</p>

<ul>
  <li>
    <p>Service의 <code class="language-plaintext highlighter-rouge">spec.selector</code>와 Pod Label이 일치하는가</p>
  </li>
  <li>
    <p>Pod가 Ready 상태인가</p>
  </li>
  <li>
    <p>Service의 <code class="language-plaintext highlighter-rouge">targetPort</code>와 Application의 Listen Port가 일치하는가</p>
  </li>
</ul>

<h2 id="5--service-traffic-전달-흐름">5 ) Service Traffic 전달 흐름</h2>

<hr />

<p>Service Manifest를 적용한 뒤 Control Plane과 Worker는 다음 역할을 수행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client의 kubectl apply
                │
                ▼
            API Server
                │
                ├── EndpointSlice Controller가 대상 Pod 기록
                ├── CoreDNS가 Service DNS Record 제공
                └── Service Proxy가 참조할 상태 제공
                                  │
                                  ▼
                    Worker의 kube-proxy 또는
                    CNI Service 구현이 Data Plane 구성
                                  │
                                  ▼
                         Backend Pod의 targetPort
</code></pre></div></div>

<p>Kubernetes의 기본 Service Proxy 구현은 kube-proxy이지만 CNI 구현이 Service Data Plane을 대신 구성할 수도 있다. Control Plane은 Service와 EndpointSlice 상태를 관리하고 실제 Packet 전달은 Worker의 Network Data Plane에서 처리된다.</p>

<p>Cluster 내부 요청을 보낼 Test Pod를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl run testpod <span class="se">\</span>
  <span class="nt">--image</span><span class="o">=</span>amsy810/tools:v2.0 <span class="se">\</span>
  <span class="nt">--restart</span><span class="o">=</span>Never

kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Ready <span class="se">\</span>
  pod/testpod <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>Test Pod에서 Service DNS 이름으로 여러 번 요청한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/testpod <span class="nt">--</span> <span class="se">\</span>
  curl <span class="nt">-s</span> http://sample-clusterip:8080
</code></pre></div></div>

<p>응답에 Backend Pod 정보가 포함되어 있다면 명령을 여러 번 실행하여 서로 다른 Pod가 선택되는지 확인할 수 있다. 다만 Service는 모든 HTTP 요청을 항상 정확히 같은 비율로 분배한다고 보장하지 않는다. Connection 재사용과 Service Proxy 구현에 따라 같은 Backend가 연속해서 선택될 수 있다.</p>

<h2 id="6--여러-port를-제공하는-service">6 ) 여러 Port를 제공하는 Service</h2>

<hr />

<p>하나의 Service에 여러 Port를 정의할 수 있다. 여러 Port를 사용하면 각 항목을 구분할 수 있도록 <code class="language-plaintext highlighter-rouge">name</code>을 지정한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-clusterip-multi.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-clusterip-multi</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">https-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8443</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">443</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<p>Service를 적용하고 Port 목록을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-clusterip-multi.yaml
kubectl describe service sample-clusterip-multi
</code></pre></div></div>

<p>이 예제의 Backend Image는 <code class="language-plaintext highlighter-rouge">80</code> Port에서만 Listen하므로 <code class="language-plaintext highlighter-rouge">8080</code> 요청은 처리할 수 있지만 <code class="language-plaintext highlighter-rouge">8443</code>에서 <code class="language-plaintext highlighter-rouge">443</code>으로 전달한 요청은 응답하지 않는다. Service에 Port를 정의했다는 사실만으로 Container Process가 해당 Port에서 실행되는 것은 아니다.</p>

<h2 id="7--named-port로-서로-다른-container-port-연결">7 ) Named Port로 서로 다른 Container Port 연결</h2>

<hr />

<p>Service의 <code class="language-plaintext highlighter-rouge">targetPort</code>에는 숫자 대신 Pod의 Port 이름을 사용할 수 있다. Pod마다 실제 Port 번호가 달라도 같은 이름을 부여하면 하나의 Service가 해당 이름을 기준으로 연결한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-named-port-pods.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-named-port-pod-80</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-named-port-app</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">amsy810/echo-nginx:v2.0</span>
      <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
          <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-named-port-pod-81</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-named-port-app</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">amsy810/echo-nginx:v2.0</span>
      <span class="na">env</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">NGINX_PORT</span>
          <span class="na">value</span><span class="pi">:</span> <span class="s2">"</span><span class="s">81"</span>
      <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
          <span class="na">containerPort</span><span class="pi">:</span> <span class="m">81</span>
</code></pre></div></div>

<p>두 Pod의 Port 이름은 모두 <code class="language-plaintext highlighter-rouge">http</code>이지만 실제 Port는 <code class="language-plaintext highlighter-rouge">80</code>과 <code class="language-plaintext highlighter-rouge">81</code>이다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-named-port-service.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-named-port-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="s">http</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-named-port-app</span>
</code></pre></div></div>

<p>Pod와 Service를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-named-port-pods.yaml
kubectl apply <span class="nt">-f</span> sample-named-port-service.yaml
</code></pre></div></div>

<p>Pod 상태와 EndpointSlice의 Port를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-named-port-app
kubectl get endpointslices <span class="se">\</span>
  <span class="nt">-l</span> kubernetes.io/service-name<span class="o">=</span>sample-named-port-service <span class="se">\</span>
  <span class="nt">-o</span> yaml
</code></pre></div></div>

<p>Service는 각 Pod에서 <code class="language-plaintext highlighter-rouge">http</code>라는 이름에 대응하는 실제 Port로 Traffic을 전달한다.</p>

<h2 id="8--service-discovery">8 ) Service Discovery</h2>

<hr />

<blockquote>
  <p><strong>Service Discovery</strong></p>

  <p>Client가 직접 Backend Pod 목록을 관리하지 않고 Service 이름이나 Cluster 상태를 이용해 접근 대상을 찾는 기능이다.</p>
</blockquote>

<p>Cluster 내부 Pod는 주로 다음 두 방식으로 Service를 찾는다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>환경 변수</td>
      <td>Pod 생성 시점에 존재하는 Service 정보를 kubelet이 주입</td>
    </tr>
    <tr>
      <td>DNS</td>
      <td>CoreDNS가 Service 이름을 ClusterIP 또는 Backend DNS Record로 해석</td>
    </tr>
  </tbody>
</table>

<p>Kubernetes API를 직접 사용하는 Application은 EndpointSlice를 조회할 수도 있지만 일반적인 Application은 Service DNS를 사용하는 방식이 적합하다.</p>

<h3 id="환경-변수를-이용한-discovery">환경 변수를 이용한 Discovery</h3>

<p>Service 이름의 <code class="language-plaintext highlighter-rouge">-</code>는 <code class="language-plaintext highlighter-rouge">_</code>로 바뀌고 대문자로 변환되어 환경 변수 이름에 사용된다. <code class="language-plaintext highlighter-rouge">sample-clusterip</code> Service는 다음과 같은 변수를 제공할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SAMPLE_CLUSTERIP_SERVICE_HOST=&lt;CLUSTER_IP&gt;
SAMPLE_CLUSTERIP_SERVICE_PORT=8080
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">testpod</code>가 <code class="language-plaintext highlighter-rouge">sample-clusterip</code> Service보다 나중에 만들어졌다면 환경 변수를 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/testpod <span class="nt">--</span> <span class="se">\</span>
  <span class="nb">env</span> | <span class="nb">grep </span>SAMPLE_CLUSTERIP
</code></pre></div></div>

<p>환경 변수는 Pod가 생성될 때 주입된다. Service보다 먼저 만들어진 Pod에는 나중에 생성된 Service 환경 변수가 자동으로 추가되지 않으므로 환경 변수 방식을 사용할 때는 Service를 먼저 생성해야 한다.</p>

<h3 id="dns를-이용한-discovery">DNS를 이용한 Discovery</h3>

<p>Service의 기본 FQDN은 다음 형식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;service-name&gt;.&lt;namespace&gt;.svc.cluster.local
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">default</code> Namespace의 <code class="language-plaintext highlighter-rouge">sample-clusterip</code> Service는 다음 이름으로 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/testpod <span class="nt">--</span> <span class="se">\</span>
  dig sample-clusterip.default.svc.cluster.local
</code></pre></div></div>

<p>같은 Namespace에서는 Service 이름만으로 접근할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/testpod <span class="nt">--</span> <span class="se">\</span>
  curl <span class="nt">-s</span> http://sample-clusterip:8080
</code></pre></div></div>

<p>다른 Namespace에서는 <code class="language-plaintext highlighter-rouge">&lt;service-name&gt;.&lt;namespace&gt;</code> 또는 FQDN을 사용한다. DNS는 Service가 먼저 만들어졌는지와 관계없이 현재 Service 정보를 조회할 수 있어 환경 변수 방식보다 일반적으로 사용하기 편하다.</p>

<h2 id="9--clusterip의-port와-고정-ip">9 ) ClusterIP의 Port와 고정 IP</h2>

<hr />

<p>ClusterIP Service의 Port 관계는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  │ sample-clusterip:8080
  ▼
Service port: 8080
  │
  ▼
Pod targetPort: 80
</code></pre></div></div>

<p>Service를 생성할 때 <code class="language-plaintext highlighter-rouge">.spec.clusterIP</code>를 직접 지정할 수 있다. 지정할 주소는 API Server에 설정된 Service CIDR 안에 있어야 하고 다른 Service가 사용하지 않는 주소여야 한다.</p>

<p>Master에서 Service CIDR을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo grep </span>service-cluster-ip-range <span class="se">\</span>
  /etc/kubernetes/manifests/kube-apiserver.yaml

kubectl get services <span class="nt">-A</span> <span class="nt">-o</span> wide
</code></pre></div></div>

<p>실습 Cluster의 Service CIDR과 사용 현황을 확인한 뒤 다음 내용을 <code class="language-plaintext highlighter-rouge">sample-clusterip-vip.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-clusterip-vip</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">clusterIP</span><span class="pi">:</span> <span class="s">10.101.106.108</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">10.101.106.108</code>은 예시 주소이다. 실제 Service CIDR 밖에 있거나 이미 할당된 주소이면 API Server가 생성을 거부하므로 환경에 맞는 미사용 주소로 바꾼다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-clusterip-vip.yaml
kubectl get service sample-clusterip-vip
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">.spec.clusterIP</code>는 Service 생성 후 변경할 수 없는 Field이다. 변경하려면 Service를 삭제하고 다시 생성해야 하므로 Client 설정에는 고정 IP보다 Service DNS 이름을 사용하는 편이 적합하다.</p>

<p>ClusterIP 직접 접근은 DNS 문제와 Service Data Plane을 구분하여 확인하는 진단 과정에서 사용할 수 있다. DNS 조회 비용을 피하기 위해 Application 설정에 ClusterIP를 고정하는 방식은 Pod와 Service 운영의 유연성을 떨어뜨린다.</p>

<h2 id="10--externalips-설정">10 ) externalIPs 설정</h2>

<hr />

<blockquote>
  <p><strong>현재 상태: Deprecated</strong></p>

  <p><code class="language-plaintext highlighter-rouge">.spec.externalIPs</code>는 Kubernetes v1.36부터 Deprecated 상태이다. 수업에서 진행한 방식은 학습 기록을 위해 내용을 유지하지만, 신규 구성에서는 External Load Balancer Controller나 Gateway API 를 고려해야 한다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">.spec.externalIPs</code>는 외부에서 하나 이상의 Cluster Node로 Routing되는 IP Address와 Service Port로 들어온 Traffic을 Backend에 전달한다. Kubernetes는 해당 외부 IP를 할당하거나 Network Route를 만들어 주지 않으며 Cluster 관리자가 Infrastructure를 준비해야 한다.</p>

<p>Worker 주소를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes <span class="nt">-o</span> wide
</code></pre></div></div>

<p>다음 내용은 <code class="language-plaintext highlighter-rouge">192.168.0.201</code>과 <code class="language-plaintext highlighter-rouge">192.168.0.202</code>가 실제 Worker 주소이고 외부 Client에서 해당 주소로 Routing할 수 있는 실습 환경을 가정한다. <code class="language-plaintext highlighter-rouge">sample-externalip.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-externalip</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">externalIPs</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">192.168.0.201</span>
    <span class="pi">-</span> <span class="s">192.168.0.202</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<p>환경의 실제 Routing 가능한 IP로 바꾼 뒤 Service를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-externalip.yaml
kubectl get service sample-externalip
</code></pre></div></div>

<p>Cluster 외부 Linux Client에서 접근한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> http://192.168.0.201:8080
curl <span class="nt">-s</span> http://192.168.0.202:8080
</code></pre></div></div>

<p>Node IP를 <code class="language-plaintext highlighter-rouge">externalIPs</code>에 넣는 동작은 Network 구성에 따라 달라질 수 있다. 해당 IP가 외부에서 Node까지 Routing되고 방화벽과 Service Data Plane이 Traffic을 허용해야 한다.</p>

<h2 id="11--nodeport-service">11 ) NodePort Service</h2>

<hr />

<blockquote>
  <p><strong>NodePort</strong></p>

  <p>각 Node의 접근 가능한 IP Address와 지정된 Port를 통해 Service를 외부에 노출하는 Type이다.</p>
</blockquote>

<p>NodePort Service는 ClusterIP 기능을 기반으로 하고 각 Node에 동일한 NodePort를 제공한다. 기본 할당 범위는 <code class="language-plaintext highlighter-rouge">30000</code>~<code class="language-plaintext highlighter-rouge">32767</code>이며 값을 생략하면 Control Plane이 사용 가능한 Port를 할당한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-nodeport.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-nodeport</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">NodePort</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">nodePort</span><span class="pi">:</span> <span class="m">30080</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<p>Service를 생성하고 ClusterIP와 NodePort를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-nodeport.yaml
kubectl get service sample-nodeport
</code></pre></div></div>

<p>외부 Client에서 접근 가능한 Node IP로 요청한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> http://&lt;NODE_IP&gt;:30080
</code></pre></div></div>

<p>NodePort가 모든 Network Interface에 일반 Process처럼 <code class="language-plaintext highlighter-rouge">0.0.0.0</code>으로 Listen한다고 단정할 수는 없다. kube-proxy 또는 CNI Service 구현이 Packet 처리 규칙을 구성하며 NodePort Address 설정, 방화벽과 Network Route에 따라 접근 가능한 Node IP가 달라질 수 있다.</p>

<p>Control Plane Node를 포함한 모든 Node IP로 항상 접근할 수 있는 것도 아니다. 해당 Node에서 Service Data Plane이 동작하고 방화벽과 외부 Routing이 허용되는지 확인해야 한다.</p>

<h2 id="12--loadbalancer-service">12 ) LoadBalancer Service</h2>

<hr />

<blockquote>
  <p><strong>LoadBalancer</strong></p>

  <p>Cloud Provider 또는 별도의 Load Balancer Controller와 연동하여 Cluster 외부 주소를 Service에 제공하는 Type이다.</p>
</blockquote>

<p>LoadBalancer Service는 기본적으로 ClusterIP 기능을 포함하고 구현에 따라 NodePort를 통해 Node로 Traffic을 전달할 수 있다. 환경에 따라 NodePort 할당을 사용하지 않는 LoadBalancer 구성도 가능하다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-loadbalancer.yaml</code>로 저장한다. 앞서 사용한 <code class="language-plaintext highlighter-rouge">30080</code>과 충돌하지 않도록 이 예제에서는 <code class="language-plaintext highlighter-rouge">30081</code>을 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-loadbalancer</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">LoadBalancer</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">nodePort</span><span class="pi">:</span> <span class="m">30081</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-service-app</span>
</code></pre></div></div>

<p>Service를 적용하고 외부 주소 할당 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-loadbalancer.yaml
kubectl get service sample-loadbalancer <span class="nt">--watch</span>
</code></pre></div></div>

<p>Public Cloud처럼 LoadBalancer 연동을 제공하는 환경에서는 Controller가 외부 IP Address 또는 Hostname을 할당한다. 외부 Load Balancer는 정상 Node로 Traffic을 전달하며 Node 장애를 감지하고 대상에서 제외하기까지 일시적인 지연이 발생할 수 있다.</p>

<p>외부 Load Balancer 구현이 없는 직접 구축 Cluster에서는 <code class="language-plaintext highlighter-rouge">EXTERNAL-IP</code>가 <code class="language-plaintext highlighter-rouge">&lt;pending&gt;</code> 상태로 유지될 수 있다. 이는 Service Object가 잘못되었다는 뜻이 아니라 외부 주소를 할당할 Controller가 없거나 아직 처리를 완료하지 않았다는 뜻이다.</p>

<p>Bare Metal 환경에서는 MetalLB와 같은 Load Balancer 구현을 별도로 구성할 수 있다. 이 문서에서는 외부 Package 설치나 MetalLB 배포를 진행하지 않는다.</p>

<h2 id="13--service-유형-비교">13 ) Service 유형 비교</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>구성</th>
      <th>접근 범위</th>
      <th>Infrastructure 요구 사항</th>
      <th>현재 사용 판단</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ClusterIP</td>
      <td>Cluster 내부</td>
      <td>CNI와 Service Data Plane</td>
      <td>내부 Service의 기본 선택</td>
    </tr>
    <tr>
      <td>NodePort</td>
      <td>Node IP와 Port</td>
      <td>외부 Route와 방화벽</td>
      <td>Test 또는 외부 Load Balancer의 Backend</td>
    </tr>
    <tr>
      <td>LoadBalancer</td>
      <td>외부 Load Balancer 주소</td>
      <td>Cloud 연동 또는 Load Balancer Controller</td>
      <td>외부 L4 Service 노출</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">externalIPs</code></td>
      <td>외부에서 Routing되는 지정 IP</td>
      <td>IP 할당과 Route를 관리자가 구성</td>
      <td>Deprecated, 기존 구성 이해용</td>
    </tr>
    <tr>
      <td>ExternalName</td>
      <td>Cluster DNS를 외부 FQDN에 연결</td>
      <td>CoreDNS와 외부 DNS</td>
      <td>DNS 수준 외부 Service 연결</td>
    </tr>
    <tr>
      <td>Headless</td>
      <td>개별 Backend DNS</td>
      <td>CoreDNS</td>
      <td>StatefulSet과 개별 Pod Discovery</td>
    </tr>
    <tr>
      <td>Ingress</td>
      <td>외부 HTTP·HTTPS</td>
      <td>Ingress Controller</td>
      <td>Host·Path 기반 L7 Routing</td>
    </tr>
  </tbody>
</table>

<p>실습에서는 NodePort로 간단히 외부 접근을 확인할 수 있다. 운영 환경에서는 직접 Node IP를 Client에 고정하기보다 Infrastructure에 맞는 Load Balancer, Ingress 또는 Gateway API를 이용하여 장애 처리와 주소 관리를 분리한다.</p>

<h2 id="14--실습-resource-정리">14 ) 실습 Resource 정리</h2>

<hr />

<p>Master 또는 관리 Client에서 외부 노출 Service부터 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-loadbalancer.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-nodeport.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-externalip.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-clusterip-vip.yaml <span class="nt">--ignore-not-found</span>
</code></pre></div></div>

<p>나머지 Service, Pod와 Deployment를 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-named-port-service.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-named-port-pods.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-clusterip-multi.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-clusterip.yaml <span class="nt">--ignore-not-found</span>
kubectl delete pod testpod <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-service-deployment.yaml <span class="nt">--ignore-not-found</span>
</code></pre></div></div>

<p>남아 있는 Resource를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get services,deployments,pods
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>같은 Pod의 Container는 <code class="language-plaintext highlighter-rouge">localhost</code>로 통신하고 서로 다른 Pod는 CNI가 구성한 Pod Network를 사용한다.</p>
    </li>
    <li>
      <p>Service는 변하는 Pod 집합에 안정적인 ClusterIP와 DNS 이름을 제공하고 EndpointSlice가 Backend 정보를 기록한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">port</code>는 Client가 Service에 접근할 Port이고 <code class="language-plaintext highlighter-rouge">targetPort</code>는 Backend Application이 Listen하는 Port이다.</p>
    </li>
    <li>
      <p>Service Discovery에는 환경 변수와 DNS를 사용할 수 있으며 일반적인 Application 연결에는 DNS가 적합하다.</p>
    </li>
    <li>
      <p>ClusterIP는 내부 통신, NodePort는 Node 주소를 통한 외부 접근, LoadBalancer는 외부 Load Balancer 구현과의 연동에 사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">.spec.externalIPs</code>는 Kubernetes가 IP나 Route를 제공하지 않으며 현재 Deprecated 상태이다.</p>
    </li>
    <li>
      <p>Control Plane은 Service와 EndpointSlice 상태를 관리하고 Worker의 kube-proxy 또는 CNI Service 구현이 실제 Traffic 경로를 구성한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Pod Network부터 Service·EndpointSlice·DNS 기반 Discovery와 ClusterIP·NodePort·LoadBalancer를 이용한 Traffic 전달 정리]]></summary></entry><entry><title type="html">Kubernetes Workload Resource와 Pod</title><link href="https://kkr010128.github.io/cloud-native-27-kubernetes-workload-pod/" rel="alternate" type="text/html" title="Kubernetes Workload Resource와 Pod" /><published>2026-08-27T00:00:00+09:00</published><updated>2026-08-27T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-27-kubernetes-workload-pod</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-27-kubernetes-workload-pod/"><![CDATA[<p>Kubernetes는 Container를 직접 하나씩 관리하는 대신 Pod와 여러 Workload Resource에 원하는 상태를 선언한다. Control Plane의 Controller와 Scheduler는 선언된 상태를 실제 상태와 비교하고, Worker의 kubelet과 Container Runtime은 할당된 Pod의 Container를 실행한다.</p>

<h2 id="1--workload-resource">1 ) Workload Resource</h2>

<hr />

<blockquote>
  <p><strong>Workload</strong></p>

  <p>Kubernetes에서 실행되는 Application이다. Workload Resource는 Pod를 직접 또는 Controller를 통해 생성하고 원하는 실행 상태를 유지한다.</p>
</blockquote>

<p>주요 Workload Resource와 관계는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pod ◀── ReplicationController
Pod ◀── ReplicaSet ◀── Deployment
Pod ◀── DaemonSet
Pod ◀── StatefulSet
Pod ◀── Job ◀── CronJob
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>역할</th>
      <th>대표 사용 사례</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Pod</td>
      <td>하나 이상의 Container를 함께 실행하는 기본 배포 단위</td>
      <td>단일 실습, 밀접하게 결합된 Container</td>
    </tr>
    <tr>
      <td>ReplicationController</td>
      <td>지정한 수의 Pod 유지</td>
      <td>Legacy 방식의 복제 관리</td>
    </tr>
    <tr>
      <td>ReplicaSet</td>
      <td>Label Selector와 일치하는 Pod 수 유지</td>
      <td>Deployment의 하위 Controller</td>
    </tr>
    <tr>
      <td>Deployment</td>
      <td>ReplicaSet을 이용한 배포, Update와 Rollback 관리</td>
      <td>Stateless Web Application</td>
    </tr>
    <tr>
      <td>DaemonSet</td>
      <td>조건에 맞는 각 Node에 Pod 실행</td>
      <td>Log 수집기, Node Monitoring Agent</td>
    </tr>
    <tr>
      <td>StatefulSet</td>
      <td>순서와 고유한 식별자가 필요한 Pod 관리</td>
      <td>Database, Message Broker</td>
    </tr>
    <tr>
      <td>Job</td>
      <td>완료될 때까지 Pod 실행</td>
      <td>Batch 작업</td>
    </tr>
    <tr>
      <td>CronJob</td>
      <td>Schedule에 따라 Job 생성</td>
      <td>정기 Backup과 점검 작업</td>
    </tr>
  </tbody>
</table>

<p>ReplicationController는 현재도 API에 남아 있는 Legacy Resource이다. 신규 Workload에서는 더 유연한 Label Selector를 지원하는 ReplicaSet을 사용하며, 일반적인 Stateless Application은 ReplicaSet을 직접 만들기보다 Deployment로 관리한다.</p>

<h2 id="2--pod">2 ) Pod</h2>

<hr />

<blockquote>
  <p><strong>Pod</strong></p>

  <p>Kubernetes가 생성하고 배치할 수 있는 가장 작은 실행 단위로, 같은 Network와 Storage 자원을 공유하는 하나 이상의 Container로 구성된다.</p>
</blockquote>

<p>같은 Pod의 Container는 항상 같은 Node에 함께 배치된다. Container마다 Process와 File System 영역은 구분되지만 Pod의 Network Namespace를 공유하므로 같은 IP Address를 사용하고 <code class="language-plaintext highlighter-rouge">localhost</code>로 통신할 수 있다.</p>

<p>여러 Container를 한 Pod에 배치하는 기준은 Lifecycle과 배치 관계이다. 서로 독립적으로 Scaling하거나 배포해야 하는 Application은 별도 Pod로 나누고, 항상 함께 실행되며 긴밀하게 협력하는 Container만 같은 Pod에 배치한다.</p>

<h3 id="control-plane과-worker의-동작">Control Plane과 Worker의 동작</h3>

<p>Pod Manifest를 적용하면 다음 순서로 실행된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client
  kubectl apply
       │
       ▼
Control Plane
  API Server에 Pod 저장
       │
       ▼
  Scheduler가 Worker 선택
       │
       ▼
Worker
  kubelet이 Pod Spec 확인
       │
       ▼
  Container Runtime이 Container 생성·실행
       │
       ▼
  kubelet이 상태를 API Server에 보고
</code></pre></div></div>

<p>Kubernetes는 Pod의 원하는 상태를 관리하고, 각 Worker의 kubelet은 CRI(Container Runtime Interface)를 통해 Container Runtime에 실행을 요청한다. 대표적인 Runtime은 <code class="language-plaintext highlighter-rouge">containerd</code>와 <code class="language-plaintext highlighter-rouge">CRI-O</code>이다. Docker Engine을 kubelet의 Runtime으로 직접 연결하던 내장 <code class="language-plaintext highlighter-rouge">dockershim</code>은 제거되었으며, Docker Engine을 사용하려면 별도의 CRI Adapter가 필요하다.</p>

<h3 id="stateless와-stateful-application">Stateless와 Stateful Application</h3>

<p>Stateless Application은 요청을 처리하는 Instance의 Local 상태에 사용자 Session이나 중요한 Data를 계속 보관하지 않는다. Pod가 교체되어도 다른 Replica가 같은 요청을 처리할 수 있도록 필요한 상태를 외부 Database나 Cache 등에 분리한다.</p>

<p>Stateful Application은 Instance별 Data, 순서 또는 고유한 식별자를 유지해야 한다. Database처럼 Data 자체를 관리하는 Application이 대표적이며, Kubernetes에서는 StatefulSet과 Persistent Storage를 조합할 수 있다.</p>

<p><a href="/cloud-native-29-daemonset-statefulset/">StatefulSet의 구체적인 Storage 동작</a></p>

<h2 id="3--multi-container-pod-pattern">3 ) Multi-container Pod Pattern</h2>

<hr />

<p>Pod에 Container가 여러 개 있다는 사실만으로 Sidecar Pattern이 되는 것은 아니다. 보조 Container가 Main Container의 기능을 어떤 방식으로 확장하는지에 따라 Pattern을 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>역할</th>
      <th>통신·공유 방식</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Sidecar</td>
      <td>Main Container에 보조 기능 추가</td>
      <td>공유 Volume, <code class="language-plaintext highlighter-rouge">localhost</code></td>
      <td>Log 수집, 설정 동기화</td>
    </tr>
    <tr>
      <td>Ambassador</td>
      <td>외부 System 연결을 대신 중계</td>
      <td>Main Container가 <code class="language-plaintext highlighter-rouge">localhost</code>로 접근</td>
      <td>Database Proxy, Service Proxy</td>
    </tr>
    <tr>
      <td>Adapter</td>
      <td>Main Container의 Data 형식 변환</td>
      <td>공유 Volume 또는 Local Network</td>
      <td>Metric 형식 표준화</td>
    </tr>
  </tbody>
</table>

<h3 id="sidecar-pattern">Sidecar Pattern</h3>

<p>Sidecar Container는 Main Container 옆에서 보조 기능을 제공한다. 같은 Pod의 Volume을 공유할 수 있으므로 Main Container가 기록한 Data나 설정을 읽어 처리하는 구조에 적합하다.</p>

<h3 id="ambassador-pattern">Ambassador Pattern</h3>

<p>Ambassador Container는 Main Container와 외부 System 사이의 연결을 중계한다. Main Container는 특정 Database 주소에 직접 결합하는 대신 <code class="language-plaintext highlighter-rouge">localhost</code>의 Ambassador에 접근하고, Ambassador가 실제 목적지 선택이나 연결 처리를 담당한다.</p>

<h3 id="adapter-pattern">Adapter Pattern</h3>

<p>Adapter Container는 Main Container가 생성한 Data를 외부 System이 요구하는 형식으로 변환한다. Application마다 다른 Metric 형식을 Monitoring System이 수집할 수 있는 공통 형식으로 바꾸는 경우가 대표적이다.</p>

<h2 id="4--단일-container-pod-생성">4 ) 단일 Container Pod 생성</h2>

<hr />

<p>Pod의 기본 구조는 <code class="language-plaintext highlighter-rouge">spec.containers</code>에 실행할 Container를 배열로 정의하는 형태이다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">&lt;pod-name&gt;</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">&lt;container-name&gt;</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">&lt;container-image&gt;</span>
</code></pre></div></div>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-pod.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-pod.yaml
</code></pre></div></div>

<p>Pod 상태와 배치된 Worker를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
kubectl get pod sample-pod <span class="nt">-o</span> wide
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-o wide</code>는 Pod IP와 Node 등 기본 출력보다 상세한 Column을 보여 준다.</p>

<h2 id="5--두-container를-포함한-pod">5 ) 두 Container를 포함한 Pod</h2>

<hr />

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-2pod.yaml</code>로 저장하여 Nginx와 Redis Container를 같은 Pod에 배치한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-2pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">redis-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">redis</span>
</code></pre></div></div>

<p>Manifest를 적용하고 Container별 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-2pod.yaml
kubectl get pod sample-2pod
kubectl describe pod sample-2pod
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">READY</code>가 <code class="language-plaintext highlighter-rouge">2/2</code>이면 두 Container가 모두 Ready 상태이다. 특정 Container에서 명령을 실행할 때는 <code class="language-plaintext highlighter-rouge">-c</code> Option을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-2pod <span class="se">\</span>
  <span class="nt">-c</span> nginx-container <span class="se">\</span>
  <span class="nt">--</span> /bin/ls /

kubectl <span class="nb">exec </span>pod/sample-2pod <span class="se">\</span>
  <span class="nt">-c</span> redis-container <span class="se">\</span>
  <span class="nt">--</span> /bin/ls /
</code></pre></div></div>

<h3 id="기존-pod의-container-구성-변경">기존 Pod의 Container 구성 변경</h3>

<p>다음 <code class="language-plaintext highlighter-rouge">sample-2pod-fail.yaml</code>은 <code class="language-plaintext highlighter-rouge">sample-2pod</code>와 같은 이름을 사용하지만 Container의 이름과 구성을 바꾼다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-2pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container1</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">redis-container2</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>이 Manifest 자체는 유효하므로 <code class="language-plaintext highlighter-rouge">sample-2pod</code>가 없는 상태에서는 생성된다. 그러나 앞서 만든 Pod에 적용하면 Pod의 Container 구성을 제자리에서 바꿀 수 없기 때문에 변경 불가능한 Field 오류가 발생한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-2pod-fail.yaml
</code></pre></div></div>

<p>Container 구성을 변경하려면 기존 Pod를 삭제한 뒤 다시 생성하거나, 일반적인 Application에서는 Deployment의 Pod Template을 변경하여 새 Pod로 교체한다.</p>

<h2 id="6--container-command-실행">6 ) Container Command 실행</h2>

<hr />

<p>Master 또는 관리 Client에서 <code class="language-plaintext highlighter-rouge">kubectl exec</code>를 실행하면 요청이 API Server와 대상 Worker의 kubelet을 거쳐 Container Runtime에 전달된다.</p>

<p>Nginx Container의 Shell을 연다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec</span> <span class="nt">-it</span> pod/sample-pod <span class="nt">--</span> /bin/sh
</code></pre></div></div>

<p>Container 내부에서 Network와 Process 확인 도구를 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt update
apt <span class="nt">-y</span> <span class="nb">install </span>iproute2 procps
ip a
ss <span class="nt">-napt</span> | <span class="nb">grep </span>LISTEN
</code></pre></div></div>

<p>실행 중인 Container에 Package를 설치한 변경은 Pod가 교체되면 사라진다. 이 방법은 학습과 임시 진단에만 사용하고, 반복해서 필요한 도구는 별도 Debug Image에 포함한다.</p>

<p>Shell에 접속하지 않고 외부에서 단일 Command를 실행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> /bin/ls /
kubectl <span class="nb">exec </span>pod/sample-2pod <span class="nt">-c</span> nginx-container <span class="nt">--</span> /bin/ls /
</code></pre></div></div>

<p>Pipe, Redirection과 Wildcard처럼 Shell 해석이 필요한 문자는 Container의 Shell에 문자열로 전달한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> <span class="se">\</span>
  /bin/sh <span class="nt">-c</span> <span class="s1">'ss -napt | grep LISTEN'</span>
</code></pre></div></div>

<h2 id="7--entrypoint와-cmd-변경">7 ) ENTRYPOINT와 CMD 변경</h2>

<hr />

<p>Container Image의 기본 실행 명령을 Pod Manifest에서 덮어쓸 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>Dockerfile</th>
      <th>Kubernetes Manifest</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ENTRYPOINT</code></td>
      <td><code class="language-plaintext highlighter-rouge">spec.containers[].command</code></td>
      <td>실행할 Program 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CMD</code></td>
      <td><code class="language-plaintext highlighter-rouge">spec.containers[].args</code></td>
      <td>Program에 전달할 기본 인자 지정</td>
    </tr>
  </tbody>
</table>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-entrypoint.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-entrypoint</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">/bin/sleep"</span><span class="pi">]</span>
      <span class="na">args</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">3600"</span><span class="pi">]</span>
</code></pre></div></div>

<p>Pod를 배포하고 실행 중인 Process를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-entrypoint.yaml
kubectl <span class="nb">exec</span> <span class="nt">-it</span> pod/sample-entrypoint <span class="nt">--</span> /bin/sh
</code></pre></div></div>

<p>Container Shell에서 다음 명령을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt update
apt <span class="nt">-y</span> <span class="nb">install </span>procps
ps aux
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">nginx</code> 대신 <code class="language-plaintext highlighter-rouge">/bin/sleep 3600</code>이 PID 1의 실행 Command로 사용된 것을 확인할 수 있다.</p>

<h2 id="8--pod-이름-규칙">8 ) Pod 이름 규칙</h2>

<hr />

<p>일반적인 Pod 이름은 DNS Subdomain 규칙을 따른다.</p>

<ul>
  <li>
    <p>영문 소문자와 숫자를 사용할 수 있다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">-</code>와 <code class="language-plaintext highlighter-rouge">.</code>을 구분 문자로 사용할 수 있다.</p>
  </li>
  <li>
    <p>이름의 시작과 끝은 영문 소문자 또는 숫자여야 한다.</p>
  </li>
</ul>

<p>대문자, <code class="language-plaintext highlighter-rouge">_</code> 또는 구분 문자로 시작하거나 끝나는 이름은 사용할 수 없다.</p>

<h2 id="9--host-network-사용">9 ) Host Network 사용</h2>

<hr />

<p>기본 Pod는 Worker와 분리된 Network Namespace 및 Cluster 내부 Pod IP를 사용한다. <code class="language-plaintext highlighter-rouge">spec.hostNetwork: true</code>를 설정하면 Pod가 배치된 Worker의 Network Namespace와 IP Address를 사용한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-hostnetwork.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-hostnetwork</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">hostNetwork</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">dnsPolicy</span><span class="pi">:</span> <span class="s">ClusterFirstWithHostNet</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Pod를 생성하고 Pod IP와 Worker IP를 비교한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-hostnetwork.yaml
kubectl get pod sample-hostnetwork <span class="nt">-o</span> wide
kubectl get nodes <span class="nt">-o</span> wide
</code></pre></div></div>

<p>Host Network Pod가 여는 Port는 Worker의 Port를 직접 사용하므로 같은 Worker에서 동일한 Port를 사용하는 다른 Process나 Host Network Pod와 충돌할 수 있다. 운영 환경에서는 반드시 필요한 System Workload에 한해 제한적으로 사용한다.</p>

<h2 id="10--working-directory-설정">10 ) Working Directory 설정</h2>

<hr />

<p>Dockerfile의 <code class="language-plaintext highlighter-rouge">WORKDIR</code>에 해당하는 Container 작업 Directory는 <code class="language-plaintext highlighter-rouge">spec.containers[].workingDir</code>로 덮어쓸 수 있다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-working-dir.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-working-dir</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
      <span class="na">workingDir</span><span class="pi">:</span> <span class="s">/usr/share/nginx/html</span>
</code></pre></div></div>

<p>Pod를 생성하고 현재 Directory를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-working-dir.yaml
kubectl <span class="nb">exec </span>pod/sample-working-dir <span class="nt">--</span> <span class="nb">pwd</span>
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Kubernetes Workload Resource는 Pod를 직접 또는 Controller 계층을 통해 생성하고 원하는 상태를 유지한다.</p>
    </li>
    <li>
      <p>Pod의 Container는 같은 Worker에 배치되고 Network Namespace를 공유하므로 <code class="language-plaintext highlighter-rouge">localhost</code>로 통신할 수 있다.</p>
    </li>
    <li>
      <p>여러 Container가 있다는 사실만으로 Sidecar가 되는 것은 아니며 보조 기능에 따라 Sidecar, Ambassador와 Adapter Pattern을 구분한다.</p>
    </li>
    <li>
      <p>Control Plane은 Pod의 상태와 배치를 결정하고 Worker의 kubelet과 Container Runtime이 실제 Container를 실행한다.</p>
    </li>
    <li>
      <p>Pod의 Container 구성은 제자리에서 자유롭게 변경할 수 없으므로 일반적인 Application Update에는 Deployment를 사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">command</code>, <code class="language-plaintext highlighter-rouge">args</code>, <code class="language-plaintext highlighter-rouge">hostNetwork</code>와 <code class="language-plaintext highlighter-rouge">workingDir</code>는 Image의 기본 실행 및 Network 설정을 Pod 수준에서 조정한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Kubernetes Workload Resource의 제어 관계와 Pod 구조, Multi-container Pattern, command·args 및 Network 설정 정리]]></summary></entry><entry><title type="html">Kubernetes ReplicaSet과 Deployment</title><link href="https://kkr010128.github.io/cloud-native-28-replicaset-deployment/" rel="alternate" type="text/html" title="Kubernetes ReplicaSet과 Deployment" /><published>2026-08-27T00:00:00+09:00</published><updated>2026-08-27T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-28-replicaset-deployment</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-28-replicaset-deployment/"><![CDATA[<p>ReplicaSet은 Label Selector와 일치하는 Pod를 지정한 수만큼 유지한다. Deployment는 ReplicaSet을 한 단계 위에서 관리하여 Pod 수 유지뿐 아니라 Application의 Rolling Update와 Rollback까지 수행한다.</p>

<h2 id="1--replicaset">1 ) ReplicaSet</h2>

<hr />

<blockquote>
  <p><strong>ReplicaSet</strong></p>

  <p>Label Selector와 일치하는 Pod가 <code class="language-plaintext highlighter-rouge">spec.replicas</code>에 지정한 수만큼 실행되도록 지속적으로 조정하는 Controller Resource이다.</p>
</blockquote>

<p>Pod만 직접 생성하면 Container Process가 실패했을 때 kubelet이 Container를 재시작할 수는 있지만, Pod가 삭제되거나 Node 장애로 사라진 Pod를 대신할 새 Pod를 생성하는 상위 Controller는 없다. ReplicaSet은 현재 Pod 수가 원하는 수보다 적으면 새 Pod를 만들고, 많으면 일치하는 Pod를 줄인다.</p>

<p>ReplicationController도 Pod 복제 수를 관리하지만 Legacy Resource이다. 신규 구성에서는 ReplicaSet을 사용하며, 일반적인 Application은 ReplicaSet을 직접 배포하기보다 Deployment를 통해 관리한다.</p>

<h3 id="control-plane과-worker의-조정-흐름">Control Plane과 Worker의 조정 흐름</h3>

<p>ReplicaSet의 상태 조정은 다음과 같이 이루어진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Control Plane
ReplicaSet Controller
  │ Label Selector와 일치하는 Pod 수 계산
  ├── 부족함 ──▶ Pod 생성 요청
  └── 초과함 ──▶ Pod 삭제 요청
                    │
                    ▼
               Scheduler
                    │ Worker 선택
                    ▼
Worker의 kubelet ──▶ Container Runtime ──▶ Container 실행
</code></pre></div></div>

<p>Worker나 Pod에 장애가 생기면 Control Plane은 API Server에 기록된 상태를 기준으로 부족한 Replica를 감지한다. Scheduler가 새 Pod를 실행할 Worker를 선택하고 해당 Worker의 kubelet이 Container를 실행한다.</p>

<p>ReplicaSet은 Application 자체의 Data 복제나 Session 복구를 수행하지 않는다. 여러 Pod가 대체 가능하도록 Application을 구성하고 필요한 Data는 외부 저장소에서 관리해야 한다.</p>

<h2 id="2--replicaset-생성">2 ) ReplicaSet 생성</h2>

<hr />

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-rs.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ReplicaSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-rs</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.replicas</code></td>
      <td>유지할 Pod 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.selector.matchLabels</code></td>
      <td>ReplicaSet이 관리 대상으로 계산할 Pod Label</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.template</code></td>
      <td>Pod 수가 부족할 때 새로 생성할 Pod Template</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">template.metadata.labels</code></td>
      <td>생성되는 Pod에 부여할 Label</td>
    </tr>
  </tbody>
</table>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-rs.yaml
</code></pre></div></div>

<p>ReplicaSet과 생성된 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get replicasets
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app <span class="nt">-o</span> wide
</code></pre></div></div>

<p>ReplicaSet이 생성한 Pod의 이름은 <code class="language-plaintext highlighter-rouge">sample-rs-&lt;임의 문자열&gt;</code> 형태이다. 각 Pod의 이름은 다르지만 같은 Pod Template과 Label을 사용한다.</p>

<p>상세 상태와 최근 Event를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe replicaset sample-rs
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">describe</code>는 장기간 증감 이력을 제공하는 명령이 아니다. 현재 Replica 수, Selector, Pod Template, Condition과 최근 Event를 확인하는 데 사용한다.</p>

<h2 id="3--label-selector와-pod-수-조정">3 ) Label Selector와 Pod 수 조정</h2>

<hr />

<p>ReplicaSet의 <code class="language-plaintext highlighter-rouge">selector</code>는 <code class="language-plaintext highlighter-rouge">template</code>에 지정한 Label을 포함해야 한다. 두 값이 일치하지 않으면 API Server가 Resource 생성을 거부한다.</p>

<p>다음 잘못된 Manifest를 <code class="language-plaintext highlighter-rouge">sample-rs-fail.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ReplicaSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-rs-fail</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app-fail</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>적용하면 Selector가 Pod Template Label과 일치하지 않는다는 오류가 발생한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-rs-fail.yaml
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The ReplicaSet "sample-rs-fail" is invalid:
spec.template.metadata.labels: Invalid value:
selector does not match template labels
</code></pre></div></div>

<p>오류를 해결하려면 <code class="language-plaintext highlighter-rouge">spec.selector.matchLabels</code>와 <code class="language-plaintext highlighter-rouge">spec.template.metadata.labels</code>의 <code class="language-plaintext highlighter-rouge">app</code> 값을 같게 설정한다.</p>

<h3 id="같은-label의-독립-pod-생성">같은 Label의 독립 Pod 생성</h3>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-rs-pod.yaml</code>로 저장한다. 이 Pod는 ReplicaSet과 같은 <code class="language-plaintext highlighter-rouge">app: sample-app</code> Label을 사용하지만 <code class="language-plaintext highlighter-rouge">ownerReferences</code>는 없는 독립 Pod로 요청된다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-rs-pod</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Pod를 생성하고 즉시 상태 변화를 관찰한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-rs-pod.yaml
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app <span class="nt">--watch</span>
</code></pre></div></div>

<p>API Server는 Pod 생성 요청을 정상적으로 처리한다. 그 결과 일치하는 Pod가 <code class="language-plaintext highlighter-rouge">replicas: 3</code>보다 많아지면 ReplicaSet Controller가 Selector와 일치하는 Pod 중 하나를 삭제하여 총수를 다시 3개로 맞춘다. 반드시 새로 생성한 <code class="language-plaintext highlighter-rouge">sample-rs-pod</code>가 삭제된다고 보장되지는 않는다.</p>

<p>최종 Pod 수를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<h2 id="4--replicaset-scaling">4 ) ReplicaSet Scaling</h2>

<hr />

<p>Replica 수는 Manifest의 <code class="language-plaintext highlighter-rouge">spec.replicas</code>를 수정한 뒤 다시 적용하거나 <code class="language-plaintext highlighter-rouge">kubectl scale</code>로 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale replicaset sample-rs <span class="nt">--replicas</span><span class="o">=</span>5
</code></pre></div></div>

<p>변경된 원하는 수와 현재 실행 수를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get replicaset sample-rs
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl scale</code>은 Cluster의 Live Object를 직접 변경한다. Manifest를 기준으로 지속적으로 관리한다면 File의 <code class="language-plaintext highlighter-rouge">spec.replicas</code>도 같은 값으로 수정하여 다음 <code class="language-plaintext highlighter-rouge">apply</code>에서 원래 값으로 되돌아가지 않게 한다.</p>

<h2 id="5--deployment">5 ) Deployment</h2>

<hr />

<blockquote>
  <p><strong>Deployment</strong></p>

  <p>ReplicaSet을 생성하고 전환하여 Stateless Application의 배포, Scaling, Rolling Update와 Rollback을 관리하는 Controller Resource이다.</p>
</blockquote>

<p>Deployment의 제어 관계는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Deployment Controller
        │ ReplicaSet 생성·전환
        ▼
    ReplicaSet
        │ Pod 수 유지
        ▼
       Pods
</code></pre></div></div>

<p>직접 생성한 단독 Pod는 삭제된 뒤 자동으로 대체되지 않는다. ReplicaSet은 Pod 수를 유지하지만 Pod Template 변경에 대한 Rollout과 Rollback을 관리하지 않는다. 일반적인 Stateless Application은 Deployment로 배포한다.</p>

<h2 id="6--deployment-생성">6 ) Deployment 생성</h2>

<hr />

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-deployment.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-deployment</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-deployment</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.26</span>
</code></pre></div></div>

<p>Manifest를 적용하고 Deployment가 사용 가능한 상태가 될 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-deployment.yaml
kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<p>Deployment, ReplicaSet과 Pod의 관계를 Label과 함께 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployments
kubectl get replicasets
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment <span class="nt">--show-labels</span>
</code></pre></div></div>

<p>Manifest 없이 간단한 Deployment를 생성할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create deployment sample-deployment-by-cli <span class="nt">--image</span><span class="o">=</span>nginx
</code></pre></div></div>

<p>명령형 생성은 빠른 확인에는 편리하지만 설정을 반복하고 변경 이력을 관리하려면 Manifest를 저장하여 <code class="language-plaintext highlighter-rouge">kubectl apply</code>로 관리하는 편이 적합하다.</p>

<h2 id="7--image-update와-replicaset-전환">7 ) Image Update와 ReplicaSet 전환</h2>

<hr />

<p>Deployment의 Pod Template에 있는 Image를 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image deployment/sample-deployment <span class="se">\</span>
  nginx-container<span class="o">=</span>nginx:1.27
</code></pre></div></div>

<p>Rollout 진행 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout status deployment/sample-deployment
kubectl get replicasets
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment
</code></pre></div></div>

<p>Pod Template이 변경되면 Deployment는 새 ReplicaSet을 만들거나 동일한 Template의 기존 ReplicaSet을 재사용하여 전환한다. 기본 Rolling Update에서는 새 Pod를 늘리고 이전 Pod를 줄이며, 준비 상태를 확인하면서 전환한다.</p>

<p>Rolling Update 중 유지할 Pod 수는 다음 Field로 조정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.strategy.rollingUpdate.maxSurge</code></td>
      <td>원하는 Replica 수보다 추가로 만들 수 있는 최대 Pod 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.strategy.rollingUpdate.maxUnavailable</code></td>
      <td>Update 중 사용할 수 없어도 되는 최대 Pod 수</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">spec.replicas</code>만 변경하는 Scaling은 Pod Template 변경이 아니므로 새로운 ReplicaSet을 만들지 않는다.</p>

<p>과거 <code class="language-plaintext highlighter-rouge">kubectl set image</code>의 <code class="language-plaintext highlighter-rouge">--record</code> Option은 실행 Command를 변경 원인으로 기록하는 데 사용되었지만 Deprecated되어 현재 명령에서는 사용하지 않는다. 변경 목적은 배포 절차나 Annotation 등 별도의 관리 방식으로 기록한다.</p>

<h2 id="8--rollout-history와-rollback">8 ) Rollout History와 Rollback</h2>

<hr />

<p>Deployment의 Revision 목록을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout <span class="nb">history </span>deployment/sample-deployment
</code></pre></div></div>

<p>특정 Revision의 Pod Template을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout <span class="nb">history </span>deployment/sample-deployment <span class="nt">--revision</span><span class="o">=</span>2
</code></pre></div></div>

<p>특정 Revision으로 되돌린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout undo deployment/sample-deployment <span class="nt">--to-revision</span><span class="o">=</span>2
kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<p>바로 이전 Revision으로 되돌릴 때는 Revision 번호를 생략한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout undo deployment/sample-deployment
kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<p>Rollback은 과거 Pod를 그대로 다시 실행하는 방식이 아니다. Deployment가 이전 Pod Template을 원하는 상태로 선택하고 ReplicaSet의 Replica 수를 조정하여 Pod를 다시 전환한다.</p>

<h2 id="9--rollout-pause와-resume">9 ) Rollout Pause와 Resume</h2>

<hr />

<p>여러 Pod Template 변경을 하나의 Rollout으로 묶어 적용하려면 진행을 일시 중지할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout pause deployment/sample-deployment
</code></pre></div></div>

<p>Image를 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image deployment/sample-deployment <span class="se">\</span>
  nginx-container<span class="o">=</span>nginx:1.28
</code></pre></div></div>

<p>Pause 상태에서도 Deployment의 Pod Template 변경은 저장되지만 새 ReplicaSet으로 전환하는 Rollout은 진행되지 않는다. <code class="language-plaintext highlighter-rouge">kubectl rollout status</code>는 Rollback 명령이 아니며 배포 완료 여부를 기다리는 명령이므로, Pause 상태에서는 완료를 기다리며 종료되지 않을 수 있다.</p>

<p>현재 Image와 Pause Condition을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployment sample-deployment <span class="se">\</span>
  <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.spec.template.spec.containers[0].image}{"\n"}'</span>

kubectl describe deployment sample-deployment
</code></pre></div></div>

<p>변경을 적용하려면 Rollout을 재개하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout resume deployment/sample-deployment
kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<h2 id="10--deployment-scaling">10 ) Deployment Scaling</h2>

<hr />

<p>Manifest의 <code class="language-plaintext highlighter-rouge">spec.replicas</code>를 수정해서 적용하거나 <code class="language-plaintext highlighter-rouge">kubectl scale</code>로 Replica 수를 변경할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment/sample-deployment <span class="nt">--replicas</span><span class="o">=</span>5
</code></pre></div></div>

<p>Deployment가 원하는 Replica 수와 사용 가능한 Replica 수를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployment sample-deployment
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl scale</code> 역시 Live Object를 변경하므로 Manifest를 지속적인 기준으로 사용한다면 File의 <code class="language-plaintext highlighter-rouge">replicas</code> 값도 함께 맞춘다.</p>

<h2 id="11--cordon으로-신규-pod-배치-차단">11 ) cordon으로 신규 Pod 배치 차단</h2>

<hr />

<blockquote>
  <p><strong>cordon</strong></p>

  <p>Node를 <code class="language-plaintext highlighter-rouge">SchedulingDisabled</code> 상태로 변경하여 Scheduler가 일반적인 신규 Pod를 해당 Node에 배치하지 않도록 설정하는 작업이다.</p>
</blockquote>

<p>Hardware 이상 징후를 확인했거나 점검을 준비하는 Worker에는 새로운 Pod가 배치되지 않도록 먼저 Cordon을 설정할 수 있다. 모든 명령은 Master 또는 kubeconfig가 설정된 관리 Client에서 실행한다.</p>

<p>실습 전 Worker 이름과 현재 Pod 배치를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
kubectl scale deployment/sample-deployment <span class="nt">--replicas</span><span class="o">=</span>2
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment <span class="nt">-o</span> wide
</code></pre></div></div>

<p>Scheduler는 Resource 여유, Scheduling 조건과 점수에 따라 Worker를 선택한다. Worker가 두 대라고 해서 Pod가 항상 정확히 절반씩 배치되는 것은 아니다.</p>

<p><code class="language-plaintext highlighter-rouge">worker1</code>에 Cordon을 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl cordon worker1
kubectl get nodes
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">worker1</code>의 상태에 <code class="language-plaintext highlighter-rouge">SchedulingDisabled</code>가 표시되는지 확인한다. Cordon은 이미 실행 중인 Pod를 이동하거나 종료하지 않는다.</p>

<p>Deployment를 4개로 확장하고 새 Pod의 배치 위치를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment/sample-deployment <span class="nt">--replicas</span><span class="o">=</span>4
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment <span class="nt">-o</span> wide
</code></pre></div></div>

<p>기존 <code class="language-plaintext highlighter-rouge">worker1</code> Pod는 그대로 실행되지만 새 Pod는 Scheduling 가능한 다른 Worker에 배치된다. Cluster에 사용할 수 있는 다른 Worker가 없거나 Resource가 부족하면 신규 Pod는 <code class="language-plaintext highlighter-rouge">Pending</code> 상태에 머물 수 있다.</p>

<p>Control Plane과 Worker 관점의 동작은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client의 kubectl cordon
                │
                ▼
            API Server
                │ Node를 SchedulingDisabled로 변경
                ▼
             Scheduler
                │ 신규 Pod 배치 대상에서 worker1 제외
                ▼
다른 Worker의 kubelet ──▶ Container Runtime ──▶ 신규 Container 실행
</code></pre></div></div>

<h2 id="12--drain과-uncordon">12 ) drain과 uncordon</h2>

<hr />

<blockquote>
  <p><strong>drain</strong></p>

  <p>Node 유지보수를 위해 Scheduling을 차단하고 Eviction 가능한 일반 Workload Pod를 안전하게 비우는 작업이다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">worker1</code>의 일반 Workload Pod를 다른 Worker로 이동할 수 있도록 Drain을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl drain worker1 <span class="nt">--ignore-daemonsets</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--ignore-daemonsets</code></td>
      <td>Drain이 직접 제거하지 않는 DaemonSet 관리 Pod를 무시하고 작업 계속</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--delete-emptydir-data</code></td>
      <td><code class="language-plaintext highlighter-rouge">emptyDir</code> Data가 삭제됨을 허용하며 명시적으로 사용해야 하는 Option</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--force</code></td>
      <td>Controller가 관리하지 않는 Pod가 있을 때 삭제를 허용하는 Option</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">--delete-emptydir-data</code>와 <code class="language-plaintext highlighter-rouge">--force</code>는 Data 손실이나 독립 Pod 삭제 가능성이 있으므로 오류를 확인하지 않고 바로 추가하지 않는다. PodDisruptionBudget이 허용 중단 수를 제한하면 Drain은 Application 가용성을 지키기 위해 대기하거나 실패할 수 있다.</p>

<p>Drain 요청이 처리되는 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client의 kubectl drain
                │
                ▼
          API Server의 Eviction
                │
                ▼
Deployment·ReplicaSet Controller가 부족한 Pod 감지
                │
                ▼
Scheduler가 다른 Worker 선택
                │
                ▼
다른 Worker의 kubelet이 대체 Pod 실행
</code></pre></div></div>

<p>DaemonSet Pod는 각 Node에서 수행할 역할이 있으므로 <code class="language-plaintext highlighter-rouge">--ignore-daemonsets</code>를 사용해도 Drain이 직접 삭제하지 않는다. Static Pod와 <code class="language-plaintext highlighter-rouge">nodeName</code>을 직접 지정하여 Scheduler를 우회한 Pod도 별도로 확인해야 한다.</p>

<p>Pod가 다른 Worker에서 Ready 상태인지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-deployment <span class="nt">-o</span> wide
kubectl get node worker1
</code></pre></div></div>

<p>Node 점검이 끝나면 Scheduling을 다시 허용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl uncordon worker1
kubectl get nodes
</code></pre></div></div>

<p>Uncordon은 이후 생성되는 Pod의 배치 대상으로 Node를 되돌린다. 다른 Worker로 이동한 기존 Pod를 자동으로 <code class="language-plaintext highlighter-rouge">worker1</code>에 재분배하지는 않는다.</p>

<h2 id="13--실습-resource-정리">13 ) 실습 Resource 정리</h2>

<hr />

<p>Master 또는 관리 Client에서 생성한 Resource를 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-rs-pod.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-rs.yaml <span class="nt">--ignore-not-found</span>
kubectl delete <span class="nt">-f</span> sample-deployment.yaml <span class="nt">--ignore-not-found</span>
kubectl delete deployment sample-deployment-by-cli <span class="nt">--ignore-not-found</span>
</code></pre></div></div>

<p>삭제 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get replicasets,deployments,pods
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>ReplicaSet은 Label Selector와 일치하는 Pod를 <code class="language-plaintext highlighter-rouge">spec.replicas</code> 수만큼 유지한다.</p>
    </li>
    <li>
      <p>Selector와 Pod Template Label은 일치해야 하며 같은 Label의 독립 Pod도 Replica 수 계산에 포함된다.</p>
    </li>
    <li>
      <p>Deployment는 ReplicaSet을 관리하여 Stateless Application의 Rolling Update와 Rollback을 제공한다.</p>
    </li>
    <li>
      <p>Pod Template 변경은 ReplicaSet 전환을 일으키지만 Replica 수만 변경하는 Scaling은 새 ReplicaSet을 만들지 않는다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">rollout history</code>는 Revision을 확인하고 <code class="language-plaintext highlighter-rouge">rollout undo</code>는 이전 Pod Template으로 전환하며 <code class="language-plaintext highlighter-rouge">rollout status</code>는 진행 상태를 기다린다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">cordon</code>은 신규 Scheduling을 차단하고 <code class="language-plaintext highlighter-rouge">drain</code>은 Eviction 가능한 Workload Pod를 비우며 <code class="language-plaintext highlighter-rouge">uncordon</code>은 Scheduling을 다시 허용한다.</p>
    </li>
    <li>
      <p>Scheduler는 여러 Worker에 Pod를 배치하지만 항상 정확히 같은 수로 균등 분배한다고 보장하지 않는다.</p>
    </li>
    <li>
      <p>Control Plane의 Controller가 원하는 상태를 조정하고 Worker의 kubelet과 Container Runtime이 실제 Pod를 실행한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Label Selector 기반 ReplicaSet의 Pod 수 조정, Deployment의 배포와 Scaling 및 cordon·drain을 이용한 Node 유지보수 정리]]></summary></entry><entry><title type="html">Kubernetes DaemonSet과 StatefulSet</title><link href="https://kkr010128.github.io/cloud-native-29-daemonset-statefulset/" rel="alternate" type="text/html" title="Kubernetes DaemonSet과 StatefulSet" /><published>2026-08-27T00:00:00+09:00</published><updated>2026-08-27T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-29-daemonset-statefulset</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-29-daemonset-statefulset/"><![CDATA[<p>DaemonSet과 StatefulSet은 서로 다른 배치 조건을 해결하는 Workload Controller이다. DaemonSet은 조건에 맞는 각 Node에 Pod를 배치하고, StatefulSet은 순서와 고유한 Network·Storage 식별자가 필요한 Pod 집합을 관리한다.</p>

<h2 id="1--daemonset">1 ) DaemonSet</h2>

<hr />

<blockquote>
  <p><strong>DaemonSet</strong></p>

  <p>Scheduling 조건을 만족하는 각 Node에 특정 Pod가 하나씩 실행되도록 관리하는 Workload Controller이다.</p>
</blockquote>

<p>ReplicaSet은 Cluster 전체에서 지정한 Pod 수를 유지하지만 DaemonSet은 대상 Node마다 Pod를 배치한다. Node가 추가되어 Scheduling 조건을 만족하면 DaemonSet Pod도 자동으로 생성되고, Node가 제거되면 해당 Pod도 함께 정리된다.</p>

<p>DaemonSet은 ReplicaSet의 특수한 형태가 아니라 별도의 Controller Resource이다. 하나의 DaemonSet은 대상 Node마다 Pod 하나를 만들며 <code class="language-plaintext highlighter-rouge">spec.replicas</code>를 사용하지 않는다.</p>

<p>주요 사용 사례는 다음과 같다.</p>

<ul>
  <li>
    <p>Fluentd와 같은 Node별 Log 수집 Agent</p>
  </li>
  <li>
    <p>Prometheus Node Exporter와 같은 Node Metric 수집 Agent</p>
  </li>
  <li>
    <p>Datadog Agent와 같은 Monitoring Agent</p>
  </li>
  <li>
    <p>Network 또는 Storage의 Node Agent</p>
  </li>
</ul>

<h2 id="2--daemonset의-배치-흐름">2 ) DaemonSet의 배치 흐름</h2>

<hr />

<p>DaemonSet Pod가 배치되는 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Control Plane
DaemonSet Controller
  │ 대상 Node와 기존 Pod 확인
  │ 부족한 Node의 Pod 생성
  ▼
Scheduler
  │ Pod가 실행될 Worker 결정
  ▼
Worker의 kubelet
  │ Pod Spec 확인
  ▼
Container Runtime
  │
  ▼
Node별 Agent Container 실행
</code></pre></div></div>

<p>모든 Node에 무조건 배치되는 것은 아니다. Node Label과 다음 Scheduling 조건에 따라 대상 Node를 제한할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">nodeSelector</code></td>
      <td>지정한 Label을 가진 Node만 선택</td>
    </tr>
    <tr>
      <td>Node Affinity</td>
      <td>여러 Label 조건으로 포함·제외 규칙 구성</td>
    </tr>
    <tr>
      <td>Taint와 Toleration</td>
      <td>Taint가 설정된 Node에 배치할 수 있는지 결정</td>
    </tr>
  </tbody>
</table>

<p>Control Plane Node에 일반 Workload를 막는 Taint가 있으면 해당 Taint를 허용하는 Toleration이 없는 DaemonSet Pod는 배치되지 않는다.</p>

<h2 id="3--daemonset-생성">3 ) DaemonSet 생성</h2>

<hr />

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-daemonset.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">DaemonSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">prometheus-daemonset</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">tier</span><span class="pi">:</span> <span class="s">monitoring</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">prometheus-exporter</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">tier</span><span class="pi">:</span> <span class="s">monitoring</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">prometheus-exporter</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">prometheus</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">prom/node-exporter</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">9100</span>
</code></pre></div></div>

<p>Node Exporter의 기본 Metric Port는 <code class="language-plaintext highlighter-rouge">9100</code>이다. 이 예제는 DaemonSet의 Node별 배치 확인을 위한 기본 구성이다. 운영 환경에서는 Host의 Metric을 정확히 수집하기 위한 Mount, Namespace와 보안 설정을 별도로 검토해야 한다.</p>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-daemonset.yaml
</code></pre></div></div>

<p>원하는 Pod 수와 준비된 Pod 수를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get daemonsets
kubectl get pods <span class="se">\</span>
  <span class="nt">-l</span> <span class="nv">tier</span><span class="o">=</span>monitoring,name<span class="o">=</span>prometheus-exporter <span class="se">\</span>
  <span class="nt">-o</span> wide
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">DESIRED</code>는 현재 Scheduling 조건에 맞는 Node 수이고 <code class="language-plaintext highlighter-rouge">READY</code>는 Ready 상태인 DaemonSet Pod 수이다. <code class="language-plaintext highlighter-rouge">-o wide</code> 출력의 <code class="language-plaintext highlighter-rouge">NODE</code> Column으로 Node별 배치를 확인한다.</p>

<p>상세 설정과 Event를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe daemonset prometheus-daemonset
</code></pre></div></div>

<p>실습이 끝나면 Manifest를 기준으로 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-daemonset.yaml
</code></pre></div></div>

<h2 id="4--statefulset">4 ) StatefulSet</h2>

<hr />

<blockquote>
  <p><strong>StatefulSet</strong></p>

  <p>순서가 있는 배포와 Scaling, 안정적인 Pod 식별자 및 Pod별 Storage 연결이 필요한 Stateful Workload를 관리하는 Controller Resource이다.</p>
</blockquote>

<p>StatefulSet도 ReplicaSet의 특수한 형태가 아니라 별도의 Controller Resource이다. 같은 Pod Template을 사용하더라도 각 Pod를 서로 교환 가능한 Replica로 취급하지 않고 고유한 Ordinal과 식별자를 부여한다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Deployment</th>
      <th>StatefulSet</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Pod 이름</td>
      <td>임의 Hash와 문자열 사용</td>
      <td><code class="language-plaintext highlighter-rouge">&lt;statefulset-name&gt;-0</code>부터 순서대로 부여</td>
    </tr>
    <tr>
      <td>Pod 관계</td>
      <td>서로 교체 가능한 Replica</td>
      <td>각 Pod가 고유한 Identity 보유</td>
    </tr>
    <tr>
      <td>Network Identity</td>
      <td>주로 일반 Service로 Pod 집합에 접근</td>
      <td>Headless Service로 Pod별 DNS 제공</td>
    </tr>
    <tr>
      <td>Storage</td>
      <td>Pod Template에 공통 Volume 정의 가능</td>
      <td><code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code>로 Pod별 PVC 생성 가능</td>
    </tr>
    <tr>
      <td>생성·삭제 순서</td>
      <td>기본적으로 병렬 처리</td>
      <td>기본값은 순차 처리</td>
    </tr>
  </tbody>
</table>

<p>Pod가 다른 Worker에서 다시 생성되어도 같은 Ordinal 이름과 연결된 PVC를 다시 사용할 수 있다. StatefulSet 자체가 Data를 저장하거나 복제하는 것은 아니며 실제 Data 보존 여부는 PVC가 연결된 PersistentVolume과 Storage System에 달려 있다.</p>

<h2 id="5--statefulset의-control-plane과-worker-흐름">5 ) StatefulSet의 Control Plane과 Worker 흐름</h2>

<hr />

<p>StatefulSet과 Storage가 연결되는 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Control Plane
StatefulSet Controller
  ├── Headless Service 기반 Network Identity 사용
  ├── Ordinal Pod 생성
  └── volumeClaimTemplates로 Pod별 PVC 생성
                │
                ▼
PV Provisioner 또는 관리자가 PV 제공
                │
                ▼
Scheduler가 Storage 조건을 만족하는 Worker 선택
                │
                ▼
Worker의 kubelet
  ├── Volume Mount 요청
  └── Container Runtime에 Container 실행 요청
</code></pre></div></div>

<p>Pod를 다른 Worker로 다시 Scheduling할 수 있는지는 Storage Backend, Access Mode, Zone과 Volume 연결 조건에 따라 달라진다. PVC가 존재한다는 사실만으로 모든 Worker에서 같은 Data에 접근할 수 있는 것은 아니다.</p>

<h2 id="6--statefulset과-persistent-storage-생성">6 ) StatefulSet과 Persistent Storage 생성</h2>

<hr />

<p>StatefulSet의 <code class="language-plaintext highlighter-rouge">serviceName</code>이 가리키는 Headless Service와 StatefulSet을 함께 정의한다. 다음 내용을 <code class="language-plaintext highlighter-rouge">sample-statefulset.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">clusterIP</span><span class="pi">:</span> <span class="s">None</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">StatefulSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">serviceName</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
          <span class="na">volumeMounts</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">www</span>
              <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/usr/share/nginx/html</span>
  <span class="na">volumeClaimTemplates</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">metadata</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">www</span>
      <span class="na">spec</span><span class="pi">:</span>
        <span class="na">accessModes</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">ReadWriteOnce</span>
        <span class="na">resources</span><span class="pi">:</span>
          <span class="na">requests</span><span class="pi">:</span>
            <span class="na">storage</span><span class="pi">:</span> <span class="s">1Gi</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Service의 <code class="language-plaintext highlighter-rouge">clusterIP: None</code></td>
      <td>ClusterIP 없이 Pod별 DNS Record를 제공하는 Headless Service 구성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.serviceName</code></td>
      <td>StatefulSet의 Network Identity를 관리할 Service 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec.replicas</code></td>
      <td>생성할 StatefulSet Pod 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code></td>
      <td>Ordinal Pod마다 별도의 PVC 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">accessModes: ReadWriteOnce</code></td>
      <td>Volume을 하나의 Node에서 Read·Write로 Mount하도록 요청</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">storage: 1Gi</code></td>
      <td>Pod별 PVC가 요청할 Storage 용량</td>
    </tr>
  </tbody>
</table>

<p>Master 또는 관리 Client에서 Resource를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-statefulset.yaml
</code></pre></div></div>

<p>StatefulSet, Pod와 PVC를 함께 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get statefulsets
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-statefulset <span class="nt">-o</span> wide
kubectl get persistentvolumeclaims
</code></pre></div></div>

<p>Pod는 기본적으로 <code class="language-plaintext highlighter-rouge">sample-statefulset-0</code>, <code class="language-plaintext highlighter-rouge">sample-statefulset-1</code>, <code class="language-plaintext highlighter-rouge">sample-statefulset-2</code> 순서로 생성된다. 각 Pod에는 <code class="language-plaintext highlighter-rouge">www-sample-statefulset-0</code>과 같은 별도 PVC가 연결된다.</p>

<p>Default StorageClass나 미리 준비된 PV가 없으면 PVC는 <code class="language-plaintext highlighter-rouge">Pending</code> 상태에 머물고 Storage를 Mount해야 하는 Pod도 정상 실행되지 않을 수 있다. 이 경우 PVC Event와 StorageClass를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe persistentvolumeclaim www-sample-statefulset-0
kubectl get storageclasses
</code></pre></div></div>

<p>Pod별 DNS와 Volume Mount를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-statefulset-0 <span class="nt">--</span> <span class="nb">hostname
</span>kubectl <span class="nb">exec </span>pod/sample-statefulset-0 <span class="nt">--</span> <span class="se">\</span>
  <span class="nb">df</span> <span class="nt">-h</span> /usr/share/nginx/html
</code></pre></div></div>

<p>같은 Namespace에서는 첫 번째 Pod를 <code class="language-plaintext highlighter-rouge">sample-statefulset-0.sample-statefulset</code>이라는 이름으로 식별할 수 있다. Pod IP가 바뀌어도 StatefulSet의 Ordinal과 Headless Service를 이용한 DNS Identity는 유지된다.</p>

<h2 id="7--statefulset-lifecycle과-scaling">7 ) StatefulSet Lifecycle과 Scaling</h2>

<hr />

<p>기본 <code class="language-plaintext highlighter-rouge">podManagementPolicy</code>는 <code class="language-plaintext highlighter-rouge">OrderedReady</code>이다. Pod를 생성할 때 낮은 Ordinal부터 앞선 Pod가 Ready가 된 뒤 다음 Pod를 생성하고, Scaling Down에서는 높은 Ordinal부터 제거한다.</p>

<p>Replica 수를 5개로 늘리고 순서를 관찰한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale statefulset sample-statefulset <span class="nt">--replicas</span><span class="o">=</span>5
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-statefulset <span class="nt">--watch</span>
</code></pre></div></div>

<p>Replica 수를 3개로 줄이면 <code class="language-plaintext highlighter-rouge">sample-statefulset-4</code>, <code class="language-plaintext highlighter-rouge">sample-statefulset-3</code> 순서로 Pod가 제거된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale statefulset sample-statefulset <span class="nt">--replicas</span><span class="o">=</span>3
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-statefulset
</code></pre></div></div>

<p>StatefulSet을 줄이거나 삭제해도 연결된 PVC는 Data 보호를 위해 기본적으로 자동 삭제되지 않는다.</p>

<h3 id="parallel-pod-management">Parallel Pod Management</h3>

<p>Pod 간 순서 의존성이 없다면 <code class="language-plaintext highlighter-rouge">spec.podManagementPolicy: Parallel</code>로 생성과 종료를 병렬 처리할 수 있다. 다음 내용을 <code class="language-plaintext highlighter-rouge">sample-stateful-parallel.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">clusterIP</span><span class="pi">:</span> <span class="s">None</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">StatefulSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">podManagementPolicy</span><span class="pi">:</span> <span class="s">Parallel</span>
  <span class="na">serviceName</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-statefulset-parallel</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">volumeMounts</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">www</span>
              <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/usr/share/nginx/html</span>
  <span class="na">volumeClaimTemplates</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">metadata</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">www</span>
      <span class="na">spec</span><span class="pi">:</span>
        <span class="na">accessModes</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="s">ReadWriteOnce</span>
        <span class="na">resources</span><span class="pi">:</span>
          <span class="na">requests</span><span class="pi">:</span>
            <span class="na">storage</span><span class="pi">:</span> <span class="s">1Gi</span>
</code></pre></div></div>

<p>Resource를 적용하고 Pod의 <code class="language-plaintext highlighter-rouge">AGE</code>를 비교한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-stateful-parallel.yaml
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-statefulset-parallel
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Parallel</code>은 각 Pod가 Ready가 될 때까지 순서대로 기다리지 않고 Pod를 함께 생성하므로 <code class="language-plaintext highlighter-rouge">AGE</code>가 비슷하게 나타난다. Pod 이름과 PVC Identity까지 임의로 바뀌는 설정은 아니다.</p>

<p>첫 번째 Pod의 Mount를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-statefulset-parallel-0 <span class="nt">--</span> <span class="se">\</span>
  <span class="nb">df</span> <span class="nt">-h</span> /usr/share/nginx/html
</code></pre></div></div>

<h2 id="8--질의-statefulset으로-database-운영-가능-여부에-대한-강사님-답변">8 ) 질의: StatefulSet으로 Database 운영 가능 여부에 대한 강사님 답변</h2>

<hr />

<p>StatefulSet을 이용하여 Database를 Kubernetes Cluster 안에서 실행할 수 있다. 다만 StatefulSet을 사용한다는 사실만으로 Data가 안전하게 보존되거나 Database의 고가용성이 완성되는 것은 아니다.</p>

<h3 id="network-접근">Network 접근</h3>

<p>Pod는 다시 생성될 때 IP Address가 바뀔 수 있으므로 Client가 Pod IP를 직접 저장해서 접근하면 안 된다.</p>

<ul>
  <li>
    <p>일반 Client Traffic은 Database Service의 DNS 이름으로 연결한다.</p>
  </li>
  <li>
    <p>Database Cluster의 Member를 개별적으로 식별해야 하면 Headless Service가 제공하는 Pod별 DNS 이름을 사용할 수 있다.</p>
  </li>
</ul>

<p>StatefulSet의 <code class="language-plaintext highlighter-rouge">serviceName</code>은 Pod마다 안정적인 Network Identity를 제공하지만 Database의 접속 권한, 암호화나 장애 조치를 대신 구성하지는 않는다.</p>

<h3 id="node-장애와-data-보존">Node 장애와 Data 보존</h3>

<p>Node가 고장났을 때 Data가 보존되는지는 Volume이 실제로 어디에 저장되는지에 따라 달라진다.</p>

<table>
  <thead>
    <tr>
      <th>Storage 구성</th>
      <th>Node 장애 시 고려 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Container Writable Layer</td>
      <td>Pod와 함께 Data를 잃을 수 있으므로 Database Data 저장에 부적합</td>
    </tr>
    <tr>
      <td>Node Local Volume</td>
      <td>Disk가 남아 있어도 다른 Worker에서 바로 Mount하지 못할 수 있음</td>
    </tr>
    <tr>
      <td>NFS·Network Storage</td>
      <td>다른 Worker가 같은 Storage에 접근할 수 있지만 Storage Server의 가용성과 성능을 별도로 확보해야 함</td>
    </tr>
    <tr>
      <td>분산 Storage·Cloud Volume</td>
      <td>Backend의 복제, Zone과 Attach 조건에 따라 장애 복구 범위가 달라짐</td>
    </tr>
  </tbody>
</table>

<p>NFS는 Network를 통해 File Storage를 제공하는 방식이고 RAID는 여러 Disk를 결합하여 Disk 장애 내성을 높이는 방식이다. 둘은 서로 같은 계층의 대안이 아니며 Database의 논리적 복제와 Backup을 대신하지 않는다.</p>

<h3 id="운영-방식-선택">운영 방식 선택</h3>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>장점</th>
      <th>운영 책임</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Cluster 내부 StatefulSet</td>
      <td>Application과 같은 배포 체계에서 관리 가능</td>
      <td>Storage, 복제, Backup, Upgrade, 장애 조치까지 직접 운영</td>
    </tr>
    <tr>
      <td>Cluster 외부 Database</td>
      <td>Kubernetes Node 장애와 Database를 분리 가능</td>
      <td>외부 Database의 연결, 보안과 가용성 관리</td>
    </tr>
    <tr>
      <td>Managed Database</td>
      <td>Backup과 장애 조치 기능을 Service로 제공 가능</td>
      <td>Provider 기능과 비용, Network 연결 정책 검토</td>
    </tr>
  </tbody>
</table>

<p>StatefulSet으로 Database를 운영하려면 다음 항목을 함께 설계해야 한다.</p>

<ul>
  <li>
    <p>Pod별 PVC와 실제 Storage Backend</p>
  </li>
  <li>
    <p>Database 자체 Replication과 Leader Election</p>
  </li>
  <li>
    <p>정기 Backup과 복구 검증</p>
  </li>
  <li>
    <p>Node 및 Availability Zone 장애 시 재배치 조건</p>
  </li>
  <li>
    <p>Upgrade, Scaling과 종료 순서</p>
  </li>
</ul>

<p>따라서 StatefulSet은 Stateful Application을 배치하기 위한 기반이며 Data 보호 전략 전체를 대신하는 Resource는 아니다.</p>

<h2 id="9--실습-resource-정리">9 ) 실습 Resource 정리</h2>

<hr />

<p>순서 있는 종료가 필요하면 StatefulSet을 먼저 0개로 줄인다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale statefulset sample-statefulset <span class="nt">--replicas</span><span class="o">=</span>0
kubectl scale statefulset sample-statefulset-parallel <span class="nt">--replicas</span><span class="o">=</span>0
</code></pre></div></div>

<p>Pod 종료를 확인한 뒤 StatefulSet과 Headless Service를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-statefulset.yaml
kubectl delete <span class="nt">-f</span> sample-stateful-parallel.yaml
</code></pre></div></div>

<p>StatefulSet을 삭제해도 PVC는 기본적으로 남을 수 있다. Data가 더 이상 필요하지 않은지 확인하기 전에는 PVC를 삭제하지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get persistentvolumeclaims
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>DaemonSet은 Scheduling 조건을 만족하는 각 Node에 Pod를 하나씩 배치하며 Node별 Agent에 적합하다.</p>
    </li>
    <li>
      <p>StatefulSet은 Ordinal Pod 이름, 안정적인 Network Identity, 순서와 Pod별 PVC 연결을 관리한다.</p>
    </li>
    <li>
      <p>Headless Service는 StatefulSet Pod별 DNS Identity를 제공하고 <code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code>는 Pod별 PVC를 생성한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">OrderedReady</code>는 Pod를 순차 처리하고 <code class="language-plaintext highlighter-rouge">Parallel</code>은 Pod 간 순서 의존성이 없을 때 병렬로 처리한다.</p>
    </li>
    <li>
      <p>StatefulSet은 Database 실행에 사용할 수 있지만 Data의 실제 내구성은 Storage Backend, Database 복제와 Backup 설계에 달려 있다.</p>
    </li>
    <li>
      <p>NFS와 RAID는 서로 다른 Storage 계층의 기술이며 Database Backup과 고가용성을 대신하지 않는다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Node별 DaemonSet 배치와 StatefulSet의 순서·고유 식별자·Persistent Storage 및 Database 운영 관점 정리]]></summary></entry><entry><title type="html">Kubernetes Namespace와 kubectl 기본 사용</title><link href="https://kkr010128.github.io/cloud-native-23-kubernetes-namespace-kubectl/" rel="alternate" type="text/html" title="Kubernetes Namespace와 kubectl 기본 사용" /><published>2026-08-26T00:00:00+09:00</published><updated>2026-08-26T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-23-kubernetes-namespace-kubectl</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-23-kubernetes-namespace-kubectl/"><![CDATA[<p>Kubernetes Cluster를 구축한 다음에는 <code class="language-plaintext highlighter-rouge">kubectl</code>로 API Server에 요청을 보내 Resource를 생성하고 관리한다. 여러 팀이나 Application이 하나의 Cluster를 함께 사용한다면 Namespace로 Resource의 논리적인 범위를 나누고, RBAC와 NetworkPolicy를 결합하여 접근 권한과 Network Traffic을 제어할 수 있다.</p>

<h2 id="1--namespace">1 ) Namespace</h2>

<hr />

<blockquote>
  <p><strong>Namespace</strong></p>

  <p>하나의 Kubernetes Cluster 안에서 Resource Group을 논리적으로 구분하는 범위이다. 같은 종류의 Resource 이름은 하나의 Namespace 안에서 고유해야 하지만 서로 다른 Namespace에서는 같은 이름을 사용할 수 있다.</p>
</blockquote>

<p>Namespace는 Cluster 자체를 여러 개로 나누는 기능이 아니다. 하나의 Control Plane과 Worker 집합을 공유하면서 Pod, Deployment, Service처럼 Namespace에 속하는 Resource를 구분한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes Cluster
├── Namespace: development
│   ├── Deployment: web
│   └── Service: web
│
└── Namespace: production
    ├── Deployment: web
    └── Service: web
</code></pre></div></div>

<p>두 Namespace에 같은 이름의 <code class="language-plaintext highlighter-rouge">web</code> Deployment와 Service가 존재할 수 있다. 그러나 Node, PersistentVolume과 StorageClass처럼 Cluster 전체 범위에서 관리되는 Resource에는 Namespace가 적용되지 않는다.</p>

<h3 id="기본-namespace">기본 Namespace</h3>

<p>Kubernetes Cluster에는 다음 Namespace가 기본으로 존재한다.</p>

<table>
  <thead>
    <tr>
      <th>Namespace</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">default</code></td>
      <td>별도 Namespace를 지정하지 않았을 때 사용하는 기본 범위</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kube-system</code></td>
      <td>Control Plane Component와 Cluster Add-on 등 Kubernetes System Resource 배치</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kube-public</code></td>
      <td>모든 Client가 읽을 수 있도록 공개할 Resource를 배치하는 범위</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kube-node-lease</code></td>
      <td>kubelet의 Node Heartbeat를 위한 Lease Resource 저장</td>
    </tr>
  </tbody>
</table>

<p>Master에서 현재 Namespace를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespaces
</code></pre></div></div>

<p>축약 Resource 이름도 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get ns
</code></pre></div></div>

<h3 id="namespace-범위-resource">Namespace 범위 Resource</h3>

<p>Namespace에 속하는 Resource와 Cluster 전체 범위 Resource를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Namespace에 속하는 Resource</span>
kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">true</span>

<span class="c"># Cluster 전체 범위 Resource</span>
kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>Namespace 자체는 특정 Namespace 안에 속하지 않는 Cluster 범위 Resource이다. Namespace를 삭제하면 그 안에 속한 Resource도 함께 삭제되므로 삭제 전에 대상을 확인해야 한다.</p>

<h2 id="2--namespace-rbac와-networkpolicy">2 ) Namespace, RBAC와 NetworkPolicy</h2>

<hr />

<p>Namespace만 생성했다고 해서 사용자 권한과 Pod Traffic이 자동으로 차단되는 것은 아니다. 분리 목적에 따라 RBAC, ResourceQuota와 NetworkPolicy 같은 Resource를 함께 구성해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>구성</th>
      <th>제어 대상</th>
      <th>주요 역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Namespace</td>
      <td>Kubernetes Resource의 논리적 범위</td>
      <td>이름 충돌 방지, 팀·환경·Application 단위 구분</td>
    </tr>
    <tr>
      <td>RBAC</td>
      <td>Kubernetes API 요청 권한</td>
      <td>사용자와 ServiceAccount가 조회·생성·변경할 수 있는 Resource 제한</td>
    </tr>
    <tr>
      <td>NetworkPolicy</td>
      <td>Pod의 Network Traffic</td>
      <td>허용할 Ingress·Egress Traffic 정의</td>
    </tr>
    <tr>
      <td>ResourceQuota</td>
      <td>Namespace의 Resource 총량</td>
      <td>CPU, Memory와 Resource 개수 제한</td>
    </tr>
  </tbody>
</table>

<p>RBAC(Role-Based Access Control)는 API Server에 전달된 요청의 권한을 검사한다. Namespace 범위의 <code class="language-plaintext highlighter-rouge">Role</code>과 <code class="language-plaintext highlighter-rouge">RoleBinding</code>을 사용하면 특정 Namespace 안의 Resource만 조작하도록 권한을 제한할 수 있다.</p>

<p>NetworkPolicy는 Pod 사이의 Network Traffic을 제어한다. NetworkPolicy Resource를 생성하더라도 사용하는 CNI가 NetworkPolicy 적용을 지원해야 실제 Traffic이 제어된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 또는 ServiceAccount
        │
        ├── RBAC ────────── API Resource 조작 권한
        │
        ▼
Namespace의 Pod
        │
        └── NetworkPolicy ─ Pod Ingress·Egress Traffic
</code></pre></div></div>

<p>Namespace는 분리의 기준을 제공하고 RBAC와 NetworkPolicy는 그 범위에 맞는 권한과 통신 정책을 적용한다.</p>

<h2 id="3--kubectl과-cluster-조작-흐름">3 ) kubectl과 Cluster 조작 흐름</h2>

<hr />

<blockquote>
  <p><strong>kubectl</strong></p>

  <p>Kubernetes API Server에 Resource 조회·생성·변경·삭제 요청을 보내는 Command Line Client이다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">kubectl</code>은 반드시 Master에서만 실행해야 하는 명령이 아니다. API Server 주소와 인증 정보가 담긴 Kubeconfig를 사용할 수 있다면 별도 관리 PC에서도 실행할 수 있다.</p>

<p>이 글의 명령은 앞에서 구축한 학습용 Cluster의 <strong>Master에서 실행</strong>한다. Master에는 <code class="language-plaintext highlighter-rouge">kubeadm init</code> 후 생성한 Kubeconfig가 설정되어 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master의 kubectl
        │
        ▼
Control Plane의 kube-apiserver
        │
        ├── 인증·인가·요청 검증
        ├── Resource 상태 저장
        └── Controller·Scheduler가 상태 관찰
                    │
                    ▼
             선택된 Worker의 kubelet
                    │
                    ▼
             Container Runtime이 Pod 실행
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl</code>이 Worker에서 Container를 직접 실행하는 것은 아니다. <code class="language-plaintext highlighter-rouge">kubectl</code>이 API Server에 원하는 상태를 전달하면 Control Plane Component가 상태를 조정하고, Pod가 배치된 Worker의 kubelet과 Container Runtime이 실제 Container를 실행한다.</p>

<h2 id="4--kubeconfig와-context">4 ) Kubeconfig와 Context</h2>

<hr />

<blockquote>
  <p><strong>Kubeconfig</strong></p>

  <p>kubectl이 접속할 Cluster, 사용자 인증 정보와 Context를 저장하는 YAML 형식의 설정이다.</p>
</blockquote>

<p>Linux에서 kubectl은 기본적으로 다음 File을 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.kube/config
</code></pre></div></div>

<p>Kubeconfig의 주요 구성은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>구성</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">clusters</code></td>
      <td>API Server 주소와 Cluster 인증서 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">users</code></td>
      <td>Client 인증서, Token 등 사용자 인증 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">contexts</code></td>
      <td>Cluster, User와 기본 Namespace의 조합</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">current-context</code></td>
      <td>현재 kubectl이 기본으로 사용하는 Context</td>
    </tr>
  </tbody>
</table>

<p>현재 Context를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config current-context
</code></pre></div></div>

<p>Kubeconfig에서 현재 선택된 정보만 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config view <span class="nt">--minify</span>
</code></pre></div></div>

<p>특정 Context를 한 명령에만 적용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--context</span> &lt;context-name&gt;
</code></pre></div></div>

<p>현재 Context를 변경하면 이후 명령에 새 Context가 기본으로 사용된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context &lt;context-name&gt;
</code></pre></div></div>

<p>특정 Namespace를 한 요청에 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--namespace</span> kube-system
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-n</code>은 <code class="language-plaintext highlighter-rouge">--namespace</code>의 축약 Option이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> kube-system
</code></pre></div></div>

<p>현재 Context의 기본 Namespace를 변경할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span><span class="o">=</span>default
</code></pre></div></div>

<p>Context의 기본 Namespace를 변경하면 Namespace Option을 생략한 후속 명령에 적용된다. 현재 작업 대상이 어느 Cluster와 Namespace인지 먼저 확인하는 습관이 중요하다.</p>

<h2 id="5--명령형-resource-생성">5 ) 명령형 Resource 생성</h2>

<hr />

<p>kubectl은 실행 명령에 필요한 값을 직접 전달하는 명령형 방식과 YAML Manifest에 원하는 상태를 기록하는 선언형 방식을 제공한다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>예시</th>
      <th>적합한 용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>명령형 Command</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl run</code>, <code class="language-plaintext highlighter-rouge">kubectl create deployment</code></td>
      <td>빠른 Test와 간단한 Resource 생성</td>
    </tr>
    <tr>
      <td>명령형 File 관리</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl create -f</code></td>
      <td>Manifest를 이용한 최초 생성</td>
    </tr>
    <tr>
      <td>선언형 File 관리</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl apply -f</code></td>
      <td>반복 적용, 변경 관리와 자동화</td>
    </tr>
  </tbody>
</table>

<h3 id="kubectl-run">kubectl run</h3>

<p>가장 간단하게 Pod를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Master에서 실행</span>
kubectl run nginx <span class="nt">--image</span><span class="o">=</span>nginx
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>인자</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">nginx</code></td>
      <td>생성할 Pod 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--image=nginx</code></td>
      <td>Container에 사용할 Image</td>
    </tr>
  </tbody>
</table>

<p>생성 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl run</code>은 Test Pod를 빠르게 만들 때 유용하다. 실행에 사용한 Option이 별도 Manifest File로 남지 않으므로 설정이 많아지면 재현과 변경 이력 관리가 어렵다.</p>

<h3 id="kubectl-create">kubectl create</h3>

<p>Resource 종류를 지정하여 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create deployment dpy-nginx <span class="nt">--image</span><span class="o">=</span>nginx
</code></pre></div></div>

<p>Deployment와 Deployment가 관리하는 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployments
kubectl get pods
</code></pre></div></div>

<p>Control Plane에는 Deployment와 Pod의 원하는 상태가 저장되고 Scheduler가 Pod를 실행할 Worker를 선택한다. 선택된 Worker의 kubelet이 Container Runtime을 통해 Nginx Container를 실행한다.</p>

<h2 id="6--manifest를-이용한-생성과-갱신">6 ) Manifest를 이용한 생성과 갱신</h2>

<hr />

<p>실제 설정을 반복해서 적용하려면 Manifest File을 작성한다. 다음 내용을 <code class="language-plaintext highlighter-rouge">sample-pod.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<h3 id="kubectl-create--f">kubectl create -f</h3>

<p>Manifest에 정의한 Resource를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create <span class="nt">-f</span> sample-pod.yaml
</code></pre></div></div>

<p>같은 이름의 Resource가 이미 존재하면 <code class="language-plaintext highlighter-rouge">AlreadyExists</code> 오류가 발생한다. <code class="language-plaintext highlighter-rouge">create</code>는 존재하지 않는 Resource의 최초 생성에 사용한다.</p>

<h3 id="kubectl-apply--f">kubectl apply -f</h3>

<p>Manifest의 선언 상태를 Cluster에 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-pod.yaml
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>현재 상태</th>
      <th>apply 결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Resource가 없음</td>
      <td>새 Resource 생성</td>
    </tr>
    <tr>
      <td>Resource가 있고 Manifest가 변경됨</td>
      <td>변경 사항 적용</td>
    </tr>
    <tr>
      <td>선언 상태에 차이가 없음</td>
      <td>변경 없이 현재 상태 유지</td>
    </tr>
  </tbody>
</table>

<p>생성과 갱신을 같은 명령으로 처리할 수 있어 Script와 CI/CD에서 조건 분기를 줄일 수 있다. 하나의 Resource를 계속 선언형으로 관리하려면 처음부터 <code class="language-plaintext highlighter-rouge">apply</code>를 사용하고 Manifest를 Git에서 관리하는 방식이 적합하다.</p>

<p>명령형 Command와 선언형 <code class="language-plaintext highlighter-rouge">apply</code>를 혼합하면 Cluster의 실제 상태와 저장된 Manifest 사이의 변경 주체를 파악하기 어려워질 수 있다. 운영 방식은 Resource별로 일관되게 유지한다.</p>

<h2 id="7--resource-삭제">7 ) Resource 삭제</h2>

<hr />

<p>종류와 이름으로 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod sample-pod
</code></pre></div></div>

<p>Manifest에 포함된 Resource를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-pod.yaml
</code></pre></div></div>

<p>특정 종류의 모든 Resource를 현재 Namespace에서 삭제하려면 Resource 종류와 <code class="language-plaintext highlighter-rouge">--all</code>을 함께 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pods <span class="nt">--all</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl delete --all</code>처럼 Resource 종류를 생략해서는 삭제 대상을 결정할 수 없다. 또한 <code class="language-plaintext highlighter-rouge">kubectl delete all --all</code>의 <code class="language-plaintext highlighter-rouge">all</code>은 일부 대표 Resource를 묶은 Category이므로 Namespace의 모든 Resource 종류를 뜻하지 않는다.</p>

<h3 id="삭제-대기">삭제 대기</h3>

<p>일반적인 <code class="language-plaintext highlighter-rouge">kubectl delete</code>는 기본적으로 Resource가 삭제될 때까지 기다린다. <code class="language-plaintext highlighter-rouge">--wait</code>를 명시할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-pod.yaml <span class="nt">--wait</span><span class="o">=</span><span class="nb">true</span>
</code></pre></div></div>

<p>Kubernetes Resource 삭제는 API에서 삭제 요청을 받은 뒤 Graceful Termination과 Finalizer 처리를 거칠 수 있어 즉시 완료되지 않을 수 있다.</p>

<h3 id="강제-삭제">강제 삭제</h3>

<p>Pod를 정상 종료할 수 없는 특별한 상황에는 Grace Period를 <code class="language-plaintext highlighter-rouge">0</code>으로 설정하고 강제로 삭제할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod sample-pod <span class="nt">--grace-period</span><span class="o">=</span>0 <span class="nt">--force</span>
</code></pre></div></div>

<p>강제 삭제는 kubelet이 실제 Process 종료를 확인하기 전에 API에서 Pod를 제거할 수 있다. 같은 이름이나 Identity를 사용하는 Process가 동시에 실행되어 Data 불일치가 발생할 수 있으므로 일반적인 삭제 방법으로 사용하지 않는다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Namespace는 하나의 Cluster 안에서 Namespaced Resource의 논리적인 범위와 이름 공간을 구분한다.</p>
    </li>
    <li>
      <p>Namespace만으로 사용자 권한과 Pod Traffic이 완전히 격리되지는 않으며 RBAC와 NetworkPolicy 같은 정책을 함께 구성한다.</p>
    </li>
    <li>
      <p>kubectl은 Kubeconfig의 Cluster·사용자·Context 정보를 사용하여 API Server에 요청한다.</p>
    </li>
    <li>
      <p>Control Plane이 원하는 상태를 저장하고 조정하면 선택된 Worker의 kubelet과 Container Runtime이 실제 Pod를 실행한다.</p>
    </li>
    <li>
      <p>빠른 Test에는 <code class="language-plaintext highlighter-rouge">run</code>과 <code class="language-plaintext highlighter-rouge">create</code>, 반복 가능한 선언형 관리에는 Manifest와 <code class="language-plaintext highlighter-rouge">apply</code>를 사용할 수 있다.</p>
    </li>
    <li>
      <p>강제 삭제는 정상 종료 확인을 생략할 수 있으므로 Data 불일치 위험을 이해한 뒤 제한적으로 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Namespace의 분리 범위와 RBAC·NetworkPolicy의 역할부터 Kubeconfig, Context, kubectl을 이용한 Resource 생성·갱신·삭제까지 정리]]></summary></entry><entry><title type="html">Kubernetes Pod Lifecycle과 kubectl wait</title><link href="https://kkr010128.github.io/cloud-native-24-pod-lifecycle-kubectl-wait/" rel="alternate" type="text/html" title="Kubernetes Pod Lifecycle과 kubectl wait" /><published>2026-08-26T00:00:00+09:00</published><updated>2026-08-26T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-24-pod-lifecycle-kubectl-wait</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-24-pod-lifecycle-kubectl-wait/"><![CDATA[<p>Kubernetes Resource를 연속해서 조작할 때는 앞에서 요청한 Resource가 의도한 상태에 도달했는지 확인해야 한다. Deployment의 Pod를 다시 생성하거나 Pod가 Ready 상태가 될 때까지 기다리려면 Controller의 역할, Pod Lifecycle과 <code class="language-plaintext highlighter-rouge">kubectl wait</code>의 Condition을 함께 이해해야 한다.</p>

<h2 id="1--deployment의-pod-재시작">1 ) Deployment의 Pod 재시작</h2>

<hr />

<p>Pod는 직접 재부팅하는 Virtual Machine과 다르다. Deployment가 관리하는 Pod를 다시 시작하려면 Deployment의 Pod Template을 갱신하여 Controller가 기존 Pod를 교체하도록 요청한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-deployment.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Master에서 Deployment를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-deployment.yaml
</code></pre></div></div>

<p>Deployment와 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployment sample-deployment
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<h3 id="rollout-restart">rollout restart</h3>

<p>Deployment가 관리하는 Pod를 순차적으로 교체한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout restart deployment/sample-deployment
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>구성</th>
      <th>동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rollout restart</code></td>
      <td>Workload의 Pod Template을 갱신하여 새 Rollout 시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">deployment/sample-deployment</code></td>
      <td>재시작할 Resource 종류와 이름</td>
    </tr>
  </tbody>
</table>

<p>Rollout 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<p>Pod 이름과 생성 시간이 바뀌는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">rollout restart</code>는 다음과 같은 상황에서 활용할 수 있다.</p>

<ul>
  <li>
    <p>Application 시작 시 수행되는 초기화 처리를 다시 실행한다.</p>
  </li>
  <li>
    <p>Secret이나 ConfigMap에서 주입된 환경 변수를 새 Pod에 다시 반영한다.</p>
  </li>
  <li>
    <p>새로운 Pod 생성 과정을 통해 일시적인 상태를 초기화한다.</p>
  </li>
</ul>

<p>Deployment Controller는 Control Plane에서 새 ReplicaSet과 Pod 상태를 조정한다. Scheduler가 새 Pod의 Worker를 선택하면 해당 Worker의 kubelet이 Container를 실행한다.</p>

<h3 id="독립-pod와-controller-관리-pod">독립 Pod와 Controller 관리 Pod</h3>

<p><code class="language-plaintext highlighter-rouge">rollout restart</code>는 Deployment, StatefulSet과 DaemonSet처럼 Rollout을 지원하는 Workload에 사용한다. Controller에 연결되지 않은 독립 Pod에는 사용할 수 없다.</p>

<p>독립 Pod를 삭제하면 자동으로 대체 Pod를 생성할 상위 Controller가 없다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Deployment가 관리하는 Pod 삭제
    → Deployment Controller가 부족한 Replica 감지
    → 대체 Pod 생성

독립 Pod 삭제
    → 대체 Pod를 생성할 Controller 없음
</code></pre></div></div>

<p>Pod를 안정적으로 운영하려면 Pod를 직접 생성하기보다 목적에 맞는 Controller를 통해 관리한다.</p>

<h2 id="2--metadataname과-generatename">2 ) metadata.name과 generateName</h2>

<hr />

<p>Kubernetes Resource는 일반적으로 <code class="language-plaintext highlighter-rouge">metadata.name</code>으로 고정된 이름을 지정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod</span>
</code></pre></div></div>

<p>같은 종류와 Namespace에 동일한 이름의 Resource가 있으면 다시 생성할 수 없다.</p>

<h3 id="generatename">generateName</h3>

<blockquote>
  <p><strong>generateName</strong></p>

  <p>Resource 생성 요청마다 API Server가 고유한 접미사를 추가할 수 있도록 이름의 접두사를 지정하는 Field이다.</p>
</blockquote>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-generatename.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">generateName</span><span class="pi">:</span> <span class="s">sample-generatename-</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">create</code>로 Resource를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create <span class="nt">-f</span> sample-generatename.yaml
</code></pre></div></div>

<p>생성된 이름을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
</code></pre></div></div>

<p>출력 예시는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                          READY   STATUS    RESTARTS   AGE
sample-generatename-k7m2p     1/1     Running   0          10s
</code></pre></div></div>

<p>같은 File로 다시 <code class="language-plaintext highlighter-rouge">create</code>하면 다른 접미사를 가진 새 Pod가 생성된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create <span class="nt">-f</span> sample-generatename.yaml
kubectl get pods
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">generateName</code>은 매 요청마다 새 Resource를 만들 때 사용한다. 동일한 Resource를 반복 갱신하는 선언형 <code class="language-plaintext highlighter-rouge">apply</code>에는 안정적인 <code class="language-plaintext highlighter-rouge">metadata.name</code>이 필요하므로 <code class="language-plaintext highlighter-rouge">generateName</code> 예제는 <code class="language-plaintext highlighter-rouge">create</code>로 실행한다.</p>

<h2 id="3--pod-lifecycle">3 ) Pod Lifecycle</h2>

<hr />

<blockquote>
  <p><strong>Pod Phase</strong></p>

  <p>Pod Lifecycle의 상위 상태를 나타내는 값이다. <code class="language-plaintext highlighter-rouge">Pending</code>, <code class="language-plaintext highlighter-rouge">Running</code>, <code class="language-plaintext highlighter-rouge">Succeeded</code>, <code class="language-plaintext highlighter-rouge">Failed</code>, <code class="language-plaintext highlighter-rouge">Unknown</code> 중 하나로 기록된다.</p>
</blockquote>

<p>Pod Phase는 Pod 전체의 큰 상태를 표현한다. <code class="language-plaintext highlighter-rouge">kubectl get pods</code>의 <code class="language-plaintext highlighter-rouge">STATUS</code> 열에는 Phase뿐 아니라 Container 생성과 재시작 과정에서 발생한 Reason도 표시될 수 있다.</p>

<h3 id="pod-phase">Pod Phase</h3>

<table>
  <thead>
    <tr>
      <th>Phase</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Pending</code></td>
      <td>Pod가 승인되었지만 Container가 아직 준비되지 않았으며 Scheduling이나 Image Download를 기다릴 수 있는 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Running</code></td>
      <td>Pod가 Node에 Binding되었고 모든 Container가 생성되었으며 하나 이상이 실행 또는 시작·재시작 중인 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Succeeded</code></td>
      <td>Pod의 모든 Container가 성공적으로 종료되었고 다시 시작되지 않는 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Failed</code></td>
      <td>모든 Container가 종료되었고 하나 이상이 실패로 종료되었으며 다시 시작되지 않는 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Unknown</code></td>
      <td>일반적으로 Node 통신 문제로 Pod 상태를 확인할 수 없는 상태</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">Completed</code>는 <code class="language-plaintext highlighter-rouge">kubectl get pods</code>의 <code class="language-plaintext highlighter-rouge">STATUS</code>에서 성공적으로 끝난 Pod를 표현할 때 보이는 값이며 Pod Phase의 실제 값은 <code class="language-plaintext highlighter-rouge">Succeeded</code>이다. Job이나 CronJob이 만든 완료형 Pod에서 주로 확인할 수 있다.</p>

<h3 id="kubectl에-표시되는-상태와-reason">kubectl에 표시되는 상태와 Reason</h3>

<table>
  <thead>
    <tr>
      <th>표시</th>
      <th>의미</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ContainerCreating</code></td>
      <td>Volume Mount와 Network 설정 등 Container 생성 진행</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl describe pod</code>의 Event</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CrashLoopBackOff</code></td>
      <td>Container가 반복 실패하여 재시작 대기 시간이 증가</td>
      <td>Application Log, Command, 환경 변수, Probe</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ErrImagePull</code></td>
      <td>Image를 가져오는 첫 시도가 실패</td>
      <td>Image 이름, Tag, Registry 인증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ImagePullBackOff</code></td>
      <td>Image Pull 실패가 반복되어 재시도 대기</td>
      <td>Registry 접근, ImagePullSecret</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OOMKilled</code></td>
      <td>Memory 제한 또는 Node Memory 상황으로 Process 종료</td>
      <td>Resource Limit과 사용량</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Terminating</code></td>
      <td>Graceful Termination과 Resource 정리 진행</td>
      <td>Finalizer, Volume, Node 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Init:0/N</code></td>
      <td>N개 Init Container 중 완료된 수가 0개</td>
      <td>Init Container Log와 상태</td>
    </tr>
  </tbody>
</table>

<p>이 값들을 모두 Pod Phase로 분류해서는 안 된다. 예를 들어 Pod Phase가 <code class="language-plaintext highlighter-rouge">Running</code>이어도 Container 하나가 <code class="language-plaintext highlighter-rouge">CrashLoopBackOff</code> 상태일 수 있다.</p>

<h3 id="상태-확인">상태 확인</h3>

<p>Pod 목록을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
</code></pre></div></div>

<p>Pod의 Phase만 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.status.phase}'</span>
</code></pre></div></div>

<p>Container별 상태와 Event를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod sample-pod
</code></pre></div></div>

<p>Control Plane의 API Server에는 Pod Status와 Condition이 저장된다. Worker의 kubelet은 실제 Container 상태를 관찰하여 API Server에 보고하고, kubectl은 저장된 상태를 조회한다.</p>

<h2 id="4--pod-condition">4 ) Pod Condition</h2>

<hr />

<p>Condition은 Pod가 특정 조건을 만족하는지를 나타낸다. <code class="language-plaintext highlighter-rouge">kubectl wait</code>는 이 값을 기준으로 다음 작업을 시작할 시점을 결정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>Condition</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PodScheduled</code></td>
      <td>Scheduler가 Pod를 실행할 Node에 Binding함</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Initialized</code></td>
      <td>모든 Init Container가 성공적으로 완료됨</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ContainersReady</code></td>
      <td>Pod의 모든 Container가 Ready 상태임</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Ready</code></td>
      <td>Pod가 Service 요청을 받을 준비가 됨</td>
    </tr>
  </tbody>
</table>

<p>Pod의 Condition을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.status.conditions}'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Running</code>은 Container가 실행 중임을 나타내지만 Application이 Traffic을 받을 준비가 끝났다는 의미와 항상 같지는 않다. 후속 작업에서 Service 제공 가능 상태가 필요하면 <code class="language-plaintext highlighter-rouge">Ready</code> Condition을 기준으로 기다린다.</p>

<h2 id="5--kubectl-wait">5 ) kubectl wait</h2>

<hr />

<blockquote>
  <p><strong>kubectl wait</strong></p>

  <p>하나 이상의 Resource가 지정한 Condition, 생성 또는 삭제 상태에 도달할 때까지 기다리는 명령이다.</p>
</blockquote>

<p>기본 Timeout은 30초이다. Image Download나 Application 초기화 시간이 더 필요한 실습에서는 <code class="language-plaintext highlighter-rouge">--timeout</code>을 명시한다.</p>

<h3 id="ready-상태-대기">Ready 상태 대기</h3>

<p><code class="language-plaintext highlighter-rouge">sample-pod</code>가 Ready 상태가 될 때까지 최대 60초 동안 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-pod.yaml
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Ready pod/sample-pod <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>성공하면 다음과 같은 결과가 출력된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pod/sample-pod condition met
</code></pre></div></div>

<h3 id="scheduling-완료-대기">Scheduling 완료 대기</h3>

<p>현재 Namespace의 모든 Pod가 Node에 Scheduling될 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>PodScheduled pods <span class="nt">--all</span> <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--all</code>은 지정한 Resource 종류의 모든 대상을 선택한다. 다른 Namespace의 Pod까지 포함하려면 <code class="language-plaintext highlighter-rouge">--all-namespaces</code>를 추가해야 한다.</p>

<h3 id="phase-값-대기">Phase 값 대기</h3>

<p>Pod Phase가 <code class="language-plaintext highlighter-rouge">Running</code>이 될 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait </span>pod/sample-pod <span class="se">\</span>
  <span class="nt">--for</span><span class="o">=</span><span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.status.phase}'</span><span class="o">=</span>Running <span class="se">\</span>
  <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>Application 사용 가능 여부가 목적이라면 Phase보다 <code class="language-plaintext highlighter-rouge">Ready</code> Condition이 더 적절할 수 있다.</p>

<h3 id="삭제-완료-대기">삭제 완료 대기</h3>

<p>일반적인 <code class="language-plaintext highlighter-rouge">kubectl delete</code>는 기본적으로 삭제 완료를 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod sample-pod
</code></pre></div></div>

<p>삭제 요청과 대기를 분리해야 한다면 먼저 기다리지 않고 삭제를 요청한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod sample-pod <span class="nt">--wait</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>이후 Resource가 사라질 때까지 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span>delete pod/sample-pod <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>현재 Namespace의 모든 Pod에 삭제를 요청하고 완료 대기를 분리할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pods <span class="nt">--all</span> <span class="nt">--wait</span><span class="o">=</span><span class="nb">false
</span>kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span>delete pods <span class="nt">--all</span> <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>두 번째 명령은 실행 시점에 남아 있는 Pod를 대상으로 기다린다. 이미 모든 Pod가 삭제되어 일치하는 Resource가 없다면 별도의 대기가 필요하지 않다.</p>

<p>여러 Pod를 대상으로 할 때도 Timeout은 명령 전체의 대기 한도를 나타낸다. Pod마다 정해진 시간을 순서대로 더해 기다린다는 의미가 아니다.</p>

<h3 id="timeout-처리">Timeout 처리</h3>

<p>지정한 시간 안에 상태가 충족되지 않으면 명령은 실패 상태로 종료된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>error: timed out waiting for the condition on pods/sample-pod
</code></pre></div></div>

<p>Timeout을 무조건 늘리기 전에 <code class="language-plaintext highlighter-rouge">kubectl get pods</code>와 <code class="language-plaintext highlighter-rouge">kubectl describe pod</code>로 Scheduling, Image Pull, Container 실행과 Probe 상태를 확인한다.</p>

<h2 id="6--실습-resource-정리">6 ) 실습 Resource 정리</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">generateName</code>으로 만든 Pod를 이름 Prefix와 Label 없이 한꺼번에 선택하기는 어렵다. 목록을 확인한 뒤 생성된 이름을 지정하여 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
kubectl delete pod &lt;generated-pod-name&gt;
</code></pre></div></div>

<p>Deployment를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-deployment.yaml
</code></pre></div></div>

<p>Deployment를 삭제하면 Deployment가 관리하던 ReplicaSet과 Pod도 함께 정리된다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">rollout restart</code>는 Deployment 같은 Workload의 Pod Template을 갱신하여 Controller가 Pod를 교체하도록 한다.</p>
    </li>
    <li>
      <p>독립 Pod는 상위 Controller가 없으므로 삭제 후 자동으로 대체되지 않는다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">generateName</code>은 생성 요청마다 고유한 이름이 필요한 Resource에 사용하며 <code class="language-plaintext highlighter-rouge">create</code>와 결합한다.</p>
    </li>
    <li>
      <p>Pod Phase와 <code class="language-plaintext highlighter-rouge">CrashLoopBackOff</code>, <code class="language-plaintext highlighter-rouge">ContainerCreating</code> 같은 kubectl 상태 표시는 구분해서 해석한다.</p>
    </li>
    <li>
      <p>Worker의 kubelet이 Container 상태를 API Server에 보고하면 kubectl이 Control Plane에 저장된 Status와 Condition을 조회한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubectl wait</code>는 Ready, Scheduling, Phase 또는 삭제 완료를 후속 작업의 조건으로 사용할 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Deployment 재시작과 generateName 사용법, Pod Phase와 kubectl 상태 표시의 차이, Condition 기반 작업 완료 대기 정리]]></summary></entry><entry><title type="html">Kubernetes Manifest와 Metadata 관리</title><link href="https://kkr010128.github.io/cloud-native-25-manifest-metadata-management/" rel="alternate" type="text/html" title="Kubernetes Manifest와 Metadata 관리" /><published>2026-08-26T00:00:00+09:00</published><updated>2026-08-26T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-25-manifest-metadata-management</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-25-manifest-metadata-management/"><![CDATA[<p>Kubernetes Resource가 많아지면 개별 명령을 반복하는 대신 Manifest를 File과 Directory 단위로 관리해야 한다. 여러 Resource를 하나의 Manifest에 정의하거나 여러 File로 분리할 수 있으며, Label과 Annotation을 이용해 Resource를 분류하고 관리 정보를 기록할 수 있다.</p>

<h2 id="1--manifest-설계">1 ) Manifest 설계</h2>

<hr />

<blockquote>
  <p><strong>Manifest</strong></p>

  <p>Kubernetes API에 생성할 Object의 원하는 상태를 YAML 또는 JSON으로 작성한 File이다.</p>
</blockquote>

<p>Kubernetes Manifest의 주요 Field는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">apiVersion</code></td>
      <td>Resource가 사용하는 Kubernetes API Version</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">kind</code></td>
      <td>Pod, Deployment, Service 등 Resource 종류</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metadata</code></td>
      <td>이름, Namespace, Label과 Annotation 등 식별·관리 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">spec</code></td>
      <td>사용자가 원하는 Resource 상태</td>
    </tr>
  </tbody>
</table>

<p>Control Plane은 Manifest로 전달된 <code class="language-plaintext highlighter-rouge">spec</code>을 Desired State로 저장한다. Controller와 Scheduler가 이 상태를 관찰하고, Worker의 kubelet이 할당된 Pod를 실제로 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manifest의 spec
      │
      ▼
API Server가 Desired State 저장
      │
      ├── Controller가 Resource 상태 조정
      ├── Scheduler가 Worker 선택
      └── Worker의 kubelet이 Pod 실행
</code></pre></div></div>

<h2 id="2--하나의-manifest에-여러-resource-정의">2 ) 하나의 Manifest에 여러 Resource 정의</h2>

<hr />

<p>YAML Document 구분자인 <code class="language-plaintext highlighter-rouge">---</code>를 사용하면 하나의 File에 여러 Resource를 정의할 수 있다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-multi-resource-manifest.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">order1-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.17</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">order2-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">LoadBalancer</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-port</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">80</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>Control Plane의 처리</th>
      <th>Worker에 미치는 결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Deployment</td>
      <td>ReplicaSet과 Pod의 Desired State 관리</td>
      <td>선택된 Worker에서 Nginx Pod 실행</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>Label Selector와 Service Port 상태 관리</td>
      <td>각 Node의 Service Network Rule에 반영</td>
    </tr>
  </tbody>
</table>

<p>Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Master에서 실행</span>
kubectl apply <span class="nt">-f</span> sample-multi-resource-manifest.yaml
</code></pre></div></div>

<p>생성 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployments
kubectl get services
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app <span class="nt">-o</span> wide
</code></pre></div></div>

<p>하나의 File로 함께 관리하면 관련 Resource를 같은 명령으로 적용하고 삭제하기 쉽다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> sample-multi-resource-manifest.yaml
</code></pre></div></div>

<h3 id="resource-적용-순서">Resource 적용 순서</h3>

<p>kubectl은 File의 YAML Document를 읽어 API Server에 요청하지만 전체 File을 하나의 Transaction으로 적용하지 않는다. 앞 Resource가 생성되었다고 해서 실제 Pod가 Ready 상태가 된 후 다음 Resource를 처리하는 것도 아니다.</p>

<p>일부 Resource 요청이 실패하더라도 이미 API Server가 승인한 Resource는 남을 수 있다. 따라서 File에 적힌 순서를 Application 기동 의존성을 보장하는 방법으로 사용하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Deployment 생성 요청 승인
Service 생성 요청 실패
        │
        └── 이미 생성된 Deployment는 자동 Rollback되지 않음
</code></pre></div></div>

<p>Resource 사이에 준비 완료 조건이 필요하면 Readiness Probe, Init Container 또는 <code class="language-plaintext highlighter-rouge">kubectl wait</code>처럼 상태를 확인하는 구조를 사용한다.</p>

<p><code class="language-plaintext highlighter-rouge">nginx:1.17</code>은 고정 Tag를 보여주기 위한 오래된 Image 예시이다. 운영 환경에서는 보안 Update가 제공되는 검증된 Version Tag 또는 Image Digest를 사용한다.</p>

<h2 id="3--여러-manifest-file-적용">3 ) 여러 Manifest File 적용</h2>

<hr />

<p>Resource 종류나 Application 구성 단위로 File을 분리할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dir/
├── sample-pod1.yaml
└── sample-pod2.yaml
</code></pre></div></div>

<p>Directory를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> <span class="nb">dir</span>
</code></pre></div></div>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">dir/sample-pod1.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod1</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.17</span>
</code></pre></div></div>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">dir/sample-pod2.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod2</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.17</span>
</code></pre></div></div>

<p>Directory 안의 Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> ./dir
</code></pre></div></div>

<p>Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods sample-pod1 sample-pod2
</code></pre></div></div>

<h3 id="하위-directory-재귀-적용">하위 Directory 재귀 적용</h3>

<p><code class="language-plaintext highlighter-rouge">-f</code>로 지정한 Directory의 하위 Directory까지 적용하려면 <code class="language-plaintext highlighter-rouge">-R</code> 또는 <code class="language-plaintext highlighter-rouge">--recursive</code>를 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> ./dir <span class="nt">-R</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-f</code>, <code class="language-plaintext highlighter-rouge">--filename</code></td>
      <td>적용할 File 또는 Directory 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-R</code>, <code class="language-plaintext highlighter-rouge">--recursive</code></td>
      <td>지정한 Directory 아래를 재귀적으로 탐색</td>
    </tr>
  </tbody>
</table>

<p>File 이름의 번호를 Resource 준비 순서를 보장하는 장치로 사용하지 않는다. Kubernetes Resource는 API 요청 후 Controller가 비동기로 상태를 조정하므로 File 처리 순서와 Application 준비 완료 순서는 다를 수 있다.</p>

<h3 id="directory-분리-기준">Directory 분리 기준</h3>

<p>규모에 따라 다음 방식으로 구성할 수 있다.</p>

<ul>
  <li>
    <p>작은 System은 관련 Manifest를 하나의 Directory에 모은다.</p>
  </li>
  <li>
    <p>규모가 커지면 Subsystem이나 운영 Team 단위로 Directory를 나눈다.</p>
  </li>
  <li>
    <p>Microservice별 Directory를 만들고 Deployment, Service와 설정 Resource를 함께 둔다.</p>
  </li>
  <li>
    <p>여러 Application이 공유하는 ConfigMap이나 공통 정책은 별도 Directory로 분리한다.</p>
  </li>
</ul>

<p>Directory가 지나치게 세분화되면 어떤 Resource를 함께 적용해야 하는지 파악하기 어려울 수 있다. 반대로 모든 Resource를 하나의 File에 모으면 변경 충돌과 검토 범위가 커진다. 배포·Rollback·소유권 단위를 기준으로 분리한다.</p>

<h2 id="4--label과-annotation">4 ) Label과 Annotation</h2>

<hr />

<p>Kubernetes Resource에는 Label과 Annotation 형태의 Metadata를 추가할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Label</th>
      <th>Annotation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목적</td>
      <td>Resource 분류, 선택과 검색</td>
      <td>설명, 도구 설정, Build 정보 등 비식별 Metadata 저장</td>
    </tr>
    <tr>
      <td>Selector 지원</td>
      <td>지원</td>
      <td>지원하지 않음</td>
    </tr>
    <tr>
      <td>값의 형태</td>
      <td>길이와 문자 구성이 제한된 String</td>
      <td>문자열이며 공백·특수 문자와 구조화된 Text 저장 가능</td>
    </tr>
    <tr>
      <td>대표 사용</td>
      <td>Application, 환경, Tier, 관리 대상 구분</td>
      <td>담당자, 배포 Revision, 외부 도구 설정</td>
    </tr>
  </tbody>
</table>

<h3 id="label">Label</h3>

<blockquote>
  <p><strong>Label</strong></p>

  <p>Resource를 분류하고 Selector로 Resource 집합을 선택하기 위한 Key-Value Metadata이다.</p>
</blockquote>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-label-pod.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-label-pod</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
    <span class="na">environment</span><span class="pi">:</span> <span class="s">development</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>적용 후 Label을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-label-pod.yaml
kubectl get pods <span class="nt">--show-labels</span>
</code></pre></div></div>

<p>Label Selector로 Pod를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<p>여러 조건을 쉼표로 연결하면 모든 조건을 만족하는 Resource를 선택한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app,environment<span class="o">=</span>development
</code></pre></div></div>

<p>Label Key는 선택적인 DNS Prefix와 <code class="language-plaintext highlighter-rouge">/</code>, 필수 Name Segment로 구성할 수 있다. Name Segment와 Label Value는 각각 63자 이하이며, Prefix를 사용하면 DNS Subdomain 형식으로 최대 253자까지 사용할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>example.com/application
─────────── ───────────
   Prefix       Name
</code></pre></div></div>

<h3 id="annotation">Annotation</h3>

<blockquote>
  <p><strong>Annotation</strong></p>

  <p>Selector로 분류할 필요는 없지만 Resource와 함께 보존해야 하는 추가 정보를 기록하는 Metadata이다.</p>
</blockquote>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-annotation-pod</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Nginx</span><span class="nv"> </span><span class="s">학습용</span><span class="nv"> </span><span class="s">Pod"</span>
    <span class="na">build.example.com/revision</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2026-08-26.1"</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Annotation의 Key와 Value는 문자열이어야 한다. 모든 Annotation Key와 Value를 합한 크기는 한 Object에서 256KiB를 넘을 수 없다.</p>

<p>Label에는 Selector에 사용할 간단한 식별값을 넣고, 긴 설명이나 도구가 사용하는 설정은 Annotation에 넣는다.</p>

<h2 id="5--prune을-이용한-resource-정리">5 ) prune을 이용한 Resource 정리</h2>

<hr />

<p>Git에서 Manifest를 삭제한 뒤 <code class="language-plaintext highlighter-rouge">kubectl apply -f</code>만 다시 실행하면 Cluster의 기존 Resource가 자동으로 삭제되지는 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Git Directory                         Kubernetes Cluster
├── sample-pod1.yaml                  ├── sample-pod1
└── sample-pod2.yaml                  └── sample-pod2

sample-pod2.yaml 삭제 후 apply

Git Directory                         Kubernetes Cluster
└── sample-pod1.yaml                  ├── sample-pod1
                                      └── sample-pod2  ← 그대로 남음
</code></pre></div></div>

<p>명시적으로 삭제하는 방법이 가장 이해하기 쉽다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> prune/sample-pod2.yaml
</code></pre></div></div>

<p>삭제된 Manifest가 더 이상 없어서 File 기반 삭제를 사용할 수 없다면 Resource 종류와 이름 또는 Label을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod sample-pod2
</code></pre></div></div>

<h3 id="prune-실습-manifest">prune 실습 Manifest</h3>

<p>Directory를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> prune
</code></pre></div></div>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">prune/sample-pod1.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod1</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">system</span><span class="pi">:</span> <span class="s">a</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.17</span>
</code></pre></div></div>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">prune/sample-pod2.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod2</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">system</span><span class="pi">:</span> <span class="s">a</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.17</span>
</code></pre></div></div>

<p>두 Resource를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> ./prune
kubectl get pods <span class="nt">-l</span> <span class="nv">system</span><span class="o">=</span>a
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">sample-pod2.yaml</code>을 Directory에서 제거한 뒤 일반 <code class="language-plaintext highlighter-rouge">apply</code>를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm </span>prune/sample-pod2.yaml
kubectl apply <span class="nt">-f</span> ./prune
kubectl get pods <span class="nt">-l</span> <span class="nv">system</span><span class="o">=</span>a
</code></pre></div></div>

<p>일반 <code class="language-plaintext highlighter-rouge">apply</code>만으로는 <code class="language-plaintext highlighter-rouge">sample-pod2</code>가 삭제되지 않는다.</p>

<h3 id="apply-prune">apply –prune</h3>

<p><code class="language-plaintext highlighter-rouge">--prune</code>은 이전에 <code class="language-plaintext highlighter-rouge">apply</code>로 관리하던 Resource 중 현재 Manifest 집합에 포함되지 않은 대상을 삭제한다.</p>

<p>Kubernetes <code class="language-plaintext highlighter-rouge">v1.36</code> 기준 기존 Allowlist 기반 <code class="language-plaintext highlighter-rouge">--prune</code>과 ApplySet 기반 방식은 모두 Alpha 상태이다. 삭제 범위를 잘못 지정하면 예상하지 않은 Resource가 제거될 수 있으므로 운영 환경에서는 명시적인 <code class="language-plaintext highlighter-rouge">kubectl delete -f</code>를 우선한다.</p>

<p>실습에서 Pod와 <code class="language-plaintext highlighter-rouge">system=a</code> Label로 범위를 제한한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> ./prune <span class="se">\</span>
  <span class="nt">--prune</span> <span class="se">\</span>
  <span class="nt">-l</span> <span class="nv">system</span><span class="o">=</span>a <span class="se">\</span>
  <span class="nt">--prune-allowlist</span><span class="o">=</span>core/v1/Pod
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--prune</code></td>
      <td>현재 Manifest 집합에 없는 기존 관리 Resource 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-l system=a</code></td>
      <td>삭제 검토 대상을 해당 Label Resource로 제한</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--prune-allowlist=core/v1/Pod</code></td>
      <td>Prune할 Group, Version과 Kind를 Pod로 제한</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">--prune</code>은 관리 대상 전체가 들어 있는 Root Directory에서 실행한다. 일부 Subdirectory만 대상으로 실행하면 다른 Directory에 있는 정상 Resource가 누락된 것으로 판단될 수 있다.</p>

<p>실습 결과를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">system</span><span class="o">=</span>a
</code></pre></div></div>

<p>실습 Resource를 명시적으로 정리한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pods <span class="nt">-l</span> <span class="nv">system</span><span class="o">=</span>a
</code></pre></div></div>

<h2 id="6--resource-일부-정보-변경">6 ) Resource 일부 정보 변경</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">kubectl set</code>은 Resource의 특정 Field를 명령형으로 변경한다.</p>

<table>
  <thead>
    <tr>
      <th>하위 명령</th>
      <th>변경 대상</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set env</code></td>
      <td>환경 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set image</code></td>
      <td>Container Image</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set resources</code></td>
      <td>Resource Request와 Limit</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set selector</code></td>
      <td>Resource Selector</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set serviceaccount</code></td>
      <td>Pod Template의 ServiceAccount</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">set subject</code></td>
      <td>RoleBinding과 ClusterRoleBinding의 Subject</td>
    </tr>
  </tbody>
</table>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-deployment.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">sample-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-deployment.yaml
</code></pre></div></div>

<p>독립 Pod의 Image도 Container 이름을 지정하여 변경할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image pod/sample-label-pod <span class="se">\</span>
  nginx-container<span class="o">=</span>nginx:stable
</code></pre></div></div>

<p>독립 Pod는 Rollout과 Replica 유지 기능이 없으므로 Application Workload는 일반적으로 Deployment 같은 Controller로 관리한다.</p>

<p>Deployment가 사용하는 Image를 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image deployment/sample-deployment <span class="se">\</span>
  nginx-container<span class="o">=</span>nginx:stable
</code></pre></div></div>

<p>Image 변경으로 Deployment Rollout이 시작된다. Control Plane의 Deployment Controller가 새 ReplicaSet을 만들고 Worker에서는 새 Image를 사용하는 Pod가 실행된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout status deployment/sample-deployment
</code></pre></div></div>

<h3 id="manifest와-실제-상태-차이-확인">Manifest와 실제 상태 차이 확인</h3>

<p>명령형으로 변경한 Cluster 상태와 Local Manifest의 차이를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl diff <span class="nt">-f</span> sample-deployment.yaml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kubectl diff</code>는 실제 변경을 적용하지 않고 <code class="language-plaintext highlighter-rouge">apply</code> 시 변경될 내용을 미리 보여준다. 운영 환경에서는 변경 전 Diff를 검토하고 Manifest에 의도한 값을 반영한 뒤 <code class="language-plaintext highlighter-rouge">apply</code>하는 방식이 적합하다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-deployment.yaml
</code></pre></div></div>

<p>명령형 <code class="language-plaintext highlighter-rouge">set</code>으로 긴급 변경한 뒤 Manifest를 갱신하지 않으면 다음 <code class="language-plaintext highlighter-rouge">apply</code>에서 값이 이전 선언으로 돌아갈 수 있다.</p>

<h2 id="7--사용-가능한-resource-종류-확인">7 ) 사용 가능한 Resource 종류 확인</h2>

<hr />

<p>현재 API Server가 제공하는 Resource 종류를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl api-resources
</code></pre></div></div>

<p>Namespace에 속하는 Resource만 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">true</span>
</code></pre></div></div>

<p>Cluster 전체 범위 Resource만 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>출력에는 Resource 이름, 축약 이름, API Version, Namespace 적용 여부와 Kind가 포함된다. Manifest의 <code class="language-plaintext highlighter-rouge">apiVersion</code>과 <code class="language-plaintext highlighter-rouge">kind</code>를 확인하거나 <code class="language-plaintext highlighter-rouge">kubectl get</code>에 사용할 Resource 이름을 찾을 때 활용한다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Manifest는 Kubernetes Object의 Desired State를 기록하며 여러 Resource를 <code class="language-plaintext highlighter-rouge">---</code>로 구분해 한 File에 작성할 수 있다.</p>
    </li>
    <li>
      <p>여러 Resource 적용은 하나의 Transaction이 아니며 File 순서가 Application 준비 완료 순서를 보장하지 않는다.</p>
    </li>
    <li>
      <p>여러 Manifest는 Directory 단위로 적용할 수 있고 <code class="language-plaintext highlighter-rouge">-R</code>을 사용하면 하위 Directory까지 재귀적으로 처리한다.</p>
    </li>
    <li>
      <p>Label은 Resource 선택과 분류에 사용하고 Annotation은 Selector가 필요 없는 추가 Metadata를 저장한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">apply --prune</code>은 삭제 범위에 따라 정상 Resource를 제거할 수 있는 Alpha 기능이므로 Label과 Allowlist로 범위를 제한하고 명시적 삭제를 우선한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubectl set</code>으로 변경한 값은 Manifest에도 반영하고 <code class="language-plaintext highlighter-rouge">kubectl diff</code>로 실제 상태와 선언 상태를 비교한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[여러 Resource를 정의하는 Manifest 구성과 Directory 적용, Label·Annotation, prune, kubectl set과 diff를 이용한 변경 관리 정리]]></summary></entry><entry><title type="html">Kubernetes Resource 조회와 Pod Debugging</title><link href="https://kkr010128.github.io/cloud-native-26-resource-inspection-debugging/" rel="alternate" type="text/html" title="Kubernetes Resource 조회와 Pod Debugging" /><published>2026-08-26T00:00:00+09:00</published><updated>2026-08-26T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-26-resource-inspection-debugging</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-26-resource-inspection-debugging/"><![CDATA[<p>Kubernetes Resource를 운영하려면 생성 명령뿐 아니라 현재 상태를 정확하게 조회하고 실행 중인 Container를 확인할 수 있어야 한다. <code class="language-plaintext highlighter-rouge">kubectl get</code>과 <code class="language-plaintext highlighter-rouge">describe</code>로 Control Plane에 저장된 상태를 확인하고, Metrics Server, <code class="language-plaintext highlighter-rouge">exec</code>, <code class="language-plaintext highlighter-rouge">debug</code>, <code class="language-plaintext highlighter-rouge">cp</code>, <code class="language-plaintext highlighter-rouge">port-forward</code>와 <code class="language-plaintext highlighter-rouge">logs</code>로 Worker에서 실행되는 Pod를 점검한다.</p>

<h2 id="1--resource-조회-범위">1 ) Resource 조회 범위</h2>

<hr />

<p>현재 API Server가 제공하는 Resource 종류를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Master에서 실행</span>
kubectl api-resources
</code></pre></div></div>

<p>Namespace에 속하는 Resource와 Cluster 전체 범위 Resource를 구분한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">true
</span>kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<p>조회할 Resource의 Namespace 범위를 이해해야 원하는 결과를 얻을 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>범위</th>
      <th>조회 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Pod, Deployment, Service</td>
      <td>Namespace</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl get pods -n default</code></td>
    </tr>
    <tr>
      <td>Node, PersistentVolume</td>
      <td>Cluster</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl get nodes</code></td>
    </tr>
    <tr>
      <td>Namespace</td>
      <td>Cluster</td>
      <td><code class="language-plaintext highlighter-rouge">kubectl get namespaces</code></td>
    </tr>
  </tbody>
</table>

<h2 id="2--kubectl-get">2 ) kubectl get</h2>

<hr />

<blockquote>
  <p><strong>kubectl get</strong></p>

  <p>API Server에서 하나 이상의 Kubernetes Resource 목록이나 현재 상태를 조회하는 명령이다.</p>
</blockquote>

<h3 id="기본-조회">기본 조회</h3>

<p>현재 Namespace의 Pod를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
</code></pre></div></div>

<p>특정 종류의 Resource를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get deployments
kubectl get services
</code></pre></div></div>

<p>특정 이름의 Resource를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod
</code></pre></div></div>

<p>Node 목록은 Cluster 범위에서 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div>

<h3 id="get-all의-범위">get all의 범위</h3>

<p>현재 Namespace의 대표적인 Workload와 Service Resource를 함께 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get all
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">all</code>은 Kubernetes의 모든 Resource 종류를 뜻하지 않는다. ConfigMap, Secret, Ingress, PersistentVolumeClaim 등은 출력에 포함되지 않을 수 있으므로 필요한 Resource 종류를 명시해서 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get configmaps,secrets,ingresses,persistentvolumeclaims
</code></pre></div></div>

<h3 id="namespace-지정">Namespace 지정</h3>

<p>특정 Namespace의 Resource를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--namespace</span> kube-system
</code></pre></div></div>

<p>축약 Option을 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> kube-system
</code></pre></div></div>

<p>모든 Namespace의 Pod를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--all-namespaces</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-A</code>는 <code class="language-plaintext highlighter-rouge">--all-namespaces</code>의 축약 Option이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-A</span>
</code></pre></div></div>

<h3 id="label-selector">Label Selector</h3>

<p>특정 Label을 가진 Pod를 조회한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<p>Label 이름과 값은 <code class="language-plaintext highlighter-rouge">-l</code> 또는 <code class="language-plaintext highlighter-rouge">--selector</code> 뒤에 지정해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--selector</span> <span class="nv">app</span><span class="o">=</span>sample-app
</code></pre></div></div>

<p>Pod가 가진 Label을 함께 출력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">--show-labels</span>
</code></pre></div></div>

<h2 id="3--출력-형식">3 ) 출력 형식</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">--output</code> 또는 <code class="language-plaintext highlighter-rouge">-o</code> Option으로 JSON, YAML, Custom Columns, JSONPath와 Go Template 등 다양한 출력 형식을 선택할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>형식</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">wide</code></td>
      <td>Node, Pod IP 등 기본 표보다 많은 Column 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">yaml</code></td>
      <td>Resource 전체 내용을 YAML로 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">json</code></td>
      <td>Resource 전체 내용을 JSON으로 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">custom-columns</code></td>
      <td>필요한 Field를 표의 Column으로 구성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jsonpath</code></td>
      <td>JSONPath 표현식으로 특정 Field 추출</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">name</code></td>
      <td><code class="language-plaintext highlighter-rouge">resource/name</code> 형식만 출력</td>
    </tr>
  </tbody>
</table>

<h3 id="wide-출력">Wide 출력</h3>

<p>Pod가 어느 Worker에서 실행되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-o</span> wide
</code></pre></div></div>

<h3 id="yaml과-json">YAML과 JSON</h3>

<p>Pod 전체 정보를 YAML로 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="nt">-o</span> yaml
</code></pre></div></div>

<p>JSON으로 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="nt">-o</span> json
</code></pre></div></div>

<h3 id="custom-columns">Custom Columns</h3>

<p>Pod 이름과 실행 중인 Worker의 Host IP를 표로 출력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="se">\</span>
  <span class="nt">-o</span> custom-columns<span class="o">=</span><span class="s1">'NAME:.metadata.name,NODE_IP:.status.hostIP'</span>
</code></pre></div></div>

<p>출력 예시는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME         NODE_IP
sample-pod   192.168.0.101
</code></pre></div></div>

<h3 id="jsonpath">JSONPath</h3>

<p>특정 Pod의 이름만 추출한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.metadata.name}'</span>
</code></pre></div></div>

<p>줄바꿈까지 출력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pod sample-pod <span class="se">\</span>
  <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.metadata.name}{"\n"}'</span>
</code></pre></div></div>

<p>Shell이 <code class="language-plaintext highlighter-rouge">{}</code>와 특수 문자를 먼저 해석하지 않도록 JSONPath 표현식은 작은따옴표로 감싼다.</p>

<h2 id="4--kubectl-describe">4 ) kubectl describe</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">kubectl describe</code>는 Resource의 주요 Field, 상태와 관련 Event를 사람이 읽기 쉬운 형식으로 출력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod sample-pod
</code></pre></div></div>

<p>Pod를 점검할 때 다음 내용을 확인한다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>확인 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Node</code></td>
      <td>Pod가 배치된 Worker</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Status</code></td>
      <td>Pod Phase</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Containers</code></td>
      <td>Image, State, Ready, Restart Count</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Conditions</code></td>
      <td>Initialized, Ready, PodScheduled 등</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Volumes</code></td>
      <td>Pod에 연결된 Volume</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Events</code></td>
      <td>Scheduling, Image Pull, Mount와 Probe 관련 Event</td>
    </tr>
  </tbody>
</table>

<p>Control Plane은 Worker의 kubelet이 보고한 Pod 상태를 API Server에 저장한다. <code class="language-plaintext highlighter-rouge">describe</code>는 이 상태와 Event를 조회하므로 Worker에서 발생한 문제를 Master의 kubectl로 확인할 수 있다.</p>

<h2 id="5--metrics-server와-kubectl-top">5 ) Metrics Server와 kubectl top</h2>

<hr />

<blockquote>
  <p><strong>Metrics Server</strong></p>

  <p>각 Node의 kubelet에서 CPU와 Memory 사용량을 수집하고 Kubernetes Metrics API로 제공하는 Cluster Add-on이다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">kubectl top</code>으로 Node와 Pod의 Resource 사용량을 조회하려면 Metrics API를 제공하는 Metrics Server가 필요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Worker의 Container Runtime·cAdvisor
              │
              ▼
           kubelet
              │ CPU·Memory Metric
              ▼
        Metrics Server
              │
              ▼
API Server의 metrics.k8s.io API
              │
              ▼
          kubectl top
</code></pre></div></div>

<p>Metrics Server는 장기간 Metric 저장과 상세 Monitoring을 위한 System이 아니다. Autoscaling과 <code class="language-plaintext highlighter-rouge">kubectl top</code>에 필요한 CPU·Memory 중심의 짧은 수명 Metric을 제공한다.</p>

<h3 id="metrics-server-설치">Metrics Server 설치</h3>

<p>Master에서 공식 Release Manifest를 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> <span class="se">\</span>
  https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">latest</code> URL은 실행 시점의 최신 Release를 가리킨다. 재현 가능한 운영 배포에서는 Cluster와 호환되는 Version을 확인하고 Release Version이 포함된 URL로 고정하는 방식이 적합하다.</p>

<p>Metrics Server는 <code class="language-plaintext highlighter-rouge">kube-system</code> Namespace에 배포된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">-n</span> kube-system get deployment metrics-server
kubectl <span class="nt">-n</span> kube-system get pods | <span class="nb">grep </span>metrics-server
</code></pre></div></div>

<p>Metrics API 등록 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get apiservice v1beta1.metrics.k8s.io
</code></pre></div></div>

<h3 id="kubelet-인증서-확인">kubelet 인증서 확인</h3>

<p>일부 학습용 Cluster에서는 kubelet Serving Certificate를 Metrics Server가 검증하지 못해 Metric 수집이 실패할 수 있다. 먼저 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nt">-n</span> kube-system logs deployment/metrics-server
</code></pre></div></div>

<p>인증서 검증 오류가 명확하고 학습용 Cluster에서만 우회가 필요하다면 Deployment를 편집한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl edit deployment metrics-server <span class="nt">-n</span> kube-system
</code></pre></div></div>

<p>Container <code class="language-plaintext highlighter-rouge">args</code>에 다음 Option을 추가할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="s">--kubelet-insecure-tls</span>
</code></pre></div></div>

<p>변경 후 Deployment를 재시작하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout restart deployment/metrics-server <span class="nt">-n</span> kube-system
kubectl rollout status deployment/metrics-server <span class="nt">-n</span> kube-system
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--kubelet-insecure-tls</code>는 kubelet이 제시하는 인증서의 CA 검증을 생략하는 Test용 Option이다. 모든 Local Kubernetes에 필요한 설정이 아니며 운영 환경에서는 올바른 CA와 kubelet Certificate를 구성하여 검증을 유지한다.</p>

<h3 id="resource-사용량-조회">Resource 사용량 조회</h3>

<p>Node의 CPU와 Memory 사용량을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top nodes
</code></pre></div></div>

<p>현재 Namespace의 Pod 사용량을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top pods
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">kube-system</code> Namespace의 Pod를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top pods <span class="nt">-n</span> kube-system
</code></pre></div></div>

<p>Pod 안의 Container별 사용량을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl top pods <span class="nt">-n</span> kube-system <span class="nt">--containers</span>
</code></pre></div></div>

<p>Metrics Server를 설치한 직후에는 첫 Metric이 수집될 때까지 잠시 결과가 나오지 않을 수 있다. Pod와 APIService가 정상인데도 조회되지 않으면 Metrics Server Log와 Worker의 kubelet 연결 상태를 확인한다.</p>

<h2 id="6--container에서-명령-실행">6 ) Container에서 명령 실행</h2>

<hr />

<blockquote>
  <p><strong>kubectl exec</strong></p>

  <p>실행 중인 Pod의 Container 안에서 Command를 실행하도록 API Server를 통해 요청하는 명령이다.</p>
</blockquote>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">sample-pod.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">sample-pod</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-container</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx</span>
</code></pre></div></div>

<p>Manifest를 적용하고 Ready 상태를 기다린다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-pod.yaml
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Ready pod/sample-pod <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>Container의 Root Directory를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> /bin/ls /
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--</code> 앞은 kubectl Option이고 뒤는 Container 안에서 실행할 Command이다.</p>

<h3 id="interactive-shell">Interactive Shell</h3>

<p>Container Shell에 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec</span> <span class="nt">-it</span> pod/sample-pod <span class="nt">--</span> /bin/sh
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>표준 입력을 열린 상태로 유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-t</code></td>
      <td>TTY 할당</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--</code></td>
      <td>kubectl 인자와 Container Command 구분</td>
    </tr>
  </tbody>
</table>

<p>모든 Container Image에 <code class="language-plaintext highlighter-rouge">/bin/bash</code>가 포함되는 것은 아니다. Nginx처럼 작은 Image에서는 <code class="language-plaintext highlighter-rouge">/bin/sh</code>를 먼저 사용하고 실제 Image에 존재하는 Shell을 확인한다.</p>

<h3 id="container-지정">Container 지정</h3>

<p>Pod에 Container가 여러 개 있으면 <code class="language-plaintext highlighter-rouge">-c</code>로 대상을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec</span> <span class="nt">-it</span> pod/sample-pod <span class="se">\</span>
  <span class="nt">-c</span> nginx-container <span class="se">\</span>
  <span class="nt">--</span> /bin/sh
</code></pre></div></div>

<p>Shell을 열지 않고 명령을 실행할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> <span class="se">\</span>
  /bin/sh <span class="nt">-c</span> <span class="s1">'ls -alF / | grep lib'</span>
</code></pre></div></div>

<p>kubectl의 요청은 Control Plane의 API Server를 거쳐 대상 Pod가 실행 중인 Worker의 kubelet로 전달된다.</p>

<h2 id="7--port-forwarding">7 ) Port Forwarding</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">kubectl port-forward</code>는 관리 Client의 Local Port를 Cluster 안의 Pod Port로 연결한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>관리 Client localhost:8080
            │
            │ kubectl port-forward
            ▼
      Pod Container :80
</code></pre></div></div>

<p>Pod의 <code class="language-plaintext highlighter-rouge">80</code> Port를 Master의 <code class="language-plaintext highlighter-rouge">8080</code> Port로 전달한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward pod/sample-pod 8080:80
</code></pre></div></div>

<p>다른 Terminal에서 접속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://127.0.0.1:8080
</code></pre></div></div>

<p>Resource 종류별 예시는 다음과 같다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 특정 Pod</span>
kubectl port-forward pod/sample-pod 8080:80

<span class="c"># sample-deployment가 관리하는 Pod 중 하나</span>
kubectl port-forward deployment/sample-deployment 8080:80

<span class="c"># sample-replicaset이 관리하는 Pod 중 하나</span>
kubectl port-forward replicaset/sample-replicaset 8080:80

<span class="c"># sample-service가 선택한 Pod 중 하나</span>
kubectl port-forward service/sample-service 8080:80
</code></pre></div></div>

<p>Deployment, ReplicaSet과 Service 예시는 Cluster에 해당 이름의 Resource가 존재할 때 실행할 수 있다. Service를 지정할 때 두 번째 Port에는 Service의 <code class="language-plaintext highlighter-rouge">port</code> 값을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>값</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>첫 번째 Port</td>
      <td>kubectl을 실행한 Machine의 Local Port</td>
    </tr>
    <tr>
      <td>두 번째 Port</td>
      <td>대상 Pod 또는 Service의 Port</td>
    </tr>
  </tbody>
</table>

<p>Deployment나 Service를 지정하면 일치하는 Pod 하나가 선택된다. 선택된 Pod가 종료되면 Port Forwarding Session도 끝날 수 있다. 이 기능은 TCP Port에 사용하며 Debugging과 임시 접근에 적합하다.</p>

<h2 id="8--pod-log-확인">8 ) Pod Log 확인</h2>

<hr />

<blockquote>
  <p><strong>kubectl logs</strong></p>

  <p>Pod Container가 표준 출력과 표준 오류로 기록한 Log를 조회하는 명령이다.</p>
</blockquote>

<p>Pod의 기본 Container Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs pod/sample-pod
</code></pre></div></div>

<p>Pod에 Container가 여러 개 있으면 이름을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs pod/sample-pod <span class="nt">-c</span> nginx-container
</code></pre></div></div>

<p>새 Log를 실시간으로 이어서 출력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs <span class="nt">-f</span> pod/sample-pod <span class="nt">-c</span> nginx-container
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-c</code>, <code class="language-plaintext highlighter-rouge">--container</code></td>
      <td>조회할 Container 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-f</code>, <code class="language-plaintext highlighter-rouge">--follow</code></td>
      <td>새 Log를 계속 출력</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">kubectl logs</code>도 API Server를 통해 대상 Worker의 kubelet과 연결된다. Application이 File에만 Log를 기록하면 기본 <code class="language-plaintext highlighter-rouge">kubectl logs</code>로 확인되지 않을 수 있으므로 Container Application은 표준 출력과 표준 오류를 사용하는 방식이 적합하다.</p>

<h2 id="9--ephemeral-container로-pod-debugging">9 ) Ephemeral Container로 Pod Debugging</h2>

<hr />

<blockquote>
  <p><strong>Ephemeral Container</strong></p>

  <p>실행 중인 Pod의 기존 Container를 재시작하지 않고 문제 분석 도구를 추가하기 위한 임시 Container이다. 일반 Application Container와 달리 주로 Troubleshooting에 사용한다.</p>
</blockquote>

<p>Application Image에 Shell이나 <code class="language-plaintext highlighter-rouge">curl</code> 같은 분석 도구가 없으면 <code class="language-plaintext highlighter-rouge">kubectl exec</code>만으로 문제를 확인하기 어렵다. <code class="language-plaintext highlighter-rouge">kubectl debug</code>는 이러한 Pod에 Ephemeral Container를 추가하여 Network와 Process 상태를 점검하게 한다.</p>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">myapp.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">myapp</span>
  <span class="na">labels</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">myapp</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hello-server</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">blux2/hello-server:1.0</span>
      <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">8080</span>
</code></pre></div></div>

<p>Master 또는 kubeconfig가 설정된 관리 Client에서 Pod를 생성하고 Ready 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> myapp.yaml
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Ready pod/myapp <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">curl</code>이 포함된 Image를 Ephemeral Container로 추가하고 같은 Pod의 <code class="language-plaintext highlighter-rouge">localhost:8080</code>에 접근한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl debug <span class="nt">--stdin</span> <span class="nt">--tty</span> pod/myapp <span class="se">\</span>
  <span class="nt">--image</span><span class="o">=</span>curlimages/curl:8.4.0 <span class="se">\</span>
  <span class="nt">--target</span><span class="o">=</span>hello-server <span class="se">\</span>
  <span class="nt">--</span> sh
</code></pre></div></div>

<p>Debug Container의 Shell에서 Web Server 응답을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://127.0.0.1:8080
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--stdin</code>, <code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>Debug Container의 표준 입력 유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--tty</code>, <code class="language-plaintext highlighter-rouge">-t</code></td>
      <td>Interactive Terminal 할당</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--image</code></td>
      <td>분석 도구가 포함된 Container Image 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--target</code></td>
      <td>Process Namespace 확인 대상으로 삼을 기존 Container 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--</code></td>
      <td>kubectl 인자와 Debug Container에서 실행할 Command 구분</td>
    </tr>
  </tbody>
</table>

<p>Debug Image는 목적에 맞는 Command와 도구를 포함해야 한다. Web 요청 확인에는 <code class="language-plaintext highlighter-rouge">curl</code> Image가 적합하고, Process나 Network 진단에는 해당 명령이 포함된 Image가 필요하다. <code class="language-plaintext highlighter-rouge">--target</code>을 통한 Process 확인 범위는 Worker의 Container Runtime 지원 여부에 따라 달라질 수 있다.</p>

<p>요청이 처리되는 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Master·관리 Client의 kubectl debug
              │
              ▼
          API Server
              │ Pod의 Ephemeral Container 정보 전달
              ▼
대상 Worker의 kubelet ──▶ Container Runtime
                              │
                              ▼
                 기존 Pod 안에 Debug Container 실행
</code></pre></div></div>

<p>추가된 Ephemeral Container는 다음 명령으로 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod myapp
</code></pre></div></div>

<p>Ephemeral Container는 실행 중인 Pod에서 제거하거나 다시 변경하는 일반 Container가 아니다. Debugging이 끝난 뒤 실습 Pod를 삭제하면 함께 정리된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod myapp
</code></pre></div></div>

<h2 id="10--container와-file-복사">10 ) Container와 File 복사</h2>

<hr />

<blockquote>
  <p><strong>kubectl cp</strong></p>

  <p>관리 Client의 File이나 Directory를 Pod의 Container로 복사하거나 Container의 File을 관리 Client로 가져오는 명령이다.</p>
</blockquote>

<p>기본 형식은 Source와 Target 중 어느 쪽에 <code class="language-plaintext highlighter-rouge">pod-name:path</code>가 포함되는지에 따라 복사 방향이 결정된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">cp</span> &lt;<span class="nb">source</span><span class="o">&gt;</span> &lt;target&gt;
</code></pre></div></div>

<p>26번 문서 앞에서 작성한 <code class="language-plaintext highlighter-rouge">sample-pod.yaml</code>을 적용하고 Pod 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> sample-pod.yaml
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>Ready pod/sample-pod <span class="nt">--timeout</span><span class="o">=</span>60s
</code></pre></div></div>

<p>Container의 <code class="language-plaintext highlighter-rouge">/etc/hostname</code>을 현재 Directory의 <code class="language-plaintext highlighter-rouge">hostname</code> File로 복사한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">cp </span>sample-pod:/etc/hostname ./hostname
</code></pre></div></div>

<p>복사한 File을 다시 Container의 <code class="language-plaintext highlighter-rouge">/tmp/newfile</code>로 전송한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">cp</span> ./hostname sample-pod:/tmp/newfile
</code></pre></div></div>

<p>Container 안에 File이 생성되었는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> <span class="nb">ls</span> <span class="nt">-l</span> /tmp/newfile
kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> <span class="nb">cat</span> /tmp/newfile
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>형식·Option</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">pod-name:/path</code></td>
      <td>현재 Namespace에 있는 Pod의 경로 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">namespace/pod-name:/path</code></td>
      <td>특정 Namespace의 Pod 경로 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-c &lt;container&gt;</code></td>
      <td>다중 Container Pod에서 복사 대상 지정</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">kubectl cp</code>는 복사 과정에서 Container 내부의 <code class="language-plaintext highlighter-rouge">tar</code> 명령을 사용한다. Container Image에 <code class="language-plaintext highlighter-rouge">tar</code>가 없으면 명령이 실패하므로 먼저 존재 여부를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>pod/sample-pod <span class="nt">--</span> /bin/sh <span class="nt">-c</span> <span class="s1">'command -v tar'</span>
</code></pre></div></div>

<p>Symbolic Link, Wildcard 또는 세부 권한을 다루는 복잡한 복사는 <code class="language-plaintext highlighter-rouge">kubectl exec</code>와 <code class="language-plaintext highlighter-rouge">tar</code>를 직접 조합하는 방법을 검토한다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubectl get</code>은 Resource 종류, 이름, Namespace와 Label Selector를 조합하여 API Server의 상태를 조회한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">all</code>은 모든 Kubernetes Resource를 의미하지 않으며 필요한 종류를 명시해서 조회해야 한다.</p>
    </li>
    <li>
      <p>Custom Columns와 JSONPath를 사용하면 Script에 필요한 Field만 추출할 수 있다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">describe</code>는 Pod의 Worker 배치, Container 상태, Condition과 Event를 함께 확인할 때 사용한다.</p>
    </li>
    <li>
      <p>Metrics Server는 Worker kubelet의 CPU·Memory Metric을 Metrics API로 제공하며 <code class="language-plaintext highlighter-rouge">kubectl top</code>이 이를 조회한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">exec</code>, <code class="language-plaintext highlighter-rouge">port-forward</code>와 <code class="language-plaintext highlighter-rouge">logs</code> 요청은 API Server를 거쳐 대상 Pod가 실행 중인 Worker로 전달된다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubectl debug</code>는 기존 Pod에 분석 도구가 없을 때 Ephemeral Container를 추가하여 문제를 확인한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">kubectl cp</code>는 관리 Client와 Container 사이에서 File을 복사하며 Container Image에 <code class="language-plaintext highlighter-rouge">tar</code>가 필요하다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">--kubelet-insecure-tls</code>는 인증서 검증을 생략하므로 학습용 문제 확인에만 제한적으로 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[kubectl get 출력 형식과 describe, Metrics Server 기반 Resource 사용량, exec·debug·cp·port-forward·logs를 이용한 Pod 확인 방법 정리]]></summary></entry><entry><title type="html">Kubernetes 개요와 Architecture</title><link href="https://kkr010128.github.io/cloud-native-20-kubernetes-overview/" rel="alternate" type="text/html" title="Kubernetes 개요와 Architecture" /><published>2026-08-25T00:00:00+09:00</published><updated>2026-08-25T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-20-kubernetes-overview</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-20-kubernetes-overview/"><![CDATA[<p>Kubernetes는 여러 Node에서 Container Application을 배포하고, 확장하며, 선언한 상태로 유지하는 Container Orchestration Platform이다. Kubernetes를 이해하려면 먼저 Container Runtime이 Application을 실행하는 범위와 Orchestrator가 Cluster를 관리하는 범위를 구분해야 한다.</p>

<h2 id="1--container-runtime">1 ) Container Runtime</h2>

<hr />

<p>일반적인 Virtual Machine은 Hardware Resource를 가상화하고 각 Virtual Machine에 Guest OS를 실행한다. Container는 Host OS의 Kernel을 공유하면서 Process, File System과 Network를 격리한다. Guest OS 전체를 실행하지 않으므로 상대적으로 적은 Resource로 더 많은 Application을 실행할 수 있다.</p>

<p>Runtime은 Program이 실행되는 환경을 의미한다. JavaScript가 Browser에서 실행된다면 Browser가 Runtime 역할을 한다. 같은 관점에서 <strong>Container Runtime은 Container Image를 가져오고 Container를 생성·실행·감독하는 Software</strong>이다.</p>

<h3 id="containerd">containerd</h3>

<p>containerd는 Container 실행과 Image 관리를 담당하는 Open Source Container Runtime이다.</p>

<ul>
  <li>
    <p>Registry에서 Container Image를 내려받는다.</p>
  </li>
  <li>
    <p>Image Layer의 압축을 풀고 Local Storage에 관리한다.</p>
  </li>
  <li>
    <p>Container를 생성·시작·중지·삭제한다.</p>
  </li>
  <li>
    <p>실행 중인 Container의 상태를 감독한다.</p>
  </li>
</ul>

<p>Docker Engine은 내부적으로 containerd를 사용하며 Kubernetes도 CRI Plugin을 갖춘 containerd를 Runtime으로 사용할 수 있다.</p>

<h3 id="docker-engine">Docker Engine</h3>

<p>Docker Engine은 Image Build, Registry 전송, Network, Volume과 Container Lifecycle을 포함한 개발·운영 기능을 제공한다. containerd보다 넓은 범위를 담당하며 내부의 Container 실행 계층에서 containerd를 사용한다.</p>

<p>Kubernetes <code class="language-plaintext highlighter-rouge">v1.20</code>에서 Docker Engine 연동을 담당하던 Dockershim이 Deprecated되었고 <code class="language-plaintext highlighter-rouge">v1.24</code>에서 제거되었다. 이는 Docker로 만든 Image를 Kubernetes에서 사용할 수 없다는 의미는 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Build
    │
    ▼
OCI 호환 Container Image
    │
    ├── containerd
    ├── CRI-O
    └── Docker Engine + cri-dockerd
</code></pre></div></div>

<p>Docker로 Build한 OCI 호환 Image를 Registry에 Push하면 containerd와 CRI-O를 사용하는 Kubernetes Cluster에서도 실행할 수 있다. Docker Engine을 Kubernetes Runtime으로 연결해야 한다면 외부 CRI Adapter인 <code class="language-plaintext highlighter-rouge">cri-dockerd</code>가 필요하다.</p>

<h3 id="cri-o">CRI-O</h3>

<p>CRI-O는 Kubernetes의 CRI(Container Runtime Interface)를 구현하기 위해 만들어진 Open Source Runtime이다. Kubernetes에서 OCI 호환 Container를 실행하는 데 필요한 범위에 집중한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Docker Engine</th>
      <th>containerd</th>
      <th>CRI-O</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주요 범위</td>
      <td>Image Build·배포·Container 관리</td>
      <td>Image 관리·Container 실행</td>
      <td>Kubernetes용 Container 실행</td>
    </tr>
    <tr>
      <td>Kubernetes 연결</td>
      <td><code class="language-plaintext highlighter-rouge">cri-dockerd</code> 필요</td>
      <td>CRI Plugin 사용</td>
      <td>CRI 직접 구현</td>
    </tr>
    <tr>
      <td>Image Build</td>
      <td>지원</td>
      <td>기본 역할 아님</td>
      <td>기본 역할 아님</td>
    </tr>
    <tr>
      <td>주요 용도</td>
      <td>개발과 Container Platform 운영</td>
      <td>Docker와 Kubernetes의 Runtime</td>
      <td>Kubernetes Runtime</td>
    </tr>
  </tbody>
</table>

<h2 id="2--container-orchestration">2 ) Container Orchestration</h2>

<hr />

<p>Container 수가 많아지고 여러 Machine에 분산되면 개별 명령으로 상태를 관리하기 어렵다. Container Orchestration은 여러 Container를 배치하고 연결하며, 상태를 추적하고, 장애가 발생했을 때 원하는 상태로 복구하는 역할을 수행한다.</p>

<p>주요 기능은 다음과 같다.</p>

<ul>
  <li>
    <p>여러 Node에 Workload를 배치한다.</p>
  </li>
  <li>
    <p>Container 장애를 감지하고 대체 실행 단위를 생성한다.</p>
  </li>
  <li>
    <p>Replica 수를 조정하여 Application을 확장하거나 축소한다.</p>
  </li>
  <li>
    <p>Service Discovery와 Traffic Routing을 제공한다.</p>
  </li>
  <li>
    <p>설정, Secret과 Persistent Storage를 Application에 연결한다.</p>
  </li>
  <li>
    <p>선언한 상태와 실제 상태가 일치하도록 지속적으로 조정한다.</p>
  </li>
</ul>

<h3 id="orchestration-도구">Orchestration 도구</h3>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker Swarm</td>
      <td>Docker Engine에 내장되어 구성과 사용이 비교적 단순</td>
    </tr>
    <tr>
      <td>Apache Mesos</td>
      <td>분산 System Resource를 관리하며 Framework와 결합하여 사용</td>
    </tr>
    <tr>
      <td>Nomad</td>
      <td>HashiCorp가 개발한 Orchestrator로 Consul, Vault와 연동 가능</td>
    </tr>
    <tr>
      <td>Kubernetes</td>
      <td>Container Workload 배포·확장·상태 관리를 위한 CNCF Platform</td>
    </tr>
  </tbody>
</table>

<p>Docker Swarm은 작은 Cluster나 Docker 중심 환경에서 비교적 쉽게 시작할 수 있다. Mesos는 분산 Resource 관리 Framework와 다른 System을 함께 구성해야 하며, Nomad는 Consul의 Service Discovery 및 Vault의 Secret 관리와 결합할 수 있다. Kubernetes는 폭넓은 Cloud·Network·Storage 생태계를 갖추고 있다.</p>

<h2 id="3--kubernetes">3 ) Kubernetes</h2>

<hr />

<p>Kubernetes는 <code class="language-plaintext highlighter-rouge">K8s</code>라고도 표기한다. <code class="language-plaintext highlighter-rouge">K</code>와 <code class="language-plaintext highlighter-rouge">s</code> 사이의 여덟 글자 <code class="language-plaintext highlighter-rouge">ubernete</code>를 숫자 <code class="language-plaintext highlighter-rouge">8</code>로 줄인 표현이다.</p>

<p>Kubernetes에 배포할 Application은 Container 환경에서 독립적으로 실행되고, 외부 설정을 주입받으며, Replica가 교체되어도 Service를 유지할 수 있도록 설계해야 장점을 충분히 활용할 수 있다.</p>

<p>Kubernetes Cluster는 한 대의 Machine으로도 구성할 수 있지만 실제 운영 환경에서는 일반적으로 여러 Node를 사용한다. 각 Node에서 여러 Pod가 실행되며 Kubernetes는 YAML Manifest에 선언한 상태를 기준으로 Pod와 관련 Resource를 생성하고 관리한다.</p>

<h3 id="kubernetes를-사용하는-이유">Kubernetes를 사용하는 이유</h3>

<h4 id="자동화된-container-orchestration">자동화된 Container Orchestration</h4>

<p>Scheduler가 Resource와 조건을 확인하여 Pod를 Node에 배치하고 Controller가 선언한 Replica 수를 유지한다.</p>

<h4 id="service-지속성과-self-healing">Service 지속성과 Self-healing</h4>

<p>Pod나 Container에 장애가 발생하면 Controller와 kubelet이 필요한 복구 작업을 수행한다. Node가 사용할 수 없는 상태가 되면 Controller가 다른 가용 Node에 대체 Pod를 생성할 수 있다.</p>

<p>대체 Pod가 생성되는 것이 기존 Process나 Local Data가 그대로 이동한다는 의미는 아니다. Stateful Application은 Persistent Volume과 Application 수준의 복구 전략이 필요하다.</p>

<h4 id="효율적인-resource-사용">효율적인 Resource 사용</h4>

<p>Container별 CPU와 Memory Request·Limit을 지정할 수 있으며 Scheduler가 가용 Resource를 고려하여 Pod를 배치한다. Service는 변하는 Pod 집합에 안정적인 Endpoint를 제공하고 Ingress 또는 Gateway API는 HTTP·HTTPS Traffic Routing을 구성한다.</p>

<h4 id="유연한-확장">유연한 확장</h4>

<p>Replica 수를 직접 조정하거나 HorizontalPodAutoscaler를 사용하여 Metric에 따라 Workload를 확장·축소할 수 있다.</p>

<h4 id="desired-state-유지">Desired State 유지</h4>

<p>사용자는 YAML Manifest로 원하는 상태를 선언한다. Controller는 실제 Cluster 상태를 관찰하고 필요한 Resource를 생성·수정·삭제하여 두 상태를 일치시킨다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 선언한 상태
Deployment Replica: 3
          │
          ▼
Controller가 실제 상태 관찰
          │
          ├── Pod 2개 → Pod 1개 추가
          └── Pod 4개 → Pod 1개 제거
</code></pre></div></div>

<h4 id="infrastructure-선택의-유연성">Infrastructure 선택의 유연성</h4>

<p>Kubernetes는 On-premises, Virtual Machine과 여러 Public Cloud에서 사용할 수 있다. 다만 Kubernetes를 사용한다고 Application과 Infrastructure가 자동으로 모든 Cloud Provider에서 동일하게 동작하는 것은 아니다. Load Balancer, Storage와 IAM 같은 Provider별 연동 요소를 함께 고려해야 한다.</p>

<h2 id="4--kubernetes-resource">4 ) Kubernetes Resource</h2>

<hr />

<p>Kubernetes에서 상태를 관리하는 대상을 Resource 또는 Object라고 한다. Resource는 API Group과 역할을 기준으로 구분할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>주요 Resource</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Workload</td>
      <td>Pod, ReplicaSet, Deployment, DaemonSet, StatefulSet, Job, CronJob</td>
      <td>Container 실행과 Lifecycle 관리</td>
    </tr>
    <tr>
      <td>Service와 Network</td>
      <td>Service, EndpointSlice, Ingress, NetworkPolicy</td>
      <td>Service Discovery, Traffic 공개와 Network 정책</td>
    </tr>
    <tr>
      <td>Config와 Storage</td>
      <td>ConfigMap, Secret, PersistentVolumeClaim, PersistentVolume, StorageClass</td>
      <td>설정·기밀 정보와 영속 Storage 관리</td>
    </tr>
    <tr>
      <td>Cluster와 접근 제어</td>
      <td>Node, Namespace, ServiceAccount, Role, ClusterRole, RoleBinding, ClusterRoleBinding</td>
      <td>Cluster 구성과 RBAC 관리</td>
    </tr>
    <tr>
      <td>정책과 확장</td>
      <td>ResourceQuota, LimitRange, HorizontalPodAutoscaler, PodDisruptionBudget, CustomResourceDefinition</td>
      <td>Resource 제한·확장·가용성·API 확장</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">ReplicationController</code>도 지원되지만 현재는 일반적으로 ReplicaSet과 Deployment를 사용한다.</p>

<p><code class="language-plaintext highlighter-rouge">ClusterIP</code>, <code class="language-plaintext highlighter-rouge">NodePort</code>, <code class="language-plaintext highlighter-rouge">LoadBalancer</code>, <code class="language-plaintext highlighter-rouge">ExternalName</code>은 각각 별도 Resource가 아니라 Service의 <code class="language-plaintext highlighter-rouge">type</code> 값이다. External IP, Headless Service와 Selector가 없는 Service도 Service를 구성하는 방식이다. Ingress는 Service Type이 아니라 HTTP·HTTPS Routing 규칙을 표현하는 별도 Resource이다.</p>

<h2 id="5--kubernetes-architecture">5 ) Kubernetes Architecture</h2>

<hr />

<p>Kubernetes Cluster는 Control Plane과 Worker Node로 구성된다. 과거에 Master Node라고 부르던 역할은 현재 문서에서 <strong>Control Plane Node</strong>라고 표현한다. 이 글에서는 처음 역할을 대응할 때 <code class="language-plaintext highlighter-rouge">Master(Control Plane)</code>로 함께 표기하고 이후에는 Control Plane을 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes Cluster
├── Control Plane Node
│   ├── kube-apiserver
│   ├── etcd
│   ├── kube-scheduler
│   └── kube-controller-manager
│
├── Worker Node 1
│   ├── kubelet
│   ├── Container Runtime
│   ├── Network 구성 요소
│   └── Pod
│
└── Worker Node 2
    ├── kubelet
    ├── Container Runtime
    ├── Network 구성 요소
    └── Pod
</code></pre></div></div>

<h3 id="cluster와-node">Cluster와 Node</h3>

<p>Cluster는 Kubernetes Resource를 함께 관리하는 집합이다. Node는 Pod가 배치되는 Machine이며 물리 Server나 Virtual Machine으로 구성할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>관점</th>
      <th>Control Plane Node</th>
      <th>Worker Node</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주요 책임</td>
      <td>Cluster 상태와 Scheduling 관리</td>
      <td>할당된 Pod 실행</td>
    </tr>
    <tr>
      <td>주요 구성 요소</td>
      <td>API Server, etcd, Scheduler, Controller Manager</td>
      <td>kubelet, Container Runtime, Network 구성 요소</td>
    </tr>
    <tr>
      <td>Application Pod</td>
      <td>기본 kubeadm 구성에서는 일반 Workload Scheduling 제한</td>
      <td>일반적으로 Application Pod 실행</td>
    </tr>
    <tr>
      <td>장애 영향</td>
      <td>API와 Cluster 관리 기능에 영향</td>
      <td>해당 Node의 Workload에 영향</td>
    </tr>
  </tbody>
</table>

<p>운영 Cluster에서는 Control Plane을 여러 대로 구성하여 관리 기능의 고가용성을 확보할 수 있다. 학습 환경에서는 Control Plane 한 대와 Worker 두 대로 기본 구조를 확인할 수 있다.</p>

<h3 id="pod">Pod</h3>

<p>Pod는 Kubernetes에서 생성하고 Scheduling할 수 있는 가장 작은 배포 단위이다. 하나 이상의 Container를 포함하며 같은 Pod의 Container는 다음 Resource를 공유한다.</p>

<ul>
  <li>
    <p>Pod IP Address와 Network Namespace</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">localhost</code> Network</p>
  </li>
  <li>
    <p>연결한 Volume</p>
  </li>
  <li>
    <p>Pod Lifecycle</p>
  </li>
</ul>

<p>같은 Pod에는 Main Application과 그 Application의 동작을 직접 보조하는 Sidecar처럼 <strong>생명주기와 Resource를 밀접하게 공유해야 하는 Container</strong>를 배치한다. Web Application과 Database처럼 독립적으로 확장·배포·복구해야 하는 구성 요소는 일반적으로 서로 다른 Pod와 Workload로 분리한다.</p>

<h3 id="persistent-storage">Persistent Storage</h3>

<p>Pod는 교체될 수 있는 Resource이므로 Container Writable Layer에만 저장한 Data는 Pod와 함께 사라질 수 있다. 중요한 Data는 PersistentVolume과 PersistentVolumeClaim을 통해 Pod 외부의 Storage에 저장한다.</p>

<p>CSI(Container Storage Interface)는 Kubernetes가 다양한 Storage System과 연결되는 표준 Interface이다. 실제 Storage는 Local Disk, NFS, Distributed Storage 또는 Cloud Provider의 Block·File Storage가 될 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Container Runtime은 Image와 Container 실행을 담당하고 Kubernetes는 여러 Node의 Workload를 Orchestration한다.</p>
    </li>
    <li>
      <p>Dockershim은 제거되었지만 Docker로 Build한 OCI Image는 containerd와 CRI-O 환경에서도 사용할 수 있다.</p>
    </li>
    <li>
      <p>Kubernetes는 Desired State, Self-healing, Scheduling, Service Discovery와 Scaling을 제공한다.</p>
    </li>
    <li>
      <p>Control Plane은 Cluster 상태와 배치를 관리하고 Worker Node는 실제 Application Pod를 실행한다.</p>
    </li>
    <li>
      <p>Pod에는 독립된 Application을 무조건 함께 넣는 것이 아니라 생명주기와 Resource를 공유해야 하는 Container를 배치한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Kubernetes" /><summary type="html"><![CDATA[Container Runtime과 Orchestration의 개념부터 Kubernetes Resource, Pod, Control Plane과 Worker Node 구조까지 정리]]></summary></entry></feed>