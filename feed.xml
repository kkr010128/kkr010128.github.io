<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://kkr010128.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://kkr010128.github.io/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-08-20T21:56:26+09:00</updated><id>https://kkr010128.github.io/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">애플리케이션별 Docker Multi-stage Build</title><link href="https://kkr010128.github.io/cloud-native-15-multi-stage-build/" rel="alternate" type="text/html" title="애플리케이션별 Docker Multi-stage Build" /><published>2026-08-20T00:00:00+09:00</published><updated>2026-08-20T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-15-multi-stage-build</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-15-multi-stage-build/"><![CDATA[<h2 id="1--애플리케이션과-multi-stage-build">1 ) 애플리케이션과 Multi-stage Build</h2>

<hr />

<p>애플리케이션의 언어와 Framework가 달라도 Multi-stage Build의 기본 흐름은 같다.</p>

<ol>
  <li>
    <p>Builder Stage에서 의존성을 설치한다.</p>
  </li>
  <li>
    <p>Source Code를 Build하거나 실행에 필요한 환경을 준비한다.</p>
  </li>
  <li>
    <p>Runtime Stage를 별도의 Base Image로 시작한다.</p>
  </li>
  <li>
    <p>Builder Stage에서 만든 산출물만 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p>최종 Image에서 Application을 실행한다.</p>
  </li>
</ol>

<p>언어별 차이는 최종 산출물과 실행 환경에 있다.</p>

<table>
  <thead>
    <tr>
      <th>애플리케이션</th>
      <th>Builder Stage의 작업</th>
      <th>Runtime Stage로 전달하는 대상</th>
      <th>Runtime</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Node.js</td>
      <td>Package 설치</td>
      <td>Source Code와 설치된 Package</td>
      <td>Node.js</td>
    </tr>
    <tr>
      <td>Spring Boot</td>
      <td>Gradle Build</td>
      <td>JAR</td>
      <td>JRE</td>
    </tr>
    <tr>
      <td>Go</td>
      <td>Source Code Compile</td>
      <td>실행 Binary</td>
      <td>Alpine Linux</td>
    </tr>
    <tr>
      <td>Python</td>
      <td>가상 환경에 Package 설치</td>
      <td>Python 가상 환경</td>
      <td>Python</td>
    </tr>
    <tr>
      <td>React</td>
      <td>정적 File Build</td>
      <td><code class="language-plaintext highlighter-rouge">dist</code> Directory</td>
      <td>Nginx</td>
    </tr>
  </tbody>
</table>

<p>Spring Boot, Go, React는 Build 산출물이 명확하므로 Builder의 Build 도구를 최종 Image에서 제외하기 쉽다. Node.js와 Python은 Source Code를 Runtime에서 실행하므로 의존성 설치 결과를 별도 Stage로 전달한다.</p>

<h2 id="2--nodejs-multi-stage-build">2 ) Node.js Multi-stage Build</h2>

<hr />

<p>Node.js 예제는 Builder Stage에서 Package를 설치하고 Runtime Stage에서는 더 작은 Alpine 기반 Node.js Image로 Application을 실행한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">node:20-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">FROM</span><span class="s"> node:20-alpine</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /app ./</span>

<span class="k">EXPOSE</span><span class="s"> 3000</span>

<span class="k">USER</span><span class="s"> node</span>

<span class="k">CMD</span><span class="s"> ["node", "app.js"]</span>
</code></pre></div></div>

<p>Build 과정은 다음과 같다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node:20-slim</code>에서 <code class="language-plaintext highlighter-rouge">package.json</code>을 먼저 복사하고 Package를 설치한다.</p>
  </li>
  <li>
    <p>Source Code를 <code class="language-plaintext highlighter-rouge">/app</code>에 복사한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node:20-alpine</code>으로 Runtime Stage를 시작한다.</p>
  </li>
  <li>
    <p>Builder의 <code class="language-plaintext highlighter-rouge">/app</code>을 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">node</code> 사용자로 <code class="language-plaintext highlighter-rouge">app.js</code>를 실행한다.</p>
  </li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> nodeapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p>이 예제는 Builder의 Application Directory 전체를 복사하므로 Source Code와 <code class="language-plaintext highlighter-rouge">node_modules</code>가 모두 Runtime Stage로 전달된다. 두 Stage를 분리하는 흐름과 더 작은 Runtime Base Image를 사용하는 방법을 보여주는 예제이다.</p>

<h2 id="3--spring-boot-gradle-multi-stage-build">3 ) Spring Boot Gradle Multi-stage Build</h2>

<hr />

<p>Spring Boot는 JDK와 Gradle이 필요한 Build 환경과 JAR 실행에 필요한 JRE 환경을 분리한다.</p>

<h3 id="프로젝트-준비">프로젝트 준비</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/itggangpae/spring_docker.git
<span class="nb">cd </span>spring_docker
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">eclipse-temurin:21-jdk</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> gradlew ./</span>
<span class="k">COPY</span><span class="s"> gradle ./gradle</span>
<span class="k">RUN </span><span class="nb">chmod</span> +x gradlew

<span class="k">COPY</span><span class="s"> build.gradle ./</span>
<span class="k">COPY</span><span class="s"> settings.gradle ./</span>
<span class="k">RUN </span>./gradlew dependencies <span class="nt">--no-daemon</span>

<span class="k">COPY</span><span class="s"> src ./src</span>
<span class="k">RUN </span>./gradlew clean build <span class="nt">-x</span> <span class="nb">test</span> <span class="nt">--no-daemon</span>

<span class="k">FROM</span><span class="s"> eclipse-temurin:21-jre</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/build/libs/*.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>이전 실습에서는 Host에서 Gradle Build를 수행하고 생성된 JAR를 Docker Image에 복사했다. Multi-stage Build에서는 다음 과정이 하나의 Dockerfile 안에서 이루어진다.</p>

<ol>
  <li>
    <p>JDK Image에서 Gradle Wrapper로 JAR를 Build한다.</p>
  </li>
  <li>
    <p>새로운 JRE Image를 Runtime Stage로 사용한다.</p>
  </li>
  <li>
    <p>Builder에서 생성한 JAR만 Runtime Stage로 복사한다.</p>
  </li>
  <li>
    <p>최종 Image에는 Gradle, Source Code와 JDK가 남지 않는다.</p>
  </li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> spring-docker:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">build.gradle</code>과 <code class="language-plaintext highlighter-rouge">settings.gradle</code>을 Source Code보다 먼저 복사하여 의존성 Layer를 분리했다. 의존성 설정이 바뀌지 않았다면 Source Code 변경 시 기존 의존성 Cache를 활용할 수 있다.</p>

<h2 id="4--go-multi-stage-build">4 ) Go Multi-stage Build</h2>

<hr />

<p>Go는 Builder Stage에서 Source Code를 실행 Binary로 Compile하고 최종 Image에는 Binary만 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">golang:1.23-alpine</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> go.mod go.sum* ./</span>
<span class="k">RUN </span>go mod download

<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">RUN </span><span class="nv">CGO_ENABLED</span><span class="o">=</span>0 <span class="nv">GOOS</span><span class="o">=</span>linux go build <span class="nt">-o</span> main .

<span class="k">FROM</span><span class="s"> alpine:latest</span>

<span class="k">RUN </span>apk <span class="nt">--no-cache</span> add ca-certificates

<span class="k">WORKDIR</span><span class="s"> /root</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/main ./main</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["./main"]</span>
</code></pre></div></div>

<p>각 Stage의 역할은 다음과 같다.</p>

<ul>
  <li>
    <p>Builder Stage는 Go Compiler와 Module 의존성을 사용한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">CGO_ENABLED=0</code>으로 C Library에 대한 동적 의존성을 사용하지 않는 Binary를 Build한다.</p>
  </li>
  <li>
    <p>Runtime Stage는 <code class="language-plaintext highlighter-rouge">alpine:latest</code>를 사용하고 HTTPS 인증에 필요한 CA 인증서를 설치한다.</p>
  </li>
  <li>
    <p>최종 Image에는 Go Compiler와 Source Code 대신 <code class="language-plaintext highlighter-rouge">main</code> Binary만 포함한다.</p>
  </li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> goapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<h2 id="5--python-multi-stage-build">5 ) Python Multi-stage Build</h2>

<hr />

<p>Python은 Compile된 단일 실행 File보다 Source Code와 Package가 설치된 환경이 필요하다. Builder Stage에서 가상 환경을 만들고 Runtime Stage로 해당 가상 환경을 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">python:3.12-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">ENV</span><span class="s"> PYTHONDONTWRITEBYTECODE=1 \</span>
    PYTHONUNBUFFERED=1

<span class="k">RUN </span>python <span class="nt">-m</span> venv /opt/venv
<span class="k">ENV</span><span class="s"> PATH="/opt/venv/bin:$PATH"</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">FROM</span><span class="s"> python:3.12-slim</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> --from=builder /opt/venv /opt/venv</span>

<span class="k">ENV</span><span class="s"> PATH="/opt/venv/bin:$PATH" \</span>
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">RUN </span>adduser <span class="nt">--disabled-password</span> <span class="nt">--gecos</span> <span class="s2">""</span> appuser
<span class="k">USER</span><span class="s"> appuser</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]</span>
</code></pre></div></div>

<p>주요 설정은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PYTHONDONTWRITEBYTECODE=1</code></td>
      <td><code class="language-plaintext highlighter-rouge">.pyc</code> File 생성 방지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PYTHONUNBUFFERED=1</code></td>
      <td>표준 출력과 오류의 Buffering 비활성화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/opt/venv</code></td>
      <td>Builder와 Runtime에서 공통으로 사용하는 가상 환경 경로</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">appuser</code></td>
      <td>Application을 실행할 일반 사용자</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> fastapiapp:2.0 <span class="nb">.</span>
</code></pre></div></div>

<p>강의에서는 처음에 일반 <code class="language-plaintext highlighter-rouge">python</code> Image를 사용한 뒤 <code class="language-plaintext highlighter-rouge">python:3.12-slim</code>으로 변경했다. Python은 Compiler를 완전히 제거하는 방식보다 작은 Base Image를 선택하고 가상 환경만 복사하는 방식으로 최종 Image를 줄인다.</p>

<h2 id="6--react-정적-file-multi-stage-build">6 ) React 정적 File Multi-stage Build</h2>

<hr />

<p>React Source Code는 Browser가 직접 실행할 수 있는 정적 HTML, JavaScript와 CSS File로 Build한다. Docker 환경에서는 Nginx나 Apache로 정적 File을 제공할 수 있고, Cloud 환경에서는 Web Endpoint를 제공하는 Object Storage에 배포할 수도 있다.</p>

<h3 id="프로젝트-생성과-확인">프로젝트 생성과 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm create vite@latest my-react-app <span class="nt">--</span> <span class="nt">--template</span> react
<span class="nb">cd </span>my-react-app
npm <span class="nb">install
</span>npm run dev <span class="nt">--</span> <span class="nt">--host</span> 0.0.0.0
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:5173
</code></pre></div></div>

<h3 id="dockerfile-1">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">node:20-alpine</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">RUN </span>npm run build

<span class="k">FROM</span><span class="s"> nginx:stable-alpine</span>

<span class="k">COPY</span><span class="s"> --from=builder /app/dist /usr/share/nginx/html</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["nginx", "-g", "daemon off;"]</span>
</code></pre></div></div>

<p>Builder Stage에서는 Vite가 React Source Code를 <code class="language-plaintext highlighter-rouge">/app/dist</code>의 정적 File로 변환한다. Runtime Stage에는 Node.js, npm, Source Code를 복사하지 않고 <code class="language-plaintext highlighter-rouge">dist</code>만 Nginx의 기본 Document Root에 복사한다.</p>

<h3 id="dockerignore"><code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node_modules/
dist/
.git/
.DS_Store
</code></pre></div></div>

<h3 id="image-build와-container-실행">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> my-react-app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> my-react-app <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  my-react-app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="7--언어별-차이-비교">7 ) 언어별 차이 비교</h2>

<hr />

<p>반복된 실습의 핵심은 Dockerfile의 형태가 아니라 Builder에서 Runtime으로 무엇을 전달하는지에 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Node.js</th>
      <th>Spring Boot</th>
      <th>Go</th>
      <th>Python</th>
      <th>React</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>실행 형태</td>
      <td>Source Code</td>
      <td>JAR</td>
      <td>Native Binary</td>
      <td>Source Code</td>
      <td>정적 File</td>
    </tr>
    <tr>
      <td>Build 도구</td>
      <td>npm</td>
      <td>Gradle·JDK</td>
      <td>Go Compiler</td>
      <td>pip·가상 환경</td>
      <td>npm·Vite</td>
    </tr>
    <tr>
      <td>Runtime</td>
      <td>Node.js</td>
      <td>JRE</td>
      <td>Alpine</td>
      <td>Python</td>
      <td>Nginx</td>
    </tr>
    <tr>
      <td>전달 대상</td>
      <td>Application Directory</td>
      <td><code class="language-plaintext highlighter-rouge">app.jar</code></td>
      <td><code class="language-plaintext highlighter-rouge">main</code></td>
      <td><code class="language-plaintext highlighter-rouge">/opt/venv</code></td>
      <td><code class="language-plaintext highlighter-rouge">/app/dist</code></td>
    </tr>
    <tr>
      <td>주요 절감 대상</td>
      <td>Builder Base Image</td>
      <td>JDK·Gradle·Source</td>
      <td>Compiler·Source</td>
      <td>Build Cache</td>
      <td>Node.js·Source</td>
    </tr>
  </tbody>
</table>

<p>Multi-stage Build가 모든 언어에서 동일한 크기 절감 효과를 만드는 것은 아니다. Compile 결과만 전달할 수 있는 Spring Boot, Go, React에서는 Build 환경과 Runtime 환경의 구분이 명확하다. Node.js와 Python은 Runtime에도 언어 실행 환경과 Package가 필요하므로 필요한 의존성을 선별하고 작은 Base Image를 사용하는 것이 중요하다.</p>

<h2 id="8--종합-실습">8 ) 종합 실습</h2>

<hr />

<p>강의의 종합 실습은 Database와 Web Application을 Container로 구성하는 과정이다.</p>

<ol>
  <li>
    <p>MySQL, MariaDB, PostgreSQL, MongoDB, Redis 중 하나를 선택한다.</p>
  </li>
  <li>
    <p>외부에서 접속할 수 있도록 Database Container를 생성한다.</p>
  </li>
  <li>
    <p>프로그래밍 언어로 Database에 접속하는 Web Application을 작성한다.</p>
  </li>
  <li>
    <p>Table에 데이터 한 건을 삽입하고 전체 데이터를 조회하는 기능을 구현한다.</p>
  </li>
  <li>
    <p>Web Application을 Multi-stage Build로 Docker Image로 만든다.</p>
  </li>
  <li>
    <p>Image로 Container를 실행하고 외부에서 접속되는지 확인한다.</p>
  </li>
</ol>

<p>Kafka를 이용한 CQRS 구현은 별도의 Kafka·CQRS 실습 범위에 해당하므로 이 문서에서는 다루지 않는다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Multi-stage Build는 Build 환경과 Runtime 환경을 서로 다른 Stage로 분리한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">COPY --from</code>으로 Builder에서 생성한 실행 산출물이나 의존성 환경만 Runtime Stage에 전달한다.</p>
    </li>
    <li>
      <p>Spring Boot는 JAR, Go는 실행 Binary, React는 정적 File을 최종 Image에 복사한다.</p>
    </li>
    <li>
      <p>Node.js와 Python은 Runtime에도 언어 실행 환경과 Package가 필요하다.</p>
    </li>
    <li>
      <p>의존성 File을 Source Code보다 먼저 복사하면 Build Cache를 효율적으로 사용할 수 있다.</p>
    </li>
    <li>
      <p>최종 Stage에서 작은 Base Image와 일반 사용자를 사용하면 Image 크기와 실행 권한을 함께 관리할 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Node.js, Spring Boot, Go, Python, React 애플리케이션의 Multi-stage Image Build 방법]]></summary></entry><entry><title type="html">Docker Compose</title><link href="https://kkr010128.github.io/cloud-native-16-docker-compose/" rel="alternate" type="text/html" title="Docker Compose" /><published>2026-08-20T00:00:00+09:00</published><updated>2026-08-20T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-16-docker-compose</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-16-docker-compose/"><![CDATA[<h2 id="1--docker-compose-개요">1 ) Docker Compose 개요</h2>

<hr />

<p>분산 Application은 Web Frontend, Backend API, Database처럼 여러 구성 요소로 이루어진다. Dockerfile은 각 구성 요소를 하나의 Image로 Packaging하지만 전체 Application을 실행하려면 여러 Container의 Port, Volume, 환경 변수와 Network를 함께 설정해야 한다.</p>

<p>각 Container를 <code class="language-plaintext highlighter-rouge">docker container run</code>으로 하나씩 실행할 수도 있지만 명령이 많아질수록 Option을 누락하거나 실행 순서를 잘못 지정하기 쉽다. Docker Compose는 공통 목적을 가진 여러 Container의 구성을 하나의 YAML File에 선언하고 함께 관리하는 도구이다.</p>

<ul>
  <li>
    <p>각 Application 구성 요소를 <code class="language-plaintext highlighter-rouge">services</code>에 정의한다.</p>
  </li>
  <li>
    <p>Container의 Image, Port, Volume, 환경 변수와 재시작 정책을 기록한다.</p>
  </li>
  <li>
    <p>필요한 Network와 Volume 같은 Docker Object를 생성한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker compose up</code>으로 Application Stack을 생성하고 실행한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker compose down</code>으로 Stack의 Container와 Network를 중지하고 삭제한다.</p>
  </li>
</ul>

<p>Compose File은 여러 Service가 실행 중일 때 갖추어야 하는 원하는 상태를 기술한다. Compose가 File을 읽으면 Docker API를 통해 Container, Network와 Volume을 구성한다.</p>

<h2 id="2--dockerfile과-docker-compose">2 ) Dockerfile과 Docker Compose</h2>

<hr />

<p>Dockerfile과 Docker Compose는 서로 다른 범위를 담당한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Dockerfile</th>
      <th>Docker Compose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목적</td>
      <td>하나의 Image 생성</td>
      <td>여러 Container로 구성된 Application 실행</td>
    </tr>
    <tr>
      <td>주요 정의</td>
      <td>Base Image, Package, Source Code, 실행 명령</td>
      <td>Service, Port, Volume, 환경 변수, Network</td>
    </tr>
    <tr>
      <td>주요 명령</td>
      <td><code class="language-plaintext highlighter-rouge">docker build</code></td>
      <td><code class="language-plaintext highlighter-rouge">docker compose up</code></td>
    </tr>
    <tr>
      <td>결과</td>
      <td>Docker Image</td>
      <td>실행 중인 Application Stack</td>
    </tr>
  </tbody>
</table>

<p>Web Frontend, Backend API와 Database가 있다면 구성 요소마다 Dockerfile을 작성할 수 있다. Compose File은 이렇게 만든 Image와 외부 Image를 조합하여 전체 Application의 실행 관계를 정의한다.</p>

<p>Compose로 실행한 Container도 각자 독립된 기능을 담당한다. 같은 Compose Project의 Service는 기본 Network에 연결되므로 IP 주소 대신 Service 이름으로 서로 통신할 수 있다.</p>

<p>Docker Compose는 개발, 테스트, CI와 운영 환경에서 사용할 수 있다. 다만 여러 Host에 걸친 자동 확장, 장애 복구와 Cluster Scheduling 같은 기능을 제공하는 Orchestrator와 역할이 같지는 않다. 그러한 운영 기능이 필요하면 Docker Swarm이나 Kubernetes 같은 별도의 Orchestration Platform을 검토한다.</p>

<h2 id="3--docker-compose-설치">3 ) Docker Compose 설치</h2>

<hr />

<p>Docker Desktop에는 Docker Compose가 포함되므로 Windows, macOS와 Docker Desktop을 사용하는 Linux에서는 별도로 설치할 필요가 없다.</p>

<p>Docker Engine과 Docker CLI만 설치된 Linux에서는 Docker의 Package Repository를 설정한 뒤 Compose Plugin을 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> docker-compose-plugin
docker compose version
</code></pre></div></div>

<p>현재 권장 방식은 Docker CLI Plugin과 공백을 사용한 <code class="language-plaintext highlighter-rouge">docker compose</code> 명령이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
docker compose down
</code></pre></div></div>

<p>강의에서 사용한 <code class="language-plaintext highlighter-rouge">docker-compose</code> 명령과 <code class="language-plaintext highlighter-rouge">/usr/local/bin/docker-compose</code>에 실행 File을 직접 설치하는 방식은 Standalone Compose 방식이다. 현재는 하위 호환을 위해 제공되는 Legacy 설치 방식이므로 이 문서의 실행 예제에서는 Compose Plugin을 사용한다.</p>

<h2 id="4--compose-file-구조">4 ) Compose File 구조</h2>

<hr />

<p>Compose File의 기본 이름으로 <code class="language-plaintext highlighter-rouge">compose.yaml</code> 또는 <code class="language-plaintext highlighter-rouge">docker-compose.yaml</code>을 사용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:80"</span>
</code></pre></div></div>

<p>주요 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">services</code></td>
      <td>Application을 구성하는 Service 정의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">image</code></td>
      <td>Container 생성에 사용할 Image</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">build</code></td>
      <td>Dockerfile을 이용해 Image를 Build할 경로와 Option</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ports</code></td>
      <td><code class="language-plaintext highlighter-rouge">HOST_PORT:CONTAINER_PORT</code> 형식의 Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Service의 영속 데이터와 Host File 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>Container에 전달할 환경 변수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">restart</code></td>
      <td>Container 종료 후 재시작 정책</td>
    </tr>
  </tbody>
</table>

<p>과거 Compose File에서는 <code class="language-plaintext highlighter-rouge">version: "3"</code> 또는 <code class="language-plaintext highlighter-rouge">version: "3.8"</code>처럼 File 형식 Version을 작성했다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.8"</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">web</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
</code></pre></div></div>

<p>최상위 <code class="language-plaintext highlighter-rouge">version</code> 속성은 현재 Compose Specification에서 호환을 위해 남아 있는 Obsolete 항목이다. Compose는 이 값과 관계없이 현재 Specification으로 File을 검증하므로 새로운 Compose File에서는 생략한다.</p>

<h2 id="5--apache-service">5 ) Apache Service</h2>

<hr />

<h3 id="docker-container-run으로-실행"><code class="language-plaintext highlighter-rouge">docker container run</code>으로 실행</h3>

<p>Apache HTTP Server Container 하나를 직접 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> apache_1 <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8080:80 <span class="se">\</span>
  httpd
</code></pre></div></div>

<h3 id="compose-file로-실행">Compose File로 실행</h3>

<p>동일한 설정을 <code class="language-plaintext highlighter-rouge">compose.yaml</code>에 선언한다. 앞의 Container와 동시에 비교할 수 있도록 Host의 8081번 Port를 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">apache_1</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">httpd</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8081:80"</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
</code></pre></div></div>

<p>Compose File이 있는 Directory에서 Service를 실행하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
curl http://localhost:8081
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker container run</code>의 <code class="language-plaintext highlighter-rouge">--name</code>, <code class="language-plaintext highlighter-rouge">-p</code>, <code class="language-plaintext highlighter-rouge">--restart</code> Option이 Compose File의 Service 이름, <code class="language-plaintext highlighter-rouge">ports</code>, <code class="language-plaintext highlighter-rouge">restart</code> 항목에 대응한다.</p>

<h2 id="6--mariadb-service">6 ) MariaDB Service</h2>

<hr />

<h3 id="docker-container-run으로-실행-1"><code class="language-plaintext highlighter-rouge">docker container run</code>으로 실행</h3>

<p>강의에서는 다음 명령으로 MariaDB 10.4.6 Container를 실행했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> mariadb-server <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 3306:3306 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>your_password <span class="se">\</span>
  <span class="nt">-v</span> ~/mariadb/data:/var/lib/mysql <span class="se">\</span>
  <span class="nt">--restart</span> always <span class="se">\</span>
  mariadb:10.4.6
</code></pre></div></div>

<p>MariaDB 10.4 계열은 유지 관리가 종료된 Legacy Version이다. 명령의 Option 대응 관계를 이해하기 위한 강의 예제로 보존하며, 새 환경의 실행 예제에는 지원 중인 10.11 계열을 사용한다.</p>

<h3 id="compose-file로-실행-1">Compose File로 실행</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">mariadb</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mariadb:10.11</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">mariadb-server</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">3307:3306"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./data:/var/lib/mysql</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">${MARIADB_ROOT_PASSWORD}</span>
</code></pre></div></div>

<p>Compose File과 같은 Directory에 <code class="language-plaintext highlighter-rouge">.env</code>를 만들고 실습용 비밀번호를 지정한다.</p>

<pre><code class="language-dotenv">MARIADB_ROOT_PASSWORD=your_password
</code></pre>

<p><code class="language-plaintext highlighter-rouge">.env</code>에는 비밀번호가 포함되므로 Version Control에 Commit하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.env
</code></pre></div></div>

<p>Service를 실행하고 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
</code></pre></div></div>

<p>두 실행 방식의 Option은 다음과 같이 대응한다.</p>

<table>
  <thead>
    <tr>
      <th><code class="language-plaintext highlighter-rouge">docker container run</code></th>
      <th>Compose File</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name mariadb-server</code></td>
      <td><code class="language-plaintext highlighter-rouge">container_name</code></td>
      <td>Container 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p 3306:3306</code></td>
      <td><code class="language-plaintext highlighter-rouge">ports</code></td>
      <td>Host와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v PATH:/var/lib/mysql</code></td>
      <td><code class="language-plaintext highlighter-rouge">volumes</code></td>
      <td>Database 데이터 영속화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e MYSQL_ROOT_PASSWORD=...</code></td>
      <td><code class="language-plaintext highlighter-rouge">environment</code></td>
      <td>초기 Root 비밀번호 전달</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--restart always</code></td>
      <td><code class="language-plaintext highlighter-rouge">restart: always</code></td>
      <td>Container 재시작 정책</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">~/mariadb/data</code>는 사용자의 Home Directory를 기준으로 하고 <code class="language-plaintext highlighter-rouge">./data</code>는 Compose File이 있는 Project Directory를 기준으로 한다. 상대 경로를 사용하면 Project File과 Database 데이터를 같은 Directory 구조 안에서 관리하기 쉽다.</p>

<h2 id="7--compose-network">7 ) Compose Network</h2>

<hr />

<p>Compose는 별도의 Network를 지정하지 않아도 Project 전용 기본 Network를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network <span class="nb">ls</span>
</code></pre></div></div>

<p>같은 Compose Network에 연결된 Service는 Container IP가 아니라 Service 이름으로 통신할 수 있다. 예를 들어 Backend Service에서 MariaDB에 접속할 때 Host를 <code class="language-plaintext highlighter-rouge">localhost</code>로 지정하지 않고 <code class="language-plaintext highlighter-rouge">mariadb</code>로 지정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host: mariadb
Port: 3306
</code></pre></div></div>

<p>Container 내부에서 <code class="language-plaintext highlighter-rouge">localhost</code>는 해당 Container 자신을 가리킨다. Host에 공개한 3307번 Port는 Host에서 MariaDB에 접속할 때 사용하고, Compose Network 안의 다른 Container는 <code class="language-plaintext highlighter-rouge">mariadb:3306</code>으로 접속한다.</p>

<h2 id="8--compose-생명주기-관리">8 ) Compose 생명주기 관리</h2>

<hr />

<p>주요 명령은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose up -d</code></td>
      <td>Service 생성 및 Background 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose ps</code></td>
      <td>Project의 Container 상태 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose logs</code></td>
      <td>Service Log 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose stop</code></td>
      <td>Container를 삭제하지 않고 중지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose start</code></td>
      <td>중지한 Container 시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker compose down</code></td>
      <td>Container와 기본 Network 중지 및 삭제</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose logs apache_1
docker compose stop
docker compose start
docker compose down
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker compose down</code>은 기본적으로 Container와 Compose가 만든 Network를 삭제한다. 위 MariaDB 예제의 <code class="language-plaintext highlighter-rouge">./data</code>는 Host Bind Mount이므로 Container를 삭제해도 Host Directory의 데이터는 유지된다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Dockerfile은 하나의 Image를 만들고 Docker Compose는 여러 Container로 구성된 Application을 실행한다.</p>
    </li>
    <li>
      <p>Compose File에는 Service별 Image, Port, Volume, 환경 변수와 재시작 정책을 YAML로 선언한다.</p>
    </li>
    <li>
      <p>현재는 Standalone <code class="language-plaintext highlighter-rouge">docker-compose</code>보다 Docker CLI Plugin의 <code class="language-plaintext highlighter-rouge">docker compose</code> 명령을 사용한다.</p>
    </li>
    <li>
      <p>최상위 <code class="language-plaintext highlighter-rouge">version</code> 속성은 현재 Obsolete이므로 새로운 Compose File에서는 생략한다.</p>
    </li>
    <li>
      <p>같은 Compose Project의 Service는 기본 Network에서 Service 이름으로 통신한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker compose up</code>으로 Stack을 생성하고 <code class="language-plaintext highlighter-rouge">docker compose down</code>으로 Container와 기본 Network를 정리한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><category term="DockerCompose" /><summary type="html"><![CDATA[Docker Compose의 역할과 Compose File 작성 방법 및 Apache와 MariaDB 실행 실습]]></summary></entry><entry><title type="html">애플리케이션별 Docker 이미지 빌드</title><link href="https://kkr010128.github.io/cloud-native-14-application-image/" rel="alternate" type="text/html" title="애플리케이션별 Docker 이미지 빌드" /><published>2026-08-19T00:00:00+09:00</published><updated>2026-08-19T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-14-application-image</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-14-application-image/"><![CDATA[<h2 id="1--애플리케이션-image-build-흐름">1 ) 애플리케이션 Image Build 흐름</h2>

<hr />

<p>애플리케이션의 언어와 Framework가 달라도 Docker Image를 만드는 전체 과정은 비슷하다.</p>

<ol>
  <li>
    <p>Host에서 Application을 작성하고 실행을 확인한다.</p>
  </li>
  <li>
    <p>Package 의존성을 File로 정의한다.</p>
  </li>
  <li>
    <p>Base Image, 작업 Directory, 복사 대상과 실행 명령을 Dockerfile에 작성한다.</p>
  </li>
  <li>
    <p>가상 환경, Log, Build Cache처럼 Image에 넣지 않을 대상을 <code class="language-plaintext highlighter-rouge">.dockerignore</code>에 등록한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker build</code>로 Image를 생성한다.</p>
  </li>
  <li>
    <p>Image로 Container를 실행하고 Host Port를 연결한다.</p>
  </li>
  <li>
    <p>Browser나 <code class="language-plaintext highlighter-rouge">curl</code>로 Application의 응답을 확인한다.</p>
  </li>
</ol>

<p>각 애플리케이션은 의존성을 표현하고 실행하는 방식에서 차이가 난다.</p>

<table>
  <thead>
    <tr>
      <th>애플리케이션</th>
      <th>Base Image</th>
      <th>의존성 또는 Build 입력</th>
      <th>Image의 실행 대상</th>
      <th>Container 실행 명령</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Django</td>
      <td><code class="language-plaintext highlighter-rouge">python</code></td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td>Python Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">manage.py runserver</code></td>
    </tr>
    <tr>
      <td>FastAPI</td>
      <td><code class="language-plaintext highlighter-rouge">python</code></td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td>Python Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">uvicorn</code></td>
    </tr>
    <tr>
      <td>Spring Boot</td>
      <td><code class="language-plaintext highlighter-rouge">eclipse-temurin:21-jdk</code></td>
      <td>Gradle로 만든 JAR</td>
      <td>Build된 JAR</td>
      <td><code class="language-plaintext highlighter-rouge">java -jar</code></td>
    </tr>
    <tr>
      <td>Node.js</td>
      <td><code class="language-plaintext highlighter-rouge">node:20-slim</code></td>
      <td><code class="language-plaintext highlighter-rouge">package.json</code></td>
      <td>JavaScript Source Code</td>
      <td><code class="language-plaintext highlighter-rouge">node app.js</code></td>
    </tr>
    <tr>
      <td>Go</td>
      <td><code class="language-plaintext highlighter-rouge">golang:1.23-alpine</code></td>
      <td><code class="language-plaintext highlighter-rouge">go.mod</code>, Go Source Code</td>
      <td>Compile한 Binary</td>
      <td><code class="language-plaintext highlighter-rouge">./main</code></td>
    </tr>
    <tr>
      <td>Apache Web</td>
      <td><code class="language-plaintext highlighter-rouge">ubuntu:20.04</code></td>
      <td><code class="language-plaintext highlighter-rouge">webapp.tar.gz</code></td>
      <td>압축 해제한 Web Source</td>
      <td><code class="language-plaintext highlighter-rouge">apachectl</code></td>
    </tr>
  </tbody>
</table>

<p>Python과 Node.js 예제는 Source Code와 Runtime을 함께 Image에 넣는다. Spring Boot는 Host에서 Build한 JAR를 복사하고, Go 예제는 Image를 Build하는 과정에서 Source Code를 Compile한다.</p>

<h2 id="2--django-application-image">2 ) Django Application Image</h2>

<hr />

<p>Django Application을 먼저 Host에서 실행한 뒤 Package 의존성과 Source Code를 Image에 복사한다. Host의 Python Package와 프로젝트 Package가 섞이지 않도록 가상 환경을 사용한다.</p>

<h3 id="프로젝트-생성과-실행">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>Python Package 설치와 가상 환경 생성에 필요한 Package를 준비한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> python3-pip python3-venv
</code></pre></div>    </div>
  </li>
  <li>
    <p>작업 Directory를 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>django_workspace
<span class="nb">cd </span>django_workspace
</code></pre></div>    </div>
  </li>
  <li>
    <p>Python 가상 환경을 생성하고 활성화한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 <span class="nt">-m</span> venv ./myvenv
<span class="nb">source</span> ./myvenv/bin/activate
</code></pre></div>    </div>

    <p>가상 환경 생성 기능이 설치되어 있지 않다면 Ubuntu에서는 <code class="language-plaintext highlighter-rouge">python3-venv</code> Package가 필요할 수 있다.</p>
  </li>
  <li>
    <p>Django를 설치하고 프로젝트를 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>django
django-admin startproject dockeredjango
<span class="nb">cd </span>dockeredjango
</code></pre></div>    </div>
  </li>
  <li>
    <p>개발 Server를 실행한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python manage.py runserver
</code></pre></div>    </div>
  </li>
  <li>
    <p>다른 Terminal에서 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="의존성-file-생성">의존성 File 생성</h3>

<p>현재 가상 환경의 Package와 Version을 <code class="language-plaintext highlighter-rouge">requirements.txt</code>로 저장한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python</span>

<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["python", "manage.py", "runserver", "0.0.0.0:8000"]</span>
</code></pre></div></div>

<p>Dockerfile의 주요 동작은 다음과 같다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">requirements.txt</code>를 먼저 복사하고 Package를 설치한다.</p>
  </li>
  <li>
    <p>프로젝트 Source Code는 Package 설치 뒤에 복사한다.</p>
  </li>
  <li>
    <p>Django 개발 Server가 Container 밖의 요청을 받을 수 있도록 <code class="language-plaintext highlighter-rouge">0.0.0.0:8000</code>에서 실행한다.</p>
  </li>
</ul>

<h3 id="image-build와-container-실행">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> dockerdjango:latest <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> dockerdjango <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  dockerdjango:latest
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">manage.py runserver</code>는 개발 과정에서 사용하는 Server이다. 이 실습에서는 Container Image 제작 흐름을 확인하기 위해 사용한다.</p>

<h2 id="3--fastapi-application-image">3 ) FastAPI Application Image</h2>

<hr />

<p>FastAPI도 Python Source Code와 <code class="language-plaintext highlighter-rouge">requirements.txt</code>를 Image에 복사하지만 Application은 Uvicorn으로 실행한다.</p>

<h3 id="프로젝트-생성과-실행-1">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>작업 Directory를 만들고 가상 환경을 활성화한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>fastapi_workspace
<span class="nb">cd </span>fastapi_workspace
python3 <span class="nt">-m</span> venv ./myvenv
<span class="nb">source</span> ./myvenv/bin/activate
</code></pre></div>    </div>
  </li>
  <li>
    <p>FastAPI와 Uvicorn을 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>fastapi uvicorn
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">main.py</code>를 작성한다.</p>

    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">fastapi</span> <span class="kn">import</span> <span class="n">FastAPI</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">FastAPI</span><span class="p">()</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">read_root</span><span class="p">():</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"Hello"</span><span class="p">:</span> <span class="s">"World"</span><span class="p">}</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"/items/{item_id}"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">read_item</span><span class="p">(</span><span class="n">item_id</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">q</span><span class="p">:</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span> <span class="o">=</span> <span class="bp">None</span><span class="p">):</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"item_id"</span><span class="p">:</span> <span class="n">item_id</span><span class="p">,</span> <span class="s">"q"</span><span class="p">:</span> <span class="n">q</span><span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>개발 Server를 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn main:app <span class="nt">--reload</span>
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl <span class="s1">'http://localhost:8000/items/1?q=docker'</span>
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="의존성-file과-dockerignore">의존성 File과 <code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<p>Host의 가상 환경과 Python Cache는 Image에 복사하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myvenv/
__pycache__/
.pytest_cache/
.env
</code></pre></div></div>

<h3 id="dockerfile-1">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">requirements.txt</code>를 Source Code보다 먼저 복사하면 다음과 같이 Build Cache를 활용할 수 있다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">requirements.txt</code>가 바뀌지 않으면 <code class="language-plaintext highlighter-rouge">pip install</code> Layer를 재사용한다.</p>
  </li>
  <li>
    <p>Source Code만 수정하면 <code class="language-plaintext highlighter-rouge">COPY . .</code> 이후의 Layer만 다시 Build한다.</p>
  </li>
  <li>
    <p>시간이 오래 걸리는 Package 설치를 매번 반복하지 않는다.</p>
  </li>
</ol>

<h3 id="image-build와-container-실행-1">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> my-fastapi-app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> my-fastapi-app <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  my-fastapi-app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>Django와 FastAPI는 Python Runtime, Package 의존성과 Source Code를 Image에 포함한다.</p>
    </li>
    <li>
      <p>두 예제 모두 <code class="language-plaintext highlighter-rouge">requirements.txt</code>를 먼저 복사하여 Package 설치 Layer의 Cache를 활용한다.</p>
    </li>
    <li>
      <p>Host의 가상 환경은 Image에 복사하지 않으며 Container 안에서 필요한 Package를 다시 설치한다.</p>
    </li>
    <li>
      <p>Container 밖에서 접속하려면 Application이 <code class="language-plaintext highlighter-rouge">0.0.0.0</code>에서 요청을 받아야 한다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="4--spring-boot-application-image">4 ) Spring Boot Application Image</h2>

<hr />

<p>Python 예제는 Source Code를 Interpreter로 실행했지만 Java Application은 Gradle로 JAR를 Build한 뒤 결과물을 Image에 복사한다.</p>

<h3 id="프로젝트-준비와-build">프로젝트 준비와 Build</h3>

<ol>
  <li>
    <p>Git을 설치하고 실습 Repository를 가져온다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> git
git clone https://github.com/itggangpae/spring_docker.git
<span class="nb">cd </span>spring_docker
</code></pre></div>    </div>
  </li>
  <li>
    <p>Linux에서 Gradle Wrapper를 실행할 수 있도록 권한을 부여한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x ./gradlew
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하여 동작을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew bootRun
</code></pre></div>    </div>
  </li>
  <li>
    <p>실행을 중지한 뒤 배포할 JAR를 Build한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew clean build
<span class="nb">ls</span> ./build/libs
</code></pre></div>    </div>
  </li>
</ol>

<p>Build 결과는 <code class="language-plaintext highlighter-rouge">./build/libs</code>에 생성된다.</p>

<h3 id="dockerfile-2">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:21-jdk</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> ./build/libs/*.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>이 Dockerfile은 Gradle Build를 Image 안에서 다시 수행하지 않는다. Host에서 생성한 JAR만 복사하여 <code class="language-plaintext highlighter-rouge">java -jar</code>로 실행한다.</p>

<h3 id="image-build와-container-실행-2">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> spring_docker:1.0.0 <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> spring_docker <span class="se">\</span>
  <span class="nt">-p</span> 8000:8080 <span class="se">\</span>
  spring_docker:1.0.0
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<p>강의 Dockerfile에는 Gradle 프로젝트에서 Maven Wrapper인 <code class="language-plaintext highlighter-rouge">./mvnw</code>를 실행하는 <code class="language-plaintext highlighter-rouge">CMD</code>와 JAR를 실행하는 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>가 함께 작성되어 있었다. 이 실습은 Host에서 Gradle Build를 마친 JAR를 실행하는 방식이므로 Maven 명령을 제거하고 JAR 실행 명령만 남겼다.</p>

<h2 id="5--nodejs-application-image">5 ) Node.js Application Image</h2>

<hr />

<p>Node.js Application은 Python Application과 마찬가지로 의존성 File과 Source Code를 Image에 복사한 뒤 Runtime으로 실행한다.</p>

<h3 id="프로젝트-생성과-실행-2">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>Node.js와 npm을 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> nodejs npm
</code></pre></div>    </div>
  </li>
  <li>
    <p>작업 Directory를 Node.js 프로젝트로 초기화하고 Package를 설치한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>node_workspace
<span class="nb">cd </span>node_workspace
npm init
npm <span class="nb">install </span>express nodemon
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">package.json</code>의 <code class="language-plaintext highlighter-rouge">scripts</code>에 실행 명령을 작성한다.</p>

    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"scripts"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"start"</span><span class="p">:</span><span class="w"> </span><span class="s2">"node app.js"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">app.js</code>를 작성한다.</p>

    <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">express</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">express</span><span class="dl">'</span><span class="p">);</span>

<span class="kd">const</span> <span class="nx">app</span> <span class="o">=</span> <span class="nx">express</span><span class="p">();</span>
<span class="nx">app</span><span class="p">.</span><span class="kd">set</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">,</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">PORT</span> <span class="o">||</span> <span class="mi">3000</span><span class="p">);</span>

<span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">/</span><span class="dl">'</span><span class="p">,</span> <span class="p">(</span><span class="nx">req</span><span class="p">,</span> <span class="nx">res</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">res</span><span class="p">.</span><span class="nx">send</span><span class="p">(</span><span class="dl">'</span><span class="s1">Hello Express</span><span class="dl">'</span><span class="p">);</span>
<span class="p">});</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">listen</span><span class="p">(</span><span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">),</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">app</span><span class="p">.</span><span class="kd">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">port</span><span class="dl">'</span><span class="p">)}</span><span class="s2">번에서 대기 중`</span><span class="p">);</span>
<span class="p">});</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node app.js
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:3000
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="dockerignore"><code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<p>Host에서 설치한 <code class="language-plaintext highlighter-rouge">node_modules</code>는 Image에 복사하지 않는다. Container의 Platform에 맞는 Package는 Image Build 과정에서 다시 설치한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>node_modules/
npm-debug.log
.git/
.env
</code></pre></div></div>

<h3 id="dockerfile-3">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> node:20-slim</span>

<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>

<span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 3000</span>

<span class="k">CMD</span><span class="s"> ["node", "app.js"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">package.json</code>과 Package Lock File을 먼저 복사하면 Source Code만 변경되었을 때 <code class="language-plaintext highlighter-rouge">npm install</code> Layer를 재사용할 수 있다.</p>

<h3 id="image-build와-container-실행-3">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> node_app <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> nodeapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:3000 <span class="se">\</span>
  node_app
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="6--go-application-image">6 ) Go Application Image</h2>

<hr />

<p>Go Application은 Compile한 실행 File을 Container의 주 Process로 실행한다. 이 실습에서는 Go Base Image 안에서 의존성을 내려받고 Source Code를 Build한다.</p>

<h3 id="go-설치">Go 설치</h3>

<p>강의에서는 Go 1.22.0 Archive를 내려받아 <code class="language-plaintext highlighter-rouge">/usr/local</code>에 설치했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-OL</span> https://golang.org/dl/go1.22.0.linux-amd64.tar.gz
<span class="nb">sudo tar</span> <span class="nt">-C</span> /usr/local <span class="nt">-xzf</span> go1.22.0.linux-amd64.tar.gz
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">~/.profile</code>에 Go 실행 경로를 추가하고 현재 Shell에 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s1">'export PATH=$PATH:/usr/local/go/bin'</span> <span class="o">&gt;&gt;</span> ~/.profile
<span class="nb">echo</span> <span class="s1">'export PATH=$PATH:$(go env GOPATH)/bin'</span> <span class="o">&gt;&gt;</span> ~/.profile
<span class="nb">source</span> ~/.profile
</code></pre></div></div>

<h3 id="프로젝트-생성과-실행-3">프로젝트 생성과 실행</h3>

<ol>
  <li>
    <p>빈 Directory에서 Go Module을 생성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>go_workspace
<span class="nb">cd </span>go_workspace
go mod init my-go-app
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">main.go</code>를 작성한다.</p>

    <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="n">main</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="s">"encoding/json"</span>
    <span class="s">"fmt"</span>
    <span class="s">"net/http"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">http</span><span class="o">.</span><span class="n">HandleFunc</span><span class="p">(</span><span class="s">"/"</span><span class="p">,</span> <span class="k">func</span><span class="p">(</span><span class="n">w</span> <span class="n">http</span><span class="o">.</span><span class="n">ResponseWriter</span><span class="p">,</span> <span class="n">r</span> <span class="o">*</span><span class="n">http</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">w</span><span class="o">.</span><span class="n">Header</span><span class="p">()</span><span class="o">.</span><span class="n">Set</span><span class="p">(</span><span class="s">"Content-Type"</span><span class="p">,</span> <span class="s">"application/json"</span><span class="p">)</span>
        <span class="n">json</span><span class="o">.</span><span class="n">NewEncoder</span><span class="p">(</span><span class="n">w</span><span class="p">)</span><span class="o">.</span><span class="n">Encode</span><span class="p">(</span><span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="kt">string</span><span class="p">{</span>
            <span class="s">"message"</span><span class="o">:</span> <span class="s">"Hello From Go"</span><span class="p">,</span>
        <span class="p">})</span>
    <span class="p">})</span>

    <span class="n">http</span><span class="o">.</span><span class="n">HandleFunc</span><span class="p">(</span><span class="s">"/health"</span><span class="p">,</span> <span class="k">func</span><span class="p">(</span><span class="n">w</span> <span class="n">http</span><span class="o">.</span><span class="n">ResponseWriter</span><span class="p">,</span> <span class="n">r</span> <span class="o">*</span><span class="n">http</span><span class="o">.</span><span class="n">Request</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">fmt</span><span class="o">.</span><span class="n">Fprint</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="s">"OK"</span><span class="p">)</span>
    <span class="p">})</span>

    <span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"서버가 8000번 Port에서 시작되어 대기 중이다."</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">http</span><span class="o">.</span><span class="n">ListenAndServe</span><span class="p">(</span><span class="s">":8000"</span><span class="p">,</span> <span class="no">nil</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Application을 실행하고 응답을 확인한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go run main.go
</code></pre></div>    </div>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl http://localhost:8000/health
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="dockerfile-4">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> golang:1.23-alpine</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">RUN </span>go mod download
<span class="k">RUN </span>go build <span class="nt">-o</span> main .

<span class="k">EXPOSE</span><span class="s"> 8000</span>

<span class="k">CMD</span><span class="s"> ["./main"]</span>
</code></pre></div></div>

<p>Build 단계에서는 다음 작업을 수행한다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">go mod download</code>로 <code class="language-plaintext highlighter-rouge">go.mod</code>에 정의된 의존성을 내려받는다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">go build -o main .</code>으로 Source Code를 <code class="language-plaintext highlighter-rouge">main</code> 실행 File로 Compile한다.</p>
  </li>
  <li>
    <p>Container가 시작되면 Compile된 <code class="language-plaintext highlighter-rouge">main</code>을 실행한다.</p>
  </li>
</ul>

<h3 id="image-build와-container-실행-4">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> goapp <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> goapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:8000 <span class="se">\</span>
  goapp
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl http://localhost:8000/health
</code></pre></div></div>

<p>이 예제는 Build 도구와 실행 File을 하나의 Image에 함께 두는 단일 Stage Build이다.</p>

<h2 id="7--압축된-web-source로-apache-image-생성">7 ) 압축된 Web Source로 Apache Image 생성</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">ADD</code>는 Local Tar Archive를 Image에 추가할 때 압축을 자동으로 해제할 수 있다. 이 기능을 이용해 <code class="language-plaintext highlighter-rouge">webapp.tar.gz</code>의 Web Source를 Apache Document Root에 배치한다.</p>

<h3 id="source-code-준비">Source Code 준비</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/brayanlee/webapp.git
<span class="nb">cd </span>webapp
<span class="nb">ls</span>
</code></pre></div></div>

<p>Build Context에 <code class="language-plaintext highlighter-rouge">webapp.tar.gz</code>가 있는지 확인한다.</p>

<h3 id="dockerfile-5">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> apache2 <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="k">ADD</span><span class="s"> webapp.tar.gz /var/www/html/</span>

<span class="k">WORKDIR</span><span class="s"> /var/www/html</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["/usr/sbin/apachectl", "-D", "FOREGROUND"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ADD webapp.tar.gz /var/www/html/</code>은 Archive의 내용을 해당 Directory에 자동으로 압축 해제한다. 자동 압축 해제가 필요하지 않은 일반 File과 Directory는 동작이 더 명확한 <code class="language-plaintext highlighter-rouge">COPY</code>를 사용한다.</p>

<h3 id="image-build와-container-실행-5">Image Build와 Container 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> webapp <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> webapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  webapp
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h2 id="8--애플리케이션별-build-방식-비교">8 ) 애플리케이션별 Build 방식 비교</h2>

<hr />

<p>애플리케이션 Image를 구성할 때는 다음 세 가지를 구분해야 한다.</p>

<ol>
  <li>
    <p>의존성을 어떤 File로 재현할 것인지 결정한다.</p>
  </li>
  <li>
    <p>Source Code와 Build 산출물 중 무엇을 Image에 복사할지 결정한다.</p>
  </li>
  <li>
    <p>Container가 시작될 때 실행할 하나의 주 Process를 지정한다.</p>
  </li>
</ol>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Django·FastAPI</th>
      <th>Node.js</th>
      <th>Spring Boot</th>
      <th>Go</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>의존성 정의</td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code></td>
      <td><code class="language-plaintext highlighter-rouge">package.json</code></td>
      <td>Gradle 설정</td>
      <td><code class="language-plaintext highlighter-rouge">go.mod</code></td>
    </tr>
    <tr>
      <td>Build 시 Package 처리</td>
      <td><code class="language-plaintext highlighter-rouge">pip install</code></td>
      <td><code class="language-plaintext highlighter-rouge">npm install</code></td>
      <td>Host에서 Gradle Build</td>
      <td><code class="language-plaintext highlighter-rouge">go mod download</code></td>
    </tr>
    <tr>
      <td>Image에 포함하는 주요 대상</td>
      <td>Python Source Code</td>
      <td>JavaScript Source Code</td>
      <td>JAR</td>
      <td>Source Code와 실행 Binary</td>
    </tr>
    <tr>
      <td>실행 방식</td>
      <td>Python Runtime</td>
      <td>Node.js Runtime</td>
      <td>JVM에서 JAR 실행</td>
      <td>Native Binary 실행</td>
    </tr>
    <tr>
      <td>Cache 활용 지점</td>
      <td>의존성 File 우선 복사</td>
      <td>Package File 우선 복사</td>
      <td>JAR 변경 시 복사</td>
      <td>의존성 다운로드와 Compile</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>애플리케이션을 먼저 Host에서 실행하여 Source Code 자체의 동작을 확인한 뒤 Image를 Build한다.</p>
    </li>
    <li>
      <p>의존성 File을 Source Code보다 먼저 복사하면 Package 설치 Layer의 Build Cache를 활용할 수 있다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">.dockerignore</code>로 Host의 가상 환경, Package Directory, Cache와 환경 변수 File을 제외한다.</p>
    </li>
    <li>
      <p>Python과 Node.js는 Source Code를 Runtime으로 실행하고, Spring Boot와 Go는 Build 또는 Compile 결과를 실행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">CMD</code>와 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>에는 Container가 시작될 때 실행할 주 Process를 지정한다.</p>
    </li>
    <li>
      <p>Local Tar Archive의 자동 압축 해제가 필요한 경우 <code class="language-plaintext highlighter-rouge">ADD</code>를 사용하고 일반 복사에는 <code class="language-plaintext highlighter-rouge">COPY</code>를 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Django, FastAPI, Spring Boot, Node.js, Go 애플리케이션을 Docker 이미지로 빌드하고 실행하는 방법]]></summary></entry><entry><title type="html">Docker Volume</title><link href="https://kkr010128.github.io/cloud-native-12-docker-volume/" rel="alternate" type="text/html" title="Docker Volume" /><published>2026-08-18T00:00:00+09:00</published><updated>2026-08-18T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-12-docker-volume</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-12-docker-volume/"><![CDATA[<h2 id="1--docker-volume">1 ) Docker Volume</h2>

<hr />

<p>Container의 쓰기 Layer는 Container의 생명주기에 종속된다. Container를 삭제하면 쓰기 Layer에 저장된 데이터도 함께 사라지므로 Database, Upload File, Log처럼 보존해야 하는 데이터는 Container 밖에 저장해야 한다.</p>

<p>Docker는 Container와 분리하여 데이터를 관리할 수 있도록 세 가지 Mount 방식을 제공한다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>저장 위치</th>
      <th>관리 주체</th>
      <th>주요 용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Volume Mount</td>
      <td>Docker가 관리하는 Host 영역</td>
      <td>Docker Engine</td>
      <td>Database 등 영속 데이터</td>
    </tr>
    <tr>
      <td>Bind Mount</td>
      <td>사용자가 지정한 Host File 또는 Directory</td>
      <td>사용자</td>
      <td>Source Code, 설정, Log 공유</td>
    </tr>
    <tr>
      <td>tmpfs Mount</td>
      <td>Host Memory</td>
      <td>Host OS</td>
      <td>일시적인 데이터, 민감 정보</td>
    </tr>
  </tbody>
</table>

<p>Volume은 Container를 삭제해도 유지되며 다른 Container에 다시 연결할 수 있다. 직접 Host 경로를 지정하지 않기 때문에 일반적으로 영속 데이터를 저장할 때 권장된다.</p>

<h2 id="2--volume-생성과-연결">2 ) Volume 생성과 연결</h2>

<hr />

<h3 id="volume-생성-및-조회">Volume 생성 및 조회</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create my-app-vol-1
docker volume <span class="nb">ls
</span>docker volume inspect my-app-vol-1
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker volume inspect</code>를 실행하면 Driver, 생성 시각, Host의 실제 저장 위치인 <code class="language-plaintext highlighter-rouge">Mountpoint</code> 등을 확인할 수 있다. Docker가 관리하는 경로의 File을 Host에서 직접 수정하기보다는 Container를 통해 다루는 것이 안전하다.</p>

<h3 id="container에-volume-mount">Container에 Volume Mount</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> vol-test <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-v</span> my-app-vol-1:/var/log <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  <span class="nb">sleep </span>infinity
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-v VOLUME_NAME:CONTAINER_PATH</code> 형식으로 Volume을 Container의 경로에 연결한다.</p>

<p><code class="language-plaintext highlighter-rouge">--mount</code>를 사용하면 같은 설정을 더 명확하게 표현할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> vol-test-mount <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">--mount</span> <span class="nb">type</span><span class="o">=</span>volume,src<span class="o">=</span>my-app-vol-1,dst<span class="o">=</span>/var/log <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  <span class="nb">sleep </span>infinity
</code></pre></div></div>

<h3 id="데이터-영속성-확인">데이터 영속성 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">exec </span>vol-test <span class="se">\</span>
  sh <span class="nt">-c</span> <span class="s1">'echo "volume data" &gt; /var/log/message.txt'</span>

docker container stop vol-test vol-test-mount
docker container <span class="nb">rm </span>vol-test vol-test-mount

docker container run <span class="nt">--rm</span> <span class="se">\</span>
  <span class="nt">-v</span> my-app-vol-1:/data <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  <span class="nb">cat</span> /data/message.txt
</code></pre></div></div>

<p>기존 Container를 삭제한 뒤 새 Container에 같은 Volume을 연결해도 <code class="language-plaintext highlighter-rouge">volume data</code>가 출력된다. Container와 Volume의 생명주기가 서로 분리되어 있기 때문이다.</p>

<h3 id="volume-삭제">Volume 삭제</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume <span class="nb">rm </span>my-app-vol-1

<span class="c"># 사용하지 않는 Volume 일괄 삭제</span>
docker volume prune
</code></pre></div></div>

<p>실행 여부와 관계없이 Volume을 참조하는 Container가 남아 있으면 Volume을 삭제할 수 없다. 해당 Container를 먼저 제거해야 한다.</p>

<p><code class="language-plaintext highlighter-rouge">docker volume prune</code>은 사용하지 않는 Volume을 한꺼번에 삭제하므로 삭제 대상을 확인한 뒤 실행한다.</p>

<h2 id="3--bind-mount">3 ) Bind Mount</h2>

<hr />

<p>Bind Mount는 Host의 기존 File 또는 Directory를 Container 경로에 직접 연결한다. Host와 Container가 같은 내용을 바로 확인할 수 있어 개발 중 Source Code나 설정 File을 공유하거나 Container의 Log를 Host에 저장할 때 유용하다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /home/user/apache

docker container run <span class="se">\</span>
  <span class="nt">--name</span> apache <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9000:80 <span class="se">\</span>
  <span class="nt">--mount</span> <span class="nb">type</span><span class="o">=</span><span class="nb">bind</span>,src<span class="o">=</span>/home/user/apache,dst<span class="o">=</span>/usr/local/apache2/htdocs <span class="se">\</span>
  httpd:2
</code></pre></div></div>

<p>짧은 <code class="language-plaintext highlighter-rouge">-v</code> 형식도 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> apache-volume <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9001:80 <span class="se">\</span>
  <span class="nt">-v</span> /home/user/apache:/usr/local/apache2/htdocs <span class="se">\</span>
  httpd:2
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--mount</code>는 Host 경로가 없으면 오류를 반환한다. 반면 <code class="language-plaintext highlighter-rouge">-v</code>는 존재하지 않는 Host 경로를 Directory로 자동 생성할 수 있으며, 생성된 Directory의 소유자가 <code class="language-plaintext highlighter-rouge">root</code>가 될 수 있다. 경로 오타와 권한 문제를 줄이려면 Mount 전에 Host 경로를 만들고 절대 경로를 확인한다.</p>

<p>Container Process도 Mount된 Host File을 변경할 수 있다. 읽기만 허용하려면 <code class="language-plaintext highlighter-rouge">readonly</code> 또는 <code class="language-plaintext highlighter-rouge">ro</code> Option을 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> readonly-web <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">--mount</span> <span class="nb">type</span><span class="o">=</span><span class="nb">bind</span>,src<span class="o">=</span>/home/user/apache,dst<span class="o">=</span>/usr/local/apache2/htdocs,readonly <span class="se">\</span>
  httpd:2
</code></pre></div></div>

<p>Container를 제거하면 Bind Mount 연결은 해제되지만 Host의 원본 File과 Directory는 그대로 유지된다.</p>

<h2 id="4--tmpfs-mount">4 ) tmpfs Mount</h2>

<hr />

<p>tmpfs Mount는 데이터를 Host의 Memory에만 저장한다. Container가 중지되거나 제거되면 데이터가 사라지므로 영속화가 목적이 아닌 임시 작업 File이나 민감한 Runtime Data에 적합하다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> webserver <span class="se">\</span>
  <span class="nt">-dit</span> <span class="se">\</span>
  <span class="nt">--tmpfs</span> /var/www/html <span class="se">\</span>
  httpd:2
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--mount type=tmpfs,dst=/var/www/html</code> 형식으로도 지정할 수 있다. tmpfs는 Linux Container에서 사용할 수 있으며 Host의 Memory를 사용하므로 크기 제한 등 Resource 설정을 함께 고려해야 한다.</p>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>Volume Mount는 Docker Engine이 관리하는 영역에 데이터를 저장한다.</p>
    </li>
    <li>
      <p>Bind Mount는 Host의 File이나 Directory를 Container에 직접 연결한다.</p>
    </li>
    <li>
      <p>tmpfs Mount는 Host Memory를 사용하며 Container가 제거되면 데이터가 사라진다.</p>
    </li>
    <li>
      <p>보존 목적과 Host에서의 직접 관리 여부에 따라 Mount 방식을 선택한다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="5--mysql-데이터-영속화">5 ) MySQL 데이터 영속화</h2>

<hr />

<p>MySQL 공식 Image는 <code class="language-plaintext highlighter-rouge">/var/lib/mysql</code>에 Database Data를 저장한다. 이 경로에 이름이 있는 Volume을 연결하면 Image나 Container를 삭제하고 다시 생성해도 데이터를 유지할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>값</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Image</td>
      <td><code class="language-plaintext highlighter-rouge">mysql</code></td>
    </tr>
    <tr>
      <td>기본 Port</td>
      <td><code class="language-plaintext highlighter-rouge">3306</code></td>
    </tr>
    <tr>
      <td>데이터 Directory</td>
      <td><code class="language-plaintext highlighter-rouge">/var/lib/mysql</code></td>
    </tr>
    <tr>
      <td>Root 비밀번호 환경 변수</td>
      <td><code class="language-plaintext highlighter-rouge">MYSQL_ROOT_PASSWORD</code></td>
    </tr>
    <tr>
      <td>기본 Database 환경 변수</td>
      <td><code class="language-plaintext highlighter-rouge">MYSQL_DATABASE</code></td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">docker search mysql</code>로 Docker Hub의 관련 Repository를 검색할 수 있으며 <code class="language-plaintext highlighter-rouge">OFFICIAL</code> 항목에서 공식 Image 여부를 확인할 수 있다.</p>

<p><img src="../../assets/post/2026-08-18-cloud-native-12-docker-volume/06.webp" alt="" /></p>

<h3 id="container-생성">Container 생성</h3>

<p>MySQL Container를 생성하고 Named Volume을 <code class="language-plaintext highlighter-rouge">/var/lib/mysql</code>에 Mount한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> mysql-volume <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>1234 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_DATABASE</span><span class="o">=</span>itstudy <span class="se">\</span>
  <span class="nt">-v</span> mysql-data-vol:/var/lib/mysql <span class="se">\</span>
  mysql
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name mysql-volume</code></td>
      <td>Container 이름을 <code class="language-plaintext highlighter-rouge">mysql-volume</code>으로 지정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>Container를 Background에서 실행한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e MYSQL_ROOT_PASSWORD=1234</code></td>
      <td>MySQL <code class="language-plaintext highlighter-rouge">root</code> 계정의 비밀번호를 설정한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e MYSQL_DATABASE=itstudy</code></td>
      <td>Container 최초 실행 시 <code class="language-plaintext highlighter-rouge">itstudy</code> Database를 생성한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v mysql-data-vol:/var/lib/mysql</code></td>
      <td><code class="language-plaintext highlighter-rouge">mysql-data-vol</code> Named Volume을 MySQL Data 저장 경로에 Mount한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mysql</code></td>
      <td>MySQL Image를 사용한다.</td>
    </tr>
  </tbody>
</table>

<h4 id="architecture-확인">Architecture 확인</h4>

<p>Apple Silicon처럼 Host Architecture와 Image가 지원하는 Architecture가 다른 경우에는 Image Tag의 Architecture 지원 여부를 먼저 확인한다.</p>

<p>필요한 경우 다음과 같이 <code class="language-plaintext highlighter-rouge">--platform</code> Option을 지정할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">--platform</span> linux/amd64
</code></pre></div></div>

<p>다만 Host와 다른 Architecture를 지정하면 Emulation으로 실행될 수 있어 성능이 낮아질 수 있다.</p>

<p>현재 MySQL 공식 Image에는 여러 Architecture를 지원하는 Tag가 있으므로, Mac을 사용한다는 이유만으로 <code class="language-plaintext highlighter-rouge">--platform linux/amd64</code> Option을 항상 추가할 필요는 없다.</p>

<h4 id="비밀번호-관리">비밀번호 관리</h4>

<p>실제 운영 환경에서는 다음과 같이 비밀번호를 명령어나 Source Code에 평문으로 직접 기록하지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>1234
</code></pre></div></div>

<p>운영 환경에서는 비밀번호와 같은 민감한 정보를 별도의 Secret 관리 수단을 통해 관리한다.</p>

<p>Container와 Volume의 연결 정보는 다음과 같이 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
docker container <span class="nb">ls</span> <span class="nt">-a</span>
docker volume <span class="nb">ls
</span>docker volume inspect mysql-data-vol
docker container inspect mysql-volume <span class="se">\</span>
  <span class="nt">--format</span> <span class="s1">'{{json .Mounts}}'</span> | jq

</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">-v mysql-data-vol:/var/lib/mysql</code>을 통해 MySQL Data를 Named Volume에 저장한다.</p>
    </li>
    <li>
      <p>Host와 Image의 Architecture가 다르다면 Image의 지원 Architecture를 먼저 확인한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">--platform linux/amd64</code>는 필요한 경우에만 사용하며, Emulation에 따른 성능 저하가 발생할 수 있다.</p>
    </li>
    <li>
      <p>운영 환경에서는 비밀번호를 명령어나 Source Code에 평문으로 기록하지 않는다.</p>
    </li>
  </ul>
</blockquote>

<h3 id="sample-data-생성">Sample Data 생성</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">exec</span> <span class="nt">-it</span> mysql-volume <span class="se">\</span>
  mysql <span class="nt">-uroot</span> <span class="nt">-p</span> itstudy
</code></pre></div></div>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">mytable</span> <span class="p">(</span>
  <span class="n">num</span> <span class="nb">INT</span><span class="p">,</span>
  <span class="n">name</span> <span class="nb">CHAR</span><span class="p">(</span><span class="mi">30</span><span class="p">)</span>
<span class="p">);</span>

<span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">mytable</span> <span class="k">VALUES</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'adam'</span><span class="p">);</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">mytable</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="container-재생성-후-확인">Container 재생성 후 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container stop mysql-volume
docker container <span class="nb">rm </span>mysql-volume

docker container run <span class="se">\</span>
  <span class="nt">--name</span> mysql-volume <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_ROOT_PASSWORD</span><span class="o">=</span>1234 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">MYSQL_DATABASE</span><span class="o">=</span>itstudy <span class="se">\</span>
  <span class="nt">-v</span> mysql-data-vol:/var/lib/mysql <span class="se">\</span>
  mysql

docker container <span class="nb">exec</span> <span class="nt">-it</span> mysql-volume <span class="se">\</span>
  mysql <span class="nt">-uroot</span> <span class="nt">-p</span> itstudy
</code></pre></div></div>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">mytable</span><span class="p">;</span>
</code></pre></div></div>

<p>새 Container에서도 앞에서 추가한 Row가 조회되면 데이터가 Volume에 영속적으로 저장된 것이다.</p>

<h2 id="6--nginx-log를-host에-저장">6 ) Nginx Log를 Host에 저장</h2>

<hr />

<p>Nginx의 Log Directory인 <code class="language-plaintext highlighter-rouge">/var/log/nginx</code>를 Host Directory에 Bind Mount하면 Container를 삭제한 뒤에도 Access Log와 Error Log를 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /home/user/logs

docker container run <span class="se">\</span>
  <span class="nt">--name</span> nginx-volume <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9000:80 <span class="se">\</span>
  <span class="nt">-v</span> /home/user/logs:/var/log/nginx <span class="se">\</span>
  nginx

curl http://localhost:9000
<span class="nb">cat</span> /home/user/logs/access.log
</code></pre></div></div>

<p>Host Directory에는 Container의 Nginx Process가 File을 생성하고 쓸 수 있는 권한이 필요하다.</p>

<h2 id="7--container-간-데이터-공유">7 ) Container 간 데이터 공유</h2>

<hr />

<p>하나의 Volume을 여러 Container에 Mount하면 File을 공유할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create shared-data

docker container run <span class="nt">--rm</span> <span class="se">\</span>
  <span class="nt">-v</span> shared-data:/data <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  sh <span class="nt">-c</span> <span class="s1">'echo "container-1" &gt; /data/message.txt'</span>

docker container run <span class="nt">--rm</span> <span class="se">\</span>
  <span class="nt">-v</span> shared-data:/data:ro <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  <span class="nb">cat</span> /data/message.txt
</code></pre></div></div>

<p>두 번째 Container는 동일한 Volume을 읽기 전용으로 연결한다. 동시에 여러 Container가 같은 데이터를 수정한다면 Application 차원의 동시성 제어가 필요하다.</p>

<p>기존 Container의 Mount 설정을 그대로 물려받는 <code class="language-plaintext highlighter-rouge">--volumes-from</code>도 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container create <span class="se">\</span>
  <span class="nt">--name</span> datavol <span class="se">\</span>
  <span class="nt">-v</span> /data-volume <span class="se">\</span>
  ubuntu:24.04

docker container run <span class="nt">--rm</span> <span class="se">\</span>
  <span class="nt">--volumes-from</span> datavol <span class="se">\</span>
  ubuntu:24.04 <span class="se">\</span>
  sh <span class="nt">-c</span> <span class="s1">'echo "shared data" &gt; /data-volume/message.txt'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--volumes-from</code>은 현재도 지원되지만, Volume 보관만을 위한 Data Volume Container Pattern은 이름이 있는 Volume이 도입되기 전에 주로 사용하던 방식이다. 새 구성에서는 이름이 있는 Volume을 각 Container에 명시적으로 Mount하는 편이 데이터의 위치와 연결 관계를 이해하기 쉽다.</p>

<h2 id="8--mount-시-주의-사항">8 ) Mount 시 주의 사항</h2>

<hr />

<p>Container의 Mount 대상 Directory에 Image가 제공하는 File이 이미 존재하더라도 Volume이나 Bind Mount를 연결하면 해당 File은 Mount 내용에 가려진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image의 /app
├── config.yml
└── default.txt

빈 Volume을 /app에 Mount
└── Mount된 Volume의 내용만 표시
</code></pre></div></div>

<p>가려진 Image File이 삭제된 것은 아니지만 Mount를 해제한 새 Container를 만들기 전에는 해당 경로에서 볼 수 없다. 설정 Directory나 Application Directory 전체를 Mount할 때는 Image의 기본 File이 필요한지 먼저 확인해야 한다.</p>

<p>Host의 File 하나를 Container의 특정 File 경로에 Bind Mount하면 그 File만 Mount 내용으로 대체되고 같은 Directory의 다른 File은 그대로 보인다. Host File을 Container Directory 자체에 Mount하면 File과 Directory Type이 달라 오류가 발생할 수 있으므로 대상에도 File 경로를 명시해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--rm</span> <span class="se">\</span>
  <span class="nt">--mount</span> <span class="nb">type</span><span class="o">=</span><span class="nb">bind</span>,src<span class="o">=</span>/home/user/nginx.conf,dst<span class="o">=</span>/etc/nginx/nginx.conf,readonly <span class="se">\</span>
  nginx
</code></pre></div></div>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Volume Mount는 Docker가 관리하는 저장 영역을 사용하며 Container를 삭제해도 데이터를 유지한다.</p>
    </li>
    <li>
      <p>Bind Mount는 Host의 File이나 Directory를 직접 연결하며 Container를 제거해도 Host 데이터는 남는다.</p>
    </li>
    <li>
      <p>tmpfs Mount는 Host Memory를 사용하므로 Container가 제거되면 데이터도 사라진다.</p>
    </li>
    <li>
      <p>MySQL 데이터와 Nginx Log처럼 보존해야 하는 데이터는 목적에 맞는 Mount 방식으로 Container 밖에 저장한다.</p>
    </li>
    <li>
      <p>여러 Container가 같은 Volume을 연결하면 데이터를 공유할 수 있다.</p>
    </li>
    <li>
      <p>기존 File이 있는 Container Directory에 Mount하면 Image의 File이 Mount 내용에 가려질 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Volume, Bind Mount, tmpfs의 차이와 데이터 영속화 및 공유 방법]]></summary></entry><entry><title type="html">Dockerfile</title><link href="https://kkr010128.github.io/cloud-native-13-dockerfile/" rel="alternate" type="text/html" title="Dockerfile" /><published>2026-08-18T00:00:00+09:00</published><updated>2026-08-18T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-13-dockerfile</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-13-dockerfile/"><![CDATA[<h2 id="1--infrastructure-as-code-iac">1 ) Infrastructure as Code, IaC</h2>

<hr />

<p>Infrastructure를 CLI로 직접 구성하면 설치 순서, Package의 의존 관계, 환경 설정을 모두 사람이 관리해야 한다. APM(Apache, PHP, MySQL)처럼 여러 Software가 연동되는 환경은 작업 누락과 설정 오류가 발생하기 쉽고, 동일한 환경을 다시 만드는 데에도 많은 시간이 든다.</p>

<p>IaC(Infrastructure as Code)는 Infrastructure의 구성과 절차를 Code로 정의하고 Version을 관리하는 방식이다. Dockerfile 역시 Application 실행 환경과 Image 생성 과정을 Text로 기록하므로 동일한 Image를 반복해서 Build하고 배포할 수 있다.</p>

<h2 id="2--dockerfile-개요">2 ) Dockerfile 개요</h2>

<hr />

<p>Dockerfile은 Docker Image를 생성하기 위한 Instruction을 순서대로 작성한 Text File이다.</p>

<ul>
  <li>
    <p>Image를 어떤 Base Image와 명령으로 만들었는지 기록할 수 있다.</p>
  </li>
  <li>
    <p>동일한 실행 환경을 반복해서 생성할 수 있다.</p>
  </li>
  <li>
    <p>Source Code와 함께 Version을 관리하고 배포할 수 있다.</p>
  </li>
  <li>
    <p>Container가 시작될 때 실행할 명령과 기본 설정을 정의할 수 있다.</p>
  </li>
</ul>

<p>Dockerfile의 각 Instruction은 Image Layer와 Build Cache에 영향을 준다. 변경 빈도가 낮은 작업을 앞에, Source Code처럼 자주 변경되는 작업을 뒤에 배치하면 Cache를 효율적으로 재사용할 수 있다.</p>

<h2 id="3--dockerfile-주요-instruction">3 ) Dockerfile 주요 Instruction</h2>

<hr />

<h3 id="from">FROM</h3>

<p>Build의 기반이 되는 Base Image를 지정한다. 일반적인 Dockerfile은 <code class="language-plaintext highlighter-rouge">FROM</code>으로 시작하며 Multi-stage Build에서는 여러 번 사용할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:24.04</span>
<span class="k">FROM</span><span class="s"> python:3.13-slim</span>
</code></pre></div></div>

<p>Tag를 생략하면 기본적으로 <code class="language-plaintext highlighter-rouge">latest</code>가 사용되지만 Build 결과를 예측하기 어려워질 수 있으므로 Version Tag나 Digest를 명시하는 편이 좋다. <code class="language-plaintext highlighter-rouge">slim</code>이나 <code class="language-plaintext highlighter-rouge">alpine</code>은 Image 크기를 줄이는 데 도움이 되지만 C Library와 Package 구성이 다르므로 Application 호환성을 먼저 확인한다.</p>

<h3 id="maintainer">MAINTAINER</h3>

<p><code class="language-plaintext highlighter-rouge">MAINTAINER</code>는 Image 작성자 정보를 기록하던 Instruction이다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">MAINTAINER</span><span class="s"> adam.park &lt;itstudy@example.com&gt;</span>
</code></pre></div></div>

<p>현재 <code class="language-plaintext highlighter-rouge">MAINTAINER</code>는 사용이 중단된 Deprecated Instruction이다. 새 Dockerfile에서는 OCI Annotation에 해당하는 <code class="language-plaintext highlighter-rouge">LABEL</code>을 사용한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">LABEL</span><span class="s"> org.opencontainers.image.authors="adam.park &lt;itstudy@example.com&gt;"</span>
</code></pre></div></div>

<h3 id="label">LABEL</h3>

<p>Image의 Version, 설명, License와 같은 Metadata를 Key-Value 형식으로 기록한다. 여러 개를 지정할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">LABEL</span><span class="s"> org.opencontainers.image.version="1.0"</span>
<span class="k">LABEL</span><span class="s"> org.opencontainers.image.description="web service"</span>
<span class="k">LABEL</span><span class="s"> org.opencontainers.image.licenses="MIT"</span>
</code></pre></div></div>

<h3 id="run">RUN</h3>

<p>Image를 Build하는 동안 Package를 설치하거나 File을 변경하는 명령을 실행한다. Shell 형식과 Exec 형식을 사용할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Shell 형식</span>
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> <span class="se">\
</span>      curl <span class="se">\
</span>      nginx <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="c"># Exec 형식</span>
<span class="k">RUN </span><span class="o">[</span><span class="s2">"/bin/bash"</span>, <span class="s2">"-c"</span>, <span class="s2">"echo build-complete &gt; /build.txt"</span><span class="o">]</span>
</code></pre></div></div>

<p>각 <code class="language-plaintext highlighter-rouge">RUN</code>은 Layer를 만들므로 관련 작업은 하나의 <code class="language-plaintext highlighter-rouge">RUN</code>에서 수행하고 Package 목록도 같은 Layer에서 제거한다.</p>

<p><code class="language-plaintext highlighter-rouge">apt autoclean</code>, <code class="language-plaintext highlighter-rouge">apt autoremove</code> 명령은 현재도 사용할 수 있지만 Container Image Build에서는 설치 직후 <code class="language-plaintext highlighter-rouge">/var/lib/apt/lists/*</code>를 제거하고 불필요한 Package를 처음부터 설치하지 않는 방식이 더 명확하다.</p>

<p>Multi-stage Build는 Compiler와 Build 도구가 들어 있는 Stage에서 결과물을 만든 뒤 실행에 필요한 File만 최종 Stage로 복사하여 Image 크기와 공격 표면을 줄이는 방식이다.</p>

<h3 id="cmd">CMD</h3>

<p>Container가 시작될 때 실행할 기본 명령 또는 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>의 기본 인자를 지정한다. 여러 번 작성해도 마지막 <code class="language-plaintext highlighter-rouge">CMD</code>만 적용된다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CMD</span><span class="s"> ["nginx", "-g", "daemon off;"]</span>
<span class="k">CMD</span><span class="s"> ["python", "app.py"]</span>
</code></pre></div></div>

<p>Exec 형식은 불필요한 Shell을 거치지 않아 Signal 전달을 예측하기 쉬우므로 일반적으로 권장된다. <code class="language-plaintext highlighter-rouge">docker run IMAGE COMMAND</code>처럼 Image 뒤에 명령을 전달하면 <code class="language-plaintext highlighter-rouge">CMD</code>를 대체할 수 있다.</p>

<h3 id="entrypoint">ENTRYPOINT</h3>

<p>Container의 주 실행 명령을 지정한다. <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>를 실행 파일로, <code class="language-plaintext highlighter-rouge">CMD</code>를 기본 인자로 조합할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ENTRYPOINT</span><span class="s"> ["python"]</span>
<span class="k">CMD</span><span class="s"> ["runapp.py"]</span>
</code></pre></div></div>

<p>이 Image를 인자 없이 실행하면 <code class="language-plaintext highlighter-rouge">python runapp.py</code>가 실행된다. <code class="language-plaintext highlighter-rouge">docker run IMAGE other.py</code>로 실행하면 <code class="language-plaintext highlighter-rouge">CMD</code>만 바뀌어 <code class="language-plaintext highlighter-rouge">python other.py</code>가 실행된다. <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>도 <code class="language-plaintext highlighter-rouge">docker run --entrypoint</code>로 변경할 수 있으므로 절대 변경할 수 없는 명령이라는 의미는 아니다.</p>

<p>초기화 Script를 사용하는 예시는 다음과 같다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">COPY</span><span class="s"> entrypoint.sh /usr/local/bin/entrypoint.sh</span>
<span class="k">RUN </span><span class="nb">chmod</span> +x /usr/local/bin/entrypoint.sh
<span class="k">ENTRYPOINT</span><span class="s"> ["/usr/local/bin/entrypoint.sh"]</span>
</code></pre></div></div>

<h3 id="copy">COPY</h3>

<p>Build Context의 File이나 Directory를 Image에 복사한다. Build Context 밖의 경로는 복사할 수 없다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">COPY</span><span class="s"> index.html /usr/share/nginx/html/index.html</span>
<span class="k">COPY</span><span class="s"> runapp.py /app/runapp.py</span>
</code></pre></div></div>

<p>단순한 Local File 복사에는 동작이 명확한 <code class="language-plaintext highlighter-rouge">COPY</code>를 우선 사용한다.</p>

<h3 id="add">ADD</h3>

<p><code class="language-plaintext highlighter-rouge">COPY</code>처럼 File을 복사하며 Local Tar Archive의 자동 압축 해제와 Remote URL 등의 추가 기능을 제공한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ADD</span><span class="s"> application.tar.gz /app/</span>
<span class="k">ADD</span><span class="s"> https://example.com/data.json /app/data.json</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ADD</code>는 현재도 사용되는 Instruction이지만 자동 압축 해제 같은 동작이 필요할 때만 사용한다. Remote File은 <code class="language-plaintext highlighter-rouge">RUN curl</code> 또는 <code class="language-plaintext highlighter-rouge">RUN wget</code>으로 내려받으면 검증과 권한 설정 과정을 더 명시적으로 작성할 수 있다.</p>

<h3 id="env">ENV</h3>

<p>Build 과정과 실행될 Container에서 사용할 환경 변수를 설정한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ENV</span><span class="s"> JAVA_HOME=/usr/lib/jvm/java-21-openjdk</span>
<span class="k">ENV</span><span class="s"> PATH="${JAVA_HOME}/bin:${PATH}"</span>
</code></pre></div></div>

<h3 id="expose">EXPOSE</h3>

<p>Container가 어떤 Port와 Protocol에서 요청을 받을지 문서화한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">EXPOSE</span><span class="s"> 80</span>
<span class="k">EXPOSE</span><span class="s"> 8080/tcp</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">EXPOSE</code>만으로 Host Port가 공개되지는 않는다. 외부에서 접근하려면 Container 실행 시 <code class="language-plaintext highlighter-rouge">-p HOST_PORT:CONTAINER_PORT</code> 또는 <code class="language-plaintext highlighter-rouge">-P</code>를 사용해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
      <th>실제 Listen 여부</th>
      <th>Host 외부 접근</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>애플리케이션</td>
      <td>Nginx, Spring Boot 등이 Container 내부 Port에서 요청을 대기한다.</td>
      <td>O</td>
      <td>X</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">EXPOSE 80</code></td>
      <td>Container가 <code class="language-plaintext highlighter-rouge">80</code> Port를 사용한다는 것을 명시하는 메타데이터이다.</td>
      <td>X</td>
      <td>X</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p 8080:80</code></td>
      <td>Host의 <code class="language-plaintext highlighter-rouge">8080</code> Port를 Container의 <code class="language-plaintext highlighter-rouge">80</code> Port에 직접 연결한다.</td>
      <td>X</td>
      <td>O</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-P</code></td>
      <td><code class="language-plaintext highlighter-rouge">EXPOSE</code>로 명시된 Port를 Host의 임의 Port에 자동으로 연결한다.</td>
      <td>X</td>
      <td>O</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">EXPOSE</code>는 Port를 실제로 열거나 Listen 상태로 만드는 명령이 아니다.</p>
    </li>
    <li>
      <p>실제 Listen은 Container 내부의 Application이 수행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">-p</code>는 <code class="language-plaintext highlighter-rouge">EXPOSE</code> 여부와 관계없이 Host와 Container의 Port를 직접 연결한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">-P</code>는 <code class="language-plaintext highlighter-rouge">EXPOSE</code>에 명시된 Port를 기준으로 Host의 임의 Port에 연결한다.</p>
    </li>
  </ul>
</blockquote>

<h3 id="volume">VOLUME</h3>

<p>Container에서 Volume으로 사용할 Mount Point를 지정한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">VOLUME</span><span class="s"> ["/var/log"]</span>
<span class="k">VOLUME</span><span class="s"> ["/var/www/html"]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">VOLUME</code>은 해당 경로를 Host의 <code class="language-plaintext highlighter-rouge">/var/lib/docker</code>와 문자 그대로 Bind Mount하는 명령이 아니다. Container 실행 시 Docker가 관리하는 Anonymous Volume의 Mount Point를 선언한다. Volume 이름, Host Bind Mount 경로 등 구체적인 연결은 <code class="language-plaintext highlighter-rouge">docker run --mount</code>나 Compose File에서 지정하는 편이 관리하기 쉽다.</p>

<h3 id="user">USER</h3>

<p>이후의 <code class="language-plaintext highlighter-rouge">RUN</code>과 Container 실행 시 사용할 사용자 또는 Group을 지정한다. 기본 사용자는 <code class="language-plaintext highlighter-rouge">root</code>이므로 Application에 관리자 권한이 필요하지 않다면 별도 사용자를 만드는 것이 안전하다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">RUN </span>useradd <span class="nt">--create-home</span> appuser
<span class="k">USER</span><span class="s"> appuser</span>
</code></pre></div></div>

<h3 id="workdir">WORKDIR</h3>

<p>이후 <code class="language-plaintext highlighter-rouge">RUN</code>, <code class="language-plaintext highlighter-rouge">CMD</code>, <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>, <code class="language-plaintext highlighter-rouge">COPY</code>, <code class="language-plaintext highlighter-rouge">ADD</code>의 기준 Directory를 지정한다. 경로가 없으면 자동으로 생성된다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">WORKDIR</span><span class="s"> /workspace</span>
<span class="k">COPY</span><span class="s"> . .</span>
</code></pre></div></div>

<h3 id="arg">ARG</h3>

<p>Build할 때만 사용할 변수를 선언하고 <code class="language-plaintext highlighter-rouge">--build-arg</code>로 값을 전달한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ARG</span><span class="s"> DB_NAME=itstudy</span>
<span class="k">RUN </span><span class="nb">echo</span> <span class="s2">"database=</span><span class="k">${</span><span class="nv">DB_NAME</span><span class="k">}</span><span class="s2">"</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">--build-arg</span> <span class="nv">DB_NAME</span><span class="o">=</span>production <span class="nt">-t</span> my-app:1.0 <span class="nb">.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ARG</code>는 비밀번호나 Secret을 안전하게 숨기는 기능이 아니다. 값이 Image History나 Build Cache에 남을 수 있으므로 민감 정보를 전달하는 용도로 사용하지 않는다.</p>

<h3 id="onbuild">ONBUILD</h3>

<p>현재 Image가 다른 Dockerfile의 Base Image로 사용될 때 실행할 Trigger Instruction을 등록한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ONBUILD</span><span class="w"> </span><span class="k">COPY</span><span class="s"> . /app/src</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ONBUILD</code>는 현재도 지원되지만 상속된 동작이 눈에 잘 드러나지 않아 예상하지 못한 Build 실패를 만들 수 있다. 전용 Base Image처럼 사용 목적이 명확한 경우에 제한적으로 사용한다.</p>

<h3 id="stopsignal">STOPSIGNAL</h3>

<p>Container를 중지할 때 주 Process에 전달할 System Call Signal을 지정한다. 기본값은 일반적으로 <code class="language-plaintext highlighter-rouge">SIGTERM</code>이다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">STOPSIGNAL</span><span class="s"> SIGTERM</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">STOPSIGNAL SIGKILL</code>도 문법상 지정할 수 있지만 Process가 종료 작업을 수행할 기회를 잃으므로 일반적인 설정으로는 권장하지 않는다.</p>

<h3 id="shell">SHELL</h3>

<p>Shell 형식의 <code class="language-plaintext highlighter-rouge">RUN</code>, <code class="language-plaintext highlighter-rouge">CMD</code>, <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>에 사용할 기본 Shell을 변경한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SHELL</span><span class="s"> ["/bin/bash", "-c"]</span>
</code></pre></div></div>

<h3 id="healthcheck">HEALTHCHECK</h3>

<p>Container 내부에서 명령을 주기적으로 실행하여 Application의 상태를 확인한다. 여러 번 작성하면 마지막 하나만 적용된다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HEALTHCHECK</span><span class="s"> --interval=1m --timeout=3s --retries=5 \</span>
  CMD curl --fail http://localhost/ || exit 1
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>종료 Code</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0</code></td>
      <td>정상 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">1</code></td>
      <td>비정상 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">2</code></td>
      <td>예약된 값이므로 사용하지 않음</td>
    </tr>
  </tbody>
</table>

<p>기본 <code class="language-plaintext highlighter-rouge">interval</code>과 <code class="language-plaintext highlighter-rouge">timeout</code>은 각각 30초이며 기본 <code class="language-plaintext highlighter-rouge">retries</code>는 3회이다.</p>

<h2 id="4--dockerfile-작성-방법">4 ) Dockerfile 작성 방법</h2>

<hr />

<p>Dockerfile은 보통 다음 원칙으로 작성한다.</p>

<ul>
  <li>
    <p>필요한 Application이나 Runtime이 이미 설치된 신뢰할 수 있는 Base Image를 선택한다.</p>
  </li>
  <li>
    <p>Version Tag를 명시하여 Build 결과를 일정하게 유지한다.</p>
  </li>
  <li>
    <p>변경 빈도가 낮은 Instruction을 먼저 배치하여 Build Cache를 활용한다.</p>
  </li>
  <li>
    <p>Package 설치와 Cache 삭제를 같은 <code class="language-plaintext highlighter-rouge">RUN</code>에서 처리한다.</p>
  </li>
  <li>
    <p>가능한 경우 권한이 제한된 사용자로 Application을 실행한다.</p>
  </li>
  <li>
    <p>Compiler가 필요한 Application은 Multi-stage Build를 고려한다.</p>
  </li>
</ul>

<h2 id="5--image-build">5 ) Image Build</h2>

<hr />

<p>기본 형식은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build [OPTIONS] PATH | URL | -
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 현재 Directory를 Build Context로 사용하고 이름과 Tag 지정</span>
docker build <span class="nt">-t</span> apache-example:1.0 <span class="nb">.</span>

<span class="c"># 다른 이름의 Dockerfile 지정</span>
docker build <span class="nt">-f</span> Dockerfile.dev <span class="nt">-t</span> apache-example:dev <span class="nb">.</span>

<span class="c"># Git Repository를 Build Context로 사용</span>
docker build <span class="nt">-t</span> php-server:2.0 <span class="se">\</span>
  https://github.com/example/docker-phpserver.git

<span class="c"># 표준 입력으로 전달한 Tar Archive를 Build Context로 사용</span>
docker build - &lt; context.tar.gz
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-t</code>는 Image의 이름과 Tag를 지정하고 <code class="language-plaintext highlighter-rouge">-f</code>는 Dockerfile의 경로 또는 이름을 지정한다. 마지막 인자인 <code class="language-plaintext highlighter-rouge">PATH</code>, <code class="language-plaintext highlighter-rouge">URL</code>, <code class="language-plaintext highlighter-rouge">-</code>는 Dockerfile 자체가 아니라 Build Context를 뜻한다. 압축된 Build Context는 <code class="language-plaintext highlighter-rouge">-f</code>가 아니라 표준 입력으로 전달한다.</p>

<h2 id="6--apache-main-page-image">6 ) Apache Main Page Image</h2>

<hr />

<h3 id="indexhtml">index.html</h3>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!doctype html&gt;</span>
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"ko"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"utf-8"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;title&gt;</span>Docker Image Build<span class="nt">&lt;/title&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;h1&gt;</span>Apache Web Server<span class="nt">&lt;/h1&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<h3 id="dockerfile">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> httpd:2.4</span>
<span class="k">COPY</span><span class="s"> index.html /usr/local/apache2/htdocs/index.html</span>
</code></pre></div></div>

<h3 id="build-및-실행">Build 및 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> apache-example:1.0 <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> apache-example <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  apache-example:1.0
</code></pre></div></div>

<p>Browser 또는 <code class="language-plaintext highlighter-rouge">curl http://localhost:8000</code>으로 변경된 Page를 확인한다.</p>

<h2 id="7--nginx-main-page-image">7 ) Nginx Main Page Image</h2>

<hr />

<p>Nginx 공식 Image의 기본 문서 경로는 <code class="language-plaintext highlighter-rouge">/usr/share/nginx/html</code>이다.</p>

<p>본 실습의 <code class="language-plaintext highlighter-rouge">/var/www/html</code>은 Ubuntu 등에 Package로 설치한 Nginx에서 자주 사용하는 경로이며 공식 Docker Image의 기본 경로와 다르다.</p>

<h3 id="dockerfile-1">Dockerfile</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> nginx</span>
<span class="k">COPY</span><span class="s"> index.html /usr/share/nginx/html/index.html</span>
</code></pre></div></div>

<h3 id="build-및-실행-1">Build 및 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> nginx-example:1.0 <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> nginx-example <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9000:80 <span class="se">\</span>
  nginx-example:1.0
</code></pre></div></div>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>Dockerfile과 <code class="language-plaintext highlighter-rouge">index.html</code>을 같은 Build Context에 두고 Image를 Build한다.</p>
    </li>
    <li>
      <p>Apache 공식 Image의 기본 문서 경로는 <code class="language-plaintext highlighter-rouge">/usr/local/apache2/htdocs</code>이다.</p>
    </li>
    <li>
      <p>Nginx 공식 Image의 기본 문서 경로는 <code class="language-plaintext highlighter-rouge">/usr/share/nginx/html</code>이다.</p>
    </li>
    <li>
      <p>Build한 Image를 Container로 실행하고 Host Port를 연결하여 Page를 확인한다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="8--dockerfile을-사용하는-이유">8 ) Dockerfile을 사용하는 이유</h2>

<hr />

<p>Dockerfile을 사용하지 않아도 실행 중인 Container를 수정한 뒤 <code class="language-plaintext highlighter-rouge">docker commit</code>으로 Image를 만들 수 있다. 다음 실습은 Ubuntu Container에 Apache와 PHP를 직접 설치하고 그 결과를 Image로 저장한다.</p>

<h3 id="container를-직접-수정하여-image-생성">Container를 직접 수정하여 Image 생성</h3>

<ol>
  <li>
    <p>Ubuntu Container를 생성하고 Terminal에 접속한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> myweb <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  <span class="nt">-it</span> <span class="se">\</span>
  ubuntu:20.04
</code></pre></div>    </div>
  </li>
  <li>
    <p>Package 정보를 갱신하고 Apache와 PHP를 설치한다. Base Image는 실행에 필요한 최소 Package만 포함하므로 설치 전에 Package Index를 갱신해야 한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt-get update
<span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> apache2 php
service apache2 start
</code></pre></div>    </div>
  </li>
  <li>
    <p>기본 Page를 보관하고 새로운 HTML 및 PHP Page를 작성한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mv</span> /var/www/html/index.html /var/www/html/index.html.org
<span class="nb">echo</span> <span class="s1">'&lt;h1&gt;Hello Docker Application&lt;/h1&gt;'</span> <span class="o">&gt;</span> /var/www/html/index.html
<span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s1">'&lt;?php phpinfo(); ?&gt;'</span> <span class="o">&gt;</span> /var/www/html/index.php
</code></pre></div>    </div>
  </li>
  <li>
    <p>다른 Terminal에서 두 Page에 접속한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
curl http://localhost:8000/index.php
</code></pre></div>    </div>
  </li>
  <li>
    <p>실행 중인 Container를 새로운 Image로 저장한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container commit myweb myphpapp:1.0
</code></pre></div>    </div>
  </li>
  <li>
    <p>저장한 Image로 새로운 Container를 실행한다. 기존 <code class="language-plaintext highlighter-rouge">myweb</code>이 Host의 8000번 Port를 사용하고 있다면 먼저 중지하거나 다른 Host Port를 선택한다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container stop myweb
docker container run <span class="se">\</span>
  <span class="nt">--name</span> myphpapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  myphpapp:1.0
</code></pre></div>    </div>
  </li>
</ol>

<p><code class="language-plaintext highlighter-rouge">docker commit</code>은 현재 Container의 File System 변경 사항을 빠르게 Image로 저장한다. 하지만 설치 명령과 수정 순서가 Image만으로는 드러나지 않으므로 같은 환경을 검토하고 반복해서 만들기 어렵다.</p>

<p>강의에서 사용한 <code class="language-plaintext highlighter-rouge">ubuntu:14.04</code>와 <code class="language-plaintext highlighter-rouge">php5</code> 조합은 과거 환경을 재현하는 Legacy 예제이다. Ubuntu 14.04의 일반 지원은 종료되었으며 PHP 5도 공식 지원이 종료되었다. 실습의 수동 구성 흐름은 유지하되, 실행 가능한 예제에는 <code class="language-plaintext highlighter-rouge">ubuntu:20.04</code>와 해당 배포판에서 제공하는 <code class="language-plaintext highlighter-rouge">php</code> Package를 사용한다.</p>

<h3 id="dockerfile로-동일한-과정-자동화">Dockerfile로 동일한 과정 자동화</h3>

<p>수동 작업을 Dockerfile로 옮기면 Image 생성 절차를 Source Code와 함께 관리할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">LABEL</span><span class="s"> org.opencontainers.image.authors="adam &lt;itstudy@kakao.com&gt;"</span>
<span class="k">LABEL</span><span class="s"> org.opencontainers.image.title="IaC PHP Web Application"</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> <span class="se">\
</span>      apache2 <span class="se">\
</span>      php <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>

<span class="k">ENV</span><span class="s"> APACHE2_RUN_USER=www-data \</span>
    APACHE2_RUN_GROUP=www-data \
    APACHE2_LOG_DIR=/var/log/apache2 \
    APACHE2_WEB_DIR=/var/www/html \
    APACHE2_PID_FILE=/var/run/apache2/apache2.pid

<span class="k">RUN </span><span class="nb">echo</span> <span class="s1">'Hello Docker Application.'</span> <span class="o">&gt;</span> /var/www/html/index.html <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">printf</span> <span class="s1">'%s\n'</span> <span class="s1">'&lt;?php phpinfo(); ?&gt;'</span> <span class="o">&gt;</span> /var/www/html/index.php

<span class="k">WORKDIR</span><span class="s"> /var/www/html</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["/usr/sbin/apache2ctl", "-D", "FOREGROUND"]</span>
</code></pre></div></div>

<p>Image를 Build하고 Container로 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> myphpapp:latest <span class="nb">.</span>
docker container run <span class="se">\</span>
  <span class="nt">--name</span> myphpapp <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 8000:80 <span class="se">\</span>
  myphpapp:latest
</code></pre></div></div>

<p>두 방식의 차이는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Container 수정 후 <code class="language-plaintext highlighter-rouge">commit</code></th>
      <th>Dockerfile Build</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>작업 기록</td>
      <td>별도로 기록해야 함</td>
      <td>Instruction으로 남음</td>
    </tr>
    <tr>
      <td>반복 생성</td>
      <td>수동 작업을 다시 수행</td>
      <td>같은 명령으로 재Build</td>
    </tr>
    <tr>
      <td>변경 검토</td>
      <td>Image 결과만으로 확인하기 어려움</td>
      <td>Text 변경 이력으로 검토 가능</td>
    </tr>
    <tr>
      <td>주요 용도</td>
      <td>임시 상태 저장, 조사</td>
      <td>반복 가능한 Image 제작</td>
    </tr>
  </tbody>
</table>

<p>Dockerfile은 Package, 설정, Source Code와 실행 명령을 선언하므로 다음 장점이 있다.</p>

<ul>
  <li>
    <p>누락하기 쉬운 설치 및 설정 순서를 Code로 고정한다.</p>
  </li>
  <li>
    <p>동일한 실행 환경을 반복해서 생성한다.</p>
  </li>
  <li>
    <p>변경 이력을 Version Control로 관리한다.</p>
  </li>
  <li>
    <p>CI/CD Pipeline에서 대화 없이 Image를 자동으로 Build한다.</p>
  </li>
</ul>

<p>다만 Dockerfile만으로 Database와 Web Application처럼 여러 Container의 실행 관계까지 모두 정의하지는 않는다. 여러 Service의 Network, Volume, 환경 변수와 실행 순서는 Docker Compose 같은 도구로 함께 관리한다.</p>

<h2 id="9--image-build-과정과-주의-사항">9 ) Image Build 과정과 주의 사항</h2>

<hr />

<p>Dockerfile Build는 사용자와 대화하면서 명령을 처리하지 않는다. Docker는 Dockerfile의 Instruction을 위에서 아래로 읽고 각 단계를 자동으로 실행하므로 Package 설치와 설정 명령도 비대화식으로 완료되어야 한다.</p>

<h3 id="package-설치-오류">Package 설치 오류</h3>

<p>다음 Dockerfile은 Package Index를 갱신하지 않아 <code class="language-plaintext highlighter-rouge">Unable to locate package</code> 오류가 발생할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> python3
</code></pre></div></div>

<p>Package Index를 먼저 갱신하더라도 <code class="language-plaintext highlighter-rouge">-y</code>가 없으면 설치 확인 질문에 답할 수 없어 Build가 중단될 수 있다. 두 작업은 같은 <code class="language-plaintext highlighter-rouge">RUN</code>에서 수행한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> python3 <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>
</code></pre></div></div>

<p>이 형식에는 다음 이유가 있다.</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">apt-get update</code>로 현재 Package Index를 내려받는다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">-y</code>로 설치 확인 질문에 자동으로 동의한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">--no-install-recommends</code>로 필수적이지 않은 권장 Package 설치를 줄인다.</p>
  </li>
  <li>
    <p>같은 Layer에서 <code class="language-plaintext highlighter-rouge">/var/lib/apt/lists</code>를 삭제하여 불필요한 Package Index가 Image에 남지 않게 한다.</p>
  </li>
</ul>

<p>원문의 <code class="language-plaintext highlighter-rouge">apt install python</code>은 Ubuntu 20.04에서 의도한 Python 3 Package를 정확히 가리키지 않으므로 <code class="language-plaintext highlighter-rouge">apt-get install python3</code>로 정리했다. Script와 Dockerfile에서는 사용자용 CLI인 <code class="language-plaintext highlighter-rouge">apt</code>보다 안정적인 Interface를 제공하는 <code class="language-plaintext highlighter-rouge">apt-get</code>을 사용한다.</p>

<h3 id="build-context와-dockerignore">Build Context와 <code class="language-plaintext highlighter-rouge">.dockerignore</code></h3>

<p><code class="language-plaintext highlighter-rouge">docker build</code>의 마지막 인자는 Build Context이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> myapp:1.0 <span class="nb">.</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">.</code>을 지정하면 현재 Directory가 Build Context가 된다. 불필요한 File이 많으면 Builder로 전송되는 범위가 커지고 <code class="language-plaintext highlighter-rouge">COPY . .</code>에 의도하지 않은 File이 포함될 수 있으므로 다음 순서로 준비한다.</p>

<ol>
  <li>
    <p>Application Build에 필요한 File만 별도 Directory에 모은다.</p>
  </li>
  <li>
    <p>Dockerfile을 해당 Directory에 작성한다.</p>
  </li>
  <li>
    <p>Log, 가상 환경, Build 산출물과 Secret File을 <code class="language-plaintext highlighter-rouge">.dockerignore</code>에 등록한다.</p>
  </li>
  <li>
    <p>해당 Directory를 Build Context로 지정한다.</p>
  </li>
</ol>

<p>Dockerfile의 이름이나 위치가 기본값과 다르면 <code class="language-plaintext highlighter-rouge">-f</code>로 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="se">\</span>
  <span class="nt">-f</span> docker/Dockerfile.dev <span class="se">\</span>
  <span class="nt">-t</span> myapp:dev <span class="se">\</span>
  <span class="nb">.</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">docker/Dockerfile.dev</code>는 Dockerfile 경로이고 마지막 <code class="language-plaintext highlighter-rouge">.</code>은 Build Context이다.</p>

<h3 id="build-cache">Build Cache</h3>

<p>Docker는 이전 Build 결과를 Cache로 저장하고 Dockerfile의 각 단계에서 재사용할 수 있는지 판단한다. Cache를 효율적으로 사용하려면 변경 빈도가 낮은 File을 먼저 복사하고 자주 변경되는 Source Code는 뒤에서 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>
</code></pre></div></div>

<p>이 순서에서는 Source Code만 변경된 경우 Package 설치 Layer를 다시 사용할 수 있다.</p>

<p>주요 Cache Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--no-cache</code></td>
      <td>기존 Build Cache를 사용하지 않고 모든 단계를 다시 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--cache-from</code></td>
      <td>지정한 외부 Cache Source에서 Cache 가져오기</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--cache-to</code></td>
      <td>Build Cache를 지정한 위치로 내보내기</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">--no-cache</span> <span class="nt">-t</span> myapp:clean <span class="nb">.</span>
docker build <span class="nt">--cache-from</span> myapp:cache <span class="nt">-t</span> myapp:latest <span class="nb">.</span>
</code></pre></div></div>

<h3 id="build-secret">Build Secret</h3>

<p>비밀번호, Token, 인증서 같은 민감 정보는 <code class="language-plaintext highlighter-rouge">ARG</code>, <code class="language-plaintext highlighter-rouge">ENV</code>, <code class="language-plaintext highlighter-rouge">COPY</code>로 Image에 넣지 않는다. 값이 Image Metadata, History 또는 Layer에 남을 수 있기 때문이다. Build 중에만 필요한 Secret은 BuildKit의 Secret Mount로 전달한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># syntax=docker/dockerfile:1</span>
<span class="k">RUN </span><span class="nt">--mount</span><span class="o">=</span><span class="nb">type</span><span class="o">=</span>secret,id<span class="o">=</span>github_token <span class="se">\
</span>    <span class="nv">GITHUB_TOKEN</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span><span class="nb">cat</span> /run/secrets/github_token<span class="si">)</span><span class="s2">"</span> ./download-private-source.sh
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="se">\</span>
  <span class="nt">--secret</span> <span class="nb">id</span><span class="o">=</span>github_token,src<span class="o">=</span>github-token.txt <span class="se">\</span>
  <span class="nt">-t</span> myapp:latest <span class="se">\</span>
  <span class="nb">.</span>
</code></pre></div></div>

<p>Secret을 Mount하면 해당 Build Step에서만 <code class="language-plaintext highlighter-rouge">/run/secrets/github_token</code>으로 읽을 수 있으며 최종 Image에 File로 복사되지 않는다.</p>

<h2 id="10--image-용량-절감">10 ) Image 용량 절감</h2>

<hr />

<p>Image가 커지면 Registry에 저장하는 공간이 늘고 Network를 통한 Push와 Pull에도 시간이 더 걸린다. Ubuntu 같은 OS Base Image에서 Package를 설치할 때는 실행에 필요하지 않은 Package와 Package Manager의 Cache가 최종 Image에 남지 않도록 관리한다.</p>

<h3 id="package-설치와-cache-정리">Package 설치와 Cache 정리</h3>

<p>다음 예제는 Ubuntu에 PHP와 Apache를 설치하고 <code class="language-plaintext highlighter-rouge">index.html</code>을 배치한다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;h1&gt;</span>Hello Docker<span class="nt">&lt;/h1&gt;</span>
</code></pre></div></div>

<p>Package 설치와 정리를 여러 <code class="language-plaintext highlighter-rouge">RUN</code>으로 분리하면 앞선 Layer에 저장된 File이 그대로 남을 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get update
<span class="k">RUN </span><span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> tzdata php apache2
<span class="k">RUN </span>apt-get clean
<span class="k">RUN </span><span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span> /tmp/<span class="k">*</span> /var/tmp/<span class="k">*</span>

<span class="k">WORKDIR</span><span class="s"> /var/www/html</span>
<span class="k">COPY</span><span class="s"> index.html ./</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["apachectl", "-D", "FOREGROUND"]</span>
</code></pre></div></div>

<p>Image Layer의 크기를 줄이려면 Package 설치와 Cache 삭제를 하나의 <code class="language-plaintext highlighter-rouge">RUN</code>에서 처리한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu:20.04</span>

<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nv">DEBIAN_FRONTEND</span><span class="o">=</span>noninteractive apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">--no-install-recommends</span> <span class="se">\
</span>      tzdata <span class="se">\
</span>      php <span class="se">\
</span>      apache2 <span class="o">&amp;&amp;</span> <span class="se">\
</span>    apt-get clean <span class="o">&amp;&amp;</span> <span class="se">\
</span>    <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span> /tmp/<span class="k">*</span> /var/tmp/<span class="k">*</span>

<span class="k">WORKDIR</span><span class="s"> /var/www/html</span>
<span class="k">COPY</span><span class="s"> index.html ./</span>

<span class="k">EXPOSE</span><span class="s"> 80</span>

<span class="k">CMD</span><span class="s"> ["apachectl", "-D", "FOREGROUND"]</span>
</code></pre></div></div>

<p>관련 명령의 역할은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
      <th>Dockerfile에서의 주의 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">apt-get clean</code></td>
      <td>내려받은 Package File 정리</td>
      <td>Package 설치와 같은 Layer에서 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">apt-get autoclean</code></td>
      <td>더 이상 내려받을 수 없는 오래된 Package File 정리</td>
      <td>Build에 필요한 명령은 아님</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">apt-get autoremove</code></td>
      <td>자동 설치된 뒤 불필요해진 의존 Package 제거</td>
      <td>필요한 Runtime 의존성까지 제거하지 않는지 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rm -rf /var/lib/apt/lists/*</code></td>
      <td><code class="language-plaintext highlighter-rouge">apt-get update</code>로 받은 Package Index 삭제</td>
      <td>이후 Package 설치가 필요하면 다시 <code class="language-plaintext highlighter-rouge">apt-get update</code> 실행</td>
    </tr>
  </tbody>
</table>

<p>Host OS에서는 이미 저장된 File을 삭제하면 사용 가능한 Disk 공간이 늘어난다. 반면 Docker Image는 이전 Layer가 보존되므로 후속 Layer에서 File을 삭제하는 것만으로는 앞선 Layer의 크기가 줄지 않는다. 생성한 Layer 안에서 불필요한 File을 함께 제거해야 효과가 있다.</p>

<h2 id="11--dockerfile-작성-시-유의사항">11 ) Dockerfile 작성 시 유의사항</h2>

<hr />

<h3 id="작은-image-구성">작은 Image 구성</h3>

<ul>
  <li>
    <p>Application 실행에 필요한 Runtime을 포함한 <code class="language-plaintext highlighter-rouge">slim</code>, <code class="language-plaintext highlighter-rouge">alpine</code> 등의 Base Image를 검토한다.</p>
  </li>
  <li>
    <p>불필요한 Package를 설치하지 않고 <code class="language-plaintext highlighter-rouge">--no-install-recommends</code> 같은 Option을 활용한다.</p>
  </li>
  <li>
    <p>서로 관련된 Package 설치와 정리 작업은 하나의 <code class="language-plaintext highlighter-rouge">RUN</code>에서 수행한다.</p>
  </li>
  <li>
    <p>Build 도구가 실행 단계에 필요하지 않다면 Multi-stage Build로 분리한다.</p>
  </li>
</ul>

<p>경량 Base Image는 크기를 줄이는 데 도움이 되지만 Library와 Package 구성이 다를 수 있으므로 Application 호환성을 함께 확인한다.</p>

<h3 id="build-cache-활용">Build Cache 활용</h3>

<p>Docker는 변경되지 않은 Layer를 재사용한다. 변경 빈도가 낮은 의존성 File을 먼저 복사하고 자주 변경되는 Source Code를 나중에 복사한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">COPY</span><span class="s"> package*.json ./</span>
<span class="k">RUN </span>npm <span class="nb">install</span>

<span class="k">COPY</span><span class="s"> . .</span>
</code></pre></div></div>

<p>Build Context에는 Image 생성에 필요한 File만 포함한다. <code class="language-plaintext highlighter-rouge">.dockerignore</code>에는 다음과 같은 대상을 등록할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.git/
node_modules/
*.log
.env
</code></pre></div></div>

<h3 id="보안과-관리">보안과 관리</h3>

<ul>
  <li>
    <p>Container Process를 항상 <code class="language-plaintext highlighter-rouge">root</code>로 실행하지 않고 <code class="language-plaintext highlighter-rouge">USER</code>로 일반 사용자를 지정한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">latest</code>에만 의존하지 않고 용도를 확인할 수 있는 Version Tag를 사용한다.</p>
  </li>
  <li>
    <p>비밀번호와 Token은 Dockerfile의 <code class="language-plaintext highlighter-rouge">ARG</code>, <code class="language-plaintext highlighter-rouge">ENV</code>, <code class="language-plaintext highlighter-rouge">COPY</code>에 직접 기록하지 않는다.</p>
  </li>
  <li>
    <p>하나의 Container에는 하나의 주요 관심사를 배치하여 Application 간 결합을 낮춘다.</p>
  </li>
</ul>

<p>하나의 관심사가 반드시 하나의 Process만을 의미하는 것은 아니다. 핵심은 Web Server, Database, Message Broker처럼 독립적으로 배포하고 확장해야 하는 구성 요소를 서로 다른 Container로 분리하는 것이다.</p>

<h3 id="directory-단위의-build-context">Directory 단위의 Build Context</h3>

<p>Image Build는 지정한 Directory를 Build Context로 사용한다. 다음 순서로 작업 Directory를 구성하면 불필요한 File이 Image에 포함되는 것을 줄일 수 있다.</p>

<ol>
  <li>
    <p>Application마다 별도의 Directory를 생성한다.</p>
  </li>
  <li>
    <p>Source Code, 의존성 File과 Dockerfile을 함께 관리한다.</p>
  </li>
  <li>
    <p>Build에 필요하지 않은 File을 <code class="language-plaintext highlighter-rouge">.dockerignore</code>로 제외한다.</p>
  </li>
  <li>
    <p>해당 Directory를 Build Context로 지정한다.</p>
  </li>
</ol>

<p>Dockerfile로 만든 Image는 Container 기반 Serverless Platform에도 배포할 수 있다. 다만 Dockerfile 자체가 Serverless 환경을 생성하거나 Server의 배포·확장·고가용성을 자동으로 관리하는 것은 아니다. 이러한 기능은 Image를 실행하는 Cloud Platform이 제공한다.</p>

<h2 id="12--multi-stage-build">12 ) Multi-stage Build</h2>

<hr />

<p>Multi-stage Build는 Dockerfile을 여러 Build Stage로 나누는 방식이다. 각 Stage는 <code class="language-plaintext highlighter-rouge">FROM</code>으로 시작하며 <code class="language-plaintext highlighter-rouge">AS</code>로 이름을 지정할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="w"> </span><span class="s">build-image</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>

<span class="c"># 의존성 설치와 Application Build</span>

<span class="k">FROM</span><span class="s"> runtime-image</span>

<span class="k">COPY</span><span class="s"> --from=builder /path/to/artifact /app/artifact</span>

<span class="c"># Application 실행</span>
</code></pre></div></div>

<p>동작 순서는 다음과 같다.</p>

<ol>
  <li>
    <p>Builder Stage에서 Compiler, Package Manager와 Build 도구를 사용한다.</p>
  </li>
  <li>
    <p>Source Code를 Build하여 JAR, 실행 Binary 또는 정적 File 같은 산출물을 생성한다.</p>
  </li>
  <li>
    <p>Runtime Stage를 새로운 <code class="language-plaintext highlighter-rouge">FROM</code>으로 시작한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">COPY --from=builder</code>로 실행에 필요한 산출물만 가져온다.</p>
  </li>
  <li>
    <p>최종 Image에는 Builder와 중간 File을 포함하지 않는다.</p>
  </li>
</ol>

<p>각 Stage는 독립된 File System을 사용하지만 앞선 Stage의 File을 선택적으로 복사할 수 있다. 최종 Image는 마지막 Stage를 기준으로 생성되므로 Compiler와 Build Cache를 제외하여 Image 크기와 공격 표면을 줄일 수 있다.</p>

<p>Node.js, Spring Boot, Go, Python, React에 Multi-stage Build를 적용하는 구체적인 차이는 다음 문서에서 다룬다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Dockerfile은 Image 생성 과정을 Code로 기록하여 동일한 환경을 반복해서 Build할 수 있게 한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">FROM</code>으로 Base Image를 지정하고 <code class="language-plaintext highlighter-rouge">RUN</code>, <code class="language-plaintext highlighter-rouge">COPY</code>, <code class="language-plaintext highlighter-rouge">ENV</code> 등으로 실행 환경을 구성한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>는 주 실행 명령을, <code class="language-plaintext highlighter-rouge">CMD</code>는 기본 명령이나 기본 인자를 정의한다.</p>
    </li>
    <li>
      <p>Dockerfile의 Instruction 순서는 Image Layer와 Build Cache에 영향을 준다.</p>
    </li>
    <li>
      <p>Package 설치와 Cache 삭제를 같은 Layer에서 수행해야 Image 용량을 줄이는 데 효과가 있다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">.dockerignore</code>로 불필요한 Build Context를 제외하고 Build Secret은 전용 Secret Mount로 전달한다.</p>
    </li>
    <li>
      <p>Multi-stage Build는 Build 환경과 Runtime 환경을 분리하여 최종 Image에 필요한 산출물만 남긴다.</p>
    </li>
    <li>
      <p>Dockerfile은 Serverless Platform에 배포할 Image를 만들 수 있지만 확장과 운영 자동화는 실행 Platform의 역할이다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">MAINTAINER</code>처럼 현재 Deprecated된 Instruction은 그대로 이해하되 새 Dockerfile에서는 대체 방법을 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Dockerfile의 주요 Instruction과 Image Build 방법 및 작성 시 주의 사항]]></summary></entry><entry><title type="html">Docker 컨테이너</title><link href="https://kkr010128.github.io/cloud-native-11-docker-container/" rel="alternate" type="text/html" title="Docker 컨테이너" /><published>2026-08-17T00:00:00+09:00</published><updated>2026-08-17T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-11-docker-container</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-11-docker-container/"><![CDATA[<h2 id="1--docker-container">1 ) Docker Container</h2>

<hr />

<p>Docker Image는 Application 실행에 필요한 File System을 담은 읽기 전용 Template이다. Docker Engine은 Image Layer 위에 읽고 쓸 수 있는 Container Layer를 추가하여 Container를 생성한다.</p>

<p>Container는 독립된 Virtual Machine이 아니라 Host의 Kernel을 공유하면서 격리된 공간에서 실행되는 Process이다. Image가 실행에 필요한 Content를 제공하고, Container의 주 Process가 실제 Application을 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container Layer     ← Read / Write
-----------------
Image Layer         ← Read Only
Image Layer         ← Read Only
Base Image Layer    ← Read Only
</code></pre></div></div>

<p>Container Format과 Runtime의 개방형 업계 표준은 OCI(Open Container Initiative)에서 정의한다. OCI는 Linux Foundation 산하 Project이며, Docker를 포함한 여러 Container 도구가 상호 운용될 수 있는 기반을 제공한다.</p>

<h2 id="2--container-생성과-수동-제어">2 ) Container 생성과 수동 제어</h2>

<hr />

<p>Image 다운로드, Container 생성, 시작은 각각 별도의 명령으로 수행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Image 다운로드</span>
docker image pull ubuntu:24.04

<span class="c"># Container 생성</span>
docker container create <span class="nt">-it</span> <span class="nt">--name</span> container-test1 ubuntu:24.04

<span class="c"># 정지된 Container까지 모두 확인</span>
docker container <span class="nb">ls</span> <span class="nt">-a</span>

<span class="c"># Container 시작</span>
docker container start container-test1

<span class="c"># 실행 중인 Container 확인</span>
docker container <span class="nb">ls</span>

<span class="c"># Container의 표준 입출력에 연결</span>
docker container attach container-test1

<span class="c"># 정지된 Container 삭제</span>
docker container <span class="nb">rm </span>container-test1
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker ps</code>는 <code class="language-plaintext highlighter-rouge">docker container ls</code>와 같은 역할을 한다. 기본 출력에는 실행 중인 Container만 표시되고, <code class="language-plaintext highlighter-rouge">-a</code>를 사용하면 정지된 Container도 함께 표시된다.</p>

<p><code class="language-plaintext highlighter-rouge">attach</code>는 Container의 주 Process에 연결한다. 연결한 Terminal에서 종료 명령을 실행하면 주 Process와 Container가 함께 종료될 수 있다. 실행 중인 Container에서 별도의 Shell을 사용하려면 <code class="language-plaintext highlighter-rouge">docker container exec</code>가 더 적합하다.</p>

<h2 id="3--container-생명주기">3 ) Container 생명주기</h2>

<hr />

<p>Container의 기본 생명주기는 생성, 실행, 중지, 삭제 단계로 구분할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image
  ↓ create
Created
  ↓ start
Running
  ↓ stop 또는 Process 종료
Stopped
  ↓ rm
Removed
</code></pre></div></div>

<h3 id="생성">생성</h3>

<p><code class="language-plaintext highlighter-rouge">docker container create</code>는 Image를 기반으로 Container를 만들지만 주 Process를 시작하지는 않는다. 생성된 Container에는 이름, Network 설정, Mount 정보와 쓰기 가능한 Container Layer가 준비된다.</p>

<h3 id="실행">실행</h3>

<p><code class="language-plaintext highlighter-rouge">docker container start</code> 또는 <code class="language-plaintext highlighter-rouge">docker container run</code>으로 Container를 시작하면 Image에 정의된 <code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>와 <code class="language-plaintext highlighter-rouge">CMD</code>를 바탕으로 주 Process가 실행된다.</p>

<p>Web Server처럼 계속 동작하는 Process가 실행되면 Container도 실행 상태를 유지한다. 반면 명령을 한 번 수행하고 끝나는 Process라면 해당 Process가 종료될 때 Container도 정지한다.</p>

<h3 id="중지">중지</h3>

<p>사용자가 Container를 중지하거나 주 Process가 정상 또는 오류 상태로 종료되면 Container는 정지 상태가 된다. 정지된 Container의 쓰기 Layer와 설정은 남아 있으므로 다시 시작할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container stop CONTAINER
docker container start CONTAINER
</code></pre></div></div>

<h3 id="삭제">삭제</h3>

<p>정지된 Container는 명시적으로 삭제하기 전까지 Disk에 남는다. 삭제하면 해당 Container는 다시 시작할 수 없고 쓰기 Layer의 데이터도 사라진다. 보존해야 하는 데이터는 Volume 또는 Bind Mount에 저장한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">rm </span>CONTAINER
docker container <span class="nb">rm</span> <span class="nt">--force</span> CONTAINER
</code></pre></div></div>

<h2 id="4--application-실행">4 ) Application 실행</h2>

<hr />

<h3 id="docker-run">docker run</h3>

<p><code class="language-plaintext highlighter-rouge">docker container run</code>은 필요한 Image가 Local에 없으면 내려받고, 새 Container를 생성한 뒤 바로 시작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image pull
        +
docker container create
        +
docker container start
        ↓
docker container run
</code></pre></div></div>

<p>기본 형식은 다음과 같다.</p>

<pre><code class="language-txt">docker container run [OPTIONS] IMAGE [COMMAND] [ARG...]
</code></pre>

<p>자주 사용하는 Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name NAME</code></td>
      <td>Container 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code>, <code class="language-plaintext highlighter-rouge">--detach</code></td>
      <td>Background에서 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code>, <code class="language-plaintext highlighter-rouge">--interactive</code></td>
      <td>표준 입력을 열린 상태로 유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-t</code>, <code class="language-plaintext highlighter-rouge">--tty</code></td>
      <td>가상 Terminal 할당</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e</code>, <code class="language-plaintext highlighter-rouge">--env</code></td>
      <td>환경 변수 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p HOST:CONTAINER</code></td>
      <td>Host Port와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-P</code>, <code class="language-plaintext highlighter-rouge">--publish-all</code></td>
      <td>Image가 공개한 Port를 임의의 Host Port에 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code>, <code class="language-plaintext highlighter-rouge">--volume</code></td>
      <td>Volume 또는 Host 경로 Mount</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--network NETWORK</code></td>
      <td>Container가 연결될 Network 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--link CONTAINER[:ALIAS]</code></td>
      <td>다른 Container의 정보를 연결하는 Legacy Option</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--hostname NAME</code></td>
      <td>Container Hostname 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--restart POLICY</code></td>
      <td>Container 재시작 정책 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--rm</code></td>
      <td>Container 종료 시 자동 삭제</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">-i</code>와 <code class="language-plaintext highlighter-rouge">-t</code>는 역할이 다르지만 대화형 Shell을 사용할 때 보통 <code class="language-plaintext highlighter-rouge">-it</code>로 함께 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--rm</span> <span class="nt">-it</span> ubuntu:24.04 /bin/bash
</code></pre></div></div>

<p>Server Application은 Terminal을 점유하지 않도록 주로 <code class="language-plaintext highlighter-rouge">-d</code>로 실행한다. 다만 <code class="language-plaintext highlighter-rouge">-d</code>가 Process를 Daemon으로 바꾸는 것은 아니며, Docker가 Container를 Background에서 실행하고 ID를 반환하는 Option이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> nginx <span class="nt">-d</span> nginx:1.18
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--restart</code>에는 다음과 같은 정책을 지정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>정책</th>
      <th>동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">no</code></td>
      <td>자동으로 다시 시작하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">on-failure[:N]</code></td>
      <td>Process가 오류 상태로 끝나면 지정 횟수까지 재시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">always</code></td>
      <td>종료되면 항상 재시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unless-stopped</code></td>
      <td>사용자가 직접 중지하지 않았다면 재시작</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">--link</code>는 현재도 호환성을 위해 남아 있지만 Legacy 기능이다. 새 구성에서는 사용자 정의 Network에 Container를 연결하고 내장 DNS를 통해 Container 이름으로 통신하는 방식을 사용한다.</p>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p>Container는 생성, 실행, 중지, 삭제의 생명주기를 가진다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker run</code>은 Image를 준비하고 새 Container를 생성한 뒤 시작한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">-it</code>는 대화형 Terminal에, <code class="language-plaintext highlighter-rouge">-d</code>는 Background 실행에 사용한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">--link</code>는 Legacy 기능이므로 새 구성에서는 사용자 정의 Network를 사용한다.</p>
    </li>
  </ul>
</blockquote>

<h2 id="5--container-조회와-관리">5 ) Container 조회와 관리</h2>

<hr />

<h3 id="container-목록">Container 목록</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">ls
</span>docker ps
</code></pre></div></div>

<p><img src="../../assets/post/2026-08-17-cloud-native-11-docker-container/02.webp" alt="" /></p>

<p>출력 항목의 의미는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CONTAINER ID</code></td>
      <td>Container 식별자의 축약 값</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">IMAGE</code></td>
      <td>Container 생성에 사용한 Image</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">COMMAND</code></td>
      <td>Container의 주 Process를 시작한 명령</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CREATED</code></td>
      <td>Container를 생성한 뒤 지난 시간</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">STATUS</code></td>
      <td>실행, 종료 등 현재 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PORTS</code></td>
      <td>공개 및 연결된 Port 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">NAMES</code></td>
      <td>Container 이름</td>
    </tr>
  </tbody>
</table>

<p>주요 Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-a</code>, <code class="language-plaintext highlighter-rouge">--all</code></td>
      <td>정지된 Container까지 모두 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-q</code>, <code class="language-plaintext highlighter-rouge">--quiet</code></td>
      <td>Container ID만 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-l</code>, <code class="language-plaintext highlighter-rouge">--latest</code></td>
      <td>가장 최근에 생성한 Container 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-n NUMBER</code>, <code class="language-plaintext highlighter-rouge">--last NUMBER</code></td>
      <td>최근에 생성한 Container를 지정한 수만큼 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-s</code>, <code class="language-plaintext highlighter-rouge">--size</code></td>
      <td>Container File System 크기 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--no-trunc</code></td>
      <td>ID와 명령을 줄이지 않고 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--filter</code></td>
      <td>조건에 맞는 Container만 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--format</code></td>
      <td>출력 형식 지정</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
docker container <span class="nb">ls</span> <span class="nt">-a</span>
docker container <span class="nb">ls</span> <span class="nt">-aq</span>
docker container <span class="nb">ls</span> <span class="nt">--filter</span> <span class="nv">status</span><span class="o">=</span>running
docker container <span class="nb">ls</span> <span class="nt">--format</span> <span class="s1">'{{.ID}} {{.Names}}'</span>

</code></pre></div></div>

<p>여러 Container ID를 명령 치환으로 전달할 수도 있다. 다음 명령은 실행 중인 모든 Container를 중지한 뒤 모든 Container를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container stop <span class="si">$(</span>docker container <span class="nb">ls</span> <span class="nt">-q</span><span class="si">)</span>
docker container <span class="nb">rm</span> <span class="si">$(</span>docker container <span class="nb">ls</span> <span class="nt">-aq</span><span class="si">)</span>
</code></pre></div></div>

<p>대상이 없으면 명령에 인수가 전달되지 않아 오류가 발생할 수 있다. 실제 환경에서는 목록을 먼저 확인한 뒤 일괄 작업을 수행한다.</p>

<h3 id="resource와-log-확인">Resource와 Log 확인</h3>

<p><code class="language-plaintext highlighter-rouge">docker container stats</code>는 CPU, Memory, Network I/O 등 Container의 Resource 사용량을 실시간으로 보여준다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container stats
docker container stats webserver
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker container logs</code>는 Container Process가 표준 출력과 표준 오류에 기록한 Log를 확인한다. <code class="language-plaintext highlighter-rouge">-f</code>를 사용하면 새 Log를 계속 따라간다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container logs webserver
docker container logs <span class="nt">-f</span> webserver
</code></pre></div></div>

<h2 id="6--port-공개와-web-server-실습">6 ) Port 공개와 Web Server 실습</h2>

<hr />

<p>Container는 격리된 Network 공간에서 실행되므로 Container 내부의 Port가 Host에 자동으로 공개되지는 않는다. 외부에서 Service에 접근하려면 <code class="language-plaintext highlighter-rouge">-p</code>로 Host Port를 Container Port에 연결한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client → Host:9000 → Container:80 → Web Server
</code></pre></div></div>

<h3 id="nginx-실행">Nginx 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> webserver <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9000:80 <span class="se">\</span>
  nginx
</code></pre></div></div>

<p>Browser에서 <code class="language-plaintext highlighter-rouge">http://localhost:9000</code>에 접속하면 Container의 80번 Port로 요청이 전달된다.</p>

<h3 id="apache-http-server-실행">Apache HTTP Server 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="se">\</span>
  <span class="nt">--name</span> apacheweb <span class="se">\</span>
  <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">-p</span> 9000:80 <span class="se">\</span>
  httpd
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-p 9000:80</code>의 왼쪽은 Host Port이고 오른쪽은 Container Port이다. 동일한 Host의 같은 Port를 여러 Container에 동시에 연결할 수는 없다.</p>

<h2 id="7--base-image">7 ) Base Image</h2>

<hr />

<p>Dockerfile은 목적에 맞는 Base Image에서 시작한다. 필요한 Runtime과 배포 환경을 고려해 적절한 Image와 Version Tag를 선택한다.</p>

<table>
  <thead>
    <tr>
      <th>분류</th>
      <th>대표 Image</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Operating System</td>
      <td><code class="language-plaintext highlighter-rouge">ubuntu</code>, <code class="language-plaintext highlighter-rouge">debian</code>, <code class="language-plaintext highlighter-rouge">fedora</code>, <code class="language-plaintext highlighter-rouge">alpine</code>, <code class="language-plaintext highlighter-rouge">busybox</code></td>
    </tr>
    <tr>
      <td>Web Server</td>
      <td><code class="language-plaintext highlighter-rouge">httpd</code>, <code class="language-plaintext highlighter-rouge">nginx</code></td>
    </tr>
    <tr>
      <td>Database</td>
      <td><code class="language-plaintext highlighter-rouge">mysql</code>, <code class="language-plaintext highlighter-rouge">postgres</code>, <code class="language-plaintext highlighter-rouge">mariadb</code>, <code class="language-plaintext highlighter-rouge">redis</code>, <code class="language-plaintext highlighter-rouge">mongo</code></td>
    </tr>
    <tr>
      <td>Language Runtime</td>
      <td><code class="language-plaintext highlighter-rouge">eclipse-temurin</code>, <code class="language-plaintext highlighter-rouge">python</code>, <code class="language-plaintext highlighter-rouge">php</code>, <code class="language-plaintext highlighter-rouge">ruby</code>, <code class="language-plaintext highlighter-rouge">golang</code>, <code class="language-plaintext highlighter-rouge">node</code></td>
    </tr>
    <tr>
      <td>Service</td>
      <td><code class="language-plaintext highlighter-rouge">registry</code>, <code class="language-plaintext highlighter-rouge">nextcloud</code></td>
    </tr>
  </tbody>
</table>

<p>Alpine과 BusyBox는 크기가 작은 Linux 환경이지만, Library와 Package 구성이 Ubuntu나 Debian과 다르다. 단순히 크기만 비교하지 않고 Application 호환성도 함께 확인한다.</p>

<h2 id="8--host와-container-사이의-파일-복사">8 ) Host와 Container 사이의 파일 복사</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">docker container cp</code>는 Host와 Container 사이에서 File 또는 Directory를 복사한다. 앞의 경로가 원본이고 뒤의 경로가 목적지이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Host에서 Container로 복사</span>
docker container <span class="nb">cp </span>HOST_PATH CONTAINER:CONTAINER_PATH

<span class="c"># Container에서 Host로 복사</span>
docker container <span class="nb">cp </span>CONTAINER:CONTAINER_PATH HOST_PATH
</code></pre></div></div>

<h3 id="apache-기본-page-변경">Apache 기본 Page 변경</h3>

<p>Apache HTTP Server Image의 기본 문서 경로는 <code class="language-plaintext highlighter-rouge">/usr/local/apache2/htdocs</code>이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Container 실행</span>
docker container run <span class="nt">--name</span> webserver <span class="nt">-d</span> <span class="nt">-p</span> 9000:80 httpd

<span class="c"># 기존 Page를 Host로 복사</span>
docker container <span class="nb">cp</span> <span class="se">\</span>
  webserver:/usr/local/apache2/htdocs/index.html <span class="se">\</span>
  index.html

<span class="c"># Host에서 수정한 Page를 다시 Container로 복사</span>
docker container <span class="nb">cp</span> <span class="se">\</span>
  index.html <span class="se">\</span>
  webserver:/usr/local/apache2/htdocs/index.html
</code></pre></div></div>

<h3 id="nginx-기본-page-변경">Nginx 기본 Page 변경</h3>

<p>Nginx Image의 기본 문서 경로는 <code class="language-plaintext highlighter-rouge">/usr/share/nginx/html</code>이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> nginx <span class="nt">-d</span> <span class="nt">-p</span> 9001:80 nginx

docker container <span class="nb">cp</span> <span class="se">\</span>
  nginx:/usr/share/nginx/html/index.html <span class="se">\</span>
  nginx.html

docker container <span class="nb">cp</span> <span class="se">\</span>
  nginx.html <span class="se">\</span>
  nginx:/usr/share/nginx/html/index.html
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker cp</code>로 변경한 File은 Container의 쓰기 Layer에 저장되므로 Container를 삭제하면 사라진다. 개발 중 File을 반복해서 수정하거나 데이터를 영구 보관해야 한다면 Bind Mount 또는 Volume을 사용한다.</p>

<h2 id="9--실행-중인-container에서-명령-수행">9 ) 실행 중인 Container에서 명령 수행</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">docker container exec</code>는 실행 중인 Container 안에서 새로운 명령을 실행한다.</p>

<pre><code class="language-txt">docker container exec [OPTIONS] CONTAINER COMMAND [ARG...]
</code></pre>

<p>Container 안에서 대화형 Shell을 실행하는 예시는 다음과 같다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> ubuntu-server <span class="nt">-dit</span> ubuntu:24.04
docker container <span class="nb">exec</span> <span class="nt">-it</span> ubuntu-server /bin/bash
</code></pre></div></div>

<h3 id="attach와-exec의-차이">attach와 exec의 차이</h3>

<blockquote>
  <p>두 명령 모두 실행 중인 Container와 상호작용하지만 연결되는 Process가 다르다.</p>
</blockquote>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">docker attach</code></th>
      <th><code class="language-plaintext highlighter-rouge">docker exec</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>동작</td>
      <td>기존 주 Process의 표준 입력·출력·오류에 Terminal을 연결</td>
      <td>Container 안에서 새로운 Process 실행</td>
    </tr>
    <tr>
      <td>대상</td>
      <td><code class="language-plaintext highlighter-rouge">ENTRYPOINT</code>와 <code class="language-plaintext highlighter-rouge">CMD</code>로 시작된 주 Process</td>
      <td>사용자가 지정한 Shell 또는 명령</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">exit</code>의 영향</td>
      <td>주 Process가 Shell이라면 Shell과 Container가 함께 종료</td>
      <td>새로 실행한 Shell만 종료되고 주 Process는 계속 실행</td>
    </tr>
    <tr>
      <td>주요 용도</td>
      <td>주 Process와 직접 대화하거나 현재 출력을 확인</td>
      <td>점검, Debugging, File 확인, 관리 명령 수행</td>
    </tr>
  </tbody>
</table>

<p>Container가 다음과 같이 실행되었다고 가정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> attach-test <span class="nt">-dit</span> ubuntu:24.04 /bin/bash
</code></pre></div></div>

<p>이때 <code class="language-plaintext highlighter-rouge">/bin/bash</code>는 Container를 유지하는 주 Process, 즉 Container 내부의 PID 1이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container attach attach-test
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">attach</code>는 새 Shell을 만들지 않고 이미 PID 1로 실행 중인 <code class="language-plaintext highlighter-rouge">/bin/bash</code>에 현재 Terminal을 연결한다. 여기에서 <code class="language-plaintext highlighter-rouge">exit</code>를 입력하면 PID 1인 Bash가 종료되고, 주 Process가 사라졌으므로 Container도 정지한다. Container를 계속 실행하면서 Terminal만 분리하려면 일반적으로 <code class="language-plaintext highlighter-rouge">Ctrl-p</code>, <code class="language-plaintext highlighter-rouge">Ctrl-q</code>를 차례로 입력한다. <code class="language-plaintext highlighter-rouge">Ctrl-c</code>는 설정과 주 Process의 Signal 처리 방식에 따라 Container를 종료할 수 있으므로 주의한다.</p>

<p>반면 다음 명령은 같은 Container 안에 새로운 Bash Process를 하나 더 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container <span class="nb">exec</span> <span class="nt">-it</span> attach-test /bin/bash
</code></pre></div></div>

<p>이 Shell은 PID 1이 아니다. 따라서 여기서 <code class="language-plaintext highlighter-rouge">exit</code>를 입력하면 <code class="language-plaintext highlighter-rouge">exec</code>로 만든 Bash만 종료되고 원래의 PID 1은 계속 실행되므로 Container도 실행 상태를 유지한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>attach
Terminal ─────────→ PID 1: /bin/bash
                         └─ exit → 주 Process 종료 → Container 정지

exec
PID 1: /bin/bash   ← 계속 실행
└─ 새 Process: /bin/bash ← Terminal 연결
                         └─ exit → 새 Process만 종료
</code></pre></div></div>

<p>Web Server Container도 같은 원리로 동작한다. <code class="language-plaintext highlighter-rouge">attach</code>를 실행하면 Nginx나 Apache 주 Process의 입출력에 연결될 뿐 Shell이 생기지 않는다. 반면 <code class="language-plaintext highlighter-rouge">exec -it CONTAINER sh</code>는 점검에 사용할 별도의 Shell을 실행한다. 이 때문에 운영 중인 Service를 중단할 위험이 비교적 작고 원하는 명령을 바로 실행할 수 있는 <code class="language-plaintext highlighter-rouge">exec</code>를 실무에서 더 자주 사용한다고 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Nginx 주 Process에 연결</span>
docker container attach webserver

<span class="c"># Nginx Container 내부에 점검용 Shell을 별도로 실행</span>
docker container <span class="nb">exec</span> <span class="nt">-it</span> webserver /bin/sh

<span class="c"># Shell 없이 설정 File만 확인</span>
docker container <span class="nb">exec </span>webserver nginx <span class="nt">-T</span>
</code></pre></div></div>

<p>다만 <code class="language-plaintext highlighter-rouge">exec</code>는 Container의 주 Process가 실행 중일 때만 사용할 수 있다. Container가 이미 정지했다면 먼저 <code class="language-plaintext highlighter-rouge">docker logs</code>나 <code class="language-plaintext highlighter-rouge">docker inspect</code>로 종료 원인을 확인하고 필요에 따라 Container를 다시 시작해야 한다. 단순히 Application Log를 확인하려는 목적이라면 주 Process에 직접 연결하는 <code class="language-plaintext highlighter-rouge">attach</code>보다 <code class="language-plaintext highlighter-rouge">docker logs</code>가 안전하고 편리하다.</p>

<blockquote>
  <p><strong>중간 정리</strong></p>

  <ul>
    <li>
      <p><code class="language-plaintext highlighter-rouge">attach</code>는 Container의 기존 주 Process에 Terminal을 연결한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">exec</code>는 실행 중인 Container 안에서 새로운 Process를 실행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">attach</code>로 연결한 주 Process를 종료하면 Container도 정지할 수 있다.</p>
    </li>
    <li>
      <p>점검 명령이나 별도 Shell 실행에는 <code class="language-plaintext highlighter-rouge">exec</code>를 주로 사용한다.</p>
    </li>
  </ul>
</blockquote>

<h3 id="python-program-실행">Python Program 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> python-test <span class="nt">-dit</span> python:3
docker container <span class="nb">cp </span>lotto.py python-test:/lotto.py
docker container <span class="nb">exec</span> <span class="nt">-it</span> python-test python /lotto.py
</code></pre></div></div>

<h3 id="nodejs-program-실행">Node.js Program 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--name</span> nodejs <span class="nt">-dit</span> node
docker container <span class="nb">cp </span>index.js nodejs:/app-node.js
docker container <span class="nb">exec</span> <span class="nt">-it</span> nodejs node /app-node.js
</code></pre></div></div>

<p>일회성 Program이라면 Container를 계속 실행해 두고 <code class="language-plaintext highlighter-rouge">exec</code>하는 대신 Source Code를 Mount하고 <code class="language-plaintext highlighter-rouge">docker run --rm</code>으로 실행하는 방식도 사용할 수 있다.</p>

<h2 id="10--container를-image로-저장">10 ) Container를 Image로 저장</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">docker container commit</code>은 Container의 변경된 File System을 새 Image로 저장한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container commit CONTAINER REPOSITORY[:TAG]
docker container commit nodejs my-node-app:1.0
docker image <span class="nb">ls </span>my-node-app
</code></pre></div></div>

<p>실습 중 변경 사항을 임시로 보관하거나 상태를 분석할 때 사용할 수 있지만, 실행한 명령과 변경 이유를 재현하기 어렵다. 배포용 Image는 변경 과정을 코드로 관리할 수 있도록 Dockerfile로 Build하는 것이 좋다.</p>

<p>Mount에 저장된 데이터는 Container File System에 포함되지 않으므로 <code class="language-plaintext highlighter-rouge">commit</code>으로 만든 Image에도 포함되지 않는다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Container는 Image에 쓰기 가능한 Layer를 추가하여 생성한 격리된 Process 실행 환경이다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker run</code>은 Image Pull, Container 생성, 시작 과정을 한 번에 수행한다.</p>
    </li>
    <li>
      <p>Container의 주 Process가 종료되면 Container도 정지하며, 삭제 전에는 다시 시작할 수 있다.</p>
    </li>
    <li>
      <p>외부에서 Service에 접근하려면 Host Port와 Container Port를 연결한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker cp</code>는 Host와 Container 사이에서 File을 복사하고 <code class="language-plaintext highlighter-rouge">docker exec</code>는 실행 중인 Container에서 새 Process를 실행한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">docker commit</code>으로 Container 상태를 Image로 저장할 수 있지만 반복 가능한 Image 생성에는 Dockerfile을 사용한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Container의 생명주기와 실행 방법, Port 공개, 명령 실행 및 파일 복사]]></summary></entry><entry><title type="html">Docker와 Container 가상화</title><link href="https://kkr010128.github.io/cloud-native-08-docker-overview/" rel="alternate" type="text/html" title="Docker와 Container 가상화" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-08-docker-overview</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-08-docker-overview/"><![CDATA[<h2 id="1--가상화">1 ) 가상화</h2>

<hr />

<p>가상화(Virtualization)는 <strong>물리적 시스템의 CPU, Memory, Storage, Network 같은 자원을 추상화하여 여러 가상 환경에서 사용할 수 있게 하는 기술</strong>이다.</p>

<p>가상화 소프트웨어는 물리적 Hardware의 기능을 논리적으로 분리한다. 하나의 물리적인 Computer에서도 여러 가상 System을 동시에 실행할 수 있으므로 Hardware Resource의 활용도를 높이고 Infrastructure를 유연하게 관리할 수 있다.</p>

<pre><code class="language-mermaid">flowchart TB
    A["물리적 컴퓨터"] --&gt; B["가상화 소프트웨어"]
    B --&gt; C["가상 시스템 1"]
    B --&gt; D["가상 시스템 2"]
    B --&gt; E["가상 시스템 3"]
</code></pre>

<h3 id="가상화의-장점">가상화의 장점</h3>

<ul>
  <li><strong>효율적인 리소스 사용</strong>
    <ul>
      <li>하나의 물리적 System을 여러 환경이 공유하여 Resource를 효율적으로 사용한다.</li>
    </ul>
  </li>
  <li><strong>자동화된 IT 관리</strong>
    <ul>
      <li>가상 환경의 생성, 배포 및 관리를 자동화하기 쉽다.</li>
    </ul>
  </li>
  <li><strong>신속한 재해 복구</strong>
    <ul>
      <li>Snapshot과 복제된 환경을 이용하여 장애 발생 시 빠르게 복구할 수 있다.</li>
    </ul>
  </li>
</ul>

<h3 id="가상화-서비스">가상화 서비스</h3>

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Server 가상화</td>
      <td>하나의 물리적 Server를 여러 가상 Server로 분리</td>
    </tr>
    <tr>
      <td>Storage 가상화</td>
      <td>여러 Storage Resource를 하나의 논리적 Storage처럼 구성</td>
    </tr>
    <tr>
      <td>Network 가상화</td>
      <td>물리적 Network를 논리적으로 분리하고 Software로 제어</td>
    </tr>
    <tr>
      <td>Data 가상화</td>
      <td>여러 위치의 Data를 하나의 Data Source처럼 제공</td>
    </tr>
    <tr>
      <td>Application 가상화</td>
      <td>Application을 운영체제와 분리된 환경에서 실행</td>
    </tr>
    <tr>
      <td>Desktop 가상화</td>
      <td>Desktop 환경을 중앙 Server에서 제공</td>
    </tr>
  </tbody>
</table>

<p>Network 가상화의 대표적인 기술로 SDN(Software-Defined Networking)이 있다. SDN은 물리적 장비가 담당하던 Traffic 제어 기능을 Software로 분리하여 Routing 정책을 중앙에서 관리한다. 예를 들어 영상 통화 Traffic을 일반 Application Traffic보다 우선 처리하도록 구성할 수 있다.</p>

<h3 id="가상화-방식">가상화 방식</h3>

<h4 id="host-os-가상화">Host OS 가상화</h4>

<p>Host OS 위에 가상화 Software를 설치하고 그 위에서 Guest OS를 실행하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
Guest OS
Virtualization Software
Host OS
Hardware
</code></pre></div></div>

<p>Host와 Guest 운영체제의 선택이 비교적 자유롭지만 운영체제 위에서 또 다른 운영체제를 실행하므로 Overhead가 클 수 있다.</p>

<h4 id="hypervisor-가상화">Hypervisor 가상화</h4>

<p>Host OS 없이 Hardware에 Hypervisor를 직접 설치하고 여러 Guest OS를 실행하는 방식이다. Hardware에 직접 설치하는 Hypervisor를 <strong>Bare Metal Hypervisor</strong>라고 하며 Server 가상화에서 주로 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
Guest OS
Hypervisor
Hardware
</code></pre></div></div>

<p>Host OS를 거치지 않으므로 Host OS 방식보다 Overhead가 작다.</p>

<h4 id="container-가상화">Container 가상화</h4>

<p>Host OS 위에 Container Runtime을 설치하고 Application 실행 환경을 논리적으로 격리하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container A        Container B
App + Library      App + Library
--------------------------------
Container Runtime
Host OS
Hardware
</code></pre></div></div>

<p>Container는 Guest OS 전체를 포함하지 않고 Host OS의 Kernel을 공유한다. 따라서 Virtual Machine보다 가볍고 빠르지만 서로 다른 종류의 Kernel을 요구하는 운영체제를 동시에 실행하기 어렵고, 격리 수준도 완전한 Virtual Machine과 차이가 있다.</p>

<p>Container 가상화 Software에는 OpenVZ, LXC, Linux VServer, Docker, Oracle Solaris Zones 등이 있다.</p>

<h2 id="2--container">2 ) Container</h2>

<hr />

<p>Container는 <strong>Application Code와 실행에 필요한 Library 및 의존성을 하나의 격리된 단위로 묶은 실행 환경</strong>이다. Guest OS Layer가 없으므로 Image 크기가 작고 시작 시간이 빠르다.</p>

<h3 id="container의-장점">Container의 장점</h3>

<ul>
  <li>Hypervisor와 Guest OS가 필요하지 않아 가볍다.</li>
  <li>Image의 복제, 이관 및 배포가 쉽다.</li>
  <li>Guest OS를 Booting하지 않으므로 Application 시작이 빠르다.</li>
  <li>동일한 Hardware에서 Virtual Machine보다 많은 Application을 실행할 수 있다.</li>
  <li>Application마다 의존성을 분리하여 Library와 설정의 충돌을 줄인다.</li>
</ul>

<p>Application 배포 방식은 다음과 같이 변화해 왔다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>운영체제에 직접 배포 → Virtual Machine 기반 배포 → Container 기반 배포
</code></pre></div></div>

<h2 id="3--docker">3 ) Docker</h2>

<hr />

<p>Docker는 <strong>Container형 가상화 기술을 구현하는 상주 애플리케이션과 이를 조작하는 명령행 도구</strong>로 구성된다. 애플리케이션과 데이터를 독립된 환경에 격리하여 실행할 수 있게 한다.</p>

<h3 id="특징">특징</h3>

<ul>
  <li>Microservice, DevOps, Testing 등 다양한 분야에서 활용된다.</li>
  <li>Linux Container 구현체의 사실상 표준(de facto)이다.</li>
  <li>가상 머신보다 가볍고 실행 및 배포가 빠르다.</li>
</ul>

<h3 id="docker와-linux">Docker와 Linux</h3>

<p>Docker는 Linux를 전제로 만들어졌다. Windows와 macOS에서도 Docker Desktop을 사용할 수 있지만 내부적으로 Linux 환경을 사용하며, Container에서 실행할 프로그램도 Linux용이다.</p>

<h3 id="lxclinux-containers">LXC(Linux Containers)</h3>

<p>LXC (Linux Containers)는 <strong>OS 수준의 가상화(OS-level Virtualization)</strong>를 구현하는 Linux Container 기술이다.
하나의 Linux System에서 격리된 Container 환경을 실행하며, 이를 위해 여러 Linux 기능을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>기술</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chroot</code></td>
      <td>특정 Directory를 Root처럼 인식시켜 File 영역을 격리</td>
    </tr>
    <tr>
      <td>cgroups</td>
      <td>CPU와 Memory 등의 Resource 사용량을 제한하고 격리</td>
    </tr>
    <tr>
      <td>Namespace</td>
      <td>Process Tree, Network, User ID, Mount 영역을 분리</td>
    </tr>
  </tbody>
</table>

<h4 id="chroot">chroot</h4>

<p><code class="language-plaintext highlighter-rouge">chroot</code>는 특정 Directory를 최상위 Directory인 Root로 인식하도록 설정하는 Linux 기능이다.
이를 통해 다음과 같은 영역을 격리할 수 있다.</p>

<ul>
  <li>User</li>
  <li>File</li>
  <li>Directory</li>
</ul>

<h4 id="cgroups">cgroups</h4>

<p>cgroups (Control Groups)는 <strong>Resource를 제한하고 격리하는 Linux Kernel 기능</strong>이다.</p>

<p>각 Container가 사용할 수 있는 CPU, Memory 등의 Resource를 제한하는 데 사용할 수 있다.</p>

<h4 id="namespace">Namespace</h4>

<p>Namespace는 <strong>Process를 독립시키기 위한 Linux Kernel의 격리 기술</strong>이다.</p>

<p>각 Process가 서로 분리된 운영 환경을 바라볼 수 있도록 하며 다음과 같은 영역을 격리할 수 있다.</p>

<ul>
  <li>Process Tree</li>
  <li>Network</li>
  <li>User ID</li>
  <li>Mount된 File System</li>
</ul>

<h3 id="프로그램과-데이터를-격리하는-이유">프로그램과 데이터를 격리하는 이유</h3>

<p>여러 프로그램이 동일한 실행 환경을 사용하면 서로 영향을 줄 수 있다.</p>

<p>예를 들어 여러 프로그램이 특정 Directory를 공유하거나 같은 경로의 설정 정보를 사용하는 경우, 하나의 프로그램을 Update했을 때 다른 프로그램에도 영향을 줄 수 있다.</p>

<p>이러한 문제는 다음과 같은 부분에서 발생할 수 있다.</p>

<ul>
  <li>실행 환경</li>
  <li>Library</li>
  <li>Directory</li>
  <li>설정 파일</li>
</ul>

<p>여러 프로그램이 같은 Directory, Library, 설정 파일을 공유하면 하나의 변경이 다른 프로그램에 영향을 줄 수 있다. Docker는 실행 환경을 Container 단위로 격리하여 이러한 충돌을 줄인다.</p>

<h2 id="4--image와-container">4 ) Image와 Container</h2>

<hr />

<p>Image는 Container를 만들기 위한 <strong>설계도</strong>, Container는 Image를 실행한 <strong>Instance</strong>에 해당한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image        → Class
Container    → Instance
</code></pre></div></div>

<p>하나의 Image로 동일한 환경의 Container를 여러 개 생성할 수 있고, 실행 중인 Container를 새로운 Image로 만드는 것도 가능하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              ┌─ Container 1
Docker Image ─┼─ Container 2
              └─ Container 3
</code></pre></div></div>
<h3 id="docker-hub">Docker Hub</h3>

<p>Docker Hub는 Docker가 제공하는 공식 Image Registry이다. 일반적으로 Docker Hub에서 Base Image를 내려받고 필요한 Layer를 추가하여 새로운 Image를 만든 뒤 배포한다.</p>

<h3 id="다양한-형태로-구성할-수-있는-container">다양한 형태로 구성할 수 있는 Container</h3>

<p>Container는 하나의 애플리케이션으로 구성할 수도 있고 여러 개의 애플리케이션을 묶어서 구성할 수도 있다.</p>
<blockquote>
  <p>WordPress를 Apache Web Server와 MySQL로 함께 구성한 사례</p>
</blockquote>

<h3 id="container와-데이터">Container와 데이터</h3>

<p>Container는 삭제 후 복구할 수 없는 일회성 실행 환경이다. Container 내부에만 저장한 데이터도 함께 사라질 수 있다.</p>

<p>따라서 Container의 Lifecycle과 저장해야 하는 데이터를 구분하여 다루어야 한다.</p>

<p>보존할 데이터는 Host의 Disk나 Docker Volume을 Mount하여 저장한다.</p>

<h2 id="5--docker의-장점과-구성-요소">5 ) Docker의 장점과 구성 요소</h2>

<hr />

<h3 id="장점">장점</h3>

<ul>
  <li>개발 환경과 운영 환경을 거의 동일하게 재현할 수 있다.</li>
  <li>물리적 환경과 Server 구성 차이의 영향을 줄일 수 있다.</li>
  <li>가상 머신보다 가볍고 Image의 복제·이관·배포가 쉽다.</li>
  <li>Cloud Platform과 Container Orchestration 환경에서 활용할 수 있다.</li>
</ul>

<p>Linux 운영체제 자체의 동작이나 비 Linux 환경이 필요한 경우에는 완전한 Guest OS를 제공하는 Virtual Machine이 더 적합할 수 있다.</p>

<p>Container는 완전한 Guest OS를 제공하는 Virtual Machine과는 구조적으로 차이가 있다.</p>

<h3 id="주요-도구">주요 도구</h3>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>containerd</td>
      <td>Image 전송·저장, Container 실행과 감독 등 Lifecycle 관리</td>
    </tr>
    <tr>
      <td>BuildKit</td>
      <td>Dockerfile을 기반으로 Docker Image Build</td>
    </tr>
    <tr>
      <td>Docker CLI</td>
      <td><code class="language-plaintext highlighter-rouge">docker</code> 명령을 통해 Docker Engine 조작</td>
    </tr>
    <tr>
      <td>Docker Compose</td>
      <td>YAML로 여러 Container를 하나의 Application처럼 관리</td>
    </tr>
    <tr>
      <td>Docker Registry</td>
      <td>Image 저장 및 Push/Pull 제공</td>
    </tr>
    <tr>
      <td>Docker Swarm</td>
      <td>여러 Docker Host를 Cluster로 관리</td>
    </tr>
  </tbody>
</table>

<h4 id="containerd">containerd</h4>

<p>containerd는 Container를 구동하고 관리하는 Runtime이다.</p>

<p>Linux 및 Windows용 Daemon으로 동작하며 다음과 같은 Container Lifecycle을 관리한다.</p>

<ul>
  <li>Image 전송 및 저장</li>
  <li>Container 실행</li>
  <li>Container 감독</li>
  <li>Network 연결</li>
  <li>Container Lifecycle 관리</li>
</ul>

<p>강의에서는 Kubernetes를 학습할 때 Docker와 containerd의 관계를 주의해야 한다고 설명하였다.</p>

<blockquote>
  <p>Kubernetes에서는 Docker를 직접 Container Runtime으로 사용하는 방식이 제거되었으며 containerd와 같은 Container Runtime을 사용한다.</p>
</blockquote>

<h4 id="buildkit">BuildKit</h4>

<p>BuildKit은 Dockerfile의 설정 정보를 이용하여 <strong>Docker Image를 Build하는 Open Source 도구</strong>이다.</p>

<p>Image Build 과정에서 다양한 Architecture와 Build 기능을 제공한다.</p>

<p>강의에서는 현재 Docker가 <strong>Docker Image를 만드는 도구로 많이 사용된다는 점</strong>을 강조하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile → BuildKit → Docker Image
</code></pre></div></div>

<h4 id="docker-cli">Docker CLI</h4>

<p>Docker CLI (Command Line Interface)는 Docker 명령을 실행하기 위한 명령행 도구이다.</p>

<p>Docker CLI를 이용하여 Docker Image와 Container 등을 조작할 수 있다.</p>

<h3 id="container와-orchestration">Container와 Orchestration</h3>

<p>Container 기술은 PaaS 서비스를 가능하게 하는 Software 개발 환경을 제공하는 데 활용할 수 있다.</p>

<p>하지만 Container의 수가 증가하면 각각의 Container를 직접 관리하기 어려워진다.</p>

<p>Container 수가 많아지면 자동 복구, Traffic Routing, Load Balancing 등을 직접 처리하기 어렵다. 이때 Kubernetes와 같은 Orchestration 도구를 사용한다.</p>

<p>다음과 같은 작업을 자동화하고 관리하기 위해 <strong>Orchestration 도구</strong>가 필요하다.</p>

<ul>
  <li>Container 자동 관리</li>
  <li>Traffic Routing</li>
  <li>Load Balancing</li>
</ul>

<p>강의에서는 Container 기반 환경의 구성을 다음과 같이 연결하여 설명하였다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container로 구동되는 Application
              ↓
      LXC / Docker 기술
              ↓
 Orchestration Tool
        Kubernetes
              ↓
             IaaS
</code></pre></div></div>

<p>즉, Docker와 같은 Container 기술을 이용하여 애플리케이션 실행 환경을 구성하고, 여러 Container를 운영해야 하는 환경에서는 Kubernetes와 같은 Orchestration 도구를 이용하여 이를 관리한다.</p>

<h3 id="docker의-구성-요소">Docker의 구성 요소</h3>

<p>Docker는 Client, Host, Engine을 중심으로 동작하며 Container 구성과 Image 관리, Cluster 관리 등을 위한 여러 도구로 구성된다.</p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker Client</td>
      <td>Docker에 명령을 전달할 수 있는 CLI</td>
    </tr>
    <tr>
      <td>Docker Host</td>
      <td>Docker가 설치되어 실행되는 Server 또는 Virtual Machine</td>
    </tr>
    <tr>
      <td>Docker Engine</td>
      <td>Docker를 이용한 Application 실행 환경을 제공하는 핵심 요소</td>
    </tr>
    <tr>
      <td>Docker Compose</td>
      <td>여러 Container의 구성 정보를 YAML로 작성하여 하나의 Application처럼 관리하는 도구</td>
    </tr>
    <tr>
      <td>Docker Registry</td>
      <td>Docker Image를 Push/Pull할 수 있도록 Image를 저장하고 배포하는 Registry</td>
    </tr>
    <tr>
      <td>Docker Swarm</td>
      <td>여러 Docker Host를 Cluster로 구성하여 관리하는 Docker Orchestration 도구</td>
    </tr>
    <tr>
      <td>Docker Hub</td>
      <td>Docker Image를 공유할 수 있는 Cloud 기반 Registry Service</td>
    </tr>
  </tbody>
</table>

<h4 id="docker-client">Docker Client</h4>

<p>Docker Client는 사용자가 Docker에 명령을 전달하기 위한 CLI (Command Line Interface)이다.</p>

<p>사용자가 <code class="language-plaintext highlighter-rouge">docker</code> 명령을 실행하면 Docker Client를 통해 Docker Engine에 명령이 전달된다.</p>

<h4 id="docker-host와-docker-engine">Docker Host와 Docker Engine</h4>

<p>Docker Host는 Docker가 설치되어 있는 Server 또는 Virtual Machine이다.</p>

<p>Docker Host 내부에서는 <strong>Docker Engine</strong>이 동작하며, Docker를 이용한 Application 실행 환경을 제공한다.</p>

<p>Docker Engine은 Docker의 핵심 요소로 Container와 Image를 실행하고 관리한다.</p>

<h4 id="docker-compose">Docker Compose</h4>

<p>Docker Compose는 서로 의존성을 가지는 여러 Container의 구성 정보를 <strong>YAML 파일</strong>로 작성하여 일원화된 Application 관리가 가능하도록 하는 도구이다.</p>

<p>여러 Container로 구성된 Application의 실행 환경을 하나의 설정 파일에서 관리할 수 있다.</p>

<h4 id="docker-registry와-docker-hub">Docker Registry와 Docker Hub</h4>

<p>Docker Registry는 Docker Image를 저장하고 <code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">pull</code>할 수 있도록 하는 Image 저장소이다.</p>

<p>Docker Hub는 Docker Image를 공유할 수 있도록 Docker에서 제공하는 Cloud Service이다.</p>

<h4 id="docker-swarm">Docker Swarm</h4>

<p>Docker Swarm은 여러 Docker Host를 하나의 Cluster로 구성하여 관리할 수 있도록 하는 <strong>Docker Orchestration 도구</strong>이다.</p>

<p>여러 Docker Host에서 실행되는 Container를 하나의 Cluster 단위로 관리할 때 사용할 수 있다.</p>

<h2 id="6--docker-설치">6 ) Docker 설치</h2>

<hr />

<p>Docker는 64-bit 운영체제에서 사용한다. Windows와 macOS에서는 Docker Desktop을 설치한다.</p>

<h3 id="windows">Windows</h3>

<p>WSL 2를 설치한 뒤 Docker Desktop을 설치한다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">wsl</span><span class="w"> </span><span class="nt">--install</span><span class="w">
</span><span class="n">wsl</span><span class="w"> </span><span class="nt">--set-default-version</span><span class="w"> </span><span class="nx">2</span><span class="w">
</span></code></pre></div></div>

<h3 id="ubuntu">Ubuntu</h3>

<p>Ubuntu에서는 Docker 공식 저장소를 등록한 뒤 Docker Engine과 관련 Plugin을 설치한다. 세부 명령은 버전에 따라 달라질 수 있으므로 <a href="https://docs.docker.com/engine/install/ubuntu/">Docker 공식 설치 문서</a>를 기준으로 진행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install </span>docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
</code></pre></div></div>

<p>설치 후 Version과 Service 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker version
<span class="nb">sudo </span>systemctl status docker
<span class="nb">sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker
</code></pre></div></div>

<p>현재 사용자가 <code class="language-plaintext highlighter-rouge">sudo</code> 없이 Docker를 실행하려면 <code class="language-plaintext highlighter-rouge">docker</code> Group에 추가한 뒤 다시 로그인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker <span class="s2">"</span><span class="si">$(</span><span class="nb">whoami</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<blockquote>
  <p>Docker Socket의 권한을 <code class="language-plaintext highlighter-rouge">666</code>으로 변경하면 모든 사용자가 Docker Daemon을 제어할 수 있으므로 사용하지 않는다.</p>
</blockquote>

<h2 id="7--기본-사용">7 ) 기본 사용</h2>

<hr />

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Image 다운로드와 확인</span>
docker pull hello-world
docker images

<span class="c"># Container 생성 및 실행</span>
docker run hello-world

<span class="c"># 실행 중인 Container 확인</span>
docker ps

<span class="c"># 모든 Container 확인</span>
docker ps <span class="nt">-a</span>

<span class="c"># Docker Version과 System 정보 확인</span>
docker version
docker info
docker system <span class="nb">df</span>
</code></pre></div></div>

<p>대화형 Terminal이 필요한 Container는 <code class="language-plaintext highlighter-rouge">-it</code> Option으로 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-it</span> IMAGE_NAME
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker run</code>은 Image가 없으면 내려받고, Container를 생성한 뒤 시작한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[가상화와 Container의 원리부터 Docker의 구성 요소, 설치 및 기본 사용법까지 정리]]></summary></entry><entry><title type="html">Docker 명령어와 Registry</title><link href="https://kkr010128.github.io/cloud-native-09-docker-commands/" rel="alternate" type="text/html" title="Docker 명령어와 Registry" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-09-docker-commands</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-09-docker-commands/"><![CDATA[<h2 id="1--docker-registry">1 ) Docker Registry</h2>

<hr />

<p>Docker Registry는 Image를 저장하고 배포하는 장소이다. Docker Hub는 Docker가 운영하는 공식 Public Registry이며 Apache, MySQL, Ubuntu 등의 공식 Image를 제공한다.</p>

<p>Registry는 여러 <strong>Repository</strong>로 구성된다. 직접 Private Registry를 구축하거나 Cloud Service Provider가 제공하는 Registry를 사용할 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Registry
  ├─ Repository A
  │    ├─ Image:1.0
  │    └─ Image:2.0
  └─ Repository B
</code></pre></div></div>

<h2 id="2--container-application-개발-흐름">2 ) Container Application 개발 흐름</h2>

<hr />

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application Code 작성
        ↓
Dockerfile 작성
        ↓
Image Build
        ↓
Container 생성 및 실행
        ↓
Application Test
        ↓
Registry에 Image Push
</code></pre></div></div>

<p>Dockerfile은 Git으로 관리하며 코드 변경 후 같은 과정을 반복한다. 여러 Container가 필요한 Application은 Docker Compose로 함께 실행하고 각각의 Container와 전체 Application을 테스트한다.</p>

<h2 id="3--docker-명령-구조">3 ) Docker 명령 구조</h2>

<hr />

<p>모든 Docker 명령은 <code class="language-plaintext highlighter-rouge">docker</code>로 시작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker [상위 명령] [하위 명령] [Option] [대상] [인자]
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image pull penguin
docker container run penguin
docker container run <span class="nt">-d</span> penguin
</code></pre></div></div>

<p>일부 명령은 상위 명령을 생략한 이전 형식도 지원하지만, 동작 대상을 명확히 나타내려면 <code class="language-plaintext highlighter-rouge">docker image ...</code>, <code class="language-plaintext highlighter-rouge">docker container ...</code> 형식을 사용하는 것이 좋다.</p>

<h3 id="image-명령">Image 명령</h3>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image pull IMAGE</code></td>
      <td>Registry에서 Image 다운로드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image ls</code></td>
      <td>Local Image 목록 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image build -t NAME .</code></td>
      <td>Dockerfile로 Image Build 및 Tag 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker image rm IMAGE</code></td>
      <td>Image 삭제</td>
    </tr>
  </tbody>
</table>

<h3 id="container-명령">Container 명령</h3>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container create IMAGE</code></td>
      <td>Container 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container start CONTAINER</code></td>
      <td>정지된 Container 시작</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container run IMAGE</code></td>
      <td>Image Pull, Container 생성과 시작 수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container stop CONTAINER</code></td>
      <td>실행 중인 Container 정지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container rm CONTAINER</code></td>
      <td>정지된 Container 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container ls</code></td>
      <td>실행 중인 Container 목록 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container exec -it CONTAINER CMD</code></td>
      <td>실행 중인 Container에서 명령 수행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container cp SRC DEST</code></td>
      <td>Host와 Container 사이에서 File 복사</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker container commit CONTAINER IMAGE</code></td>
      <td>Container 상태를 Image로 저장</td>
    </tr>
  </tbody>
</table>

<p>자주 사용하는 <code class="language-plaintext highlighter-rouge">run</code> Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name</code></td>
      <td>Container 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>Background 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>표준 입력 유지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-t</code></td>
      <td>가상 Terminal 할당</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-e</code></td>
      <td>환경 변수 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p</code></td>
      <td>Host와 Container Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code></td>
      <td>Volume 또는 Directory Mount</td>
    </tr>
  </tbody>
</table>

<h2 id="4--volume과-network-명령">4 ) Volume과 Network 명령</h2>

<hr />

<h3 id="volume">Volume</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create VOLUME_NAME
docker volume inspect VOLUME_NAME
docker volume <span class="nb">ls
</span>docker volume <span class="nb">rm </span>VOLUME_NAME
docker volume prune
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">prune</code>은 사용하지 않는 Resource를 일괄 삭제하므로 삭제 대상을 확인한 후 실행한다.</p>

<h3 id="network">Network</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network create NETWORK_NAME
docker network inspect NETWORK_NAME
docker network <span class="nb">ls
</span>docker network connect NETWORK_NAME CONTAINER_NAME
docker network <span class="nb">rm </span>NETWORK_NAME
docker network prune
</code></pre></div></div>

<h2 id="5--그-밖의-명령">5 ) 그 밖의 명령</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker login</code> / <code class="language-plaintext highlighter-rouge">logout</code></td>
      <td>Registry 인증과 해제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker search</code></td>
      <td>Registry의 Image 검색</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker version</code></td>
      <td>Client와 Server Version 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker info</code></td>
      <td>Docker Engine 정보 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker system</code></td>
      <td>Docker System 정보와 Resource 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker swarm</code></td>
      <td>Docker Swarm 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker node</code></td>
      <td>Swarm Node 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker service</code></td>
      <td>Swarm Service 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker stack</code></td>
      <td>여러 Swarm Service로 구성한 Stack 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker secret</code></td>
      <td>Swarm의 비밀 정보 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">docker plugin</code></td>
      <td>Docker Plugin 관리</td>
    </tr>
  </tbody>
</table>

<p>명령의 자세한 사용법은 <code class="language-plaintext highlighter-rouge">--help</code>로 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker container run <span class="nt">--help</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Registry와 Container 개발 흐름, Image·Container·Volume·Network 관리 명령어]]></summary></entry><entry><title type="html">Docker 이미지와 Union File System</title><link href="https://kkr010128.github.io/cloud-native-10-docker-images/" rel="alternate" type="text/html" title="Docker 이미지와 Union File System" /><published>2026-08-14T00:00:00+09:00</published><updated>2026-08-14T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-native-10-docker-images</id><content type="html" xml:base="https://kkr010128.github.io/cloud-native-10-docker-images/"><![CDATA[<h2 id="1--docker-image">1 ) Docker Image</h2>

<hr />

<p>Docker Image는 Container 형태로 Software를 배포하고 실행하는 데 필요한 코드, Runtime, Library와 설정을 담은 Package이다.</p>

<ul>
  <li>실행에 필요한 요소를 포함하므로 환경별 의존성 차이를 줄인다.</li>
  <li>특정 시점의 Container File System을 담은 Snapshot으로 볼 수 있다.</li>
  <li>하나의 Image로 동일한 환경을 가진 Container를 여러 개 생성할 수 있다.</li>
  <li>여러 개의 읽기 전용 Layer로 구성된다.</li>
</ul>

<h3 id="image-이름">Image 이름</h3>

<p>Image의 전체 참조 형식은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>REGISTRY/NAMESPACE/REPOSITORY:TAG
</code></pre></div></div>

<p>Docker Hub 공식 Image는 <code class="language-plaintext highlighter-rouge">docker.io/library</code>를 생략할 수 있고, Tag를 생략하면 기본적으로 <code class="language-plaintext highlighter-rouge">latest</code>가 사용된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker.io/library/debian:latest
library/debian:latest
debian:latest
debian
</code></pre></div></div>

<p>네 표현은 모두 같은 Image를 가리킨다. 다만 <code class="language-plaintext highlighter-rouge">latest</code>는 가장 최신 Version을 보장하는 의미가 아니라 이름이 <code class="language-plaintext highlighter-rouge">latest</code>인 Tag이므로, 재현 가능한 배포에는 명시적인 Version Tag나 Digest를 사용하는 것이 좋다.</p>

<h2 id="2--image-검색과-다운로드">2 ) Image 검색과 다운로드</h2>

<hr />

<h3 id="검색">검색</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker search <span class="o">[</span>OPTIONS] KEYWORD
docker search <span class="nt">--limit</span> 5 httpd
</code></pre></div></div>

<p>검색 결과에는 Image 이름, 설명, Star 수, 공식 Image 여부 등이 표시된다.</p>

<h3 id="다운로드">다운로드</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image pull NAME[:TAG]
docker image pull NAME@DIGEST
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull jenkins/jenkins:lts
docker pull debian:latest
docker pull gcr.io/google-samples/hello-app:1.0
</code></pre></div></div>

<p>주요 Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--all-tags</code>, <code class="language-plaintext highlighter-rouge">-a</code></td>
      <td>Repository의 모든 Tag 다운로드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--platform</code></td>
      <td>대상 Platform 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--quiet</code>, <code class="language-plaintext highlighter-rouge">-q</code></td>
      <td>상세 출력 생략</td>
    </tr>
  </tbody>
</table>

<p>Apple Silicon Mac에서는 필요에 따라 <code class="language-plaintext highlighter-rouge">--platform linux/arm64</code> 또는 실행할 Image가 요구하는 Platform을 지정한다.</p>

<p>Digest는 Image Content를 식별하는 Hash 값이다. 같은 Digest를 사용하면 동일한 Image Content를 가리킨다.</p>

<h2 id="3--image-조회">3 ) Image 조회</h2>

<hr />

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Local Image 목록</span>
docker images
docker image <span class="nb">ls</span>

<span class="c"># 상세 정보</span>
docker image inspect httpd

<span class="c"># 생성 시각만 출력</span>
docker image inspect <span class="nt">--format</span><span class="o">=</span><span class="s1">''</span> httpd

<span class="c"># Layer 생성 이력</span>
docker image <span class="nb">history </span>httpd
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker image history</code>를 사용하면 Image를 구성한 명령과 Layer 정보를 확인할 수 있다.</p>

<h2 id="4--union-file-system">4 ) Union File System</h2>

<hr />

<p>Union File System은 서로 다른 File System이나 Directory의 내용을 하나의 File System처럼 합쳐 보여주는 기술이다.</p>

<p>Docker Image는 변경할 수 없는 여러 읽기 전용 Layer로 구성된다. Image로 Container를 실행하면 그 위에 쓰기 가능한 Container Layer가 추가되고, 실행 중 발생한 변경 사항은 이 Layer에 기록된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container Layer     ← Read / Write
-----------------
Application Layer   ← Read Only
Library Layer       ← Read Only
Base Image Layer    ← Read Only
</code></pre></div></div>

<p>동일한 Image에서 만든 Container들은 읽기 전용 Image Layer를 공유하므로 Disk를 효율적으로 사용할 수 있다. Container Layer는 Container 삭제 시 함께 사라질 수 있으므로 영구 데이터는 Volume에 저장한다.</p>

<p>Linux Docker Engine의 데이터와 Log는 일반적으로 <code class="language-plaintext highlighter-rouge">/var/lib/docker</code> 아래에 저장되며 Storage Driver에 따라 실제 구조가 달라질 수 있다. <code class="language-plaintext highlighter-rouge">overlay2</code>를 사용하는 환경에서는 관련 Layer 데이터가 해당 영역에 관리된다.</p>

<h2 id="5--docker-hub-로그인과-image-배포">5 ) Docker Hub 로그인과 Image 배포</h2>

<hr />

<h3 id="로그인">로그인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker login
docker login <span class="nt">-u</span> USERNAME
docker <span class="nb">logout</span>
</code></pre></div></div>

<p>비밀번호를 Command에 직접 작성하지 않는다. Docker Hub가 요구하는 경우 Password 대신 Access Token을 사용하고, Token은 문서나 Git Repository에 저장하지 않는다.</p>

<h3 id="tag-지정">Tag 지정</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image tag SOURCE_IMAGE TARGET_IMAGE
docker image tag httpd:latest USERNAME/myapache:1.0
</code></pre></div></div>

<p>Tag는 같은 Image에 Registry에 업로드할 참조 이름과 Version을 부여한다.</p>

<h3 id="push">Push</h3>

<p>Docker Hub에서 Repository를 만든 뒤 계정 Namespace가 포함된 이름으로 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push USERNAME/myapache:1.0
</code></pre></div></div>

<p>새 Version은 다른 Tag를 붙여 같은 Repository에 추가할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image tag httpd:latest USERNAME/myapache:2.0
docker push USERNAME/myapache:2.0
</code></pre></div></div>

<h2 id="6--docker-image를-파일로-관리">6 ) Docker Image를 파일로 관리</h2>

<hr />

<p>Docker Image는 Registry를 거치지 않고 <code class="language-plaintext highlighter-rouge">tar</code> Archive로 저장하고 다른 Docker 환경에서 다시 불러올 수 있다. <code class="language-plaintext highlighter-rouge">docker image save</code>는 Image의 Layer와 Tag 정보를 함께 보존하므로, Network를 사용할 수 없는 환경으로 Image를 옮기거나 특정 Image를 보관할 때 유용하다.</p>

<h3 id="image-저장">Image 저장</h3>

<pre><code class="language-txt">docker image save [OPTIONS] IMAGE [IMAGE...]
</code></pre>

<p><code class="language-plaintext highlighter-rouge">-o</code> 또는 <code class="language-plaintext highlighter-rouge">--output</code> Option을 사용하면 출력 파일을 직접 지정할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Image 다운로드</span>
docker image pull eclipse-mosquitto:latest

<span class="c"># Local Image 확인</span>
docker image <span class="nb">ls</span>

<span class="c"># tar Archive로 저장</span>
docker image save <span class="nt">-o</span> eclipse-mosquitto.tar eclipse-mosquitto:latest
</code></pre></div></div>

<p>Shell의 출력 Redirection을 사용해도 같은 방식으로 저장할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image save eclipse-mosquitto:latest <span class="o">&gt;</span> eclipse-mosquitto.tar
</code></pre></div></div>

<h3 id="image-불러오기">Image 불러오기</h3>

<p><code class="language-plaintext highlighter-rouge">docker image load</code>는 <code class="language-plaintext highlighter-rouge">save</code>로 만든 Archive에서 Image와 Tag 정보를 복원한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image load <span class="nt">-i</span> eclipse-mosquitto.tar
docker image <span class="nb">ls</span>
</code></pre></div></div>

<p>입력 Redirection을 사용할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image load &lt; eclipse-mosquitto.tar
</code></pre></div></div>

<p>Container의 File System을 <code class="language-plaintext highlighter-rouge">docker export</code>와 <code class="language-plaintext highlighter-rouge">docker import</code>로 옮기는 방식과 달리, <code class="language-plaintext highlighter-rouge">save</code>와 <code class="language-plaintext highlighter-rouge">load</code>는 Image Layer와 Metadata를 유지한다.</p>

<h3 id="image-삭제와-정리">Image 삭제와 정리</h3>

<p>Image 이름과 Tag 또는 Image ID를 지정해 Local Image를 삭제할 수 있다. <code class="language-plaintext highlighter-rouge">docker rmi</code>는 <code class="language-plaintext highlighter-rouge">docker image rm</code>의 단축 명령이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image <span class="nb">rm </span>IMAGE[:TAG]
docker image <span class="nb">rm </span>IMAGE_ID
docker rmi IMAGE[:TAG]
</code></pre></div></div>

<p>주요 Option은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--force</code>, <code class="language-plaintext highlighter-rouge">-f</code></td>
      <td>충돌이 발생해도 Image를 강제로 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--no-prune</code></td>
      <td>기본적으로 함께 정리되는 Tag가 없는 Parent Image를 보존</td>
    </tr>
  </tbody>
</table>

<p>Local Image ID 전체를 명령 치환으로 전달하면 모든 Local Image의 삭제를 시도한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image <span class="nb">rm</span> <span class="si">$(</span>docker image <span class="nb">ls</span> <span class="nt">-q</span><span class="si">)</span>
</code></pre></div></div>

<p>실행 중인 Container가 사용하거나 다른 Image가 참조하는 Image는 삭제되지 않을 수 있다. <code class="language-plaintext highlighter-rouge">-f</code>를 사용하기 전에 Container와 Image의 사용 관계를 먼저 확인한다.</p>

<p>사용하지 않는 Image만 정리하려면 <code class="language-plaintext highlighter-rouge">prune</code>을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Dangling Image만 제거</span>
docker image prune

<span class="c"># Container가 사용하지 않는 모든 Image 제거</span>
docker image prune <span class="nt">-a</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker image prune -a</code>는 다시 필요할 수 있는 Local Image도 삭제할 수 있으므로, 표시되는 삭제 대상을 확인한 뒤 실행한다.</p>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Docker Image 검색과 다운로드, Layer 구조, Tag 및 Docker Hub 배포 방법]]></summary></entry><entry><title type="html">클라우드 보안</title><link href="https://kkr010128.github.io/cloud-03-security/" rel="alternate" type="text/html" title="클라우드 보안" /><published>2026-08-13T00:00:00+09:00</published><updated>2026-08-13T00:00:00+09:00</updated><id>https://kkr010128.github.io/cloud-03-security</id><content type="html" xml:base="https://kkr010128.github.io/cloud-03-security/"><![CDATA[<h2 id="1--클라우드-보안">1 ) 클라우드 보안</h2>

<hr />

<p>클라우드 환경에서는 설정 오류, 자격 증명 노출, 과도한 권한 부여 등이 보안 사고로 이어질 수 있다.</p>

<p>대표적인 사례는 다음과 같다.</p>

<ul>
  <li>S3 Bucket을 Public으로 설정하여 고객 정보가 유출되는 경우</li>
  <li>개발자가 AWS Access Key를 GitHub에 업로드하는 경우</li>
  <li>사용자 또는 서비스에 과도한 권한을 부여하는 경우</li>
</ul>

<p>특히 Access Key를 소스 코드에 직접 포함하면 GitHub와 같은 외부 저장소에 코드가 공개될 때 자격 증명도 함께 노출될 수 있다.</p>

<p>최근에는 GitHub에서 AWS Access Key가 포함된 코드를 Push하지 못하도록 제한하며, AWS에서도 GitHub에 노출된 Key를 이용한 접근을 감지하면 계정을 잠그는 방식으로 대응한다.</p>

<h3 id="클라우드-보안이-중요한-이유">클라우드 보안이 중요한 이유</h3>

<p>클라우드 보안은 크게 비용, 신뢰, 법적인 측면에서 중요하다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>비용</td>
      <td>보안 사고로 인한 직접적인 금전적 피해가 발생할 수 있다.</td>
    </tr>
    <tr>
      <td>신뢰</td>
      <td>정보 유출 등의 사고는 서비스와 기업에 대한 신뢰 하락으로 이어질 수 있다.</td>
    </tr>
    <tr>
      <td>법</td>
      <td>개인정보 및 데이터와 관련된 법적 책임이 발생할 수 있다.</td>
    </tr>
  </tbody>
</table>

<h2 id="2--iam">2 ) IAM</h2>

<hr />

<p>IAM (Identity and Access Management)은 AWS에서 사용자와 서비스의 접근 권한을 관리하기 위한 서비스이다.</p>

<h3 id="주요-구성-요소">주요 구성 요소</h3>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>User</td>
      <td>AWS를 사용하는 개별 사용자</td>
    </tr>
    <tr>
      <td>Group</td>
      <td>여러 User를 묶어 관리</td>
    </tr>
    <tr>
      <td>Role</td>
      <td>권한의 묶음</td>
    </tr>
    <tr>
      <td>Policy</td>
      <td>허용하거나 거부할 권한을 정의하는 정책</td>
    </tr>
  </tbody>
</table>

<h3 id="root-사용자와-iam-사용자">Root 사용자와 IAM 사용자</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Root 사용자</th>
      <th>IAM 사용자</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>권한</td>
      <td>모든 권한 소유</td>
      <td>부여된 권한만 사용</td>
    </tr>
    <tr>
      <td>생성</td>
      <td>자동 생성</td>
      <td>Root 사용자가 생성</td>
    </tr>
    <tr>
      <td>삭제</td>
      <td>삭제할 수 없음</td>
      <td>삭제 가능</td>
    </tr>
    <tr>
      <td>일상 사용</td>
      <td>사용하지 않음</td>
      <td>일상 업무에 사용</td>
    </tr>
  </tbody>
</table>

<p>Root 계정에서는 다음과 같은 작업을 수행한다.</p>

<ul>
  <li>AWS 계정 이름, 이메일, 비밀번호 변경</li>
  <li>결제 정보 변경</li>
  <li>AWS Support Plan 변경</li>
  <li>계정 해지</li>
  <li>IAM 사용자의 권한 복원</li>
</ul>

<p>Root 계정은 모든 권한을 가지고 있으므로 반드시 MFA (Multi-Factor Authentication)를 설정한다.</p>

<h3 id="iam-role">IAM Role</h3>

<p>Role은 권한을 묶어서 필요한 대상에게 부여하는 방식이다.</p>

<p>대표적인 사용 사례는 다음과 같다.</p>

<ul>
  <li>AWS 서비스 간 접근</li>
  <li>다른 AWS 계정에서 접근</li>
  <li>임시 접근 권한 부여</li>
</ul>

<p>예를 들어 EC2가 S3에 접근해야 하는 경우 EC2에 필요한 Role을 부여할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>EC2
 │
 │ IAM Role
 ▼
S3
</code></pre></div></div>

<h3 id="iam-policy"><strong>IAM Policy</strong></h3>

<p>Policy는 무엇을 허용하고 무엇을 거부할지를 정의한 규칙이다.</p>

<p>Policy는 JSON으로 직접 작성하거나 GUI를 이용하여 작성할 수 있다.</p>

<p>주요 구성 요소는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>항목</strong></th>
      <th><strong>역할</strong></th>
      <th><strong>예</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Effect</td>
      <td>허용 또는 거부</td>
      <td><code class="language-plaintext highlighter-rouge">Allow</code>, <code class="language-plaintext highlighter-rouge">Deny</code></td>
    </tr>
    <tr>
      <td>Action</td>
      <td>수행할 작업</td>
      <td><code class="language-plaintext highlighter-rouge">s3:GetObject</code></td>
    </tr>
    <tr>
      <td>Resource</td>
      <td>작업 대상 Resource</td>
      <td><code class="language-plaintext highlighter-rouge">arn:aws:s3:::my-bucket/*</code></td>
    </tr>
  </tbody>
</table>

<p>예시는 다음과 같다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"s3:GetObject"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"s3:ListBucket"</span><span class="w">
      </span><span class="p">],</span><span class="w">
      </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"arn:aws:s3:::my-bucket"</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Policy에서 <code class="language-plaintext highlighter-rouge">Allow</code>와 <code class="language-plaintext highlighter-rouge">Deny</code>가 동시에 적용되는 경우 <code class="language-plaintext highlighter-rouge">Deny</code>가 적용된다.</p>

<h3 id="최소-권한-원칙"><strong>최소 권한 원칙</strong></h3>

<p>사용자와 서비스에는 작업에 필요한 최소한의 권한만 부여한다.</p>

<p>불필요하게 많은 권한을 부여하면 계정이나 Access Key가 탈취되었을 때 피해 범위도 커질 수 있다.</p>

<h3 id="iam-모범-사례"><strong>IAM 모범 사례</strong></h3>

<ul>
  <li>Root 계정에 MFA를 설정한다.</li>
  <li>일상적인 작업에는 IAM 계정을 생성하여 사용한다.</li>
  <li>Group을 이용하여 권한을 관리한다.</li>
  <li>최소 권한 원칙을 적용한다.</li>
  <li>강력한 비밀번호 정책을 설정한다.</li>
  <li>Access Key를 코드에 직접 포함하지 않는다.</li>
  <li>사용하지 않는 사용자와 Key를 정리한다.</li>
  <li>Access Key 대신 IAM Role을 활용한다.</li>
</ul>

<p>IAM 계정을 사용하면 누가 어떤 작업을 수행했는지 구분할 수 있으며, Group을 이용하면 여러 사용자의 권한을 일관되고 효율적으로 관리할 수 있다.</p>

<h3 id="비밀번호-정책"><strong>비밀번호 정책</strong></h3>

<p>권장하는 비밀번호 정책은 다음과 같다.</p>

<ul>
  <li>최소 길이 12자 이상</li>
  <li>대문자, 소문자, 숫자, 특수 문자 포함</li>
  <li>비밀번호 만료 시간 설정</li>
  <li>최근 12개의 비밀번호를 기억하여 재사용 방지</li>
  <li>사용자가 자신의 비밀번호를 변경할 수 있도록 허용</li>
</ul>

<p>짧은 비밀번호는 무차별 대입 공격에 취약할 수 있으며, 여러 종류의 문자를 사용하여 비밀번호 복잡도를 높일 수 있다.</p>

<h3 id="aws-접근-방법"><strong>AWS 접근 방법</strong></h3>

<p>AWS에는 콘솔을 이용하는 방법과 Access Key를 이용하는 방법이 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>Console Login</strong></th>
      <th><strong>Access Key</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>접근 방법</td>
      <td>Web Browser로 AWS Console 접속</td>
      <td>프로그램이나 명령어로 AWS API 호출</td>
    </tr>
    <tr>
      <td>사용 방법</td>
      <td>ID + Password + MFA</td>
      <td>Access Key ID + Secret Access Key</td>
    </tr>
    <tr>
      <td>사용 대상</td>
      <td>사람</td>
      <td>프로그램, CLI Tool, 자동화 Script</td>
    </tr>
    <tr>
      <td>보안 위험</td>
      <td>비밀번호 유출, Phishing</td>
      <td>Key를 코드에 포함하면 유출 위험</td>
    </tr>
  </tbody>
</table>

<p>프로그램에서 AWS에 접근할 때는 Access Key를 코드에 직접 저장하는 것보다 IAM Role을 사용하는 것을 권장한다.</p>

<p><strong>정리</strong></p>

<ul>
  <li>IAM은 AWS Resource에 대한 접근 권한을 관리한다.</li>
  <li>User, Group, Role, Policy가 주요 구성 요소이다.</li>
  <li>Root 계정은 일상적인 작업에 사용하지 않고 MFA를 설정한다.</li>
  <li>사용자와 서비스에는 필요한 최소한의 권한만 부여한다.</li>
  <li>Access Key를 소스 코드에 직접 포함하지 않는다.</li>
</ul>

<h2 id="3--네트워크-보안-아키텍처"><strong>3 ) 네트워크 보안 아키텍처</strong></h2>

<hr />

<p>AWS에서는 하나의 보안 장치에만 의존하지 않고 여러 계층에 보안 기능을 배치하는 방어 심층 전략 (Defense in Depth)을 적용할 수 있다.</p>

<h3 id="방어-심층-전략"><strong>방어 심층 전략</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>계층</strong></th>
      <th><strong>서비스</strong></th>
      <th><strong>보호 대상</strong></th>
      <th><strong>주요 기능</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Edge</td>
      <td>CloudFront, Shield, WAF</td>
      <td>외부 Internet 경계</td>
      <td>DDoS 방어, L7 Filtering</td>
    </tr>
    <tr>
      <td>VPC 경계</td>
      <td>Network Firewall, IGW</td>
      <td>VPC 진입점</td>
      <td>IDS/IPS, Stateful 검사</td>
    </tr>
    <tr>
      <td>Subnet</td>
      <td>NACL, Route Table</td>
      <td>Subnet 경계</td>
      <td>Stateless Filtering</td>
    </tr>
    <tr>
      <td>Instance</td>
      <td>Security Group</td>
      <td>Instance 경계</td>
      <td>Stateful Filtering</td>
    </tr>
    <tr>
      <td>Application</td>
      <td>IAM, 암호화, 인증</td>
      <td>Application 내부</td>
      <td>인증/인가, 데이터 암호화</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-네트워크-보안-서비스"><strong>AWS 네트워크 보안 서비스</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>서비스</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AWS Shield</td>
      <td>DDoS 방어</td>
    </tr>
    <tr>
      <td>AWS WAF</td>
      <td>Web Application 공격 Filtering</td>
    </tr>
    <tr>
      <td>AWS Firewall Manager</td>
      <td>여러 계정의 보안 정책 중앙 관리</td>
    </tr>
    <tr>
      <td>AWS Network Firewall</td>
      <td>VPC 수준 Firewall 및 IDS/IPS</td>
    </tr>
    <tr>
      <td>VPC Flow Logs</td>
      <td>Network Interface Traffic 기록</td>
    </tr>
    <tr>
      <td>Traffic Mirroring</td>
      <td>Traffic을 복제하여 보안 분석 도구로 전달</td>
    </tr>
    <tr>
      <td>Route 53 Resolver DNS Firewall</td>
      <td>악성 Domain DNS Query 차단 및 DNS Tunneling 방지</td>
    </tr>
  </tbody>
</table>

<p><strong>AWS Shield</strong></p>

<p>DDoS 방어를 목적으로 사용한다.</p>

<ul>
  <li>Standard: 무료이며 L3/L4 자동 보호</li>
  <li>Advanced: L7 방어</li>
</ul>

<p><strong>AWS WAF</strong></p>

<p>Web Application Firewall로 SQL Injection, XSS, Bot 등 L7 공격을 Filtering한다.</p>

<p>CloudFront와 ALB 등에 적용할 수 있다.</p>

<p><strong>AWS Firewall Manager</strong></p>

<p>여러 AWS 계정에서 사용하는 WAF, Shield, Security Group, NACL 규칙 등을 중앙에서 관리한다.</p>

<p><strong>AWS Network Firewall</strong></p>

<p>VPC 수준에서 Stateful/Stateless Packet 검사를 수행하며 IDS/IPS 기능을 제공한다.</p>

<p><strong>VPC Flow Logs</strong></p>

<p>VPC 내부 Network Interface의 Traffic 정보를 기록한다.</p>

<p><strong>Traffic Mirroring</strong></p>

<p>Traffic을 복제하여 보안 분석 도구로 전달한다.</p>

<p><strong>Route 53 Resolver DNS Firewall</strong></p>

<p>악성 Domain에 대한 DNS Query를 차단하고 DNS Tunneling을 방지하는 데 사용한다.</p>

<h2 id="4--aws-waf"><strong>4 ) AWS WAF</strong></h2>

<hr />

<p>AWS WAF는 Web ACL에 Rule을 정의하고 CloudFront 또는 ALB에 연결하여 HTTP/HTTPS Traffic을 Filtering한다.</p>

<h3 id="waf-동작-구조"><strong>WAF 동작 구조</strong></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Web Request
HTTP / HTTPS
     │
     ▼
CloudFront / ALB
     │
     │ WAF Web ACL
     ▼
Rule 평가
     │
     │ 우선순위 순서로 Rule Matching
     ▼
Allow / Block / Count
</code></pre></div></div>

<p>Rule에 Matching되면 해당 Action을 수행하고, Matching되지 않으면 기본 Action을 수행한다.</p>

<h3 id="waf-규칙-유형"><strong>WAF 규칙 유형</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>유형</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IP 기반</td>
      <td>특정 IP/CIDR을 허용하거나 차단</td>
    </tr>
    <tr>
      <td>Rate 기반</td>
      <td>일정 시간 동안의 요청 수 제한</td>
    </tr>
    <tr>
      <td>관리형 규칙</td>
      <td>AWS 또는 Marketplace에서 제공하는 규칙</td>
    </tr>
    <tr>
      <td>사용자 정의</td>
      <td>Header, Query String 등의 조건을 직접 정의</td>
    </tr>
    <tr>
      <td>정규식 패턴</td>
      <td>URL, User-Agent 등을 정규표현식으로 Matching</td>
    </tr>
  </tbody>
</table>

<p>관리형 규칙은 OWASP Top 10이나 Bot 제어 등에 활용할 수 있다.</p>

<h3 id="aws-관리형-규칙-그룹"><strong>AWS 관리형 규칙 그룹</strong></h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesCommonRuleSet</code>: XSS, 파일 포함, SSRF 등 공통 취약점</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesSQLiRuleSet</code>: SQL Injection 공격 패턴</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesKnownBadInputsRuleSet</code>: Log4j, Java Deserialization 등 알려진 악성 입력</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesAmazonIpReputationRuleSet</code>: AWS 위협 Intelligence 기반 악성 IP</li>
  <li><code class="language-plaintext highlighter-rouge">AWSManagedRulesBotControlRuleSet</code>: Bot Traffic 탐지 및 제어</li>
</ul>

<h3 id="waf-logging"><strong>WAF Logging</strong></h3>

<p>WAF Log를 분석하면 공격 패턴을 파악하고 Rule을 최적화할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>로그 대상</strong></th>
      <th><strong>활용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3</td>
      <td>대용량 저장 및 Athena를 이용한 장기 분석</td>
    </tr>
    <tr>
      <td>CloudWatch Logs</td>
      <td>실시간 조회 및 Metric Filter를 이용한 차단 현황 Monitoring</td>
    </tr>
    <tr>
      <td>Kinesis Data Firehose</td>
      <td>SIEM 연동 및 실시간 Dashboard 활용</td>
    </tr>
  </tbody>
</table>

<h2 id="5--vpc-보안-전략"><strong>5 ) VPC 보안 전략</strong></h2>

<hr />

<h3 id="vpc-보안-설계-원칙"><strong>VPC 보안 설계 원칙</strong></h3>

<p><strong>최소 노출</strong></p>

<p>Internet에 노출되는 Resource를 최소화한다. Public Subnet에는 ALB, NAT 등의 최소한의 Resource만 배치한다.</p>

<p><strong>네트워크 분리</strong></p>

<p>워크로드별 Network를 격리한다. 예를 들어 <code class="language-plaintext highlighter-rouge">dev</code>, <code class="language-plaintext highlighter-rouge">stg</code>, <code class="language-plaintext highlighter-rouge">prod</code> 환경별로 VPC를 분리할 수 있다.</p>

<p><strong>트래픽 제어</strong></p>

<p>SG와 NACL을 이용하여 허용된 Traffic만 통과시키는 White List 방식을 사용한다.</p>

<p><strong>가시성 확보</strong></p>

<p>VPC Flow Logs를 활성화하여 Traffic 흐름을 기록한다.</p>

<p><strong>암호화 전송</strong></p>

<p>Network 구간의 데이터를 보호하기 위해 TLS를 강제하고 VPN, PrivateLink 등을 활용한다.</p>

<h3 id="subnet-분리-전략"><strong>Subnet 분리 전략</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>Tier</strong></th>
      <th><strong>Subnet</strong></th>
      <th><strong>배치 Resource</strong></th>
      <th><strong>Internet 접근</strong></th>
      <th><strong>보안</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public Tier</td>
      <td>Public Subnet</td>
      <td>ALB, NAT Gateway, Bastion</td>
      <td>Inbound/Outbound 허용</td>
      <td>최소 Resource</td>
    </tr>
    <tr>
      <td>App Tier</td>
      <td>Private Subnet</td>
      <td>EC2, ECS, Lambda</td>
      <td>NAT를 통한 Outbound만</td>
      <td>ALB에서만 접근</td>
    </tr>
    <tr>
      <td>Data Tier</td>
      <td>격리된 Private Subnet</td>
      <td>RDS, ElastiCache</td>
      <td>Internet 접근 불가</td>
      <td>App Tier에서만 접근</td>
    </tr>
  </tbody>
</table>

<h3 id="vpc-간-연결-보안"><strong>VPC 간 연결 보안</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>연결 방식</strong></th>
      <th><strong>용도</strong></th>
      <th><strong>보안 고려사항</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>VPC Peering</td>
      <td>두 VPC 간 Private 연결</td>
      <td>CIDR 중복 불가, 전이적 Routing 불가</td>
    </tr>
    <tr>
      <td>Transit Gateway</td>
      <td>다수 VPC/On-Premises Hub 연결</td>
      <td>Routing Table을 통한 Traffic 제어, Blackhole Routing</td>
    </tr>
    <tr>
      <td>PrivateLink</td>
      <td>서비스 간 Private 접근</td>
      <td>Internet 노출 없이 서비스 제공</td>
    </tr>
    <tr>
      <td>Site-to-Site VPN</td>
      <td>On-Premises와 VPC의 암호화 연결</td>
      <td>IPSec Tunnel, BGP Routing, 이중화</td>
    </tr>
    <tr>
      <td>Direct Connect</td>
      <td>전용선 연결</td>
      <td>물리적 분리, MACsec 암호화</td>
    </tr>
  </tbody>
</table>

<h3 id="vpc-endpoint"><strong>VPC Endpoint</strong></h3>

<p>VPC Endpoint를 이용하면 Internet Gateway 없이 AWS 서비스에 Private하게 접근할 수 있어 공격 표면을 줄일 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>유형</strong></th>
      <th><strong>지원 서비스</strong></th>
      <th><strong>보안 제어</strong></th>
      <th><strong>비용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Gateway Endpoint</td>
      <td>S3, DynamoDB</td>
      <td>Endpoint Policy로 접근 제한</td>
      <td>무료</td>
    </tr>
    <tr>
      <td>Interface Endpoint</td>
      <td>대부분의 AWS 서비스</td>
      <td>Security Group 적용</td>
      <td>시간당 + GB당 과금</td>
    </tr>
  </tbody>
</table>

<p>Interface Endpoint는 PrivateLink 기반 ENI를 사용한다.</p>

<p>보안에 필요한 주요 VPC Endpoint는 다음과 같다.</p>

<ul>
  <li><strong>S3</strong>: Private Subnet에서 Internet을 경유하지 않고 접근하며 NAT Gateway 비용을 줄일 수 있다.</li>
  <li><strong>STS</strong>: 임시 자격 증명 발급을 Private하게 수행한다.</li>
  <li><strong>Secrets Manager</strong>: 비밀값을 Internet을 통하지 않고 조회한다.</li>
</ul>

<h2 id="6--security-group과-nacl"><strong>6 ) Security Group과 NACL</strong></h2>

<hr />

<p>Security Group과 NACL (Network Access Control List)은 AWS Network Traffic을 제어하지만 적용 범위와 동작 방식이 다르다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>Security Group</strong></th>
      <th><strong>Network ACL</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>적용 대상</td>
      <td>Instance 수준</td>
      <td>Subnet 수준</td>
    </tr>
    <tr>
      <td>상태</td>
      <td>Stateful</td>
      <td>Stateless</td>
    </tr>
    <tr>
      <td>응답 Traffic</td>
      <td>자동 허용</td>
      <td>별도 Rule 필요</td>
    </tr>
    <tr>
      <td>기본 동작</td>
      <td>Inbound 거부 / Outbound 허용</td>
      <td>기본 NACL 모두 허용 / Custom NACL 모두 거부</td>
    </tr>
    <tr>
      <td>Rule 유형</td>
      <td>Allow만 가능</td>
      <td>Allow / Deny 가능</td>
    </tr>
    <tr>
      <td>Rule 평가</td>
      <td>모든 Rule 종합 평가</td>
      <td>번호 순서대로 평가 후 첫 Matching에서 중단</td>
    </tr>
    <tr>
      <td>SG 참조</td>
      <td>다른 SG를 Source로 참조 가능</td>
      <td>IP/CIDR 사용</td>
    </tr>
  </tbody>
</table>

<h3 id="트래픽-평가-순서"><strong>트래픽 평가 순서</strong></h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Internet
   ↓
NACL Inbound
   ↓
Security Group Inbound
   ↓
Instance
   ↓
Security Group 응답 자동 허용
   ↓
NACL Outbound
</code></pre></div></div>

<p>Security Group은 Stateful이므로 허용된 요청에 대한 응답 Traffic은 자동으로 허용된다.</p>

<p>NACL은 Stateless이므로 Inbound와 Outbound Rule을 각각 설정해야 한다.</p>

<h3 id="서비스별-security-group-설정"><strong>서비스별 Security Group 설정</strong></h3>

<p><strong>RDS</strong></p>

<ul>
  <li>App SG에서만 DB Port 허용</li>
  <li>Public 접근 비활성화</li>
  <li>DB 전용 Subnet 사용</li>
</ul>

<p><strong>ElastiCache</strong></p>

<ul>
  <li>App SG에서만 Redis/Memcached Port 허용</li>
  <li>VPC 외부 접근 불가</li>
  <li>전송 중 TLS 활성화</li>
</ul>

<p><strong>Lambda</strong></p>

<ul>
  <li>Outbound 중심으로 관리</li>
  <li>Lambda SG의 Outbound를 필요한 서비스에만 허용</li>
</ul>

<p><strong>정리</strong></p>

<ul>
  <li>AWS Network 보안은 Defense in Depth 방식으로 여러 계층에서 구성한다.</li>
  <li>WAF는 Web Application Traffic을 Filtering한다.</li>
  <li>VPC는 Public, App, Data Tier 등으로 분리하여 Resource 노출을 최소화할 수 있다.</li>
  <li>VPC Endpoint를 이용하면 AWS 서비스에 Internet을 통하지 않고 접근할 수 있다.</li>
  <li>Security Group은 Stateful이며 NACL은 Stateless이다.</li>
</ul>

<h2 id="7--데이터-보안-전략"><strong>7 ) 데이터 보안 전략</strong></h2>

<hr />

<p>데이터 보안의 기본 원칙은 기밀성, 무결성, 가용성이다.</p>

<ul>
  <li><strong>기밀성 (Confidentiality)</strong>: 허가된 대상만 데이터에 접근할 수 있도록 한다.</li>
  <li><strong>무결성 (Integrity)</strong>: 데이터가 허가되지 않은 방식으로 변경되지 않도록 한다.</li>
  <li><strong>가용성 (Availability)</strong>: 필요한 시점에 데이터와 서비스를 사용할 수 있도록 한다.</li>
</ul>

<h3 id="암호화-유형"><strong>암호화 유형</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>구분</strong></th>
      <th><strong>저장 데이터 암호화</strong></th>
      <th><strong>전송 데이터 암호화</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>보호 대상</td>
      <td>Disk, S3, DB 등에 저장된 데이터</td>
      <td>Network를 통해 이동하는 데이터</td>
    </tr>
    <tr>
      <td>방법</td>
      <td>AES-256 대칭키 암호화</td>
      <td>TLS 1.2/1.3</td>
    </tr>
    <tr>
      <td>AWS</td>
      <td>KMS, CloudHSM</td>
      <td>ACM</td>
    </tr>
    <tr>
      <td>적용 대상</td>
      <td>EBS, S3, RDS, DynamoDB, EFS 등</td>
      <td>HTTPS, VPN, PrivateLink</td>
    </tr>
  </tbody>
</table>

<h3 id="서비스별-암호화"><strong>서비스별 암호화</strong></h3>

<table>
  <thead>
    <tr>
      <th><strong>서비스</strong></th>
      <th><strong>저장 암호화</strong></th>
      <th><strong>전송 암호화</strong></th>
      <th><strong>특징</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3</td>
      <td>SSE-S3, SSE-KMS, SSE-C</td>
      <td>HTTPS 강제</td>
      <td>기본 암호화 설정 가능</td>
    </tr>
    <tr>
      <td>EBS</td>
      <td>KMS 암호화</td>
      <td>Instance-EBS 간 자동 암호화</td>
      <td>계정 수준 암호화</td>
    </tr>
    <tr>
      <td>RDS</td>
      <td>KMS 암호화</td>
      <td>SSL/TLS 인증서</td>
      <td>생성 후 암호화 변경 불가</td>
    </tr>
    <tr>
      <td>DynamoDB</td>
      <td>기본 암호화</td>
      <td>HTTPS Endpoint</td>
      <td>고객 관리 Key 선택 가능</td>
    </tr>
    <tr>
      <td>EFS</td>
      <td>KMS 암호화</td>
      <td>TLS Mount Helper</td>
      <td>생성 시 암호화 설정</td>
    </tr>
  </tbody>
</table>

<h2 id="8--보안-감사"><strong>8 ) 보안 감사</strong></h2>

<hr />

<p>AWS에서는 Resource의 설정을 지속적으로 확인하고 보안 상태와 규정 준수 여부를 점검하기 위한 여러 서비스를 제공한다.</p>

<h3 id="aws-config"><strong>AWS Config</strong></h3>

<p>AWS Config는 Resource의 구성 변경을 지속적으로 기록하고 Rule에 따라 규정 준수 여부를 자동으로 평가하는 서비스이다.</p>

<h4 id="config-rule-유형"><strong>Config Rule 유형</strong></h4>

<ul>
  <li><strong>AWS 관리형 Rule</strong>: AWS가 사전에 정의한 Rule</li>
  <li><strong>사용자 정의 Rule</strong>: Lambda 함수로 작성한 Custom Rule</li>
</ul>

<p>보안에 활용할 수 있는 Rule로 <code class="language-plaintext highlighter-rouge">restricted-ssh</code>가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Security Group
      │
      │ SSH 0.0.0.0/0
      ▼
AWS Config
      │
      ▼
Rule 위반 탐지
</code></pre></div></div>

<h4 id="auto-remediation"><strong>Auto Remediation</strong></h4>

<p>Config Rule을 위반하면 SSM Automation 문서를 연결하여 자동으로 교정할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th><strong>위반 사항</strong></th>
      <th><strong>대응</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public S3 Bucket</td>
      <td>Block Public Access 자동 활성화</td>
    </tr>
    <tr>
      <td>암호화되지 않은 EBS</td>
      <td>알림 전송 후 새로운 Volume 생성 필요</td>
    </tr>
    <tr>
      <td>SSH <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 허용 SG</td>
      <td>해당 Rule 자동 제거</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-security-hub"><strong>AWS Security Hub</strong></h3>

<p>AWS Security Hub는 여러 보안 서비스의 결과를 중앙에서 통합 관리하는 서비스이다.</p>

<p>주요 보안 표준은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>보안 표준</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AWS Foundational Security Best Practices</td>
      <td>AWS 자체 보안 모범 사례, 약 200개</td>
    </tr>
    <tr>
      <td>CIS AWS Foundations Benchmark</td>
      <td>CIS의 AWS 보안 Benchmark, 약 50개</td>
    </tr>
    <tr>
      <td>PCI DSS</td>
      <td>결제 카드 산업 보안 표준, 약 30개</td>
    </tr>
    <tr>
      <td>NIST SP 800-53</td>
      <td>미국 연방 보안 Framework, 약 160개</td>
    </tr>
  </tbody>
</table>

<h4 id="cis-benchmark"><strong>CIS Benchmark</strong></h4>

<p>CIS (Center for Internet Security)는 System과 Cloud 환경을 안전하게 구성하기 위한 보안 Benchmark를 제공한다.</p>

<p>Ubuntu Server에서는 다음과 같은 항목을 점검할 수 있다.</p>

<ul>
  <li>불필요한 서비스 비활성화</li>
  <li>SSH 보안 설정</li>
  <li>비밀번호 정책 강화</li>
  <li>파일 및 Directory 권한 설정</li>
  <li>Log 및 감사 설정</li>
  <li>Firewall 설정</li>
  <li>계정 및 권한 관리</li>
  <li>시간 동기화</li>
  <li>Kernel 및 Package 보안 설정</li>
</ul>

<p>SSH를 예로 들면 단순히 SSH 설치 여부만 확인하는 것이 아니라 다음과 같은 보안 설정을 점검할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Root 직접 로그인 차단
        ↓
비밀번호 인증 제한
        ↓
적절한 암호화 알고리즘 사용
        ↓
접근 가능한 사용자 제한
</code></pre></div></div>

<p>Kubernetes에도 별도의 CIS Kubernetes Benchmark가 존재한다.</p>

<p>Security Hub를 활성화하면 Security Score가 산출되며 이를 기반으로 개선 우선순위를 설정하고 Critical/High 항목부터 대응할 수 있다.</p>

<h3 id="aws-audit-manager"><strong>AWS Audit Manager</strong></h3>

<p>AWS Audit Manager는 AWS 사용에 대한 감사 증거를 자동으로 수집하고 규정 준수 보고서를 생성하는 서비스이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>기능</strong></th>
      <th><strong>설명</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>사전 정의 Framework</td>
      <td>SOC 2, PCI DSS, GDPR, HIPAA 등 제공</td>
    </tr>
    <tr>
      <td>자동 증거 수집</td>
      <td>Config, CloudTrail, Security Hub 등에서 증거 수집</td>
    </tr>
    <tr>
      <td>Custom Framework</td>
      <td>조직의 내부 정책에 맞는 감사 Framework 생성</td>
    </tr>
    <tr>
      <td>감사 보고서</td>
      <td>감사인에게 제출 가능한 보고서 생성</td>
    </tr>
  </tbody>
</table>

<h3 id="aws-trusted-advisor"><strong>AWS Trusted Advisor</strong></h3>

<p>AWS Trusted Advisor는 AWS 환경의 보안, 비용 최적화, 성능, 서비스 한도 등을 점검하는 서비스이다.</p>

<p>보안과 관련된 주요 점검 항목은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th><strong>점검 항목</strong></th>
      <th><strong>내용</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3 Bucket Public 접근</td>
      <td>Public 읽기/쓰기가 허용된 S3 Bucket 탐지</td>
    </tr>
    <tr>
      <td>Root 계정 MFA</td>
      <td>Root 계정 MFA 활성화 여부 확인</td>
    </tr>
    <tr>
      <td>Security Group 무제한 접근</td>
      <td><code class="language-plaintext highlighter-rouge">0.0.0.0/0</code>으로 개방된 Port 탐지</td>
    </tr>
    <tr>
      <td>IAM Access Key Rotation</td>
      <td>90일 이상 Rotation되지 않은 Access Key 탐지</td>
    </tr>
    <tr>
      <td>EBS Public Snapshot</td>
      <td>Public으로 공유된 EBS Snapshot 탐지</td>
    </tr>
    <tr>
      <td>CloudTrail Logging</td>
      <td>CloudTrail 활성화 여부 확인</td>
    </tr>
  </tbody>
</table>

<p>CloudTrail은 AWS에서 누가 무엇을 수행했는지를 기록하는 감사 및 보안 Logging 서비스이다.</p>

<h3 id="amazon-inspector"><strong>Amazon Inspector</strong></h3>

<p>Amazon Inspector는 EC2 Instance, Container Image, Lambda 함수의 Software 취약점과 Network 노출을 자동으로 Scan하는 서비스이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>Scan 대상</strong></th>
      <th><strong>검출 내용</strong></th>
      <th><strong>특징</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EC2 Instance</td>
      <td>OS Package 취약점, Network 도달 가능성</td>
      <td>SSM Agent 기반 Agentless Scan</td>
    </tr>
    <tr>
      <td>ECR Container Image</td>
      <td>Container Image 내부 Package 취약점</td>
      <td>Image Push 시 자동 Scan</td>
    </tr>
    <tr>
      <td>Lambda 함수</td>
      <td>함수 Code 및 Dependency 취약점</td>
      <td>배포 시 자동 Scan</td>
    </tr>
  </tbody>
</table>

<p>ECR은 Docker Image 등을 저장하는 Container Image Repository로 사용할 수 있다.</p>

<h3 id="규정-준수의-코드화"><strong>규정 준수의 코드화</strong></h3>

<p>규정 준수의 코드화는 규정 준수 요구 사항을 코드로 정의하고 자동으로 검증하는 접근 방식이다.</p>

<table>
  <thead>
    <tr>
      <th><strong>구성 요소</strong></th>
      <th><strong>AWS 서비스</strong></th>
      <th><strong>역할</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정책 정의</td>
      <td>AWS Config Rules, SCP, CloudFormation Guard</td>
      <td>보안 정책을 코드로 정의</td>
    </tr>
    <tr>
      <td>자동 평가</td>
      <td>AWS Config, Security Hub</td>
      <td>Resource의 정책 준수 여부 지속 평가</td>
    </tr>
    <tr>
      <td>자동 교정</td>
      <td>SSM Automation, Lambda</td>
      <td>위반 사항 자동 수정</td>
    </tr>
    <tr>
      <td>증거 수집</td>
      <td>Audit Manager, CloudTrail</td>
      <td>규정 준수 증거 자동 수집</td>
    </tr>
    <tr>
      <td>보고</td>
      <td>Security Hub, Audit Manager</td>
      <td>규정 준수 현황 보고</td>
    </tr>
  </tbody>
</table>

<p>전체 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>정책 코드 작성
      ↓
CI/CD 배포
      ↓
지속적 평가
      ↓
자동 교정 / 보고
</code></pre></div></div>

<hr />

<blockquote>
  <p><strong>전체 정리</strong></p>

  <ul>
    <li>클라우드 보안에서는 Public Resource 설정, Access Key 노출, 과도한 권한과 같은 보안 위험을 줄이는 것이 중요하다.</li>
    <li>IAM을 통해 User, Group, Role, Policy를 관리하며, Root 계정의 사용을 최소화하고 MFA와 최소 권한 원칙을 적용한다.</li>
    <li>AWS Network는 WAF, Network Firewall, NACL, Security Group 등을 이용하여 여러 계층을 보호하는 Defense in Depth 전략을 적용할 수 있다.</li>
    <li>VPC에서는 Public, App, Data 영역을 분리하고 필요한 Traffic만 허용하여 Resource의 외부 노출을 최소화한다.</li>
    <li>저장 데이터와 전송 데이터에는 각각 적절한 암호화를 적용하여 데이터의 기밀성과 무결성을 보호한다.</li>
    <li>AWS Config, Security Hub, Audit Manager, Trusted Advisor, Amazon Inspector 등을 활용하여 보안 상태와 취약점, 규정 준수 여부를 지속적으로 점검할 수 있다.</li>
    <li>규정 준수 요구 사항을 코드로 정의하면 정책 정의 → 지속적 평가 → 자동 교정 → 증거 수집 → 보고 과정으로 관리할 수 있다.</li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="Cloud" /><category term="AutoEverSW" /><summary type="html"><![CDATA[IAM과 네트워크 보안, 데이터 암호화, 보안 감사 및 규정 준수 중심의 AWS 클라우드 보안 전략]]></summary></entry></feed>