<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://hyn128.site/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hyn128.site/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-09-15T12:32:27+09:00</updated><id>https://hyn128.site/feed.xml</id><title type="html">renamed.log</title><subtitle>키보드가 망가지도록 공부하기</subtitle><entry><title type="html">홈서버로 이해하는 VPN</title><link href="https://hyn128.site/notes-09-techbites-vpn/" rel="alternate" type="text/html" title="홈서버로 이해하는 VPN" /><published>2026-09-14T00:00:00+09:00</published><updated>2026-09-14T00:00:00+09:00</updated><id>https://hyn128.site/notes-09-techbites-vpn</id><content type="html" xml:base="https://hyn128.site/notes-09-techbites-vpn/"><![CDATA[<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/20.webp" alt="" /></p>
<h2 id="1--외부에서-집에-있는-컴퓨터에-접속할-일이-생겼다">1 ) 외부에서 집에 있는 컴퓨터에 접속할 일이 생겼다</h2>

<hr />

<p>부트캠프 실습 환경을 구성해둔 데스크톱이 맥북보다 빠르고 편했다. 문제는 밖에서도 이 데스크톱으로 실습하고 싶었다는 것이다. 집에는 그 외에도 사용 중인 서버가 몇 대 더 있었다.</p>

<p>홈 네트워크는 다음과 같이 구성되어 있었다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부의 맥북
    │
    │ 인터넷
    ▼
ipTIME 공유기
    │
    ├── Server 1
    ├── Server 2
    └── Server 3
</code></pre></div></div>

<p>처음에는 Windows RDP, Chrome Remote Desktop, VNC, SSH와 SFTP 등 여러 방법을 사용했다. 직접 접속해야 하는 서비스는 공유기에서 포트 포워딩도 설정했다. 외부 포트를 각 서버의 서비스 포트와 연결해두는 방식이다. 참고로 SFTP는 SSH를 기반으로 동작하므로 보통 SSH와 같은 연결 포트를 사용한다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/22.webp" alt="" /></p>

<p>그런데 사용하다 보니 불편한 점이 생겼다.</p>

<ol>
  <li>
    <p>SSH, RDP, VNC처럼 필요한 서비스마다 포트 포워딩을 설정해야 했다.</p>
  </li>
  <li>
    <p>외부에 포트를 열어둔 몇몇 서버에는 알 수 없는 IP의 접속 시도 로그가 남았다.</p>
  </li>
  <li>
    <p>여러 서버가 같은 서비스 포트를 사용하면 외부 포트를 다르게 정하고 기억해야 했다.</p>
  </li>
</ol>

<p>그러던 중 ipTIME 공유기에서 L2TP VPN 서버를 만들 수 있다는 것을 알게 됐다. 원래 나만 접속할 용도였기 때문에 기존 포트 포워딩은 전부 비활성화하고, 외부에서는 먼저 VPN에 연결한 뒤 각 서버의 사설 IP로 접속하도록 바꿨다.</p>

<p>그럼 VPN에 연결했을 뿐인데, 밖에 있는 맥북에서 어떻게 집 안의 사설 IP로 접속할 수 있었을까? 여러 VPN 형태 중 내가 사용한 <strong>Remote Access VPN</strong>을 기준으로 알아보자.</p>

<h2 id="2--사설-ip는-왜-밖에서-바로-사용할-수-없을까">2 ) 사설 IP는 왜 밖에서 바로 사용할 수 없을까?</h2>

<hr />

<p>먼저 VPN을 연결하기 전부터 생각해보자. 홈서버가 <code class="language-plaintext highlighter-rouge">192.168.0.10</code>이라는 주소를 사용한다고 가정하겠다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/08.webp" alt="" /></p>

<p><code class="language-plaintext highlighter-rouge">192.168.0.10</code>은 집 안에서만 사용하는 사설 IP다. 인터넷에서는 이 주소만 보고 어느 집에 있는 서버인지 찾아갈 수 없다.</p>

<p>학교에 비유하면 이렇다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/26.webp" alt="" /></p>

<p>학교 밖에서는 학교 주소를 알아야 그 학교까지 찾아갈 수 있다. <code class="language-plaintext highlighter-rouge">3학년 2반</code>이라는 교실 번호만 가지고는 어느 학교인지 알 수 없다. 사설 IP도 마찬가지다. <code class="language-plaintext highlighter-rouge">192.168.0.10</code>은 어느 네트워크에 속한 주소인지 알아야 의미가 있다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/32.webp" alt="" /></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>공인 IP
→ 인터넷에서 어느 네트워크인지 찾는 주소

사설 IP (192.168.0.10)
→ 해당 네트워크 안에서 어느 기기인지 찾는 주소
</code></pre></div></div>

<h2 id="3--vpn을-연결하면-무엇이-달라질까">3 ) VPN을 연결하면 무엇이 달라질까?</h2>

<hr />

<p>VPN을 학교 참관 수업에 참석하려는 학부모의 출입증이라고 생각해보자. 출입증을 확인받으면 학교 안으로 들어가 필요한 교실까지 갈 수 있다.</p>

<p>VPN도 비슷하다. VPN에 연결했다고 맥북이 실제로 집 안에 들어가는 것은 아니다. 대신 맥북과 집 공유기 사이에 홈 네트워크용 패킷을 보낼 수 있는 전용 경로가 생긴다. 그래서 밖에서도 집 안의 사설 IP를 목적지로 사용할 수 있다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/34.webp" alt="" /></p>

<p>실제 연결을 기준으로 보면 다음 네 단계다.</p>

<p>![[36.webp]]</p>

<ol>
  <li>
    <p>맥북이 인터넷을 통해 ipTIME VPN 서버에 접속한다.</p>
  </li>
  <li>
    <p>공유기가 VPN 비밀키와 사용자 계정을 확인한다.</p>
  </li>
  <li>
    <p>맥북과 공유기 사이에 암호화로 보호된 터널이 만들어진다.</p>
  </li>
  <li>
    <p>맥북에 홈 네트워크로 패킷을 보낼 경로가 추가된다.</p>
  </li>
</ol>

<p>여기서 터널과 경로라는 말이 조금 막연하게 느껴질 수 있다. 뒤에서 패킷이 실제로 어떻게 이동하는지 하나씩 살펴보겠다.</p>

<h2 id="4--포트-포워딩과는-뭐가-다를까">4 ) 포트 포워딩과는 뭐가 다를까?</h2>

<hr />

<p>먼저 포트 포워딩부터 똑같이 학교와 비교해보자. 포트 포워딩은 외부의 특정 포트로 들어온 요청을 내부 서버의 포트로 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부 사용자
    │
    │ 공인 IP:2222
    ▼
공유기
    │
    │ 192.168.0.10:22
    ▼
SSH 서버
</code></pre></div></div>

<p>학교로 치면 특정 교실의 창문에 사다리를 놓아 외부에서 바로 들어올 수 있게 만드는 것과 비슷하다. SSH를 위한 창문, 웹 서버를 위한 창문, DB를 위한 창문이 따로 있는 셈이다. 서비스가 늘어날수록 사다리를 놓고 관리할 창문도 늘어난다.</p>

<p>VPN은 교실마다 외부 출입구를 만들지 않는다. VPN이라는 정문 하나로 들어온 뒤, 신원을 확인받은 사람이 필요한 교실로 이동한다.</p>

<table>
  <thead>
    <tr>
      <th>포트 포워딩</th>
      <th>VPN</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>서비스별 외부 포트를 공개한다</td>
      <td>VPN 진입점을 공개한다</td>
    </tr>
    <tr>
      <td>공인 IP와 외부 포트로 접근한다</td>
      <td>VPN 연결 후 사설 IP로 접근한다</td>
    </tr>
    <tr>
      <td>공개한 서비스마다 공격에 대비해야 한다</td>
      <td>VPN 인증을 먼저 통과해야 한다</td>
    </tr>
  </tbody>
</table>

<p>그렇다면 포트가 하나도 열리지 않은 것일까? 그렇지는 않다. 홈서버의 SSH, RDP, VNC 포트를 각각 공개하지 않을 뿐, ipTIME의 VPN 서버는 외부 연결을 받는 진입점으로 남아 있다.</p>

<h2 id="5--vpn-프로토콜은-뭘까">5 ) VPN 프로토콜은 뭘까?</h2>

<hr />

<p>VPN이라고 해서 모두 같은 방법으로 연결되는 것은 아니다. 연결을 시작하는 방법, 사용자를 확인하는 방법, 패킷을 감싸고 보호하는 방법이 서로 다를 수 있다. 이렇게 통신할 때 지켜야 하는 약속을 <strong>프로토콜(Protocol)</strong>이라고 한다.</p>

<p>택배를 보내는 방법에도 일반 택배와 등기가 있듯이 VPN에도 데이터를 전달하고 보호하는 여러 방식이 있다고 생각하면 된다.</p>

<p>대표적인 VPN 프로토콜에는 다음과 같은 것들이 있다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/38.webp" alt="" /></p>

<p>내가 ipTIME 공유기에서 사용하는 방식은 <strong>L2TP/IPsec</strong>이다. 이름에 두 기술이 함께 들어가는 이유는 맡은 일이 다르기 때문이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L2TP
→ PPP 데이터를 터널로 전달한다

IPsec
→ L2TP 통신을 암호화하고 변조를 방지한다
</code></pre></div></div>

<p>L2TP 자체가 데이터를 암호화하는 것은 아니다. 지금 사용하는 환경에서는 IPsec이 L2TP 통신을 보호한다. 각 프로토콜의 패킷 구조나 암호화 알고리즘까지 들어가면 내용이 너무 길어지므로, 여기서는 VPN 연결을 만드는 방법이 여러 가지라는 것만 알고 넘어가겠다.</p>

<h2 id="6--암호화된-통로-터널은-뭘까">6 ) 암호화된 통로, 터널은 뭘까?</h2>

<hr />

<p>앞에서 맥북과 공유기 사이에 암호화로 보호된 터널이 생긴다고 했다. 여기서 터널은 땅을 뚫어 만든 실제 통로가 아니다. <strong>원래 패킷을 다른 패킷 안에 넣어 두 지점 사이로 운반하는 방식</strong>을 말한다.</p>

<p>맥북이 홈서버에 SSH 요청을 보낸다고 가정하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>원래 패킷

출발지: 맥북의 VPN IP
목적지: 홈서버 192.168.0.10
내용: SSH 요청
</code></pre></div></div>

<p>목적지인 <code class="language-plaintext highlighter-rouge">192.168.0.10</code>은 사설 IP라서 인터넷이 이 패킷을 그대로 홈서버까지 배달할 수 없다. 그래서 VPN은 원래 패킷을 집 공유기까지 전달할 수 있는 외부 패킷 안에 넣는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부 패킷
├── 목적지: 집 공유기의 공인 IP
└── IPsec으로 보호된 내용
    └── 원래 패킷
        ├── 목적지: 홈서버 192.168.0.10
        └── 내용: SSH 요청
</code></pre></div></div>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/42.webp" alt="" /></p>

<p>쉽게 말하면 편지를 택배 상자에 넣어 보내는 것과 비슷하다. 내부 패킷은 집 안에서 사용할 편지이고, 외부 패킷은 그 편지를 집까지 운반할 택배 상자다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>택배 상자의 주소: 집 공유기의 공인 IP
상자 안 편지의 주소: 홈서버 192.168.0.10
</code></pre></div></div>

<p>택배 기사 역할을 하는 인터넷은 상자 바깥에 적힌 주소를 보고 집 공유기까지 배달하면 된다. 중간 네트워크에서는 외부 IP 헤더나 패킷 크기 같은 정보는 볼 수 있지만, IPsec으로 보호된 내부 내용은 직접 읽기 어렵다.</p>

<p>공유기는 자신에게 도착한 외부 패킷을 검증하고 복호화한 뒤, 그 안에서 원래 패킷을 꺼낸다. 편지에 적힌 목적지 <code class="language-plaintext highlighter-rouge">192.168.0.10</code>을 확인하고 홈서버로 전달하는 것이다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/44.webp" alt="" /></p>

<p>내가 사용하는 L2TP/IPsec에서는 각 기술이 맡은 일이 다르다.</p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>L2TP</td>
      <td>PPP 데이터를 터널로 전달한다</td>
    </tr>
    <tr>
      <td>IPsec</td>
      <td>L2TP 통신을 암호화하고 변조를 방지한다</td>
    </tr>
    <tr>
      <td>라우팅</td>
      <td>어떤 패킷을 VPN으로 보낼지 결정한다</td>
    </tr>
  </tbody>
</table>

<p>여기서 터널링과 암호화는 같은 개념이 아니다. 터널링은 패킷을 감싸서 운반하는 방식이고, 암호화는 그 내용을 보호하는 방식이다. L2TP/IPsec VPN은 이 둘을 함께 사용한다.</p>

<h2 id="7--홈-네트워크로-가는-경로가-추가된다는-건-무슨-뜻일까">7 ) “홈 네트워크로 가는 경로가 추가된다”는 건 무슨 뜻일까?</h2>

<hr />

<p>VPN 터널이 만들어졌다고 모든 패킷이 자동으로 터널에 들어가는 것은 아니다. 맥북은 패킷을 보낼 때마다 목적지를 보고 어느 길로 보낼지 먼저 결정한다.</p>

<p>이때 사용하는 것이 <strong>라우팅 테이블(Routing Table)</strong>이다. 목적지에 따라 어느 길을 이용할지 적어둔 길 안내표라고 보면 된다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/46.webp" alt="" /></p>

<p>예를 들어 VPN에 연결한 뒤 다음과 같은 경로가 추가될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>192.168.0.0/24로 가는 패킷 → VPN으로 전달
그 밖의 목적지로 가는 패킷 → 기존 Wi-Fi로 전달
</code></pre></div></div>

<p>맥북에서 홈서버 <code class="language-plaintext highlighter-rouge">192.168.0.10</code>으로 SSH 접속을 시도해보자. 이 주소는 <code class="language-plaintext highlighter-rouge">192.168.0.0/24</code> 홈 네트워크에 포함되므로 맥북은 VPN 경로를 선택한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>목적지 192.168.0.10
    ↓
라우팅 테이블 확인
    ↓
VPN 경로 선택
    ↓
터널로 집 공유기까지 전달
</code></pre></div></div>

<p>이처럼 홈 네트워크로 가는 트래픽만 VPN으로 보내고, 일반 인터넷 트래픽은 기존 네트워크로 보내는 방식을 <strong>Split Tunnel</strong>이라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>맥북
├── 홈 네트워크 → VPN → 집 공유기 → 홈서버
└── 일반 인터넷 → 기존 Wi-Fi → 인터넷 서비스
</code></pre></div></div>

<p>반대로 홈 네트워크뿐 아니라 일반 인터넷 트래픽까지 전부 VPN으로 보내는 방식도 있다. 이것을 <strong>Full Tunnel</strong>이라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>맥북
├── 홈서버 ─┐
├── 검색 ───┼──→ VPN → 집 공유기
└── 동영상 ─┘
</code></pre></div></div>

<p>내 환경이 어느 방식으로 동작하는지는 VPN 설정과 라우팅 테이블을 직접 확인해야 한다. macOS에서는 다음 명령으로 VPN 연결 전후의 경로를 비교할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-rn</span> <span class="nt">-f</span> inet
route <span class="nt">-n</span> get default
route <span class="nt">-n</span> get 192.168.0.10
</code></pre></div></div>

<h2 id="8--지금까지의-과정을-한-번에-보면">8 ) 지금까지의 과정을 한 번에 보면</h2>

<hr />

<p>맥북에서 홈서버 <code class="language-plaintext highlighter-rouge">192.168.0.10</code>으로 SSH 패킷을 보낸다면 다음 순서로 처리된다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/48.webp" alt="" /></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>① 맥북이 홈서버를 목적지로 하는 패킷을 만든다.

② 라우팅 테이블에서 홈 네트워크용 VPN 경로를 선택한다.

③ L2TP가 데이터를 터널로 전달할 형태로 감싼다.

④ IPsec이 L2TP 통신을 암호화하고 보호한다.

⑤ 외부 패킷이 인터넷을 통해 집 공유기의 공인 IP로 전달된다.

⑥ ipTIME이 패킷을 검증하고 복호화한 뒤 원래 패킷을 꺼낸다.

⑦ 공유기가 원래 목적지인 홈서버로 패킷을 전달한다.
</code></pre></div></div>

<p>헷갈리기 쉬운 세 가지 역할만 다시 구분하면 된다.</p>

<ul>
  <li>
    <p><strong>라우팅</strong>은 어떤 패킷을 VPN으로 보낼지 결정한다.</p>
  </li>
  <li>
    <p><strong>터널링</strong>은 선택된 패킷을 다른 패킷 안에 넣어 공유기까지 운반한다.</p>
  </li>
  <li>
    <p><strong>암호화</strong>는 운반되는 내용을 외부에서 읽거나 변조하기 어렵게 보호한다.</p>
  </li>
</ul>

<h2 id="9--vpn은-실제로-어디에-쓰일까">9 ) VPN은 실제로 어디에 쓰일까?</h2>

<hr />

<p>지금까지는 내가 외부 맥북에서 홈서버에 접속한 과정을 기준으로 살펴봤다. 이것은 외부에 있는 사용자 한 명이 사설 네트워크에 접속하는 <strong>Remote Access VPN</strong>이다.</p>

<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/50.webp" alt="" /></p>

<p>회사에서 재택근무를 하는 직원이 사내 서버나 업무 시스템에 접속할 때도 같은 형태를 사용할 수 있다. 집 대신 회사 네트워크에 접속한다는 점만 다르다.</p>

<p>반대로 사용자 한 명이 아니라 서로 떨어진 두 네트워크를 연결할 수도 있다. 본사와 지사 네트워크를 연결하거나, 회사 내부 네트워크와 클라우드의 가상 네트워크를 연결하는 <strong>Site-to-Site VPN</strong>이 여기에 해당한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>회사 내부 네트워크
        │
        │ Site-to-Site VPN
        ▼
클라우드의 가상 네트워크
</code></pre></div></div>

<p>이 경우에는 각 사용자가 VPN에 접속하기보다 양쪽 네트워크의 VPN 장비가 연결을 유지한다. 사용자는 목적지에 맞는 경로만 설정되어 있으면 반대편 네트워크의 서버와 통신할 수 있다.</p>

<p>일반 사용자가 사용하는 상용 VPN 서비스는 인터넷 트래픽을 VPN 업체의 서버를 거쳐 보내는 경우가 많다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자
   │
   │ VPN 터널
   ▼
VPN 업체의 서버
   │
   ▼
인터넷 서비스
</code></pre></div></div>

<p>웹사이트에서는 사용자의 원래 공인 IP 대신 VPN 서버의 공인 IP에서 요청이 온 것으로 보게 된다. 그래서 VPN이 흔히 IP를 바꾸거나 숨기는 도구로 소개되기도 한다. 하지만 VPN의 기본 동작은 떨어져 있는 두 지점 사이에 보호된 통신 경로를 만드는 것이다. 트래픽이 VPN 서버를 거쳐 나가면서 외부에 VPN 서버의 IP가 보이는 것이다.</p>

<p>카페나 공항처럼 내가 관리하지 않는 Wi-Fi에서 기기와 VPN 서버 사이의 통신을 보호하려고 사용할 수도 있다. 다만 VPN 서버를 지난 뒤의 구간까지 전부 VPN이 보호하는 것은 아니다. 접속한 웹사이트가 HTTPS를 사용하는지, 서비스 자체가 안전한지는 따로 확인해야 한다.</p>

<p>정리하면 내가 사용한 홈서버 VPN, 회사의 원격 접속 VPN, 네트워크를 서로 연결하는 VPN과 상용 VPN은 목적은 달라도 모두 떨어진 두 지점 사이에서 패킷을 전달한다는 공통점이 있다.</p>

<h2 id="10--그럼-vpn만-사용하면-안전할까">10 ) 그럼 VPN만 사용하면 안전할까?</h2>

<hr />

<p>VPN을 사용하면 홈서버의 여러 관리 포트를 외부에 하나씩 노출하지 않아도 된다. 외부 진입점을 VPN 서버로 모으고, 인증을 통과한 사용자만 홈 네트워크에 접근하게 만들 수 있다.</p>

<p>하지만 VPN을 쓴다고 무조건 안전해지는 것은 아니다. VPN 서버 자체도 외부 연결을 받는 서비스이고, 비밀키나 사용자 계정이 유출되면 공격자가 내부 네트워크에 접근할 수 있다. 그래서 다음과 같은 관리는 여전히 필요하다.</p>

<ul>
  <li>
    <p>충분히 긴 VPN 비밀키와 계정 암호를 사용한다.</p>
  </li>
  <li>
    <p>사용하지 않는 VPN 계정은 제거한다.</p>
  </li>
  <li>
    <p>공유기 펌웨어를 최신 상태로 유지한다.</p>
  </li>
  <li>
    <p>VPN에 연결한 뒤에도 SSH나 RDP의 자체 인증을 유지한다.</p>
  </li>
</ul>

<p>내가 포트 포워딩 대신 VPN을 사용했을 때 일어난 일은 생각보다 단순하다. 맥북은 라우팅 테이블을 보고 홈 네트워크로 가는 패킷을 VPN에 보낸다. 패킷은 터널링과 암호화 과정을 거쳐 집 공유기까지 이동하고, 공유기는 그 안의 원래 목적지를 확인해 홈서버로 전달한다.</p>

<p>이 덕분에 여러 서버의 관리 포트를 하나씩 외부에 공개하지 않고도 사설 IP로 접속할 수 있었다. 다만 외부 진입점이 사라진 것은 아니다. 이제는 그 진입점이 VPN 서버로 모인 것이므로 VPN 계정과 공유기 자체를 계속 안전하게 관리해야 한다.</p>

<h2 id="11--느낀-점">11 ) 느낀 점</h2>

<hr />
<p><img src="../../assets/post/2026-09-14-notes-09-techbites-vpn/52.webp" alt="" /></p>

<p>처음에는 포트 포워딩을 여러 개 설정하는 것이 불편했고, 외부에서 들어오는 접속 시도도 신경 쓰여서 VPN을 사용하기 시작했다. VPN에 연결한 뒤 사설 IP로 접속이 되니 편하게 사용했지만, 정작 그 연결이 어떻게 만들어지는지는 제대로 알지 못했다.</p>

<p>이번에 알아보면서 VPN에 연결된 맥북이 실제로 집 안의 네트워크로 들어가는 것은 아니라는 점을 알게 됐다. 맥북에 홈 네트워크로 가는 경로가 추가되고, 그 패킷이 터널을 통해 집 공유기까지 전달되고 있었던 것이다.</p>

<p>평소에는 연결 버튼 한 번 누르고 사용하던 기능이지만, 그 뒤에서는 라우팅과 터널링, 암호화가 각자 맡은 일을 하고 있었다.</p>

<p>내가 홈서버를 사용하면서 궁금했던 VPN의 내용은 여기까지지만, 앞으로도 다양한 곳에서 더 깊은 기술로 접할 때 빠르게 이해하고 적용할 수 있도록 깊은 내용을 공부해야할 필요를 느꼈다.</p>]]></content><author><name></name></author><category term="TechBites" /><category term="VPN" /><category term="Network" /><category term="IPsec" /><category term="Tennel" /><summary type="html"><![CDATA[L2TP/IPsec VPN을 통해 외부 맥북에서 홈 네트워크의 서버에 접근하는 과정]]></summary></entry><entry><title type="html">Jenkins Controller-Agent 구조와 설치</title><link href="https://hyn128.site/cloud-native-47-jenkins-architecture-installation/" rel="alternate" type="text/html" title="Jenkins Controller-Agent 구조와 설치" /><published>2026-09-14T00:00:00+09:00</published><updated>2026-09-14T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-47-jenkins-architecture-installation</id><content type="html" xml:base="https://hyn128.site/cloud-native-47-jenkins-architecture-installation/"><![CDATA[<p><a href="/cloud-native-46-github-actions-ci/">GitHub Actions Workflow와 CI/CD 실습</a>은 GitHub가 제공하는 Runner에서 Build와 Test를 실행했다. Jenkins는 조직이 직접 설치하고 확장할 수 있는 Java 기반 Open Source 자동화 Server이다. Source Checkout, Compile, Test, 정적 분석, Package, 배포와 결과 알림을 Plugin과 Job으로 연결한다.</p>

<h2 id="1--jenkins가-담당하는-자동화">1 ) Jenkins가 담당하는 자동화</h2>

<hr />

<p>여러 개발자의 변경 사항이 Main Branch에 병합되면 Jenkins는 Webhook을 받거나 Repository를 주기적으로 확인해 Job을 시작할 수 있다. 일반적인 처리 과정은 다음과 같다.</p>

<ol>
  <li>
    <p>SCM Plugin으로 대상 Commit을 Checkout한다.</p>
  </li>
  <li>
    <p>Maven이나 Gradle 같은 Build Tool로 Source를 Compile한다.</p>
  </li>
  <li>
    <p>Unit Test와 Integration Test를 실행한다.</p>
  </li>
  <li>
    <p>정적 분석 도구로 Coding Convention, 결함과 사용하지 않는 Code를 확인한다.</p>
  </li>
  <li>
    <p>JAR, WAR 또는 Container Image 같은 Artifact로 Package한다.</p>
  </li>
  <li>
    <p>배포 도구를 호출해 검증된 Artifact를 대상 환경으로 전달한다.</p>
  </li>
  <li>
    <p>배포된 Application을 Selenium 같은 자동화 도구로 추가 검증할 수 있다.</p>
  </li>
  <li>
    <p>수행 결과를 Email이나 협업 도구로 알린다.</p>
  </li>
</ol>

<p>Jenkins Core가 모든 도구를 직접 구현하는 것은 아니다. Git, Maven, Email Extension처럼 외부 도구와 연결하는 기능은 주로 Plugin이 제공한다. 따라서 Jenkins Version뿐 아니라 설치한 Plugin의 Version과 호환성도 함께 관리해야 한다.</p>

<h2 id="2--controller-agent와-executor">2 ) Controller, Agent와 Executor</h2>

<hr />

<p>Jenkins의 분산 구조를 이해하려면 실행을 결정하는 역할과 실제 명령을 수행하는 역할을 분리해서 봐야 한다.</p>

<blockquote>
  <p><strong>Controller</strong>
Jenkins의 중앙 조정 Process이다. 설정과 Plugin을 보관하고 Web UI를 제공하며 Trigger, Build Queue와 Agent 할당을 관리한다. 과거에는 Master라고 불렀지만 현재 공식 용어는 Controller이다.</p>
</blockquote>

<blockquote>
  <p><strong>Agent</strong>
Controller에 연결되어 할당받은 Build Step을 실행하는 Machine, Virtual Machine 또는 Container이다.</p>
</blockquote>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>관점</th>
      <th>주요 역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Controller</td>
      <td>제어</td>
      <td>Job 설정 저장, Trigger 처리, Queue 관리, Agent 선택, UI와 결과 제공</td>
    </tr>
    <tr>
      <td>Queue</td>
      <td>대기</td>
      <td>실행 조건을 만족했지만 Executor를 아직 배정받지 못한 Build 보관</td>
    </tr>
    <tr>
      <td>Agent</td>
      <td>실행</td>
      <td>Workspace 준비, Source Checkout, Compile, Test와 Package 실행</td>
    </tr>
    <tr>
      <td>Executor</td>
      <td>동시 실행 용량</td>
      <td>Node에서 동시에 실행할 수 있는 작업 Slot</td>
    </tr>
    <tr>
      <td>Label</td>
      <td>작업 배치 조건</td>
      <td><code class="language-plaintext highlighter-rouge">linux</code>, <code class="language-plaintext highlighter-rouge">docker</code>, <code class="language-plaintext highlighter-rouge">jdk21</code>처럼 Agent의 능력을 표시</td>
    </tr>
    <tr>
      <td>Workspace</td>
      <td>작업 File 영역</td>
      <td>Checkout한 Source와 Build 중간 결과를 저장하는 Node의 Directory</td>
    </tr>
    <tr>
      <td>Artifact</td>
      <td>보존 결과</td>
      <td>Build가 만든 JAR, WAR, Report와 같은 불변 결과물</td>
    </tr>
  </tbody>
</table>

<figure class="post-visual" aria-labelledby="jenkins-controller-agent-caption">
  <figcaption class="post-visual__caption" id="jenkins-controller-agent-caption">
    Trigger부터 Controller의 작업 배정과 Agent의 Build 실행까지 이어지는 Jenkins 흐름
  </figcaption>
  <div class="post-visual__frame">
    <svg viewBox="0 0 840 790" role="img" aria-labelledby="jenkins-flow-title jenkins-flow-desc">
      <title id="jenkins-flow-title">Jenkins Controller와 Agent의 실행 흐름</title>
      <desc id="jenkins-flow-desc">Trigger, Controller와 Agent는 수직으로 연결된다. Controller와 Agent 내부의 처리 단계는 왼쪽에서 오른쪽으로 진행하며 상태, 로그와 아티팩트는 Controller로 반환된다.</desc>
      <defs>
        <marker id="arrow-jenkins-flow" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
          <path d="M0 0 7 3.5 0 7z" fill="var(--text3)"></path>
        </marker>
        <marker id="arrow-jenkins-dispatch" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
          <path d="M0 0 7 3.5 0 7z" fill="var(--diagram-blue)"></path>
        </marker>
        <marker id="arrow-jenkins-return" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
          <path d="M0 0 7 3.5 0 7z" fill="var(--diagram-orange)"></path>
        </marker>
      </defs>

      <g id="jenkins-trigger">
        <title>Build Trigger</title>
        <rect x="220" y="38" width="400" height="92" rx="18" fill="var(--diagram-violet-soft)" stroke="var(--diagram-violet)" stroke-width="2"></rect>
        <text x="420" y="72" text-anchor="middle" fill="var(--diagram-violet)" font-size="14" font-weight="700">TRIGGER</text>
        <text x="420" y="104" text-anchor="middle" fill="var(--text1)" font-size="20" font-weight="700">SCM · 일정 · 사용자 요청</text>
      </g>
      <line id="jenkins-trigger-request" x1="420" y1="130" x2="420" y2="172" stroke="var(--text3)" stroke-width="3" marker-end="url(#arrow-jenkins-flow)"></line>

      <g id="jenkins-controller">
        <title>Controller 제어 영역</title>
        <rect x="56" y="184" width="728" height="220" rx="22" fill="var(--diagram-primary-soft)" stroke="var(--primary2)" stroke-width="3"></rect>
        <text x="86" y="215" fill="var(--primary1)" font-size="14" font-weight="700">CONTROLLER · 제어 영역</text>
        <text x="420" y="244" text-anchor="middle" fill="var(--text1)" font-size="21" font-weight="700">요청을 판단하고 실행 위치를 결정</text>

        <g id="jenkins-job-config">
          <title>Job 설정 확인</title>
          <rect x="100" y="270" width="190" height="92" rx="14" fill="var(--bg-element1)" stroke="var(--primary2)" stroke-width="2"></rect>
          <text x="195" y="305" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">1. Job 설정</text>
          <text x="195" y="333" text-anchor="middle" fill="var(--text2)" font-size="13">SCM · Trigger · Step</text>
        </g>
        <line x1="290" y1="316" x2="326" y2="316" stroke="var(--text3)" stroke-width="2" marker-end="url(#arrow-jenkins-flow)"></line>

        <g id="jenkins-build-queue">
          <title>Build Queue</title>
          <rect x="334" y="270" width="172" height="92" rx="14" fill="var(--bg-element1)" stroke="var(--primary2)" stroke-width="2"></rect>
          <text x="420" y="305" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">2. Build Queue</text>
          <text x="420" y="333" text-anchor="middle" fill="var(--text2)" font-size="13">Executor 대기</text>
        </g>
        <line x1="506" y1="316" x2="542" y2="316" stroke="var(--text3)" stroke-width="2" marker-end="url(#arrow-jenkins-flow)"></line>

        <g id="jenkins-scheduler">
          <title>Scheduler가 Agent를 선택</title>
          <rect x="550" y="270" width="190" height="92" rx="14" fill="var(--bg-element1)" stroke="var(--primary2)" stroke-width="2"></rect>
          <text x="645" y="305" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">3. Scheduler</text>
          <text x="645" y="333" text-anchor="middle" fill="var(--text2)" font-size="13">Label · 빈 Executor</text>
        </g>
      </g>

      <line id="jenkins-build-dispatch" x1="420" y1="404" x2="420" y2="478" stroke="var(--diagram-blue)" stroke-width="3" marker-end="url(#arrow-jenkins-dispatch)"></line>
      <text x="452" y="446" fill="var(--diagram-blue)" font-size="13" font-weight="700">Build 할당</text>

      <g id="jenkins-agent">
        <title>Agent 실행 영역</title>
        <rect x="56" y="490" width="728" height="220" rx="22" fill="var(--diagram-blue-soft)" stroke="var(--diagram-blue)" stroke-width="3"></rect>
        <text x="86" y="521" fill="var(--diagram-blue)" font-size="14" font-weight="700">AGENT · 실행 영역</text>
        <text x="420" y="550" text-anchor="middle" fill="var(--text1)" font-size="21" font-weight="700">선택된 Node에서 Build 수행</text>

        <g id="jenkins-executor">
          <title>Executor Slot 점유</title>
          <rect x="82" y="578" width="146" height="84" rx="14" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
          <text x="155" y="611" text-anchor="middle" fill="var(--text1)" font-size="16" font-weight="700">1. Executor</text>
          <text x="155" y="638" text-anchor="middle" fill="var(--text2)" font-size="12">Slot 점유</text>
        </g>
        <line x1="228" y1="620" x2="258" y2="620" stroke="var(--text3)" stroke-width="2" marker-end="url(#arrow-jenkins-flow)"></line>

        <g id="jenkins-workspace">
          <title>Workspace 준비</title>
          <rect x="266" y="578" width="146" height="84" rx="14" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
          <text x="339" y="611" text-anchor="middle" fill="var(--text1)" font-size="16" font-weight="700">2. Workspace</text>
          <text x="339" y="638" text-anchor="middle" fill="var(--text2)" font-size="12">Source · 중간 결과</text>
        </g>
        <line x1="412" y1="620" x2="442" y2="620" stroke="var(--text3)" stroke-width="2" marker-end="url(#arrow-jenkins-flow)"></line>

        <g id="jenkins-build-steps">
          <title>Build Step 실행</title>
          <rect x="450" y="578" width="176" height="84" rx="14" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
          <text x="538" y="607" text-anchor="middle" fill="var(--text1)" font-size="15" font-weight="700">3. Build Step</text>
          <text x="538" y="630" text-anchor="middle" fill="var(--text2)" font-size="11">Checkout · Build</text>
          <text x="538" y="648" text-anchor="middle" fill="var(--text2)" font-size="11">Test · Package</text>
        </g>
        <line x1="626" y1="620" x2="648" y2="620" stroke="var(--text3)" stroke-width="2" marker-end="url(#arrow-jenkins-flow)"></line>

        <g id="jenkins-build-result">
          <title>Build 결과 생성</title>
          <rect x="656" y="578" width="102" height="84" rx="14" fill="var(--diagram-orange-soft)" stroke="var(--diagram-orange)" stroke-width="2"></rect>
          <text x="707" y="607" text-anchor="middle" fill="var(--text1)" font-size="15" font-weight="700">4. 결과</text>
          <text x="707" y="632" text-anchor="middle" fill="var(--text2)" font-size="11">상태 · Log</text>
          <text x="707" y="650" text-anchor="middle" fill="var(--text2)" font-size="11">Artifact</text>
        </g>
      </g>

      <g id="jenkins-result-return">
        <title>Build 결과를 Controller로 반환</title>
        <line x1="758" y1="620" x2="758" y2="404" stroke="var(--diagram-orange)" stroke-width="3" stroke-dasharray="8 6" marker-end="url(#arrow-jenkins-return)"></line>
        <rect x="572" y="425" width="166" height="40" rx="16" fill="var(--bg-element1)" stroke="var(--diagram-orange)"></rect>
        <text x="655" y="450" text-anchor="middle" fill="var(--diagram-orange)" font-size="12" font-weight="700">상태 · Log · Artifact 반환</text>
      </g>

      <g id="jenkins-isolation-note">
        <title>Controller와 Build 실행 환경 분리</title>
        <rect x="176" y="736" width="488" height="34" rx="17" fill="var(--bg-element3)"></rect>
        <text x="420" y="758" text-anchor="middle" fill="var(--text2)" font-size="13">운영 환경에서는 Controller와 Build 실행 환경을 분리</text>
      </g>
    </svg>
  </div>
  <div class="post-visual__notes">
    <div class="post-visual__note">
      <strong>Controller의 관점</strong>
      <p>실행 요청을 Queue에 넣고 Label과 빈 Executor를 기준으로 Agent를 선택한다.</p>
    </div>
    <div class="post-visual__note">
      <strong>Agent의 관점</strong>
      <p>Executor가 Workspace에서 Build Step을 실행하고 상태, Log와 Artifact를 반환한다.</p>
    </div>
  </div>
</figure>

<p>Job이 시작되는 흐름은 다음과 같다.</p>

<ol>
  <li>
    <p>사용자의 <strong>Build Now</strong>, SCM 변경 또는 일정 Trigger가 Controller에 실행을 요청한다.</p>
  </li>
  <li>
    <p>Controller가 Job 설정을 읽고 실행할 Build를 Queue에 넣는다.</p>
  </li>
  <li>
    <p>Scheduler가 Label, Agent 연결 상태와 사용 가능한 Executor를 확인한다.</p>
  </li>
  <li>
    <p>조건을 만족하는 Agent의 Executor가 Build를 가져간다.</p>
  </li>
  <li>
    <p>Agent가 자신의 Workspace에서 Checkout, Build와 Test를 실행한다.</p>
  </li>
  <li>
    <p>Console Log, Build 상태와 보관하도록 설정한 Artifact를 Controller가 사용자에게 제공한다.</p>
  </li>
</ol>

<p>Agent 한 대에 Executor를 2개 설정하면 원칙적으로 작업 2개를 동시에 실행할 수 있다. 그러나 CPU, Memory, Disk I/O와 외부 Tool이 같은 Node를 공유하므로 Executor 수를 늘리는 것이 항상 처리량 증가로 이어지지는 않는다.</p>

<p>Controller도 Node이므로 Executor를 가질 수 있지만, 운영 환경에서는 중앙 조정 Process와 Build Process를 격리하기 위해 Controller의 Executor를 <code class="language-plaintext highlighter-rouge">0</code>으로 두고 전용 Agent에서 작업을 실행하는 구성이 일반적이다. 실습처럼 단일 Container에서 시작할 때는 Built-In Node가 Build를 실행할 수 있으나 이 차이를 알고 사용해야 한다.</p>

<h2 id="3--확장-방식">3 ) 확장 방식</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>변경 대상</th>
      <th>장점</th>
      <th>주의 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>수직 확장</td>
      <td>Controller 또는 Agent의 CPU, Memory, Disk 증설</td>
      <td>구성과 운영 지점이 적음</td>
      <td>한 Node의 한계와 장애 영향 범위가 큼</td>
    </tr>
    <tr>
      <td>Agent 수평 확장</td>
      <td>같은 Controller에 Agent 추가</td>
      <td>Build 실행 용량과 환경 격리 확장</td>
      <td>Queue 정책, Label과 Tool Version 관리 필요</td>
    </tr>
    <tr>
      <td>Controller 분리</td>
      <td>목적별 Jenkins Instance 운영</td>
      <td>팀·보안 영역·환경별 독립 설정 가능</td>
      <td>Plugin, Credential, Job과 Upgrade를 각각 관리해야 함</td>
    </tr>
  </tbody>
</table>

<p>서로 독립된 Controller 여러 대를 운영한다고 해서 자동 Failover가 구성되는 것은 아니다. 한 Controller의 장애 시 다른 Controller가 Build를 자동 인계하려면 별도의 Job 동기화, Shared Storage, Backup과 복구 설계가 필요하다.</p>

<p>Test용과 Production용 Controller를 분리하면 실험적인 Plugin이나 설정 변경이 운영 자동화에 미치는 영향을 줄일 수 있다. 대규모 조직이 Public Cloud와 On-Premises를 함께 사용하며 많은 Build를 처리한 사례는 Jenkins를 역할별로 분리할 수 있음을 보여주는 역사적 사례로 이해한다. 실제 분리 기준은 Build 수보다 보안 경계, 장애 범위와 유지보수 비용으로 결정한다.</p>

<h2 id="4--설치-방법과-요구-사항">4 ) 설치 방법과 요구 사항</h2>

<hr />

<p>Jenkins는 여러 방식으로 설치할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>설치 방식</th>
      <th>특징</th>
      <th>관리 대상</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Standalone WAR</td>
      <td>Java로 <code class="language-plaintext highlighter-rouge">jenkins.war</code> 직접 실행</td>
      <td>Java, Service와 Jenkins Home</td>
    </tr>
    <tr>
      <td>OS Package</td>
      <td>Windows MSI, Debian·Ubuntu Package, Homebrew 등 사용</td>
      <td>OS Package와 Service 설정</td>
    </tr>
    <tr>
      <td>Docker</td>
      <td>공식 Image와 Volume으로 실행</td>
      <td>Image Tag, Container와 Volume</td>
    </tr>
    <tr>
      <td>Kubernetes</td>
      <td>Pod와 Persistent Volume으로 실행</td>
      <td>Kubernetes Resource와 Storage</td>
    </tr>
    <tr>
      <td>Cloud Service</td>
      <td>공급자가 제공하는 관리형 환경 사용</td>
      <td>공급자 정책과 연결 설정</td>
    </tr>
  </tbody>
</table>

<p>Jenkins WAR에는 Winstone/Jetty 기반 실행 환경이 포함되어 있어 일반적으로 별도 Tomcat 없이 Standalone Application으로 실행한다. 외부 Servlet Container에 WAR를 배포하는 방식도 존재하지만 지원 범위와 호환성 제약을 먼저 확인해야 한다.</p>

<p>공식 Docker 설치 문서의 최소 요구 사항은 Memory 256 MB와 Disk 1 GB이며, Docker 환경은 Disk 10 GB 이상을 권장한다. 작은 팀에는 이보다 넉넉한 Memory와 Disk가 필요하다. 실제 용량은 Build 수, Workspace, Artifact 보존 기간과 Plugin 수를 기준으로 산정한다. 직접 설치할 때는 현재 Jenkins가 지원하는 Java Version도 확인해야 한다.</p>

<h2 id="5--docker-compose로-설치">5 ) Docker Compose로 설치</h2>

<hr />

<p>다음 구성은 Jenkins Web UI와 향후 Inbound Agent 연결을 위한 Port를 열고 Jenkins Home을 Named Volume에 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">jenkins</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">jenkins/jenkins:lts-jdk21</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">jenkins</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">unless-stopped</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">50000:50000"</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">jenkins_home:/var/jenkins_home</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">jenkins_home</span><span class="pi">:</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jenkins/jenkins:lts-jdk21</code></td>
      <td>Java 21이 포함된 Jenkins LTS Image</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">8080:8080</code></td>
      <td>Browser가 Jenkins Web UI에 접속하는 HTTP Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">50000:50000</code></td>
      <td>외부 Inbound Agent가 Controller에 연결할 때 사용하는 기본 TCP Port</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jenkins_home</code></td>
      <td>Job, Plugin, Credential Metadata와 Build 이력을 Container 교체 후에도 유지</td>
    </tr>
  </tbody>
</table>

<p>WebSocket으로 Agent를 연결하거나 외부 Agent를 사용하지 않는다면 <code class="language-plaintext highlighter-rouge">50000</code> Port 공개는 필요하지 않다. Host의 <code class="language-plaintext highlighter-rouge">/var/run/docker.sock</code>을 Mount하고 Container를 <code class="language-plaintext highlighter-rouge">root</code>로 실행하면 Jenkins Job이 Host Docker Daemon을 사실상 제어할 수 있다. 단순한 실습에는 편리하지만 Host 권한 탈취로 이어질 수 있으므로 위 기본 설치에는 포함하지 않는다.</p>

<p>Compose File이 있는 Directory에서 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker compose up <span class="nt">-d</span>
docker compose ps
docker compose logs jenkins
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker compose ps</code>에서 Container가 실행 중인지 확인하고, <code class="language-plaintext highlighter-rouge">docker compose logs jenkins</code>에서 초기화 진행과 오류를 확인한다. Jenkins는 <code class="language-plaintext highlighter-rouge">http://localhost:8080</code>으로 접속한다.</p>

<h2 id="6--초기-잠금-해제와-관리자-생성">6 ) 초기 잠금 해제와 관리자 생성</h2>

<hr />

<p>최초 실행 시 Jenkins가 생성한 초기 관리자 Password로 Setup Wizard의 잠금을 해제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec </span>jenkins <span class="se">\</span>
  <span class="nb">cat</span> /var/jenkins_home/secrets/initialAdminPassword
</code></pre></div></div>

<p>이 값은 현재 Instance의 인증 정보이므로 문서, Source Repository 또는 화면 캡처에 남기지 않는다. 화면에 입력한 뒤 추천 Plugin을 설치하거나 필요한 Plugin을 선택하고, 실제로 사용할 관리자 계정을 생성한다.</p>

<p>초기 설정 순서는 다음과 같다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">http://localhost:8080</code>에 접속한다.</p>
  </li>
  <li>
    <p>명령으로 확인한 초기 Password를 입력한다.</p>
  </li>
  <li>
    <p>추천 Plugin 설치 또는 Plugin 선택 설치를 진행한다.</p>
  </li>
  <li>
    <p>관리자 계정과 Jenkins URL을 설정한다.</p>
  </li>
  <li>
    <p>Dashboard가 표시되는지 확인한다.</p>
  </li>
</ol>

<h2 id="7--plugin-관리">7 ) Plugin 관리</h2>

<hr />

<p><strong>Manage Jenkins → Plugins</strong>에서 설치 가능, 설치됨과 Update 대상 Plugin을 확인한다.</p>

<table>
  <thead>
    <tr>
      <th>Plugin</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Git</td>
      <td>Git Repository Checkout과 SCM 변경 확인</td>
    </tr>
    <tr>
      <td>Maven Integration</td>
      <td>Maven Project 유형과 Maven Build 연동</td>
    </tr>
    <tr>
      <td>Email Extension</td>
      <td>Build 결과에 따른 확장 Email 알림</td>
    </tr>
    <tr>
      <td>Credentials Binding</td>
      <td>Credential을 Build 환경 변수나 File로 제한적으로 주입</td>
    </tr>
    <tr>
      <td>Throttle Concurrent Builds</td>
      <td>Job 또는 Category별 동시 Build 수 제한</td>
    </tr>
  </tbody>
</table>

<p>Plugin이 많아질수록 기능뿐 아니라 의존성, Upgrade와 보안 관리 범위도 커진다. Job에 필요한 Plugin만 설치하고 Jenkins Core를 Upgrade하기 전에 Plugin 호환성을 확인한다.</p>

<p>Update Center 오류가 발생하면 다음을 점검한다.</p>

<ol>
  <li>
    <p>Jenkins Container의 DNS와 외부 Network 연결을 확인한다.</p>
  </li>
  <li>
    <p>Host와 Container의 날짜와 시간이 올바른지 확인한다.</p>
  </li>
  <li>
    <p>Proxy 환경이면 <strong>Manage Jenkins → Plugins → Advanced settings</strong>에서 Proxy를 설정한다.</p>
  </li>
  <li>
    <p>사설 CA를 사용한다면 인증서를 Java Truststore에 올바르게 등록한다.</p>
  </li>
  <li>
    <p>Update Center Metadata를 다시 불러오고 Jenkins Log의 원인을 확인한다.</p>
  </li>
</ol>

<p><code class="language-plaintext highlighter-rouge">SSLHandshakeException</code>을 피하려고 Update Center URL을 HTTPS에서 HTTP로 바꾸면 Plugin과 Metadata 전송의 기밀성과 무결성을 잃는다. 인증서 Chain, CA Truststore와 Proxy의 TLS Inspection 여부를 수정해야 한다.</p>

<p>현재 명칭과 설치 요구 사항은 <a href="https://www.jenkins.io/doc/book/glossary/">Jenkins 용어집</a>, <a href="https://www.jenkins.io/doc/book/installing/">Jenkins 설치 문서</a>와 <a href="https://www.jenkins.io/doc/book/installing/docker/">Docker 설치 문서</a>에서 확인할 수 있다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Controller는 Trigger, Queue, 설정과 결과를 관리하고 Agent의 Executor가 실제 Build Step을 실행한다.</p>
    </li>
    <li>
      <p>Agent Label과 Executor 수는 작업의 실행 위치와 동시 실행 용량을 결정한다.</p>
    </li>
    <li>
      <p>독립된 Controller를 늘리는 것은 자동 Failover가 아니며 관리 대상도 함께 늘어난다.</p>
    </li>
    <li>
      <p>Docker 설치에서는 <code class="language-plaintext highlighter-rouge">/var/jenkins_home</code>을 영속화하고 외부 Agent 사용 여부에 따라 <code class="language-plaintext highlighter-rouge">50000</code> Port를 결정한다.</p>
    </li>
    <li>
      <p>초기 Password와 Credential은 문서나 Repository에 남기지 않고, TLS 오류는 HTTP 전환이 아니라 인증서와 Network 원인을 수정한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Jenkins" /><summary type="html"><![CDATA[Jenkins의 Job 실행 흐름을 Controller, Queue, Agent와 Executor 관점에서 이해하고 Docker로 설치해 초기 설정과 Plugin을 관리한다]]></summary></entry><entry><title type="html">Jenkins Freestyle Job과 Credentials</title><link href="https://hyn128.site/cloud-native-48-jenkins-freestyle-credentials/" rel="alternate" type="text/html" title="Jenkins Freestyle Job과 Credentials" /><published>2026-09-14T00:00:00+09:00</published><updated>2026-09-14T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-48-jenkins-freestyle-credentials</id><content type="html" xml:base="https://hyn128.site/cloud-native-48-jenkins-freestyle-credentials/"><![CDATA[<p><a href="/cloud-native-47-jenkins-architecture-installation/">Jenkins Controller-Agent 구조와 설치</a>에서 Controller가 Trigger와 Queue를 관리하고 Agent의 Executor가 Build를 실행하는 구조를 정리했다. 이번에는 Jenkins가 수행할 작업을 Freestyle Job으로 정의하고, 실행 결과와 외부 서비스 인증 정보를 관리한다.</p>

<h2 id="1--job-build와-freestyle">1 ) Job, Build와 Freestyle</h2>

<hr />

<blockquote>
  <p><strong>Job</strong>
Jenkins가 수행해야 할 작업의 설정이다. 언제 시작하고, 어느 Source를 가져오며, 어떤 명령을 실행하고, 완료 후 무엇을 할지 정의한다.</p>
</blockquote>

<blockquote>
  <p><strong>Build</strong>
Job을 한 번 실행한 결과이다. 실행할 때마다 고유한 Build Number가 붙고 상태, Console Log와 Artifact가 연결된다.</p>
</blockquote>

<p>Freestyle Job은 화면에서 SCM, Trigger, Build Step과 Post-build Action을 조합하는 일반적인 Job 유형이다. 간단한 Test, Build, Package와 알림을 빠르게 구성하기 좋다. 복잡한 분기와 반복 가능한 Code Review가 필요하면 이후에 다룰 Pipeline과 <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>이 더 적합하다.</p>

<table>
  <thead>
    <tr>
      <th>설정 영역</th>
      <th>답하는 질문</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>General</td>
      <td>Job 자체를 어떻게 관리하는가</td>
      <td>설명, Parameter, Build 보존, 동시 실행</td>
    </tr>
    <tr>
      <td>Source Code Management</td>
      <td>어떤 Source를 가져오는가</td>
      <td>Git Repository, Branch, Credential</td>
    </tr>
    <tr>
      <td>Build Triggers</td>
      <td>언제 Build를 시작하는가</td>
      <td>수동, 일정, Poll SCM, Webhook, Upstream</td>
    </tr>
    <tr>
      <td>Build Environment</td>
      <td>실행 직전에 어떤 환경을 준비하는가</td>
      <td>Workspace 정리, Secret 주입, Timestamp, Timeout</td>
    </tr>
    <tr>
      <td>Build Steps</td>
      <td>실제로 무엇을 실행하는가</td>
      <td>Shell, Batch, Maven, Gradle</td>
    </tr>
    <tr>
      <td>Post-build Actions</td>
      <td>실행 후 무엇을 남기거나 알리는가</td>
      <td>Artifact 보관, Test Report, Email, Downstream Job</td>
    </tr>
  </tbody>
</table>

<h2 id="2--freestyle-job-생성과-첫-build">2 ) Freestyle Job 생성과 첫 Build</h2>

<hr />

<p>Dashboard에서 <strong>New Item</strong>을 선택하고 이름을 입력한 뒤 <strong>Freestyle project</strong>를 선택한다. Description에는 이 Job이 다루는 Repository, Build 결과와 실행 목적을 적는다.</p>

<p><strong>Build Steps → Add build step → Execute shell</strong>을 선택하고 다음 명령을 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"Welcome to my first project using Jenkins"</span>
</code></pre></div></div>

<p>저장 후 <strong>Build Now</strong>를 누르면 실행 요청이 Queue에 들어가고 사용 가능한 Executor가 Build를 수행한다. Build History에서 생성된 Build Number를 선택하고 <strong>Console Output</strong>을 열어 다음 항목을 확인한다.</p>

<ul>
  <li>
    <p>어떤 Node와 Workspace에서 실행됐는가</p>
  </li>
  <li>
    <p>어떤 사용자 또는 Trigger가 시작했는가</p>
  </li>
  <li>
    <p>Shell 명령과 출력은 무엇인가</p>
  </li>
  <li>
    <p>Process 종료 Code와 최종 Build 상태는 무엇인가</p>
  </li>
</ul>

<p>Job의 <strong>Workspace</strong> 메뉴는 현재 Source와 작업 File을 보여준다. 기본 경로 형식은 다음과 같지만 실제 경로는 Build를 실행한 Node와 Jenkins Home 설정에 따라 달라진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/var/jenkins_home/workspace/&lt;job-name&gt;
</code></pre></div></div>

<p>Workspace는 작업 공간이며 보관소가 아니다. Cleanup 정책이나 Agent 교체로 사라질 수 있으므로 이후 Build나 배포에 필요한 결과는 Artifact로 보관하거나 외부 Artifact Repository에 올린다.</p>

<h2 id="3--general-설정">3 ) General 설정</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>동작</th>
      <th>적용 판단</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Discard old builds</td>
      <td>기간이나 개수를 넘은 Build Record와 Artifact 삭제</td>
      <td>Disk 사용량을 제어할 때 설정</td>
    </tr>
    <tr>
      <td>This project is parameterized</td>
      <td>실행 시 문자열, 선택 값 등 입력 수신</td>
      <td>환경이나 Version을 선택해야 할 때 사용</td>
    </tr>
    <tr>
      <td>Execute concurrent builds</td>
      <td>같은 Job의 Build 여러 개를 동시에 실행</td>
      <td>Workspace와 외부 Resource 충돌이 없을 때만 사용</td>
    </tr>
    <tr>
      <td>Throttle builds</td>
      <td>Plugin 정책으로 동시 실행 수 제한</td>
      <td>Agent 과부하나 외부 API 제한을 제어할 때 사용</td>
    </tr>
    <tr>
      <td>Quiet period</td>
      <td>요청 후 지정한 초만큼 Queue에서 대기</td>
      <td>짧은 시간에 연속된 변경을 모아 실행할 때 사용</td>
    </tr>
    <tr>
      <td>Retry Count</td>
      <td>SCM Checkout 실패 시 재시도</td>
      <td>일시적인 Network 오류를 허용할 때 사용</td>
    </tr>
    <tr>
      <td>Custom workspace</td>
      <td>기본값이 아닌 Directory 사용</td>
      <td>공유 Directory 충돌과 정리 책임을 검토한 뒤 사용</td>
    </tr>
    <tr>
      <td>Display Name</td>
      <td>UI에 표시할 별도 이름 설정</td>
      <td>Job 이름과 사용자 표시 이름을 구분할 때 사용</td>
    </tr>
  </tbody>
</table>

<p>Quiet Period는 첫 Commit을 누락시키는 기능이 아니다. 실행 요청을 잠시 대기시켜 짧은 시간에 들어온 후속 요청과 함께 처리할 기회를 제공한다. 너무 길게 설정하면 Feedback도 늦어진다.</p>

<p>동시 Build를 허용하면 같은 Workspace, 고정 Port, Test DB 또는 배포 대상을 동시에 수정할 수 있다. 단순히 Agent 성능이 좋다는 이유만으로 켜지 않고 작업 간 격리가 보장되는지 확인한다.</p>

<h2 id="4--upstream과-downstream">4 ) Upstream과 Downstream</h2>

<hr />

<p>한 Job의 결과를 다른 Job이 사용하면 실행 순서를 정의해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>image-build (upstream) → kubernetes-deploy (downstream)
</code></pre></div></div>

<p>Image를 만드는 <code class="language-plaintext highlighter-rouge">image-build</code>가 완료되기 전에 <code class="language-plaintext highlighter-rouge">kubernetes-deploy</code>가 시작되면 존재하지 않거나 이전 Version의 Image를 배포할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>방지하려는 상황</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Block build when upstream project is building</td>
      <td>선행 Job이 실행 또는 대기 중인데 후속 Job이 먼저 시작되는 상황</td>
    </tr>
    <tr>
      <td>Block build when downstream project is building</td>
      <td>후속 Job이 사용하는 대상을 선행 Job이 동시에 변경하는 상황</td>
    </tr>
    <tr>
      <td>Build after other projects are built</td>
      <td>선행 Job의 결과에 따라 현재 Job을 시작해야 하는 상황</td>
    </tr>
  </tbody>
</table>

<p>선행 Job의 Artifact를 사용하는 Build는 어떤 Build Number와 Artifact를 전달했는지도 함께 추적해야 한다. 단순 실행 순서만 연결하면 재실행 시 서로 다른 Version이 섞일 수 있다.</p>

<h2 id="5--build-trigger-선택">5 ) Build Trigger 선택</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>Trigger</th>
      <th>동작</th>
      <th>적합한 경우</th>
      <th>주의 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Build Now</td>
      <td>사용자가 화면에서 실행</td>
      <td>설정 확인, 일회성 작업</td>
      <td>반복 자동화에 부적합</td>
    </tr>
    <tr>
      <td>Trigger builds remotely</td>
      <td>Token이 포함된 Endpoint를 외부 Script가 호출</td>
      <td>제한된 외부 시스템 연동</td>
      <td>HTTPS, 인증과 Token 노출 방지 필요</td>
    </tr>
    <tr>
      <td>Build after other projects are built</td>
      <td>Upstream Build 결과에 따라 실행</td>
      <td>Job 간 순서 연결</td>
      <td>Artifact Version 연결 필요</td>
    </tr>
    <tr>
      <td>Build periodically</td>
      <td>Cron 일정에 따라 실행</td>
      <td>Source 변경과 무관한 정기 작업</td>
      <td>변경이 없어도 실행</td>
    </tr>
    <tr>
      <td>Poll SCM</td>
      <td>일정마다 SCM 변경 여부를 확인하고 변경 시 실행</td>
      <td>Jenkins가 외부 요청을 받을 수 없는 환경</td>
      <td>Polling 요청과 감지 지연 발생</td>
    </tr>
    <tr>
      <td>GitHub hook trigger</td>
      <td>GitHub Webhook Event 수신 시 실행</td>
      <td>변경 직후 Build가 필요한 환경</td>
      <td>GitHub에서 Jenkins Endpoint로 접근 가능해야 함</td>
    </tr>
  </tbody>
</table>

<p>Remote Trigger URL은 다음 형태를 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://&lt;jenkins-host&gt;/job/&lt;job-name&gt;/build?token=&lt;token&gt;
</code></pre></div></div>

<p>Token을 URL에 넣으면 Browser History, Proxy와 Access Log에 남을 수 있다. 외부 호출에는 HTTPS와 별도 인증을 적용하고 권한을 최소화한다.</p>

<p>Cron 표현식 <code class="language-plaintext highlighter-rouge">H/5 * * * *</code>를 Poll SCM에 설정하면 Jenkins가 부하를 분산한 시작 시점을 기준으로 약 5분 간격으로 변경을 확인한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H/5 * * * *
</code></pre></div></div>

<p>Webhook은 Event가 발생할 때 GitHub가 Jenkins에 알리므로 일반적으로 Poll SCM보다 빠르고 불필요한 조회가 적다. 그러나 외부에서 Jenkins에 접근할 수 없는 내부 실습 환경에서는 Poll SCM이 단순할 수 있다. 실제 Webhook 생성과 Network 공개는 별도 배포 범위에서 다룬다.</p>

<h2 id="6--build-environment와-step">6 ) Build Environment와 Step</h2>

<hr />

<p>Build Environment는 Step 실행 전에 Workspace와 실행 조건을 준비한다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
      <th>필요한 Plugin 또는 조건</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Delete workspace before build starts</td>
      <td>이전 Build의 Source와 중간 File 제거</td>
      <td>Workspace Cleanup Plugin이 제공할 수 있음</td>
    </tr>
    <tr>
      <td>Use secret text(s) or file(s)</td>
      <td>Credential을 환경 변수나 임시 File로 주입</td>
      <td>Credentials Binding Plugin</td>
    </tr>
    <tr>
      <td>Add timestamps to Console Output</td>
      <td>Log 각 줄에 시간 표시</td>
      <td>Timestamper Plugin</td>
    </tr>
    <tr>
      <td>Terminate a build if it is stuck</td>
      <td>지정 시간 초과 시 Build 중단</td>
      <td>Build Timeout Plugin</td>
    </tr>
    <tr>
      <td>Inspect build log for published build scans</td>
      <td>Maven·Gradle Build Scan URL 수집</td>
      <td>관련 Build Scan Plugin</td>
    </tr>
    <tr>
      <td>With Ant</td>
      <td>Jenkins에 설정한 Ant Tool 사용</td>
      <td>Ant Plugin과 Tool 설정</td>
    </tr>
  </tbody>
</table>

<p>Build Step은 작성 순서대로 실행된다. 앞 Step이 실패하면 기본적으로 뒤 Step으로 진행하지 않는다. Linux와 macOS Agent에서는 <strong>Execute shell</strong>, Windows Agent에서는 <strong>Execute Windows batch command</strong> 또는 PowerShell 관련 Step을 사용한다.</p>

<p>Ant는 Java Build 자동화 도구이며 Compile, Test와 Package를 명시적인 Target으로 구성한다. Maven과 Gradle을 사용하는 Project도 많으므로 Build Tool은 Project의 실제 설정 File에 맞춰 선택한다.</p>

<p>Post-build Action은 Build Step이 끝난 뒤 Report 발행, Artifact 보관, Email 알림이나 Downstream Job 실행 같은 후속 처리를 수행한다. Post-build Action 자체의 결과에 따라 Build가 <code class="language-plaintext highlighter-rouge">Unstable</code>로 바뀔 수도 있으므로 Console Log와 상태 원인을 함께 확인한다.</p>

<h2 id="7--credentials가-필요한-이유">7 ) Credentials가 필요한 이유</h2>

<hr />

<p>Jenkins Job은 Private Git Repository, Artifact Repository, Cloud API와 배포 Server 같은 외부 시스템에 접속한다. 인증 값을 Job 설정이나 Shell Script에 직접 넣으면 다음 문제가 생긴다.</p>

<ul>
  <li>
    <p>Job 설정을 볼 수 있는 사용자에게 Secret이 노출된다.</p>
  </li>
  <li>
    <p>Script를 Git에 Commit하면 이력에 Secret이 남는다.</p>
  </li>
  <li>
    <p>값이 바뀔 때마다 여러 Job을 수정해야 한다.</p>
  </li>
  <li>
    <p>동일한 계정을 여러 곳에 복사해 사용 범위와 폐기 지점을 추적하기 어렵다.</p>
  </li>
</ul>

<p>Credentials Store에 값을 등록하면 식별자인 Credential ID로 필요한 Job에 연결하고, 접근 범위와 교체 지점을 중앙에서 관리할 수 있다. Jenkins가 값을 저장한다고 해서 모든 노출을 자동으로 막는 것은 아니다. Job이 Secret을 출력하거나 외부로 전송할 권한은 여전히 가지므로 Script와 Plugin을 신뢰할 수 있어야 한다.</p>

<h2 id="8--credential-종류-scope와-domain">8 ) Credential 종류, Scope와 Domain</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>사용 예</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Username with password</td>
      <td>HTTP Basic 인증, Username과 Token 조합</td>
    </tr>
    <tr>
      <td>SSH Username with private key</td>
      <td>SSH 방식 Git Checkout, 원격 Server 접속</td>
    </tr>
    <tr>
      <td>Secret text</td>
      <td>API Token, 단일 Password</td>
    </tr>
    <tr>
      <td>Secret file</td>
      <td>Service Account JSON, 인증서 File</td>
    </tr>
    <tr>
      <td>Certificate</td>
      <td>PKCS#12 인증서와 Password</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Scope</th>
      <th>사용 범위</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Global</td>
      <td>일반적인 Job과 Item에서 사용할 수 있는 Credential</td>
    </tr>
    <tr>
      <td>System</td>
      <td>Agent 연결이나 System Email 같은 Jenkins 내부 기능 전용 Credential</td>
    </tr>
  </tbody>
</table>

<p>Domain은 Hostname, Port 또는 Protocol 같은 조건으로 Credential을 그룹화하고 선택 후보를 좁힌다. Domain은 강한 보안 경계 자체가 아니므로 Folder 권한, Role과 Credential Scope도 함께 설계해야 한다.</p>

<p>Credential은 <strong>Manage Jenkins → Credentials</strong>에서 Store와 Domain을 선택한 뒤 추가한다. Job에서 Credential을 참조할 때는 의미가 드러나는 ID를 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>github-readonly
dockerhub-push
production-ssh
</code></pre></div></div>

<p>Private Git Repository를 연결할 때 <strong>Source Code Management → Git</strong>에 Repository URL과 Branch를 입력하고, URL 방식에 맞는 Credential을 선택한다.</p>

<table>
  <thead>
    <tr>
      <th>Repository URL</th>
      <th>적합한 Credential</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">https://github.com/&lt;owner&gt;/&lt;repo&gt;.git</code></td>
      <td>Username과 Personal Access Token</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git@github.com:&lt;owner&gt;/&lt;repo&gt;.git</code></td>
      <td>SSH Username과 Private Key</td>
    </tr>
  </tbody>
</table>

<p>Public Repository는 읽기만 할 경우 Credential이 없어도 Checkout할 수 있다. Private Repository는 Repository 읽기 범위만 가진 Credential을 우선 사용하고, Build에 Push 권한까지 불필요하게 주지 않는다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Job은 작업 정의이고 Build는 Job을 한 번 실행한 이력이며 Build Number, 상태와 Log를 가진다.</p>
    </li>
    <li>
      <p>Freestyle Job은 General, SCM, Trigger, Environment, Build Step과 Post-build Action 순으로 실행 조건과 작업을 구성한다.</p>
    </li>
    <li>
      <p>Poll SCM은 Jenkins가 변경을 조회하고 Webhook은 GitHub가 변경 Event를 전달한다.</p>
    </li>
    <li>
      <p>Workspace는 일시적인 작업 공간이므로 필요한 결과는 Artifact로 별도 보관한다.</p>
    </li>
    <li>
      <p>Credential은 ID로 참조하고 Job에 필요한 최소 권한과 범위만 제공한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Jenkins" /><summary type="html"><![CDATA[Jenkins Freestyle Job의 설정 영역과 Build 실행 흐름을 이해하고 SCM 및 외부 서비스 인증 정보를 Credentials로 관리한다]]></summary></entry><entry><title type="html">Jenkins Maven·Gradle Build와 Email 알림</title><link href="https://hyn128.site/cloud-native-49-jenkins-maven-gradle-build/" rel="alternate" type="text/html" title="Jenkins Maven·Gradle Build와 Email 알림" /><published>2026-09-14T00:00:00+09:00</published><updated>2026-09-14T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-49-jenkins-maven-gradle-build</id><content type="html" xml:base="https://hyn128.site/cloud-native-49-jenkins-maven-gradle-build/"><![CDATA[<p><a href="/cloud-native-48-jenkins-freestyle-credentials/">Jenkins Freestyle Job과 Credentials</a>에서 Job의 SCM, Trigger, Build Step과 Credential을 정리했다. 이번에는 Java Project의 Dependency, Compile, Test와 Package 과정을 Maven과 Gradle에 맡기고 Jenkins가 같은 명령을 반복 실행하도록 구성한다.</p>

<h2 id="1--java-build-tool이-담당하는-과정">1 ) Java Build Tool이 담당하는 과정</h2>

<hr />

<p>Java Application을 배포 가능한 결과물로 만들려면 다음 작업이 필요하다.</p>

<ol>
  <li>
    <p>외부 Artifact Repository에서 Third-party Library와 Plugin을 내려받는다.</p>
  </li>
  <li>
    <p>Dependency를 Compile과 Test Classpath에 연결한다.</p>
  </li>
  <li>
    <p>Application Source와 Test Source를 Compile한다.</p>
  </li>
  <li>
    <p>Unit Test를 실행하고 Report를 만든다.</p>
  </li>
  <li>
    <p>실행 가능한 JAR 또는 WAR로 Package한다.</p>
  </li>
  <li>
    <p>필요한 경우 Artifact Repository에 결과물을 배포한다.</p>
  </li>
</ol>

<p>Ant, Maven과 Gradle은 이 과정을 자동화하는 Java Build Tool이다. Jenkins는 Build Tool을 대체하지 않는다. Jenkins가 Build를 시작하고 환경과 결과를 관리하며, 실제 Compile과 Test는 Project에 정의된 Maven Goal이나 Gradle Task가 수행한다.</p>

<table>
  <thead>
    <tr>
      <th>도구</th>
      <th>핵심 설정</th>
      <th>실행 단위</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Ant</td>
      <td><code class="language-plaintext highlighter-rouge">build.xml</code></td>
      <td>Target</td>
      <td>실행 절차를 명시적으로 구성</td>
    </tr>
    <tr>
      <td>Maven</td>
      <td><code class="language-plaintext highlighter-rouge">pom.xml</code></td>
      <td>Lifecycle Phase와 Goal</td>
      <td>Convention과 표준 Directory 중심</td>
    </tr>
    <tr>
      <td>Gradle</td>
      <td><code class="language-plaintext highlighter-rouge">build.gradle</code> 또는 <code class="language-plaintext highlighter-rouge">build.gradle.kts</code></td>
      <td>Task</td>
      <td>Task Graph와 DSL 기반 구성</td>
    </tr>
  </tbody>
</table>

<h2 id="2--maven-lifecycle과-결과-directory">2 ) Maven Lifecycle과 결과 Directory</h2>

<hr />

<p>Maven은 Lifecycle의 Phase를 순서대로 실행한다. 뒤 Phase를 요청하면 앞 Phase도 함께 실행된다.</p>

<table>
  <thead>
    <tr>
      <th>Phase</th>
      <th>주요 동작</th>
      <th>대표 결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">clean</code></td>
      <td>이전 Build 결과 제거</td>
      <td><code class="language-plaintext highlighter-rouge">target</code> 정리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">compile</code></td>
      <td><code class="language-plaintext highlighter-rouge">src/main/java</code> Source Compile</td>
      <td><code class="language-plaintext highlighter-rouge">target/classes</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test-compile</code></td>
      <td><code class="language-plaintext highlighter-rouge">src/test/java</code> Test Source Compile</td>
      <td><code class="language-plaintext highlighter-rouge">target/test-classes</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">test</code></td>
      <td>Surefire Plugin으로 JUnit·TestNG Test 실행</td>
      <td><code class="language-plaintext highlighter-rouge">target/surefire-reports</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">package</code></td>
      <td>Packaging 설정에 따라 JAR 또는 WAR 생성</td>
      <td><code class="language-plaintext highlighter-rouge">target/*.jar</code>, <code class="language-plaintext highlighter-rouge">target/*.war</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">install</code></td>
      <td>Local Maven Repository에 Artifact 설치</td>
      <td><code class="language-plaintext highlighter-rouge">~/.m2/repository</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">deploy</code></td>
      <td>Remote Artifact Repository에 배포</td>
      <td>원격 Repository Artifact</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">mvn clean package</code>는 이전 결과를 지운 뒤 <code class="language-plaintext highlighter-rouge">package</code>까지 필요한 Compile과 Test Phase를 순서대로 실행한다. 따라서 <code class="language-plaintext highlighter-rouge">mvn clean compile package</code>처럼 <code class="language-plaintext highlighter-rouge">compile</code>을 별도로 넣을 필요가 없다.</p>

<p>Maven은 <a href="https://central.sonatype.com/">Maven Central</a> 같은 Repository에서 Project Dependency뿐 아니라 Build Plugin의 Dependency도 해결한다. Network가 제한된 Jenkins Agent에서는 Proxy, 사내 Mirror와 Local Cache 정책을 별도로 설정해야 한다.</p>

<h2 id="3--maven-spring-calculator-project">3 ) Maven Spring Calculator Project</h2>

<hr />

<p>Spring Initializr에서 Maven Project를 만들고 Spring Web과 Spring Boot DevTools Dependency를 선택한다. WAR가 필요하면 Packaging을 <code class="language-plaintext highlighter-rouge">war</code>로 설정하고, 단독 실행 JAR가 목적이면 기본 <code class="language-plaintext highlighter-rouge">jar</code>를 사용한다.</p>

<p><code class="language-plaintext highlighter-rouge">src/main/java/&lt;package&gt;/Calculator.java</code>를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.stereotype.Service</span><span class="o">;</span>

<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Calculator</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">addition</span><span class="o">(</span><span class="kt">int</span> <span class="n">num1</span><span class="o">,</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">num1</span> <span class="o">+</span> <span class="n">num2</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">subtraction</span><span class="o">(</span><span class="kt">int</span> <span class="n">num1</span><span class="o">,</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">num1</span> <span class="o">-</span> <span class="n">num2</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">multiplication</span><span class="o">(</span><span class="kt">int</span> <span class="n">num1</span><span class="o">,</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">num1</span> <span class="o">*</span> <span class="n">num2</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">src/main/java/&lt;package&gt;/CalculatorController.java</code>는 HTTP 요청을 Service에 전달한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RequestMapping</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RequestParam</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RestController</span><span class="o">;</span>

<span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">CalculatorController</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Calculator</span> <span class="n">calculator</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">CalculatorController</span><span class="o">(</span><span class="nc">Calculator</span> <span class="n">calculator</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">calculator</span> <span class="o">=</span> <span class="n">calculator</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">index</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"health check"</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/add"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">add</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num1"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num1</span><span class="o">,</span>
                      <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num2"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">String</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="n">calculator</span><span class="o">.</span><span class="na">addition</span><span class="o">(</span><span class="n">num1</span><span class="o">,</span> <span class="n">num2</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/sub"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">sub</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num1"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num1</span><span class="o">,</span>
                      <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num2"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">String</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="n">calculator</span><span class="o">.</span><span class="na">subtraction</span><span class="o">(</span><span class="n">num1</span><span class="o">,</span> <span class="n">num2</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/mul"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">mul</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num1"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num1</span><span class="o">,</span>
                      <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"num2"</span><span class="o">)</span> <span class="kt">int</span> <span class="n">num2</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">String</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="n">calculator</span><span class="o">.</span><span class="na">multiplication</span><span class="o">(</span><span class="n">num1</span><span class="o">,</span> <span class="n">num2</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>TestNG를 사용하려면 <code class="language-plaintext highlighter-rouge">pom.xml</code>의 <code class="language-plaintext highlighter-rouge">dependencies</code>에 Test Scope Dependency를 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.testng<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>testng<span class="nt">&lt;/artifactId&gt;</span>
    <span class="nt">&lt;version&gt;</span>7.11.0<span class="nt">&lt;/version&gt;</span>
    <span class="nt">&lt;scope&gt;</span>test<span class="nt">&lt;/scope&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<p>Test Source는 Maven 표준 경로인 <code class="language-plaintext highlighter-rouge">src/test/java/&lt;package&gt;/TestAdditionFunctionality.java</code>에 둔다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.testng.Assert</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.testng.annotations.AfterClass</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.testng.annotations.BeforeClass</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.testng.annotations.BeforeMethod</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.testng.annotations.Test</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">TestAdditionFunctionality</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">Calculator</span> <span class="n">calculator</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">result</span><span class="o">;</span>

    <span class="nd">@BeforeClass</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">init</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">calculator</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Calculator</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="nd">@BeforeMethod</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">resetResult</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">result</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Test</span><span class="o">(</span><span class="n">priority</span> <span class="o">=</span> <span class="mi">1</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">testAdditionWithPositiveNumbers</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">result</span> <span class="o">=</span> <span class="n">calculator</span><span class="o">.</span><span class="na">addition</span><span class="o">(</span><span class="mi">10</span><span class="o">,</span> <span class="mi">20</span><span class="o">);</span>
        <span class="nc">Assert</span><span class="o">.</span><span class="na">assertEquals</span><span class="o">(</span><span class="n">result</span><span class="o">,</span> <span class="mi">30</span><span class="o">,</span> <span class="s">"positive number"</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@AfterClass</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">tearDown</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">calculator</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@BeforeClass</code>는 Class의 Test 전에 한 번 실행하고, <code class="language-plaintext highlighter-rouge">@BeforeMethod</code>는 각 Test Method 전에 실행한다. <code class="language-plaintext highlighter-rouge">@AfterClass</code>는 Class의 Test가 끝난 뒤 정리한다. Group 단위 준비가 필요한 Test에는 <code class="language-plaintext highlighter-rouge">@BeforeGroups</code>를 추가할 수 있지만 Group을 사용하지 않는 예제에는 불필요하다.</p>

<p><code class="language-plaintext highlighter-rouge">src/main/resources/application.properties</code>에서 실행 Port를 지정한다.</p>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">server.port</span><span class="p">=</span><span class="s">80</span>
</code></pre></div></div>

<p>Port <code class="language-plaintext highlighter-rouge">80</code>은 Linux에서 일반 사용자 Process가 바로 Binding하지 못할 수 있고 기존 Web Server와 충돌할 수도 있다. Local 검증에서는 기본 <code class="language-plaintext highlighter-rouge">8080</code> 또는 사용하지 않는 일반 Port를 선택해도 된다.</p>

<p>Local에서 먼저 Test와 Package를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./mvnw clean package
find target <span class="nt">-maxdepth</span> 2 <span class="nt">-type</span> f
</code></pre></div></div>

<p>Maven Wrapper가 없다면 설치된 Maven으로 <code class="language-plaintext highlighter-rouge">mvn clean package</code>를 실행한다. Test 실패 시 명령이 0이 아닌 종료 Code를 반환하고 Package 단계도 실패한다.</p>

<p>검증된 Project를 Remote Repository에 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Add Maven calculator project"</span>
git push
</code></pre></div></div>

<h2 id="4--jenkins-maven-build-구성">4 ) Jenkins Maven Build 구성</h2>

<hr />

<p>Maven Project 유형을 사용하려면 Maven Integration Plugin을 설치한다. Freestyle Job에서 Shell로 Maven Wrapper를 실행하는 방식이라면 Maven Project 유형 없이도 구성할 수 있다.</p>

<h3 id="jenkins-tool로-maven-사용">Jenkins Tool로 Maven 사용</h3>

<ol>
  <li>
    <p><strong>Manage Jenkins → Plugins</strong>에서 Git과 Maven Integration Plugin을 확인한다.</p>
  </li>
  <li>
    <p><strong>Manage Jenkins → Tools</strong>에서 JDK와 Maven Installation을 추가한다.</p>
  </li>
  <li>
    <p>새 Maven Job을 만들고 <strong>Source Code Management → Git</strong>에 Repository URL과 Branch를 입력한다.</p>
  </li>
  <li>
    <p>Private Repository라면 읽기 권한을 가진 Credential을 선택한다.</p>
  </li>
  <li>
    <p>Maven Goal에 다음 값을 입력한다.</p>
  </li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clean package
</code></pre></div></div>

<ol>
  <li>저장 후 <strong>Build Now</strong>를 실행한다.</li>
</ol>

<p><code class="language-plaintext highlighter-rouge">mvn: command not found</code> 또는 Maven Installation을 찾을 수 없다는 오류가 발생하면 <strong>Manage Jenkins → Tools</strong>에서 Maven 이름과 자동 설치 설정을 확인한다. Job이 참조한 Tool 이름과 설정 이름도 일치해야 한다.</p>

<h3 id="maven-wrapper-사용">Maven Wrapper 사용</h3>

<p>Project에 <code class="language-plaintext highlighter-rouge">mvnw</code>와 <code class="language-plaintext highlighter-rouge">.mvn/wrapper</code>가 Commit되어 있다면 Freestyle Job의 <strong>Execute shell</strong>에서 다음과 같이 실행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x ./mvnw
./mvnw clean package
</code></pre></div></div>

<p>Wrapper를 사용하면 Project가 요구하는 Maven Version을 Source와 함께 관리할 수 있다. 다만 JDK는 Agent에 별도로 준비해야 하고 Wrapper가 처음 Maven Distribution을 받을 수 있도록 Network 접근이 필요하다.</p>

<p>Build가 끝나면 다음을 확인한다.</p>

<ul>
  <li>
    <p>Console Output에서 Dependency Download, Compile과 Test 결과</p>
  </li>
  <li>
    <p>Workspace의 <code class="language-plaintext highlighter-rouge">target/classes</code>와 <code class="language-plaintext highlighter-rouge">target/test-classes</code></p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">target/surefire-reports</code>의 Test Report</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">target</code>에 생성된 JAR 또는 WAR</p>
  </li>
</ul>

<p>Jenkins Container에서 직접 확인해야 한다면 다음 명령을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker <span class="nb">exec</span> <span class="nt">-it</span> jenkins bash
<span class="nb">cd</span> /var/jenkins_home/workspace/&lt;job-name&gt;
find target <span class="nt">-maxdepth</span> 2 <span class="nt">-type</span> f
</code></pre></div></div>

<p>단순히 File이 존재하는지만 확인하지 않고 해당 Build Number와 연결해 Artifact를 보관한다. Freestyle Job의 <strong>Post-build Actions → Archive the artifacts</strong>에 다음 Pattern을 지정할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>target/*.jar, target/*.war
</code></pre></div></div>

<h2 id="5--poll-scm으로-변경-감지">5 ) Poll SCM으로 변경 감지</h2>

<hr />

<p>Jenkins가 외부 Webhook을 받을 수 없는 환경에서는 <strong>Build Triggers → Poll SCM</strong>을 사용해 Repository 변경을 확인한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H/5 * * * *
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">H/5</code>는 Jenkins가 Job별 시작 시점을 분산하면서 약 5분 간격으로 확인하도록 한다. Polling할 때마다 Build하는 것이 아니라 SCM Revision이 바뀐 경우에 Build를 시작한다. 설정 후 Commit을 Push하고 다음을 확인한다.</p>

<ol>
  <li>
    <p>Polling Log에서 변경 Commit을 감지했는가</p>
  </li>
  <li>
    <p>새 Build Number가 생성됐는가</p>
  </li>
  <li>
    <p>Console Output에 새 Commit ID가 표시되는가</p>
  </li>
  <li>
    <p>Test와 Package가 성공했는가</p>
  </li>
</ol>

<h2 id="6--email-알림">6 ) Email 알림</h2>

<hr />

<p>Jenkins는 Build 결과를 Email로 알릴 수 있다. Gmail이나 Naver 같은 외부 SMTP Server를 사용할 때는 일반 계정 Password 대신 서비스가 제공하는 App Password를 사용한다.</p>

<p><strong>Manage Jenkins → System</strong>의 Email Notification 또는 Extended E-mail Notification 영역에 SMTP Host, Port, TLS와 발신 계정을 설정한다. 항목 이름은 설치한 Plugin과 Jenkins Version에 따라 달라질 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>SMTP Server</td>
      <td>Email을 전달할 Server 주소</td>
    </tr>
    <tr>
      <td>SMTP Port</td>
      <td>TLS 방식에 맞는 연결 Port</td>
    </tr>
    <tr>
      <td>Authentication</td>
      <td>발신 계정과 App Password</td>
    </tr>
    <tr>
      <td>Use SSL/TLS</td>
      <td>SMTP Server가 요구하는 암호화 방식</td>
    </tr>
    <tr>
      <td>Reply-To Address</td>
      <td>수신자가 회신할 주소</td>
    </tr>
  </tbody>
</table>

<p>가능하면 SMTP 인증 값을 Jenkins Credential로 관리하고 접근 권한을 제한한다. App Password를 Job Script, Repository나 문서에 직접 적지 않는다.</p>

<p>Freestyle Job의 <strong>Post-build Actions</strong>에서 Email Notification을 추가하고 수신 주소를 입력한다. Test Email을 보내 Server 설정을 먼저 확인한 뒤 성공, 실패 또는 불안정 상태 중 어떤 경우에 알릴지 정한다. Email 전송 실패와 Application Build 실패는 원인이 다르므로 Console Log의 Build Step과 Post-build Action을 구분해서 확인한다.</p>

<h2 id="7--gradle-spring-calculator-project">7 ) Gradle Spring Calculator Project</h2>

<hr />

<p>Spring Initializr에서 Gradle Project를 만들고 Spring Boot DevTools, Lombok과 Spring Web Dependency를 선택한다.</p>

<p><code class="language-plaintext highlighter-rouge">src/main/java/&lt;package&gt;/Calculator.java</code>를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.stereotype.Service</span><span class="o">;</span>

<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Calculator</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">sum</span><span class="o">(</span><span class="kt">int</span> <span class="n">a</span><span class="o">,</span> <span class="kt">int</span> <span class="n">b</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">src/main/java/&lt;package&gt;/CalculatorController.java</code>를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RequestMapping</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RequestParam</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.RestController</span><span class="o">;</span>

<span class="nd">@RestController</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">CalculatorController</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Calculator</span> <span class="n">calculator</span><span class="o">;</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">health</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"healthy"</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/sum"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">sum</span><span class="o">(</span><span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"a"</span><span class="o">)</span> <span class="nc">Integer</span> <span class="n">a</span><span class="o">,</span>
                      <span class="nd">@RequestParam</span><span class="o">(</span><span class="s">"b"</span><span class="o">)</span> <span class="nc">Integer</span> <span class="n">b</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">String</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="n">calculator</span><span class="o">.</span><span class="na">sum</span><span class="o">(</span><span class="n">a</span><span class="o">,</span> <span class="n">b</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">src/main/resources/application.properties</code>에 Port를 지정한다.</p>

<div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">server.port</span><span class="p">=</span><span class="s">9000</span>
</code></pre></div></div>

<p>Gradle Wrapper를 사용해 목적별 Task를 실행한다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">./gradlew compileJava</code></td>
      <td>Main Java Source Compile</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">./gradlew build</code></td>
      <td>Compile, Test와 Package를 포함한 Build</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">./gradlew jar</code></td>
      <td>Java Plugin의 일반 JAR 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">./gradlew bootRun</code></td>
      <td>Spring Boot Application 실행</td>
    </tr>
  </tbody>
</table>

<p>Application을 실행해 Endpoint를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew bootRun
curl <span class="s2">"http://localhost:9000/sum?a=10&amp;b=20"</span>
</code></pre></div></div>

<p>예상 응답은 <code class="language-plaintext highlighter-rouge">30</code>이다. 검증 후 Project File과 Gradle Wrapper를 GitHub에 Push한다.</p>

<h2 id="8--jenkins-gradle-build-구성">8 ) Jenkins Gradle Build 구성</h2>

<hr />

<p>Freestyle Job을 만들고 Git Repository와 Branch를 연결한다. Linux Agent에서 Windows가 생성한 Repository를 Checkout하면 <code class="language-plaintext highlighter-rouge">gradlew</code>의 실행 권한이 Git에 기록되지 않았을 수 있다. <strong>Execute shell</strong>에서 권한을 확인한 뒤 Build한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x ./gradlew
./gradlew clean build
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">build</code> Task가 <code class="language-plaintext highlighter-rouge">compileJava</code>를 포함하므로 두 명령을 연속으로 실행할 필요가 없다. <code class="language-plaintext highlighter-rouge">clean</code>은 이전 <code class="language-plaintext highlighter-rouge">build</code> Directory를 지워 남은 결과가 새 Build에 섞이지 않게 한다.</p>

<p>반복해서 <code class="language-plaintext highlighter-rouge">chmod</code>하지 않으려면 개발 환경에서 실행 Bit를 Git Index에 기록해 Commit한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git update-index <span class="nt">--chmod</span><span class="o">=</span>+x gradlew
git commit <span class="nt">-m</span> <span class="s2">"Make Gradle wrapper executable"</span>
git push
</code></pre></div></div>

<p>Build 후 다음을 확인한다.</p>

<ul>
  <li>
    <p>Console Output의 JDK와 Gradle Version</p>
  </li>
  <li>
    <p>Test 실행 결과와 실패한 Test 이름</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">build/classes</code>의 Compile 결과</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">build/reports/tests</code>의 Test Report</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">build/libs</code>의 JAR</p>
  </li>
</ul>

<p>JAR를 Jenkins Build에 보관하려면 <strong>Archive the artifacts</strong> Pattern을 지정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>build/libs/*.jar
</code></pre></div></div>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Jenkins는 실행을 조정하고 Maven과 Gradle은 Dependency 해결, Compile, Test와 Package를 수행한다.</p>
    </li>
    <li>
      <p>Maven의 <code class="language-plaintext highlighter-rouge">package</code>는 앞선 Compile과 Test Phase를 포함하므로 <code class="language-plaintext highlighter-rouge">clean package</code>로 충분하다.</p>
    </li>
    <li>
      <p>Maven과 Gradle 표준 Directory에서 Test Report와 JAR·WAR 결과를 확인하고 Build Number에 연결해 Artifact로 보관한다.</p>
    </li>
    <li>
      <p>Poll SCM은 약 5분마다 변경을 확인하고 변경된 Revision이 있을 때 Build를 시작한다.</p>
    </li>
    <li>
      <p>SMTP App Password는 Credential로 관리하고 Email 전송 결과와 Build 결과를 분리해 진단한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Jenkins" /><summary type="html"><![CDATA[Spring Boot Project를 Maven과 Gradle로 Build하고 Jenkins Freestyle Job에서 Test, Package, Poll SCM과 Email 알림을 구성한다]]></summary></entry><entry><title type="html">CI/CD Pipeline과 Container 배포 흐름</title><link href="https://hyn128.site/cloud-native-45-cicd-pipeline/" rel="alternate" type="text/html" title="CI/CD Pipeline과 Container 배포 흐름" /><published>2026-09-11T00:00:00+09:00</published><updated>2026-09-11T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-45-cicd-pipeline</id><content type="html" xml:base="https://hyn128.site/cloud-native-45-cicd-pipeline/"><![CDATA[<p><a href="/cloud-native-41-container-image-registry/">Container Image Registry</a>는 배포할 Image를 저장하고 전달하며, <a href="/cloud-native-44-github-remote-workflow/">GitHub Remote Repository 작업 흐름</a>은 여러 개발자의 변경을 중앙 Repository에서 합치는 방법을 다룬다. CI/CD는 이 두 지점 사이의 Build, Test, Package와 배포 과정을 반복 가능한 Pipeline으로 연결한다.</p>

<h2 id="1--개발-변경-사항이-배포되기까지">1 ) 개발 변경 사항이 배포되기까지</h2>

<hr />

<p>자동화가 없는 개발 흐름에서도 다음 작업은 필요하다.</p>

<ol>
  <li>
    <p>중앙 Repository의 최신 코드를 Local로 가져온다.</p>
  </li>
  <li>
    <p>요구 사항을 구현하고 Local Unit Test를 반복한다.</p>
  </li>
  <li>
    <p>변경 사항을 중앙 Repository에 Push하고 Main Branch에 병합한다.</p>
  </li>
  <li>
    <p>병합된 Source를 Compile 또는 Build한다.</p>
  </li>
  <li>
    <p>Unit Test, Integration Test, Regression Test와 Static Analysis를 수행한다.</p>
  </li>
  <li>
    <p>검증된 결과물을 Package하고 실행 환경에 배포한다.</p>
  </li>
</ol>

<p>Test-driven Development(TDD)를 사용한다면 작은 Test를 먼저 작성하고 구현을 반복할 수 있다. TDD는 개발 방식이고 CI는 여러 변경을 자주 통합해 자동 검증하는 방식이므로 같은 개념은 아니다.</p>

<p>사람이 각 단계를 수동으로 넘기면 병합 후에야 Compile 오류를 발견하거나, 배포 직전의 큰 변경에서 결함이 한꺼번에 드러날 수 있다. Hotfix처럼 시간이 촉박한 작업에서는 Test 범위가 줄어들 위험도 커진다.</p>

<table>
  <thead>
    <tr>
      <th>수동 흐름에서 생기는 문제</th>
      <th>Pipeline이 줄이는 부분</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>긴 인도 기간</td>
      <td>Commit 이후 Build와 Test를 즉시 시작</td>
    </tr>
    <tr>
      <td>느린 Feedback</td>
      <td>실패한 Job과 Step을 실행 Log로 빠르게 확인</td>
    </tr>
    <tr>
      <td>반복 작업 누락</td>
      <td>같은 Workflow를 같은 순서로 실행</td>
    </tr>
    <tr>
      <td>큰 Release 위험</td>
      <td>작은 변경을 자주 검증하고 배포 후보를 지속적으로 생성</td>
    </tr>
    <tr>
      <td>역할 간 전달 지연</td>
      <td>Artifact와 실행 결과를 공통 Repository와 Pipeline에 남김</td>
    </tr>
    <tr>
      <td>의사소통 부족과 책임 분산</td>
      <td>단계별 담당 범위, 승인과 실행 이력을 Pipeline에 기록</td>
    </tr>
    <tr>
      <td>긴급 Hotfix와 반복 작업의 부담</td>
      <td>표준 Test와 배포 절차를 재사용해 생략 가능성을 줄임</td>
    </tr>
  </tbody>
</table>

<p>반복적인 수동 전달과 긴급 배포가 줄어들면 개발과 운영 업무의 예측 가능성도 높아진다.</p>

<blockquote>
  <p><strong>Artifact</strong>
Build 과정이 만들어 낸 배포 가능한 결과물이다. JAR, WAR, 실행 Binary, 압축 Package, Container Image 또는 Helm Chart 등이 될 수 있다. Artifact가 항상 하나의 압축 File인 것은 아니다.</p>
</blockquote>

<h2 id="2--ci-continuous-delivery와-continuous-deployment">2 ) CI, Continuous Delivery와 Continuous Deployment</h2>

<hr />

<p>CI/CD는 하나의 제품명이 아니라 개발 변경을 통합하고 검증하며 배포 가능한 상태로 전달하는 일련의 관행과 자동화된 Pipeline을 뜻한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>목적</th>
      <th>자동화가 끝나는 지점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Continuous Integration(CI)</td>
      <td>작은 변경을 자주 통합하고 Build와 Test로 검증</td>
      <td>배포 가능한 Artifact 생성과 검증</td>
    </tr>
    <tr>
      <td>Continuous Delivery</td>
      <td>검증된 변경을 언제든 Release할 수 있는 상태로 유지</td>
      <td>운영 배포 직전까지 자동화하며 최종 Release에는 승인 절차를 둘 수 있음</td>
    </tr>
    <tr>
      <td>Continuous Deployment</td>
      <td>검증을 통과한 변경을 운영 환경까지 계속 배포</td>
      <td>운영 Release까지 자동화</td>
    </tr>
  </tbody>
</table>

<p>Continuous Delivery와 Continuous Deployment는 배포 전에 사람이 승인하는 지점이 있는지로 구분할 수 있다. 조직에 따라 두 용어를 넓은 의미의 CD로 묶어서 사용하므로 Pipeline 문서에서는 자동화 경계와 승인 조건을 함께 적어야 한다.</p>

<h2 id="3--continuous-integration">3 ) Continuous Integration</h2>

<hr />

<p>CI는 개발자가 작은 변경을 Version Control Repository에 자주 통합하고, 통합할 때마다 동일한 검사를 자동 실행하는 방식이다. 여러 언어, Platform과 Build Tool이 섞인 환경에서는 개발자마다 다른 Local 환경에 의존하지 않고 공통 검증 절차를 유지하는 것이 중요하다.</p>

<p>일반적인 CI 단계는 다음과 같다.</p>

<ol>
  <li>
    <p><code class="language-plaintext highlighter-rouge">push</code> 또는 Pull Request 같은 Event가 Pipeline을 시작한다.</p>
  </li>
  <li>
    <p>Runner가 대상 Commit을 Checkout한다.</p>
  </li>
  <li>
    <p>Dependency를 설치하고 Source를 Compile 또는 Build한다.</p>
  </li>
  <li>
    <p>Unit Test와 Static Analysis를 수행한다.</p>
  </li>
  <li>
    <p>다음 단계에서 사용할 Artifact를 Package한다.</p>
  </li>
  <li>
    <p>결과와 Log를 저장하고 실패한 변경을 개발자에게 알린다.</p>
  </li>
</ol>

<p>CI 성공은 Workflow에 정의한 검사를 통과했다는 뜻이다. 정의하지 않은 Test, 운영 환경의 장애 조건과 Security 문제까지 모두 없다는 보장은 아니다. 따라서 Pipeline의 신뢰도는 어떤 검사를 어느 단계에 넣었는지에 달려 있다.</p>

<p>작은 변경을 자주 통합하면 여러 Branch가 오래 분리된 뒤 한꺼번에 충돌하는 Integration Hell을 줄일 수 있다. 실패 지점도 최근 변경 범위에 가까워져 원인을 찾기 쉬워진다.</p>

<h2 id="4--continuous-delivery와-release">4 ) Continuous Delivery와 Release</h2>

<hr />

<p>Continuous Delivery는 CI를 통과한 Artifact를 배포 가능한 Repository에 보관하고, Test 환경이나 Staging 환경까지 자동으로 전달한다. 운영팀은 검증된 같은 Artifact를 선택해 Production에 Release할 수 있다.</p>

<p>Continuous Deployment는 승인 대기 없이 정책과 Test를 모두 통과한 변경을 Production까지 자동 Release한다. 배포 횟수를 늘리는 것만으로 완성되지 않으며 자동 Test, Monitoring과 복구 절차가 함께 있어야 한다.</p>

<p>지속적인 전달 체계가 제공하는 효과는 다음과 같다.</p>

<ul>
  <li>
    <p>변경부터 사용자 Feedback까지 걸리는 시간이 짧아진다.</p>
  </li>
  <li>
    <p>한 번에 배포되는 변경 범위가 작아져 Release 위험을 줄일 수 있다.</p>
  </li>
  <li>
    <p>검증된 Artifact를 필요한 시점에 Release할 수 있다.</p>
  </li>
  <li>
    <p>Build와 배포 절차가 Pipeline에 기록되어 재현하기 쉬워진다.</p>
  </li>
</ul>

<h2 id="5--자동-배포-pipeline">5 ) 자동 배포 Pipeline</h2>

<hr />

<p>Pipeline은 보통 다음 흐름으로 구성한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Code Change
    ↓
Continuous Integration
    ↓
Automated Acceptance Test
    ↓
Configuration Change
    ↓
Deployment
    ↓
Monitoring and Feedback
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>주요 작업</th>
      <th>실패 시 확인할 대상</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Code Change</td>
      <td>Commit, Push, Pull Request</td>
      <td>Branch와 Commit, Trigger 조건</td>
    </tr>
    <tr>
      <td>CI</td>
      <td>Checkout, Build, Unit Test, Static Analysis</td>
      <td>Runner Log, Dependency, Test 결과</td>
    </tr>
    <tr>
      <td>Acceptance Test</td>
      <td>통합·인수·회귀 Test</td>
      <td>Test 환경, 외부 Service, Test Data</td>
    </tr>
    <tr>
      <td>Configuration Change</td>
      <td>Image Tag, Manifest와 환경별 설정 갱신</td>
      <td>설정 차이, Secret과 권한</td>
    </tr>
    <tr>
      <td>Deployment</td>
      <td>대상 환경에 원하는 상태 반영</td>
      <td>배포 도구, API 응답, Rollout 상태</td>
    </tr>
    <tr>
      <td>Monitoring</td>
      <td>Log, Metric과 사용자 Feedback 확인</td>
      <td>Error Rate, 응답 시간, Resource 상태</td>
    </tr>
  </tbody>
</table>

<p>각 환경에서 Artifact를 다시 Build하면 같은 Version 이름이라도 Binary나 Image 내용이 달라질 수 있다. 배포할 Artifact를 한 번 만들고 식별 가능한 Version 또는 Digest로 저장한 뒤 같은 결과물을 다음 환경으로 승격하면 Build와 배포의 책임을 구분하기 쉽다.</p>

<h2 id="6--container와-kubernetes의-cicd-흐름">6 ) Container와 Kubernetes의 CI/CD 흐름</h2>

<hr />

<p>Container 환경에서는 Application Artifact를 Container Image로 만들고 Registry에 저장할 수 있다. Kubernetes에 배포할 때는 Image와 함께 Deployment, StatefulSet 같은 Workload Resource의 Manifest가 필요하다.</p>

<figure class="post-visual" aria-labelledby="cicd-flow-caption">
  <figcaption class="post-visual__caption" id="cicd-flow-caption">
    Source 변경이 Container Image와 Kubernetes Pod로 이어지는 CI/CD 흐름
  </figcaption>
  <div class="post-visual__frame">
    <svg viewBox="0 0 960 920" role="img" aria-labelledby="cicd-flow-title cicd-flow-desc">
      <title id="cicd-flow-title">Container CI/CD와 Kubernetes 배포 흐름</title>
      <desc id="cicd-flow-desc">개발자의 코드 Push가 CI의 Build와 Test, Registry의 Image 저장, CD의 배포 요청, Kubernetes Control Plane의 배치 결정, Worker의 Image Pull과 Pod 실행으로 이어진다.</desc>
      <defs>
        <marker id="arrow-cicd-flow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" markerUnits="strokeWidth">
          <path d="M0,0 L6,3 L0,6 z" fill="var(--text2)"></path>
        </marker>
      </defs>

      <g id="cicd-build-lane">
        <title>Build and verification lane</title>
        <rect x="32" y="32" width="896" height="162" rx="22" fill="var(--bg-element1)" stroke="var(--border3)" stroke-width="2"></rect>
        <text x="58" y="65" fill="var(--text3)" font-size="16" font-weight="700">BUILD &amp; VERIFY</text>
      </g>

      <g id="cicd-source">
        <title>Developer and source repository</title>
        <rect x="60" y="92" width="170" height="80" rx="16" fill="var(--diagram-blue-soft)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="145" y="124" text-anchor="middle" fill="var(--text1)" font-size="20" font-weight="700">Source</text>
        <text x="145" y="151" text-anchor="middle" fill="var(--text1)" font-size="15">commit · push</text>
      </g>

      <g id="cicd-source-to-ci">
        <title>Push event triggers CI</title>
        <line x1="230" y1="132" x2="290" y2="132" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
        <text x="260" y="112" text-anchor="middle" fill="var(--text2)" font-size="15">event</text>
      </g>

      <g id="cicd-ci-runner">
        <title>CI runner checks out, builds, tests, and creates an image</title>
        <rect x="302" y="80" width="340" height="104" rx="16" fill="var(--diagram-primary-soft)" stroke="var(--primary2)" stroke-width="3"></rect>
        <text x="472" y="112" text-anchor="middle" fill="var(--text1)" font-size="21" font-weight="700">CI Runner</text>
        <text x="472" y="141" text-anchor="middle" fill="var(--text1)" font-size="15">checkout → build → test</text>
        <text x="472" y="165" text-anchor="middle" fill="var(--primary1)" font-size="15" font-weight="700">container image 생성</text>
      </g>

      <g id="cicd-ci-to-registry">
        <title>CI pushes a versioned image</title>
        <line x1="642" y1="132" x2="702" y2="132" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
        <text x="672" y="112" text-anchor="middle" fill="var(--text2)" font-size="15">push</text>
      </g>

      <g id="cicd-registry">
        <title>Container registry stores versioned images</title>
        <rect x="714" y="92" width="186" height="80" rx="16" fill="var(--diagram-violet-soft)" stroke="var(--diagram-violet)" stroke-width="2"></rect>
        <text x="807" y="124" text-anchor="middle" fill="var(--text1)" font-size="20" font-weight="700">Registry</text>
        <text x="807" y="151" text-anchor="middle" fill="var(--text1)" font-size="15">image:tag</text>
      </g>

      <g id="cicd-release-lane">
        <title>Release and run lane</title>
        <rect x="32" y="220" width="896" height="668" rx="22" fill="var(--bg-element1)" stroke="var(--border3)" stroke-width="2"></rect>
        <text x="58" y="252" fill="var(--text2)" font-size="16" font-weight="700">RELEASE &amp; RUN</text>
      </g>

      <g id="cicd-cd-system">
        <title>CD system changes the desired deployment state</title>
        <rect x="60" y="278" width="840" height="106" rx="16" fill="var(--diagram-orange-soft)" stroke="var(--diagram-orange)" stroke-width="2"></rect>
        <text x="92" y="312" fill="var(--diagram-orange)" font-size="15" font-weight="700">CD SYSTEM</text>
        <text x="480" y="321" text-anchor="middle" fill="var(--text1)" font-size="21" font-weight="700">Manifest의 Image Version 갱신</text>
        <text x="480" y="352" text-anchor="middle" fill="var(--text1)" font-size="16">배포 도구가 원하는 상태를 API Server에 전달</text>
      </g>

      <g id="cicd-registry-to-cd">
        <title>New image version becomes the release target</title>
        <path d="M807 172 V206 H820 V268" fill="none" stroke="var(--diagram-violet)" stroke-width="2.5" stroke-dasharray="7 6" marker-end="url(#arrow-cicd-flow)"></path>
        <rect x="574" y="204" width="306" height="34" rx="17" fill="var(--bg-element1)" stroke="var(--border3)"></rect>
        <text x="727" y="227" text-anchor="middle" fill="var(--text1)" font-size="15">새 Image Version을 배포 대상으로 선택</text>
      </g>

      <g id="cicd-control-plane">
        <title>Kubernetes Control Plane</title>
        <rect x="60" y="438" width="840" height="188" rx="18" fill="var(--diagram-blue-soft)" stroke="var(--diagram-blue)" stroke-width="2" stroke-dasharray="7 6"></rect>
        <text x="92" y="472" fill="var(--diagram-blue)" font-size="15" font-weight="700">CONTROL PLANE</text>
      </g>

      <g id="cicd-cd-to-api">
        <title>CD sends the desired state to the API Server</title>
        <line x1="480" y1="384" x2="480" y2="426" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
        <rect x="500" y="394" width="132" height="28" rx="14" fill="var(--bg-element1)"></rect>
        <text x="566" y="414" text-anchor="middle" fill="var(--text1)" font-size="14">배포 요청</text>
      </g>

      <g id="cicd-api-server">
        <title>API Server receives and persists the requested state</title>
        <rect x="106" y="500" width="254" height="82" rx="14" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="233" y="533" text-anchor="middle" fill="var(--text1)" font-size="19" font-weight="700">API Server</text>
        <text x="233" y="560" text-anchor="middle" fill="var(--text1)" font-size="15">요청 검증 · 상태 저장</text>
      </g>

      <g id="cicd-api-to-controllers">
        <title>Controllers and scheduler reconcile and place the Pod</title>
        <line x1="360" y1="541" x2="464" y2="541" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
      </g>

      <g id="cicd-controllers-scheduler">
        <title>Controllers reconcile state and scheduler chooses a worker</title>
        <rect x="478" y="500" width="376" height="82" rx="14" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="666" y="533" text-anchor="middle" fill="var(--text1)" font-size="19" font-weight="700">Controllers · Scheduler</text>
        <text x="666" y="560" text-anchor="middle" fill="var(--text1)" font-size="15">상태 조정 · 실행할 Worker 결정</text>
      </g>

      <g id="cicd-worker-node">
        <title>Kubernetes Worker</title>
        <rect x="60" y="684" width="840" height="170" rx="18" fill="var(--diagram-primary-soft)" stroke="var(--primary2)" stroke-width="2" stroke-dasharray="7 6"></rect>
        <text x="92" y="718" fill="var(--primary1)" font-size="15" font-weight="700">WORKER NODE</text>
      </g>

      <g id="cicd-control-plane-to-worker">
        <title>The assigned Pod specification is observed by kubelet</title>
        <line x1="480" y1="626" x2="480" y2="672" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
        <rect x="500" y="638" width="160" height="28" rx="14" fill="var(--bg-element1)"></rect>
        <text x="580" y="658" text-anchor="middle" fill="var(--text1)" font-size="14">배치된 Pod Spec</text>
      </g>

      <g id="cicd-kubelet">
        <title>Kubelet observes the Pod specification on its worker</title>
        <rect x="106" y="752" width="210" height="70" rx="14" fill="var(--bg-element1)" stroke="var(--primary2)" stroke-width="2"></rect>
        <text x="211" y="782" text-anchor="middle" fill="var(--text1)" font-size="19" font-weight="700">kubelet</text>
        <text x="211" y="807" text-anchor="middle" fill="var(--text1)" font-size="14">Pod Spec 확인</text>
      </g>

      <g id="cicd-kubelet-to-runtime">
        <title>Kubelet asks the container runtime to create containers</title>
        <line x1="316" y1="787" x2="374" y2="787" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
      </g>

      <g id="cicd-runtime">
        <title>Container runtime pulls the requested image</title>
        <rect x="388" y="752" width="210" height="70" rx="14" fill="var(--bg-element1)" stroke="var(--primary2)" stroke-width="2"></rect>
        <text x="493" y="782" text-anchor="middle" fill="var(--text1)" font-size="19" font-weight="700">Runtime</text>
        <text x="493" y="807" text-anchor="middle" fill="var(--text1)" font-size="14">Image Pull · Container 생성</text>
      </g>

      <g id="cicd-runtime-to-pod">
        <title>Runtime starts the Pod containers</title>
        <line x1="598" y1="787" x2="672" y2="787" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-cicd-flow)"></line>
      </g>

      <g id="cicd-pod">
        <title>Pod runs on the selected worker</title>
        <rect x="686" y="752" width="168" height="70" rx="35" fill="var(--primary2)" stroke="var(--primary1)" stroke-width="2"></rect>
        <text x="770" y="782" text-anchor="middle" fill="#08251d" font-size="19" font-weight="800">Pod</text>
        <text x="770" y="807" text-anchor="middle" fill="#08251d" font-size="14">Running</text>
      </g>

      <g id="cicd-registry-to-runtime">
        <title>Worker runtime pulls the image from the registry</title>
        <path d="M900 132 H938 V736 H493 V744" fill="none" stroke="var(--diagram-violet)" stroke-width="2.2" stroke-dasharray="7 7" marker-end="url(#arrow-cicd-flow)"></path>
        <rect x="746" y="696" width="144" height="30" rx="15" fill="var(--bg-element1)" stroke="var(--diagram-violet)"></rect>
        <text x="818" y="717" text-anchor="middle" fill="var(--diagram-violet)" font-size="14" font-weight="700">Registry Pull</text>
      </g>
    </svg>
  </div>
  <div class="post-visual__notes">
    <div class="post-visual__note">
      <strong>CI의 책임</strong>
      <p>코드를 검증하고 재현 가능한 Image를 Registry에 남긴다. CI 성공은 정의한 검사를 통과했다는 뜻이다.</p>
    </div>
    <div class="post-visual__note">
      <strong>Kubernetes의 책임</strong>
      <p>Control Plane이 원하는 상태와 배치를 결정하고, 선택된 Worker가 Image를 받아 Pod를 실행한다.</p>
    </div>
  </div>
</figure>

<h3 id="ci-관점">CI 관점</h3>

<p>개발자가 Source를 Push하면 CI Runner가 대상 Commit을 Checkout하고 Build와 Test를 수행한다. 검증을 통과하면 Container Image를 한 번 만들고 Version Tag 또는 Digest와 함께 Registry에 Push한다.</p>

<p>Image 생성 시점을 CI와 CD 중 어디에 둘지는 Pipeline 설계에 따라 달라질 수 있다. 다만 여러 환경에 같은 결과물을 배포하려면 Image를 한 번 생성한 뒤 Registry에서 승격하는 구성이 유리하다.</p>

<h3 id="cd와-control-plane-관점">CD와 Control Plane 관점</h3>

<p>CD System은 배포할 Image Version을 선택하고 Manifest의 원하는 상태를 갱신한다. <code class="language-plaintext highlighter-rouge">kubectl apply</code>, GitOps Controller 또는 배포 도구가 이 변경을 Kubernetes API Server에 전달한다.</p>

<p>API Server가 요청을 검증하고 Cluster 상태로 저장하면 Controller가 현재 상태를 원하는 상태에 맞추려 한다. 새 Pod가 필요할 경우 Scheduler는 실행할 Worker Node를 결정한다.</p>

<h3 id="worker-관점">Worker 관점</h3>

<p>선택된 Worker의 kubelet은 자신에게 배정된 Pod Spec을 확인한다. kubelet이 Container Runtime에 Container 생성을 요청하면 Runtime은 Registry에서 지정된 Image를 Pull하고 Pod의 Container를 실행한다.</p>

<p>따라서 CI가 성공했더라도 배포 단계에서는 별도의 문제가 생길 수 있다.</p>

<ul>
  <li>
    <p>Worker가 Registry에 인증하지 못해 <code class="language-plaintext highlighter-rouge">ImagePullBackOff</code>가 발생할 수 있다.</p>
  </li>
  <li>
    <p>Manifest의 Image Tag가 존재하지 않을 수 있다.</p>
  </li>
  <li>
    <p>Resource 요청량이나 Scheduling 조건 때문에 Pod가 <code class="language-plaintext highlighter-rouge">Pending</code>에 머물 수 있다.</p>
  </li>
  <li>
    <p>Readiness Probe를 통과하지 못해 Service Traffic을 받지 못할 수 있다.</p>
  </li>
</ul>

<p>Control Plane의 배포 상태와 Worker의 실제 실행 상태를 함께 확인해야 Pipeline 결과와 Runtime 상태를 연결할 수 있다.</p>

<h2 id="7--역할별-도구">7 ) 역할별 도구</h2>

<hr />

<p>모든 단계를 하나의 제품으로 구성할 필요는 없다. 팀의 Repository, 배포 대상과 운영 방식에 맞춰 역할별 도구를 조합한다.</p>

<table>
  <thead>
    <tr>
      <th>역할</th>
      <th>도구 예시</th>
      <th>담당 범위</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Source Code Management</td>
      <td>GitHub, GitLab, Bitbucket</td>
      <td>Commit, Branch, Pull Request와 권한 관리</td>
    </tr>
    <tr>
      <td>CI</td>
      <td>GitHub Actions, Jenkins, TeamCity, CircleCI, GitLab CI/CD, Bamboo</td>
      <td>Build, Test, 분석과 Artifact 생성</td>
    </tr>
    <tr>
      <td>Artifact·Image 저장</td>
      <td>GitHub Packages, GitLab Registry, Docker Hub, Cloud Registry</td>
      <td>Version별 Package와 Container Image 저장</td>
    </tr>
    <tr>
      <td>Configuration Management</td>
      <td>Ansible</td>
      <td>Server Provisioning과 설정 자동화</td>
    </tr>
    <tr>
      <td>CD·GitOps</td>
      <td>Argo CD, Spinnaker</td>
      <td>환경별 배포, 원하는 상태 동기화와 Release 제어</td>
    </tr>
    <tr>
      <td>Public Cloud Pipeline</td>
      <td>AWS CodeBuild, CodePipeline, CodeDeploy 등</td>
      <td>Cloud Service와 연계한 Build 및 배포 자동화</td>
    </tr>
  </tbody>
</table>

<p>TeamCity는 Kotlin DSL로 Build 구성을 코드화할 수 있다. Jenkins는 UI Job과 Pipeline Code를 모두 지원하고 Plugin 생태계가 넓다. Bamboo는 Jira, Confluence와 Bitbucket 같은 Atlassian 제품군과 연계할 수 있다. GitHub Actions와 GitLab CI/CD는 Repository Event와 Workflow File을 가까이 관리할 수 있다.</p>

<p>무료 실행 시간, Storage와 요금은 서비스와 Plan에 따라 바뀐다. 고정된 수치를 문서에 남기기보다 도입 시점의 공식 정책과 Self-hosted Runner 운영 비용을 함께 확인한다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>CI는 작은 변경을 자주 통합하고 Build, Test와 Package 과정을 자동으로 검증한다.</p>
    </li>
    <li>
      <p>Continuous Delivery는 언제든 Release할 수 있는 상태를 유지하고, Continuous Deployment는 운영 Release까지 자동화한다.</p>
    </li>
    <li>
      <p>Container Pipeline에서는 검증된 Image를 Registry에 저장하고 식별 가능한 Version으로 다음 환경에 전달한다.</p>
    </li>
    <li>
      <p>Kubernetes 배포는 CD 요청, Control Plane의 상태 조정과 배치, Worker의 Image Pull과 Pod 실행으로 이어진다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="CI/CD" /><summary type="html"><![CDATA[CI·Continuous Delivery·Continuous Deployment의 차이와 Source 변경이 Container Image 및 Kubernetes Pod로 이어지는 과정을 정리한다]]></summary></entry><entry><title type="html">GitHub Actions Workflow와 CI/CD 실습</title><link href="https://hyn128.site/cloud-native-46-github-actions-ci/" rel="alternate" type="text/html" title="GitHub Actions Workflow와 CI/CD 실습" /><published>2026-09-11T00:00:00+09:00</published><updated>2026-09-11T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-46-github-actions-ci</id><content type="html" xml:base="https://hyn128.site/cloud-native-46-github-actions-ci/"><![CDATA[<p><a href="/cloud-native-45-cicd-pipeline/">CI/CD Pipeline과 Container 배포 흐름</a>에서 Code 변경부터 Build, Test와 배포까지의 전체 흐름을 정리했다. GitHub Actions는 GitHub Repository에서 발생하는 Event를 기준으로 이 작업을 자동 실행하는 Workflow 도구이다.</p>

<h2 id="1--github-actions-개요">1 ) GitHub Actions 개요</h2>

<hr />

<p>GitHub Actions는 Software 개발 주기에 필요한 Build, Test와 배포 작업을 Repository 안의 Workflow File로 정의한다. GitHub의 <code class="language-plaintext highlighter-rouge">push</code>, Pull Request, Release 같은 Event가 발생하면 지정한 Runner에서 Job을 실행한다.</p>

<p>Workflow를 시작하는 방법은 두 가지이다.</p>

<ul>
  <li>
    <p>GitHub Repository의 <strong>Actions</strong> Tab에서 Starter Workflow를 선택한다.</p>
  </li>
  <li>
    <p>Repository의 <code class="language-plaintext highlighter-rouge">.github/workflows</code> Directory에 YAML File을 직접 작성한다.</p>
  </li>
</ul>

<p>Starter Workflow는 <a href="https://github.com/actions/starter-workflows">actions/starter-workflows</a>에서 확인할 수 있고, 재사용 가능한 Action은 <a href="https://github.com/marketplace?type=actions">GitHub Marketplace</a>에서 찾을 수 있다. Public Repository, Private Repository와 GitHub-hosted 또는 Self-hosted Runner에 따라 제공 범위와 과금 방식이 달라지므로 사용 시점의 공식 정책을 확인한다.</p>

<h2 id="2--event-workflow-job-step과-runner">2 ) Event, Workflow, Job, Step과 Runner</h2>

<hr />

<p>GitHub Actions의 실행 단위는 다음 관계를 가진다.</p>

<figure class="post-visual" aria-labelledby="github-actions-flow-caption">
  <figcaption class="post-visual__caption" id="github-actions-flow-caption">
    Event부터 Workflow와 Job을 거쳐 Step이 실행되는 GitHub Actions 흐름
  </figcaption>
  <div class="post-visual__frame">
    <svg viewBox="0 0 960 870" role="img" aria-labelledby="github-actions-flow-title github-actions-flow-desc">
      <title id="github-actions-flow-title">GitHub Actions Event, Workflow, Job, Step, Runner 관계</title>
      <desc id="github-actions-flow-desc">Push나 Pull Request Event가 Workflow를 시작한다. Workflow의 test Job에서 checkout, setup, install과 test Step이 순차 실행된다. test Job이 성공하면 needs로 연결된 package Job이 별도 Runner에서 시작된다.</desc>
      <defs>
        <marker id="arrow-github-actions-flow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" markerUnits="strokeWidth">
          <path d="M0,0 L6,3 L0,6 z" fill="var(--text2)"></path>
        </marker>
      </defs>

      <g id="github-actions-event">
        <title>Repository event</title>
        <rect x="60" y="40" width="840" height="94" rx="18" fill="var(--diagram-violet-soft)" stroke="var(--diagram-violet)" stroke-width="2"></rect>
        <text x="92" y="73" fill="var(--diagram-violet)" font-size="15" font-weight="700">EVENT</text>
        <text x="480" y="83" text-anchor="middle" fill="var(--text1)" font-size="22" font-weight="700">Repository 상태 변경</text>
        <text x="480" y="112" text-anchor="middle" fill="var(--text1)" font-size="16">push · pull_request · workflow_dispatch</text>
      </g>

      <g id="github-actions-event-to-workflow">
        <title>Event triggers workflow</title>
        <line x1="480" y1="134" x2="480" y2="174" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-github-actions-flow)"></line>
        <rect x="500" y="142" width="112" height="28" rx="14" fill="var(--bg-element1)" stroke="var(--border3)"></rect>
        <text x="556" y="162" text-anchor="middle" fill="var(--text1)" font-size="14">trigger</text>
      </g>

      <g id="github-actions-workflow">
        <title>One workflow YAML file</title>
        <rect x="60" y="186" width="840" height="106" rx="18" fill="var(--diagram-primary-soft)" stroke="var(--primary2)" stroke-width="3"></rect>
        <text x="92" y="220" fill="var(--primary1)" font-size="15" font-weight="700">WORKFLOW</text>
        <text x="480" y="228" text-anchor="middle" fill="var(--text1)" font-size="22" font-weight="700">CI Workflow</text>
        <text x="480" y="260" text-anchor="middle" fill="var(--text1)" font-size="16">.github/workflows/ci.yml</text>
      </g>

      <g id="github-actions-workflow-to-test">
        <title>Workflow starts the test job</title>
        <line x1="480" y1="292" x2="480" y2="332" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-github-actions-flow)"></line>
        <rect x="500" y="300" width="132" height="28" rx="14" fill="var(--bg-element1)" stroke="var(--border3)"></rect>
        <text x="566" y="320" text-anchor="middle" fill="var(--text1)" font-size="14">job 시작</text>
      </g>

      <g id="github-actions-test-job">
        <title>Test job runs on Runner A</title>
        <rect x="60" y="344" width="840" height="284" rx="20" fill="var(--diagram-blue-soft)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="92" y="378" fill="var(--diagram-blue)" font-size="15" font-weight="700">RUNNER A</text>
        <text x="480" y="386" text-anchor="middle" fill="var(--text1)" font-size="22" font-weight="700">Job: test</text>
      </g>

      <g id="github-actions-test-steps">
        <title>Test job steps run sequentially</title>
        <rect x="102" y="430" width="188" height="78" rx="13" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="196" y="462" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">Step 1</text>
        <text x="196" y="488" text-anchor="middle" fill="var(--text1)" font-size="15">checkout</text>
        <line x1="290" y1="469" x2="370" y2="469" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-github-actions-flow)"></line>
        <rect x="384" y="430" width="188" height="78" rx="13" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="478" y="462" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">Step 2</text>
        <text x="478" y="488" text-anchor="middle" fill="var(--text1)" font-size="15">setup</text>
        <line x1="572" y1="469" x2="652" y2="469" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-github-actions-flow)"></line>
        <rect x="666" y="430" width="192" height="78" rx="13" fill="var(--bg-element1)" stroke="var(--diagram-blue)" stroke-width="2"></rect>
        <text x="762" y="462" text-anchor="middle" fill="var(--text1)" font-size="17" font-weight="700">Step 3</text>
        <text x="762" y="488" text-anchor="middle" fill="var(--text1)" font-size="15">install · test</text>
        <text x="480" y="552" text-anchor="middle" fill="var(--text1)" font-size="16">Step은 같은 Runner에서 왼쪽부터 순서대로 실행</text>
        <rect x="302" y="574" width="356" height="36" rx="18" fill="var(--bg-element3)"></rect>
        <text x="480" y="598" text-anchor="middle" fill="var(--text1)" font-size="15">하나라도 실패하면 test Job은 실패</text>
      </g>

      <g id="github-actions-test-to-package">
        <title>Package job waits for test through needs</title>
        <line x1="480" y1="628" x2="480" y2="682" stroke="var(--text2)" stroke-width="2.5" marker-end="url(#arrow-github-actions-flow)"></line>
        <rect x="500" y="642" width="150" height="30" rx="15" fill="var(--bg-element1)" stroke="var(--border3)"></rect>
        <text x="575" y="663" text-anchor="middle" fill="var(--text1)" font-size="14" font-weight="700">needs: test</text>
      </g>

      <g id="github-actions-package-job">
        <title>Dependent package job starts after test succeeds</title>
        <rect x="60" y="696" width="840" height="128" rx="18" fill="var(--diagram-orange-soft)" stroke="var(--diagram-orange)" stroke-width="2"></rect>
        <text x="92" y="730" fill="var(--diagram-orange)" font-size="15" font-weight="700">RUNNER B</text>
        <text x="480" y="748" text-anchor="middle" fill="var(--text1)" font-size="22" font-weight="700">Job: package</text>
        <text x="480" y="780" text-anchor="middle" fill="var(--text1)" font-size="16">test Job이 성공한 뒤 별도 Runner에서 시작</text>
        <rect x="350" y="794" width="260" height="24" rx="12" fill="var(--bg-element1)"></rect>
        <text x="480" y="811" text-anchor="middle" fill="var(--text1)" font-size="13">Job 간 파일과 설치 상태는 자동 공유되지 않음</text>
      </g>
    </svg>
  </div>
  <div class="post-visual__notes">
    <div class="post-visual__note">
      <strong>Job 간 격리</strong>
      <p>Job마다 Runner가 달라질 수 있으므로 파일이나 설치 상태를 자동으로 공유하지 않는다.</p>
    </div>
    <div class="post-visual__note">
      <strong>순서가 필요할 때</strong>
      <p><code>needs</code>를 지정하면 선행 Job의 성공 여부를 기준으로 다음 Job을 시작할 수 있다.</p>
    </div>
  </div>
</figure>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Event</td>
      <td>Workflow를 시작하는 활동</td>
      <td><code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">pull_request</code>, 수동 실행, 일정</td>
    </tr>
    <tr>
      <td>Workflow</td>
      <td>하나 이상의 Job을 정의한 자동화 File</td>
      <td><code class="language-plaintext highlighter-rouge">.github/workflows/ci.yml</code></td>
    </tr>
    <tr>
      <td>Job</td>
      <td>하나의 Runner에서 실행되는 Step 묶음</td>
      <td>Build, Test, Package</td>
    </tr>
    <tr>
      <td>Step</td>
      <td>Job 안에서 순서대로 실행되는 최소 작업 단위</td>
      <td>Action 호출 또는 Shell 명령 실행</td>
    </tr>
    <tr>
      <td>Action</td>
      <td><code class="language-plaintext highlighter-rouge">uses</code>로 가져와 실행하는 재사용 가능한 작업</td>
      <td>Source Checkout, Runtime 설치</td>
    </tr>
    <tr>
      <td>Runner</td>
      <td>Job을 실행하는 Virtual Machine, Physical Machine 또는 Container</td>
      <td><code class="language-plaintext highlighter-rouge">ubuntu-latest</code>, Self-hosted Runner</td>
    </tr>
  </tbody>
</table>

<p>하나의 Job에 있는 Step은 같은 Runner에서 작성 순서대로 실행되므로 앞 Step에서 만든 File과 설치 상태를 뒤 Step이 사용할 수 있다. 서로 다른 Job은 기본적으로 독립된 Runner에서 병렬 실행되며 File System을 자동으로 공유하지 않는다.</p>

<p>Job 실행 순서가 필요하면 <code class="language-plaintext highlighter-rouge">needs</code>를 사용한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">npm test</span>

  <span class="na">package</span><span class="pi">:</span>
    <span class="na">needs</span><span class="pi">:</span> <span class="s">test</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">npm pack</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">package</code> Job은 <code class="language-plaintext highlighter-rouge">test</code> Job이 성공한 뒤 시작한다. Job의 성공 또는 실패 상태뿐 아니라 각 Step의 실행 명령, 출력과 실패 위치도 GitHub Actions 화면에서 확인할 수 있다.</p>

<h2 id="3--workflow-기본-구조와-trigger">3 ) Workflow 기본 구조와 Trigger</h2>

<hr />

<p>Workflow File의 기본 구조는 <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">on</code>과 <code class="language-plaintext highlighter-rouge">jobs</code>이다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">echo "run test"</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">name</code></td>
      <td>Actions 화면에 표시할 Workflow 이름</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">on</code></td>
      <td>Workflow를 시작할 Event와 조건</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jobs</code></td>
      <td>실행할 Job Map</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">runs-on</code></td>
      <td>Job을 실행할 Runner 환경</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">steps</code></td>
      <td>같은 Runner에서 순차 실행할 작업 목록</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">uses</code></td>
      <td>Repository에 공개된 Action 또는 Local Action 사용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">run</code></td>
      <td>Runner의 Shell에서 명령 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">with</code></td>
      <td>Action에 전달할 입력 값</td>
    </tr>
  </tbody>
</table>

<h3 id="code-변경-event">Code 변경 Event</h3>

<p><code class="language-plaintext highlighter-rouge">push</code>와 <code class="language-plaintext highlighter-rouge">pull_request</code>에 Branch와 Pull Request 활동 조건을 지정할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">,</span> <span class="nv">dev</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">opened</span><span class="pi">,</span> <span class="nv">synchronize</span><span class="pi">,</span> <span class="nv">reopened</span><span class="pi">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">create</code>와 <code class="language-plaintext highlighter-rouge">delete</code> Event는 Branch 또는 Tag가 생성되거나 삭제될 때 실행할 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">pull_request_target</code>은 Pull Request의 Head가 아니라 Base Branch 문맥에서 실행되고 Base Repository의 권한과 Secret에 접근할 수 있다. 외부 Fork의 신뢰하지 않은 Code를 Checkout해 실행하는 Workflow에는 사용하지 않는다. 일반적인 Pull Request Build와 Test에는 <code class="language-plaintext highlighter-rouge">pull_request</code>가 안전한 기본 선택이다.</p>

<h3 id="수동-실행과-외부-호출">수동 실행과 외부 호출</h3>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>사용 시점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">workflow_dispatch</code></td>
      <td>Actions 화면 또는 API에서 사용자가 직접 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">repository_dispatch</code></td>
      <td>외부 Application이나 Server가 GitHub API로 실행 요청</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">workflow_call</code></td>
      <td>다른 Workflow가 현재 Workflow를 재사용</td>
    </tr>
  </tbody>
</table>

<p>수동 실행 Button을 제공하려면 다음과 같이 작성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>
</code></pre></div></div>

<h3 id="일정-실행">일정 실행</h3>

<p>일정은 POSIX Cron 형식으로 작성한다. 기본 시간대는 UTC이며, 필요하면 IANA Time Zone 이름을 지정할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">cron</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0</span><span class="nv"> </span><span class="s">9</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">1-5"</span>
      <span class="na">timezone</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Asia/Seoul"</span>
</code></pre></div></div>

<p>이 예시는 평일 Asia/Seoul 09:00에 실행된다. 예약 Workflow는 Default Branch의 최신 Commit을 사용한다.</p>

<p>Issue, Issue Comment, Release와 Watch 같은 GitHub 활동도 Event로 사용할 수 있다. 실제 Workflow에는 필요한 Trigger만 지정해야 불필요한 실행과 비용을 줄일 수 있다.</p>

<h3 id="workflow-작성-중-값과-문법을-찾는-방법">Workflow 작성 중 값과 문법을 찾는 방법</h3>

<p>Workflow에 사용할 수 있는 Key, Event와 내부 값은 한 문서에 모두 모여 있지 않다. 찾으려는 대상에 따라 다음 공식 문서를 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>확인할 대상</th>
      <th>공식 문서</th>
      <th>확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>YAML 구조와 Key</td>
      <td><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax">Workflow syntax for GitHub Actions</a></td>
      <td><code class="language-plaintext highlighter-rouge">on</code>, <code class="language-plaintext highlighter-rouge">permissions</code>, <code class="language-plaintext highlighter-rouge">jobs</code>, <code class="language-plaintext highlighter-rouge">needs</code>, <code class="language-plaintext highlighter-rouge">runs-on</code>, <code class="language-plaintext highlighter-rouge">steps</code> 등</td>
    </tr>
    <tr>
      <td>Trigger와 세부 동작</td>
      <td><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows">Events that trigger workflows</a></td>
      <td><code class="language-plaintext highlighter-rouge">issues</code>, <code class="language-plaintext highlighter-rouge">pull_request</code>, <code class="language-plaintext highlighter-rouge">push</code>와 각 Event의 <code class="language-plaintext highlighter-rouge">types</code>, Filter, 주의 사항</td>
    </tr>
    <tr>
      <td>Expression에서 접근할 객체</td>
      <td><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/contexts">Contexts reference</a></td>
      <td><code class="language-plaintext highlighter-rouge">github</code>, <code class="language-plaintext highlighter-rouge">env</code>, <code class="language-plaintext highlighter-rouge">vars</code>, <code class="language-plaintext highlighter-rouge">secrets</code>, <code class="language-plaintext highlighter-rouge">runner</code>, <code class="language-plaintext highlighter-rouge">job</code>, <code class="language-plaintext highlighter-rouge">steps</code>, <code class="language-plaintext highlighter-rouge">matrix</code>, <code class="language-plaintext highlighter-rouge">needs</code> 등</td>
    </tr>
    <tr>
      <td>Runner의 기본 환경 변수</td>
      <td><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/variables">Variables reference</a></td>
      <td><code class="language-plaintext highlighter-rouge">GITHUB_REPOSITORY</code>, <code class="language-plaintext highlighter-rouge">GITHUB_SHA</code>, <code class="language-plaintext highlighter-rouge">GITHUB_REF</code>, <code class="language-plaintext highlighter-rouge">RUNNER_OS</code> 등</td>
    </tr>
    <tr>
      <td>조건식, 연산자와 함수</td>
      <td><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/expressions">Evaluate expressions in workflows and actions</a></td>
      <td><code class="language-plaintext highlighter-rouge">if</code>, 비교 연산자, <code class="language-plaintext highlighter-rouge">contains</code>, <code class="language-plaintext highlighter-rouge">startsWith</code>, <code class="language-plaintext highlighter-rouge">toJSON</code>, 상태 확인 함수 등</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">uses</code> Action의 입력과 출력</td>
      <td><a href="https://github.com/marketplace?type=actions">GitHub Marketplace</a>와 해당 Action의 README</td>
      <td><code class="language-plaintext highlighter-rouge">with</code>에 전달할 입력, <code class="language-plaintext highlighter-rouge">steps.&lt;id&gt;.outputs</code>로 받을 출력, 지원 Version</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">on</code>, <code class="language-plaintext highlighter-rouge">jobs</code>와 <code class="language-plaintext highlighter-rouge">permissions</code>는 GitHub Actions 자체의 Workflow 문법이다. 반면 <code class="language-plaintext highlighter-rouge">with</code> 아래에서 사용할 수 있는 값은 호출하는 Action마다 다르다. 예를 들어 <code class="language-plaintext highlighter-rouge">actions/setup-node</code>의 <code class="language-plaintext highlighter-rouge">node-version</code>과 <code class="language-plaintext highlighter-rouge">cache</code>는 해당 Action의 README에서 확인해야 한다.</p>

<p>Event가 제공하는 값도 Trigger마다 다르다. <code class="language-plaintext highlighter-rouge">issues</code> Event로 실행된 Workflow에서는 <code class="language-plaintext highlighter-rouge">github.event.issue</code>를 사용할 수 있지만, <code class="language-plaintext highlighter-rouge">push</code> Event에는 같은 객체가 없다. Issue가 닫혔을 때 전달되는 값을 확인하는 최소 Workflow는 다음과 같다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Inspect closed issue</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">issues</span><span class="pi">:</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">closed</span><span class="pi">]</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>
  <span class="na">issues</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">inspect</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Show selected event values</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">EVENT_ACTION</span><span class="pi">:</span> <span class="s">${{ github.event.action }}</span>
          <span class="na">ISSUE_NUMBER</span><span class="pi">:</span> <span class="s">${{ github.event.issue.number }}</span>
          <span class="na">ISSUE_URL</span><span class="pi">:</span> <span class="s">${{ github.event.issue.html_url }}</span>
          <span class="na">REPOSITORY</span><span class="pi">:</span> <span class="s">${{ github.repository }}</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">echo "action=$EVENT_ACTION"</span>
          <span class="s">echo "issue_number=$ISSUE_NUMBER"</span>
          <span class="s">echo "issue_url=$ISSUE_URL"</span>
          <span class="s">echo "repository=$REPOSITORY"</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>값</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">github.event.action</code></td>
      <td>Event의 세부 동작이다. 이 예시에서는 <code class="language-plaintext highlighter-rouge">closed</code>이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">github.event.issue.number</code></td>
      <td>닫힌 Issue의 번호이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">github.event.issue.html_url</code></td>
      <td>닫힌 Issue의 Web URL이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">github.repository</code></td>
      <td><code class="language-plaintext highlighter-rouge">owner/repository</code> 형식의 실행 대상 Repository이다.</td>
    </tr>
  </tbody>
</table>

<p>필요한 Property가 명확하지 않으면 <a href="https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow#using-event-information">Triggering a workflow의 event information</a>과 연결된 Webhook Payload 문서를 확인한다. 임시 진단 단계에서는 <code class="language-plaintext highlighter-rouge">toJSON(github.event)</code>로 Event Payload를 확인할 수도 있다. Issue 본문과 사용자 입력 같은 신뢰할 수 없는 값이 포함될 수 있으므로 Payload를 명령으로 실행하거나 외부로 전달하지 않는다. <code class="language-plaintext highlighter-rouge">github</code> Context 전체에는 Token 같은 민감한 값이 포함될 수 있으므로 통째로 Log에 출력하지 않는다.</p>

<h3 id="issue-종료-시-project-상태를-done으로-변경">Issue 종료 시 Project 상태를 Done으로 변경</h3>

<p>Issue나 Pull Request가 닫힐 때 Project의 <code class="language-plaintext highlighter-rouge">Status</code>를 <code class="language-plaintext highlighter-rouge">Done</code>으로 바꾸는 작업은 별도의 Actions YAML을 작성하기 전에 <a href="https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/using-the-built-in-automations">GitHub Projects의 내장 자동화</a>를 사용한다. 새 Project에는 닫힌 Issue·Pull Request와 병합된 Pull Request의 상태를 <code class="language-plaintext highlighter-rouge">Done</code>으로 바꾸는 기본 Workflow가 활성화되어 있다.</p>

<p>설정은 Project의 <strong>메뉴 → Workflows → Default workflows</strong>에서 대상 Workflow를 선택하고, 변경할 <code class="language-plaintext highlighter-rouge">Status</code>를 <code class="language-plaintext highlighter-rouge">Done</code>으로 지정한 뒤 저장하여 활성화한다. 이 자동화는 Project에 들어 있는 Item의 상태를 변경한다. Issue를 Project에 넣는 과정도 자동화하려면 별도의 <a href="https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/adding-items-automatically">Auto-add to project</a> Workflow와 Filter를 설정한다.</p>

<p>내장 자동화로 표현할 수 없는 조건이나 사용자 정의 Field를 변경해야 한다면 <a href="https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/automating-projects-using-actions">Automating Projects using Actions</a>와 <a href="https://docs.github.com/en/graphql/reference/projects#updateprojectv2itemfieldvalue">Projects GraphQL reference</a>를 사용한다. 이 경우 Project ID, Item ID, Field ID와 선택 Option ID를 조회한 뒤 <code class="language-plaintext highlighter-rouge">updateProjectV2ItemFieldValue</code> Mutation을 호출한다. Repository 범위의 <code class="language-plaintext highlighter-rouge">GITHUB_TOKEN</code>은 Projects에 접근할 수 없으므로 Organization Project에는 GitHub App, User Project에는 필요한 Project 권한을 가진 Personal Access Token을 사용한다.</p>

<h2 id="4--가장-작은-workflow-실행">4 ) 가장 작은 Workflow 실행</h2>

<hr />

<p>실습용 Remote Repository를 만들고 Local Directory를 초기화한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>githubaction-sample
<span class="nb">cd </span>githubaction-sample
git init
git branch <span class="nt">-M</span> main
</code></pre></div></div>

<p>확인용 File을 만든 뒤 Commit하고 자신의 Remote Repository URL을 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch </span>README.md
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Add GitHub Actions sample"</span>
git remote add origin <span class="se">\</span>
  https://github.com/&lt;owner&gt;/githubaction-sample.git
git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">&lt;owner&gt;</code>는 자신의 GitHub 계정이나 Organization 이름으로 바꾼다. <code class="language-plaintext highlighter-rouge">git remote -v</code>로 등록된 URL을 확인할 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">.github/workflows/ci-example.yml</code>을 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> .github/workflows
</code></pre></div></div>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI Example</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v7</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Print one line</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">echo "Hello GitHub Actions"</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Python</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v7</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.13"</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Display Python version</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">python --version</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">actions/checkout</code>은 Runner에 Repository Source를 가져오고 <code class="language-plaintext highlighter-rouge">actions/setup-python</code>은 지정한 Python을 사용할 수 있도록 설정한다. <code class="language-plaintext highlighter-rouge">permissions: contents: read</code>는 Workflow Token에 Source를 읽는 최소 권한만 부여한다.</p>

<p>예시의 공식 Action Major Version은 2026년 9월 기준이다. Version 7 Action을 Self-hosted Runner에서 사용하려면 Runner Version이 <code class="language-plaintext highlighter-rouge">v2.327.1</code> 이상인지 확인한다. 장기간 유지하는 운영 Workflow에서는 Action Release Note를 확인하고, 공급망 변경을 엄격하게 제어해야 한다면 검증한 Full Commit SHA로 고정한다.</p>

<p>Workflow File을 Commit하고 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add .github/workflows/ci-example.yml
git commit <span class="nt">-m</span> <span class="s2">"Add CI example workflow"</span>
git push
</code></pre></div></div>

<p>GitHub Repository의 <strong>Actions → CI Example</strong>에서 실행 중인 Workflow와 각 Step의 Log를 확인한다.</p>

<h2 id="5--nodejs와-mocha-test-자동화">5 ) Node.js와 Mocha Test 자동화</h2>

<hr />

<h3 id="local-project-생성">Local Project 생성</h3>

<p>Node.js Project를 만들고 Mocha를 Development Dependency로 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>nodetest
<span class="nb">cd </span>nodetest
npm init <span class="nt">-y</span>
npm <span class="nb">install</span> <span class="nt">--save-dev</span> mocha
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">npm init -y</code></td>
      <td>기본값으로 <code class="language-plaintext highlighter-rouge">package.json</code> 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">npm install --save-dev mocha</code></td>
      <td>Mocha를 개발용 Dependency에 추가하고 <code class="language-plaintext highlighter-rouge">package-lock.json</code> 생성</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">test.spec.js</code>에 실제 Assertion이 있는 Test를 작성한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">assert</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="dl">"</span><span class="s2">node:assert/strict</span><span class="dl">"</span><span class="p">);</span>

<span class="nx">describe</span><span class="p">(</span><span class="dl">"</span><span class="s2">Default Test Set</span><span class="dl">"</span><span class="p">,</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">it</span><span class="p">(</span><span class="dl">"</span><span class="s2">adds two numbers</span><span class="dl">"</span><span class="p">,</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">assert</span><span class="p">.</span><span class="nx">equal</span><span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">);</span>
  <span class="p">});</span>

  <span class="nx">it</span><span class="p">(</span><span class="dl">"</span><span class="s2">compares text</span><span class="dl">"</span><span class="p">,</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">assert</span><span class="p">.</span><span class="nx">equal</span><span class="p">(</span><span class="dl">"</span><span class="s2">github</span><span class="dl">"</span><span class="p">.</span><span class="nx">toUpperCase</span><span class="p">(),</span> <span class="dl">"</span><span class="s2">GITHUB</span><span class="dl">"</span><span class="p">);</span>
  <span class="p">});</span>
<span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">console.log</code>만 실행하면 Test가 실패 조건을 검증하지 못한다. <code class="language-plaintext highlighter-rouge">assert.equal</code>을 사용하면 실제 값이 기대값과 다를 때 Mocha가 실패 상태를 반환한다.</p>

<p><code class="language-plaintext highlighter-rouge">package.json</code>의 <code class="language-plaintext highlighter-rouge">scripts</code>를 다음과 같이 설정한다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"scripts"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"test"</span><span class="p">:</span><span class="w"> </span><span class="s2">"mocha test.spec.js"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Local에서 먼저 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm <span class="nb">test</span>
</code></pre></div></div>

<p>두 Test가 통과하고 Process 종료 Code가 <code class="language-plaintext highlighter-rouge">0</code>인지 확인한다. 실패하면 Mocha는 0이 아닌 종료 Code를 반환하고 GitHub Actions의 Step도 실패 처리된다.</p>

<h3 id="git-repository-준비">Git Repository 준비</h3>

<p><code class="language-plaintext highlighter-rouge">node_modules</code>는 설치 결과이므로 Commit하지 않는다. <code class="language-plaintext highlighter-rouge">.gitignore</code>에 추가하고 Source, Package Metadata와 Lockfile을 Commit한다.</p>

<pre><code class="language-gitignore">node_modules/
</code></pre>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
git branch <span class="nt">-M</span> main
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Add Node.js tests"</span>
git remote add origin https://github.com/&lt;owner&gt;/nodetest.git
git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<h3 id="nodejs-workflow">Node.js Workflow</h3>

<p><code class="language-plaintext highlighter-rouge">.github/workflows/nodetest.yml</code>을 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> .github/workflows
</code></pre></div></div>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Node.js CI</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">strategy</span><span class="pi">:</span>
      <span class="na">matrix</span><span class="pi">:</span>
        <span class="na">node-version</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">24.x</span><span class="pi">]</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v7</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Node.js ${{ matrix.node-version }}</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-node@v7</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">node-version</span><span class="pi">:</span> <span class="s">${{ matrix.node-version }}</span>
          <span class="na">cache</span><span class="pi">:</span> <span class="s">npm</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install dependencies</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm ci</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build when a build script exists</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm run build --if-present</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run tests</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm test</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">npm ci</code>는 <code class="language-plaintext highlighter-rouge">package-lock.json</code>의 정확한 Dependency Version을 사용해 깨끗하게 설치한다. 따라서 Lockfile을 Repository에 Commit해야 한다. <code class="language-plaintext highlighter-rouge">cache: npm</code>은 npm의 Download Cache를 재사용하지만 <code class="language-plaintext highlighter-rouge">node_modules</code> 자체를 Job 사이에 공유하지는 않는다.</p>

<p>Matrix에 Version을 추가하면 같은 Test Job을 여러 Node.js Version에서 실행할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">strategy</span><span class="pi">:</span>
  <span class="na">matrix</span><span class="pi">:</span>
    <span class="na">node-version</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">22.x</span><span class="pi">,</span> <span class="nv">24.x</span><span class="pi">]</span>
</code></pre></div></div>

<p>Matrix의 각 조합은 별도 Job으로 실행되므로 한 Version의 실패가 다른 Version의 Log와 섞이지 않는다.</p>

<h2 id="6--환경-변수-variable과-secret">6 ) 환경 변수, Variable과 Secret</h2>

<hr />

<p>Workflow는 GitHub가 제공하는 기본 환경 변수와 사용자가 정의한 Configuration을 사용할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>종류</th>
      <th>이름 또는 문법</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기본 환경 변수</td>
      <td><code class="language-plaintext highlighter-rouge">GITHUB_*</code>, <code class="language-plaintext highlighter-rouge">RUNNER_*</code></td>
      <td>Repository, Commit, Workflow와 Runner 정보</td>
    </tr>
    <tr>
      <td>Workflow 환경 변수</td>
      <td><code class="language-plaintext highlighter-rouge">env</code></td>
      <td>Workflow, Job 또는 Step 범위의 일반 설정</td>
    </tr>
    <tr>
      <td>Configuration Variable</td>
      <td><code class="language-plaintext highlighter-rouge">vars</code> Context</td>
      <td>환경별 공개 설정 값</td>
    </tr>
    <tr>
      <td>Secret</td>
      <td><code class="language-plaintext highlighter-rouge">secrets</code> Context</td>
      <td>Token, Password와 인증 정보</td>
    </tr>
  </tbody>
</table>

<p>기본 환경 변수는 Shell에서 직접 확인할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Show workflow context</span>
  <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
    <span class="s">echo "repository=$GITHUB_REPOSITORY"</span>
    <span class="s">echo "sha=$GITHUB_SHA"</span>
    <span class="s">echo "runner=$RUNNER_OS"</span>
</code></pre></div></div>

<p>일반 환경 변수와 Repository Variable을 함께 사용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">env</span><span class="pi">:</span>
  <span class="na">APP_NAME</span><span class="pi">:</span> <span class="s">sample-api</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">echo "$APP_NAME runs in $APP_ENV"</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">APP_ENV</span><span class="pi">:</span> <span class="s">${{ vars.APP_ENV }}</span>
</code></pre></div></div>

<p>Password나 Access Token을 Workflow File에 직접 적지 않는다. Repository 또는 Environment의 Secret으로 등록하고 필요한 Step에서만 전달한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Authenticate</span>
  <span class="na">env</span><span class="pi">:</span>
    <span class="na">REGISTRY_TOKEN</span><span class="pi">:</span> <span class="s">${{ secrets.REGISTRY_TOKEN }}</span>
  <span class="na">run</span><span class="pi">:</span> <span class="s">./scripts/login.sh</span>
</code></pre></div></div>

<p>Secret은 Log에서 Masking되지만 변환하거나 조합한 값이 항상 안전하게 가려진다고 가정해서는 안 된다. Secret 자체를 <code class="language-plaintext highlighter-rouge">echo</code>하거나 Debug Log에 출력하지 않고, 외부 Fork에서 시작한 Workflow에 어떤 권한과 Secret이 제공되는지 확인한다.</p>

<h2 id="7--python-flask와-pytest-자동화">7 ) Python, Flask와 Pytest 자동화</h2>

<hr />

<h3 id="project와-virtual-environment">Project와 Virtual Environment</h3>

<p>Python Project Directory에서 Virtual Environment를 만들고 활성화한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>python-ci-sample
<span class="nb">cd </span>python-ci-sample
python <span class="nt">-m</span> venv .venv
<span class="nb">source</span> .venv/bin/activate
</code></pre></div></div>

<p>Windows PowerShell에서는 다음 명령을 사용한다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">.</span><span class="nf">venv</span><span class="n">\Scripts\Activate.ps1</span><span class="w">
</span></code></pre></div></div>

<p>필요한 Package를 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> pip <span class="nb">install</span> <span class="se">\</span>
  flask requests beautifulsoup4 pymongo pytest
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">app.py</code>는 MongoDB Host를 환경 변수로 받도록 작성한다. Host Computer에서 실행할 때 기본값은 <code class="language-plaintext highlighter-rouge">localhost</code>이고, Container Network에서 Service 이름이 <code class="language-plaintext highlighter-rouge">mongo</code>라면 <code class="language-plaintext highlighter-rouge">MONGO_HOST=mongo</code>를 전달할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">os</span>

<span class="kn">from</span> <span class="nn">flask</span> <span class="kn">import</span> <span class="n">Flask</span><span class="p">,</span> <span class="n">render_template</span>
<span class="kn">from</span> <span class="nn">pymongo</span> <span class="kn">import</span> <span class="n">MongoClient</span>


<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>
<span class="n">client</span> <span class="o">=</span> <span class="n">MongoClient</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="n">getenv</span><span class="p">(</span><span class="s">"MONGO_HOST"</span><span class="p">,</span> <span class="s">"localhost"</span><span class="p">),</span> <span class="mi">27017</span><span class="p">)</span>
<span class="n">db</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">dbsparta</span>


<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">home</span><span class="p">():</span>
    <span class="k">return</span> <span class="n">render_template</span><span class="p">(</span><span class="s">"index.html"</span><span class="p">)</span>


<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">app</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="s">"0.0.0.0"</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">5000</span><span class="p">,</span> <span class="n">debug</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">templates/index.html</code>을 작성한다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!doctype html&gt;</span>
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"ko"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"utf-8"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;title&gt;</span>Flask sample<span class="nt">&lt;/title&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;h3&gt;</span>Flask Web Application<span class="nt">&lt;/h3&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>Application까지 Local에서 실행하려면 MongoDB가 필요하다. Docker를 사용할 경우 Project Directory의 <code class="language-plaintext highlighter-rouge">data</code>에 DB Data를 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> data
docker run <span class="se">\</span>
  <span class="nt">--name</span> mongodb <span class="se">\</span>
  <span class="nt">-v</span> <span class="s2">"</span><span class="nv">$PWD</span><span class="s2">/data:/data/db"</span> <span class="se">\</span>
  <span class="nt">-p</span> 27017:27017 <span class="se">\</span>
  <span class="nt">-d</span> mongo
docker ps <span class="nt">--filter</span> <span class="nv">name</span><span class="o">=</span>mongodb
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--name mongodb</code></td>
      <td>Container 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v "$PWD/data:/data/db"</code></td>
      <td>Host Directory를 MongoDB Data Directory에 Mount</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p 27017:27017</code></td>
      <td>Host와 Container의 MongoDB Port 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>Background에서 Container 실행</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">python app.py</code>를 실행한 뒤 <code class="language-plaintext highlighter-rouge">http://localhost:5000</code>에서 Page를 확인한다.</p>

<h3 id="외부-환경에-의존하지-않는-unit-test">외부 환경에 의존하지 않는 Unit Test</h3>

<p><code class="language-plaintext highlighter-rouge">utils.py</code>에 작은 함수를 작성한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_title</span><span class="p">():</span>
    <span class="k">return</span> <span class="s">"title"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">test_utils.py</code>에서 반환 값을 검증한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">utils</span> <span class="kn">import</span> <span class="n">get_title</span>


<span class="k">def</span> <span class="nf">test_get_title</span><span class="p">():</span>
    <span class="k">assert</span> <span class="n">get_title</span><span class="p">()</span> <span class="o">==</span> <span class="s">"title"</span>
</code></pre></div></div>

<p>Test를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pytest
</code></pre></div></div>

<p>이 Test는 Flask Server나 MongoDB를 사용하지 않는 Unit Test이다. 따라서 CI Runner에 MongoDB Container를 시작하지 않아도 된다. DB 연결까지 검증하는 Integration Test를 추가한다면 Workflow에 MongoDB Service Container와 준비 상태 확인이 필요하다.</p>

<p>Virtual Environment에서 실제로 설치한 Package Version을 <code class="language-plaintext highlighter-rouge">requirements.txt</code>에 기록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">.gitignore</code>에는 Local 환경과 실행 중 생긴 File을 추가한다.</p>

<pre><code class="language-gitignore">.venv/
__pycache__/
.pytest_cache/
data/
</code></pre>

<h3 id="pytest-workflow">Pytest Workflow</h3>

<p><code class="language-plaintext highlighter-rouge">.github/workflows/ci-test.yml</code>을 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> .github/workflows
</code></pre></div></div>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Python CI</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v7</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Python</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v7</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.13"</span>
          <span class="na">cache</span><span class="pi">:</span> <span class="s">pip</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install dependencies</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">python -m pip install -r requirements.txt</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run tests</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">pytest</span>
</code></pre></div></div>

<p>Workflow가 실행하는 것은 Dependency 설치와 Pytest까지이다. Container Image를 Build하거나 Registry에 Push하는 Step은 정의되어 있지 않다.</p>

<p>Workflow와 Project File을 Remote Repository에 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
git branch <span class="nt">-M</span> main
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Add Python CI workflow"</span>
git remote add origin <span class="se">\</span>
  https://github.com/&lt;owner&gt;/python-ci-sample.git
git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<p>Push가 끝나면 Actions Tab에서 <code class="language-plaintext highlighter-rouge">Python CI</code> Workflow의 <code class="language-plaintext highlighter-rouge">Install dependencies</code>와 <code class="language-plaintext highlighter-rouge">Run tests</code> Step을 확인한다.</p>

<h2 id="8--flask-application을-image로-만들기">8 ) Flask Application을 Image로 만들기</h2>

<hr />

<p>Pytest가 통과한 Source를 같은 실행 환경으로 전달하려면 Container Image로 Package할 수 있다. 다음 <code class="language-plaintext highlighter-rouge">Dockerfile</code>은 Python 3.13 기반 Image에 Dependency와 Application Source를 넣고 Flask Server를 실행한다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> python:3.13-slim</span>

<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>

<span class="k">COPY</span><span class="s"> requirements.txt ./</span>
<span class="k">RUN </span>python <span class="nt">-m</span> pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="k">EXPOSE</span><span class="s"> 5000</span>

<span class="k">CMD</span><span class="s"> ["python", "-m", "flask", "run", "--host=0.0.0.0", "--port=5000"]</span>
</code></pre></div></div>

<p>Flask는 <code class="language-plaintext highlighter-rouge">app.py</code>를 기본 Application Module로 탐색한다. File 이름이 다르면 <code class="language-plaintext highlighter-rouge">FLASK_APP</code> 환경 변수를 지정해야 한다. Image에 Local Virtual Environment와 MongoDB Data가 들어가지 않도록 <code class="language-plaintext highlighter-rouge">.dockerignore</code>도 작성한다.</p>

<pre><code class="language-dockerignore">.git/
.github/
.venv/
__pycache__/
.pytest_cache/
data/
</code></pre>

<p>Workflow에 연결하기 전에 Local에서 Image를 Build하고 Container가 시작되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> flaskweb:local <span class="nb">.</span>
docker run <span class="nt">--rm</span> <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">--name</span> flaskweb <span class="se">\</span>
  <span class="nt">-p</span> 5000:5000 <span class="se">\</span>
  flaskweb:local
docker ps <span class="nt">--filter</span> <span class="nv">name</span><span class="o">=</span>flaskweb
curl http://localhost:5000
docker stop flaskweb
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">docker build</code>가 성공해도 Application Process가 정상적으로 시작된다는 보장은 없다. <code class="language-plaintext highlighter-rouge">docker ps</code>, HTTP 요청과 <code class="language-plaintext highlighter-rouge">docker logs flaskweb</code>을 함께 사용해 Build 결과와 실행 결과를 구분해서 확인한다.</p>

<h2 id="9--test-성공-후-docker-hub에-push">9 ) Test 성공 후 Docker Hub에 Push</h2>

<hr />

<p><a href="/cloud-native-41-container-image-registry/">Container Image Registry</a>에서 정리한 Docker Hub Repository를 생성하고, Docker Hub Personal Access Token(PAT)을 준비한다. 계정 Password나 Token을 Workflow File에 직접 작성하면 Git 이력과 Log를 통해 노출될 수 있으므로 Repository Secret으로 등록한다.</p>

<p>GitHub Repository의 <strong>Settings → Secrets and variables → Actions → New repository secret</strong>에서 다음 값을 만든다.</p>

<table>
  <thead>
    <tr>
      <th>Secret</th>
      <th>저장할 값</th>
      <th>사용 목적</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DOCKERHUB_USERNAME</code></td>
      <td>Docker Hub 계정 이름</td>
      <td>Registry 로그인과 Image 이름 구성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DOCKERHUB_TOKEN</code></td>
      <td>Docker Hub PAT</td>
      <td>Password 대신 Registry 인증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DOCKERHUB_REPO</code></td>
      <td>생성한 Repository 이름</td>
      <td>Push할 Image Repository 지정</td>
    </tr>
  </tbody>
</table>

<p>실제 Token을 Source, Workflow, 화면 캡처 또는 문서에 기록하지 않는다. 이미 노출한 Token은 삭제만으로 안전해지지 않으므로 Docker Hub에서 폐기하고 새 Token을 발급한다.</p>

<p>기존 <code class="language-plaintext highlighter-rouge">.github/workflows/ci-test.yml</code>을 다음과 같이 확장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Python CI and Docker Image</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v7</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Python</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v7</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.13"</span>
          <span class="na">cache</span><span class="pi">:</span> <span class="s">pip</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install dependencies</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">python -m pip install -r requirements.txt</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run tests</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">pytest</span>

  <span class="na">build-and-push</span><span class="pi">:</span>
    <span class="na">needs</span><span class="pi">:</span> <span class="s">test</span>
    <span class="na">if</span><span class="pi">:</span> <span class="s">github.event_name == 'push'</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout repository</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v7</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Log in to Docker Hub</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">docker/login-action@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">username</span><span class="pi">:</span> <span class="s">${{ secrets.DOCKERHUB_USERNAME }}</span>
          <span class="na">password</span><span class="pi">:</span> <span class="s">${{ secrets.DOCKERHUB_TOKEN }}</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build image</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">IMAGE</span><span class="pi">:</span> <span class="s">${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_REPO }}</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">docker build -t "$IMAGE:${GITHUB_SHA}" -t "$IMAGE:latest" .</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Push image</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">IMAGE</span><span class="pi">:</span> <span class="s">${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.DOCKERHUB_REPO }}</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">docker push "$IMAGE:${GITHUB_SHA}"</span>
          <span class="s">docker push "$IMAGE:latest"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">needs: test</code>는 Test가 성공해야 <code class="language-plaintext highlighter-rouge">build-and-push</code> Job을 시작한다. <code class="language-plaintext highlighter-rouge">if: github.event_name == 'push'</code>는 Pull Request 검증에서는 Test만 수행하고 <code class="language-plaintext highlighter-rouge">main</code> Branch에 실제 Push된 실행에서만 Registry를 변경하도록 제한한다.</p>

<p>Image에는 두 Tag를 붙인다.</p>

<table>
  <thead>
    <tr>
      <th>Tag</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">${GITHUB_SHA}</code></td>
      <td>어떤 Commit으로 만든 Image인지 추적</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">latest</code></td>
      <td>가장 최근에 Push한 Image를 가리키는 편의용 Tag</td>
    </tr>
  </tbody>
</table>

<p>배포 자동화에서는 변경될 수 있는 <code class="language-plaintext highlighter-rouge">latest</code>보다 Commit SHA처럼 불변에 가까운 Tag를 지정하는 편이 재현과 Rollback에 유리하다. Workflow를 Push한 뒤 Actions 화면에서 <code class="language-plaintext highlighter-rouge">test</code>, <code class="language-plaintext highlighter-rouge">build-and-push</code> 순서와 각 Step의 Log를 확인하고 Docker Hub Repository에서 두 Tag가 생성됐는지 확인한다.</p>

<p>공식 입력 값과 권장 인증 방식은 <a href="https://github.com/docker/login-action">docker/login-action</a>과 <a href="https://docs.docker.com/security/access-tokens/">Docker Hub access token</a> 문서에서 확인한다.</p>

<h2 id="10--actions-화면과-log-확인">10 ) Actions 화면과 Log 확인</h2>

<hr />

<p>Workflow를 Push한 뒤 다음 순서로 결과를 확인한다.</p>

<ol>
  <li>
    <p>GitHub Repository의 <strong>Actions</strong> Tab을 연다.</p>
  </li>
  <li>
    <p>왼쪽에서 Workflow 이름을 선택한다.</p>
  </li>
  <li>
    <p>Commit에 대응하는 Workflow Run을 선택한다.</p>
  </li>
  <li>
    <p>실패하거나 확인할 Job을 선택한다.</p>
  </li>
  <li>
    <p>Step을 펼쳐 실행 명령, 표준 출력과 Error Message를 확인한다.</p>
  </li>
</ol>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>우선 확인할 항목</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Workflow가 시작되지 않음</td>
      <td>Workflow File 경로, YAML 문법, <code class="language-plaintext highlighter-rouge">on</code> Branch 조건</td>
    </tr>
    <tr>
      <td>Checkout 실패</td>
      <td>Repository 권한, <code class="language-plaintext highlighter-rouge">permissions</code>, Submodule 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">npm ci</code> 실패</td>
      <td><code class="language-plaintext highlighter-rouge">package-lock.json</code> 누락 또는 <code class="language-plaintext highlighter-rouge">package.json</code>과 불일치</td>
    </tr>
    <tr>
      <td>Python Package 설치 실패</td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code>, Python Version, Package 지원 범위</td>
    </tr>
    <tr>
      <td>Test 실패</td>
      <td>실패한 Step의 Log, Assertion, Local과 CI 환경 차이</td>
    </tr>
    <tr>
      <td>Secret을 찾지 못함</td>
      <td>Secret 이름, Repository·Environment 범위, Fork Event 정책</td>
    </tr>
  </tbody>
</table>

<p>실패한 Step 이름이 구체적이면 전체 Log를 훑지 않고도 <code class="language-plaintext highlighter-rouge">Install dependencies</code>, <code class="language-plaintext highlighter-rouge">Run tests</code>처럼 문제 범위를 빠르게 좁힐 수 있다. 하나의 Job이 너무 많은 책임을 가지면 독립적으로 재실행하거나 원인을 분리하기 어려우므로 Build, Test와 Package 경계를 기준으로 나눈다.</p>

<blockquote>
  <p><strong>최종 정리</strong></p>
  <ul>
    <li>
      <p>Event가 Workflow를 시작하고 Job은 Runner에서 실행되며, Job 내부 Step은 작성 순서대로 실행된다.</p>
    </li>
    <li>
      <p>서로 다른 Job은 실행 환경을 자동으로 공유하지 않고, 순서가 필요하면 <code class="language-plaintext highlighter-rouge">needs</code>로 의존성을 정의한다.</p>
    </li>
    <li>
      <p>Node.js CI는 Lockfile과 <code class="language-plaintext highlighter-rouge">npm ci</code>를 사용하고, Python CI는 Virtual Environment에서 확정한 Dependency와 Pytest를 사용한다.</p>
    </li>
    <li>
      <p>Variable과 Secret은 역할을 구분하고 인증 정보를 Workflow File이나 Log에 직접 노출하지 않는다.</p>
    </li>
    <li>
      <p>Workflow 작성 중에는 Syntax, Event, Context, Variable, Expression과 Action별 README를 구분하여 필요한 값을 찾는다.</p>
    </li>
    <li>
      <p>Issue 종료에 따른 Project 상태 변경은 내장 자동화를 우선 사용하고, 복잡한 조건과 사용자 정의 Field 변경에는 Actions와 GraphQL API를 사용한다.</p>
    </li>
    <li>
      <p>Python Test가 성공한 뒤 <code class="language-plaintext highlighter-rouge">needs</code>로 Image Build를 연결하고, Docker Hub 인증 정보는 Repository Secret으로 주입한다.</p>
    </li>
    <li>
      <p>Image에는 Commit SHA Tag를 함께 붙여 Source와 배포 Artifact의 관계를 추적한다.</p>
    </li>
    <li>
      <p>Actions 화면에서는 Workflow, Job과 Step 단위로 상태와 Log를 확인할 수 있다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="GitHubActions" /><summary type="html"><![CDATA[GitHub Actions의 실행 구조를 이해하고 Node.js와 Python Test부터 Docker Image Build 및 Docker Hub Push까지 자동화한다]]></summary></entry><entry><title type="html">Git 이력 관리와 Branch 작업</title><link href="https://hyn128.site/cloud-native-43-git-history-branch/" rel="alternate" type="text/html" title="Git 이력 관리와 Branch 작업" /><published>2026-09-10T00:00:00+09:00</published><updated>2026-09-10T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-43-git-history-branch</id><content type="html" xml:base="https://hyn128.site/cloud-native-43-git-history-branch/"><![CDATA[<p><a href="/cloud-native-42-git-version-control-basics/">Version Control과 Git 기본 작업 흐름</a>에서 Working Tree, Staging Area와 Local Repository의 관계를 살펴봤다. 이 글에서는 저장된 Commit을 조회하고 비교하는 방법부터 변경 복구, Branch 통합까지 다룬다.</p>

<p>SourceTree를 사용해도 Git의 상태와 History는 CLI를 사용할 때와 같다. GUI 조작과 명령을 함께 확인하면 버튼을 눌렀을 때 Repository에서 무엇이 바뀌는지 이해할 수 있다.</p>

<h2 id="1--sourcetree와-git-cli">1 ) SourceTree와 Git CLI</h2>

<hr />

<p>SourceTree는 Git Repository를 시각적으로 다루는 GUI Client이다. File 상태, Commit Graph와 Branch 관계를 화면에 표시하고 선택한 작업에 해당하는 Git 명령을 실행한다.</p>

<table>
  <thead>
    <tr>
      <th>Git 작업</th>
      <th>SourceTree에서 확인할 영역</th>
      <th>CLI</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Repository 생성</td>
      <td>Local Repository 생성</td>
      <td><code class="language-plaintext highlighter-rouge">git init</code></td>
    </tr>
    <tr>
      <td>File 상태 확인</td>
      <td>File Status·Working Copy</td>
      <td><code class="language-plaintext highlighter-rouge">git status</code></td>
    </tr>
    <tr>
      <td>Stage</td>
      <td>Stage 대상 선택</td>
      <td><code class="language-plaintext highlighter-rouge">git add</code>, <code class="language-plaintext highlighter-rouge">git restore --staged</code></td>
    </tr>
    <tr>
      <td>Commit</td>
      <td>Commit Message 입력 영역</td>
      <td><code class="language-plaintext highlighter-rouge">git commit</code></td>
    </tr>
    <tr>
      <td>History</td>
      <td>Commit Graph·History</td>
      <td><code class="language-plaintext highlighter-rouge">git log --graph</code></td>
    </tr>
    <tr>
      <td>Tag</td>
      <td>Commit의 Tag 메뉴</td>
      <td><code class="language-plaintext highlighter-rouge">git tag</code></td>
    </tr>
    <tr>
      <td>Branch 전환</td>
      <td>Branch 목록</td>
      <td><code class="language-plaintext highlighter-rouge">git switch</code></td>
    </tr>
    <tr>
      <td>통합</td>
      <td>Merge·Rebase 메뉴</td>
      <td><code class="language-plaintext highlighter-rouge">git merge</code>, <code class="language-plaintext highlighter-rouge">git rebase</code></td>
    </tr>
    <tr>
      <td>임시 보관</td>
      <td>Stash 메뉴</td>
      <td><code class="language-plaintext highlighter-rouge">git stash</code></td>
    </tr>
  </tbody>
</table>

<p>버튼 이름과 화면 배치는 SourceTree Version과 운영체제에 따라 달라질 수 있다. 조작 후 <code class="language-plaintext highlighter-rouge">git status</code>, <code class="language-plaintext highlighter-rouge">git log</code>와 <code class="language-plaintext highlighter-rouge">git branch</code>로 실제 Repository 상태를 확인하면 GUI 표시와 Git 개념을 연결할 수 있다.</p>

<h2 id="2--local-repository에서-version-생성">2 ) Local Repository에서 Version 생성</h2>

<hr />

<h3 id="repository-준비">Repository 준비</h3>

<p>SourceTree에서는 Local Repository 생성 화면에서 작업 Directory를 선택한다. CLI에서는 같은 Directory로 이동해 <code class="language-plaintext highlighter-rouge">git init</code>을 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>git-history-lab
<span class="nb">cd </span>git-history-lab
git init <span class="nt">--initial-branch</span><span class="o">=</span>main
</code></pre></div></div>

<p>초기 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div>

<h3 id="첫-번째-commit">첫 번째 Commit</h3>

<p><code class="language-plaintext highlighter-rouge">a.txt</code>, <code class="language-plaintext highlighter-rouge">b.txt</code>, <code class="language-plaintext highlighter-rouge">c.txt</code>를 만들고 각각 <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">C</code>를 기록한다. File 작성 방식은 사용하는 Editor에 따라 달라지므로 여기서는 Git 상태 변화에 집중한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git-history-lab/
├── a.txt
├── b.txt
└── c.txt
</code></pre></div></div>

<p>새 File은 Untracked 상태이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div>

<p>세 File을 다음 Commit 대상으로 Stage한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add a.txt b.txt c.txt
git diff <span class="nt">--staged</span>
git status
</code></pre></div></div>

<p>SourceTree에서는 Unstaged File 목록에서 대상을 선택하여 Staged File 영역으로 옮긴다. Commit 전에는 Staged Diff를 읽어 의도한 내용만 포함됐는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"docs: add initial text files"</span>
</code></pre></div></div>

<h3 id="두-번째-commit">두 번째 Commit</h3>

<p><code class="language-plaintext highlighter-rouge">a.txt</code>를 수정하고 <code class="language-plaintext highlighter-rouge">c.txt</code>를 삭제하면 두 File 모두 Working Tree 변경으로 표시된다. 삭제도 Git이 추적하는 변경이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff
</code></pre></div></div>

<p>변경을 Stage하고 두 번째 Commit을 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add a.txt c.txt
git diff <span class="nt">--staged</span>
git commit <span class="nt">-m</span> <span class="s2">"docs: update A and remove C"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git add .</code>은 현재 Directory 아래의 추가, 수정과 삭제를 한꺼번에 Stage할 수 있다. 범위가 넓으므로 사용 직후 <code class="language-plaintext highlighter-rouge">git diff --staged</code>로 Commit 대상을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git diff <span class="nt">--staged</span>
</code></pre></div></div>

<p>이미 추적 중인 File의 수정과 삭제를 Stage하면서 Commit까지 수행할 때는 <code class="language-plaintext highlighter-rouge">-a</code>를 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-am</span> <span class="s2">"docs: update tracked files"</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git commit -am</code>은 Untracked File을 포함하지 않는다. 새 File은 먼저 <code class="language-plaintext highlighter-rouge">git add</code>로 추적 대상에 포함해야 한다.</p>

<h2 id="3--commit-history-조회">3 ) Commit History 조회</h2>

<hr />

<p>SourceTree의 History 화면은 Commit을 Graph로 표시한다. CLI에서는 목적에 따라 <code class="language-plaintext highlighter-rouge">git log</code> Option을 조합한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 상세 History</span>
git log

<span class="c"># 한 Commit을 한 줄로 표시</span>
git log <span class="nt">--oneline</span>

<span class="c"># Commit별 Patch 포함</span>
git log <span class="nt">--patch</span>

<span class="c"># Branch와 Tag를 함께 표시</span>
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<p>Commit을 선택하면 작성자, 시각, Message, Parent와 변경된 File을 확인할 수 있다. CLI에서는 <code class="language-plaintext highlighter-rouge">git show</code>를 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show &lt;commit&gt;
git show <span class="nt">--stat</span> &lt;commit&gt;
git show <span class="nt">--name-status</span> &lt;commit&gt;
</code></pre></div></div>

<p>Commit 개수는 작업을 나눈 방식에 따라 크게 달라진다. 많은 Commit이 Project 품질이나 기여 수준을 자동으로 증명하지 않으므로 변경 내용과 Message, Review 결과를 함께 봐야 한다.</p>

<h2 id="4--tag로-version-표시">4 ) Tag로 Version 표시</h2>

<hr />

<blockquote>
  <p><strong>Tag</strong></p>

  <p>특정 Git Object를 읽기 쉬운 이름으로 가리키는 Reference이다. Release Version을 표시할 때 주로 Commit에 연결한다.</p>
</blockquote>

<p>Commit Object ID는 Git이 Content와 Metadata를 바탕으로 생성한다. 사용자가 원하는 문자열로 Object ID를 정할 수 없으므로 <code class="language-plaintext highlighter-rouge">v1.0.0</code> 같은 Tag를 별도로 붙인다.</p>

<h3 id="lightweight-tag">Lightweight Tag</h3>

<p>현재 Commit을 직접 가리키는 Lightweight Tag를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git tag v1.0.0
</code></pre></div></div>

<h3 id="annotated-tag">Annotated Tag</h3>

<p>Annotated Tag에는 작성자, 생성 시각과 Message가 별도 Tag Object로 기록된다. Release 표시에는 Annotated Tag가 적합하다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git tag <span class="nt">-a</span> v1.0.0 <span class="nt">-m</span> <span class="s2">"release v1.0.0"</span>
</code></pre></div></div>

<p>특정 Commit에 Tag를 붙이려면 Commit을 함께 지정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span>
git tag <span class="nt">-a</span> v0.9.0 &lt;commit&gt; <span class="nt">-m</span> <span class="s2">"release v0.9.0"</span>
</code></pre></div></div>

<p>Tag 목록과 대상을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git tag <span class="nt">--list</span>
git show v1.0.0
</code></pre></div></div>

<p>Local Tag를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git tag <span class="nt">--delete</span> v1.0.0
</code></pre></div></div>

<p>Tag는 기본 <code class="language-plaintext highlighter-rouge">git push</code>에 항상 포함되지 않는다. Remote에 특정 Tag를 올리거나 삭제하는 작업은 명시적으로 수행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin v1.0.0
git push origin <span class="nt">--delete</span> v1.0.0
</code></pre></div></div>

<p>SourceTree에서는 History에서 대상 Commit을 선택해 Tag를 만들 수 있다. 생성 후 Local Tag인지 Remote에도 Push된 Tag인지 구분해서 확인한다.</p>

<h2 id="5--version-비교">5 ) Version 비교</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">git diff</code>는 어떤 두 상태를 비교하는지에 따라 출력 범위가 달라진다.</p>

<table>
  <thead>
    <tr>
      <th>비교 대상</th>
      <th>명령</th>
      <th>확인하는 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Working Tree ↔ Staging Area</td>
      <td><code class="language-plaintext highlighter-rouge">git diff</code></td>
      <td>아직 Stage하지 않은 변경</td>
    </tr>
    <tr>
      <td>Staging Area ↔ <code class="language-plaintext highlighter-rouge">HEAD</code></td>
      <td><code class="language-plaintext highlighter-rouge">git diff --staged</code></td>
      <td>다음 Commit에 들어갈 변경</td>
    </tr>
    <tr>
      <td>Commit ↔ Commit</td>
      <td><code class="language-plaintext highlighter-rouge">git diff &lt;commit-a&gt; &lt;commit-b&gt;</code></td>
      <td>두 Commit의 Snapshot 차이</td>
    </tr>
    <tr>
      <td>Branch ↔ Branch</td>
      <td><code class="language-plaintext highlighter-rouge">git diff &lt;branch-a&gt;..&lt;branch-b&gt;</code></td>
      <td>두 Branch Tip의 Snapshot 차이</td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff
git diff <span class="nt">--staged</span>
git diff HEAD~1 HEAD
git diff main..feature/login
</code></pre></div></div>

<p>SourceTree에서는 Commit 두 개를 선택하여 변경 File과 Diff를 비교할 수 있다. 다중 선택에 사용하는 보조 Key는 운영체제에 따라 다르므로 선택된 두 Commit의 ID를 화면에서 확인한다.</p>

<h3 id="head-와-"><code class="language-plaintext highlighter-rouge">HEAD</code>, <code class="language-plaintext highlighter-rouge">^</code>와 <code class="language-plaintext highlighter-rouge">~</code></h3>

<p><code class="language-plaintext highlighter-rouge">HEAD</code>는 현재 Checkout한 위치를 가리킨다. Branch에 정상적으로 연결된 상태에서는 현재 Branch의 Tip Commit을 가리킨다.</p>

<table>
  <thead>
    <tr>
      <th>표현</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">HEAD</code></td>
      <td>현재 Checkout한 Commit</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">HEAD^</code></td>
      <td><code class="language-plaintext highlighter-rouge">HEAD</code>의 첫 번째 Parent</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">HEAD^2</code></td>
      <td>Merge Commit인 <code class="language-plaintext highlighter-rouge">HEAD</code>의 두 번째 Parent</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">HEAD~2</code></td>
      <td>첫 번째 Parent를 두 번 따라간 Commit</td>
    </tr>
  </tbody>
</table>

<p>일직선 History에서는 <code class="language-plaintext highlighter-rouge">HEAD^</code>와 <code class="language-plaintext highlighter-rouge">HEAD~1</code>이 같은 Commit을 가리킨다. Parent가 여러 개인 Merge Commit에서는 <code class="language-plaintext highlighter-rouge">^&lt;번호&gt;</code>로 어느 Parent를 선택하는지 지정할 수 있다.</p>

<h2 id="6--file-변경-복구">6 ) File 변경 복구</h2>

<hr />

<p>File 복구는 Staging Area만 되돌리는 작업과 Working Tree의 내용을 폐기하는 작업을 구분해야 한다.</p>

<h3 id="stage-취소">Stage 취소</h3>

<p>다음 명령은 File의 Staged 상태를 <code class="language-plaintext highlighter-rouge">HEAD</code> 기준으로 되돌린다. Working Tree의 수정 내용은 유지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore <span class="nt">--staged</span> &lt;file&gt;
git status
</code></pre></div></div>

<p>SourceTree에서는 Staged File을 Unstaged 영역으로 옮기는 동작에 해당한다.</p>

<h3 id="working-tree-변경-폐기">Working Tree 변경 폐기</h3>

<p>다음 명령은 Working Tree의 File을 Staging Area의 내용으로 복원한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git diff <span class="nt">--</span> &lt;file&gt;
git restore &lt;file&gt;
</code></pre></div></div>

<p>저장하지 않은 변경은 사라질 수 있다. 실행 전에 <code class="language-plaintext highlighter-rouge">git diff</code>를 확인하고 보존할 내용이 있으면 Commit이나 Stash로 저장한다.</p>

<p>특정 Commit의 File만 가져올 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore <span class="nt">--source</span><span class="o">=</span>&lt;commit&gt; <span class="nt">--</span> &lt;file&gt;
</code></pre></div></div>

<p>이 명령은 Branch 전체를 해당 Commit으로 이동시키지 않고 선택한 File의 Working Tree 내용만 바꾼다.</p>

<h2 id="7--commit-되돌리기">7 ) Commit 되돌리기</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">reset</code>과 <code class="language-plaintext highlighter-rouge">revert</code>는 결과가 비슷해 보이지만 History 처리 방식이 다르다.</p>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>현재 Branch Tip</th>
      <th>Staging Area</th>
      <th>Working Tree</th>
      <th>공유 Branch 사용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">reset --soft</code></td>
      <td>대상 Commit으로 이동</td>
      <td>유지</td>
      <td>유지</td>
      <td>History가 달라지므로 주의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">reset --mixed</code></td>
      <td>대상 Commit으로 이동</td>
      <td>대상 Commit 기준으로 초기화</td>
      <td>유지</td>
      <td>History가 달라지므로 주의</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">reset --hard</code></td>
      <td>대상 Commit으로 이동</td>
      <td>대상 Commit 기준으로 초기화</td>
      <td>대상 Commit 기준으로 변경</td>
      <td>저장하지 않은 변경 손실 가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">revert</code></td>
      <td>새 Commit 추가</td>
      <td>새 Commit 과정에 따라 반영</td>
      <td>취소 결과 반영</td>
      <td>기존 History를 보존하므로 적합</td>
    </tr>
  </tbody>
</table>

<h3 id="soft-reset">Soft Reset</h3>

<p>Commit만 취소하고 변경 내용을 Staging Area와 Working Tree에 유지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset <span class="nt">--soft</span> &lt;target-commit&gt;
git status
git diff <span class="nt">--staged</span>
</code></pre></div></div>

<h3 id="mixed-reset">Mixed Reset</h3>

<p>Option을 생략한 <code class="language-plaintext highlighter-rouge">git reset</code>은 기본적으로 Mixed Mode이다. Branch를 대상 Commit으로 옮기고 Staging Area를 초기화하지만 Working Tree 변경은 유지한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset &lt;target-commit&gt;
git status
git diff
</code></pre></div></div>

<h3 id="hard-reset">Hard Reset</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset <span class="nt">--hard</span> &lt;target-commit&gt;
</code></pre></div></div>

<p>Hard Reset은 Branch, Staging Area와 추적 중인 Working Tree File을 대상 Commit 상태에 맞춘다. Commit하지 않은 변경이 사라질 수 있으므로 다음 내용을 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff
git diff <span class="nt">--staged</span>
git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">-n</span> 10
</code></pre></div></div>

<p>필요하면 현재 위치를 임시 Branch로 보존한 뒤 Reset한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch backup-before-reset
</code></pre></div></div>

<h3 id="revert">Revert</h3>

<p><code class="language-plaintext highlighter-rouge">git revert</code>는 취소할 Commit의 반대 변경을 적용한 새 Commit을 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git revert &lt;commit-to-cancel&gt;
</code></pre></div></div>

<p>기존 Commit이 History에 남으므로 이미 Remote에 공유한 Branch에서 변경을 취소할 때 사용하기 쉽다. 충돌이 발생하면 File을 수정하고 Stage한 뒤 작업을 계속하거나 중단한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add &lt;resolved-file&gt;
git revert <span class="nt">--continue</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git revert <span class="nt">--abort</span>
</code></pre></div></div>

<h2 id="8--stash로-변경-임시-보관">8 ) Stash로 변경 임시 보관</h2>

<hr />

<p>Stash는 Commit하기 이른 Working Tree와 Staging Area의 변경을 임시로 저장하고 현재 Branch의 작업 공간을 정리할 때 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash push <span class="nt">-m</span> <span class="s2">"temporary work"</span>
git status
</code></pre></div></div>

<p>기본 Stash에는 추적 중인 File의 Staged·Unstaged 변경이 들어간다. Untracked File은 기본 대상이 아니며 필요할 때 <code class="language-plaintext highlighter-rouge">-u</code>를 명시한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash push <span class="nt">-u</span> <span class="nt">-m</span> <span class="s2">"include untracked files"</span>
</code></pre></div></div>

<p>저장된 목록과 변경 내용을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash list
git stash show <span class="nt">--patch</span> stash@<span class="o">{</span>0<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">apply</code>는 Stash를 적용한 뒤 목록에 남긴다. Stage 상태까지 복원하려면 <code class="language-plaintext highlighter-rouge">--index</code>를 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash apply stash@<span class="o">{</span>0<span class="o">}</span>
git stash apply <span class="nt">--index</span> stash@<span class="o">{</span>0<span class="o">}</span>
</code></pre></div></div>

<p>더 이상 필요하지 않은 Stash를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash drop stash@<span class="o">{</span>0<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">pop</code>은 적용에 성공하면 해당 Stash를 목록에서 제거한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash pop stash@<span class="o">{</span>0<span class="o">}</span>
</code></pre></div></div>

<p>Stash 적용 중에도 현재 Branch의 변경과 충돌할 수 있다. 적용 후 <code class="language-plaintext highlighter-rouge">git status</code>와 Diff를 확인한다.</p>

<h2 id="9--branch와-head">9 ) Branch와 <code class="language-plaintext highlighter-rouge">HEAD</code></h2>

<hr />

<blockquote>
  <p><strong>Branch</strong></p>

  <p>Commit을 가리키며 새 Commit이 만들어질 때 함께 이동하는 Reference이다.</p>
</blockquote>

<p>Branch는 Project File을 통째로 복사한 Directory가 아니다. 여러 작업 흐름이 서로 다른 Commit을 가리키도록 분기한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              D──E  feature/login
             /
A──B──C───────────  main
</code></pre></div></div>

<p>현재 Branch와 목록을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">--show-current</span>
git branch
</code></pre></div></div>

<p>Branch를 만들고 전환한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch feature/login
git switch feature/login
</code></pre></div></div>

<p>생성과 전환을 동시에 수행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch <span class="nt">-c</span> feature/payment
</code></pre></div></div>

<p>기존 환경에서는 다음 <code class="language-plaintext highlighter-rouge">checkout</code> 명령도 계속 볼 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout feature/login
git checkout <span class="nt">-b</span> feature/payment
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">checkout</code>은 Branch 전환과 File 복구를 모두 담당하는 오래된 범용 명령이다. 새 문서에서는 의도가 분명한 <code class="language-plaintext highlighter-rouge">switch</code>와 <code class="language-plaintext highlighter-rouge">restore</code>를 우선 사용한다.</p>

<p>특정 Commit을 직접 Checkout하면 <code class="language-plaintext highlighter-rouge">HEAD</code>가 Branch가 아닌 Commit을 가리키는 Detached HEAD 상태가 될 수 있다. 이 상태에서 만든 Commit을 보존하려면 해당 위치에서 Branch를 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch <span class="nt">-c</span> save-detached-work
</code></pre></div></div>

<h3 id="branch-삭제">Branch 삭제</h3>

<p>현재 Checkout한 Branch는 삭제할 수 없다. 다른 Branch로 이동한 뒤 병합이 끝난 Branch를 삭제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch main
git branch <span class="nt">-d</span> feature/login
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-d</code>는 병합되지 않은 Commit이 있으면 삭제를 거절한다. <code class="language-plaintext highlighter-rouge">-D</code>는 이를 무시하는 강제 삭제이므로 보존할 Commit이 없는지 확인하지 않고 사용하지 않는다.</p>

<h2 id="10--merge">10 ) Merge</h2>

<hr />

<p>Merge는 현재 Branch에 다른 Branch의 History를 통합한다. <code class="language-plaintext highlighter-rouge">feature/login</code>을 <code class="language-plaintext highlighter-rouge">main</code>에 합치려면 결과를 받을 <code class="language-plaintext highlighter-rouge">main</code>으로 먼저 이동한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch main
git merge feature/login
</code></pre></div></div>

<h3 id="fast-forward-merge">Fast-forward Merge</h3>

<p>Branch가 분기된 뒤 <code class="language-plaintext highlighter-rouge">main</code>에 새 Commit이 없다면 <code class="language-plaintext highlighter-rouge">main</code> Reference를 <code class="language-plaintext highlighter-rouge">feature/login</code>의 Tip까지 앞으로 이동할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>병합 전

A──B  main
    \
     C──D  feature/login

병합 후

A──B──C──D  main, feature/login
</code></pre></div></div>

<p>Fast-forward Merge는 별도 Merge Commit을 만들지 않아도 된다.</p>

<h3 id="merge-commit">Merge Commit</h3>

<p>두 Branch가 각각 새 Commit을 가진 경우 Git은 공통 조상을 기준으로 변경을 통합하고 Merge Commit을 만들 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>       C──D  feature/login
      /    \
A──B──E─────M  main
</code></pre></div></div>

<p>특정 Commit을 <code class="language-plaintext highlighter-rouge">git merge &lt;commit&gt;</code> 대상으로 지정할 수 있지만 해당 Commit 하나의 Patch만 적용하는 작업은 아니다. Git은 지정한 Commit까지 이어지는 History를 현재 Branch와 병합한다. 하나의 Commit 변경만 적용하려는 경우에는 목적을 확인한 뒤 <code class="language-plaintext highlighter-rouge">git cherry-pick &lt;commit&gt;</code>을 검토한다.</p>

<h3 id="merge-충돌">Merge 충돌</h3>

<p>서로 다른 Branch가 같은 File의 같은 영역을 다르게 수정하면 Git이 결과를 자동으로 결정하지 못할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div>

<p>충돌 File에는 다음과 같은 Marker가 생길 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
현재 Branch의 내용
=======
병합하는 Branch의 내용
&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/login
</code></pre></div></div>

<p>적용할 내용을 결정하고 Marker를 제거한 뒤 File을 Stage한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add &lt;resolved-file&gt;
git commit
</code></pre></div></div>

<p>Merge를 완료하지 않고 시작 전으로 돌아가려면 다음 명령을 사용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git merge <span class="nt">--abort</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ours</code>와 <code class="language-plaintext highlighter-rouge">theirs</code>는 현재 진행 중인 작업 종류와 방향에 따라 가리키는 쪽을 정확히 확인해야 한다. 이름만 보고 자동으로 한쪽 전체를 선택하지 않는다.</p>

<h2 id="11--rebase">11 ) Rebase</h2>

<hr />

<p>Rebase는 현재 Branch의 Commit을 새로운 Base 위에 다시 적용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>변경 전

A──B──C  main
    \
     D──E  feature/login

변경 후

A──B──C  main
       \
        D'──E'  feature/login
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">feature/login</code>을 최신 <code class="language-plaintext highlighter-rouge">main</code> 위로 옮긴다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch feature/login
git rebase main
</code></pre></div></div>

<p>Rebase는 <code class="language-plaintext highlighter-rouge">D</code>와 <code class="language-plaintext highlighter-rouge">E</code>를 그대로 이동하지 않는다. 같은 변경을 새 Parent 위에 적용하여 <code class="language-plaintext highlighter-rouge">D'</code>, <code class="language-plaintext highlighter-rouge">E'</code>라는 새 Commit을 만들기 때문에 Object ID가 달라진다.</p>

<p>충돌이 발생하면 File을 수정하고 Stage한 뒤 다음 Commit 적용을 계속한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git add &lt;resolved-file&gt;
git rebase <span class="nt">--continue</span>
</code></pre></div></div>

<p>현재 Rebase를 중단하고 시작 전 상태로 돌아간다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--abort</span>
</code></pre></div></div>

<p>이미 여러 사람이 사용하는 Remote Branch의 Commit을 Rebase하면 기존 History와 다른 Commit ID가 만들어진다. 공유하기 전의 개인 작업 Branch에서 사용하는 범위와 팀의 History 정책을 먼저 확인한다.</p>

<h2 id="12--non-fast-forward-push와-merge의-차이">12 ) Non-fast-forward Push와 Merge의 차이</h2>

<hr />

<p>Remote Branch에 Local이 가지고 있지 않은 Commit이 있으면 Push가 <code class="language-plaintext highlighter-rouge">non-fast-forward</code>로 거절될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Local main       A──B──L
                     
Remote main      A──B──R
</code></pre></div></div>

<p>이 오류는 Fast-forward Merge 기능의 실패가 아니다. Remote Branch를 Local Commit까지 단순 이동하면 <code class="language-plaintext highlighter-rouge">R</code>이 History에서 빠질 수 있으므로 Server가 Update를 거절한 것이다.</p>

<p>Remote 상태를 먼저 가져와 History를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<p>팀 정책에 따라 Remote 변경을 Merge하거나 Rebase한 뒤 충돌을 해결하고 다시 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git merge origin/main
</code></pre></div></div>

<p>또는 다음과 같이 현재 Commit을 Remote Branch 위에 다시 적용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase origin/main
</code></pre></div></div>

<p>원격 Repository 연결과 Push 전체 흐름은 <a href="/cloud-native-44-github-remote-workflow/">GitHub Remote Repository 작업 흐름</a>에서 다룬다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>SourceTree와 CLI는 같은 Git Repository와 상태를 다루므로 GUI 작업 뒤에도 Git 명령으로 결과를 확인할 수 있다.</p>
    </li>
    <li>
      <p>Tag는 Commit Object ID를 바꾸지 않고 특정 Version을 읽기 쉬운 이름으로 가리킨다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">git diff</code>는 Working Tree, Staging Area, Commit과 Branch 중 어떤 두 상태를 비교하는지 구분해야 한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">restore</code>는 File, <code class="language-plaintext highlighter-rouge">reset</code>은 Branch와 Index, <code class="language-plaintext highlighter-rouge">revert</code>는 기존 Commit을 취소하는 새 Commit을 다룬다.</p>
    </li>
    <li>
      <p>기본 Stash는 추적 중인 변경을 저장하며 Untracked File을 포함하려면 <code class="language-plaintext highlighter-rouge">-u</code>가 필요하다.</p>
    </li>
    <li>
      <p>Merge는 History를 통합하고 Rebase는 현재 Branch의 Commit을 새 Base 위에 다시 적용한다.</p>
    </li>
    <li>
      <p>Non-fast-forward Push 거절은 Remote에 Local이 가지지 않은 Commit이 있을 때 History 손실을 막기 위해 발생한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Git" /><summary type="html"><![CDATA[SourceTree와 Git CLI를 연결하여 Commit 조회, Tag, Diff, Restore, Reset, Revert, Stash, Merge와 Rebase를 다루는 방법]]></summary></entry><entry><title type="html">GitHub Remote Repository 작업 흐름</title><link href="https://hyn128.site/cloud-native-44-github-remote-workflow/" rel="alternate" type="text/html" title="GitHub Remote Repository 작업 흐름" /><published>2026-09-10T00:00:00+09:00</published><updated>2026-09-10T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-44-github-remote-workflow</id><content type="html" xml:base="https://hyn128.site/cloud-native-44-github-remote-workflow/"><![CDATA[<p><a href="/cloud-native-42-git-version-control-basics/">Version Control과 Git 기본 작업 흐름</a>에서 Local Repository의 구조를 정리했고 <a href="/cloud-native-43-git-history-branch/">Git 이력 관리와 Branch 작업</a>에서 Branch를 나누고 통합하는 방법을 다뤘다. 이 글에서는 Local Repository와 GitHub의 Remote Repository 사이에서 Commit과 Branch를 교환한다.</p>

<h2 id="1--github와-git">1 ) GitHub와 Git</h2>

<hr />

<p>Git은 Local에서 Commit과 Branch History를 관리하는 Version Control System이다. GitHub는 Git Repository를 Hosting하고 협업 기능을 제공하는 Service이다.</p>

<table>
  <thead>
    <tr>
      <th>GitHub 기능</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Repository Hosting</td>
      <td>Git Commit, Branch와 Tag 저장·공유</td>
    </tr>
    <tr>
      <td>Pull Request</td>
      <td>Branch 변경 검토와 병합</td>
    </tr>
    <tr>
      <td>Issues</td>
      <td>작업과 문제 추적</td>
    </tr>
    <tr>
      <td>Actions</td>
      <td>Build, Test와 배포 Workflow 실행</td>
    </tr>
    <tr>
      <td>Packages</td>
      <td>Package와 Container Image 저장</td>
    </tr>
    <tr>
      <td>Projects</td>
      <td>Issue와 Pull Request 기반 작업 관리</td>
    </tr>
    <tr>
      <td>Pages</td>
      <td>Repository Content 기반 정적 Site 배포</td>
    </tr>
  </tbody>
</table>

<p>GitHub의 무료 범위, Storage와 Actions 사용량은 Plan에 따라 달라질 수 있으므로 사용 시점의 공식 정책을 확인한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Local Repository
├── Working Tree
├── Staging Area
└── Local Branch와 Commit
          │
          │ fetch·pull·push
          ▼
GitHub Remote Repository
├── Remote Branch
├── Tag
└── Pull Request·Issue·Actions
</code></pre></div></div>

<h2 id="2--remote-repository-인증-방식">2 ) Remote Repository 인증 방식</h2>

<hr />

<p>Private Repository를 Clone하거나 Push할 때 GitHub는 사용자를 인증하고 Repository 권한을 확인한다. Remote URL 형식에 따라 HTTPS 또는 SSH 인증을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>인증 수단</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>HTTPS</td>
      <td>Personal Access Token과 Credential Helper</td>
      <td>HTTPS URL을 사용하며 운영체제 Credential Store와 연동 가능</td>
    </tr>
    <tr>
      <td>SSH</td>
      <td>SSH Private Key와 GitHub에 등록한 Public Key</td>
      <td>GitHub Password나 PAT를 Push마다 전달하지 않음</td>
    </tr>
    <tr>
      <td>GitHub CLI</td>
      <td><code class="language-plaintext highlighter-rouge">gh auth login</code></td>
      <td>Browser, HTTPS 또는 SSH 인증 설정을 안내하고 GitHub API도 함께 사용</td>
    </tr>
  </tbody>
</table>

<h3 id="https와-personal-access-token">HTTPS와 Personal Access Token</h3>

<p>GitHub의 HTTPS Git 작업에서 Account Password를 대신해 Personal Access Token을 사용할 수 있다. Token에는 필요한 Repository와 작업 범위만 부여하고 만료 기간을 설정한다.</p>

<p>Token 값을 다음 위치에 직접 작성하지 않는다.</p>

<ul>
  <li>
    <p>Markdown과 Source Code</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">git remote</code> URL</p>
  </li>
  <li>
    <p>Shell Script</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.gitconfig</code>의 평문 설정</p>
  </li>
  <li>
    <p>CI Log에 출력되는 명령 인자</p>
  </li>
</ul>

<p>현재 Credential Helper 설정을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--show-origin</span> <span class="nt">--get-all</span> credential.helper
</code></pre></div></div>

<p>Git Credential Manager, macOS Keychain 같은 Helper를 사용하면 Credential을 운영체제의 Credential Store와 연결할 수 있다. 설치 방식과 Helper 이름은 운영체제와 Git 배포판에 따라 다르므로 임의로 덮어쓰기 전에 기존 설정을 확인한다.</p>

<p>저장된 계정을 바꿀 때는 Git 설정만 반복해서 추가하지 않는다. 현재 Helper 또는 운영체제 Credential Manager에서 기존 GitHub Credential을 제거한 뒤 다시 인증한다.</p>

<h3 id="ssh-인증">SSH 인증</h3>

<p>SSH Remote는 다음 형식을 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git@github.com:&lt;owner&gt;/&lt;repository&gt;.git
</code></pre></div></div>

<p>Local Private Key는 외부에 공개하지 않고 대응하는 Public Key만 GitHub 계정이나 조직에 등록한다. SSH Key 생성과 공개키 등록 이유는 <a href="/12-remote-access/">원격 접속</a>의 SSH Key 절에서 다룬다.</p>

<p>GitHub SSH 연결을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-T</span> git@github.com
</code></pre></div></div>

<p>첫 연결에서는 Host Key Fingerprint를 GitHub 공식 문서의 값과 대조한 뒤 신뢰 여부를 결정한다.</p>

<h3 id="github-cli-인증">GitHub CLI 인증</h3>

<p>GitHub CLI를 사용한다면 대화형 Login 절차를 실행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh auth login
gh auth status
</code></pre></div></div>

<p>필요한 경우 현재 <code class="language-plaintext highlighter-rouge">gh</code> 인증을 Git Credential 설정에 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh auth setup-git
</code></pre></div></div>

<p>계정을 변경할 때는 현재 인증 상태를 확인하고 Logout한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh auth status
gh auth <span class="nb">logout
</span>gh auth login
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">gh</code> 인증과 SSH Agent에 올라간 Key는 별도 상태일 수 있다. 오류가 발생하면 Remote URL이 HTTPS인지 SSH인지 먼저 확인한다.</p>

<h2 id="3--올바른-remote-url">3 ) 올바른 Remote URL</h2>

<hr />

<p>GitHub Repository의 HTTPS와 SSH URL은 형식이 다르다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTPS
https://github.com/&lt;owner&gt;/&lt;repository&gt;.git

SSH
git@github.com:&lt;owner&gt;/&lt;repository&gt;.git
</code></pre></div></div>

<p>다음 주소는 HTTPS Scheme과 SSH 구분자를 섞었으므로 올바른 GitHub Remote URL이 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://github.com:&lt;owner&gt;/&lt;repository&gt;.git
</code></pre></div></div>

<p>현재 Repository의 Remote 이름과 URL을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
git remote get-url origin
</code></pre></div></div>

<p>Remote 이름은 URL 자체가 아니다. <code class="language-plaintext highlighter-rouge">origin</code>은 <code class="language-plaintext highlighter-rouge">git clone</code>이 기본으로 등록하는 관례적인 이름이며 원하는 이름으로 바꿀 수 있다.</p>

<h2 id="4--clone과-fork">4 ) Clone과 Fork</h2>

<hr />

<p>Clone과 Fork는 Repository의 복사 위치와 소유 관계가 다르다.</p>

<table>
  <thead>
    <tr>
      <th>작업</th>
      <th>생성 위치</th>
      <th>결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Clone</td>
      <td>Local Computer</td>
      <td>Remote Repository의 Local Repository와 Working Tree 생성</td>
    </tr>
    <tr>
      <td>Fork</td>
      <td>GitHub 계정</td>
      <td>원본과 별도로 관리되는 GitHub Remote Repository 생성</td>
    </tr>
  </tbody>
</table>

<h3 id="clone">Clone</h3>

<p>Repository를 현재 Directory 아래에 복제한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="se">\</span>
  https://github.com/&lt;owner&gt;/&lt;repository&gt;.git
</code></pre></div></div>

<p>Local Directory 이름을 직접 지정할 수 있다. 대상 Directory는 비어 있거나 존재하지 않아야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="se">\</span>
  https://github.com/&lt;owner&gt;/&lt;repository&gt;.git <span class="se">\</span>
  &lt;local-directory&gt;
</code></pre></div></div>

<p>Clone이 끝나면 등록된 Remote와 Branch를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> &lt;local-directory&gt;
git remote <span class="nt">-v</span>
git branch <span class="nt">--all</span>
git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">-n</span> 5
</code></pre></div></div>

<p>큰 공개 Repository를 예제로 그대로 Clone하면 Network와 Disk를 많이 사용할 수 있다. Git 명령 학습에는 직접 만든 작은 Repository나 전용 실습 Repository를 사용한다.</p>

<h3 id="fork">Fork</h3>

<p>Fork는 GitHub에서 원본 Repository를 자신의 계정이나 조직 아래에 복사하는 기능이다. 원본 Repository에 직접 Push 권한이 없어도 Fork에서 Branch를 Push하고 Pull Request를 만들 수 있다.</p>

<p>Fork를 Clone하면 일반적으로 자신의 Fork가 <code class="language-plaintext highlighter-rouge">origin</code>이 된다. 원본 Repository의 변경을 가져오려면 <code class="language-plaintext highlighter-rouge">upstream</code> Remote를 별도로 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add upstream <span class="se">\</span>
  https://github.com/&lt;original-owner&gt;/&lt;repository&gt;.git
git remote <span class="nt">-v</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>origin
└── 자신의 GitHub Fork

upstream
└── 원본 GitHub Repository
</code></pre></div></div>

<h2 id="5--remote-연결과-변경">5 ) Remote 연결과 변경</h2>

<hr />

<h3 id="remote-추가">Remote 추가</h3>

<p>기존 Local Repository에 GitHub Repository를 연결한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin <span class="se">\</span>
  https://github.com/&lt;owner&gt;/&lt;repository&gt;.git
git remote <span class="nt">-v</span>
</code></pre></div></div>

<p>같은 이름의 Remote가 이미 있으면 <code class="language-plaintext highlighter-rouge">remote origin already exists</code> 오류가 발생한다. 기존 URL을 확인하고 새 Remote가 필요한지 URL 변경이 필요한지 판단한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote get-url origin
</code></pre></div></div>

<h3 id="remote-url-변경">Remote URL 변경</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote set-url origin <span class="se">\</span>
  git@github.com:&lt;owner&gt;/&lt;repository&gt;.git
git remote <span class="nt">-v</span>
</code></pre></div></div>

<p>이 명령은 Local Repository의 연결 URL을 변경한다. GitHub Repository의 이름이나 소유자를 바꾸는 작업은 아니다.</p>

<h3 id="remote-이름-변경">Remote 이름 변경</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote rename &lt;old-name&gt; &lt;new-name&gt;
git remote <span class="nt">-v</span>
</code></pre></div></div>

<h3 id="remote-연결-제거">Remote 연결 제거</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote remove &lt;remote-name&gt;
git remote <span class="nt">-v</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git remote remove</code>는 Local <code class="language-plaintext highlighter-rouge">.git/config</code>의 Remote 연결을 제거한다. GitHub의 Remote Repository와 그 안의 Data는 삭제하지 않는다.</p>

<h2 id="6--fetch와-pull">6 ) Fetch와 Pull</h2>

<hr />

<p>Remote의 변경을 가져오는 명령은 Local Branch를 자동으로 바꾸는지에 따라 구분한다.</p>

<h3 id="fetch">Fetch</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
</code></pre></div></div>

<p>Fetch는 Remote의 새 Commit, Tag와 Branch Reference를 Local Repository로 가져온다. 현재 Working Tree와 Local Branch에는 자동으로 통합하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub origin/main
        │ git fetch origin
        ▼
Local origin/main 갱신
        │
        └── 현재 Local main은 그대로 유지
</code></pre></div></div>

<p>가져온 상태와 Local Branch를 비교한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
git diff main..origin/main
</code></pre></div></div>

<h3 id="pull">Pull</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull origin main
</code></pre></div></div>

<p>Pull은 먼저 Fetch한 뒤 가져온 변경을 현재 Branch에 통합한다. 기본 통합 방식은 Git 설정과 Option에 따라 Merge 또는 Rebase가 될 수 있다.</p>

<p>실행 전 현재 Branch와 Working Tree 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">--show-current</span>
git status
git fetch origin
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<p>통합 방식을 명시하면 의도가 분명해진다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Merge 방식</span>
git pull <span class="nt">--no-rebase</span> origin main

<span class="c"># Rebase 방식</span>
git pull <span class="nt">--rebase</span> origin main

<span class="c"># Fast-forward만 허용</span>
git pull <span class="nt">--ff-only</span> origin main
</code></pre></div></div>

<p>팀이 정한 History 정책과 현재 Branch의 공유 여부에 맞는 방식을 선택한다.</p>

<h2 id="7--local-repository를-github에-push">7 ) Local Repository를 GitHub에 Push</h2>

<hr />

<p>빈 GitHub Repository를 준비한 뒤 기존 Local Repository를 연결하는 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Local File 작성
      │ git add
      ▼
Staging Area
      │ git commit
      ▼
Local Branch
      │ git remote add
      ▼
Remote 연결
      │ git push -u
      ▼
GitHub Remote Branch
</code></pre></div></div>

<h3 id="local-repository-확인">Local Repository 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git branch <span class="nt">--show-current</span>
git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">-n</span> 5
</code></pre></div></div>

<p>아직 Commit이 없다면 File을 Stage하고 Commit한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add &lt;file&gt;
git diff <span class="nt">--staged</span>
git commit <span class="nt">-m</span> <span class="s2">"docs: add initial content"</span>
</code></pre></div></div>

<p>GitHub에서 만든 빈 Repository를 <code class="language-plaintext highlighter-rouge">origin</code>으로 등록한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin <span class="se">\</span>
  https://github.com/&lt;owner&gt;/&lt;repository&gt;.git
git remote <span class="nt">-v</span>
</code></pre></div></div>

<p>현재 Branch 이름을 확인한 뒤 같은 이름의 Remote Branch로 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">--show-current</span>
git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">-u</code> 또는 <code class="language-plaintext highlighter-rouge">--set-upstream</code>은 현재 Local Branch가 어느 Remote Branch를 추적할지 설정한다. 관계가 설정된 뒤에는 Remote와 Branch 인자를 생략할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
git pull <span class="nt">--ff-only</span>
</code></pre></div></div>

<p>새로운 Local Branch를 처음 Push할 때는 해당 Branch의 Upstream을 다시 설정할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch <span class="nt">-c</span> feature/login
git push <span class="nt">-u</span> origin feature/login
</code></pre></div></div>

<p>Local Repository의 초기 Branch가 항상 <code class="language-plaintext highlighter-rouge">master</code>이고 GitHub가 항상 <code class="language-plaintext highlighter-rouge">main</code>인 것은 아니다. 실제 이름을 확인하고 필요할 때만 Branch 이름을 변경한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">--show-current</span>
git branch <span class="nt">-M</span> main
</code></pre></div></div>

<p>공유 중인 Branch 이름을 변경하면 Remote 설정, Pull Request와 다른 작업자의 Local Branch에 영향을 줄 수 있다.</p>

<h2 id="8--sourcetree에서-remote-작업">8 ) SourceTree에서 Remote 작업</h2>

<hr />

<p>SourceTree의 Remote 작업도 같은 Git 상태를 변경한다.</p>

<table>
  <thead>
    <tr>
      <th>SourceTree 작업</th>
      <th>Git 동작</th>
      <th>실행 후 확인</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Clone</td>
      <td><code class="language-plaintext highlighter-rouge">git clone</code></td>
      <td><code class="language-plaintext highlighter-rouge">git remote -v</code></td>
    </tr>
    <tr>
      <td>Remote 추가</td>
      <td><code class="language-plaintext highlighter-rouge">git remote add</code></td>
      <td><code class="language-plaintext highlighter-rouge">git remote get-url</code></td>
    </tr>
    <tr>
      <td>Fetch</td>
      <td><code class="language-plaintext highlighter-rouge">git fetch</code></td>
      <td><code class="language-plaintext highlighter-rouge">git log --all --graph</code></td>
    </tr>
    <tr>
      <td>Pull</td>
      <td><code class="language-plaintext highlighter-rouge">git pull</code></td>
      <td><code class="language-plaintext highlighter-rouge">git status</code>, <code class="language-plaintext highlighter-rouge">git log</code></td>
    </tr>
    <tr>
      <td>Push</td>
      <td><code class="language-plaintext highlighter-rouge">git push</code></td>
      <td>Local·Remote Branch 관계</td>
    </tr>
  </tbody>
</table>

<p>Push 화면에서는 대상 Remote와 Branch, Tag 포함 여부를 확인한다. GUI에 저장된 계정과 Repository를 실제로 Push할 권한이 있는 GitHub 계정이 다르면 인증 오류가 발생할 수 있다.</p>

<p>SourceTree에서 작업한 뒤 CLI로 다음 상태를 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git remote <span class="nt">-v</span>
git branch <span class="nt">-vv</span>
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<h2 id="9--push-실패-진단">9 ) Push 실패 진단</h2>

<hr />

<p>오류 Message에서 인증, 권한, URL과 History 문제를 먼저 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>우선 확인할 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">non-fast-forward</code></td>
      <td>Remote에 Local이 가지지 않은 Commit이 있는지 확인</td>
    </tr>
    <tr>
      <td>HTTPS <code class="language-plaintext highlighter-rouge">403</code></td>
      <td>현재 계정의 Repository 쓰기 권한과 Token Scope</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Authentication failed</code></td>
      <td>PAT 만료·폐기 여부와 Credential Helper의 저장 계정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Permission denied (publickey)</code></td>
      <td>SSH Key, SSH Agent, GitHub Public Key 등록과 SSH URL</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Repository not found</code></td>
      <td>Owner·Repository 이름, Private Repository 접근 권한</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">remote origin already exists</code></td>
      <td>기존 <code class="language-plaintext highlighter-rouge">origin</code> URL과 추가하려는 URL 비교</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src refspec ... does not match any</code></td>
      <td>Local Commit 존재 여부와 Branch 이름</td>
    </tr>
  </tbody>
</table>

<h3 id="현재-연결-상태-확인">현재 연결 상태 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
git branch <span class="nt">--show-current</span>
git branch <span class="nt">-vv</span>
git status
</code></pre></div></div>

<h3 id="github-cli-인증-확인">GitHub CLI 인증 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh auth status
</code></pre></div></div>

<h3 id="ssh-인증-확인">SSH 인증 확인</h3>

<p>Remote가 SSH URL일 때 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-T</span> git@github.com
</code></pre></div></div>

<h3 id="remote-history-확인">Remote History 확인</h3>

<p>Non-fast-forward 오류가 발생하면 Force Push 전에 Remote History를 가져온다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<p>Remote 변경을 확인하지 않은 <code class="language-plaintext highlighter-rouge">git push --force</code>는 다른 작업자의 Commit을 Remote Branch에서 제외할 수 있다. History를 다시 써야 하는 명확한 이유와 팀 합의가 없다면 사용하지 않는다.</p>

<h2 id="10--credential-노출-대응">10 ) Credential 노출 대응</h2>

<hr />

<p>PAT, Password나 Private Key가 Source File, Commit 또는 화면 공유에 노출되면 문자열을 지우는 작업만으로 해결되지 않는다.</p>

<ol>
  <li>
    <p>GitHub에서 해당 Credential을 폐기한다.</p>
  </li>
  <li>
    <p>필요한 최소 권한과 만료 기간으로 새 Credential을 발급한다.</p>
  </li>
  <li>
    <p>Local Credential Helper와 CI Secret을 새 값으로 갱신한다.</p>
  </li>
  <li>
    <p>Repository에 Commit됐다면 Git History와 Remote Cache의 정리 범위를 확인한다.</p>
  </li>
  <li>
    <p>Audit Log와 관련 Service Log에서 의심스러운 사용 기록을 확인한다.</p>
  </li>
</ol>

<p>새 Token 값을 문서의 예제에 다시 넣지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;GITHUB_USERNAME&gt;
&lt;GITHUB_PAT&gt;
&lt;REPOSITORY_URL&gt;
</code></pre></div></div>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>GitHub는 Git Remote Repository를 Hosting하고 Pull Request, Issue와 자동화 기능을 제공한다.</p>
    </li>
    <li>
      <p>HTTPS는 PAT와 Credential Helper를, SSH는 Local Private Key와 GitHub에 등록한 Public Key를 사용한다.</p>
    </li>
    <li>
      <p>Clone은 Local Repository를 만들고 Fork는 GitHub 계정 아래에 별도 Remote Repository를 만든다.</p>
    </li>
    <li>
      <p>Fetch는 Remote 상태를 가져오고 Pull은 가져온 변경을 현재 Branch에 통합한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">git push -u</code>는 현재 Local Branch와 Remote Branch의 Upstream 관계를 설정한다.</p>
    </li>
    <li>
      <p>Push 실패 시 Force Push보다 Remote URL, 인증 계정, 권한, Branch와 Remote History를 먼저 확인한다.</p>
    </li>
    <li>
      <p>노출된 Credential은 문서에서 삭제한 뒤 GitHub에서 폐기하고 새로 발급해야 한다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Git" /><summary type="html"><![CDATA[GitHub 인증 방식과 Clone·Fork·Remote·Fetch·Pull·Push의 차이 및 Local Repository를 Remote와 연결하는 방법]]></summary></entry><entry><title type="html">Container Image Registry와 Kubernetes Image Pull</title><link href="https://hyn128.site/cloud-native-41-container-image-registry/" rel="alternate" type="text/html" title="Container Image Registry와 Kubernetes Image Pull" /><published>2026-09-09T00:00:00+09:00</published><updated>2026-09-09T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-41-container-image-registry</id><content type="html" xml:base="https://hyn128.site/cloud-native-41-container-image-registry/"><![CDATA[<p><a href="/cloud-native-10-docker-images/">Docker 이미지와 Union File System</a>에서는 Image 이름, Tag와 Docker Hub Push를 다뤘다. 이 글에서는 범위를 Registry 전체로 확장하여 Image가 저장되고 배포되는 과정과 Kubernetes Worker가 Registry에서 Image를 가져오는 과정을 연결한다.</p>

<h2 id="1--image-registry">1 ) Image Registry</h2>

<hr />

<blockquote>
  <p><strong>Image Registry</strong></p>

  <p>Container Image와 OCI Artifact를 이름과 Version별로 저장하고 Client의 Push와 Pull 요청에 응답하는 Service이다.</p>
</blockquote>

<p>Registry는 Image File 하나를 그대로 보관하는 단순 File Server가 아니다. Image Manifest, 여러 Layer와 설정 정보를 Content Digest로 관리하고 Repository와 Tag를 통해 이를 찾을 수 있게 한다.</p>

<p>Image의 전체 참조 형식은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>REGISTRY/NAMESPACE/REPOSITORY:TAG
REGISTRY/NAMESPACE/REPOSITORY@DIGEST
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>예시</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Registry</td>
      <td><code class="language-plaintext highlighter-rouge">docker.io</code></td>
      <td>Image를 제공하는 Registry 주소</td>
    </tr>
    <tr>
      <td>Namespace</td>
      <td><code class="language-plaintext highlighter-rouge">example-user</code></td>
      <td>사용자, 조직 또는 Project 구분</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td><code class="language-plaintext highlighter-rouge">python-app</code></td>
      <td>하나의 Application Image 계열</td>
    </tr>
    <tr>
      <td>Tag</td>
      <td><code class="language-plaintext highlighter-rouge">v1.0</code></td>
      <td>사람이 읽을 수 있는 Version 별칭</td>
    </tr>
    <tr>
      <td>Digest</td>
      <td><code class="language-plaintext highlighter-rouge">sha256:...</code></td>
      <td>Image Content를 식별하는 불변 Hash</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">docker.io/library/nginx:1.29</code>에서 <code class="language-plaintext highlighter-rouge">docker.io</code>는 Registry, <code class="language-plaintext highlighter-rouge">library</code>는 Namespace, <code class="language-plaintext highlighter-rouge">nginx</code>는 Repository, <code class="language-plaintext highlighter-rouge">1.29</code>는 Tag이다. Tag는 다른 Image를 가리키도록 바뀔 수 있지만 Digest는 같은 Content를 계속 가리킨다.</p>

<h2 id="2--public-registry와-private-registry">2 ) Public Registry와 Private Registry</h2>

<hr />

<p>Registry는 접근 범위와 운영 주체에 따라 구분할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>예시</th>
      <th>특징</th>
      <th>확인할 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public Registry Service</td>
      <td>Docker Hub, GHCR, Quay.io</td>
      <td>Internet에서 바로 사용하기 쉽고 공개 Image 생태계가 큼</td>
      <td>Rate Limit, 공개 범위, 계정 보안</td>
    </tr>
    <tr>
      <td>Cloud Registry Service</td>
      <td>Amazon ECR, Google Artifact Registry 등</td>
      <td>Cloud IAM, Audit와 CI/CD Service 연동</td>
      <td>Region, 권한, Network와 비용 정책</td>
    </tr>
    <tr>
      <td>자체 Private Registry</td>
      <td>CNCF Distribution, Harbor</td>
      <td>내부 Network와 보안 정책에 맞게 직접 운영</td>
      <td>TLS, 인증, Storage, Backup, 고가용성</td>
    </tr>
  </tbody>
</table>

<p>Private Registry가 반드시 사내 Server를 의미하는 것은 아니다. Docker Hub, GHCR와 Cloud Registry에서도 비공개 Repository를 사용할 수 있다. 반대로 자체 Registry를 실행해도 인증과 Network 접근 제어가 없다면 안전한 Private Registry라고 볼 수 없다.</p>

<p>Service의 무료 범위, 저장 용량과 과금 정책은 변경될 수 있으므로 Registry 선택 시 현재 공식 Plan을 확인한다.</p>

<h2 id="3--image-배포-흐름">3 ) Image 배포 흐름</h2>

<hr />

<p>Application을 Registry를 통해 배포하는 기본 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Code + Dockerfile
          │ docker build
          ▼
      Local Image
          │ docker tag
          ▼
Registry 주소가 포함된 Image 참조
          │ docker push
          ▼
        Registry
          │ docker pull
          ▼
Docker Host 또는 Kubernetes Worker
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>명령</th>
      <th>결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Build</td>
      <td><code class="language-plaintext highlighter-rouge">docker build</code></td>
      <td>Local Image Store에 Image 생성</td>
    </tr>
    <tr>
      <td>Tag</td>
      <td><code class="language-plaintext highlighter-rouge">docker tag</code></td>
      <td>같은 Image에 Registry용 참조 이름 추가</td>
    </tr>
    <tr>
      <td>Push</td>
      <td><code class="language-plaintext highlighter-rouge">docker push</code></td>
      <td>Registry에 Manifest와 Layer 업로드</td>
    </tr>
    <tr>
      <td>Pull</td>
      <td><code class="language-plaintext highlighter-rouge">docker pull</code></td>
      <td>Registry에서 Manifest와 필요한 Layer 다운로드</td>
    </tr>
  </tbody>
</table>

<p>Application Image를 만드는 방법은 <a href="/cloud-native-14-application-image/">애플리케이션별 Docker 이미지 빌드</a>에서 자세히 다룬다. 여기서는 다음 Image가 Local에 준비되어 있다고 가정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image <span class="nb">ls </span>my-python-app
</code></pre></div></div>

<h2 id="4--docker-hub에-image-배포">4 ) Docker Hub에 Image 배포</h2>

<hr />

<h3 id="로그인">로그인</h3>

<p>Docker Hub 계정 이름을 지정하고 로그인한다. Password 대신 Personal Access Token을 사용하는 경우에도 명령 인자에 Token을 직접 작성하지 않고 Prompt에 입력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker login <span class="nt">-u</span> &lt;docker-hub-username&gt;
</code></pre></div></div>

<p>로그인 결과는 일반적으로 사용자별 Docker 설정 File에 저장된다. 개인 Token을 Markdown, Shell Script, Git Repository나 화면에 표시되는 명령에 넣지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker info
</code></pre></div></div>

<h3 id="tag와-push">Tag와 Push</h3>

<p>Local Image에 Docker Hub Namespace가 포함된 이름을 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker tag my-python-app:latest <span class="se">\</span>
  &lt;docker-hub-username&gt;/python-app:v1.0
</code></pre></div></div>

<p>두 이름이 같은 Image ID를 가리키는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker image <span class="nb">ls</span> <span class="se">\</span>
  &lt;docker-hub-username&gt;/python-app:v1.0
</code></pre></div></div>

<p>Registry로 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push <span class="se">\</span>
  &lt;docker-hub-username&gt;/python-app:v1.0
</code></pre></div></div>

<p>Push가 끝나면 다른 Host 또는 Local Image를 사용하지 않는 격리된 환경에서 Pull하여 Registry 배포를 검증한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull <span class="se">\</span>
  &lt;docker-hub-username&gt;/python-app:v1.0
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">latest</code>는 자동으로 최신 Version을 찾는 기능이 아니라 이름이 <code class="language-plaintext highlighter-rouge">latest</code>인 Tag이다. 배포 Manifest에는 검증한 Version Tag를 사용하고 더 강한 재현성이 필요하면 Digest를 고정한다.</p>

<h3 id="public과-private-repository">Public과 Private Repository</h3>

<p>Public Repository의 Image는 일반적으로 인증 없이 Pull할 수 있다. Private Repository는 Registry가 발급한 Credential이 필요하며 Docker Hub의 제공 범위와 비용 정책은 현재 Plan을 확인해야 한다.</p>

<p>로그인하지 않은 상태에서 Private Image Pull이 실패하면 다음 항목을 확인한다.</p>

<ul>
  <li>
    <p>Image 이름의 Namespace와 Repository가 정확한지 확인한다.</p>
  </li>
  <li>
    <p>계정 또는 Token에 Repository를 읽을 권한이 있는지 확인한다.</p>
  </li>
  <li>
    <p>Token이 만료되거나 폐기되지 않았는지 확인한다.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">docker logout</code> 후 올바른 계정으로 다시 로그인했는지 확인한다.</p>
  </li>
</ul>

<h2 id="5--kubernetes의-image-pull">5 ) Kubernetes의 Image Pull</h2>

<hr />

<p>Kubernetes는 Registry에 Image를 Push하지 않는다. 관리자가 Deployment를 생성하면 Control Plane이 Pod 배치를 결정하고, 선택된 Worker의 kubelet이 Container Runtime을 통해 Image를 Pull한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>관리 Client
    │ Deployment 제출
    ▼
API Server
    │
    ├── Deployment·ReplicaSet Controller가 Pod 생성
    │
    └── Scheduler가 실행할 Worker 결정
                         │
                         ▼
                    Worker kubelet
                         │ CRI 요청
                         ▼
                       containerd
                         │ Image Pull
                         ▼
                       Registry
                         │ Layer 저장
                         ▼
                    Container 실행
</code></pre></div></div>

<h3 id="public-image를-사용하는-deployment">Public Image를 사용하는 Deployment</h3>

<p>다음 내용을 <code class="language-plaintext highlighter-rouge">flask-deployment.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">flask-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">flask-web</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">flask-web</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">flask-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">&lt;docker-hub-username&gt;/python-app:v1.0</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
              <span class="na">containerPort</span><span class="pi">:</span> <span class="m">5000</span>
</code></pre></div></div>

<p>Service는 세 Pod의 Port <code class="language-plaintext highlighter-rouge">5000</code>으로 요청을 전달한다. 다음 내용을 <code class="language-plaintext highlighter-rouge">flask-service.yaml</code>로 저장한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">flask-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">type</span><span class="pi">:</span> <span class="s">ClusterIP</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">flask-web</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http</span>
      <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
      <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">targetPort</span><span class="pi">:</span> <span class="m">5000</span>
</code></pre></div></div>

<p>Control Plane에 접근 가능한 관리 Client에서 적용한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> flask-deployment.yaml
kubectl apply <span class="nt">-f</span> flask-service.yaml
kubectl rollout status deployment/flask-deployment
kubectl get pods <span class="nt">-o</span> wide
</code></pre></div></div>

<p>Ingress를 연결하기 전에 Port Forwarding으로 Service와 Pod 응답을 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward service/flask-service 8080:8080
</code></pre></div></div>

<p>다른 Terminal에서 요청한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://127.0.0.1:8080
</code></pre></div></div>

<p>Application Log와 Pod Event를 함께 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl logs deployment/flask-deployment <span class="nt">--all-pods</span><span class="o">=</span><span class="nb">true
</span>kubectl describe pod &lt;pod-name&gt;
kubectl get events <span class="se">\</span>
  <span class="nt">--sort-by</span><span class="o">=</span>.metadata.creationTimestamp
</code></pre></div></div>

<p>외부 Host 기반 Routing은 <a href="/cloud-native-33-kubernetes-ingress-routing/">Kubernetes Ingress와 HTTP Routing</a>에서 이어서 다룬다. 새 Ingress Manifest는 Legacy Annotation 대신 <code class="language-plaintext highlighter-rouge">spec.ingressClassName</code>을 사용한다.</p>

<h3 id="private-image와-imagepullsecrets">Private Image와 <code class="language-plaintext highlighter-rouge">imagePullSecrets</code></h3>

<p>Private Repository를 사용하는 Pod는 같은 Namespace의 Registry Credential Secret을 참조한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spec</span><span class="pi">:</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">imagePullSecrets</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">regcred</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">flask-container</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">&lt;docker-hub-username&gt;/private-python-app:v1.0</span>
</code></pre></div></div>

<p>동작 관계는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pod의 imagePullSecrets
        │
        ▼
Worker kubelet이 같은 Namespace의 Secret 확인
        │
        ▼
CRI Image Pull 요청에 Registry 인증 정보 전달
        │
        ▼
containerd가 Private Registry에서 Image Pull
</code></pre></div></div>

<p>Secret 생성과 안전한 Credential 관리 방법은 <a href="/cloud-native-34-kubernetes-env-secret-configmap/">Kubernetes 환경 변수, Secret과 ConfigMap</a>의 Private Registry Secret 절에서 다룬다.</p>

<p><code class="language-plaintext highlighter-rouge">ImagePullBackOff</code>가 발생하면 Application Log보다 Pod Event를 먼저 확인한다. Container가 시작되기 전 Image Pull 단계에서 실패하면 Application Log가 존재하지 않을 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod &lt;pod-name&gt;
kubectl get pod &lt;pod-name&gt; <span class="se">\</span>
  <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.spec.containers[*].image}{"\n"}'</span>
</code></pre></div></div>

<h2 id="6--자체-registry-구성">6 ) 자체 Registry 구성</h2>

<hr />

<p>CNCF Distribution은 OCI Distribution API를 제공하는 가벼운 Registry 구현이다. Local File System을 기본 Storage로 사용할 수 있고 필요에 따라 외부 Object Storage와 인증 구성을 연결할 수 있다.</p>

<h3 id="격리된-실습용-registry-실행">격리된 실습용 Registry 실행</h3>

<p>다음 구성은 <code class="language-plaintext highlighter-rouge">localhost</code> 또는 신뢰할 수 있는 폐쇄형 실습 Network에서 Registry API를 확인하기 위한 예제이다. 외부에 공개할 운영 구성으로 사용하지 않는다.</p>

<p>Registry Data를 Container 삭제와 분리하기 위해 Named Volume을 먼저 생성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker volume create registry-data
</code></pre></div></div>

<p>Registry Container를 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">--name</span> registry <span class="se">\</span>
  <span class="nt">--restart</span><span class="o">=</span>always <span class="se">\</span>
  <span class="nt">-p</span> 5000:5000 <span class="se">\</span>
  <span class="nt">-v</span> registry-data:/var/lib/registry <span class="se">\</span>
  registry:3
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-p 5000:5000</code></td>
      <td>Host의 TCP <code class="language-plaintext highlighter-rouge">5000</code>을 Registry API에 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--restart=always</code></td>
      <td>Docker Daemon 재시작 후 Container 재실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v registry-data:/var/lib/registry</code></td>
      <td>Registry Manifest와 Layer를 Volume에 저장</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">registry:3</code></td>
      <td>CNCF Distribution 3.x Image 사용</td>
    </tr>
  </tbody>
</table>

<p>Container 상태, API 응답과 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker ps <span class="nt">--filter</span> <span class="nv">name</span><span class="o">=</span>registry
curl <span class="nt">-i</span> http://127.0.0.1:5000/v2/
docker logs registry
</code></pre></div></div>

<p>인증이 없는 Local Registry의 <code class="language-plaintext highlighter-rouge">/v2/</code>가 정상 동작하면 빈 JSON Object인 <code class="language-plaintext highlighter-rouge">{}</code>가 반환될 수 있다. 인증을 적용한 Registry에서는 <code class="language-plaintext highlighter-rouge">401 Unauthorized</code>와 인증 Challenge가 정상 상태일 수 있다.</p>

<h3 id="local-registry에-push">Local Registry에 Push</h3>

<p>Image에 Registry 주소가 포함된 Tag를 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker tag my-python-app:latest <span class="se">\</span>
  127.0.0.1:5000/python-app:v1
</code></pre></div></div>

<p>Push한 뒤 Registry Catalog와 Tag를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push 127.0.0.1:5000/python-app:v1
curl http://127.0.0.1:5000/v2/_catalog
curl http://127.0.0.1:5000/v2/python-app/tags/list
</code></pre></div></div>

<p>Registry Container를 재시작하고 Image를 다시 Pull하여 Volume의 Data가 유지되는지 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker restart registry
docker pull 127.0.0.1:5000/python-app:v1
docker logs <span class="nt">--tail</span> 50 registry
</code></pre></div></div>

<h2 id="7--http-registry와-tls-registry">7 ) HTTP Registry와 TLS Registry</h2>

<hr />

<p>Docker와 containerd는 Registry 통신에서 HTTPS를 기본으로 기대한다. Plain HTTP Registry는 Traffic과 Credential을 보호하지 못하므로 격리된 실습 환경에서만 제한적으로 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>환경</th>
      <th>통신 방식</th>
      <th>요구 사항</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Local 단일 Host 실습</td>
      <td><code class="language-plaintext highlighter-rouge">http://127.0.0.1:5000</code></td>
      <td>외부 접근 차단, 실제 Credential 사용 금지</td>
    </tr>
    <tr>
      <td>폐쇄형 다중 Node 실습</td>
      <td>임시 HTTP 또는 내부 CA 기반 HTTPS</td>
      <td>접근 가능한 Node 제한, 사용 목적과 위험 명시</td>
    </tr>
    <tr>
      <td>운영 환경</td>
      <td>신뢰 가능한 TLS 기반 HTTPS</td>
      <td>인증, 권한, 영구 Storage, Backup, Monitoring</td>
    </tr>
  </tbody>
</table>

<p>운영 Registry는 다음 항목을 함께 설계한다.</p>

<ul>
  <li>
    <p>DNS Name과 Server 인증서의 Subject Alternative Name이 일치해야 한다.</p>
  </li>
  <li>
    <p>Registry를 사용하는 모든 Client가 인증서 발급 CA를 신뢰해야 한다.</p>
  </li>
  <li>
    <p>Repository별 Push와 Pull 권한을 분리한다.</p>
  </li>
  <li>
    <p>Registry Metadata와 Blob Storage를 영구 저장하고 Backup한다.</p>
  </li>
  <li>
    <p>Disk 사용량, 오류율, 인증 실패와 취약점 Scan 결과를 Monitoring한다.</p>
  </li>
</ul>

<h3 id="docker-engine의-실습용-http-허용">Docker Engine의 실습용 HTTP 허용</h3>

<p>Docker Engine으로 Remote HTTP Registry에 Push하거나 Pull해야 하는 격리된 실습 환경에서는 <code class="language-plaintext highlighter-rouge">/etc/docker/daemon.json</code>의 <code class="language-plaintext highlighter-rouge">insecure-registries</code>에 정확한 Host와 Port를 등록할 수 있다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"insecure-registries"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"192.168.0.100:5000"</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>기존 <code class="language-plaintext highlighter-rouge">daemon.json</code>에 다른 설정이 있다면 File 전체를 덮어쓰지 않고 JSON Object 안에 항목을 병합한다. 문법을 확인한 뒤 Docker를 재시작한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart docker
docker info
</code></pre></div></div>

<p>이 설정은 해당 Docker Daemon의 동작만 바꾼다. Kubernetes Worker가 containerd를 사용한다면 Docker 설정만으로 kubelet의 Image Pull이 허용되지 않는다.</p>

<h2 id="8--kubernetes-worker의-containerd-registry-설정">8 ) Kubernetes Worker의 containerd Registry 설정</h2>

<hr />

<p>Registry 설정은 실제 Image를 Pull할 가능성이 있는 모든 Worker에 필요하다. Control Plane Node에도 Workload Scheduling을 허용했다면 해당 Node 역시 같은 설정 대상이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Control Plane
└── Pod를 어느 Worker에 배치할지 결정

Worker
├── kubelet이 PodSpec의 Image 참조 확인
├── containerd에 Pull 요청
└── Worker Local Storage에 Image Layer 저장
</code></pre></div></div>

<h3 id="hoststoml-작성"><code class="language-plaintext highlighter-rouge">hosts.toml</code> 작성</h3>

<p>각 Worker에서 Registry Host와 Port에 해당하는 Directory를 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> <span class="se">\</span>
  /etc/containerd/certs.d/192.168.0.100:5000
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">/etc/containerd/certs.d/192.168.0.100:5000/hosts.toml</code>에 격리된 HTTP 실습 Registry를 등록한다.</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">server</span> <span class="p">=</span> <span class="s">"http://192.168.0.100:5000"</span>

<span class="nn">[host."http://192.168.0.100:5000"]</span>
  <span class="py">capabilities</span> <span class="p">=</span> <span class="p">[</span><span class="s">"pull"</span><span class="p">,</span> <span class="s">"resolve"</span><span class="p">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">skip_verify</code>는 HTTPS 인증서 검증을 건너뛰는 Option이다. Plain HTTP를 지정하는 설정에 습관적으로 추가하지 않는다. 운영 환경에서는 <code class="language-plaintext highlighter-rouge">http://</code> 대신 <code class="language-plaintext highlighter-rouge">https://</code>를 사용하고 필요한 CA 인증서를 배포한다.</p>

<h3 id="containerd-1x의-config_path">containerd 1.x의 <code class="language-plaintext highlighter-rouge">config_path</code></h3>

<p><code class="language-plaintext highlighter-rouge">/etc/containerd/config.toml</code>이 Version 2 형식이고 containerd 1.x를 사용한다면 다음 Section을 확인한다.</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">version</span> <span class="p">=</span> <span class="mi">2</span>

<span class="nn">[plugins."io.containerd.grpc.v1.cri".registry]</span>
  <span class="py">config_path</span> <span class="p">=</span> <span class="s">"/etc/containerd/certs.d"</span>
</code></pre></div></div>

<h3 id="containerd-2x의-config_path">containerd 2.x의 <code class="language-plaintext highlighter-rouge">config_path</code></h3>

<p>containerd 2.x의 Version 3 설정에서는 Plugin 경로가 다르다.</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">version</span> <span class="p">=</span> <span class="mi">3</span>

<span class="nn">[plugins."io.containerd.cri.v1.images".registry]</span>
  <span class="py">config_path</span> <span class="p">=</span> <span class="s">"/etc/containerd/certs.d"</span>
</code></pre></div></div>

<p>기존 설정의 <code class="language-plaintext highlighter-rouge">version</code>과 Plugin Section을 먼저 확인하고 서로 다른 Version 예제를 한 File에 동시에 넣지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>containerd <span class="nt">--version</span>
<span class="nb">sudo </span>containerd config dump | less
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">config_path</code>를 처음 활성화하도록 <code class="language-plaintext highlighter-rouge">config.toml</code>을 변경했다면 containerd를 재시작하고 상태와 Log를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart containerd
<span class="nb">sudo </span>systemctl status containerd <span class="nt">--no-pager</span>
<span class="nb">sudo </span>journalctl <span class="nt">-u</span> containerd <span class="nt">-n</span> 100 <span class="nt">--no-pager</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">config_path</code>가 이미 설정된 환경에서 Registry별 <code class="language-plaintext highlighter-rouge">hosts.toml</code>만 변경하는 경우에는 containerd가 해당 Directory의 변경을 읽으므로 일반적으로 Daemon 재시작이 필요하지 않다.</p>

<p>각 Worker에서 CRI 경로를 직접 검증한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>crictl pull <span class="se">\</span>
  192.168.0.100:5000/python-app:v1
<span class="nb">sudo </span>crictl images
</code></pre></div></div>

<p>그다음 Control Plane에 접근 가능한 관리 Client에서 Pod를 다시 생성하거나 Deployment Rollout을 수행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout restart deployment/flask-deployment
kubectl rollout status deployment/flask-deployment
kubectl get pods <span class="nt">-o</span> wide
</code></pre></div></div>

<h3 id="deprecated-registry-설정">Deprecated Registry 설정</h3>

<p>다음과 같이 CRI Plugin 아래에 <code class="language-plaintext highlighter-rouge">registry.mirrors</code>와 <code class="language-plaintext highlighter-rouge">registry.configs</code>를 직접 작성하는 방식은 Deprecated 상태이다.</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[plugins."io.containerd.grpc.v1.cri".registry.mirrors."192.168.0.100:5000"]</span>
  <span class="py">endpoint</span> <span class="p">=</span> <span class="nn">["http://192.168.0.100:5000"]</span>
</code></pre></div></div>

<p>기존 환경을 확인할 때는 볼 수 있지만 신규 구성은 <code class="language-plaintext highlighter-rouge">config_path</code>와 Registry별 <code class="language-plaintext highlighter-rouge">hosts.toml</code>을 사용한다.</p>

<h2 id="9--legacy-registry-web-ui">9 ) Legacy Registry Web UI</h2>

<hr />

<p>Registry API 자체는 Image 검색과 권한 관리를 위한 완성된 Web Portal을 제공하지 않는다. 과거에는 <code class="language-plaintext highlighter-rouge">hyper/docker-registry-web</code> Container를 <code class="language-plaintext highlighter-rouge">--link</code>로 Registry에 연결하는 예제가 사용되었다.</p>

<p>이 구성은 다음 이유로 신규 환경에 그대로 적용하지 않는다.</p>

<ul>
  <li>
    <p>Docker의 Legacy Container Link에 의존한다.</p>
  </li>
  <li>
    <p>오래된 별도 Image의 유지보수와 보안 상태를 추가로 검증해야 한다.</p>
  </li>
  <li>
    <p>인증, 권한, 취약점 Scan과 Project 관리가 Registry와 분리된다.</p>
  </li>
</ul>

<p>단순 API 확인은 <code class="language-plaintext highlighter-rouge">/v2/</code>, <code class="language-plaintext highlighter-rouge">/v2/_catalog</code>와 <code class="language-plaintext highlighter-rouge">/tags/list</code>를 사용하고 관리 UI와 보안 기능이 필요하면 Harbor 같은 Registry Platform을 검토한다.</p>

<h2 id="10--harbor">10 ) Harbor</h2>

<hr />

<p>Harbor는 OCI 호환 Registry를 기반으로 내부 Image와 Artifact를 관리하는 Open Source Registry Platform이다.</p>

<table>
  <thead>
    <tr>
      <th>기능</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Project</td>
      <td>Repository와 접근 범위를 조직 단위로 구분</td>
    </tr>
    <tr>
      <td>RBAC</td>
      <td>사용자와 Robot Account의 Push·Pull 권한 관리</td>
    </tr>
    <tr>
      <td>Web UI</td>
      <td>Repository, Artifact, Tag와 Scan 결과 확인</td>
    </tr>
    <tr>
      <td>Vulnerability Scan</td>
      <td>저장된 Artifact의 알려진 취약점 검사</td>
    </tr>
    <tr>
      <td>Replication</td>
      <td>다른 Registry와 Artifact 복제</td>
    </tr>
    <tr>
      <td>OCI Artifact</td>
      <td>Container Image 외 Helm Chart 등의 Artifact 저장</td>
    </tr>
  </tbody>
</table>

<p>CNCF Distribution이 Registry API의 가벼운 기반을 제공한다면 Harbor는 조직에서 필요한 사용자 관리, 보안 검사와 운영 UI를 함께 제공한다. 그 대신 구성 요소와 운영 부담이 더 크므로 단일 개발 Host의 단순 저장소에는 Distribution이 적합할 수 있고, 여러 사용자와 Project를 관리해야 하는 환경에는 Harbor가 적합할 수 있다.</p>

<p>Helm Chart도 OCI Artifact로 Registry에 저장할 수 있다. Chart Package의 실제 Push와 Pull 과정은 <a href="/cloud-native-40-kubernetes-helm/">Kubernetes Helm Chart와 Release 관리</a>에서 이어서 다룬다.</p>

<h2 id="11--registry-문제-진단-순서">11 ) Registry 문제 진단 순서</h2>

<hr />

<p>Image Pull 실패는 Application 실행 이전 단계의 문제이다. 다음 순서로 범위를 좁힌다.</p>

<ol>
  <li>
    <p>Image 참조의 Registry, Namespace, Repository와 Tag가 정확한지 확인한다.</p>
  </li>
  <li>
    <p>실행 주체가 Public Image인지 Private Image인지 확인한다.</p>
  </li>
  <li>
    <p>Registry API와 TCP Port에 접근할 수 있는지 확인한다.</p>
  </li>
  <li>
    <p>TLS 인증서와 Client의 CA 신뢰 상태를 확인한다.</p>
  </li>
  <li>
    <p>Docker 또는 containerd 중 실제 Pull을 수행하는 Runtime 설정을 확인한다.</p>
  </li>
  <li>
    <p>Kubernetes에서는 Secret의 Namespace와 <code class="language-plaintext highlighter-rouge">imagePullSecrets</code> 이름을 확인한다.</p>
  </li>
  <li>
    <p>Pod Event, containerd Log와 Registry Log의 같은 시간대 요청을 대조한다.</p>
  </li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Registry Host</span>
docker logs <span class="nt">--since</span> 10m registry

<span class="c"># Worker</span>
<span class="nb">sudo </span>journalctl <span class="nt">-u</span> containerd <span class="nt">--since</span> <span class="s2">"10 minutes ago"</span>

<span class="c"># Control Plane에 접근 가능한 관리 Client</span>
kubectl describe pod &lt;pod-name&gt;
kubectl get events <span class="se">\</span>
  <span class="nt">--sort-by</span><span class="o">=</span>.metadata.creationTimestamp
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>증상</th>
      <th>우선 확인 대상</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ImagePullBackOff</code></td>
      <td>직전 Pull 실패 원인을 보여주는 Pod Event</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unauthorized</code></td>
      <td>Registry Credential과 Repository 권한</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">manifest unknown</code></td>
      <td>Repository, Tag와 Platform Manifest</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">x509: certificate signed by unknown authority</code></td>
      <td>Worker의 CA Trust와 Registry 인증서 Chain</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">http: server gave HTTP response to HTTPS client</code></td>
      <td>HTTP Registry 허용 설정과 실제 Protocol</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">connection refused</code></td>
      <td>Registry Process, Listen Port, Firewall와 주소</td>
    </tr>
  </tbody>
</table>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Registry는 Image Manifest와 Layer를 Repository, Tag와 Digest로 관리하고 Push와 Pull API를 제공한다.</p>
    </li>
    <li>
      <p>Kubernetes에서는 Scheduler가 Worker를 선택하고 해당 Worker의 kubelet과 containerd가 Registry에서 Image를 Pull한다.</p>
    </li>
    <li>
      <p>Private Image는 같은 Namespace의 <code class="language-plaintext highlighter-rouge">imagePullSecrets</code>와 Registry 읽기 권한이 필요하다.</p>
    </li>
    <li>
      <p>Docker Engine의 Registry 설정과 Kubernetes Worker의 containerd Registry 설정은 서로 다른 대상이다.</p>
    </li>
    <li>
      <p>Plain HTTP와 인증서 검증 생략은 격리된 실습에만 제한하고 운영 Registry에는 TLS, 인증, 권한과 영구 Storage를 구성한다.</p>
    </li>
    <li>
      <p>containerd의 Legacy <code class="language-plaintext highlighter-rouge">registry.mirrors</code> 방식은 Deprecated 상태이며 신규 구성은 <code class="language-plaintext highlighter-rouge">config_path</code>와 <code class="language-plaintext highlighter-rouge">hosts.toml</code>을 사용한다.</p>
    </li>
    <li>
      <p>Harbor는 OCI Registry에 Project, RBAC, Web UI, 취약점 Scan과 Replication을 결합한 Platform이다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Docker" /><summary type="html"><![CDATA[Public·Private Image Registry의 구조와 Docker Hub 배포, 자체 Registry 구성, Kubernetes Worker의 Image Pull 및 Harbor의 역할]]></summary></entry><entry><title type="html">Version Control과 Git 기본 작업 흐름</title><link href="https://hyn128.site/cloud-native-42-git-version-control-basics/" rel="alternate" type="text/html" title="Version Control과 Git 기본 작업 흐름" /><published>2026-09-09T00:00:00+09:00</published><updated>2026-09-09T00:00:00+09:00</updated><id>https://hyn128.site/cloud-native-42-git-version-control-basics</id><content type="html" xml:base="https://hyn128.site/cloud-native-42-git-version-control-basics/"><![CDATA[<p>Container Image, Kubernetes Manifest와 Helm Chart도 Source Code와 함께 계속 변경된다. Git을 사용하면 이러한 File의 변경 이력을 기록하고, 특정 시점의 상태를 다시 확인하며, 여러 사람이 변경 내용을 교환하고 병합할 수 있다.</p>

<p>이 글에서 Local Repository와 Commit의 기본 구조를 익힌 뒤 <a href="/cloud-native-43-git-history-branch/">Git 이력 관리와 Branch 작업</a>에서 이력 비교, 복구와 Branch 통합을 다룬다. Remote Repository와의 작업은 <a href="/cloud-native-44-github-remote-workflow/">GitHub Remote Repository 작업 흐름</a>으로 이어진다.</p>

<h2 id="1--version-control-system">1 ) Version Control System</h2>

<hr />

<blockquote>
  <p><strong>Version Control System, VCS</strong></p>

  <p>File의 변경 내용을 시간 순서대로 기록하여 특정 Version을 확인하거나 복원하고 여러 작업자의 변경을 조정하는 System이다.</p>
</blockquote>

<p>Version 관리 대상은 Source Code로 제한되지 않는다. Text로 표현할 수 있는 설정 File, 문서, Kubernetes Manifest와 Script도 관리할 수 있다. Binary File도 저장할 수 있지만 작은 변경에도 File 전체가 크게 달라질 수 있어 저장 공간과 변경 비교 측면에서 별도 관리 전략이 필요하다.</p>

<p>Version 관리로 해결하려는 주요 문제는 다음과 같다.</p>

<ul>
  <li>
    <p>누가 언제 어떤 내용을 변경했는지 추적한다.</p>
  </li>
  <li>
    <p>현재 상태와 이전 상태의 차이를 비교한다.</p>
  </li>
  <li>
    <p>문제가 생긴 변경의 범위를 찾고 필요한 Version으로 복원한다.</p>
  </li>
  <li>
    <p>여러 작업자가 만든 변경을 하나의 History로 병합한다.</p>
  </li>
  <li>
    <p>Local 작업과 Server의 공유 상태를 분리한다.</p>
  </li>
</ul>

<h2 id="2--version-control-system의-종류">2 ) Version Control System의 종류</h2>

<hr />

<p>Version Control System은 History를 저장하고 공유하는 구조에 따라 Local, Centralized와 Distributed 방식으로 구분할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>History 저장 위치</th>
      <th>협업 방식</th>
      <th>특징</th>
      <th>대표 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Local VCS</td>
      <td>한 Computer의 Local Database</td>
      <td>File을 별도 수단으로 전달</td>
      <td>개인 변경 추적에는 사용할 수 있지만 중앙 협업 기능이 부족</td>
      <td>RCS</td>
    </tr>
    <tr>
      <td>Centralized VCS</td>
      <td>중앙 Server</td>
      <td>Client가 Server에서 Checkout·Commit</td>
      <td>권한과 History를 중앙에서 관리하지만 Server 의존도가 큼</td>
      <td>CVS, Subversion, Perforce</td>
    </tr>
    <tr>
      <td>Distributed VCS</td>
      <td>각 Clone에 Repository History 저장</td>
      <td>Remote Repository와 Fetch·Push</td>
      <td>Network 연결 없이도 Local Commit과 History 조회 가능</td>
      <td>Git, Mercurial</td>
    </tr>
  </tbody>
</table>

<h3 id="local-version-control">Local Version Control</h3>

<p>Local VCS는 같은 Computer 안의 Database에 Revision이나 Patch를 기록한다. 수동으로 날짜별 Directory를 복사하는 것보다 변경 이력을 체계적으로 관리할 수 있지만 다른 작업자와 History를 공유하고 병합하기에는 적합하지 않다.</p>

<p>RCS는 대표적인 Local Version Control System이다. 특정 운영체제나 개발 도구 설치 여부만으로 항상 제공된다고 가정하지 않고 현재 환경에서 Package 존재 여부를 확인한다.</p>

<h3 id="centralized-version-control">Centralized Version Control</h3>

<p>Centralized VCS는 중앙 Server가 기준 Repository를 관리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client A ─┐
          ├── Central VCS Server
Client B ─┘
</code></pre></div></div>

<p>모든 사용자가 같은 중앙 History를 기준으로 작업하기 쉬운 반면 Server에 접속할 수 없으면 Update나 Commit 같은 주요 작업이 제한될 수 있다. Server 장애가 곧바로 모든 Local 작업 File의 소실을 의미하지는 않지만 중앙 Repository의 Backup과 가용성이 중요하다.</p>

<h3 id="distributed-version-control">Distributed Version Control</h3>

<p>Distributed VCS의 Clone에는 Working File만 아니라 Repository History와 Metadata가 함께 저장된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer A Local Repository
              │ fetch·push
              ▼
        Remote Repository
              ▲
              │ fetch·push
Developer B Local Repository
</code></pre></div></div>

<p>각 작업자는 Network가 없어도 Local Commit, Branch 생성과 History 조회를 수행할 수 있다. <code class="language-plaintext highlighter-rouge">checkout</code>할 때마다 Repository 전체를 다시 Backup하는 방식은 아니다. 일반적으로 <code class="language-plaintext highlighter-rouge">clone</code>이 Repository를 처음 가져오고, 이후 <code class="language-plaintext highlighter-rouge">fetch</code>가 Remote의 새로운 Object와 Reference를 전달하며, <code class="language-plaintext highlighter-rouge">checkout</code> 또는 <code class="language-plaintext highlighter-rouge">switch</code>는 가지고 있는 Object를 이용해 Working Tree의 상태를 바꾼다.</p>

<h2 id="3--git">3 ) Git</h2>

<hr />

<p>Git은 File 변경을 추적하고 여러 작업자의 변경을 병합할 수 있는 Open Source Distributed Version Control System이다. Linux Kernel 개발을 지원하기 위해 2005년에 Linus Torvalds를 중심으로 처음 개발되었다.</p>

<p>Git의 주요 특징은 다음과 같다.</p>

<ul>
  <li>
    <p>대부분의 작업을 Local Repository에서 수행할 수 있다.</p>
  </li>
  <li>
    <p>Commit 단위로 변경 이력과 작성자 정보를 추적한다.</p>
  </li>
  <li>
    <p>Branch를 이용해 서로 다른 작업 흐름을 분리한다.</p>
  </li>
  <li>
    <p>Remote Repository를 통해 다른 작업자와 History를 교환한다.</p>
  </li>
  <li>
    <p>같은 기반에서 갈라진 변경을 Merge하거나 Rebase할 수 있다.</p>
  </li>
</ul>

<p>Git은 GitHub와 같은 Service의 이름이 아니다. Git은 Local에서 실행하는 Version Control 도구이고 GitHub, GitLab과 Bitbucket은 Git Repository Hosting과 협업 기능을 제공하는 Service이다.</p>

<h2 id="4--git-hosting-service">4 ) Git Hosting Service</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>Service</th>
      <th>Git 이외의 주요 기능</th>
      <th>자체 설치 선택지</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>GitHub</td>
      <td>Pull Request, Issues, Actions, Packages, Pages와 Project 기능</td>
      <td>GitHub Enterprise Server</td>
    </tr>
    <tr>
      <td>GitLab</td>
      <td>Merge Request, Issues, CI/CD, Package·Container Registry</td>
      <td>GitLab Self-Managed</td>
    </tr>
    <tr>
      <td>Bitbucket</td>
      <td>Pull Request, Pipeline과 Jira 등 Atlassian 제품 연동</td>
      <td>제공 형태와 Plan 확인 필요</td>
    </tr>
  </tbody>
</table>

<p>Hosting Service의 무료 사용자 수, Private Repository 범위, CI 실행 시간과 Storage 한도는 변경될 수 있다. 문서에 특정 수치를 고정하지 않고 사용할 시점의 공식 Plan과 조직 정책을 확인한다.</p>

<h3 id="remote-repository와-local-repository">Remote Repository와 Local Repository</h3>

<blockquote>
  <p><strong>Local Repository</strong></p>

  <p>현재 Computer의 <code class="language-plaintext highlighter-rouge">.git</code> Directory에 저장된 Git Object, Reference와 설정이다.</p>
</blockquote>

<blockquote>
  <p><strong>Remote Repository</strong></p>

  <p>다른 Repository와 History를 교환하기 위해 이름과 URL로 등록한 Repository이다.</p>
</blockquote>

<p>Remote Repository가 반드시 Cloud Service에 있어야 하는 것은 아니다. 접근 가능한 Server의 Bare Repository도 Remote로 사용할 수 있다. <code class="language-plaintext highlighter-rouge">origin</code>은 특별한 Protocol이 아니라 <code class="language-plaintext highlighter-rouge">git clone</code>이 기본적으로 등록하는 Remote 이름이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>
</code></pre></div></div>

<h2 id="5--git-설치와-version-확인">5 ) Git 설치와 Version 확인</h2>

<hr />

<p>Git 공식 배포 경로나 운영체제의 Package Manager를 사용하여 설치한다. Linux 배포판에서는 일반적으로 <code class="language-plaintext highlighter-rouge">git</code> Package로 제공된다.</p>

<p>설치된 Git Version을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nt">--version</span>
</code></pre></div></div>

<p>GitHub 기능을 Terminal에서 사용해야 한다면 GitHub CLI인 <code class="language-plaintext highlighter-rouge">gh</code>를 별도로 설치할 수 있다. <code class="language-plaintext highlighter-rouge">gh</code>는 Git 자체를 대체하지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh <span class="nt">--version</span>
</code></pre></div></div>

<p>GUI Client가 필요하면 SourceTree 같은 도구를 사용할 수 있다. GUI에서 Stage, Commit과 Push를 실행해도 내부적으로 다루는 Git Repository와 기본 개념은 같다.</p>

<h2 id="6--사용자-정보-설정">6 ) 사용자 정보 설정</h2>

<hr />

<p>Git Commit에는 작성자 이름과 Email이 Metadata로 기록된다. 이 값은 Hosting Service Login 정보와 자동으로 같아지는 것이 아니므로 직접 설정을 확인한다.</p>

<p>모든 Local Repository의 기본값으로 사용할 이름과 Email을 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name <span class="s2">"&lt;name&gt;"</span>
git config <span class="nt">--global</span> user.email <span class="s2">"&lt;email&gt;"</span>
</code></pre></div></div>

<p>현재 Repository에서만 다른 값을 사용하려면 <code class="language-plaintext highlighter-rouge">--global</code>을 제외한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config user.name <span class="s2">"&lt;project-name&gt;"</span>
git config user.email <span class="s2">"&lt;project-email&gt;"</span>
</code></pre></div></div>

<p>설정 값을 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config user.name
git config user.email
git config <span class="nt">--list</span> <span class="nt">--show-origin</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>범위</th>
      <th>Option</th>
      <th>일반적인 저장 위치</th>
      <th>우선순위</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>System</td>
      <td><code class="language-plaintext highlighter-rouge">--system</code></td>
      <td>System Git 설정 File</td>
      <td>낮음</td>
    </tr>
    <tr>
      <td>사용자</td>
      <td><code class="language-plaintext highlighter-rouge">--global</code></td>
      <td>사용자 Git 설정 File</td>
      <td>중간</td>
    </tr>
    <tr>
      <td>현재 Repository</td>
      <td><code class="language-plaintext highlighter-rouge">--local</code> 또는 생략</td>
      <td><code class="language-plaintext highlighter-rouge">.git/config</code></td>
      <td>높음</td>
    </tr>
  </tbody>
</table>

<p>같은 Key가 여러 범위에 있으면 더 구체적인 Repository 설정이 사용자 기본값보다 우선한다.</p>

<h2 id="7--초기-branch-이름">7 ) 초기 Branch 이름</h2>

<hr />

<p>Git이 새 Repository에 사용할 초기 Branch 이름은 Git Version, 배포판 설정과 사용자 설정에 따라 달라질 수 있다. Hosting Service가 새 Remote Repository에 선택하는 기본 Branch 이름과 Local <code class="language-plaintext highlighter-rouge">git init</code>의 결과도 항상 같다고 가정할 수 없다.</p>

<p>새 Local Repository의 초기 Branch 이름을 <code class="language-plaintext highlighter-rouge">main</code>으로 통일하려면 다음과 같이 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> init.defaultBranch main
</code></pre></div></div>

<p>특정 Repository를 만들 때 직접 지정할 수도 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init <span class="nt">--initial-branch</span><span class="o">=</span>main
</code></pre></div></div>

<p>이미 Commit이 있는 Repository의 Branch 이름을 바꾸는 작업은 Remote Branch와 협업자에게 영향을 줄 수 있으므로 단순 초기 설정과 구분한다.</p>

<h2 id="8--git의-세-작업-영역">8 ) Git의 세 작업 영역</h2>

<hr />

<p>Git의 기본 흐름을 이해하려면 Working Tree, Staging Area와 Local Repository를 구분해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Working Tree
    │ git add
    ▼
Staging Area(Index)
    │ git commit
    ▼
Local Repository(.git)
    │ git push
    ▼
Remote Repository
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>저장되는 내용</th>
      <th>주요 확인 명령</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Working Tree</td>
      <td>사용자가 현재 편집하는 File</td>
      <td><code class="language-plaintext highlighter-rouge">git status</code>, <code class="language-plaintext highlighter-rouge">git diff</code></td>
    </tr>
    <tr>
      <td>Staging Area</td>
      <td>다음 Commit에 넣기로 선택한 내용</td>
      <td><code class="language-plaintext highlighter-rouge">git diff --staged</code></td>
    </tr>
    <tr>
      <td>Local Repository</td>
      <td>Commit, Tree, Blob와 Reference 등 Git Object</td>
      <td><code class="language-plaintext highlighter-rouge">git log</code>, <code class="language-plaintext highlighter-rouge">git show</code></td>
    </tr>
    <tr>
      <td>Remote Repository</td>
      <td>다른 Repository와 교환한 Commit과 Branch Reference</td>
      <td><code class="language-plaintext highlighter-rouge">git remote -v</code>, <code class="language-plaintext highlighter-rouge">git branch -r</code></td>
    </tr>
  </tbody>
</table>

<h3 id="working-tree">Working Tree</h3>

<p>Working Tree는 특정 Commit에서 Checkout한 File을 실제로 편집하는 공간이다. 일반 Repository에서는 <code class="language-plaintext highlighter-rouge">.git</code> Directory를 제외한 Project File 영역으로 볼 수 있다.</p>

<h3 id="staging-area">Staging Area</h3>

<p>Staging Area는 다음 Commit에 포함할 내용을 선택하는 영역이며 Index라고도 한다. File을 단순히 다른 Directory로 이동시키는 것이 아니라 다음 Commit의 Snapshot에 포함할 내용을 Git Index에 기록한다.</p>

<h3 id="local-repository">Local Repository</h3>

<p><code class="language-plaintext highlighter-rouge">.git</code> Directory에는 Git Object Database, Branch와 Tag Reference, Repository 설정 등이 저장된다. <code class="language-plaintext highlighter-rouge">.git</code>을 삭제하면 Working File이 남아 있더라도 해당 Local Repository의 History와 설정을 잃을 수 있다.</p>

<h2 id="9--file-상태">9 ) File 상태</h2>

<hr />

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>의미</th>
      <th>다음에 주로 사용하는 명령</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Untracked</td>
      <td>Working Tree에는 있지만 현재 Commit이 추적하지 않는 File</td>
      <td><code class="language-plaintext highlighter-rouge">git add</code> 또는 <code class="language-plaintext highlighter-rouge">.gitignore</code> 등록</td>
    </tr>
    <tr>
      <td>Modified</td>
      <td>추적 중인 File이 Index 또는 현재 Commit과 달라진 상태</td>
      <td><code class="language-plaintext highlighter-rouge">git diff</code>, <code class="language-plaintext highlighter-rouge">git add</code></td>
    </tr>
    <tr>
      <td>Staged</td>
      <td>다음 Commit에 포함할 내용이 Index에 기록된 상태</td>
      <td><code class="language-plaintext highlighter-rouge">git diff --staged</code>, <code class="language-plaintext highlighter-rouge">git commit</code></td>
    </tr>
    <tr>
      <td>Committed</td>
      <td>내용이 Local Repository의 Commit에 기록된 상태</td>
      <td><code class="language-plaintext highlighter-rouge">git log</code>, <code class="language-plaintext highlighter-rouge">git show</code></td>
    </tr>
  </tbody>
</table>

<p>하나의 File이 항상 한 상태만 가지는 것은 아니다. File을 Stage한 후 다시 수정하면 같은 File에 Staged 변경과 아직 Stage하지 않은 변경이 동시에 존재할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff
git diff <span class="nt">--staged</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git status</code>는 상태를 요약하고 <code class="language-plaintext highlighter-rouge">git diff</code>는 Working Tree와 Index의 차이, <code class="language-plaintext highlighter-rouge">git diff --staged</code>는 Index와 현재 Commit의 차이를 보여준다.</p>

<h3 id="gitignore"><code class="language-plaintext highlighter-rouge">.gitignore</code></h3>

<p><code class="language-plaintext highlighter-rouge">.gitignore</code>는 Git이 추적하지 않을 File과 Directory Pattern을 기록한다. Build 결과, Package 설치 Directory, 가상 환경과 Local Credential처럼 Repository에 포함할 필요가 없는 항목을 제외할 때 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Python
.venv/
__pycache__/
*.pyc

# Node.js
node_modules/

# Java·Spring
.gradle/
build/
target/

# Local 환경과 Credential
.env
*.key
</code></pre></div></div>

<p>Project 구성원이 공통으로 제외할 Pattern은 Repository의 <code class="language-plaintext highlighter-rouge">.gitignore</code>에 기록하고 함께 Commit한다. 개인 Editor의 임시 File처럼 Project에 공유할 필요가 없는 Pattern은 Repository의 <code class="language-plaintext highlighter-rouge">.git/info/exclude</code>나 사용자 전역 Ignore File에서 관리할 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">.gitignore</code>는 Untracked File에 적용된다. 이미 Commit했거나 Stage하여 Git이 추적 중인 File은 Pattern을 추가해도 자동으로 추적 대상에서 빠지지 않는다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git check-ignore <span class="nt">-v</span> &lt;file&gt;
git ls-files &lt;file&gt;
</code></pre></div></div>

<p>추적 중인 File을 Working Tree에는 남겨두고 이후 Commit 대상에서 제외하려면 먼저 변경 범위와 Repository 정책을 확인한 뒤 Index에서 제거한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nb">rm</span> <span class="nt">--cached</span> &lt;file&gt;
git status
git diff <span class="nt">--staged</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git rm --cached</code> 결과는 다음 Commit에 반영되는 변경이다. Directory 전체나 넓은 Pattern에 실행하기 전에 <code class="language-plaintext highlighter-rouge">git status</code>와 <code class="language-plaintext highlighter-rouge">git diff --staged</code>로 제거 대상을 확인한다. 이미 History에 들어간 Password나 Token은 <code class="language-plaintext highlighter-rouge">.gitignore</code>로 제거되지 않으므로 Credential을 폐기하고 History 정리 필요 여부를 별도로 판단해야 한다.</p>

<h2 id="10--local-repository-기본-실습">10 ) Local Repository 기본 실습</h2>

<hr />

<h3 id="repository-생성">Repository 생성</h3>

<p>새 실습 Directory를 만들고 이동한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>git-basic-lab
<span class="nb">cd </span>git-basic-lab
git init <span class="nt">--initial-branch</span><span class="o">=</span>main
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">.git</code> Directory와 현재 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
<span class="nb">ls</span> <span class="nt">-la</span>
</code></pre></div></div>

<h3 id="untracked-file-생성">Untracked File 생성</h3>

<p><code class="language-plaintext highlighter-rouge">README.md</code>를 작성한 뒤 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch </span>README.md
git status
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">README.md</code>는 아직 Commit이 추적하지 않는 Untracked 상태이다.</p>

<h3 id="staging-area에-등록">Staging Area에 등록</h3>

<p>다음 Commit에 포함할 File을 선택한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add README.md
git status
git diff <span class="nt">--staged</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git add</code>는 File을 Repository History에 즉시 기록하지 않는다. 현재 내용을 Staging Area에 올려 다음 Commit의 입력으로 만든다.</p>

<h3 id="첫-commit-생성">첫 Commit 생성</h3>

<p>Editor를 열어 Commit Message를 작성한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit
</code></pre></div></div>

<p>짧은 실습에서는 <code class="language-plaintext highlighter-rouge">-m</code>으로 제목을 지정할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"docs: add project README"</span>
</code></pre></div></div>

<p>Commit과 Working Tree 상태를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span>
git status
</code></pre></div></div>

<h3 id="수정-후-두-번째-commit">수정 후 두 번째 Commit</h3>

<p><code class="language-plaintext highlighter-rouge">README.md</code>를 수정한 뒤 Working Tree와 현재 Commit의 차이를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
git diff
</code></pre></div></div>

<p>변경 내용을 Stage하고 실제로 Commit될 차이를 다시 검토한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add README.md
git diff <span class="nt">--staged</span>
git commit <span class="nt">-m</span> <span class="s2">"docs: explain project purpose"</span>
</code></pre></div></div>

<p>History를 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span>
</code></pre></div></div>

<h2 id="11--commit">11 ) Commit</h2>

<hr />

<p>Commit은 Staging Area에 선택한 Project 상태를 가리키는 Snapshot과 다음 Metadata를 Local Repository에 기록한다.</p>

<ul>
  <li>
    <p>작성자와 Committer 정보</p>
  </li>
  <li>
    <p>작성 시각</p>
  </li>
  <li>
    <p>Commit Message</p>
  </li>
  <li>
    <p>Project File과 Directory 구조를 가리키는 Tree</p>
  </li>
  <li>
    <p>이전 History를 연결하는 Parent Commit</p>
  </li>
</ul>

<p>Git은 File의 전체 복사본을 날짜별 Directory에 반복 저장하는 방식처럼 보이지 않는다. Content를 Object로 관리하고 같은 Content는 같은 Object를 재사용하며 Commit이 Project Snapshot을 가리키도록 구성한다.</p>

<h3 id="commit-message">Commit Message</h3>

<p>Commit Message는 변경한 결과뿐 아니라 변경 이유를 History에 남기는 정보이다. 여러 줄 Message는 다음 구조로 작성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>변경 내용을 요약한 제목

변경한 이유, 고려한 제약과 필요한 추가 설명
</code></pre></div></div>

<p>제목과 본문 사이를 빈 줄로 구분하면 <code class="language-plaintext highlighter-rouge">git log</code>, Hosting Service와 여러 Git 도구가 두 영역을 구분해서 표시할 수 있다.</p>

<h3 id="commit-object-id">Commit Object ID</h3>

<p>Commit은 Git Object ID로 식별된다. 일반적인 SHA-1 Repository에서는 40자리 16진수이고 SHA-256 형식의 Repository에서는 64자리 16진수이다. 화면에서는 식별에 필요한 앞부분만 축약하여 보여주는 경우가 많다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span>
git rev-parse HEAD
</code></pre></div></div>

<p>짧은 ID는 현재 Repository에서 다른 Object와 구분될 때 편리하게 사용할 수 있지만 고정 길이의 전역 Identifier라고 가정하지 않는다.</p>

<h2 id="12--remote-repository와-push">12 ) Remote Repository와 Push</h2>

<hr />

<p>Local Commit은 <code class="language-plaintext highlighter-rouge">git commit</code>만으로 Remote에 전송되지 않는다. Remote Repository URL을 등록하고 Push해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin &lt;remote-repository-url&gt;
git remote <span class="nt">-v</span>
</code></pre></div></div>

<p>현재 <code class="language-plaintext highlighter-rouge">main</code> Branch를 <code class="language-plaintext highlighter-rouge">origin</code>에 Push하고 Upstream 관계를 설정한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Working Tree 변경
      │ git add
      ▼
Staging Area
      │ git commit
      ▼
Local main Branch
      │ git push
      ▼
Remote의 main Branch
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">git push</code>는 Working Tree File을 바로 Server로 복사하는 명령이 아니다. Local Repository의 Commit과 Reference를 Remote와 교환하여 Remote Branch를 갱신한다.</p>

<p>Push가 거절되면 강제 Push부터 실행하지 않는다. Remote에 Local이 가지고 있지 않은 Commit이 있는지 먼저 확인한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git log <span class="nt">--oneline</span> <span class="nt">--graph</span> <span class="nt">--decorate</span> <span class="nt">--all</span>
</code></pre></div></div>

<h2 id="13--containerkubernetes-작업과-git">13 ) Container·Kubernetes 작업과 Git</h2>

<hr />

<p>Git Repository에는 Container Image Binary 자체보다 Image를 재현할 수 있는 입력을 저장한다.</p>

<table>
  <thead>
    <tr>
      <th>저장 대상</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Application Source</td>
      <td>Python, Java, Go와 JavaScript Source File</td>
    </tr>
    <tr>
      <td>Image Build 정의</td>
      <td><code class="language-plaintext highlighter-rouge">Dockerfile</code>, <code class="language-plaintext highlighter-rouge">.dockerignore</code></td>
    </tr>
    <tr>
      <td>Package 의존성</td>
      <td><code class="language-plaintext highlighter-rouge">requirements.txt</code>, <code class="language-plaintext highlighter-rouge">package.json</code>, Build 설정</td>
    </tr>
    <tr>
      <td>Kubernetes 설정</td>
      <td>Deployment, Service, ConfigMap Manifest</td>
    </tr>
    <tr>
      <td>배포 구성</td>
      <td>Kustomize Base·Overlay, Helm Chart와 Values</td>
    </tr>
    <tr>
      <td>자동화</td>
      <td>CI/CD Workflow와 Shell Script</td>
    </tr>
  </tbody>
</table>

<p>다음 정보는 Repository에 직접 Commit하지 않는다.</p>

<ul>
  <li>
    <p>Docker Hub Personal Access Token</p>
  </li>
  <li>
    <p>Private Key</p>
  </li>
  <li>
    <p>실제 Password가 들어 있는 Secret Manifest</p>
  </li>
  <li>
    <p>Cloud Access Key</p>
  </li>
  <li>
    <p>Local 개발 환경의 Credential File</p>
  </li>
</ul>

<p>Secret을 <code class="language-plaintext highlighter-rouge">.gitignore</code>에 넣는 것은 실수 방지 수단일 뿐 이미 Commit된 Secret을 History에서 자동으로 제거하지 않는다. Credential이 Commit되거나 외부에 노출됐다면 값을 삭제하는 것과 별개로 해당 Credential을 폐기하고 새로 발급해야 한다.</p>

<h2 id="전체-정리">전체 정리</h2>

<hr />

<blockquote>
  <p><strong>최종 정리</strong></p>

  <ul>
    <li>
      <p>Version Control System은 File의 변경 이력을 기록하고 특정 Version의 비교, 복원과 협업을 지원한다.</p>
    </li>
    <li>
      <p>Git은 각 Clone이 Local Repository와 History를 가지는 Distributed Version Control System이다.</p>
    </li>
    <li>
      <p>Git과 GitHub·GitLab·Bitbucket 같은 Hosting Service는 서로 다른 대상이다.</p>
    </li>
    <li>
      <p>Git의 기본 작업 영역은 Working Tree, Staging Area와 Local Repository이다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">git add</code>는 다음 Commit에 포함할 내용을 선택하고 <code class="language-plaintext highlighter-rouge">git commit</code>은 이를 Local Repository에 기록한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">git push</code>는 Local Commit과 Branch Reference를 Remote Repository로 전송한다.</p>
    </li>
    <li>
      <p><code class="language-plaintext highlighter-rouge">.gitignore</code>는 의도적으로 추적하지 않을 File을 지정하며 이미 추적 중인 File에는 자동으로 적용되지 않는다.</p>
    </li>
    <li>
      <p>초기 Branch 이름과 Object ID 길이는 Version과 Repository 형식에 따라 달라질 수 있으므로 고정된 값으로 단정하지 않는다.</p>
    </li>
    <li>
      <p>Container와 Kubernetes 작업에서는 재현 가능한 설정을 Git에 저장하고 Token, Password와 Private Key는 Commit하지 않는다.</p>
    </li>
  </ul>
</blockquote>]]></content><author><name></name></author><category term="CloudNative" /><category term="AutoEverSW" /><category term="Git" /><summary type="html"><![CDATA[Version Control System의 종류와 Git의 Working Tree·Staging Area·Repository 구조, File 상태 및 기본 Commit 흐름]]></summary></entry></feed>